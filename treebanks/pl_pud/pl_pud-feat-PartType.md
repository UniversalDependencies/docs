---
layout: base
title:  'Statistics of PartType in UD_Polish-PUD'
udver: '2'
---

## Treebank Statistics: UD_Polish-PUD: Features: `PartType`

This feature is language-specific.
It occurs with 2 different values: `Int`, `Mod`.

23 tokens (0%) have a non-empty value of `PartType`.
5 types (0%) occur at least once with a non-empty value of `PartType`.
3 lemmas (0%) occur at least once with a non-empty value of `PartType`.
The feature is used with 1 part-of-speech tags: <tt><a href="pl_pud-pos-PART.html">PART</a></tt> (23; 0% instances).

### `PART`

23 <tt><a href="pl_pud-pos-PART.html">PART</a></tt> tokens (6% of all `PART` tokens) have a non-empty value of `PartType`.

The most frequent other feature values with which `PART` and `PartType` co-occurred: <tt><a href="pl_pud-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (23; 100%).

`PART` tokens may have the following values of `PartType`:

* `Int` (14; 61% of non-empty `PartType`): <em>czy, Czyżby</em>
* `Mod` (9; 39% of non-empty `PartType`): <em>może</em>

