---
layout: base
title:  'Statistics of Polarity in UD_Maltese'
udver: '2'
---

## Treebank Statistics: UD_Maltese: Features: `Polarity`

This feature is universal.
It occurs with 1 different values: `Neg`.

10 tokens (0%) have a non-empty value of `Polarity`.
4 types (0%) occur at least once with a non-empty value of `Polarity`.
1 lemmas (0) occur at least once with a non-empty value of `Polarity`.
The feature is used with 1 part-of-speech tags: <tt><a href="mt-pos-PART.html">PART</a></tt> (10; 0% instances).

### `PART`

10 <tt><a href="mt-pos-PART.html">PART</a></tt> tokens (40% of all `PART` tokens) have a non-empty value of `Polarity`.

The most frequent other feature values with which `PART` and `Polarity` co-occurred: <tt><a href="mt-feat-Aspect.html">Aspect</a></tt><tt>=EMPTY</tt> (10; 100%).

`PART` tokens may have the following values of `Polarity`:

* `Neg` (10; 100% of non-empty `Polarity`): ma mhux dixx mhuwiex
* `EMPTY` (15): se qed

