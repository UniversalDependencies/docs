---
layout: base
title:  'Statistics of Style in UD_Czech-CAC'
udver: '2'
---

## Treebank Statistics: UD_Czech-CAC: Features: `Style`

This feature is language-specific.
It occurs with 6 different values: `Arch`, `Coll`, `Expr`, `Rare`, `Slng`, `Vrnc`.

453 tokens (0%) have a non-empty value of `Style`.
183 types (0%) occur at least once with a non-empty value of `Style`.
143 lemmas (1%) occur at least once with a non-empty value of `Style`.
The feature is used with 9 part-of-speech tags: [NOUN](cs_cac-pos-NOUN.html) (113; 0% instances), [PRON](cs_cac-pos-PRON.html) (111; 0% instances), [ADV](cs_cac-pos-ADV.html) (78; 0% instances), [VERB](cs_cac-pos-VERB.html) (53; 0% instances), [ADJ](cs_cac-pos-ADJ.html) (48; 0% instances), [AUX](cs_cac-pos-AUX.html) (35; 0% instances), [PROPN](cs_cac-pos-PROPN.html) (9; 0% instances), [DET](cs_cac-pos-DET.html) (3; 0% instances), [PART](cs_cac-pos-PART.html) (3; 0% instances).

### `NOUN`

113 [NOUN](cs_cac-pos-NOUN.html) tokens (0% of all `NOUN` tokens) have a non-empty value of `Style`.

The most frequent other feature values with which `NOUN` and `Style` co-occurred: <tt><a href="Polarity.html">Polarity</a>=Pos</tt> (113; 100%), <tt><a href="Number.html">Number</a>=Sing</tt> (73; 65%), <tt><a href="Animacy.html">Animacy</a>=EMPTY</tt> (66; 58%).

`NOUN` tokens may have the following values of `Style`:

* `Coll` (36; 32% of non-empty `Style`): pracech mejkap elpíčko pivčo pásák Dvacka Komunálu bétéček dolama fajnovka
* `Expr` (33; 29% of non-empty `Style`): ramínko vlákénka znaménka polínka prkének prkénka vlákének znaménkem jezírku polínko
* `Rare` (26; 23% of non-empty `Style`): folklorismu folklorismus styrén vládce vládcem dvoukolka folklorismem imanentismu imanentismus manažerismu
* `Slng` (17; 15% of non-empty `Style`): šňůry Moho šňůru knajpou kšeftu pochoďáku pětikilo textilačkami textilačky valchaře
* `Vrnc` (1; 1% of non-empty `Style`): ostrví

`Style` seems to be **lexical feature** of `NOUN`. 100% lemmas (54) occur only with one value of `Style`.

### `PRON`

111 [PRON](cs_cac-pos-PRON.html) tokens (1% of all `PRON` tokens) have a non-empty value of `Style`.

The most frequent other feature values with which `PRON` and `Style` co-occurred: <tt><a href="PrepCase.html">PrepCase</a>=EMPTY</tt> (111; 100%), <tt><a href="Reflex.html">Reflex</a>=EMPTY</tt> (111; 100%), <tt><a href="Variant.html">Variant</a>=EMPTY</tt> (111; 100%), <tt><a href="Gender.html">Gender</a>=Masc,Neut</tt> (104; 94%), <tt><a href="Case.html">Case</a>=Acc</tt> (104; 94%), <tt><a href="Person.html">Person</a>=3</tt> (104; 94%), <tt><a href="PronType.html">PronType</a>=Prs</tt> (104; 94%), <tt><a href="Number.html">Number</a>=Sing</tt> (104; 94%).

`PRON` tokens may have the following values of `Style`:

* `Arch` (111; 100% of non-empty `Style`): jej kdož

### `ADV`

78 [ADV](cs_cac-pos-ADV.html) tokens (0% of all `ADV` tokens) have a non-empty value of `Style`.

The most frequent other feature values with which `ADV` and `Style` co-occurred: <tt><a href="PronType.html">PronType</a>=EMPTY</tt> (74; 95%), <tt><a href="Polarity.html">Polarity</a>=Pos</tt> (56; 72%).

`ADV` tokens may have the following values of `Style`:

* `Arch` (60; 77% of non-empty `Style`): dosti dřív nejdřív tentokráte blížeji snáz tenkráte
* `Coll` (18; 23% of non-empty `Style`): loni akorát teda

### `VERB`

53 [VERB](cs_cac-pos-VERB.html) tokens (0% of all `VERB` tokens) have a non-empty value of `Style`.

The most frequent other feature values with which `VERB` and `Style` co-occurred: <tt><a href="Polarity.html">Polarity</a>=Pos</tt> (53; 100%), <tt><a href="Gender.html">Gender</a>=EMPTY</tt> (50; 94%), <tt><a href="Mood.html">Mood</a>=EMPTY</tt> (40; 75%), <tt><a href="Person.html">Person</a>=EMPTY</tt> (40; 75%), <tt><a href="Tense.html">Tense</a>=EMPTY</tt> (37; 70%), <tt><a href="Voice.html">Voice</a>=EMPTY</tt> (37; 70%), <tt><a href="VerbForm.html">VerbForm</a>=Inf</tt> (37; 70%), <tt><a href="Number.html">Number</a>=EMPTY</tt> (37; 70%), <tt><a href="Aspect.html">Aspect</a>=EMPTY</tt> (33; 62%).

`VERB` tokens may have the following values of `Style`:

* `Arch` (46; 87% of non-empty `Style`): býti jest dáti míti dodržeti existovati housti hýbati jeti nalézti
* `Coll` (5; 9% of non-empty `Style`): potřebujou tancujou tlačej zamuzicírovali študoval
* `Rare` (1; 2% of non-empty `Style`): objektivuje
* `Vrnc` (1; 2% of non-empty `Style`): zavází

`Style` seems to be **lexical feature** of `VERB`. 100% lemmas (32) occur only with one value of `Style`.

### `ADJ`

48 [ADJ](cs_cac-pos-ADJ.html) tokens (0% of all `ADJ` tokens) have a non-empty value of `Style`.

The most frequent other feature values with which `ADJ` and `Style` co-occurred: <tt><a href="Degree.html">Degree</a>=Pos</tt> (40; 83%), <tt><a href="Polarity.html">Polarity</a>=Pos</tt> (40; 83%), <tt><a href="Number.html">Number</a>=Sing</tt> (28; 58%).

`ADJ` tokens may have the following values of `Style`:

* `Coll` (27; 56% of non-empty `Style`): Žídkova Žídkových dolarovej drahej druhej fest hodný každej krásnejch mluvený
* `Expr` (1; 2% of non-empty `Style`): ultralevých
* `Rare` (16; 33% of non-empty `Style`): gnoseologických gnoseologickou gnoseologické gnoseologického gnoseologická gnoseologickém gnoseologickým nivelisační objektivované poslední
* `Slng` (4; 8% of non-empty `Style`): nabiflovaná vypikýrovaný zbrojovácké škodováckých

`Style` seems to be **lexical feature** of `ADJ`. 100% lemmas (33) occur only with one value of `Style`.

### `AUX`

35 [AUX](cs_cac-pos-AUX.html) tokens (0% of all `AUX` tokens) have a non-empty value of `Style`.

The most frequent other feature values with which `AUX` and `Style` co-occurred: <tt><a href="Gender.html">Gender</a>=EMPTY</tt> (35; 100%), <tt><a href="Polarity.html">Polarity</a>=Pos</tt> (35; 100%), <tt><a href="Person.html">Person</a>=EMPTY</tt> (33; 94%), <tt><a href="Tense.html">Tense</a>=EMPTY</tt> (33; 94%), <tt><a href="Voice.html">Voice</a>=EMPTY</tt> (33; 94%), <tt><a href="Mood.html">Mood</a>=EMPTY</tt> (33; 94%), <tt><a href="VerbForm.html">VerbForm</a>=Inf</tt> (33; 94%), <tt><a href="Number.html">Number</a>=EMPTY</tt> (33; 94%).

`AUX` tokens may have the following values of `Style`:

* `Arch` (34; 97% of non-empty `Style`): býti jest
* `Coll` (1; 3% of non-empty `Style`): seš

<table>
  <tr><th>Paradigm <i>být</i></th><th><tt>Arch</tt></th><th><tt>Coll</tt></th></tr>
  <tr><td><tt><a href="Mood.html">Mood</a>=Ind|<a href="Number.html">Number</a>=Sing|<a href="Person.html">Person</a>=2|<a href="Tense.html">Tense</a>=Pres|<a href="VerbForm.html">VerbForm</a>=Fin|<a href="Voice.html">Voice</a>=Act</tt></td><td></td><td>seš</td></tr>
  <tr><td><tt><a href="Mood.html">Mood</a>=Ind|<a href="Number.html">Number</a>=Sing|<a href="Person.html">Person</a>=3|<a href="Tense.html">Tense</a>=Pres|<a href="VerbForm.html">VerbForm</a>=Fin|<a href="Voice.html">Voice</a>=Act</tt></td><td>jest</td><td></td></tr>
  <tr><td><tt><a href="VerbForm.html">VerbForm</a>=Inf</tt></td><td>býti</td><td></td></tr>
</table>

### `PROPN`

9 [PROPN](cs_cac-pos-PROPN.html) tokens (0% of all `PROPN` tokens) have a non-empty value of `Style`.

The most frequent other feature values with which `PROPN` and `Style` co-occurred: <tt><a href="Abbr.html">Abbr</a>=EMPTY</tt> (9; 100%), <tt><a href="Polarity.html">Polarity</a>=Pos</tt> (9; 100%), <tt><a href="Number.html">Number</a>=Sing</tt> (7; 78%), <tt><a href="NameType.html">NameType</a>=Giv</tt> (5; 56%), <tt><a href="Animacy.html">Animacy</a>=EMPTY</tt> (5; 56%).

`PROPN` tokens may have the following values of `Style`:

* `Arch` (1; 11% of non-empty `Style`): Alšovi
* `Coll` (7; 78% of non-empty `Style`): Drahušce Drahuška Fanoušů Jaroušků Kyšický dvoukolákem embéčko
* `Expr` (1; 11% of non-empty `Style`): Zuzanka

### `DET`

3 [DET](cs_cac-pos-DET.html) tokens (0% of all `DET` tokens) have a non-empty value of `Style`.

The most frequent other feature values with which `DET` and `Style` co-occurred: <tt><a href="Person.html">Person</a>=EMPTY</tt> (3; 100%), <tt><a href="Number[psor].html">Number[psor]</a>=EMPTY</tt> (3; 100%), <tt><a href="Animacy.html">Animacy</a>=EMPTY</tt> (3; 100%), <tt><a href="Number.html">Number</a>=Plur</tt> (2; 67%), <tt><a href="Poss.html">Poss</a>=EMPTY</tt> (2; 67%).

`DET` tokens may have the following values of `Style`:

* `Coll` (3; 100% of non-empty `Style`): nějakejch svý tý

### `PART`

3 [PART](cs_cac-pos-PART.html) tokens (0% of all `PART` tokens) have a non-empty value of `Style`.

`PART` tokens may have the following values of `Style`:

* `Coll` (3; 100% of non-empty `Style`): jo fakt

## Relations with Agreement in `Style`

The 10 most frequent relations where parent and child node agree in `Style`:
<tt>ADV --[<a href="../dep/appos.html">appos</a>]--> ADV</tt> (1; 100%).

