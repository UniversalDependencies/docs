---
layout: base
title:  'Statistics of NumType in UD_Greek-GLCII'
udver: '2'
---

## Treebank Statistics: UD_Greek-GLCII: Features: `NumType`

This feature is universal.
It occurs with 3 different values: `Card`, `Ord`, `Sets`.

90 tokens (1%) have a non-empty value of `NumType`.
54 types (2%) occur at least once with a non-empty value of `NumType`.
42 lemmas (2%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="el_glcii-pos-NUM.html">NUM</a></tt> (74; 1% instances), <tt><a href="el_glcii-pos-ADJ.html">ADJ</a></tt> (16; 0% instances).

### `NUM`

74 <tt><a href="el_glcii-pos-NUM.html">NUM</a></tt> tokens (93% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (72; 97% of non-empty `NumType`): <em>δύο, πέντε, 3, σαράντα, ένα, 4, 5, μια, δέκα, δυο</em>
* `Sets` (2; 3% of non-empty `NumType`): <em>χιλιάδες</em>
* `EMPTY` (6): <em>Vierzehen, α, β, δεκαεννιά, ενασ, μία</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (35) occur only with one value of `NumType`.

### `ADJ`

16 <tt><a href="el_glcii-pos-ADJ.html">ADJ</a></tt> tokens (3% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="el_glcii-feat-Number.html">Number</a></tt><tt>=Sing</tt> (14; 88%), <tt><a href="el_glcii-feat-Case.html">Case</a></tt><tt>=Nom</tt> (10; 63%).

`ADJ` tokens may have the following values of `NumType`:

* `Ord` (16; 100% of non-empty `NumType`): <em>δεύτερη, δεύτερο, πρώτη, πρώτο, τρίτη, ΠΡΩΤΟ, έκτο, δεύθερη, πρώτες, πρώτοι</em>
* `EMPTY` (540): <em>σημαντικό, μεγάλο, καλή, γκέι, μεγάλη, ωραίο, καλό, ξένες, ωραία, όμορφη</em>

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="el_glcii-dep-compound.html">compound</a></tt>]--> NUM</tt> (3; 60%),
<tt>NUM --[<tt><a href="el_glcii-dep-conj.html">conj</a></tt>]--> NUM</tt> (2; 100%),
<tt>ADJ --[<tt><a href="el_glcii-dep-parataxis.html">parataxis</a></tt>]--> ADJ</tt> (1; 100%).

