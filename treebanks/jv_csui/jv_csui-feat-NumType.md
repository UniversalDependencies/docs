---
layout: base
title:  'Statistics of NumType in UD_Javanese-CSUI'
udver: '2'
---

## Treebank Statistics: UD_Javanese-CSUI: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

115 tokens (6%) have a non-empty value of `NumType`.
83 types (8%) occur at least once with a non-empty value of `NumType`.
81 lemmas (8%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="jv_csui-pos-NUM.html">NUM</a></tt> (112; 5% instances), <tt><a href="jv_csui-pos-ADJ.html">ADJ</a></tt> (3; 0% instances).

### `NUM`

112 <tt><a href="jv_csui-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (112; 100% of non-empty `NumType`): <em>3, 1, 5, 11, 12, 1994, 2006, 1101, 17, 1948</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (78) occur only with one value of `NumType`.

### `ADJ`

3 <tt><a href="jv_csui-pos-ADJ.html">ADJ</a></tt> tokens (4% of all `ADJ` tokens) have a non-empty value of `NumType`.

`ADJ` tokens may have the following values of `NumType`:

* `Ord` (3; 100% of non-empty `NumType`): <em>pertama, kapisanan, kapitu</em>
* `EMPTY` (75): <em>pungkasan, ageng, rikat, umum, apik, langka, lokal, salah, saé, Semana</em>

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="jv_csui-dep-flat.html">flat</a></tt>]--> NUM</tt> (10; 100%),
<tt>NUM --[<tt><a href="jv_csui-dep-conj.html">conj</a></tt>]--> NUM</tt> (5; 100%),
<tt>NUM --[<tt><a href="jv_csui-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (1; 100%).

