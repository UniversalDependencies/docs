---
layout: base
title:  'Statistics of PronType in UD_Turkish_English-BUTR'
udver: '2'
---

## Treebank Statistics: UD_Turkish_English-BUTR: Features: `PronType`

This feature is universal.
It occurs with 7 different values: `Art`, `Dem`, `Ind`, `Int`, `Neg`, `Prs`, `Tot`.

64 tokens (15%) have a non-empty value of `PronType`.
38 types (12%) occur at least once with a non-empty value of `PronType`.
23 lemmas (9%) occur at least once with a non-empty value of `PronType`.
The feature is used with 2 part-of-speech tags: <tt><a href="qti_butr-pos-PRON.html">PRON</a></tt> (38; 9% instances), <tt><a href="qti_butr-pos-DET.html">DET</a></tt> (26; 6% instances).

### `PRON`

38 <tt><a href="qti_butr-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="qti_butr-feat-Number.html">Number</a></tt><tt>=Sing</tt> (25; 66%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (11; 29% of non-empty `PronType`): <em>this, bu, That's, Bunda, Bunu, Bunun, burada, o</em>
* `Ind` (2; 5% of non-empty `PronType`): <em>Birinin, biri</em>
* `Int` (2; 5% of non-empty `PronType`): <em>Ne, nerede</em>
* `Prs` (23; 61% of non-empty `PronType`): <em>i, Ben, Me, You're, bana, bence, beni, he, it, u</em>

<table>
  <tr><th>Paradigm <i>o</i></th><th><tt>Prs</tt></th><th><tt>Dem</tt></th></tr>
  <tr><td><tt><tt><a href="qti_butr-feat-Case.html">Case</a></tt><tt>=Acc</tt>|<tt><a href="qti_butr-feat-Person.html">Person</a></tt><tt>=3</tt></tt></td><td><em>onu</em></td><td></td></tr>
  <tr><td><tt><tt><a href="qti_butr-feat-Case.html">Case</a></tt><tt>=Nom</tt></tt></td><td></td><td><em>o</em></td></tr>
</table>

`PronType` seems to be **lexical feature** of `PRON`. 94% lemmas (15) occur only with one value of `PronType`.

### `DET`

26 <tt><a href="qti_butr-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="qti_butr-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (23; 88%).

`DET` tokens may have the following values of `PronType`:

* `Art` (15; 58% of non-empty `PronType`): <em>bir, a, the</em>
* `Dem` (4; 15% of non-empty `PronType`): <em>bu, o</em>
* `Ind` (1; 4% of non-empty `PronType`): <em>Other</em>
* `Int` (1; 4% of non-empty `PronType`): <em>hangi</em>
* `Neg` (1; 4% of non-empty `PronType`): <em>hiçbir</em>
* `Prs` (3; 12% of non-empty `PronType`): <em>my</em>
* `Tot` (1; 4% of non-empty `PronType`): <em>All</em>

`PronType` seems to be **lexical feature** of `DET`. 100% lemmas (10) occur only with one value of `PronType`.

