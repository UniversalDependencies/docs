---
layout: base
title:  'Statistics of NumType in UD_Portuguese-DANTEStocks'
udver: '2'
---

## Treebank Statistics: UD_Portuguese-DANTEStocks: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

5099 tokens (6%) have a non-empty value of `NumType`.
2415 types (22%) occur at least once with a non-empty value of `NumType`.
2403 lemmas (27%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="pt_dantestocks-pos-NUM.html">NUM</a></tt> (5018; 6% instances), <tt><a href="pt_dantestocks-pos-ADJ.html">ADJ</a></tt> (81; 0% instances).

### `NUM`

5018 <tt><a href="pt_dantestocks-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (5018; 100% of non-empty `NumType`): <em>13, 5, 3, 10, 15, 2, 1, 4, 31/12/2013, 6</em>
* `EMPTY` (3): <em>5,xx, @27,07, uma</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (2375) occur only with one value of `NumType`.

### `ADJ`

81 <tt><a href="pt_dantestocks-pos-ADJ.html">ADJ</a></tt> tokens (3% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="pt_dantestocks-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (66; 81%), <tt><a href="pt_dantestocks-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (61; 75%).

`ADJ` tokens may have the following values of `NumType`:

* `Ord` (81; 100% of non-empty `NumType`): <em>1º, terceiro, 4º, 1ª, 2º, 3º, 4°, 5º, décimo, oitavo</em>
* `EMPTY` (2836): <em>diário, nova, última, superior, financeiro, financeiras, bom, maiores, semanal, novo</em>

`NumType` seems to be **lexical feature** of `ADJ`. 100% lemmas (28) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="pt_dantestocks-dep-conj.html">conj</a></tt>]--> NUM</tt> (247; 100%),
<tt>NUM --[<tt><a href="pt_dantestocks-dep-list.html">list</a></tt>]--> NUM</tt> (23; 100%),
<tt>NUM --[<tt><a href="pt_dantestocks-dep-flat.html">flat</a></tt>]--> NUM</tt> (14; 100%),
<tt>NUM --[<tt><a href="pt_dantestocks-dep-nmod.html">nmod</a></tt>]--> NUM</tt> (7; 100%),
<tt>NUM --[<tt><a href="pt_dantestocks-dep-appos.html">appos</a></tt>]--> NUM</tt> (1; 100%),
<tt>NUM --[<tt><a href="pt_dantestocks-dep-nsubj.html">nsubj</a></tt>]--> NUM</tt> (1; 100%),
<tt>NUM --[<tt><a href="pt_dantestocks-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (1; 100%).

