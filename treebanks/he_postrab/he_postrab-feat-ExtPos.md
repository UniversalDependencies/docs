---
layout: base
title:  'Statistics of ExtPos in UD_Hebrew-PostRab'
udver: '2'
---

## Treebank Statistics: UD_Hebrew-PostRab: Features: `ExtPos`

This feature is language-specific.
It occurs with 3 different values: `ADP`, `ADV`, `SCONJ`.

8 tokens (0%) have a non-empty value of `ExtPos`.
6 types (0%) occur at least once with a non-empty value of `ExtPos`.
6 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 4 part-of-speech tags: <tt><a href="he_postrab-pos-ADP.html">ADP</a></tt> (3; 0% instances), <tt><a href="he_postrab-pos-DET.html">DET</a></tt> (2; 0% instances), <tt><a href="he_postrab-pos-NOUN.html">NOUN</a></tt> (2; 0% instances), <tt><a href="he_postrab-pos-ADJ.html">ADJ</a></tt> (1; 0% instances).

### `ADP`

3 <tt><a href="he_postrab-pos-ADP.html">ADP</a></tt> tokens (0% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADV` (3; 100% of non-empty `ExtPos`): אחר, על

### `DET`

2 <tt><a href="he_postrab-pos-DET.html">DET</a></tt> tokens (0% of all `DET` tokens) have a non-empty value of `ExtPos`.

`DET` tokens may have the following values of `ExtPos`:

* `ADV` (1; 50% of non-empty `ExtPos`): כל
* `SCONJ` (1; 50% of non-empty `ExtPos`): כל

<table>
  <tr><th>Paradigm <i>כל</i></th><th><tt>ADV</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td>כל</td><td>כל</td></tr>
</table>

### `NOUN`

2 <tt><a href="he_postrab-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `ExtPos`.

`NOUN` tokens may have the following values of `ExtPos`:

* `ADV` (2; 100% of non-empty `ExtPos`): פנים, שתי

### `ADJ`

1 <tt><a href="he_postrab-pos-ADJ.html">ADJ</a></tt> tokens (0% of all `ADJ` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADJ` and `ExtPos` co-occurred: <tt><a href="he_postrab-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (1; 100%), <tt><a href="he_postrab-feat-Number.html">Number</a></tt><tt>=Sing</tt> (1; 100%).

`ADJ` tokens may have the following values of `ExtPos`:

* `ADP` (1; 100% of non-empty `ExtPos`): סמוך

