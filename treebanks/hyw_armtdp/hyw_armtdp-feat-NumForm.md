---
layout: base
title:  'Statistics of NumForm in UD_Western_Armenian-ArmTDP'
udver: '2'
---

## Treebank Statistics: UD_Western_Armenian-ArmTDP: Features: `NumForm`

This feature is language-specific.
It occurs with 4 different values: `Armenian`, `Combi`, `Digit`, `Word`.

903 tokens (1%) have a non-empty value of `NumForm`.
236 types (1%) occur at least once with a non-empty value of `NumForm`.
209 lemmas (2%) occur at least once with a non-empty value of `NumForm`.
The feature is used with 2 part-of-speech tags: <tt><a href="hyw_armtdp-pos-NUM.html">NUM</a></tt> (684; 1% instances), <tt><a href="hyw_armtdp-pos-ADJ.html">ADJ</a></tt> (219; 0% instances).

### `NUM`

684 <tt><a href="hyw_armtdp-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumForm`.

The most frequent other feature values with which `NUM` and `NumForm` co-occurred: <tt><a href="hyw_armtdp-feat-NumType.html">NumType</a></tt><tt>=Card</tt> (662; 97%).

`NUM` tokens may have the following values of `NumForm`:

* `Armenian` (11; 2% of non-empty `NumForm`): <em>ա, բ, գ</em>
* `Digit` (342; 50% of non-empty `NumForm`): <em>2021, 6, 7, 15, 2020, 24, 12, 1915, 2, 19</em>
* `Word` (331; 48% of non-empty `NumForm`): <em>երկու, մէկ, երեք, չորս, հինգ, վեց, կէս, հազար, քսան, հարիւր</em>

`NumForm` seems to be **lexical feature** of `NUM`. 100% lemmas (159) occur only with one value of `NumForm`.

### `ADJ`

219 <tt><a href="hyw_armtdp-pos-ADJ.html">ADJ</a></tt> tokens (3% of all `ADJ` tokens) have a non-empty value of `NumForm`.

The most frequent other feature values with which `ADJ` and `NumForm` co-occurred: <tt><a href="hyw_armtdp-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (130; 59%).

`ADJ` tokens may have the following values of `NumForm`:

* `Armenian` (50; 23% of non-empty `NumForm`): <em>Բ, Ա, ԺԹ, Գ, Զ, Ը, Դ, Ե, Է, ԺԷ</em>
* `Combi` (36; 16% of non-empty `NumForm`): <em>44-օրեայ, 113-րդ, 106-րդ, 12-ամեայ, 18ամեայ, 1950-ական, 20-րդ, 24-եան, 36նոց, 44օրեայ</em>
* `Word` (133; 61% of non-empty `NumForm`): <em>առաջին, երկրորդ, առջի, երրորդ, չորրորդ, եօթանասուննոց, առաջի, երկուքուկէսնոց, եօթանասուներորդ, իններորդ</em>

`NumForm` seems to be **lexical feature** of `ADJ`. 100% lemmas (50) occur only with one value of `NumForm`.

## Relations with Agreement in `NumForm`

The 10 most frequent relations where parent and child node agree in `NumForm`:
<tt>NUM --[<tt><a href="hyw_armtdp-dep-flat.html">flat</a></tt>]--> NUM</tt> (28; 100%),
<tt>NUM --[<tt><a href="hyw_armtdp-dep-flat-range.html">flat:range</a></tt>]--> NUM</tt> (19; 100%),
<tt>NUM --[<tt><a href="hyw_armtdp-dep-conj.html">conj</a></tt>]--> NUM</tt> (8; 100%),
<tt>NUM --[<tt><a href="hyw_armtdp-dep-appos.html">appos</a></tt>]--> NUM</tt> (4; 100%),
<tt>NUM --[<tt><a href="hyw_armtdp-dep-flat-dist.html">flat:dist</a></tt>]--> NUM</tt> (3; 100%),
<tt>NUM --[<tt><a href="hyw_armtdp-dep-flat-frac.html">flat:frac</a></tt>]--> NUM</tt> (1; 100%),
<tt>NUM --[<tt><a href="hyw_armtdp-dep-nmod-npmod.html">nmod:npmod</a></tt>]--> NUM</tt> (1; 100%).

