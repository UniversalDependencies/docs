---
layout: base
title:  'Statistics of NumType in UD_Chintang-CTNTB'
udver: '2'
---

## Treebank Statistics: UD_Chintang-CTNTB: Features: `NumType`

This feature is universal.
It occurs with 1 different values: `Card`.

225 tokens (2%) have a non-empty value of `NumType`.
74 types (2%) occur at least once with a non-empty value of `NumType`.
30 lemmas (2%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="ctn_ctntb-pos-NUM.html">NUM</a></tt> (222; 2% instances), <tt><a href="ctn_ctntb-pos-ADV.html">ADV</a></tt> (3; 0% instances).

### `NUM`

222 <tt><a href="ctn_ctntb-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="ctn_ctntb-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (185; 83%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (222; 100% of non-empty `NumType`): <em>thitta, ek, hicce, car, dui, das, hiccibaŋ, hicci, sumce, pãc</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (29) occur only with one value of `NumType`.

### `ADV`

3 <tt><a href="ctn_ctntb-pos-ADV.html">ADV</a></tt> tokens (0% of all `ADV` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADV` and `NumType` co-occurred: <tt><a href="ctn_ctntb-feat-Deixis.html">Deixis</a></tt><tt>=EMPTY</tt> (3; 100%), <tt><a href="ctn_ctntb-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (3; 100%), <tt><a href="ctn_ctntb-feat-AdvType.html">AdvType</a></tt><tt>=EMPTY</tt> (2; 67%), <tt><a href="ctn_ctntb-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (2; 67%).

`ADV` tokens may have the following values of `NumType`:

* `Card` (3; 100% of non-empty `NumType`): <em>Thitta-thitta, Hiccipatti</em>
* `EMPTY` (882): <em>abo, baiʔ, paĩ, pheri, utti, appi, mo, theke, aŋ, jatti</em>

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="ctn_ctntb-dep-conj.html">conj</a></tt>]--> NUM</tt> (12; 100%),
<tt>NUM --[<tt><a href="ctn_ctntb-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (4; 100%),
<tt>NUM --[<tt><a href="ctn_ctntb-dep-flat-num.html">flat:num</a></tt>]--> NUM</tt> (1; 100%),
<tt>NUM --[<tt><a href="ctn_ctntb-dep-reparandum.html">reparandum</a></tt>]--> NUM</tt> (1; 100%).

