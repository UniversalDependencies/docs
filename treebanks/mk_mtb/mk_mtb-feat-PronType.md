---
layout: base
title:  'Statistics of PronType in UD_Macedonian-MTB'
udver: '2'
---

## Treebank Statistics: UD_Macedonian-MTB: Features: `PronType`

This feature is universal.
It occurs with 7 different values: `Dem`, `Ind`, `Int`, `Neg`, `Prs`, `Rel`, `Tot`.

192 tokens (14%) have a non-empty value of `PronType`.
73 types (13%) occur at least once with a non-empty value of `PronType`.
59 lemmas (12%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="mk_mtb-pos-PRON.html">PRON</a></tt> (156; 11% instances), <tt><a href="mk_mtb-pos-DET.html">DET</a></tt> (20; 1% instances), <tt><a href="mk_mtb-pos-ADV.html">ADV</a></tt> (16; 1% instances).

### `PRON`

156 <tt><a href="mk_mtb-pos-PRON.html">PRON</a></tt> tokens (87% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="mk_mtb-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (138; 88%), <tt><a href="mk_mtb-feat-Reflex.html">Reflex</a></tt><tt>=EMPTY</tt> (125; 80%), <tt><a href="mk_mtb-feat-Number.html">Number</a></tt><tt>=Sing</tt> (94; 60%), <tt><a href="mk_mtb-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (90; 58%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (4; 3% of non-empty `PronType`): <em>тоа</em>
* `Ind` (2; 1% of non-empty `PronType`): <em>неколку, нешто</em>
* `Int` (3; 2% of non-empty `PronType`): <em>кој, Што</em>
* `Neg` (1; 1% of non-empty `PronType`): <em>ништо</em>
* `Prs` (139; 89% of non-empty `PronType`): <em>се, го, ми, ја, ме, му, тие, Тој, ти, ги</em>
* `Rel` (5; 3% of non-empty `PronType`): <em>којшто, Кој, каков</em>
* `Tot` (2; 1% of non-empty `PronType`): <em>Сѐ, сите</em>
* `EMPTY` (24): <em>се, што, ја, Сето, Сѐ, каде, јас</em>

<table>
  <tr><th>Paradigm <i>кој</i></th><th><tt>Int</tt></th><th><tt>Rel</tt></th></tr>
  <tr><td><tt></tt></td><td><em>кој</em></td><td><em>Кој</em></td></tr>
</table>

`PronType` seems to be **lexical feature** of `PRON`. 97% lemmas (37) occur only with one value of `PronType`.

### `DET`

20 <tt><a href="mk_mtb-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="mk_mtb-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (15; 75%), <tt><a href="mk_mtb-feat-Number.html">Number</a></tt><tt>=Sing</tt> (15; 75%), <tt><a href="mk_mtb-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (12; 60%), <tt><a href="mk_mtb-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (11; 55%).

`DET` tokens may have the following values of `PronType`:

* `Dem` (6; 30% of non-empty `PronType`): <em>она, Ова, Оваа, Тие, тоа</em>
* `Ind` (3; 15% of non-empty `PronType`): <em>една, некои, некоја</em>
* `Int` (2; 10% of non-empty `PronType`): <em>каков, такво</em>
* `Prs` (9; 45% of non-empty `PronType`): <em>моите, мојот, неговиот, нејзиниот, својот, твојот</em>

`PronType` seems to be **lexical feature** of `DET`. 100% lemmas (13) occur only with one value of `PronType`.

### `ADV`

16 <tt><a href="mk_mtb-pos-ADV.html">ADV</a></tt> tokens (28% of all `ADV` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADV` and `PronType` co-occurred: <tt><a href="mk_mtb-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (16; 100%).

`ADV` tokens may have the following values of `PronType`:

* `Dem` (6; 38% of non-empty `PronType`): <em>Таму, така, тогаш, туку, толкава, толку</em>
* `Int` (8; 50% of non-empty `PronType`): <em>кога, како, каде, колкава, колку</em>
* `Neg` (1; 6% of non-empty `PronType`): <em>никогаш</em>
* `Rel` (1; 6% of non-empty `PronType`): <em>како</em>
* `EMPTY` (41): <em>вчера, дома, само, многу, овде, пак, потоа, Јасно, Надвор, бавно</em>

<table>
  <tr><th>Paradigm <i>како</i></th><th><tt>Int</tt></th><th><tt>Rel</tt></th></tr>
  <tr><td><tt></tt></td><td><em>како</em></td><td><em>како</em></td></tr>
</table>

`PronType` seems to be **lexical feature** of `ADV`. 92% lemmas (11) occur only with one value of `PronType`.

