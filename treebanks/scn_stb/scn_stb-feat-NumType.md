---
layout: base
title:  'Statistics of NumType in UD_Sicilian-STB'
udver: '2'
---

## Treebank Statistics: UD_Sicilian-STB: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

60 tokens (1%) have a non-empty value of `NumType`.
28 types (1%) occur at least once with a non-empty value of `NumType`.
20 lemmas (1%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="scn_stb-pos-NUM.html">NUM</a></tt> (48; 0% instances), <tt><a href="scn_stb-pos-ADJ.html">ADJ</a></tt> (12; 0% instances).

### `NUM`

48 <tt><a href="scn_stb-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (48; 100% of non-empty `NumType`): <em>tri, quattru, dui, ddu', du’, trent', vintiquattr', dicidott', se', '62</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (14) occur only with one value of `NumType`.

### `ADJ`

12 <tt><a href="scn_stb-pos-ADJ.html">ADJ</a></tt> tokens (3% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="scn_stb-feat-Gender.html">Gender</a></tt><tt>=Fem</tt> (10; 83%), <tt><a href="scn_stb-feat-Number.html">Number</a></tt><tt>=Sing</tt> (10; 83%).

`ADJ` tokens may have the following values of `NumType`:

* `Ord` (12; 100% of non-empty `NumType`): <em>prima, terza, primi, primu, secunna, sicunnu, ùrtimi</em>
* `EMPTY` (375): <em>menzu, marinu, autra, sana, veru, beḍḍa, funnutu, gran, granni, megghiu</em>

