---
layout: base
title:  'Statistics of PronType in UD_Italian-ParlaMint'
udver: '2'
---

## Treebank Statistics: UD_Italian-ParlaMint: Features: `PronType`

This feature is universal.
It occurs with 8 different values: `Art`, `Dem`, `Ind`, `Int`, `Neg`, `Prs`, `Rel`, `Tot`.

4561 tokens (22%) have a non-empty value of `PronType`.
171 types (5%) occur at least once with a non-empty value of `PronType`.
70 lemmas (3%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="it_parlamint-pos-DET.html">DET</a></tt> (3204; 16% instances), <tt><a href="it_parlamint-pos-PRON.html">PRON</a></tt> (1136; 6% instances), <tt><a href="it_parlamint-pos-ADV.html">ADV</a></tt> (221; 1% instances).

### `DET`

3204 <tt><a href="it_parlamint-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="it_parlamint-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (2392; 75%), <tt><a href="it_parlamint-feat-Number.html">Number</a></tt><tt>=Sing</tt> (2315; 72%).

`DET` tokens may have the following values of `PronType`:

* `Art` (2713; 85% of non-empty `PronType`): <em>il, la, l', i, le, un, una, gli, lo, un'</em>
* `Dem` (204; 6% of non-empty `PronType`): <em>questo, questi, questa, queste, tale, quei, quella, quel, quelle, quest'</em>
* `Ind` (128; 4% of non-empty `PronType`): <em>ogni, delle, alcuni, tutto, alcune, dei, più, qualche, alcuna, degli</em>
* `Int` (12; 0% of non-empty `PronType`): <em>che, quanta, Quanti, quali, quale, quante</em>
* `Prs` (119; 4% of non-empty `PronType`): <em>sua, loro, suo, propria, mio, nostro, nostri, propri, miei, nostra</em>
* `Rel` (2; 0% of non-empty `PronType`): <em>cui</em>
* `Tot` (26; 1% of non-empty `PronType`): <em>tutti, tutta, tutto, tutte</em>
* `EMPTY` (15): <em>tutte, tutti, tutta, tutto, tutt'</em>

<table>
  <tr><th>Paradigm <i>tutto</i></th><th><tt>Tot</tt></th><th><tt>Ind</tt></th></tr>
  <tr><td><tt><tt><a href="it_parlamint-feat-Gender.html">Gender</a></tt><tt>=Masc</tt>|<tt><a href="it_parlamint-feat-Number.html">Number</a></tt><tt>=Sing</tt></tt></td><td><em>tutto</em></td><td><em>tutto</em></td></tr>
  <tr><td><tt><tt><a href="it_parlamint-feat-Gender.html">Gender</a></tt><tt>=Masc</tt>|<tt><a href="it_parlamint-feat-Number.html">Number</a></tt><tt>=Plur</tt></tt></td><td><em>tutti</em></td><td><em>tutti</em></td></tr>
  <tr><td><tt><tt><a href="it_parlamint-feat-Gender.html">Gender</a></tt><tt>=Fem</tt>|<tt><a href="it_parlamint-feat-Number.html">Number</a></tt><tt>=Sing</tt></tt></td><td><em>tutta</em></td><td><em>tutta</em></td></tr>
  <tr><td><tt><tt><a href="it_parlamint-feat-Gender.html">Gender</a></tt><tt>=Fem</tt>|<tt><a href="it_parlamint-feat-Number.html">Number</a></tt><tt>=Plur</tt></tt></td><td><em>tutte</em></td><td></td></tr>
</table>

`PronType` seems to be **lexical feature** of `DET`. 91% lemmas (32) occur only with one value of `PronType`.

### `PRON`

1136 <tt><a href="it_parlamint-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="it_parlamint-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (820; 72%), <tt><a href="it_parlamint-feat-Clitic.html">Clitic</a></tt><tt>=EMPTY</tt> (740; 65%), <tt><a href="it_parlamint-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (730; 64%), <tt><a href="it_parlamint-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (618; 54%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (124; 11% of non-empty `PronType`): <em>questo, quello, ciò, coloro, quella, questa, quelli, quelle, queste, questi</em>
* `Ind` (84; 7% of non-empty `PronType`): <em>altro, altri, tutto, tutti, uno, nulla, qualcosa, qualcuno, nessuno, una</em>
* `Int` (13; 1% of non-empty `PronType`): <em>cosa, qual, Quali, quanto</em>
* `Prs` (495; 44% of non-empty `PronType`): <em>si, ci, lo, lei, mi, c', noi, io, la, le</em>
* `Rel` (420; 37% of non-empty `PronType`): <em>che, cui, chi, quanto, quale, quali, quanti</em>
* `EMPTY` (3): <em>c', quale, stessi</em>

<table>
  <tr><th>Paradigm <i>quale</i></th><th><tt>Int</tt></th><th><tt>Rel</tt></th></tr>
  <tr><td><tt><tt><a href="it_parlamint-feat-Number.html">Number</a></tt><tt>=Sing</tt></tt></td><td><em>qual</em></td><td><em>quale</em></td></tr>
  <tr><td><tt><tt><a href="it_parlamint-feat-Number.html">Number</a></tt><tt>=Plur</tt></tt></td><td><em>Quali</em></td><td><em>quali</em></td></tr>
</table>

`PronType` seems to be **lexical feature** of `PRON`. 96% lemmas (48) occur only with one value of `PronType`.

### `ADV`

221 <tt><a href="it_parlamint-pos-ADV.html">ADV</a></tt> tokens (21% of all `ADV` tokens) have a non-empty value of `PronType`.

`ADV` tokens may have the following values of `PronType`:

* `Neg` (221; 100% of non-empty `PronType`): <em>non, neanche, no, nemmeno</em>
* `EMPTY` (855): <em>anche, più, oggi, quindi, così, già, infatti, pertanto, solo, prima</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="it_parlamint-dep-conj.html">conj</a></tt>]--> PRON</tt> (8; 89%),
<tt>DET --[<tt><a href="it_parlamint-dep-conj.html">conj</a></tt>]--> DET</tt> (1; 100%).

