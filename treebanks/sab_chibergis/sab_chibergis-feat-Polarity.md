---
layout: base
title:  'Statistics of Polarity in UD_Bokota-ChibErgIS'
udver: '2'
---

## Treebank Statistics: UD_Bokota-ChibErgIS: Features: `Polarity`

This feature is universal.
It occurs with 1 different values: `Neg`.

26 tokens (1%) have a non-empty value of `Polarity`.
1 types (0%) occur at least once with a non-empty value of `Polarity`.
1 lemmas (0%) occur at least once with a non-empty value of `Polarity`.
The feature is used with 1 part-of-speech tags: <tt><a href="sab_chibergis-pos-AUX.html">AUX</a></tt> (26; 1% instances).

### `AUX`

26 <tt><a href="sab_chibergis-pos-AUX.html">AUX</a></tt> tokens (10% of all `AUX` tokens) have a non-empty value of `Polarity`.

`AUX` tokens may have the following values of `Polarity`:

* `Neg` (26; 100% of non-empty `Polarity`): <em>me</em>
* `EMPTY` (237): <em>kle, be, gna, hoke, mim, tiene, ho, ponia</em>

