---
layout: base
title:  'Statistics of NumType in UD_Egyptian-UJaen'
udver: '2'
---

## Treebank Statistics: UD_Egyptian-UJaen: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

296 tokens (1%) have a non-empty value of `NumType`.
34 types (1%) occur at least once with a non-empty value of `NumType`.
25 lemmas (2%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="egy_ujaen-pos-NUM.html">NUM</a></tt> (296; 1% instances).

### `NUM`

296 <tt><a href="egy_ujaen-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="egy_ujaen-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (257; 87%), <tt><a href="egy_ujaen-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (229; 77%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (291; 98% of non-empty `NumType`): <em>4, 2, 1, ḫꜣ, 5, fṭ.w, wꜥ, ḫꜣ(.w), [2], 3</em>
* `Ord` (5; 2% of non-empty `NumType`): <em>ḫtm.nw, fṭ.nw, ḫmt.nw, ṭp(.ꞽ)</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (25) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="egy_ujaen-dep-conj.html">conj</a></tt>]--> NUM</tt> (17; 100%).

