---
layout: base
title:  'Statistics of NumType in UD_Armenian-ArmTDP'
udver: '2'
---

## Treebank Statistics: UD_Armenian-ArmTDP: Features: `NumType`

This feature is universal.
It occurs with 5 different values: `Card`, `Dist`, `Frac`, `Ord`, `Range`.

246 tokens (2%) have a non-empty value of `NumType`.
134 types (3%) occur at least once with a non-empty value of `NumType`.
127 lemmas (4%) occur at least once with a non-empty value of `NumType`.
The feature is used with 3 part-of-speech tags: <tt><a href="hy_armtdp-pos-NUM.html">NUM</a></tt> (212; 2% instances), <tt><a href="hy_armtdp-pos-ADJ.html">ADJ</a></tt> (28; 0% instances), <tt><a href="hy_armtdp-pos-ADV.html">ADV</a></tt> (6; 0% instances).

### `NUM`

212 <tt><a href="hy_armtdp-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="hy_armtdp-feat-NumForm.html">NumForm</a></tt><tt>=Digit</tt> (140; 66%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (179; 84% of non-empty `NumType`): <em>մլն, 2017, 2016, հազար, մեկ, երկու, մի, 2, մլրդ, 06</em>
* `Frac` (28; 13% of non-empty `NumType`): <em>3.2, կես, 0.2, 13,99, 14,07, 176.7, 2,3, 2,7, 201.5, 210.4</em>
* `Range` (5; 2% of non-empty `NumType`): <em>12-13, 17-23, 1878-1943, 2008-2012, մեկ-երկու</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (118) occur only with one value of `NumType`.

### `ADJ`

28 <tt><a href="hy_armtdp-pos-ADJ.html">ADJ</a></tt> tokens (3% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="hy_armtdp-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (16; 57%).

`ADJ` tokens may have the following values of `NumType`:

* `Card` (2; 7% of non-empty `NumType`): <em>մեկ, յոթերորդ</em>
* `Ord` (26; 93% of non-empty `NumType`): <em>առաջին, երկրորդ, 100-րդ, 11-րդ, 20-րդ, XX, 113-րդ, քսաներորդ</em>
* `EMPTY` (1003): <em>նոր, քաղաքական, մեծ, կարող, հայ, ազգային, խորհրդային, պետք, տնտեսական, փոքր</em>

`NumType` seems to be **lexical feature** of `ADJ`. 100% lemmas (10) occur only with one value of `NumType`.

### `ADV`

6 <tt><a href="hy_armtdp-pos-ADV.html">ADV</a></tt> tokens (1% of all `ADV` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADV` and `NumType` co-occurred: <tt><a href="hy_armtdp-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (6; 100%).

`ADV` tokens may have the following values of `NumType`:

* `Dist` (6; 100% of non-empty `NumType`): <em>մեկ</em>
* `EMPTY` (431): <em>ավելի, արդեն, միայն, հիմա, հետո, դուրս, հենց, տեղի, շատ, ինչպես</em>

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="hy_armtdp-dep-flat.html">flat</a></tt>]--> NUM</tt> (38; 73%),
<tt>NUM --[<tt><a href="hy_armtdp-dep-conj.html">conj</a></tt>]--> NUM</tt> (10; 100%),
<tt>ADJ --[<tt><a href="hy_armtdp-dep-conj.html">conj</a></tt>]--> ADJ</tt> (1; 100%),
<tt>ADV --[<tt><a href="hy_armtdp-dep-compound-redup.html">compound:redup</a></tt>]--> ADV</tt> (1; 100%).

