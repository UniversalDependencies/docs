---
layout: base
title:  'Statistics of Abbr in UD_Dutch'
udver: '2'
---

## Treebank Statistics: UD_Dutch: Features: `Abbr`

This feature is universal.
It occurs with 1 different values: `Yes`.

131 tokens (0%) have a non-empty value of `Abbr`.
47 types (0%) occur at least once with a non-empty value of `Abbr`.
48 lemmas (0%) occur at least once with a non-empty value of `Abbr`.
The feature is used with 1 part-of-speech tags: <tt><a href="nl-pos-X.html">X</a></tt> (131; 0% instances).

### `X`

131 <tt><a href="nl-pos-X.html">X</a></tt> tokens (17% of all `X` tokens) have a non-empty value of `Abbr`.

The most frequent other feature values with which `X` and `Abbr` co-occurred: <tt><a href="nl-feat-Foreign.html">Foreign</a></tt><tt>=EMPTY</tt> (131; 100%).

`X` tokens may have the following values of `Abbr`:

* `Yes` (131; 100% of non-empty `Abbr`): <em>o.a., jl., o.m., Ad, a.s., etc., v.j., enz., b.v., e.d.</em>
* `EMPTY` (620): <em>fancy, a, the, binnen-, and, front, to, National, be, fiction</em>

`Abbr` seems to be **lexical feature** of `X`. 100% lemmas (48) occur only with one value of `Abbr`.

## Relations with Agreement in `Abbr`

The 10 most frequent relations where parent and child node agree in `Abbr`:
<tt>X --[<tt><a href="nl-dep-fixed.html">fixed</a></tt>]--> X</tt> (3; 100%).

