---
layout: base
title:  'Statistics of PronType in UD_Galician-CTG'
udver: '2'
---

## Treebank Statistics: UD_Galician-CTG: Features: `PronType`

This feature is universal.
It occurs with 8 different values: `Art`, `Dem`, `Ind`, `Int`, `Neg`, `Prs`, `Rel`, `Tot`.

30083 tokens (22%) have a non-empty value of `PronType`.
187 types (1%) occur at least once with a non-empty value of `PronType`.
61 lemmas (1%) occur at least once with a non-empty value of `PronType`.
The feature is used with 2 part-of-speech tags: <tt><a href="gl_ctg-pos-DET.html">DET</a></tt> (24185; 17% instances), <tt><a href="gl_ctg-pos-PRON.html">PRON</a></tt> (5898; 4% instances).

### `DET`

24185 <tt><a href="gl_ctg-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="gl_ctg-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (18461; 76%).

`DET` tokens may have the following values of `PronType`:

* `Art` (20504; 85% of non-empty `PronType`): <em>a, o, os, as, un, unha, unhas, la, el, los</em>
* `Dem` (1257; 5% of non-empty `PronType`): <em>este, esta, estes, estas, ese, aqueles, tal, esa, aquelas, tales</em>
* `Ind` (945; 4% of non-empty `PronType`): <em>outros, mesmo, outra, outras, outro, calquera, algúns, mesma, uns, moitas</em>
* `Int` (5; 0% of non-empty `PronType`): <em>qué</em>
* `Neg` (40; 0% of non-empty `PronType`): <em>ningún, ningunha</em>
* `Prs` (923; 4% of non-empty `PronType`): <em>súa, seu, seus, súas, nosa, noso, nosos, nosas, meu, miña</em>
* `Rel` (20; 0% of non-empty `PronType`): <em>que, canto, cantas</em>
* `Tot` (491; 2% of non-empty `PronType`): <em>cada, todo, todos, todas, toda, ambos, ambas, entrambos</em>

<table>
  <tr><th>Paradigm <i>un</i></th><th><tt>Art</tt></th><th><tt>Ind</tt></th></tr>
  <tr><td><tt>_</tt></td><td></td><td><em>uns</em></td></tr>
  <tr><td><tt><tt><a href="gl_ctg-feat-Definite.html">Definite</a></tt><tt>=Ind</tt></tt></td><td><em>un, unha, unhas</em></td><td></td></tr>
  <tr><td><tt><tt><a href="gl_ctg-feat-Definite.html">Definite</a></tt><tt>=Ind</tt>|<tt><a href="gl_ctg-feat-Foreign.html">Foreign</a></tt><tt>=Yes</tt></tt></td><td><em>un</em></td><td></td></tr>
</table>

`PronType` seems to be **lexical feature** of `DET`. 94% lemmas (31) occur only with one value of `PronType`.

### `PRON`

5898 <tt><a href="gl_ctg-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="gl_ctg-feat-Reflex.html">Reflex</a></tt><tt>=EMPTY</tt> (3706; 63%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (123; 2% of non-empty `PronType`): <em>isto, iso, aqueles, aquilo, esta</em>
* `Ind` (59; 1% of non-empty `PronType`): <em>un, unha, algo, alguén</em>
* `Neg` (7; 0% of non-empty `PronType`): <em>nada, ninguén</em>
* `Prs` (2976; 50% of non-empty `PronType`): <em>se, o, os, lle, me, nos, lles, eles, a, el</em>
* `Rel` (2733; 46% of non-empty `PronType`): <em>que, como, cando, onde, cal, quen, canto, cales, cantos, cuxa</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (35) occur only with one value of `PronType`.

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="gl_ctg-dep-iobj.html">iobj</a></tt>]--> PRON</tt> (10; 67%),
<tt>PRON --[<tt><a href="gl_ctg-dep-conj.html">conj</a></tt>]--> PRON</tt> (1; 100%).

