---
layout: base
title:  'Statistics of NumType in UD_Ancient_Greek-PTNK'
udver: '2'
---

## Treebank Statistics: UD_Ancient_Greek-PTNK: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

492 tokens (1%) have a non-empty value of `NumType`.
92 types (2%) occur at least once with a non-empty value of `NumType`.
47 lemmas (2%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="grc_ptnk-pos-NUM.html">NUM</a></tt> (492; 1% instances).

### `NUM`

492 <tt><a href="grc_ptnk-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (430; 87% of non-empty `NumType`): <em>ἑπτὰ, δύο, ἑκατὸν, δέκα, πέντε, τριάκοντα, τεσσαράκοντα, ἑξήκοντα, ἑβδομήκοντα, δώδεκα</em>
* `Ord` (62; 13% of non-empty `NumType`): <em>δεύτερον, τρίτῃ, δευτέρῳ, ἑβδόμῃ, δευτέρου, δεκάτην, δευτέρα, πέμπτον, πρώτῳ, τετάρτη</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (47) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="grc_ptnk-dep-conj.html">conj</a></tt>]--> NUM</tt> (94; 99%),
<tt>NUM --[<tt><a href="grc_ptnk-dep-appos.html">appos</a></tt>]--> NUM</tt> (2; 100%),
<tt>NUM --[<tt><a href="grc_ptnk-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (1; 100%).

