---
layout: base
title:  'Statistics of Foreign in UD_Greek-GDT'
udver: '2'
---

## Treebank Statistics: UD_Greek-GDT: Features: `Foreign`

This feature is universal.
It occurs with 1 different values: `Yes`.

974 tokens (2%) have a non-empty value of `Foreign`.
499 types (4%) occur at least once with a non-empty value of `Foreign`.
499 lemmas (8%) occur at least once with a non-empty value of `Foreign`.
The feature is used with 1 part-of-speech tags: <tt><a href="el_gdt-pos-X.html">X</a></tt> (974; 2% instances).

### `X`

974 <tt><a href="el_gdt-pos-X.html">X</a></tt> tokens (100% of all `X` tokens) have a non-empty value of `Foreign`.

`X` tokens may have the following values of `Foreign`:

* `Yes` (974; 100% of non-empty `Foreign`): <em>αλ, Κάστρο, σκορ, Αφγανιστάν, Ουάσιγκτον, Σαντόρουμ, Μιτ, Ρικ, Ρόμνεϊ, Θιβέτ</em>

`Foreign` seems to be **lexical feature** of `X`. 100% lemmas (499) occur only with one value of `Foreign`.

## Relations with Agreement in `Foreign`

The 10 most frequent relations where parent and child node agree in `Foreign`:
<tt>X --[<tt><a href="el_gdt-dep-flat.html">flat</a></tt>]--> X</tt> (200; 100%),
<tt>X --[<tt><a href="el_gdt-dep-conj.html">conj</a></tt>]--> X</tt> (33; 100%),
<tt>X --[<tt><a href="el_gdt-dep-nmod.html">nmod</a></tt>]--> X</tt> (12; 100%),
<tt>X --[<tt><a href="el_gdt-dep-appos.html">appos</a></tt>]--> X</tt> (1; 100%).

