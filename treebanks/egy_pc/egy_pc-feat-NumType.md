---
layout: base
title:  'Statistics of NumType in UD_Egyptian-PC'
udver: '2'
---

## Treebank Statistics: UD_Egyptian-PC: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

333 tokens (1%) have a non-empty value of `NumType`.
48 types (1%) occur at least once with a non-empty value of `NumType`.
30 lemmas (2%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="egy_pc-pos-NUM.html">NUM</a></tt> (333; 1% instances).

### `NUM`

333 <tt><a href="egy_pc-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="egy_pc-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (287; 86%), <tt><a href="egy_pc-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (238; 71%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (324; 97% of non-empty `NumType`): <em>4, 2, 1, ḫꜣ, fṭ.w, 5, wꜥ, ḫꜣ(.w), fṭ.t, mč̣</em>
* `Ord` (9; 3% of non-empty `NumType`): <em>ḫmt.nwt, ḫtm.nw, [śn.nw], [ḫtm.nw], fṭ.nw, ḫmt.nw, ṭp(.ꞽ)</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (30) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="egy_pc-dep-conj.html">conj</a></tt>]--> NUM</tt> (19; 100%).

