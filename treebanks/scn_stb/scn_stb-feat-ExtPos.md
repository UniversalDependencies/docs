---
layout: base
title:  'Statistics of ExtPos in UD_Sicilian-STB'
udver: '2'
---

## Treebank Statistics: UD_Sicilian-STB: Features: `ExtPos`

This feature is language-specific.
It occurs with 4 different values: `ADP`, `ADV`, `INTJ`, `SCONJ`.

65 tokens (1%) have a non-empty value of `ExtPos`.
32 types (1%) occur at least once with a non-empty value of `ExtPos`.
26 lemmas (2%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 3 part-of-speech tags: <tt><a href="scn_stb-pos-ADV.html">ADV</a></tt> (55; 0% instances), <tt><a href="scn_stb-pos-ADP.html">ADP</a></tt> (9; 0% instances), <tt><a href="scn_stb-pos-INTJ.html">INTJ</a></tt> (1; 0% instances).

### `ADV`

55 <tt><a href="scn_stb-pos-ADV.html">ADV</a></tt> tokens (8% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="scn_stb-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (55; 100%).

`ADV` tokens may have the following values of `ExtPos`:

* `ADP` (11; 20% of non-empty `ExtPos`): <em>nzèmmula, Ntra, appressu, comu, dintra, doppu, jintra, ncapu, supra, sutta</em>
* `ADV` (40; 73% of non-empty `ExtPos`): <em>supra, davanzi, prima, agghiri, ntunnu, 'ntunnu, fora, jintra, Ddoppu, Doppu</em>
* `SCONJ` (4; 7% of non-empty `ExtPos`): <em>doppu, puru</em>

<table>
  <tr><th>Paradigm <i>doppu</i></th><th><tt>ADP</tt></th><th><tt>ADV</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>doppu</em></td><td><em>Ddoppu, Doppu</em></td><td><em>doppu</em></td></tr>
</table>

### `ADP`

9 <tt><a href="scn_stb-pos-ADP.html">ADP</a></tt> tokens (1% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (8; 89% of non-empty `ExtPos`): <em>senza, Sicunnu, a, comu, jintra, ntra</em>
* `SCONJ` (1; 11% of non-empty `ExtPos`): <em>Pi</em>

### `INTJ`

1 <tt><a href="scn_stb-pos-INTJ.html">INTJ</a></tt> tokens (4% of all `INTJ` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `INTJ` and `ExtPos` co-occurred: <tt><a href="scn_stb-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (1; 100%).

`INTJ` tokens may have the following values of `ExtPos`:

* `INTJ` (1; 100% of non-empty `ExtPos`): <em>grazzi</em>

