---
layout: base
title:  'Statistics of ExtPos in UD_Occitan-CorAG'
udver: '2'
---

## Treebank Statistics: UD_Occitan-CorAG: Features: `ExtPos`

This feature is language-specific.
It occurs with 4 different values: `ADP`, `ADV`, `CCONJ`, `SCONJ`.

297 tokens (1%) have a non-empty value of `ExtPos`.
68 types (1%) occur at least once with a non-empty value of `ExtPos`.
1 lemmas (2%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 7 part-of-speech tags: <tt><a href="oc_corag-pos-ADV.html">ADV</a></tt> (143; 0% instances), <tt><a href="oc_corag-pos-ADP.html">ADP</a></tt> (85; 0% instances), <tt><a href="oc_corag-pos-SCONJ.html">SCONJ</a></tt> (37; 0% instances), <tt><a href="oc_corag-pos-PRON.html">PRON</a></tt> (23; 0% instances), <tt><a href="oc_corag-pos-VERB.html">VERB</a></tt> (7; 0% instances), <tt><a href="oc_corag-pos-CCONJ.html">CCONJ</a></tt> (1; 0% instances), <tt><a href="oc_corag-pos-NOUN.html">NOUN</a></tt> (1; 0% instances).

### `ADV`

143 <tt><a href="oc_corag-pos-ADV.html">ADV</a></tt> tokens (8% of all `ADV` tokens) have a non-empty value of `ExtPos`.

`ADV` tokens may have the following values of `ExtPos`:

* `ADP` (15; 10% of non-empty `ExtPos`): <em>fore, no, quant, ensemps, for, senes, sino</em>
* `ADV` (23; 16% of non-empty `ExtPos`): <em>tant, ayssi, no, aixi, ayssi-, certas, sino</em>
* `CCONJ` (8; 6% of non-empty `ExtPos`): <em>ayssi, aixi, ayssi-</em>
* `SCONJ` (97; 68% of non-empty `ExtPos`): <em>ayssi, tant, sino, aixi, apres, puix, ont, aissi, abantz, despuix</em>

### `ADP`

85 <tt><a href="oc_corag-pos-ADP.html">ADP</a></tt> tokens (2% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (24; 28% of non-empty `ExtPos`): <em>entro, per, saub, am, aprees, en, par, sentz</em>
* `ADV` (2; 2% of non-empty `ExtPos`): <em>per</em>
* `SCONJ` (59; 69% of non-empty `ExtPos`): <em>entro, per, ab, en, segont, de, exceptat, a, entre, saub</em>

### `SCONJ`

37 <tt><a href="oc_corag-pos-SCONJ.html">SCONJ</a></tt> tokens (3% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

`SCONJ` tokens may have the following values of `ExtPos`:

* `ADP` (1; 3% of non-empty `ExtPos`): <em>cum</em>
* `ADV` (9; 24% of non-empty `ExtPos`): <em>si, ayssicum</em>
* `SCONJ` (27; 73% of non-empty `ExtPos`): <em>si, jassia, jassie, affin, cum, mentra, sinauque</em>

### `PRON`

23 <tt><a href="oc_corag-pos-PRON.html">PRON</a></tt> tokens (1% of all `PRON` tokens) have a non-empty value of `ExtPos`.

`PRON` tokens may have the following values of `ExtPos`:

* `ADV` (23; 100% of non-empty `ExtPos`): <em>so, so-</em>

### `VERB`

7 <tt><a href="oc_corag-pos-VERB.html">VERB</a></tt> tokens (0% of all `VERB` tokens) have a non-empty value of `ExtPos`.

`VERB` tokens may have the following values of `ExtPos`:

* `ADV` (3; 43% of non-empty `ExtPos`): <em>es</em>
* `SCONJ` (4; 57% of non-empty `ExtPos`): <em>pausat, actendut</em>

### `CCONJ`

1 <tt><a href="oc_corag-pos-CCONJ.html">CCONJ</a></tt> tokens (0% of all `CCONJ` tokens) have a non-empty value of `ExtPos`.

`CCONJ` tokens may have the following values of `ExtPos`:

* `SCONJ` (1; 100% of non-empty `ExtPos`): <em>pero</em>

### `NOUN`

1 <tt><a href="oc_corag-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `ExtPos`.

`NOUN` tokens may have the following values of `ExtPos`:

* `SCONJ` (1; 100% of non-empty `ExtPos`): <em>rason</em>

