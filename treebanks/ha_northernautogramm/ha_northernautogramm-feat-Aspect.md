---
layout: base
title:  'Statistics of Aspect in UD_Hausa-NorthernAutogramm'
udver: '2'
---

## Treebank Statistics: UD_Hausa-NorthernAutogramm: Features: `Aspect`

This feature is universal but the values `Aor`, `Iter`, `PerfBkg`, `PerfNeg`, `ProgBkg`, `ProgLocBkg`, `ProgNeg` are language-specific.
It occurs with 9 different values: `Aor`, `Iter`, `Perf`, `PerfBkg`, `PerfNeg`, `Prog`, `ProgBkg`, `ProgLocBkg`, `ProgNeg`.

656 tokens (17%) have a non-empty value of `Aspect`.
96 types (12%) occur at least once with a non-empty value of `Aspect`.
3 lemmas (1%) occur at least once with a non-empty value of `Aspect`.
The feature is used with 2 part-of-speech tags: <tt><a href="ha_northernautogramm-pos-AUX.html">AUX</a></tt> (631; 16% instances), <tt><a href="ha_northernautogramm-pos-PART.html">PART</a></tt> (25; 1% instances).

### `AUX`

631 <tt><a href="ha_northernautogramm-pos-AUX.html">AUX</a></tt> tokens (99% of all `AUX` tokens) have a non-empty value of `Aspect`.

The most frequent other feature values with which `AUX` and `Aspect` co-occurred: <tt><a href="ha_northernautogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (478; 76%), <tt><a href="ha_northernautogramm-feat-Person.html">Person</a></tt><tt>=3</tt> (374; 59%).

`AUX` tokens may have the following values of `Aspect`:

* `Aor` (112; 18% of non-empty `Aspect`): <em>shì, à, ìn, tà, kà, sù, kì, kù, mù</em>
* `Perf` (99; 16% of non-empty `Aspect`): <em>yaː, kaː, naː, sun, taː, kun, mun, an, kyaː</em>
* `PerfBkg` (263; 42% of non-empty `Aspect`): <em>yac, ankà, sunkà, tac, yaz, yat, yah, yay, yas, yag</em>
* `PerfNeg` (15; 2% of non-empty `Aspect`): <em>bài, bàkà, bàkù, bàn, bàsù, bàtà</em>
* `Prog` (81; 13% of non-empty `Aspect`): <em>shinàː, anàː, tanàː, sunàː, inàː, kanàː, nàː, kunàː</em>
* `ProgBkg` (38; 6% of non-empty `Aspect`): <em>akà, sukà, kukà, kà, shikà, mukà, kakà, nikà, takà</em>
* `ProgLocBkg` (14; 2% of non-empty `Aspect`): <em>takè, shikè, sukè, akè, kukè, kakè</em>
* `ProgNeg` (9; 1% of non-empty `Aspect`): <em>baː'à, baːkà, baːmù, baːkù</em>
* `EMPTY` (4): <em>ani, zâki, zâːku, zâːshi</em>

### `PART`

25 <tt><a href="ha_northernautogramm-pos-PART.html">PART</a></tt> tokens (14% of all `PART` tokens) have a non-empty value of `Aspect`.

The most frequent other feature values with which `PART` and `Aspect` co-occurred: <tt><a href="ha_northernautogramm-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (25; 100%).

`PART` tokens may have the following values of `Aspect`:

* `Iter` (22; 88% of non-empty `Aspect`): <em>ta</em>
* `Prog` (3; 12% of non-empty `Aspect`): <em>nàː</em>
* `EMPTY` (148): <em>ba, nàː, baːbù, gàː, dai, àkwai, bâː, kòː, bàː, naː</em>

## Relations with Agreement in `Aspect`

The 10 most frequent relations where parent and child node agree in `Aspect`:
<tt>AUX --[<tt><a href="ha_northernautogramm-dep-conj.html">conj</a></tt>]--> AUX</tt> (3; 100%),
<tt>PART --[<tt><a href="ha_northernautogramm-dep-conj.html">conj</a></tt>]--> PART</tt> (1; 100%).

