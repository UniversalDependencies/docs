---
layout: base
title:  'Statistics of PronType in UD_Indonesian'
udver: '2'
---

## Treebank Statistics: UD_Indonesian: Features: `PronType`

This feature is universal.
It occurs with 5 different values: `Dem`, `Ind`, `Int`, `Prs`, `Tot`.

5153 tokens (4%) have a non-empty value of `PronType`.
119 types (1%) occur at least once with a non-empty value of `PronType`.
47 lemmas (0%) occur at least once with a non-empty value of `PronType`.
The feature is used with 7 part-of-speech tags: <tt><a href="id-pos-DET.html">DET</a></tt> (3269; 3% instances), <tt><a href="id-pos-PRON.html">PRON</a></tt> (1471; 1% instances), <tt><a href="id-pos-ADV.html">ADV</a></tt> (340; 0% instances), <tt><a href="id-pos-SCONJ.html">SCONJ</a></tt> (26; 0% instances), <tt><a href="id-pos-ADP.html">ADP</a></tt> (23; 0% instances), <tt><a href="id-pos-VERB.html">VERB</a></tt> (15; 0% instances), <tt><a href="id-pos-CCONJ.html">CCONJ</a></tt> (9; 0% instances).

### `DET`

3269 <tt><a href="id-pos-DET.html">DET</a></tt> tokens (82% of all `DET` tokens) have a non-empty value of `PronType`.

`DET` tokens may have the following values of `PronType`:

* `Dem` (1989; 61% of non-empty `PronType`): <em>ini, itu, tersebut, para, inilah, itulah, Begitu, beginilah, itupun</em>
* `Ind` (1011; 31% of non-empty `PronType`): <em>sebuah, seorang, berbagai, suatu, beberapa, tertentu, banyak, sejumlah, sepasang, Banyaknya</em>
* `Int` (40; 1% of non-empty `PronType`): <em>bagaimana, apakah, berapa, Bagaimanakah, apapun, kenapa, manakah</em>
* `Tot` (229; 7% of non-empty `PronType`): <em>semua, setiap, seluruh, segala, tiap, keseluruhan, segenap, semuanya</em>
* `EMPTY` (737): <em>satu, salah, masing, sebagian, sang, kedua, satunya, si, an, kebanyakan</em>

`PronType` seems to be **lexical feature** of `DET`. 100% lemmas (27) occur only with one value of `PronType`.

### `PRON`

1471 <tt><a href="id-pos-PRON.html">PRON</a></tt> tokens (31% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="id-feat-Person.html">Person</a></tt><tt>=3</tt> (885; 60%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (25; 2% of non-empty `PronType`): <em>itu, itulah, Inilah, ini, Para, begitu, Inikah, inipun</em>
* `Ind` (31; 2% of non-empty `PronType`): <em>adanya, seorang, sejumlah, banyak, beberapa</em>
* `Int` (252; 17% of non-empty `PronType`): <em>apa, siapa, apakah, siapakah, bagaimana, kapan, mana, mengapa, apapun, kapankah</em>
* `Prs` (1136; 77% of non-empty `PronType`): <em>ia, mereka, kita, dia, kamu, engkau, aku, kami, beliau, saya</em>
* `Tot` (27; 2% of non-empty `PronType`): <em>keseluruhan, semua, semuanya, segala, segenap, keseluruhannya</em>
* `EMPTY` (3240): <em>yang, dia, dirinya, nya, Perang, bahwa, sendiri, Aku, sini, Vertebrata</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (36) occur only with one value of `PronType`.

### `ADV`

340 <tt><a href="id-pos-ADV.html">ADV</a></tt> tokens (7% of all `ADV` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADV` and `PronType` co-occurred: <tt><a href="id-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (340; 100%), <tt><a href="id-feat-Voice.html">Voice</a></tt><tt>=EMPTY</tt> (340; 100%).

`ADV` tokens may have the following values of `PronType`:

* `Dem` (20; 6% of non-empty `PronType`): <em>begitu</em>
* `Ind` (82; 24% of non-empty `PronType`): <em>banyak</em>
* `Int` (236; 69% of non-empty `PronType`): <em>apakah, bagaimana, mengapa, dimana, kenapa, bagaimanakah, apa, Kemana, Berapa, Mengapakah</em>
* `Tot` (2; 1% of non-empty `PronType`): <em>semua, segalanya</em>
* `EMPTY` (4420): <em>juga, akan, dapat, telah, lebih, kemudian, hanya, bisa, masih, sangat</em>

`PronType` seems to be **lexical feature** of `ADV`. 100% lemmas (12) occur only with one value of `PronType`.

### `SCONJ`

26 <tt><a href="id-pos-SCONJ.html">SCONJ</a></tt> tokens (2% of all `SCONJ` tokens) have a non-empty value of `PronType`.

`SCONJ` tokens may have the following values of `PronType`:

* `Dem` (5; 19% of non-empty `PronType`): <em>begitu, itu</em>
* `Int` (21; 81% of non-empty `PronType`): <em>dimana, apakah, mana, Bagaimana</em>
* `EMPTY` (1449): <em>karena, bahwa, namun, ketika, sehingga, maka, jika, setelah, yaitu, sementara</em>

### `ADP`

23 <tt><a href="id-pos-ADP.html">ADP</a></tt> tokens (0% of all `ADP` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADP` and `PronType` co-occurred: <tt><a href="id-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (23; 100%).

`ADP` tokens may have the following values of `PronType`:

* `Ind` (1; 4% of non-empty `PronType`): <em>Berbagai</em>
* `Int` (22; 96% of non-empty `PronType`): <em>dimana, bagaimana, mana, apakah</em>
* `EMPTY` (11996): <em>di, pada, dari, dengan, untuk, dalam, oleh, sebagai, ke, seperti</em>

### `VERB`

15 <tt><a href="id-pos-VERB.html">VERB</a></tt> tokens (0% of all `VERB` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `VERB` and `PronType` co-occurred: <tt><a href="id-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (15; 100%), <tt><a href="id-feat-Voice.html">Voice</a></tt><tt>=EMPTY</tt> (15; 100%).

`VERB` tokens may have the following values of `PronType`:

* `Dem` (1; 7% of non-empty `PronType`): <em>Tersebutlah</em>
* `Ind` (13; 87% of non-empty `PronType`): <em>adanya</em>
* `Int` (1; 7% of non-empty `PronType`): <em>apa</em>
* `EMPTY` (12187): <em>menjadi, merupakan, memiliki, ada, terletak, digunakan, berada, menggunakan, dikenal, terjadi</em>

### `CCONJ`

9 <tt><a href="id-pos-CCONJ.html">CCONJ</a></tt> tokens (0% of all `CCONJ` tokens) have a non-empty value of `PronType`.

`CCONJ` tokens may have the following values of `PronType`:

* `Dem` (8; 89% of non-empty `PronType`): <em>begitu, itu</em>
* `Int` (1; 11% of non-empty `PronType`): <em>Bagaimana</em>
* `EMPTY` (3650): <em>dan, atau, serta, karena, maupun, tetapi, namun, tapi, ataupun, and</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="id-dep-conj.html">conj</a></tt>]--> PRON</tt> (7; 100%),
<tt>PRON --[<tt><a href="id-dep-parataxis.html">parataxis</a></tt>]--> PRON</tt> (2; 67%),
<tt>PRON --[<tt><a href="id-dep-compound-plur.html">compound:plur</a></tt>]--> PRON</tt> (1; 100%),
<tt>PRON --[<tt><a href="id-dep-dep.html">dep</a></tt>]--> PRON</tt> (1; 100%).

