---
layout: base
title:  'Statistics of ExtPos in UD_Beja-Autogramm'
udver: '2'
---

## Treebank Statistics: UD_Beja-Autogramm: Features: `ExtPos`

This feature is language-specific.
It occurs with 4 different values: `ADV`, `INTJ`, `PRON`, `SCONJ`.

28 tokens (0%) have a non-empty value of `ExtPos`.
8 types (0%) occur at least once with a non-empty value of `ExtPos`.
1 lemmas (0) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 5 part-of-speech tags: <tt><a href="bej_autogramm-pos-PART.html">PART</a></tt> (12; 0% instances), <tt><a href="bej_autogramm-pos-SCONJ.html">SCONJ</a></tt> (12; 0% instances), <tt><a href="bej_autogramm-pos-NOUN.html">NOUN</a></tt> (2; 0% instances), <tt><a href="bej_autogramm-pos-ADP.html">ADP</a></tt> (1; 0% instances), <tt><a href="bej_autogramm-pos-INTJ.html">INTJ</a></tt> (1; 0% instances).

### `PART`

12 <tt><a href="bej_autogramm-pos-PART.html">PART</a></tt> tokens (4% of all `PART` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PART` and `ExtPos` co-occurred: <tt><a href="bej_autogramm-feat-Aspect.html">Aspect</a></tt><tt>=EMPTY</tt> (12; 100%), <tt><a href="bej_autogramm-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (12; 100%), <tt><a href="bej_autogramm-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (12; 100%).

`PART` tokens may have the following values of `ExtPos`:

* `INTJ` (12; 100% of non-empty `ExtPos`): <em>jaː</em>

### `SCONJ`

12 <tt><a href="bej_autogramm-pos-SCONJ.html">SCONJ</a></tt> tokens (2% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `SCONJ` and `ExtPos` co-occurred: <tt><a href="bej_autogramm-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (10; 83%), <tt><a href="bej_autogramm-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (7; 58%).

`SCONJ` tokens may have the following values of `ExtPos`:

* `SCONJ` (12; 100% of non-empty `ExtPos`): <em>=eːb, =eːt, =jeːb, =jeːt</em>

### `NOUN`

2 <tt><a href="bej_autogramm-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `NOUN` and `ExtPos` co-occurred: <tt><a href="bej_autogramm-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (2; 100%), <tt><a href="bej_autogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (2; 100%).

`NOUN` tokens may have the following values of `ExtPos`:

* `ADV` (2; 100% of non-empty `ExtPos`): <em>doːr</em>

### `ADP`

1 <tt><a href="bej_autogramm-pos-ADP.html">ADP</a></tt> tokens (0% of all `ADP` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADP` and `ExtPos` co-occurred: <tt><a href="bej_autogramm-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (1; 100%), <tt><a href="bej_autogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (1; 100%).

`ADP` tokens may have the following values of `ExtPos`:

* `PRON` (1; 100% of non-empty `ExtPos`): <em>hoːj</em>

### `INTJ`

1 <tt><a href="bej_autogramm-pos-INTJ.html">INTJ</a></tt> tokens (2% of all `INTJ` tokens) have a non-empty value of `ExtPos`.

`INTJ` tokens may have the following values of `ExtPos`:

* `INTJ` (1; 100% of non-empty `ExtPos`): <em>iraːnaj</em>

## Relations with Agreement in `ExtPos`

The 10 most frequent relations where parent and child node agree in `ExtPos`:
<tt>SCONJ --[<tt><a href="bej_autogramm-dep-dep-conj.html">dep:conj</a></tt>]--> SCONJ</tt> (1; 100%).

