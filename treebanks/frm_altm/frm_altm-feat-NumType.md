---
layout: base
title:  'Statistics of NumType in UD_Middle_French-ALTM'
udver: '2'
---

## Treebank Statistics: UD_Middle_French-ALTM: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

59 tokens (1%) have a non-empty value of `NumType`.
26 types (2%) occur at least once with a non-empty value of `NumType`.
19 lemmas (3%) occur at least once with a non-empty value of `NumType`.
The feature is used with 3 part-of-speech tags: <tt><a href="frm_altm-pos-ADJ.html">ADJ</a></tt> (34; 0% instances), <tt><a href="frm_altm-pos-NUM.html">NUM</a></tt> (24; 0% instances), <tt><a href="frm_altm-pos-PRON.html">PRON</a></tt> (1; 0% instances).

### `ADJ`

34 <tt><a href="frm_altm-pos-ADJ.html">ADJ</a></tt> tokens (13% of all `ADJ` tokens) have a non-empty value of `NumType`.

`ADJ` tokens may have the following values of `NumType`:

* `Card` (3; 9% of non-empty `NumType`): <em>deu, tieulx</em>
* `Ord` (31; 91% of non-empty `NumType`): <em>premier, deuxième, première, XVe, deuxiesme, cinquième, deuxieme, prémier, second</em>
* `EMPTY` (224): <em>dit, tel, nouvelle, prouchaines, apparente, criminieulx, prouchaine, aultres, prouchain, présens</em>

### `NUM`

24 <tt><a href="frm_altm-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (24; 100% of non-empty `NumType`): <em>IIII, XL., deux, XL, quatre, troys, IX, Neuf, X., XV</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (12) occur only with one value of `NumType`.

### `PRON`

1 <tt><a href="frm_altm-pos-PRON.html">PRON</a></tt> tokens (0% of all `PRON` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `PRON` and `NumType` co-occurred: <tt><a href="frm_altm-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (1; 100%).

`PRON` tokens may have the following values of `NumType`:

* `Ord` (1; 100% of non-empty `NumType`): <em>première</em>
* `EMPTY` (673): <em>il, qui, ce, en, lui, le, que, y, c', je</em>

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="frm_altm-dep-conj.html">conj</a></tt>]--> NUM</tt> (2; 100%).

