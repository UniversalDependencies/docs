---
layout: base
title:  'Statistics of PronType in UD_Ottoman_Turkish-DUDU'
udver: '2'
---

## Treebank Statistics: UD_Ottoman_Turkish-DUDU: Features: `PronType`

This feature is universal.
It occurs with 5 different values: `Art`, `Dem`, `Ind`, `Int`, `Prs`.

36 tokens (4%) have a non-empty value of `PronType`.
22 types (4%) occur at least once with a non-empty value of `PronType`.
13 lemmas (3%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="ota_dudu-pos-PRON.html">PRON</a></tt> (20; 2% instances), <tt><a href="ota_dudu-pos-DET.html">DET</a></tt> (15; 2% instances), <tt><a href="ota_dudu-pos-ADV.html">ADV</a></tt> (1; 0% instances).

### `PRON`

20 <tt><a href="ota_dudu-pos-PRON.html">PRON</a></tt> tokens (87% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="ota_dudu-feat-Number.html">Number</a></tt><tt>=Sing</tt> (13; 65%), <tt><a href="ota_dudu-feat-Person.html">Person</a></tt><tt>=3</tt> (13; 65%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (9; 45% of non-empty `PronType`): <em>bu, bundan, bunuñ, bunı, bunlardan, buña</em>
* `Ind` (1; 5% of non-empty `PronType`): <em>cümlesin</em>
* `Int` (3; 15% of non-empty `PronType`): <em>ne, nereye</em>
* `Prs` (7; 35% of non-empty `PronType`): <em>sen, andan, beni, benim, kendi, ol, onlar</em>
* `EMPTY` (3): <em>cümle, cümlesi, cümlesin</em>

### `DET`

15 <tt><a href="ota_dudu-pos-DET.html">DET</a></tt> tokens (32% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="ota_dudu-feat-Definite.html">Definite</a></tt><tt>=Ind</tt> (8; 53%).

`DET` tokens may have the following values of `PronType`:

* `Art` (15; 100% of non-empty `PronType`): <em>bir, o, cümle, birḳaç, bu, bâżı, her, ol</em>
* `EMPTY` (32): <em>bir, bu, ol, birḳac</em>

### `ADV`

1 <tt><a href="ota_dudu-pos-ADV.html">ADV</a></tt> tokens (3% of all `ADV` tokens) have a non-empty value of `PronType`.

`ADV` tokens may have the following values of `PronType`:

* `Int` (1; 100% of non-empty `PronType`): <em>ne</em>
* `EMPTY` (31): <em>yine, baʿdehû, elbette, hep, bi'l-ḳuvve, biraz, böyle, daha, daḫı, en</em>

