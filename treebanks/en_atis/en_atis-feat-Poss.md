---
layout: base
title:  'Statistics of Poss in UD_English-Atis'
udver: '2'
---

## Treebank Statistics: UD_English-Atis: Features: `Poss`

This feature is universal.
It occurs with 1 different values: `Yes`.

35 tokens (0%) have a non-empty value of `Poss`.
3 types (0%) occur at least once with a non-empty value of `Poss`.
3 lemmas (0%) occur at least once with a non-empty value of `Poss`.
The feature is used with 1 part-of-speech tags: <tt><a href="en_atis-pos-DET.html">DET</a></tt> (35; 0% instances).

### `DET`

35 <tt><a href="en_atis-pos-DET.html">DET</a></tt> tokens (1% of all `DET` tokens) have a non-empty value of `Poss`.

The most frequent other feature values with which `DET` and `Poss` co-occurred: <tt><a href="en_atis-feat-PronType.html">PronType</a></tt><tt>=Prs</tt> (35; 100%).

`DET` tokens may have the following values of `Poss`:

* `Yes` (35; 100% of non-empty `Poss`): <em>your, my, their</em>
* `EMPTY` (4850): <em>the, a, all, what, which, any, an, that, this, no</em>

