---
layout: base
title:  'Statistics of Foreign in UD_Latvian'
udver: '2'
---

## Treebank Statistics: UD_Latvian: Features: `Foreign`

This feature is universal.
It occurs with 1 different values: `Yes`.

296 tokens (0%) have a non-empty value of `Foreign`.
209 types (1%) occur at least once with a non-empty value of `Foreign`.
212 lemmas (2%) occur at least once with a non-empty value of `Foreign`.
The feature is used with 1 part-of-speech tags: <tt><a href="lv-pos-X.html">X</a></tt> (296; 0% instances).

### `X`

296 <tt><a href="lv-pos-X.html">X</a></tt> tokens (99% of all `X` tokens) have a non-empty value of `Foreign`.

`X` tokens may have the following values of `Foreign`:

* `Yes` (296; 100% of non-empty `Foreign`): <em>Imprimatur, fund, Capital, Initiatives, Spatial, Management, education, of, See, Seed</em>
* `EMPTY` (4): <em>_, ., aug</em>

`Foreign` seems to be **lexical feature** of `X`. 100% lemmas (212) occur only with one value of `Foreign`.

## Relations with Agreement in `Foreign`

The 10 most frequent relations where parent and child node agree in `Foreign`:
<tt>X --[<tt><a href="lv-dep-flat-name.html">flat:name</a></tt>]--> X</tt> (63; 100%),
<tt>X --[<tt><a href="lv-dep-flat-foreign.html">flat:foreign</a></tt>]--> X</tt> (54; 100%),
<tt>X --[<tt><a href="lv-dep-flat.html">flat</a></tt>]--> X</tt> (14; 100%),
<tt>X --[<tt><a href="lv-dep-conj.html">conj</a></tt>]--> X</tt> (13; 100%),
<tt>X --[<tt><a href="lv-dep-parataxis.html">parataxis</a></tt>]--> X</tt> (1; 100%).

