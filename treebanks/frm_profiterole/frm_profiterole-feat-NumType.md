---
layout: base
title:  'Statistics of NumType in UD_Middle_French-PROFITEROLE'
udver: '2'
---

## Treebank Statistics: UD_Middle_French-PROFITEROLE: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

742 tokens (1%) have a non-empty value of `NumType`.
108 types (2%) occur at least once with a non-empty value of `NumType`.
61 lemmas (2%) occur at least once with a non-empty value of `NumType`.
The feature is used with 4 part-of-speech tags: <tt><a href="frm_profiterole-pos-NUM.html">NUM</a></tt> (601; 1% instances), <tt><a href="frm_profiterole-pos-ADJ.html">ADJ</a></tt> (77; 0% instances), <tt><a href="frm_profiterole-pos-PRON.html">PRON</a></tt> (38; 0% instances), <tt><a href="frm_profiterole-pos-DET.html">DET</a></tt> (26; 0% instances).

### `NUM`

601 <tt><a href="frm_profiterole-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (601; 100% of non-empty `NumType`): <em>deux, cens, cinq, quatre, trois, mil, CCC, mille, six, cent</em>
* `EMPTY` (2): <em>hommes, jour</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (34) occur only with one value of `NumType`.

### `ADJ`

77 <tt><a href="frm_profiterole-pos-ADJ.html">ADJ</a></tt> tokens (4% of all `ADJ` tokens) have a non-empty value of `NumType`.

`ADJ` tokens may have the following values of `NumType`:

* `Card` (7; 9% of non-empty `NumType`): <em>deux, mil, cens</em>
* `Ord` (70; 91% of non-empty `NumType`): <em>premier, IXe, Ve, premiere, septiesme, tiers, IIIIe, Quint, VIIIe, VIIe</em>
* `EMPTY` (1878): <em>grant, grans, belle, bonne, beau, grand, bon, Sainct, riche, telle</em>

`NumType` seems to be **lexical feature** of `ADJ`. 100% lemmas (29) occur only with one value of `NumType`.

### `PRON`

38 <tt><a href="frm_profiterole-pos-PRON.html">PRON</a></tt> tokens (1% of all `PRON` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `PRON` and `NumType` co-occurred: <tt><a href="frm_profiterole-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (38; 100%).

`PRON` tokens may have the following values of `NumType`:

* `Card` (31; 82% of non-empty `NumType`): <em>deux, trois, vingt, 1, 2, V, cinq, dix, mille, quarante</em>
* `Ord` (7; 18% of non-empty `NumType`): <em>premier, premiers, premiere</em>
* `EMPTY` (7023): <em>il, qui, luy, vous, que, ilz, se, ce, je, nous</em>

`NumType` seems to be **lexical feature** of `PRON`. 100% lemmas (10) occur only with one value of `NumType`.

### `DET`

26 <tt><a href="frm_profiterole-pos-DET.html">DET</a></tt> tokens (0% of all `DET` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `DET` and `NumType` co-occurred: <tt><a href="frm_profiterole-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (26; 100%), <tt><a href="frm_profiterole-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (26; 100%), <tt><a href="frm_profiterole-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (26; 100%).

`DET` tokens may have the following values of `NumType`:

* `Card` (26; 100% of non-empty `NumType`): <em>un, cens, cinq, deux, quatre, trois, VIII, c, cinquante, huyct</em>
* `EMPTY` (6715): <em>le, la, les, l', son, ung, ses, une, sa, ce</em>

`NumType` seems to be **lexical feature** of `DET`. 100% lemmas (11) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="frm_profiterole-dep-flat.html">flat</a></tt>]--> NUM</tt> (177; 100%),
<tt>NUM --[<tt><a href="frm_profiterole-dep-conj.html">conj</a></tt>]--> NUM</tt> (38; 100%),
<tt>DET --[<tt><a href="frm_profiterole-dep-flat.html">flat</a></tt>]--> DET</tt> (5; 100%),
<tt>ADJ --[<tt><a href="frm_profiterole-dep-flat.html">flat</a></tt>]--> NUM</tt> (4; 100%),
<tt>NUM --[<tt><a href="frm_profiterole-dep-conj.html">conj</a></tt>]--> DET</tt> (4; 100%),
<tt>NUM --[<tt><a href="frm_profiterole-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (4; 80%),
<tt>PRON --[<tt><a href="frm_profiterole-dep-flat.html">flat</a></tt>]--> PRON</tt> (2; 100%),
<tt>PRON --[<tt><a href="frm_profiterole-dep-nmod.html">nmod</a></tt>]--> PRON</tt> (2; 100%),
<tt>DET --[<tt><a href="frm_profiterole-dep-conj.html">conj</a></tt>]--> DET</tt> (1; 100%),
<tt>NUM --[<tt><a href="frm_profiterole-dep-flat.html">flat</a></tt>]--> DET</tt> (1; 100%).

