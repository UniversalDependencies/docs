---
layout: base
title:  'Statistics of PronType in UD_Sicilian-STB'
udver: '2'
---

## Treebank Statistics: UD_Sicilian-STB: Features: `PronType`

This feature is universal but the values `Exc` are language-specific.
It occurs with 9 different values: `Art`, `Dem`, `Exc`, `Ind`, `Int`, `Neg`, `Prs`, `Rel`, `Tot`.

2656 tokens (24%) have a non-empty value of `PronType`.
207 types (9%) occur at least once with a non-empty value of `PronType`.
79 lemmas (5%) occur at least once with a non-empty value of `PronType`.
The feature is used with 4 part-of-speech tags: <tt><a href="scn_stb-pos-DET.html">DET</a></tt> (1293; 12% instances), <tt><a href="scn_stb-pos-PRON.html">PRON</a></tt> (1198; 11% instances), <tt><a href="scn_stb-pos-ADV.html">ADV</a></tt> (157; 1% instances), <tt><a href="scn_stb-pos-SCONJ.html">SCONJ</a></tt> (8; 0% instances).

### `DET`

1293 <tt><a href="scn_stb-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="scn_stb-feat-Number.html">Number</a></tt><tt>=Sing</tt> (1033; 80%), <tt><a href="scn_stb-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (779; 60%), <tt><a href="scn_stb-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (670; 52%).

`DET` tokens may have the following values of `PronType`:

* `Art` (1033; 80% of non-empty `PronType`): <em>lu, la, li, un, u, a, l', na, i, 'na</em>
* `Dem` (127; 10% of non-empty `PronType`): <em>stu, sta, ḍḍa, ḍḍu, sti, st', ḍḍi, ssa, ssu, Chistu</em>
* `Exc` (2; 0% of non-empty `PronType`): <em>chi</em>
* `Ind` (46; 4% of non-empty `PronType`): <em>quarchi, tutti, ogni, tanti, tuttu, Nuḍḍa, Tutta, assà, autri, bbeni</em>
* `Int` (6; 0% of non-empty `PronType`): <em>chi, quantu, quanti</em>
* `Prs` (54; 4% of non-empty `PronType`): <em>so, me, ma, sò, mè, sa, ta, Miei, miu, nostra</em>
* `Rel` (1; 0% of non-empty `PronType`): <em>quantu</em>
* `Tot` (24; 2% of non-empty `PronType`): <em>tutti, tuttu, tutta</em>

<table>
  <tr><th>Paradigm <i>tuttu</i></th><th><tt>Tot</tt></th><th><tt>Ind</tt></th></tr>
  <tr><td><tt><tt><a href="scn_stb-feat-Gender.html">Gender</a></tt><tt>=Masc</tt>|<tt><a href="scn_stb-feat-Number.html">Number</a></tt><tt>=Sing</tt></tt></td><td><em>tuttu</em></td><td><em>tuttu</em></td></tr>
  <tr><td><tt><tt><a href="scn_stb-feat-Gender.html">Gender</a></tt><tt>=Masc</tt>|<tt><a href="scn_stb-feat-Number.html">Number</a></tt><tt>=Plur</tt></tt></td><td><em>tutti</em></td><td><em>tutti</em></td></tr>
  <tr><td><tt><tt><a href="scn_stb-feat-Gender.html">Gender</a></tt><tt>=Fem</tt>|<tt><a href="scn_stb-feat-Number.html">Number</a></tt><tt>=Sing</tt></tt></td><td><em>tutta</em></td><td><em>Tutta</em></td></tr>
  <tr><td><tt><tt><a href="scn_stb-feat-Gender.html">Gender</a></tt><tt>=Fem</tt>|<tt><a href="scn_stb-feat-Number.html">Number</a></tt><tt>=Plur</tt></tt></td><td><em>tutti</em></td><td><em>tutti</em></td></tr>
</table>

`PronType` seems to be **lexical feature** of `DET`. 92% lemmas (35) occur only with one value of `PronType`.

### `PRON`

1198 <tt><a href="scn_stb-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="scn_stb-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (702; 59%), <tt><a href="scn_stb-feat-Number.html">Number</a></tt><tt>=Sing</tt> (655; 55%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (45; 4% of non-empty `PronType`): <em>chistu, chiḍḍu, chiḍḍi, chiddu, chidda, chiddi, chissu, chista, Chisti, chiddru</em>
* `Ind` (103; 9% of non-empty `PronType`): <em>nenti, una, tutti, nuḍḍu, àutra, autri, pocu, tuttu, unu, quarchidunu</em>
* `Int` (21; 2% of non-empty `PronType`): <em>chi, zoccu, c', ch', quali, Cosa, qual', quant', quantu, soccu</em>
* `Prs` (910; 76% of non-empty `PronType`): <em>cci, si, ci, cc', mi, lu, nni, ti, la, s'</em>
* `Rel` (119; 10% of non-empty `PronType`): <em>ca, chi, ch', cu', cu’, cui, pari, quantu</em>

<table>
  <tr><th>Paradigm <i>ca</i></th><th><tt>Int</tt></th><th><tt>Rel</tt></th></tr>
  <tr><td><tt></tt></td><td><em>c'</em></td><td><em>ca</em></td></tr>
</table>

`PronType` seems to be **lexical feature** of `PRON`. 92% lemmas (47) occur only with one value of `PronType`.

### `ADV`

157 <tt><a href="scn_stb-pos-ADV.html">ADV</a></tt> tokens (22% of all `ADV` tokens) have a non-empty value of `PronType`.

`ADV` tokens may have the following values of `PronType`:

* `Neg` (157; 100% of non-empty `PronType`): <em>nun, non, 'un, mancu, nimmancu, nu, ni, no</em>
* `EMPTY` (550): <em>cchiù, accussì, sulu, ora, ancora, comu, tantu, sempri, supra, allura</em>

### `SCONJ`

8 <tt><a href="scn_stb-pos-SCONJ.html">SCONJ</a></tt> tokens (3% of all `SCONJ` tokens) have a non-empty value of `PronType`.

`SCONJ` tokens may have the following values of `PronType`:

* `Rel` (8; 100% of non-empty `PronType`): <em>ca, chi</em>
* `EMPTY` (289): <em>ca, chi, si, quannu, comu, picchì, cà, su, ch', pirchì</em>

