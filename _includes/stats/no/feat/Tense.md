

--------------------------------------------------------------------------------

## Treebank Statistics (UD_Norwegian)

This feature is universal.
It occurs with 2 different values: `Past`, `Pres`.

34591 tokens (11%) have a non-empty value of `Tense`.
2515 types (8%) occur at least once with a non-empty value of `Tense`.
1430 lemmas (6%) occur at least once with a non-empty value of `Tense`.
The feature is used with 2 part-of-speech tags: [no-pos/VERB]() (25420; 8% instances), [no-pos/AUX]() (9171; 3% instances).

### `VERB`

25420 [no-pos/VERB]() tokens (63% of all `VERB` tokens) have a non-empty value of `Tense`.

The most frequent other feature values with which `VERB` and `Tense` co-occurred: <tt><a href="VerbForm.html">VerbForm</a>=Fin</tt> (25420; 100%), <tt><a href="Mood.html">Mood</a>=Ind</tt> (25420; 100%).

`VERB` tokens may have the following values of `Tense`:

* `Past` (6841; 27% of non-empty `Tense`): <em>var, ble, hadde, kom, fikk, sa, gikk, tok, gjorde, så</em>
* `Pres` (18579; 73% of non-empty `Tense`): <em>er, har, sier, blir, kommer, går, mener, får, gjør, ser</em>
* `EMPTY` (15119): <em>være, vært, ha, få, bli, ta, gjøre, se, si, gå</em>

<table>
  <tr><th>Paradigm <i>være</i></th><th><tt>Pres</tt></th><th><tt>Past</tt></th></tr>
  <tr><td><tt></tt></td><td><em>er, e</em></td><td><em>var</em></td></tr>
</table>

### `AUX`

9171 [no-pos/AUX]() tokens (92% of all `AUX` tokens) have a non-empty value of `Tense`.

The most frequent other feature values with which `AUX` and `Tense` co-occurred: <tt><a href="VerbForm.html">VerbForm</a>=Fin</tt> (9171; 100%), <tt><a href="Mood.html">Mood</a>=Ind</tt> (9171; 100%).

`AUX` tokens may have the following values of `Tense`:

* `Past` (2064; 23% of non-empty `Tense`): <em>ble, hadde, skulle, ville, kunne, måtte, var, burde, fikk, så</em>
* `Pres` (7107; 77% of non-empty `Tense`): <em>har, kan, vil, skal, er, må, blir, bør, får, trenger</em>
* `EMPTY` (822): <em>ha, bli, blitt, kunne, få, vært, fått, være, måtte, måttet</em>

<table>
  <tr><th>Paradigm <i>ha</i></th><th><tt>Pres</tt></th><th><tt>Past</tt></th></tr>
  <tr><td><tt></tt></td><td><em>har</em></td><td><em>hadde</em></td></tr>
</table>

## Relations with Agreement in `Tense`

The 10 most frequent relations where parent and child node agree in `Tense`:
<tt>VERB --[<a href="../dep/conj.html">conj</a>]--> VERB</tt> (1517; 66%).

