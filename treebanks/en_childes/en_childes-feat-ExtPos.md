---
layout: base
title:  'Statistics of ExtPos in UD_English-CHILDES'
udver: '2'
---

## Treebank Statistics: UD_English-CHILDES: Features: `ExtPos`

This feature is language-specific.
It occurs with 2 different values: `ADP`, `ADV`.

151 tokens (0%) have a non-empty value of `ExtPos`.
27 types (0%) occur at least once with a non-empty value of `ExtPos`.
18 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 5 part-of-speech tags: <tt><a href="en_childes-pos-ADV.html">ADV</a></tt> (85; 0% instances), <tt><a href="en_childes-pos-NOUN.html">NOUN</a></tt> (36; 0% instances), <tt><a href="en_childes-pos-ADP.html">ADP</a></tt> (23; 0% instances), <tt><a href="en_childes-pos-PRON.html">PRON</a></tt> (5; 0% instances), <tt><a href="en_childes-pos-ADJ.html">ADJ</a></tt> (2; 0% instances).

### `ADV`

85 <tt><a href="en_childes-pos-ADV.html">ADV</a></tt> tokens (1% of all `ADV` tokens) have a non-empty value of `ExtPos`.

`ADV` tokens may have the following values of `ExtPos`:

* `ADV` (85; 100% of non-empty `ExtPos`): <em>how, as, instead, upside, kind, so, at, more, of, rather</em>

`ExtPos` seems to be **lexical feature** of `ADV`. 100% lemmas (12) occur only with one value of `ExtPos`.

### `NOUN`

36 <tt><a href="en_childes-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `ExtPos`.

`NOUN` tokens may have the following values of `ExtPos`:

* `ADV` (36; 100% of non-empty `ExtPos`): <em>kind, upside, sort</em>

### `ADP`

23 <tt><a href="en_childes-pos-ADP.html">ADP</a></tt> tokens (0% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (1; 4% of non-empty `ExtPos`): <em>up</em>
* `ADV` (22; 96% of non-empty `ExtPos`): <em>of, out, because, at, in</em>

### `PRON`

5 <tt><a href="en_childes-pos-PRON.html">PRON</a></tt> tokens (0% of all `PRON` tokens) have a non-empty value of `ExtPos`.

`PRON` tokens may have the following values of `ExtPos`:

* `ADV` (5; 100% of non-empty `ExtPos`): <em>what</em>

### `ADJ`

2 <tt><a href="en_childes-pos-ADJ.html">ADJ</a></tt> tokens (0% of all `ADJ` tokens) have a non-empty value of `ExtPos`.

`ADJ` tokens may have the following values of `ExtPos`:

* `ADV` (2; 100% of non-empty `ExtPos`): <em>Less, upside</em>

## Relations with Agreement in `ExtPos`

The 10 most frequent relations where parent and child node agree in `ExtPos`:
<tt>NOUN --[<tt><a href="en_childes-dep-conj.html">conj</a></tt>]--> NOUN</tt> (2; 100%),
<tt>NOUN --[<tt><a href="en_childes-dep-reparandum.html">reparandum</a></tt>]--> NOUN</tt> (2; 67%),
<tt>ADP --[<tt><a href="en_childes-dep-reparandum.html">reparandum</a></tt>]--> ADP</tt> (1; 100%).

