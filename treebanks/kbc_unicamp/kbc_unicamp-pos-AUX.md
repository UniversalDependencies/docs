---
layout: base
title:  'Statistics of AUX in UD_Kadiweu-Unicamp'
udver: '2'
---

## Treebank Statistics: UD_Kadiweu-Unicamp: POS Tags: `AUX`

There are 1 `AUX` lemmas (1%), 1 `AUX` types (1%) and 2 `AUX` tokens (1%).
Out of 11 observed tags, the rank of `AUX` is: 9 in number of lemmas, 9 in number of types and 11 in number of tokens.

The 10 most frequent `AUX` lemmas: <em>jaG</em>

The 10 most frequent `AUX` types:  <em>ja</em>

The 10 most frequent ambiguous lemmas: 

The 10 most frequent ambiguous types:  



## Morphology

The form / lemma ratio of `AUX` is 1.000000 (the average of all parts of speech is 1.209877).

The 1st highest number of forms (1) was observed with the lemma “jaG”: <em>ja</em>.

`AUX` occurs with 1 features: <tt><a href="kbc_unicamp-feat-Aspect.html">Aspect</a></tt> (2; 100% instances)

`AUX` occurs with 1 feature-value pairs: `Aspect=Perf`

`AUX` occurs with 1 feature combinations.
The most frequent feature combination is `Aspect=Perf` (2 tokens).
Examples: <em>ja</em>


## Relations

`AUX` nodes are attached to their parents using 1 different relations: <tt><a href="kbc_unicamp-dep-aux.html">aux</a></tt> (2; 100% instances)

Parents of `AUX` nodes belong to 1 different parts of speech: <tt><a href="kbc_unicamp-pos-VERB.html">VERB</a></tt> (2; 100% instances)

2 (100%) `AUX` nodes are leaves.

The highest child degree of a `AUX` node is 0.

