---
layout: base
title:  'Statistics of NumType in UD_French'
udver: '2'
---

## Treebank Statistics: UD_French: Features: `NumType`

This feature is universal.
It occurs with 1 different values: `Ord`.

200 tokens (0%) have a non-empty value of `NumType`.
21 types (0%) occur at least once with a non-empty value of `NumType`.
21 lemmas (0%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="fr-pos-ADJ.html">ADJ</a></tt> (200; 0% instances).

### `ADJ`

200 <tt><a href="fr-pos-ADJ.html">ADJ</a></tt> tokens (1% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="fr-feat-Number.html">Number</a></tt><tt>=Sing</tt> (198; 99%), <tt><a href="fr-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (194; 97%).

`ADJ` tokens may have the following values of `NumType`:

* `Ord` (200; 100% of non-empty `NumType`): <em>XIXe, XXe, XVIIe, XVIe, XVIIIe, XIIe, XVe, XIe, XIIIe, VIe</em>
* `EMPTY` (22779): <em>première, premier, français, autres, même, française, grande, grand, nouveau, autre</em>

`NumType` seems to be **lexical feature** of `ADJ`. 100% lemmas (21) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>ADJ --[<tt><a href="fr-dep-conj.html">conj</a></tt>]--> ADJ</tt> (10; 100%).

