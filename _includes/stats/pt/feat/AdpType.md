

--------------------------------------------------------------------------------

## Treebank Statistics (UD_Portuguese)

This feature is language-specific.
It occurs with 1 different values: `Prep`.

35959 tokens (16%) have a non-empty value of `AdpType`.
124 types (0%) occur at least once with a non-empty value of `AdpType`.
100 lemmas (1%) occur at least once with a non-empty value of `AdpType`.
The feature is used with 5 part-of-speech tags: [pt-pos/ADP]() (35492; 16% instances), [pt-pos/ADV]() (319; 0% instances), [pt-pos/PRON]() (108; 0% instances), [pt-pos/ADJ]() (39; 0% instances), [pt-pos/SCONJ]() (1; 0% instances).

### `ADP`

35492 [pt-pos/ADP]() tokens (100% of all `ADP` tokens) have a non-empty value of `AdpType`.

`ADP` tokens may have the following values of `AdpType`:

* `Prep` (35492; 100% of non-empty `AdpType`): <em>de, em, a, por, para, com, entre, sobre, do, sem</em>

`AdpType` seems to be **lexical feature** of `ADP`. 100% lemmas (82) occur only with one value of `AdpType`.

### `ADV`

319 [pt-pos/ADV]() tokens (4% of all `ADV` tokens) have a non-empty value of `AdpType`.

The most frequent other feature values with which `ADV` and `AdpType` co-occurred: <tt><a href="NumType.html">NumType</a>=EMPTY</tt> (318; 100%), <tt><a href="PronType.html">PronType</a>=EMPTY</tt> (318; 100%).

`ADV` tokens may have the following values of `AdpType`:

* `Prep` (319; 100% of non-empty `AdpType`): <em>além, apesar, quanto, dentro, cerca, acima, diante, acerca, abaixo, mais</em>

`AdpType` seems to be **lexical feature** of `ADV`. 100% lemmas (16) occur only with one value of `AdpType`.

### `PRON`

108 [pt-pos/PRON]() tokens (1% of all `PRON` tokens) have a non-empty value of `AdpType`.

The most frequent other feature values with which `PRON` and `AdpType` co-occurred: <tt><a href="Reflex.html">Reflex</a>=EMPTY</tt> (108; 100%), <tt><a href="Person.html">Person</a>=EMPTY</tt> (108; 100%), <tt><a href="Number.html">Number</a>=EMPTY</tt> (108; 100%), <tt><a href="PronType.html">PronType</a>=Ind,Neg,Tot</tt> (108; 100%), <tt><a href="Case.html">Case</a>=EMPTY</tt> (108; 100%), <tt><a href="Gender.html">Gender</a>=EMPTY</tt> (108; 100%).

`PRON` tokens may have the following values of `AdpType`:

* `Prep` (108; 100% of non-empty `AdpType`): <em>de, pelo</em>

### `ADJ`

39 [pt-pos/ADJ]() tokens (0% of all `ADJ` tokens) have a non-empty value of `AdpType`.

The most frequent other feature values with which `ADJ` and `AdpType` co-occurred: <tt><a href="Number.html">Number</a>=EMPTY</tt> (39; 100%), <tt><a href="Gender.html">Gender</a>=EMPTY</tt> (39; 100%).

`ADJ` tokens may have the following values of `AdpType`:

* `Prep` (39; 100% of non-empty `AdpType`): <em>devido, próximo, junto</em>

### `SCONJ`

1 [pt-pos/SCONJ]() tokens (0% of all `SCONJ` tokens) have a non-empty value of `AdpType`.

`SCONJ` tokens may have the following values of `AdpType`:

* `Prep` (1; 100% of non-empty `AdpType`): <em>como</em>

## Relations with Agreement in `AdpType`

The 10 most frequent relations where parent and child node agree in `AdpType`:
<tt>ADP --[<a href="../dep/mwe.html">mwe</a>]--> ADP</tt> (304; 100%),
<tt>ADV --[<a href="../dep/mwe.html">mwe</a>]--> ADP</tt> (250; 75%),
<tt>ADJ --[<a href="../dep/mwe.html">mwe</a>]--> ADP</tt> (16; 100%),
<tt>ADP --[<a href="../dep/case.html">case</a>]--> ADP</tt> (12; 100%),
<tt>ADP --[<a href="../dep/nmod.html">nmod</a>]--> ADP</tt> (2; 100%),
<tt>ADP --[<a href="../dep/advmod.html">advmod</a>]--> ADP</tt> (1; 100%).

