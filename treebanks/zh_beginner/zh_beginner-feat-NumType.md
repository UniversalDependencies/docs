---
layout: base
title:  'Statistics of NumType in UD_Chinese-Beginner'
udver: '2'
---

## Treebank Statistics: UD_Chinese-Beginner: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

599 tokens (3%) have a non-empty value of `NumType`.
31 types (2%) occur at least once with a non-empty value of `NumType`.
31 lemmas (2%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="zh_beginner-pos-NUM.html">NUM</a></tt> (599; 3% instances).

### `NUM`

599 <tt><a href="zh_beginner-pos-NUM.html">NUM</a></tt> tokens (94% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (592; 99% of non-empty `NumType`): 一、 两、 十、 三、 几、 五、 1、 二、 八、 四
* `Ord` (7; 1% of non-empty `NumType`): 第
* `EMPTY` (41): 一、 70、 万、 三、 二、 五、 半

<table>
  <tr><th>Paradigm <i>第</i></th><th><tt>Card</tt></th><th><tt>Ord</tt></th></tr>
  <tr><td><tt></tt></td><td>第</td><td>第</td></tr>
</table>

`NumType` seems to be **lexical feature** of `NUM`. 97% lemmas (30) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="zh_beginner-dep-flat.html">flat</a></tt>]--> NUM</tt> (105; 100%),
<tt>NUM --[<tt><a href="zh_beginner-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (12; 75%),
<tt>NUM --[<tt><a href="zh_beginner-dep-conj.html">conj</a></tt>]--> NUM</tt> (10; 100%).

