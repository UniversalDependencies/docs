---
layout: base
title:  'Statistics of NumType in UD_Maltese'
udver: '2'
---

## Treebank Statistics: UD_Maltese: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Gen`.
Some words have combined values of the feature; 1 combinations have been observed: `Card|Gen`.

46 tokens (2%) have a non-empty value of `NumType`.
37 types (4%) occur at least once with a non-empty value of `NumType`.
1 lemmas (0) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="mt-pos-NUM.html">NUM</a></tt> (42; 2% instances), <tt><a href="mt-pos-DET.html">DET</a></tt> (4; 0% instances).

### `NUM`

42 <tt><a href="mt-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="mt-feat-NumValue.html">NumValue</a></tt><tt>=EMPTY</tt> (36; 86%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (36; 86% of non-empty `NumType`): 25 12 2010 żewġ 0.50 1 11 13 14 15
* `Card,Gen` (6; 14% of non-empty `NumType`): wieħed waħda

### `DET`

4 <tt><a href="mt-pos-DET.html">DET</a></tt> tokens (2% of all `DET` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `DET` and `NumType` co-occurred: <tt><a href="mt-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (4; 100%), <tt><a href="mt-feat-PronType.html">PronType</a></tt><tt>=Ind</tt> (4; 100%).

`DET` tokens may have the following values of `NumType`:

* `Card` (4; 100% of non-empty `NumType`): ftit Għaldaqstant ħafna
* `EMPTY` (262): l- il- dan is- it- tagħhom din tagħna tiegħu dawn

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="mt-dep-compound.html">compound</a></tt>]--> NUM</tt> (1; 100%),
<tt>NUM --[<tt><a href="mt-dep-conj.html">conj</a></tt>]--> NUM</tt> (1; 100%).

