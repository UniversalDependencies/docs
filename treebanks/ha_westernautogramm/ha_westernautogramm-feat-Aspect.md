---
layout: base
title:  'Statistics of Aspect in UD_Hausa-WesternAutogramm'
udver: '2'
---

## Treebank Statistics: UD_Hausa-WesternAutogramm: Features: `Aspect`

This feature is universal but the values `Iter` are language-specific.
It occurs with 3 different values: `Iter`, `Perf`, `Prog`.

1509 tokens (11%) have a non-empty value of `Aspect`.
120 types (7%) occur at least once with a non-empty value of `Aspect`.
3 lemmas (0%) occur at least once with a non-empty value of `Aspect`.
The feature is used with 2 part-of-speech tags: <tt><a href="ha_westernautogramm-pos-AUX.html">AUX</a></tt> (1478; 11% instances), <tt><a href="ha_westernautogramm-pos-PART.html">PART</a></tt> (31; 0% instances).

### `AUX`

1478 <tt><a href="ha_westernautogramm-pos-AUX.html">AUX</a></tt> tokens (60% of all `AUX` tokens) have a non-empty value of `Aspect`.

The most frequent other feature values with which `AUX` and `Aspect` co-occurred: <tt><a href="ha_westernautogramm-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (1478; 100%), <tt><a href="ha_westernautogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (1337; 90%), <tt><a href="ha_westernautogramm-feat-Person.html">Person</a></tt><tt>=3</tt> (1058; 72%).

`AUX` tokens may have the following values of `Aspect`:

* `Perf` (1069; 72% of non-empty `Aspect`): <em>ankà, sunkà, yaː, yac, an, taː, tac, yat, yay, yab</em>
* `Prog` (409; 28% of non-empty `Aspect`): <em>bâːshi, sunàː, shinàː, bâːta, kà, nàː, tanàː, akà, anàː, bâːsu</em>
* `EMPTY` (996): <em>à, shì, tà, sù, neː, kà, zâːshi, ìn, zâː'a, kì</em>

### `PART`

31 <tt><a href="ha_westernautogramm-pos-PART.html">PART</a></tt> tokens (8% of all `PART` tokens) have a non-empty value of `Aspect`.

The most frequent other feature values with which `PART` and `Aspect` co-occurred: <tt><a href="ha_westernautogramm-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (31; 100%), <tt><a href="ha_westernautogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (31; 100%), <tt><a href="ha_westernautogramm-feat-PartType.html">PartType</a></tt><tt>=Aspect</tt> (31; 100%), <tt><a href="ha_westernautogramm-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (31; 100%).

`PART` tokens may have the following values of `Aspect`:

* `Iter` (31; 100% of non-empty `Aspect`): <em>ta</em>
* `EMPTY` (343): <em>na, ba, kuma, kâu, mài, dai, ta, bâː, maː, àkwai</em>

## Relations with Agreement in `Aspect`

The 10 most frequent relations where parent and child node agree in `Aspect`:
<tt>AUX --[<tt><a href="ha_westernautogramm-dep-conj.html">conj</a></tt>]--> AUX</tt> (1; 100%).

