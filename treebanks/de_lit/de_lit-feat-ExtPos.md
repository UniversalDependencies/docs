---
layout: base
title:  'Statistics of ExtPos in UD_German-LIT'
udver: '2'
---

## Treebank Statistics: UD_German-LIT: Features: `ExtPos`

This feature is language-specific.
It occurs with 1 different values: `ADV`.

5 tokens (0%) have a non-empty value of `ExtPos`.
4 types (0%) occur at least once with a non-empty value of `ExtPos`.
4 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 3 part-of-speech tags: <tt><a href="de_lit-pos-ADP.html">ADP</a></tt> (2; 0% instances), <tt><a href="de_lit-pos-ADV.html">ADV</a></tt> (2; 0% instances), <tt><a href="de_lit-pos-DET.html">DET</a></tt> (1; 0% instances).

### `ADP`

2 <tt><a href="de_lit-pos-ADP.html">ADP</a></tt> tokens (0% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADV` (2; 100% of non-empty `ExtPos`): <em>In, durch</em>

### `ADV`

2 <tt><a href="de_lit-pos-ADV.html">ADV</a></tt> tokens (0% of all `ADV` tokens) have a non-empty value of `ExtPos`.

`ADV` tokens may have the following values of `ExtPos`:

* `ADV` (2; 100% of non-empty `ExtPos`): <em>mehr, so</em>

### `DET`

1 <tt><a href="de_lit-pos-DET.html">DET</a></tt> tokens (0% of all `DET` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `DET` and `ExtPos` co-occurred: <tt><a href="de_lit-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (1; 100%), <tt><a href="de_lit-feat-NumType.html">NumType</a></tt><tt>=EMPTY</tt> (1; 100%), <tt><a href="de_lit-feat-PronType.html">PronType</a></tt><tt>=Ind</tt> (1; 100%).

`DET` tokens may have the following values of `ExtPos`:

* `ADV` (1; 100% of non-empty `ExtPos`): <em>mehr</em>

