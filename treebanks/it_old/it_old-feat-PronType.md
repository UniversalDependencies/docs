---
layout: base
title:  'Statistics of PronType in UD_Italian-Old'
udver: '2'
---

## Treebank Statistics: UD_Italian-Old: Features: `PronType`

This feature is universal but the values `Exc` are language-specific.
It occurs with 9 different values: `Art`, `Dem`, `Exc`, `Ind`, `Int`, `Neg`, `Prs`, `Rel`, `Tot`.

20096 tokens (24%) have a non-empty value of `PronType`.
302 types (3%) occur at least once with a non-empty value of `PronType`.
97 lemmas (2%) occur at least once with a non-empty value of `PronType`.
The feature is used with 5 part-of-speech tags: <tt><a href="it_old-pos-DET.html">DET</a></tt> (9537; 12% instances), <tt><a href="it_old-pos-PRON.html">PRON</a></tt> (9522; 12% instances), <tt><a href="it_old-pos-ADV.html">ADV</a></tt> (1032; 1% instances), <tt><a href="it_old-pos-ADJ.html">ADJ</a></tt> (4; 0% instances), <tt><a href="it_old-pos-SCONJ.html">SCONJ</a></tt> (1; 0% instances).

### `DET`

9537 <tt><a href="it_old-pos-DET.html">DET</a></tt> tokens (98% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="it_old-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (8266; 87%), <tt><a href="it_old-feat-Number.html">Number</a></tt><tt>=Sing</tt> (7196; 75%), <tt><a href="it_old-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (6126; 64%), <tt><a href="it_old-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (5391; 57%).

`DET` tokens may have the following values of `PronType`:

* `Art` (6431; 67% of non-empty `PronType`): <em>il, la, l', 'l, li, lo, le, i, un, una</em>
* `Dem` (827; 9% of non-empty `PronType`): <em>le, quel, quella, questa, questo, quell', quelle, quello, stesso, queste</em>
* `Exc` (11; 0% of non-empty `PronType`): <em>che, quanto, quanta, Qual, Quante, quali, quanti</em>
* `Ind` (650; 7% of non-empty `PronType`): <em>altra, altro, ogne, alcun, altrui, altri, una, alcuna, tutte, tanta</em>
* `Int` (56; 1% of non-empty `PronType`): <em>che, qual, quai, quale, quali, quant', quanto</em>
* `Prs` (1273; 13% of non-empty `PronType`): <em>mio, sua, suo, mia, lor, tua, tuo, sue, suoi, miei</em>
* `Rel` (83; 1% of non-empty `PronType`): <em>qual, quale, quali, quante, quanto, cui, quanta, quanti, quantunque</em>
* `Tot` (206; 2% of non-empty `PronType`): <em>tutto, ogne, tutti, tutte, tutta, amendue, ambedue, tutt', ogn', ogni</em>
* `EMPTY` (182): <em>un, tal, più, una, uno, poco, men, tale, tutta, l</em>

<table>
  <tr><th>Paradigm <i>quale</i></th><th><tt>Int</tt></th><th><tt>Rel</tt></th><th><tt>Ind</tt></th><th><tt>Exc</tt></th></tr>
  <tr><td><tt>_</tt></td><td></td><td><em>qual, quale, quali</em></td><td></td><td></td></tr>
  <tr><td><tt><tt><a href="it_old-feat-Number.html">Number</a></tt><tt>=Sing</tt></tt></td><td><em>qual, quale</em></td><td><em>qual, quale</em></td><td><em>qual</em></td><td><em>Qual</em></td></tr>
  <tr><td><tt><tt><a href="it_old-feat-Number.html">Number</a></tt><tt>=Plur</tt></tt></td><td><em>quai, quali</em></td><td><em>quali</em></td><td></td><td><em>quali</em></td></tr>
</table>

### `PRON`

9522 <tt><a href="it_old-pos-PRON.html">PRON</a></tt> tokens (99% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="it_old-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (8129; 85%), <tt><a href="it_old-feat-Reflex.html">Reflex</a></tt><tt>=EMPTY</tt> (8071; 85%), <tt><a href="it_old-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (7312; 77%), <tt><a href="it_old-feat-Clitic.html">Clitic</a></tt><tt>=EMPTY</tt> (6381; 67%), <tt><a href="it_old-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (4960; 52%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (657; 7% of non-empty `PronType`): <em>ciò, quel, colui, quei, quella, questi, questo, questa, quelli, color</em>
* `Ind` (575; 6% of non-empty `PronType`): <em>altro, un, altri, ciascun, altrui, qual, una, alcun, altra, uno</em>
* `Int` (73; 1% of non-empty `PronType`): <em>che, cui, qual, quai, quant', quali</em>
* `Prs` (6086; 64% of non-empty `PronType`): <em>io, si, mi, tu, me, ti, noi, s', m', i'</em>
* `Rel` (2097; 22% of non-empty `PronType`): <em>che, ch', chi, cui, c', qual, ond', quale, dove, cu'</em>
* `Tot` (34; 0% of non-empty `PronType`): <em>tutti, amendue, tutto, tutte, 'mbedue, ambedue, ambedui, intrambi</em>
* `EMPTY` (115): <em>ne, ove, onde, ov', ci, n', om, vi, tal, vos</em>

<table>
  <tr><th>Paradigm <i>che</i></th><th><tt>Int</tt></th><th><tt>Rel</tt></th><th><tt>Ind</tt></th></tr>
  <tr><td><tt></tt></td><td><em>che</em></td><td><em>che, ch', c'</em></td><td><em>che</em></td></tr>
</table>

`PronType` seems to be **lexical feature** of `PRON`. 94% lemmas (80) occur only with one value of `PronType`.

### `ADV`

1032 <tt><a href="it_old-pos-ADV.html">ADV</a></tt> tokens (14% of all `ADV` tokens) have a non-empty value of `PronType`.

`ADV` tokens may have the following values of `PronType`:

* `Ind` (1; 0% of non-empty `PronType`): <em>alquanto</em>
* `Neg` (1031; 100% of non-empty `PronType`): <em>non</em>
* `EMPTY` (6118): <em>sì, più, come, poi, là, già, così, qui, pur, or</em>

### `ADJ`

4 <tt><a href="it_old-pos-ADJ.html">ADJ</a></tt> tokens (0% of all `ADJ` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADJ` and `PronType` co-occurred: <tt><a href="it_old-feat-Number.html">Number</a></tt><tt>=Sing</tt> (4; 100%).

`ADJ` tokens may have the following values of `PronType`:

* `Dem` (3; 75% of non-empty `PronType`): <em>quella, tal</em>
* `Ind` (1; 25% of non-empty `PronType`): <em>altro</em>
* `EMPTY` (3350): <em>gran, dolce, buon, tal, alto, primo, prima, maggior, grande, novo</em>

### `SCONJ`

1 <tt><a href="it_old-pos-SCONJ.html">SCONJ</a></tt> tokens (0% of all `SCONJ` tokens) have a non-empty value of `PronType`.

`SCONJ` tokens may have the following values of `PronType`:

* `Rel` (1; 100% of non-empty `PronType`): <em>che</em>
* `EMPTY` (2393): <em>che, ch', se, quando, ché, s', perché, come, quand', perch'</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="it_old-dep-orphan.html">orphan</a></tt>]--> PRON</tt> (70; 85%),
<tt>PRON --[<tt><a href="it_old-dep-conj.html">conj</a></tt>]--> PRON</tt> (27; 75%),
<tt>DET --[<tt><a href="it_old-dep-conj.html">conj</a></tt>]--> DET</tt> (3; 100%),
<tt>DET --[<tt><a href="it_old-dep-flat-redup.html">flat:redup</a></tt>]--> DET</tt> (2; 100%),
<tt>DET --[<tt><a href="it_old-dep-conj.html">conj</a></tt>]--> PRON</tt> (1; 100%),
<tt>DET --[<tt><a href="it_old-dep-fixed.html">fixed</a></tt>]--> PRON</tt> (1; 100%),
<tt>PRON --[<tt><a href="it_old-dep-fixed.html">fixed</a></tt>]--> PRON</tt> (1; 100%),
<tt>PRON --[<tt><a href="it_old-dep-flat-redup.html">flat:redup</a></tt>]--> PRON</tt> (1; 100%),
<tt>PRON --[<tt><a href="it_old-dep-nmod-lmod.html">nmod:lmod</a></tt>]--> PRON</tt> (1; 100%),
<tt>PRON --[<tt><a href="it_old-dep-obl-lmod.html">obl:lmod</a></tt>]--> DET</tt> (1; 100%).

