

--------------------------------------------------------------------------------

## Treebank Statistics (UD_Estonian)

This feature is language-specific.
It occurs with 2 different values: `Digit`, `Word`.

135 tokens (1%) have a non-empty value of `NumForm`.
90 types (2%) occur at least once with a non-empty value of `NumForm`.
80 lemmas (2%) occur at least once with a non-empty value of `NumForm`.
The feature is used with 2 part-of-speech tags: [et-pos/NUM]() (118; 1% instances), [et-pos/ADJ]() (17; 0% instances).

### `NUM`

118 [et-pos/NUM]() tokens (98% of all `NUM` tokens) have a non-empty value of `NumForm`.

The most frequent other feature values with which `NUM` and `NumForm` co-occurred: <tt><a href="NumType.html">NumType</a>=Card</tt> (103; 87%), <tt><a href="Number.html">Number</a>=Sing</tt> (85; 72%).

`NUM` tokens may have the following values of `NumForm`:

* `Digit` (29; 25% of non-empty `NumForm`): <em>1992., 1991., 100, 110, 120, 15.40, 1875, 1959., 1977, 1990</em>
* `Word` (89; 75% of non-empty `NumForm`): <em>kaks, kahest, kahe, kolmandat, neli, paar, kahte, kolme, kolmekümne, kuussada</em>

`NumForm` seems to be **lexical feature** of `NUM`. 100% lemmas (70) occur only with one value of `NumForm`.

### `ADJ`

17 [et-pos/ADJ]() tokens (4% of all `ADJ` tokens) have a non-empty value of `NumForm`.

The most frequent other feature values with which `ADJ` and `NumForm` co-occurred: <tt><a href="VerbForm.html">VerbForm</a>=EMPTY</tt> (17; 100%), <tt><a href="Degree.html">Degree</a>=EMPTY</tt> (17; 100%), <tt><a href="Number.html">Number</a>=Sing</tt> (16; 94%).

`ADJ` tokens may have the following values of `NumForm`:

* `Word` (17; 100% of non-empty `NumForm`): <em>teise, kolmandale, kolmandat, Esimene, Kolmas, Teisel, esimese, esimesest, esimest, esimestes</em>

`NumForm` seems to be **lexical feature** of `ADJ`. 100% lemmas (12) occur only with one value of `NumForm`.

## Relations with Agreement in `NumForm`

The 10 most frequent relations where parent and child node agree in `NumForm`:
<tt>NUM --[<a href="../dep/compound.html">compound</a>]--> NUM</tt> (4; 57%),
<tt>NUM --[<a href="../dep/conj.html">conj</a>]--> NUM</tt> (2; 100%).

