---
layout: base
title:  'Statistics of ExtPos in UD_Greek-GDT'
udver: '2'
---

## Treebank Statistics: UD_Greek-GDT: Features: `ExtPos`

This feature is language-specific.
It occurs with 4 different values: `ADP`, `ADV`, `CCONJ`, `SCONJ`.

85 tokens (0%) have a non-empty value of `ExtPos`.
17 types (0%) occur at least once with a non-empty value of `ExtPos`.
12 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 4 part-of-speech tags: <tt><a href="el_gdt-pos-ADV.html">ADV</a></tt> (39; 0% instances), <tt><a href="el_gdt-pos-ADP.html">ADP</a></tt> (30; 0% instances), <tt><a href="el_gdt-pos-SCONJ.html">SCONJ</a></tt> (15; 0% instances), <tt><a href="el_gdt-pos-CCONJ.html">CCONJ</a></tt> (1; 0% instances).

### `ADV`

39 <tt><a href="el_gdt-pos-ADV.html">ADV</a></tt> tokens (1% of all `ADV` tokens) have a non-empty value of `ExtPos`.

`ADV` tokens may have the following values of `ExtPos`:

* `ADP` (15; 38% of non-empty `ExtPos`): <em>όσον, Όσο</em>
* `ADV` (4; 10% of non-empty `ExtPos`): <em>ως, ούτως</em>
* `SCONJ` (20; 51% of non-empty `ExtPos`): <em>έτσι, δεδομένου, ούτως, παρόλο, Όσο</em>

<table>
  <tr><th>Paradigm <i>όσο</i></th><th><tt>ADP</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>όσον, Όσο</em></td><td><em>Όσο</em></td></tr>
</table>

### `ADP`

30 <tt><a href="el_gdt-pos-ADP.html">ADP</a></tt> tokens (1% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (25; 83% of non-empty `ExtPos`): <em>ως, απ', από</em>
* `SCONJ` (5; 17% of non-empty `ExtPos`): <em>έως, μέχρι</em>

### `SCONJ`

15 <tt><a href="el_gdt-pos-SCONJ.html">SCONJ</a></tt> tokens (1% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

`SCONJ` tokens may have the following values of `ExtPos`:

* `SCONJ` (15; 100% of non-empty `ExtPos`): <em>αν, μιας</em>

### `CCONJ`

1 <tt><a href="el_gdt-pos-CCONJ.html">CCONJ</a></tt> tokens (0% of all `CCONJ` tokens) have a non-empty value of `ExtPos`.

`CCONJ` tokens may have the following values of `ExtPos`:

* `CCONJ` (1; 100% of non-empty `ExtPos`): <em>πλην</em>

