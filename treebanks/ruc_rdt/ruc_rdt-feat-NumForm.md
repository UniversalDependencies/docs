---
layout: base
title:  'Statistics of NumForm in UD_Ruuli-RDT'
udver: '2'
---

## Treebank Statistics: UD_Ruuli-RDT: Features: `NumForm`

This feature is language-specific.
It occurs with 2 different values: `Digit`, `Word`.

50 tokens (1%) have a non-empty value of `NumForm`.
32 types (1%) occur at least once with a non-empty value of `NumForm`.
22 lemmas (2%) occur at least once with a non-empty value of `NumForm`.
The feature is used with 1 part-of-speech tags: <tt><a href="ruc_rdt-pos-NUM.html">NUM</a></tt> (50; 1% instances).

### `NUM`

50 <tt><a href="ruc_rdt-pos-NUM.html">NUM</a></tt> tokens (96% of all `NUM` tokens) have a non-empty value of `NumForm`.

The most frequent other feature values with which `NUM` and `NumForm` co-occurred: <tt><a href="ruc_rdt-feat-NumType.html">NumType</a></tt><tt>=Card</tt> (50; 100%), <tt><a href="ruc_rdt-feat-NounClass.html">NounClass</a></tt><tt>=EMPTY</tt> (42; 84%), <tt><a href="ruc_rdt-feat-Referent.html">Referent</a></tt><tt>=EMPTY</tt> (42; 84%).

`NUM` tokens may have the following values of `NumForm`:

* `Digit` (1; 2% of non-empty `NumForm`): <em>1400</em>
* `Word` (49; 98% of non-empty `NumForm`): <em>ibiri, mukaaga, ikumi, musanju, abiri, emwe, inai, kanai, kataanu, kimwei</em>

`NumForm` seems to be **lexical feature** of `NUM`. 100% lemmas (22) occur only with one value of `NumForm`.

## Relations with Agreement in `NumForm`

The 10 most frequent relations where parent and child node agree in `NumForm`:
<tt>NUM --[<tt><a href="ruc_rdt-dep-conj.html">conj</a></tt>]--> NUM</tt> (6; 100%),
<tt>NUM --[<tt><a href="ruc_rdt-dep-flat-num.html">flat:num</a></tt>]--> NUM</tt> (5; 100%),
<tt>NUM --[<tt><a href="ruc_rdt-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (3; 100%).

