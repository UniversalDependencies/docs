---
layout: base
title:  'Statistics of ExtPos in UD_Turkish-IMST'
udver: '2'
---

## Treebank Statistics: UD_Turkish-IMST: Features: `ExtPos`

This feature is language-specific.
It occurs with 2 different values: `ADV`, `CCONJ`.

50 tokens (0%) have a non-empty value of `ExtPos`.
9 types (0%) occur at least once with a non-empty value of `ExtPos`.
5 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 2 part-of-speech tags: <tt><a href="tr_imst-pos-CCONJ.html">CCONJ</a></tt> (49; 0% instances), <tt><a href="tr_imst-pos-ADV.html">ADV</a></tt> (1; 0% instances).

### `CCONJ`

49 <tt><a href="tr_imst-pos-CCONJ.html">CCONJ</a></tt> tokens (2% of all `CCONJ` tokens) have a non-empty value of `ExtPos`.

`CCONJ` tokens may have the following values of `ExtPos`:

* `CCONJ` (49; 100% of non-empty `ExtPos`): <em>ya, hem, ve, ne</em>

### `ADV`

1 <tt><a href="tr_imst-pos-ADV.html">ADV</a></tt> tokens (0% of all `ADV` tokens) have a non-empty value of `ExtPos`.

`ADV` tokens may have the following values of `ExtPos`:

* `ADV` (1; 100% of non-empty `ExtPos`): <em>Yine</em>

