---
layout: base
title:  'Statistics of PunctType in UD_Greek-Lesbian'
udver: '2'
---

## Treebank Statistics: UD_Greek-Lesbian: Features: `PunctType`

This feature is language-specific.
It occurs with 8 different values: `Brck`, `Colo`, `Comm`, `Dash`, `Excl`, `Peri`, `Qest`, `Quot`.

1005 tokens (15%) have a non-empty value of `PunctType`.
11 types (0%) occur at least once with a non-empty value of `PunctType`.
11 lemmas (1%) occur at least once with a non-empty value of `PunctType`.
The feature is used with 1 part-of-speech tags: <tt><a href="el_lesbian-pos-PUNCT.html">PUNCT</a></tt> (1005; 15% instances).

### `PUNCT`

1005 <tt><a href="el_lesbian-pos-PUNCT.html">PUNCT</a></tt> tokens (100% of all `PUNCT` tokens) have a non-empty value of `PunctType`.

`PUNCT` tokens may have the following values of `PunctType`:

* `Brck` (4; 0% of non-empty `PunctType`): <em>(, )</em>
* `Colo` (6; 1% of non-empty `PunctType`): <em>:</em>
* `Comm` (328; 33% of non-empty `PunctType`): <em>,</em>
* `Dash` (14; 1% of non-empty `PunctType`): <em>-</em>
* `Excl` (22; 2% of non-empty `PunctType`): <em>!</em>
* `Peri` (550; 55% of non-empty `PunctType`): <em>., ...</em>
* `Qest` (53; 5% of non-empty `PunctType`): <em>;</em>
* `Quot` (28; 3% of non-empty `PunctType`): <em>«, »</em>

`PunctType` seems to be **lexical feature** of `PUNCT`. 100% lemmas (11) occur only with one value of `PunctType`.

