---
layout: base
title:  'Statistics of PronType in UD_Beja-NSC'
udver: '2'
---

## Treebank Statistics: UD_Beja-NSC: Features: `PronType`

This feature is universal.
It occurs with 2 different values: `Dem`, `Rel`.

246 tokens (4%) have a non-empty value of `PronType`.
30 types (3%) occur at least once with a non-empty value of `PronType`.
1 lemmas (0) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="bej_nsc-pos-DET.html">DET</a></tt> (129; 2% instances), <tt><a href="bej_nsc-pos-SCONJ.html">SCONJ</a></tt> (107; 2% instances), <tt><a href="bej_nsc-pos-PRON.html">PRON</a></tt> (10; 0% instances).

### `DET`

129 <tt><a href="bej_nsc-pos-DET.html">DET</a></tt> tokens (14% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="bej_nsc-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (129; 100%), <tt><a href="bej_nsc-feat-Deixis.html">Deixis</a></tt><tt>=Prox</tt> (117; 91%), <tt><a href="bej_nsc-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (107; 83%), <tt><a href="bej_nsc-feat-Number.html">Number</a></tt><tt>=Sing</tt> (105; 81%), <tt><a href="bej_nsc-feat-Case.html">Case</a></tt><tt>=Acc</tt> (73; 57%).

`DET` tokens may have the following values of `PronType`:

* `Dem` (129; 100% of non-empty `PronType`): <em>oːn, uːn, eːn, aːn, beːn, oːt, toːt, tuːt, oːnaːj, uːt</em>
* `EMPTY` (804): <em>=t, i=, oː=, =b, uː=, w=, ti=, t=, j=, toː=</em>

### `SCONJ`

107 <tt><a href="bej_nsc-pos-SCONJ.html">SCONJ</a></tt> tokens (36% of all `SCONJ` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `SCONJ` and `PronType` co-occurred: <tt><a href="bej_nsc-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (76; 71%), <tt><a href="bej_nsc-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (57; 53%).

`SCONJ` tokens may have the following values of `PronType`:

* `Rel` (107; 100% of non-empty `PronType`): <em>=eːb, =jeː, =eː, =jeːb, =i, =eːt, =jeːt, wi=, =ji, w=</em>
* `EMPTY` (191): <em>=hoːb, =ajt, =it, =aj, =eːk, =jeːk, =eːt, =jeːt, =eː, =i</em>

### `PRON`

10 <tt><a href="bej_nsc-pos-PRON.html">PRON</a></tt> tokens (3% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="bej_nsc-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (10; 100%), <tt><a href="bej_nsc-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (10; 100%), <tt><a href="bej_nsc-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (8; 80%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (2; 20% of non-empty `PronType`): <em>beːn, oːn</em>
* `Rel` (8; 80% of non-empty `PronType`): <em>i=, wi=, =b, =t, ji=, ti=</em>
* `EMPTY` (385): <em>=heːb, =i, =oː, ani, =eː, kna, =hoːk, =oːk, =joː, =ji</em>

