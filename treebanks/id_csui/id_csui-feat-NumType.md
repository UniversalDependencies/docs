---
layout: base
title:  'Statistics of NumType in UD_Indonesian-CSUI'
udver: '2'
---

## Treebank Statistics: UD_Indonesian-CSUI: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

1924 tokens (7%) have a non-empty value of `NumType`.
704 types (16%) occur at least once with a non-empty value of `NumType`.
695 lemmas (19%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="id_csui-pos-NUM.html">NUM</a></tt> (1857; 7% instances), <tt><a href="id_csui-pos-ADJ.html">ADJ</a></tt> (67; 0% instances).

### `NUM`

1857 <tt><a href="id_csui-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (1857; 100% of non-empty `NumType`): <em>2007, triliun, miliar, 2006, juta, 2008, satu, dua, 30, 10</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (685) occur only with one value of `NumType`.

### `ADJ`

67 <tt><a href="id_csui-pos-ADJ.html">ADJ</a></tt> tokens (6% of all `ADJ` tokens) have a non-empty value of `NumType`.

`ADJ` tokens may have the following values of `NumType`:

* `Ord` (67; 100% of non-empty `NumType`): <em>pertama, kedua, ketiga, keenam, kedelapan, kelima, ke-10, ke-2, ke-4, ke-40</em>
* `EMPTY` (1103): <em>sama, bersih, besar, tinggi, lain, baik, asing, rendah, utama, fiskal</em>

`NumType` seems to be **lexical feature** of `ADJ`. 100% lemmas (12) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="id_csui-dep-flat.html">flat</a></tt>]--> NUM</tt> (380; 100%),
<tt>NUM --[<tt><a href="id_csui-dep-conj.html">conj</a></tt>]--> NUM</tt> (12; 100%),
<tt>NUM --[<tt><a href="id_csui-dep-nmod.html">nmod</a></tt>]--> NUM</tt> (5; 100%),
<tt>NUM --[<tt><a href="id_csui-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (3; 100%),
<tt>ADJ --[<tt><a href="id_csui-dep-advcl.html">advcl</a></tt>]--> ADJ</tt> (2; 67%),
<tt>ADJ --[<tt><a href="id_csui-dep-flat.html">flat</a></tt>]--> ADJ</tt> (2; 100%),
<tt>NUM --[<tt><a href="id_csui-dep-nmod-tmod.html">nmod:tmod</a></tt>]--> NUM</tt> (1; 100%).

