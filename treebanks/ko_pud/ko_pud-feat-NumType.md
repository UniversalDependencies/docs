---
layout: base
title:  'Statistics of NumType in UD_Korean-PUD'
udver: '2'
---

## Treebank Statistics: UD_Korean-PUD: Features: `NumType`

This feature is universal.
It occurs with 1 different values: `Card`.

513 tokens (3%) have a non-empty value of `NumType`.
218 types (3%) occur at least once with a non-empty value of `NumType`.
196 lemmas (7%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="ko_pud-pos-NUM.html">NUM</a></tt> (513; 3% instances).

### `NUM`

513 <tt><a href="ko_pud-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (513; 100% of non-empty `NumType`): 1, 10, 3, 2, 4, 6, 20, 8, 5, 9

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (196) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="ko_pud-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (13; 100%).

