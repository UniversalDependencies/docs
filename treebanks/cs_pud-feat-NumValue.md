---
layout: base
title:  'Statistics of NumValue in UD_Czech-PUD'
udver: '2'
---

## Treebank Statistics: UD_Czech-PUD: Features: `NumValue`

This feature is language-specific.
It occurs with 3 different values: `1`, `2`, `3`.
Some words have combined values of the feature; 1 combinations have been observed: `1|2|3`.

103 tokens (1%) have a non-empty value of `NumValue`.
31 types (0%) occur at least once with a non-empty value of `NumValue`.
8 lemmas (0%) occur at least once with a non-empty value of `NumValue`.
The feature is used with 2 part-of-speech tags: [NUM](cs_pud-pos-NUM.html) (102; 1% instances), [ADJ](cs_pud-pos-ADJ.html) (1; 0% instances).

### `NUM`

102 [NUM](cs_pud-pos-NUM.html) tokens (22% of all `NUM` tokens) have a non-empty value of `NumValue`.

The most frequent other feature values with which `NUM` and `NumValue` co-occurred: <tt><a href="NumForm.html">NumForm</a>=Word</tt> (102; 100%), <tt><a href="NumType.html">NumType</a>=Card</tt> (102; 100%), <tt><a href="Number.html">Number</a>=Plur</tt> (63; 62%).

`NUM` tokens may have the following values of `NumValue`:

* `1,2,3` (102; 100% of non-empty `NumValue`): dva čtyři dvou dvě jedné jeden dvěma jedna jedním tři

### `ADJ`

1 [ADJ](cs_pud-pos-ADJ.html) tokens (0% of all `ADJ` tokens) have a non-empty value of `NumValue`.

The most frequent other feature values with which `ADJ` and `NumValue` co-occurred: <tt><a href="Case.html">Case</a>=Nom</tt> (1; 100%), <tt><a href="Gender.html">Gender</a>=Masc</tt> (1; 100%), <tt><a href="Degree.html">Degree</a>=EMPTY</tt> (1; 100%), <tt><a href="Voice.html">Voice</a>=EMPTY</tt> (1; 100%), <tt><a href="VerbForm.html">VerbForm</a>=EMPTY</tt> (1; 100%), <tt><a href="Animacy.html">Animacy</a>=Anim</tt> (1; 100%), <tt><a href="Polarity.html">Polarity</a>=EMPTY</tt> (1; 100%), <tt><a href="Number.html">Number</a>=Plur</tt> (1; 100%).

`ADJ` tokens may have the following values of `NumValue`:

* `1` (1; 100% of non-empty `NumValue`): jedni

## Relations with Agreement in `NumValue`

The 10 most frequent relations where parent and child node agree in `NumValue`:
<tt>NUM --[<a href="../dep/conj.html">conj</a>]--> NUM</tt> (1; 100%).

