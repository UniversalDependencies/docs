---
layout: base
title:  'Statistics of PronType in UD_English-GUM'
udver: '2'
---

## Treebank Statistics: UD_English-GUM: Features: `PronType`

This feature is universal but the values `Emp` are language-specific.
It occurs with 10 different values: `Art`, `Dem`, `Emp`, `Ind`, `Int`, `Neg`, `Prs`, `Rcp`, `Rel`, `Tot`.

45093 tokens (18%) have a non-empty value of `PronType`.
179 types (1%) occur at least once with a non-empty value of `PronType`.
77 lemmas (1%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="en_gum-pos-PRON.html">PRON</a></tt> (21973; 9% instances), <tt><a href="en_gum-pos-DET.html">DET</a></tt> (21037; 8% instances), <tt><a href="en_gum-pos-ADV.html">ADV</a></tt> (2083; 1% instances).

### `PRON`

21973 <tt><a href="en_gum-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="en_gum-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (18481; 84%), <tt><a href="en_gum-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (16379; 75%), <tt><a href="en_gum-feat-Number.html">Number</a></tt><tt>=Sing</tt> (14508; 66%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (2040; 9% of non-empty `PronType`): <em>there, that, this, those, these</em>
* `Emp` (37; 0% of non-empty `PronType`): <em>itself, themselves, himself, yourself</em>
* `Ind` (327; 1% of non-empty `PronType`): <em>something, anything, someone, anyone, somebody, anybody</em>
* `Int` (294; 1% of non-empty `PronType`): <em>what, who, which, Whoever, whose</em>
* `Neg` (72; 0% of non-empty `PronType`): <em>nothing, one, nobody</em>
* `Prs` (16852; 77% of non-empty `PronType`): <em>i, it, you, we, they, he, your, his, my, our</em>
* `Rcp` (4; 0% of non-empty `PronType`): <em>one</em>
* `Rel` (2208; 10% of non-empty `PronType`): <em>that, which, what, who, whatever, whom, whose, whoever, Whosoever, wish</em>
* `Tot` (139; 1% of non-empty `PronType`): <em>everything, everyone, everybody, ev</em>

<table>
  <tr><th>Paradigm <i>one</i></th><th><tt>Prs</tt></th><th><tt>Rcp</tt></th><th><tt>Neg</tt></th></tr>
  <tr><td><tt><tt><a href="en_gum-feat-ExtPos.html">ExtPos</a></tt><tt>=PRON</tt></tt></td><td></td><td><em>one</em></td><td></td></tr>
  <tr><td><tt><tt><a href="en_gum-feat-Number.html">Number</a></tt><tt>=Sing</tt></tt></td><td></td><td></td><td><em>one</em></td></tr>
  <tr><td><tt><tt><a href="en_gum-feat-Number.html">Number</a></tt><tt>=Sing</tt>|<tt><a href="en_gum-feat-Person.html">Person</a></tt><tt>=3</tt></tt></td><td><em>one</em></td><td></td><td></td></tr>
</table>

### `DET`

21037 <tt><a href="en_gum-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="en_gum-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (12104; 58%).

`DET` tokens may have the following values of `PronType`:

* `Art` (17393; 83% of non-empty `PronType`): <em>the, a, an</em>
* `Dem` (1633; 8% of non-empty `PronType`): <em>this, that, these, those, yonder</em>
* `Ind` (1398; 7% of non-empty `PronType`): <em>some, all, any, every, another, each, such, both, either, quite</em>
* `Int` (59; 0% of non-empty `PronType`): <em>which, what</em>
* `Neg` (229; 1% of non-empty `PronType`): <em>no, neither</em>
* `Rcp` (23; 0% of non-empty `PronType`): <em>each</em>
* `Rel` (14; 0% of non-empty `PronType`): <em>what</em>
* `Tot` (288; 1% of non-empty `PronType`): <em>all, both, each, every</em>

<table>
  <tr><th>Paradigm <i>each</i></th><th><tt>Rcp</tt></th><th><tt>Tot</tt></th><th><tt>Ind</tt></th></tr>
  <tr><td><tt>_</tt></td><td></td><td><em>each</em></td><td><em>each</em></td></tr>
  <tr><td><tt><tt><a href="en_gum-feat-ExtPos.html">ExtPos</a></tt><tt>=PRON</tt></tt></td><td><em>each</em></td><td></td><td></td></tr>
</table>

### `ADV`

2083 <tt><a href="en_gum-pos-ADV.html">ADV</a></tt> tokens (17% of all `ADV` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADV` and `PronType` co-occurred: <tt><a href="en_gum-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (2083; 100%).

`ADV` tokens may have the following values of `PronType`:

* `Dem` (876; 42% of non-empty `PronType`): <em>then, here, there</em>
* `Int` (805; 39% of non-empty `PronType`): <em>when, how, where, why, whither, whenever, whereupon</em>
* `Rel` (402; 19% of non-empty `PronType`): <em>where, how, why, when, whenever, wherever, however, whereby</em>
* `EMPTY` (10123): <em>so, just, also, now, more, really, very, even, as, only</em>

<table>
  <tr><th>Paradigm <i>when</i></th><th><tt>Int</tt></th><th><tt>Rel</tt></th></tr>
  <tr><td><tt></tt></td><td><em>when</em></td><td><em>when</em></td></tr>
</table>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="en_gum-dep-reparandum.html">reparandum</a></tt>]--> PRON</tt> (53; 85%),
<tt>DET --[<tt><a href="en_gum-dep-reparandum.html">reparandum</a></tt>]--> DET</tt> (35; 97%),
<tt>PRON --[<tt><a href="en_gum-dep-conj.html">conj</a></tt>]--> PRON</tt> (31; 89%),
<tt>ADV --[<tt><a href="en_gum-dep-conj.html">conj</a></tt>]--> ADV</tt> (15; 71%),
<tt>PRON --[<tt><a href="en_gum-dep-conj.html">conj</a></tt>]--> ADV</tt> (9; 69%),
<tt>DET --[<tt><a href="en_gum-dep-reparandum.html">reparandum</a></tt>]--> PRON</tt> (8; 100%),
<tt>PRON --[<tt><a href="en_gum-dep-appos.html">appos</a></tt>]--> PRON</tt> (6; 86%),
<tt>DET --[<tt><a href="en_gum-dep-conj.html">conj</a></tt>]--> DET</tt> (5; 63%),
<tt>PRON --[<tt><a href="en_gum-dep-parataxis.html">parataxis</a></tt>]--> PRON</tt> (4; 80%),
<tt>ADV --[<tt><a href="en_gum-dep-conj.html">conj</a></tt>]--> PRON</tt> (2; 67%).

