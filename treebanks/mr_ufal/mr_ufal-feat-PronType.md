---
layout: base
title:  'Statistics of PronType in UD_Marathi-UFAL'
udver: '2'
---

## Treebank Statistics: UD_Marathi-UFAL: Features: `PronType`

This feature is universal.
It occurs with 6 different values: `Dem`, `Ind`, `Int`, `Prs`, `Rel`, `Tot`.

493 tokens (13%) have a non-empty value of `PronType`.
58 types (5%) occur at least once with a non-empty value of `PronType`.
22 lemmas (3%) occur at least once with a non-empty value of `PronType`.
The feature is used with 2 part-of-speech tags: <tt><a href="mr_ufal-pos-PRON.html">PRON</a></tt> (379; 10% instances), <tt><a href="mr_ufal-pos-DET.html">DET</a></tt> (114; 3% instances).

### `PRON`

379 <tt><a href="mr_ufal-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="mr_ufal-feat-Number.html">Number</a></tt><tt>=Sing</tt> (273; 72%), <tt><a href="mr_ufal-feat-Person.html">Person</a></tt><tt>=3</tt> (204; 54%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (206; 54% of non-empty `PronType`): <em>तो, ती, त्या, त्याला, त्याने, ति, तिला, ते, त्यां, हे</em>
* `Ind` (2; 1% of non-empty `PronType`): <em>काही, कोणी</em>
* `Int` (29; 8% of non-empty `PronType`): <em>काय, कोण, कोणी, कोणा, कशाने, कोणता</em>
* `Prs` (136; 36% of non-empty `PronType`): <em>मी, माझ, मला, आपल, तुझ, तुम, तुला, तू, तुम्ही, आपण</em>
* `Rel` (2; 1% of non-empty `PronType`): <em>जो</em>
* `Tot` (4; 1% of non-empty `PronType`): <em>सर्वांना, सर्वांनी, सारे</em>

<table>
  <tr><th>Paradigm <i>कोण</i></th><th><tt>Int</tt></th><th><tt>Ind</tt></th></tr>
  <tr><td><tt>_</tt></td><td><em>कोणा</em></td><td></td></tr>
  <tr><td><tt><tt><a href="mr_ufal-feat-Case.html">Case</a></tt><tt>=Erg</tt></tt></td><td><em>कोणी</em></td><td><em>कोणी</em></td></tr>
  <tr><td><tt><tt><a href="mr_ufal-feat-Case.html">Case</a></tt><tt>=Nom</tt></tt></td><td><em>कोण</em></td><td></td></tr>
</table>

### `DET`

114 <tt><a href="mr_ufal-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="mr_ufal-feat-Number.html">Number</a></tt><tt>=Sing</tt> (74; 65%), <tt><a href="mr_ufal-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (72; 63%).

`DET` tokens may have the following values of `PronType`:

* `Dem` (82; 72% of non-empty `PronType`): <em>तो, त्या, ती, या, ही, हा, हे, ते, आम्ही, तुम्हा</em>
* `Ind` (32; 28% of non-empty `PronType`): <em>एक, एका, काही, कोणी</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>DET --[<tt><a href="mr_ufal-dep-conj.html">conj</a></tt>]--> DET</tt> (1; 100%),
<tt>DET --[<tt><a href="mr_ufal-dep-det.html">det</a></tt>]--> DET</tt> (1; 100%),
<tt>PRON --[<tt><a href="mr_ufal-dep-conj.html">conj</a></tt>]--> PRON</tt> (1; 100%).

