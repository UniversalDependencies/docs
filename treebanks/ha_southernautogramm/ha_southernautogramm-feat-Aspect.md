---
layout: base
title:  'Statistics of Aspect in UD_Hausa-SouthernAutogramm'
udver: '2'
---

## Treebank Statistics: UD_Hausa-SouthernAutogramm: Features: `Aspect`

This feature is universal but the values `Hab`, `Iter` are language-specific.
It occurs with 4 different values: `Hab`, `Iter`, `Perf`, `Prog`.

1530 tokens (11%) have a non-empty value of `Aspect`.
78 types (4%) occur at least once with a non-empty value of `Aspect`.
4 lemmas (0%) occur at least once with a non-empty value of `Aspect`.
The feature is used with 2 part-of-speech tags: <tt><a href="ha_southernautogramm-pos-AUX.html">AUX</a></tt> (1510; 10% instances), <tt><a href="ha_southernautogramm-pos-PART.html">PART</a></tt> (20; 0% instances).

### `AUX`

1510 <tt><a href="ha_southernautogramm-pos-AUX.html">AUX</a></tt> tokens (70% of all `AUX` tokens) have a non-empty value of `Aspect`.

The most frequent other feature values with which `AUX` and `Aspect` co-occurred: <tt><a href="ha_southernautogramm-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (1510; 100%), <tt><a href="ha_southernautogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (1046; 69%).

`AUX` tokens may have the following values of `Aspect`:

* `Hab` (71; 5% of non-empty `Aspect`): <em>mukàn, kukàn, takàn, yakàn, akàn, sukàn</em>
* `Perf` (1052; 70% of non-empty `Aspect`): <em>ya, yaː, kaː, ta, akà, an, sukà, mukà, naː, ka</em>
* `Prog` (387; 26% of non-empty `Aspect`): <em>yanàː, anàː, munàː, sunàː, akèː, inàː, nàː, kanàː, tanàː, mukèː</em>
* `EMPTY` (650): <em>nèː, kà, à, yà, zâi, mù, zân, zaːkà, tà, sù</em>

### `PART`

20 <tt><a href="ha_southernautogramm-pos-PART.html">PART</a></tt> tokens (2% of all `PART` tokens) have a non-empty value of `Aspect`.

The most frequent other feature values with which `PART` and `Aspect` co-occurred: <tt><a href="ha_southernautogramm-feat-PartType.html">PartType</a></tt><tt>=Aspect</tt> (20; 100%), <tt><a href="ha_southernautogramm-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (20; 100%).

`PART` tokens may have the following values of `Aspect`:

* `Iter` (20; 100% of non-empty `Aspect`): <em>ta</em>
* `EMPTY` (1023): <em>ba, na, maː, dai, kuma, nèː, gàː, kèːnan, àkwai, bàː</em>

## Relations with Agreement in `Aspect`

The 10 most frequent relations where parent and child node agree in `Aspect`:
<tt>AUX --[<tt><a href="ha_southernautogramm-dep-reparandum.html">reparandum</a></tt>]--> AUX</tt> (5; 100%).

