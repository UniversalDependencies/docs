---
layout: base
title:  'Statistics of ExtPos in UD_Portuguese-GSD'
udver: '2'
---

## Treebank Statistics: UD_Portuguese-GSD: Features: `ExtPos`

This feature is language-specific.
It occurs with 8 different values: `ADP`, `ADV`, `AUX`, `CCONJ`, `INTJ`, `NOUN`, `PROPN`, `SCONJ`.

1783 tokens (1%) have a non-empty value of `ExtPos`.
958 types (3%) occur at least once with a non-empty value of `ExtPos`.
900 lemmas (6%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 11 part-of-speech tags: <tt><a href="pt_gsd-pos-PROPN.html">PROPN</a></tt> (1312; 0% instances), <tt><a href="pt_gsd-pos-VERB.html">VERB</a></tt> (242; 0% instances), <tt><a href="pt_gsd-pos-ADV.html">ADV</a></tt> (127; 0% instances), <tt><a href="pt_gsd-pos-ADP.html">ADP</a></tt> (48; 0% instances), <tt><a href="pt_gsd-pos-NOUN.html">NOUN</a></tt> (30; 0% instances), <tt><a href="pt_gsd-pos-SCONJ.html">SCONJ</a></tt> (7; 0% instances), <tt><a href="pt_gsd-pos-AUX.html">AUX</a></tt> (4; 0% instances), <tt><a href="pt_gsd-pos-CCONJ.html">CCONJ</a></tt> (4; 0% instances), <tt><a href="pt_gsd-pos-X.html">X</a></tt> (4; 0% instances), <tt><a href="pt_gsd-pos-DET.html">DET</a></tt> (3; 0% instances), <tt><a href="pt_gsd-pos-PRON.html">PRON</a></tt> (2; 0% instances).

### `PROPN`

1312 <tt><a href="pt_gsd-pos-PROPN.html">PROPN</a></tt> tokens (4% of all `PROPN` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PROPN` and `ExtPos` co-occurred: <tt><a href="pt_gsd-feat-Number.html">Number</a></tt><tt>=Sing</tt> (1301; 99%).

`PROPN` tokens may have the following values of `ExtPos`:

* `PROPN` (1312; 100% of non-empty `ExtPos`): <em>The, São, la, O, Luiz, José, Los, Nova, United, Paulo</em>

`ExtPos` seems to be **lexical feature** of `PROPN`. 100% lemmas (836) occur only with one value of `ExtPos`.

### `VERB`

242 <tt><a href="pt_gsd-pos-VERB.html">VERB</a></tt> tokens (1% of all `VERB` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `VERB` and `ExtPos` co-occurred: <tt><a href="pt_gsd-feat-VerbForm.html">VerbForm</a></tt><tt>=Fin</tt> (212; 88%), <tt><a href="pt_gsd-feat-Number.html">Number</a></tt><tt>=Sing</tt> (169; 70%).

`VERB` tokens may have the following values of `ExtPos`:

* `AUX` (242; 100% of non-empty `ExtPos`): <em>chegou, passou, começou, voltou, começaram, passa, deixou, voltar, volta, voltaram</em>

`ExtPos` seems to be **lexical feature** of `VERB`. 100% lemmas (13) occur only with one value of `ExtPos`.

### `ADV`

127 <tt><a href="pt_gsd-pos-ADV.html">ADV</a></tt> tokens (1% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="pt_gsd-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (127; 100%).

`ADV` tokens may have the following values of `ExtPos`:

* `ADP` (57; 45% of non-empty `ExtPos`): <em>apesar, diante, graças, quanto</em>
* `ADV` (14; 11% of non-empty `ExtPos`): <em>cerca, Mesmo</em>
* `CCONJ` (7; 6% of non-empty `ExtPos`): <em>Além, ainda</em>
* `PROPN` (1; 1% of non-empty `ExtPos`): <em>Nada</em>
* `SCONJ` (48; 38% of non-empty `ExtPos`): <em>depois, antes, apesar, ainda, além, assim, já, Mesmo, tanto</em>

<table>
  <tr><th>Paradigm <i>ainda</i></th><th><tt>CCONJ</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>ainda</em></td><td><em>ainda</em></td></tr>
</table>

### `ADP`

48 <tt><a href="pt_gsd-pos-ADP.html">ADP</a></tt> tokens (0% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (18; 38% of non-empty `ExtPos`): <em>a, em, por</em>
* `ADV` (11; 23% of non-empty `ExtPos`): <em>por, em, a</em>
* `CCONJ` (8; 17% of non-empty `ExtPos`): <em>em, A, por</em>
* `NOUN` (2; 4% of non-empty `ExtPos`): <em>por</em>
* `SCONJ` (9; 19% of non-empty `ExtPos`): <em>a, por, de</em>

<table>
  <tr><th>Paradigm <i>por</i></th><th><tt>ADP</tt></th><th><tt>ADV</tt></th><th><tt>CCONJ</tt></th><th><tt>NOUN</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>por</em></td><td><em>por</em></td><td><em>por</em></td><td><em>por</em></td><td><em>por</em></td></tr>
</table>

### `NOUN`

30 <tt><a href="pt_gsd-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `NOUN` and `ExtPos` co-occurred: <tt><a href="pt_gsd-feat-Number.html">Number</a></tt><tt>=Sing</tt> (30; 100%).

`NOUN` tokens may have the following values of `ExtPos`:

* `NOUN` (15; 50% of non-empty `ExtPos`): <em>meio, ponto, ambiente, bota, carteira, faixa, fim, força, habeas, horário</em>
* `PROPN` (15; 50% of non-empty `ExtPos`): <em>Casa, TV, Associação, Beleza, Blue, Condado, Distrito, Igreja, Lei, Professor</em>

`ExtPos` seems to be **lexical feature** of `NOUN`. 100% lemmas (26) occur only with one value of `ExtPos`.

### `SCONJ`

7 <tt><a href="pt_gsd-pos-SCONJ.html">SCONJ</a></tt> tokens (1% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

`SCONJ` tokens may have the following values of `ExtPos`:

* `SCONJ` (7; 100% of non-empty `ExtPos`): <em>já, assim, de, mesmo</em>

### `AUX`

4 <tt><a href="pt_gsd-pos-AUX.html">AUX</a></tt> tokens (0% of all `AUX` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `AUX` and `ExtPos` co-occurred: <tt><a href="pt_gsd-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (4; 100%), <tt><a href="pt_gsd-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (4; 100%), <tt><a href="pt_gsd-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (4; 100%), <tt><a href="pt_gsd-feat-Tense.html">Tense</a></tt><tt>=EMPTY</tt> (4; 100%), <tt><a href="pt_gsd-feat-VerbForm.html">VerbForm</a></tt><tt>=EMPTY</tt> (4; 100%).

`AUX` tokens may have the following values of `ExtPos`:

* `INTJ` (4; 100% of non-empty `ExtPos`): <em>é</em>

### `CCONJ`

4 <tt><a href="pt_gsd-pos-CCONJ.html">CCONJ</a></tt> tokens (0% of all `CCONJ` tokens) have a non-empty value of `ExtPos`.

`CCONJ` tokens may have the following values of `ExtPos`:

* `CCONJ` (4; 100% of non-empty `ExtPos`): <em>ou</em>

### `X`

4 <tt><a href="pt_gsd-pos-X.html">X</a></tt> tokens (1% of all `X` tokens) have a non-empty value of `ExtPos`.

`X` tokens may have the following values of `ExtPos`:

* `NOUN` (4; 100% of non-empty `ExtPos`): <em>best, food, pet, market</em>

### `DET`

3 <tt><a href="pt_gsd-pos-DET.html">DET</a></tt> tokens (0% of all `DET` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `DET` and `ExtPos` co-occurred: <tt><a href="pt_gsd-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (3; 100%), <tt><a href="pt_gsd-feat-Number.html">Number</a></tt><tt>=Sing</tt> (3; 100%), <tt><a href="pt_gsd-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (3; 100%), <tt><a href="pt_gsd-feat-Gender.html">Gender</a></tt><tt>=Fem</tt> (2; 67%).

`DET` tokens may have the following values of `ExtPos`:

* `PROPN` (3; 100% of non-empty `ExtPos`): <em>The, Nossa</em>

### `PRON`

2 <tt><a href="pt_gsd-pos-PRON.html">PRON</a></tt> tokens (0% of all `PRON` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PRON` and `ExtPos` co-occurred: <tt><a href="pt_gsd-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (2; 100%), <tt><a href="pt_gsd-feat-Number.html">Number</a></tt><tt>=Sing</tt> (2; 100%).

`PRON` tokens may have the following values of `ExtPos`:

* `CCONJ` (1; 50% of non-empty `ExtPos`): <em>Isto</em>
* `PROPN` (1; 50% of non-empty `ExtPos`): <em>Que</em>

## Relations with Agreement in `ExtPos`

The 10 most frequent relations where parent and child node agree in `ExtPos`:
<tt>PROPN --[<tt><a href="pt_gsd-dep-conj.html">conj</a></tt>]--> PROPN</tt> (145; 52%).

