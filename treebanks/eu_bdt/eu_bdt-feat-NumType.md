---
layout: base
title:  'Statistics of NumType in UD_Basque-BDT'
udver: '2'
---

## Treebank Statistics: UD_Basque-BDT: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

4614 tokens (4%) have a non-empty value of `NumType`.
1028 types (4%) occur at least once with a non-empty value of `NumType`.
717 lemmas (7%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="eu_bdt-pos-NUM.html">NUM</a></tt> (3566; 3% instances), <tt><a href="eu_bdt-pos-ADJ.html">ADJ</a></tt> (1048; 1% instances).

### `NUM`

3566 <tt><a href="eu_bdt-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (3566; 100% of non-empty `NumType`): <em>bat, bi, hiru, batean, baten, batek, lau, batez, bost, sei</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (585) occur only with one value of `NumType`.

### `ADJ`

1048 <tt><a href="eu_bdt-pos-ADJ.html">ADJ</a></tt> tokens (15% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="eu_bdt-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (1048; 100%), <tt><a href="eu_bdt-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (1048; 100%), <tt><a href="eu_bdt-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (1048; 100%).

`ADJ` tokens may have the following values of `NumType`:

* `Ord` (1048; 100% of non-empty `NumType`): <em>azken, lehen, bigarren, hirugarren, lehenengo, laugarren, bosgarren, seigarren, II.a, lehena</em>
* `EMPTY` (5973): <em>handia, ona, bakarra, nagusia, zaila, berria, onena, hurrengo, nagusi, berri</em>

`NumType` seems to be **lexical feature** of `ADJ`. 100% lemmas (137) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="eu_bdt-dep-conj.html">conj</a></tt>]--> NUM</tt> (62; 100%),
<tt>NUM --[<tt><a href="eu_bdt-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (18; 100%),
<tt>NUM --[<tt><a href="eu_bdt-dep-fixed.html">fixed</a></tt>]--> NUM</tt> (14; 100%),
<tt>ADJ --[<tt><a href="eu_bdt-dep-conj.html">conj</a></tt>]--> ADJ</tt> (10; 91%),
<tt>NUM --[<tt><a href="eu_bdt-dep-nmod.html">nmod</a></tt>]--> NUM</tt> (6; 100%),
<tt>NUM --[<tt><a href="eu_bdt-dep-appos.html">appos</a></tt>]--> NUM</tt> (2; 100%).

