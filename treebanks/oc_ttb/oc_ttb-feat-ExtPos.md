---
layout: base
title:  'Statistics of ExtPos in UD_Occitan-TTB'
udver: '2'
---

## Treebank Statistics: UD_Occitan-TTB: Features: `ExtPos`

This feature is language-specific.
It occurs with 8 different values: `ADP`, `ADV`, `CCONJ`, `DET`, `INTJ`, `PRON`, `PROPN`, `SCONJ`.

180 tokens (1%) have a non-empty value of `ExtPos`.
69 types (1%) occur at least once with a non-empty value of `ExtPos`.
52 lemmas (1%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 11 part-of-speech tags: <tt><a href="oc_ttb-pos-ADP.html">ADP</a></tt> (94; 0% instances), <tt><a href="oc_ttb-pos-ADV.html">ADV</a></tt> (54; 0% instances), <tt><a href="oc_ttb-pos-DET.html">DET</a></tt> (9; 0% instances), <tt><a href="oc_ttb-pos-CCONJ.html">CCONJ</a></tt> (5; 0% instances), <tt><a href="oc_ttb-pos-SCONJ.html">SCONJ</a></tt> (5; 0% instances), <tt><a href="oc_ttb-pos-NOUN.html">NOUN</a></tt> (4; 0% instances), <tt><a href="oc_ttb-pos-PRON.html">PRON</a></tt> (3; 0% instances), <tt><a href="oc_ttb-pos-X.html">X</a></tt> (3; 0% instances), <tt><a href="oc_ttb-pos-ADJ.html">ADJ</a></tt> (1; 0% instances), <tt><a href="oc_ttb-pos-AUX.html">AUX</a></tt> (1; 0% instances), <tt><a href="oc_ttb-pos-PART.html">PART</a></tt> (1; 0% instances).

### `ADP`

94 <tt><a href="oc_ttb-pos-ADP.html">ADP</a></tt> tokens (3% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (36; 38% of non-empty `ExtPos`): <em>a, cap, en, fins, de, gràcias, a-, amb, dempuèi, rapòrt</em>
* `ADV` (27; 29% of non-empty `ExtPos`): <em>de, d', en, per, a</em>
* `INTJ` (1; 1% of non-empty `ExtPos`): <em>de</em>
* `PRON` (3; 3% of non-empty `ExtPos`): <em>de</em>
* `PROPN` (1; 1% of non-empty `ExtPos`): <em>De</em>
* `SCONJ` (26; 28% of non-empty `ExtPos`): <em>per, a, coma, d', de, Pèr, Tre, abans, alevat, aprèp</em>

<table>
  <tr><th>Paradigm <i>de</i></th><th><tt>ADP</tt></th><th><tt>ADV</tt></th><th><tt>INTJ</tt></th><th><tt>PRON</tt></th><th><tt>PROPN</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>de</em></td><td><em>de, d'</em></td><td><em>de</em></td><td><em>de</em></td><td><em>De</em></td><td><em>d', de</em></td></tr>
</table>

### `ADV`

54 <tt><a href="oc_ttb-pos-ADV.html">ADV</a></tt> tokens (4% of all `ADV` tokens) have a non-empty value of `ExtPos`.

`ADV` tokens may have the following values of `ExtPos`:

* `ADP` (2; 4% of non-empty `ExtPos`): <em>Mai, ençà</em>
* `ADV` (22; 41% of non-empty `ExtPos`): <em>mai, ça, pauc, tant, Tot, Uei, ara, açai, se, un</em>
* `CCONJ` (1; 2% of non-empty `ExtPos`): <em>tanplan</em>
* `DET` (18; 33% of non-empty `ExtPos`): <em>mai, cap, gaire, ges, aitant, pro, tant</em>
* `SCONJ` (11; 20% of non-empty `ExtPos`): <em>abans, tant, 'vances, afin, avant, mèslèu, pr'amor, tan</em>

<table>
  <tr><th>Paradigm <i>mai</i></th><th><tt>ADP</tt></th><th><tt>ADV</tt></th><th><tt>DET</tt></th></tr>
  <tr><td><tt></tt></td><td><em>Mai</em></td><td><em>mai</em></td><td><em>mai</em></td></tr>
</table>

`ExtPos` seems to be **lexical feature** of `ADV`. 92% lemmas (23) occur only with one value of `ExtPos`.

### `DET`

9 <tt><a href="oc_ttb-pos-DET.html">DET</a></tt> tokens (0% of all `DET` tokens) have a non-empty value of `ExtPos`.

`DET` tokens may have the following values of `ExtPos`:

* `ADV` (1; 11% of non-empty `ExtPos`): <em>un</em>
* `DET` (3; 33% of non-empty `ExtPos`): <em>mai, qualqu', tant</em>
* `PRON` (4; 44% of non-empty `ExtPos`): <em>qualqu', qualques, quauqua</em>
* `SCONJ` (1; 11% of non-empty `ExtPos`): <em>Un</em>

<table>
  <tr><th>Paradigm <i>un</i></th><th><tt>ADV</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>un</em></td><td><em>Un</em></td></tr>
</table>

### `CCONJ`

5 <tt><a href="oc_ttb-pos-CCONJ.html">CCONJ</a></tt> tokens (1% of all `CCONJ` tokens) have a non-empty value of `ExtPos`.

`CCONJ` tokens may have the following values of `ExtPos`:

* `ADV` (1; 20% of non-empty `ExtPos`): <em>E</em>
* `CCONJ` (1; 20% of non-empty `ExtPos`): <em>e</em>
* `SCONJ` (3; 60% of non-empty `ExtPos`): <em>e, mas</em>

<table>
  <tr><th>Paradigm <i>e</i></th><th><tt>ADV</tt></th><th><tt>CCONJ</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>E</em></td><td><em>e</em></td><td><em>e</em></td></tr>
</table>

### `SCONJ`

5 <tt><a href="oc_ttb-pos-SCONJ.html">SCONJ</a></tt> tokens (1% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

`SCONJ` tokens may have the following values of `ExtPos`:

* `SCONJ` (5; 100% of non-empty `ExtPos`): <em>mentre, se, coma</em>

### `NOUN`

4 <tt><a href="oc_ttb-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `ExtPos`.

`NOUN` tokens may have the following values of `ExtPos`:

* `ADP` (3; 75% of non-empty `ExtPos`): <em>cap, ras</em>
* `ADV` (1; 25% of non-empty `ExtPos`): <em>Fin</em>

### `PRON`

3 <tt><a href="oc_ttb-pos-PRON.html">PRON</a></tt> tokens (0% of all `PRON` tokens) have a non-empty value of `ExtPos`.

`PRON` tokens may have the following values of `ExtPos`:

* `ADV` (3; 100% of non-empty `ExtPos`): <em>Aquò, un, çai</em>

### `X`

3 <tt><a href="oc_ttb-pos-X.html">X</a></tt> tokens (16% of all `X` tokens) have a non-empty value of `ExtPos`.

`X` tokens may have the following values of `ExtPos`:

* `PRON` (3; 100% of non-empty `ExtPos`): <em>z', -z-</em>

### `ADJ`

1 <tt><a href="oc_ttb-pos-ADJ.html">ADJ</a></tt> tokens (0% of all `ADJ` tokens) have a non-empty value of `ExtPos`.

`ADJ` tokens may have the following values of `ExtPos`:

* `DET` (1; 100% of non-empty `ExtPos`): <em>quantes</em>

### `AUX`

1 <tt><a href="oc_ttb-pos-AUX.html">AUX</a></tt> tokens (0% of all `AUX` tokens) have a non-empty value of `ExtPos`.

`AUX` tokens may have the following values of `ExtPos`:

* `CCONJ` (1; 100% of non-empty `ExtPos`): <em>es</em>

### `PART`

1 <tt><a href="oc_ttb-pos-PART.html">PART</a></tt> tokens (1% of all `PART` tokens) have a non-empty value of `ExtPos`.

`PART` tokens may have the following values of `ExtPos`:

* `ADV` (1; 100% of non-empty `ExtPos`): <em>d'</em>

