---
layout: base
title:  'Statistics of Aspect in UD_Hausa-SouthernAutogramm'
udver: '2'
---

## Treebank Statistics: UD_Hausa-SouthernAutogramm: Features: `Aspect`

This feature is universal but the values `Aor`, `Hab`, `Iter`, `PerfBkg`, `PerfNeg`, `ProgBkg`, `ProgNeg` are language-specific.
It occurs with 9 different values: `Aor`, `Hab`, `Iter`, `Perf`, `PerfBkg`, `PerfNeg`, `Prog`, `ProgBkg`, `ProgNeg`.

1772 tokens (12%) have a non-empty value of `Aspect`.
76 types (4%) occur at least once with a non-empty value of `Aspect`.
2 lemmas (0%) occur at least once with a non-empty value of `Aspect`.
The feature is used with 2 part-of-speech tags: <tt><a href="ha_southernautogramm-pos-AUX.html">AUX</a></tt> (1753; 12% instances), <tt><a href="ha_southernautogramm-pos-PART.html">PART</a></tt> (19; 0% instances).

### `AUX`

1753 <tt><a href="ha_southernautogramm-pos-AUX.html">AUX</a></tt> tokens (81% of all `AUX` tokens) have a non-empty value of `Aspect`.

The most frequent other feature values with which `AUX` and `Aspect` co-occurred: <tt><a href="ha_southernautogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (1245; 71%).

`AUX` tokens may have the following values of `Aspect`:

* `Aor` (347; 20% of non-empty `Aspect`): <em>kà, à, yà, mù, tà, sù, ìn, kù, kì, shì</em>
* `Hab` (71; 4% of non-empty `Aspect`): <em>mukàn, kukàn, takàn, yakàn, akàn, sukàn</em>
* `Perf` (416; 24% of non-empty `Aspect`): <em>yaː, kaː, an, naː, sun, mun, taː, kin, kun, am</em>
* `PerfBkg` (580; 33% of non-empty `Aspect`): <em>ya, ta, akà, sukà, mukà, ka, na, kikà, kukà, kakèː</em>
* `PerfNeg` (56; 3% of non-empty `Aspect`): <em>bàkà, bàn, bài, bàmù, bàʼà, bàsù, bàtà, bàkì</em>
* `Prog` (168; 10% of non-empty `Aspect`): <em>anàː, yanàː, munàː, sunàː, inàː, kanàː, nàː, tanàː, kunàː, kinàː</em>
* `ProgBkg` (87; 5% of non-empty `Aspect`): <em>akèː, mukèː, kukèː, sukèː, yakèː, kèː, kakèː, kukà, takèː, kikèː</em>
* `ProgNeg` (28; 2% of non-empty `Aspect`): <em>baː'àː, baːkàː, baːmàː, baːyàː, bân, baːkà, baːtà, baːnàː, baːsàː, baːsù</em>
* `EMPTY` (407): <em>nèː, zâi, zân, zaːkà, yanàː, zaːsù, zaːʼà, yakè, zaːtà, inàː</em>

### `PART`

19 <tt><a href="ha_southernautogramm-pos-PART.html">PART</a></tt> tokens (2% of all `PART` tokens) have a non-empty value of `Aspect`.

The most frequent other feature values with which `PART` and `Aspect` co-occurred: <tt><a href="ha_southernautogramm-feat-PartType.html">PartType</a></tt><tt>=Adv</tt> (19; 100%), <tt><a href="ha_southernautogramm-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (19; 100%).

`PART` tokens may have the following values of `Aspect`:

* `Iter` (19; 100% of non-empty `Aspect`): <em>ta</em>
* `EMPTY` (849): <em>ba, maː, dai, kuma, nèː, gàː, kèːnan, àkwai, bàː, fa</em>

## Relations with Agreement in `Aspect`

The 10 most frequent relations where parent and child node agree in `Aspect`:
<tt>AUX --[<tt><a href="ha_southernautogramm-dep-reparandum.html">reparandum</a></tt>]--> AUX</tt> (5; 83%),
<tt>AUX --[<tt><a href="ha_southernautogramm-dep-parataxis.html">parataxis</a></tt>]--> AUX</tt> (2; 100%).

