---
layout: base
title:  'Statistics of Foreign in UD_Lithuanian-ALKSNIS'
udver: '2'
---

## Treebank Statistics: UD_Lithuanian-ALKSNIS: Features: `Foreign`

This feature is universal.
It occurs with 1 different values: `Yes`.

130 tokens (0%) have a non-empty value of `Foreign`.
107 types (1%) occur at least once with a non-empty value of `Foreign`.
106 lemmas (1%) occur at least once with a non-empty value of `Foreign`.
The feature is used with 1 part-of-speech tags: <tt><a href="lt_alksnis-pos-X.html">X</a></tt> (130; 0% instances).

### `X`

130 <tt><a href="lt_alksnis-pos-X.html">X</a></tt> tokens (8% of all `X` tokens) have a non-empty value of `Foreign`.

The most frequent other feature values with which `X` and `Foreign` co-occurred: <tt><a href="lt_alksnis-feat-Abbr.html">Abbr</a></tt><tt>=EMPTY</tt> (130; 100%), <tt><a href="lt_alksnis-feat-Hyph.html">Hyph</a></tt><tt>=EMPTY</tt> (126; 97%).

`X` tokens may have the following values of `Foreign`:

* `Yes` (130; 100% of non-empty `Foreign`): <em>Levin, Baltic, Via, Cilley, Inter, Baltica, Digital, Europe, Gilgun, Kosmos</em>
* `EMPTY` (1441): <em>pat, ES, d, proc, Nr, nors, p, to, a, tūkst</em>

`Foreign` seems to be **lexical feature** of `X`. 100% lemmas (106) occur only with one value of `Foreign`.

## Relations with Agreement in `Foreign`

The 10 most frequent relations where parent and child node agree in `Foreign`:
<tt>X --[<tt><a href="lt_alksnis-dep-flat-foreign.html">flat:foreign</a></tt>]--> X</tt> (31; 100%),
<tt>X --[<tt><a href="lt_alksnis-dep-conj.html">conj</a></tt>]--> X</tt> (4; 67%).

