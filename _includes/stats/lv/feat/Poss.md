

--------------------------------------------------------------------------------

## Treebank Statistics (UD_Latvian)

This feature is universal.
It occurs with 1 different values: `Yes`.

44 tokens (0%) have a non-empty value of `Poss`.
17 types (0%) occur at least once with a non-empty value of `Poss`.
6 lemmas (0%) occur at least once with a non-empty value of `Poss`.
The feature is used with 2 part-of-speech tags: [lv-pos/DET]() (40; 0% instances), [lv-pos/PRON]() (4; 0% instances).

### `DET`

40 [lv-pos/DET]() tokens (10% of all `DET` tokens) have a non-empty value of `Poss`.

The most frequent other feature values with which `DET` and `Poss` co-occurred: <tt><a href="PronType.html">PronType</a>=Prs</tt> (40; 100%), <tt><a href="Number.html">Number</a>=Sing</tt> (29; 73%), <tt><a href="Case.html">Case</a>=Acc</tt> (23; 57%).

`DET` tokens may have the following values of `Poss`:

* `Yes` (40; 100% of non-empty `Poss`): <em>savu, savā, manu, savas, saviem, Mans, mana, manas, savus, Savās</em>
* `EMPTY` (352): <em>šo, šī, tā, šajā, šāda, šādu, šīs, šis, to, visas</em>

### `PRON`

4 [lv-pos/PRON]() tokens (1% of all `PRON` tokens) have a non-empty value of `Poss`.

The most frequent other feature values with which `PRON` and `Poss` co-occurred: <tt><a href="Person.html">Person</a>=EMPTY</tt> (4; 100%), <tt><a href="PronType.html">PronType</a>=Prs</tt> (4; 100%), <tt><a href="Gender.html">Gender</a>=Fem</tt> (3; 75%), <tt><a href="Number.html">Number</a>=Sing</tt> (3; 75%).

`PRON` tokens may have the following values of `Poss`:

* `Yes` (4; 100% of non-empty `Poss`): <em>mans, savu, savās, savējo</em>
* `EMPTY` (561): <em>tas, to, viņš, es, tā, kas, viņa, ko, mēs, man</em>

