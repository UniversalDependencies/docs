---
layout: base
title:  'Statistics of ExtPos in UD_Greek-Cretan'
udver: '2'
---

## Treebank Statistics: UD_Greek-Cretan: Features: `ExtPos`

This feature is language-specific.
It occurs with 6 different values: `ADJ`, `ADP`, `ADV`, `CCONJ`, `INTJ`, `SCONJ`.

33 tokens (1%) have a non-empty value of `ExtPos`.
22 types (2%) occur at least once with a non-empty value of `ExtPos`.
20 lemmas (2%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 6 part-of-speech tags: <tt><a href="el_cretan-pos-ADV.html">ADV</a></tt> (16; 0% instances), <tt><a href="el_cretan-pos-ADP.html">ADP</a></tt> (10; 0% instances), <tt><a href="el_cretan-pos-CCONJ.html">CCONJ</a></tt> (2; 0% instances), <tt><a href="el_cretan-pos-DET.html">DET</a></tt> (2; 0% instances), <tt><a href="el_cretan-pos-PART.html">PART</a></tt> (2; 0% instances), <tt><a href="el_cretan-pos-ADJ.html">ADJ</a></tt> (1; 0% instances).

### `ADV`

16 <tt><a href="el_cretan-pos-ADV.html">ADV</a></tt> tokens (9% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="el_cretan-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (16; 100%).

`ADV` tokens may have the following values of `ExtPos`:

* `ADP` (5; 31% of non-empty `ExtPos`): <em>κάτω, μέσα, απάνω</em>
* `ADV` (10; 63% of non-empty `ExtPos`): <em>γύρου, ίσα, 'δά, Σαν, Στόσο, γύρω, ούτε</em>
* `SCONJ` (1; 6% of non-empty `ExtPos`): <em>έτσι</em>

`ExtPos` seems to be **lexical feature** of `ADV`. 100% lemmas (11) occur only with one value of `ExtPos`.

### `ADP`

10 <tt><a href="el_cretan-pos-ADP.html">ADP</a></tt> tokens (7% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADV` (1; 10% of non-empty `ExtPos`): <em>για</em>
* `SCONJ` (9; 90% of non-empty `ExtPos`): <em>για, ίσαμε, πρίχου</em>

<table>
  <tr><th>Paradigm <i>για</i></th><th><tt>ADV</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>για</em></td><td><em>για</em></td></tr>
</table>

### `CCONJ`

2 <tt><a href="el_cretan-pos-CCONJ.html">CCONJ</a></tt> tokens (1% of all `CCONJ` tokens) have a non-empty value of `ExtPos`.

`CCONJ` tokens may have the following values of `ExtPos`:

* `ADV` (1; 50% of non-empty `ExtPos`): <em>και</em>
* `CCONJ` (1; 50% of non-empty `ExtPos`): <em>απού</em>

### `DET`

2 <tt><a href="el_cretan-pos-DET.html">DET</a></tt> tokens (0% of all `DET` tokens) have a non-empty value of `ExtPos`.

`DET` tokens may have the following values of `ExtPos`:

* `ADV` (1; 50% of non-empty `ExtPos`): <em>μια</em>
* `INTJ` (1; 50% of non-empty `ExtPos`): <em>Τάδε</em>

### `PART`

2 <tt><a href="el_cretan-pos-PART.html">PART</a></tt> tokens (3% of all `PART` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PART` and `ExtPos` co-occurred: <tt><a href="el_cretan-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (2; 100%).

`PART` tokens may have the following values of `ExtPos`:

* `ADV` (1; 50% of non-empty `ExtPos`): <em>Μπας</em>
* `SCONJ` (1; 50% of non-empty `ExtPos`): <em>μπας</em>

<table>
  <tr><th>Paradigm <i>μπας</i></th><th><tt>ADV</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>Μπας</em></td><td><em>μπας</em></td></tr>
</table>

### `ADJ`

1 <tt><a href="el_cretan-pos-ADJ.html">ADJ</a></tt> tokens (1% of all `ADJ` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADJ` and `ExtPos` co-occurred: <tt><a href="el_cretan-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (1; 100%), <tt><a href="el_cretan-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (1; 100%), <tt><a href="el_cretan-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (1; 100%).

`ADJ` tokens may have the following values of `ExtPos`:

* `ADJ` (1; 100% of non-empty `ExtPos`): <em>μπλε</em>

