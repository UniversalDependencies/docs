---
layout: base
title:  'Statistics of NumForm in UD_Czech-Poetry'
udver: '2'
---

## Treebank Statistics: UD_Czech-Poetry: Features: `NumForm`

This feature is language-specific.
It occurs with 2 different values: `Digit`, `Word`.

18 tokens (0%) have a non-empty value of `NumForm`.
14 types (1%) occur at least once with a non-empty value of `NumForm`.
10 lemmas (1%) occur at least once with a non-empty value of `NumForm`.
The feature is used with 1 part-of-speech tags: <tt><a href="cs_poetry-pos-NUM.html">NUM</a></tt> (18; 0% instances).

### `NUM`

18 <tt><a href="cs_poetry-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumForm`.

The most frequent other feature values with which `NUM` and `NumForm` co-occurred: <tt><a href="cs_poetry-feat-NumType.html">NumType</a></tt><tt>=Card</tt> (18; 100%), <tt><a href="cs_poetry-feat-Number.html">Number</a></tt><tt>=Sing</tt> (12; 67%).

`NUM` tokens may have the following values of `NumForm`:

* `Digit` (1; 6% of non-empty `NumForm`): <em>80</em>
* `Word` (17; 94% of non-empty `NumForm`): <em>jeden, dvé, Deset, dva, dvacet, jedna, jednom, jednou, jedné, obou</em>

`NumForm` seems to be **lexical feature** of `NUM`. 100% lemmas (10) occur only with one value of `NumForm`.

