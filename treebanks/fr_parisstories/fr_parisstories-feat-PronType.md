---
layout: base
title:  'Statistics of PronType in UD_French-ParisStories'
udver: '2'
---

## Treebank Statistics: UD_French-ParisStories: Features: `PronType`

This feature is universal.
It occurs with 7 different values: `Art`, `Dem`, `Ind`, `Int`, `Neg`, `Prs`, `Rel`.

6642 tokens (22%) have a non-empty value of `PronType`.
97 types (3%) occur at least once with a non-empty value of `PronType`.
59 lemmas (3%) occur at least once with a non-empty value of `PronType`.
The feature is used with 4 part-of-speech tags: <tt><a href="fr_parisstories-pos-PRON.html">PRON</a></tt> (4141; 14% instances), <tt><a href="fr_parisstories-pos-DET.html">DET</a></tt> (2481; 8% instances), <tt><a href="fr_parisstories-pos-ADJ.html">ADJ</a></tt> (10; 0% instances), <tt><a href="fr_parisstories-pos-ADV.html">ADV</a></tt> (10; 0% instances).

### `PRON`

4141 <tt><a href="fr_parisstories-pos-PRON.html">PRON</a></tt> tokens (96% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="fr_parisstories-feat-Number.html">Number</a></tt><tt>=Sing</tt> (3176; 77%), <tt><a href="fr_parisstories-feat-Person.html">Person</a></tt><tt>=3</tt> (2661; 64%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (765; 18% of non-empty `PronType`): <em>c', ça, ce, -ce, celui, ceux, cela, celui-là</em>
* `Ind` (634; 15% of non-empty `PronType`): <em>on, un, tous, tout, quoi, une, chacun, quelqu'un, autre, autrui</em>
* `Int` (14; 0% of non-empty `PronType`): <em>qu', quoi</em>
* `Neg` (16; 0% of non-empty `PronType`): <em>rien, personne</em>
* `Prs` (2448; 59% of non-empty `PronType`): <em>je, il, j', y, elle, me, tu, s', se, ils</em>
* `Rel` (264; 6% of non-empty `PronType`): <em>qui, où, que, qu', quoi, dont, auxquelles, lesquelles</em>
* `EMPTY` (151): <em>qui, nous, me, que, m', ce, t', où, qu', tous</em>

<table>
  <tr><th>Paradigm <i>quoi</i></th><th><tt>Int</tt></th><th><tt>Rel</tt></th><th><tt>Ind</tt></th></tr>
  <tr><td><tt>_</tt></td><td></td><td><em>quoi</em></td><td></td></tr>
  <tr><td><tt><tt><a href="fr_parisstories-feat-Person.html">Person</a></tt><tt>=3</tt></tt></td><td><em>quoi</em></td><td></td><td><em>quoi</em></td></tr>
</table>

`PronType` seems to be **lexical feature** of `PRON`. 92% lemmas (36) occur only with one value of `PronType`.

### `DET`

2481 <tt><a href="fr_parisstories-pos-DET.html">DET</a></tt> tokens (95% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="fr_parisstories-feat-Number-psor.html">Number[psor]</a></tt><tt>=EMPTY</tt> (2182; 88%), <tt><a href="fr_parisstories-feat-Person-psor.html">Person[psor]</a></tt><tt>=EMPTY</tt> (2182; 88%), <tt><a href="fr_parisstories-feat-Number.html">Number</a></tt><tt>=Sing</tt> (1957; 79%), <tt><a href="fr_parisstories-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (1358; 55%).

`DET` tokens may have the following values of `PronType`:

* `Art` (2053; 83% of non-empty `PronType`): <em>le, la, un, les, une, l', des, du, de, d'</em>
* `Dem` (74; 3% of non-empty `PronType`): <em>cette, ce, ces, cet</em>
* `Ind` (45; 2% of non-empty `PronType`): <em>quelque, chaque, quelques, plusieurs, quelqu', certaines, différents, toute</em>
* `Int` (3; 0% of non-empty `PronType`): <em>quelle</em>
* `Neg` (3; 0% of non-empty `PronType`): <em>aucune, aucun</em>
* `Prs` (303; 12% of non-empty `PronType`): <em>mon, ma, mes, son, notre, nos, sa, ses, leur, leurs</em>
* `EMPTY` (141): <em>le, des, son, les, de, du, quelqu', ta, quelques, aucune</em>

`PronType` seems to be **lexical feature** of `DET`. 100% lemmas (25) occur only with one value of `PronType`.

### `ADJ`

10 <tt><a href="fr_parisstories-pos-ADJ.html">ADJ</a></tt> tokens (1% of all `ADJ` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADJ` and `PronType` co-occurred: <tt><a href="fr_parisstories-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (9; 90%), <tt><a href="fr_parisstories-feat-Number.html">Number</a></tt><tt>=Sing</tt> (9; 90%).

`ADJ` tokens may have the following values of `PronType`:

* `Ind` (10; 100% of non-empty `PronType`): <em>tout, tous, toute</em>
* `EMPTY` (864): <em>tout, petit, petite, tous, autre, première, bonne, même, vrai, sympa</em>

### `ADV`

10 <tt><a href="fr_parisstories-pos-ADV.html">ADV</a></tt> tokens (0% of all `ADV` tokens) have a non-empty value of `PronType`.

`ADV` tokens may have the following values of `PronType`:

* `Rel` (10; 100% of non-empty `PronType`): <em>où</em>
* `EMPTY` (2368): <em>donc, pas, vraiment, enfin, là, plus, très, tout, bien, même</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>DET --[<tt><a href="fr_parisstories-dep-reparandum.html">reparandum</a></tt>]--> DET</tt> (75; 99%),
<tt>PRON --[<tt><a href="fr_parisstories-dep-reparandum.html">reparandum</a></tt>]--> PRON</tt> (70; 91%),
<tt>DET --[<tt><a href="fr_parisstories-dep-advmod.html">advmod</a></tt>]--> DET</tt> (2; 100%),
<tt>DET --[<tt><a href="fr_parisstories-dep-reparandum.html">reparandum</a></tt>]--> PRON</tt> (1; 100%),
<tt>PRON --[<tt><a href="fr_parisstories-dep-discourse.html">discourse</a></tt>]--> PRON</tt> (1; 100%).

