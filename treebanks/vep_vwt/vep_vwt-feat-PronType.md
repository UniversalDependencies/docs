---
layout: base
title:  'Statistics of PronType in UD_Veps-VWT'
udver: '2'
---

## Treebank Statistics: UD_Veps-VWT: Features: `PronType`

This feature is universal.
It occurs with 5 different values: `Dem`, `Ind`, `Int`, `Prs`, `Tot`.

168 tokens (13%) have a non-empty value of `PronType`.
69 types (12%) occur at least once with a non-empty value of `PronType`.
17 lemmas (5%) occur at least once with a non-empty value of `PronType`.
The feature is used with 1 part-of-speech tags: <tt><a href="vep_vwt-pos-PRON.html">PRON</a></tt> (168; 13% instances).

### `PRON`

168 <tt><a href="vep_vwt-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="vep_vwt-feat-Reflex.html">Reflex</a></tt><tt>=EMPTY</tt> (149; 89%), <tt><a href="vep_vwt-feat-Number.html">Number</a></tt><tt>=Sing</tt> (120; 71%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (36; 21% of non-empty `PronType`): <em>nece, se, necen, sen, neche, necidä, neciš, Ned, Niiš, necil</em>
* `Ind` (2; 1% of non-empty `PronType`): <em>midä-se</em>
* `Int` (12; 7% of non-empty `PronType`): <em>kudambad, mitte, Ken, Mi, Mittušt, ked, kudambal, midä, mugoine</em>
* `Prs` (102; 61% of non-empty `PronType`): <em>minä, ičeze, meiden, minun, mö, minei, hän, heiden, Tö, ičein</em>
* `Tot` (16; 10% of non-empty `PronType`): <em>kaik, kaiken, kaikes, kaiked, kaikile, kaikse</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (17) occur only with one value of `PronType`.

