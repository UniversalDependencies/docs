---
layout: base
title:  'Statistics of ExtPos in UD_Zaar-Autogramm'
udver: '2'
---

## Treebank Statistics: UD_Zaar-Autogramm: Features: `ExtPos`

This feature is language-specific.
It occurs with 10 different values: `ADJ`, `ADP`, `ADV`, `AUX`, `INTJ`, `NOUN`, `PART`, `PRON`, `SCONJ`, `VERB`.

244 tokens (1%) have a non-empty value of `ExtPos`.
123 types (5%) occur at least once with a non-empty value of `ExtPos`.
92 lemmas (7%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 9 part-of-speech tags: <tt><a href="say_autogramm-pos-VERB.html">VERB</a></tt> (154; 1% instances), <tt><a href="say_autogramm-pos-ADP.html">ADP</a></tt> (35; 0% instances), <tt><a href="say_autogramm-pos-SCONJ.html">SCONJ</a></tt> (24; 0% instances), <tt><a href="say_autogramm-pos-ADV.html">ADV</a></tt> (11; 0% instances), <tt><a href="say_autogramm-pos-NOUN.html">NOUN</a></tt> (6; 0% instances), <tt><a href="say_autogramm-pos-PRON.html">PRON</a></tt> (6; 0% instances), <tt><a href="say_autogramm-pos-DET.html">DET</a></tt> (4; 0% instances), <tt><a href="say_autogramm-pos-AUX.html">AUX</a></tt> (3; 0% instances), <tt><a href="say_autogramm-pos-PART.html">PART</a></tt> (1; 0% instances).

### `VERB`

154 <tt><a href="say_autogramm-pos-VERB.html">VERB</a></tt> tokens (5% of all `VERB` tokens) have a non-empty value of `ExtPos`.

`VERB` tokens may have the following values of `ExtPos`:

* `ADV` (1; 1% of non-empty `ExtPos`): <em>yi</em>
* `NOUN` (148; 96% of non-empty `ExtPos`): <em>láːpkə́nì, mbútkə̂n, láːpkə̂n, ʧáːɣə̂n, maráɣə̂n, vyàːɣə̀n, ŋálɣə́nì, ɬyáɣə̂n, fíɣə̂n, gìːɣə̀n</em>
* `PART` (1; 1% of non-empty `ExtPos`): <em>wu</em>
* `SCONJ` (1; 1% of non-empty `ExtPos`): <em>naː</em>
* `VERB` (3; 2% of non-empty `ExtPos`): <em>wul, fí</em>

<table>
  <tr><th>Paradigm <i>fi</i></th><th><tt>NOUN</tt></th><th><tt>VERB</tt></th></tr>
  <tr><td><tt>_</tt></td><td></td><td><em>fí</em></td></tr>
  <tr><td><tt><tt><a href="say_autogramm-feat-VerbForm.html">VerbForm</a></tt><tt>=Vnoun</tt></tt></td><td><em>fíɣə̂n, fíɣə́nì</em></td><td></td></tr>
</table>

`ExtPos` seems to be **lexical feature** of `VERB`. 97% lemmas (61) occur only with one value of `ExtPos`.

### `ADP`

35 <tt><a href="say_autogramm-pos-ADP.html">ADP</a></tt> tokens (5% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (27; 77% of non-empty `ExtPos`): <em>á, ɗa, ɗan, ɣá, kàmán, nə́, séː, teː, dón, hár</em>
* `ADV` (1; 3% of non-empty `ExtPos`): <em>kàmán</em>
* `SCONJ` (7; 20% of non-empty `ExtPos`): <em>ɗan, séː</em>

<table>
  <tr><th>Paradigm <i>ɗan</i></th><th><tt>ADP</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>ɗan</em></td><td><em>ɗan</em></td></tr>
</table>

### `SCONJ`

24 <tt><a href="say_autogramm-pos-SCONJ.html">SCONJ</a></tt> tokens (4% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `SCONJ` and `ExtPos` co-occurred: <tt><a href="say_autogramm-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (24; 100%).

`SCONJ` tokens may have the following values of `ExtPos`:

* `ADP` (2; 8% of non-empty `ExtPos`): <em>tu, ɗan</em>
* `PART` (1; 4% of non-empty `ExtPos`): <em>tu</em>
* `SCONJ` (20; 83% of non-empty `ExtPos`): <em>ɗan, séː, tu, kàmán, ín, sábòːtu, ìdán</em>
* `VERB` (1; 4% of non-empty `ExtPos`): <em>tu</em>

<table>
  <tr><th>Paradigm <i>tu</i></th><th><tt>ADP</tt></th><th><tt>PART</tt></th><th><tt>SCONJ</tt></th><th><tt>VERB</tt></th></tr>
  <tr><td><tt></tt></td><td><em>tu</em></td><td><em>tu</em></td><td><em>tu</em></td><td><em>tu</em></td></tr>
</table>

### `ADV`

11 <tt><a href="say_autogramm-pos-ADV.html">ADV</a></tt> tokens (1% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="say_autogramm-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (10; 91%).

`ADV` tokens may have the following values of `ExtPos`:

* `ADV` (7; 64% of non-empty `ExtPos`): <em>dóːlêː, séː, dúk, ngaːláy</em>
* `INTJ` (1; 9% of non-empty `ExtPos`): <em>tá</em>
* `PRON` (1; 9% of non-empty `ExtPos`): <em>yádda</em>
* `SCONJ` (2; 18% of non-empty `ExtPos`): <em>séː</em>

<table>
  <tr><th>Paradigm <i>séː</i></th><th><tt>ADV</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>séː</em></td><td><em>séː</em></td></tr>
</table>

### `NOUN`

6 <tt><a href="say_autogramm-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `NOUN` and `ExtPos` co-occurred: <tt><a href="say_autogramm-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (5; 83%).

`NOUN` tokens may have the following values of `ExtPos`:

* `ADJ` (4; 67% of non-empty `ExtPos`): <em>gwaɬkay, naː, ʃéːgè, ʒèy</em>
* `NOUN` (2; 33% of non-empty `ExtPos`): <em>naː</em>

<table>
  <tr><th>Paradigm <i>naː</i></th><th><tt>ADJ</tt></th><th><tt>NOUN</tt></th></tr>
  <tr><td><tt></tt></td><td><em>naː</em></td><td><em>naː</em></td></tr>
</table>

### `PRON`

6 <tt><a href="say_autogramm-pos-PRON.html">PRON</a></tt> tokens (0% of all `PRON` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PRON` and `ExtPos` co-occurred: <tt><a href="say_autogramm-feat-Person.html">Person</a></tt><tt>=3</tt> (5; 83%), <tt><a href="say_autogramm-feat-PronType.html">PronType</a></tt><tt>=Prs</tt> (4; 67%).

`PRON` tokens may have the following values of `ExtPos`:

* `ADP` (3; 50% of non-empty `ExtPos`): <em>ʧì</em>
* `ADV` (2; 33% of non-empty `ExtPos`): <em>tə</em>
* `PART` (1; 17% of non-empty `ExtPos`): <em>yâːn</em>

### `DET`

4 <tt><a href="say_autogramm-pos-DET.html">DET</a></tt> tokens (1% of all `DET` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `DET` and `ExtPos` co-occurred: <tt><a href="say_autogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (4; 100%), <tt><a href="say_autogramm-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (3; 75%).

`DET` tokens may have the following values of `ExtPos`:

* `ADV` (1; 25% of non-empty `ExtPos`): <em>kóː</em>
* `NOUN` (1; 25% of non-empty `ExtPos`): <em>káp</em>
* `PRON` (2; 50% of non-empty `ExtPos`): <em>gón</em>

### `AUX`

3 <tt><a href="say_autogramm-pos-AUX.html">AUX</a></tt> tokens (0% of all `AUX` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `AUX` and `ExtPos` co-occurred: <tt><a href="say_autogramm-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (3; 100%), <tt><a href="say_autogramm-feat-Aspect.html">Aspect</a></tt><tt>=EMPTY</tt> (2; 67%), <tt><a href="say_autogramm-feat-Number.html">Number</a></tt><tt>=Sing</tt> (2; 67%), <tt><a href="say_autogramm-feat-Person.html">Person</a></tt><tt>=3</tt> (2; 67%), <tt><a href="say_autogramm-feat-Tense.html">Tense</a></tt><tt>=Rem</tt> (2; 67%).

`AUX` tokens may have the following values of `ExtPos`:

* `ADV` (2; 67% of non-empty `ExtPos`): <em>átâ</em>
* `AUX` (1; 33% of non-empty `ExtPos`): <em>yi</em>

### `PART`

1 <tt><a href="say_autogramm-pos-PART.html">PART</a></tt> tokens (0% of all `PART` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PART` and `ExtPos` co-occurred: <tt><a href="say_autogramm-feat-PartType.html">PartType</a></tt><tt>=EMPTY</tt> (1; 100%), <tt><a href="say_autogramm-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (1; 100%).

`PART` tokens may have the following values of `ExtPos`:

* `NOUN` (1; 100% of non-empty `ExtPos`): <em>nə</em>

## Relations with Agreement in `ExtPos`

The 10 most frequent relations where parent and child node agree in `ExtPos`:
<tt>NOUN --[<tt><a href="say_autogramm-dep-conj.html">conj</a></tt>]--> NOUN</tt> (1; 100%),
<tt>VERB --[<tt><a href="say_autogramm-dep-acl-relcl.html">acl:relcl</a></tt>]--> VERB</tt> (1; 100%),
<tt>VERB --[<tt><a href="say_autogramm-dep-compound.html">compound</a></tt>]--> VERB</tt> (1; 100%).

