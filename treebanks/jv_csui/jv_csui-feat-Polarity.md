---
layout: base
title:  'Statistics of Polarity in UD_Javanese-CSUI'
udver: '2'
---

## Treebank Statistics: UD_Javanese-CSUI: Features: `Polarity`

This feature is universal.
It occurs with 1 different values: `Neg`.

161 tokens (1%) have a non-empty value of `Polarity`.
13 types (0%) occur at least once with a non-empty value of `Polarity`.
1 lemmas (0) occur at least once with a non-empty value of `Polarity`.
The feature is used with 1 part-of-speech tags: <tt><a href="jv_csui-pos-PART.html">PART</a></tt> (161; 1% instances).

### `PART`

161 <tt><a href="jv_csui-pos-PART.html">PART</a></tt> tokens (69% of all `PART` tokens) have a non-empty value of `Polarity`.

The most frequent other feature values with which `PART` and `Polarity` co-occurred: <tt><a href="jv_csui-feat-Polite.html">Polite</a></tt><tt>=Infm</tt> (154; 96%).

`PART` tokens may have the following values of `Polarity`:

* `Neg` (161; 100% of non-empty `Polarity`): <em>ora, durung, nora, ra, aja, mboten, dudu, boten, tan</em>
* `EMPTY` (73): <em>ya, ta, gek, ning, mbok, Kok, genten, lah, lak, pun</em>

