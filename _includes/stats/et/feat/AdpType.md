

--------------------------------------------------------------------------------

## Treebank Statistics (UD_Estonian)

This feature is language-specific.
It occurs with 2 different values: `Post`, `Prep`.

4968 tokens (2%) have a non-empty value of `AdpType`.
199 types (0%) occur at least once with a non-empty value of `AdpType`.
178 lemmas (1%) occur at least once with a non-empty value of `AdpType`.
The feature is used with 1 part-of-speech tags: [et-pos/ADP]() (4968; 2% instances).

### `ADP`

4968 [et-pos/ADP]() tokens (99% of all `ADP` tokens) have a non-empty value of `AdpType`.

`ADP` tokens may have the following values of `AdpType`:

* `Post` (3938; 79% of non-empty `AdpType`): <em>eest, kohta, vahel, vastu, poole, järgi, pärast, all, peale, juurde</em>
* `Prep` (1030; 21% of non-empty `AdpType`): <em>üle, pärast, enne, koos, läbi, mööda, ilma, vastu, peale, keset</em>

<table>
  <tr><th>Paradigm <i>üle</i></th><th><tt>Post</tt></th><th><tt>Prep</tt></th></tr>
  <tr><td><tt></tt></td><td><em>üle</em></td><td><em>üle</em></td></tr>
</table>

`AdpType` seems to be **lexical feature** of `ADP`. 90% lemmas (161) occur only with one value of `AdpType`.

## Relations with Agreement in `AdpType`

The 10 most frequent relations where parent and child node agree in `AdpType`:
<tt>ADP --[<a href="../dep/conj.html">conj</a>]--> ADP</tt> (5; 100%).

