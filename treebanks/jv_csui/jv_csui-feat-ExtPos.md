---
layout: base
title:  'Statistics of ExtPos in UD_Javanese-CSUI'
udver: '2'
---

## Treebank Statistics: UD_Javanese-CSUI: Features: `ExtPos`

This feature is language-specific.
It occurs with 3 different values: `ADP`, `ADV`, `SCONJ`.

17 tokens (0%) have a non-empty value of `ExtPos`.
5 types (0%) occur at least once with a non-empty value of `ExtPos`.
1 lemmas (0) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 4 part-of-speech tags: <tt><a href="jv_csui-pos-AUX.html">AUX</a></tt> (8; 0% instances), <tt><a href="jv_csui-pos-ADV.html">ADV</a></tt> (6; 0% instances), <tt><a href="jv_csui-pos-ADP.html">ADP</a></tt> (2; 0% instances), <tt><a href="jv_csui-pos-SCONJ.html">SCONJ</a></tt> (1; 0% instances).

### `AUX`

8 <tt><a href="jv_csui-pos-AUX.html">AUX</a></tt> tokens (2% of all `AUX` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `AUX` and `ExtPos` co-occurred: <tt><a href="jv_csui-feat-Polite.html">Polite</a></tt><tt>=Form</tt> (8; 100%).

`AUX` tokens may have the following values of `ExtPos`:

* `ADP` (7; 88% of non-empty `ExtPos`): <em>inggih</em>
* `SCONJ` (1; 13% of non-empty `ExtPos`): <em>inggih</em>

### `ADV`

6 <tt><a href="jv_csui-pos-ADV.html">ADV</a></tt> tokens (1% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="jv_csui-feat-Polite.html">Polite</a></tt><tt>=EMPTY</tt> (6; 100%), <tt><a href="jv_csui-feat-PronType.html">PronType</a></tt><tt>=Int</tt> (4; 67%).

`ADV` tokens may have the following values of `ExtPos`:

* `ADV` (6; 100% of non-empty `ExtPos`): <em>Kena, babar, kadang</em>

### `ADP`

2 <tt><a href="jv_csui-pos-ADP.html">ADP</a></tt> tokens (0% of all `ADP` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADP` and `ExtPos` co-occurred: <tt><a href="jv_csui-feat-Polite.html">Polite</a></tt><tt>=Infm</tt> (2; 100%).

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (2; 100% of non-empty `ExtPos`): <em>kaya</em>

### `SCONJ`

1 <tt><a href="jv_csui-pos-SCONJ.html">SCONJ</a></tt> tokens (0% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `SCONJ` and `ExtPos` co-occurred: <tt><a href="jv_csui-feat-Polite.html">Polite</a></tt><tt>=Infm</tt> (1; 100%).

`SCONJ` tokens may have the following values of `ExtPos`:

* `SCONJ` (1; 100% of non-empty `ExtPos`): <em>kaya</em>

