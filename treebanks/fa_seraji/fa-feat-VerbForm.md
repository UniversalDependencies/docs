---
layout: base
title:  'Statistics of VerbForm in UD_Persian'
udver: '2'
---

## Treebank Statistics: UD_Persian: Features: `VerbForm`

This feature is universal.
It occurs with 3 different values: `Fin`, `Inf`, `Part`.

3276 tokens (2%) have a non-empty value of `VerbForm`.
384 types (3%) occur at least once with a non-empty value of `VerbForm`.
143 lemmas (2%) occur at least once with a non-empty value of `VerbForm`.
The feature is used with 3 part-of-speech tags: <tt><a href="fa-pos-VERB.html">VERB</a></tt> (1850; 1% instances), <tt><a href="fa-pos-AUX.html">AUX</a></tt> (1187; 1% instances), <tt><a href="fa-pos-ADJ.html">ADJ</a></tt> (239; 0% instances).

### `VERB`

1850 <tt><a href="fa-pos-VERB.html">VERB</a></tt> tokens (15% of all `VERB` tokens) have a non-empty value of `VerbForm`.

The most frequent other feature values with which `VERB` and `VerbForm` co-occurred: <tt><a href="fa-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (1820; 98%), <tt><a href="fa-feat-Tense.html">Tense</a></tt><tt>=EMPTY</tt> (1820; 98%), <tt><a href="fa-feat-Person.html">Person</a></tt><tt>=3</tt> (1761; 95%), <tt><a href="fa-feat-Number.html">Number</a></tt><tt>=Sing</tt> (1552; 84%).

`VERB` tokens may have the following values of `VerbForm`:

* `Fin` (30; 2% of non-empty `VerbForm`): بتواند، بتوانند، بتوانم، بتوانیم
* `Part` (1820; 98% of non-empty `VerbForm`): کرده، داشته، داده، شده، گرفته، آمده، کرده‌اند، نوشته، دیده، بوده
* `EMPTY` (10391): است، کرد، گفت، دارد، می‌کند، بود، کنند، کند، می‌کنند، داشت

`VerbForm` seems to be **lexical feature** of `VERB`. 98% lemmas (78) occur only with one value of `VerbForm`.

### `AUX`

1187 <tt><a href="fa-pos-AUX.html">AUX</a></tt> tokens (22% of all `AUX` tokens) have a non-empty value of `VerbForm`.

The most frequent other feature values with which `AUX` and `VerbForm` co-occurred: <tt><a href="fa-feat-Tense.html">Tense</a></tt><tt>=EMPTY</tt> (871; 73%), <tt><a href="fa-feat-Person.html">Person</a></tt><tt>=3</tt> (799; 67%), <tt><a href="fa-feat-Number.html">Number</a></tt><tt>=Sing</tt> (719; 61%).

`AUX` tokens may have the following values of `VerbForm`:

* `Fin` (316; 27% of non-empty `VerbForm`): خواهد، خواهند، نخواهد، دارد، داریم، دارند، خواهم، دارم، خواهیم، نتوانند
* `Inf` (335; 28% of non-empty `VerbForm`): باید، نباید، بایستی، می‌باید، می‌بایست، بباید، می‌شود، نمی‌باید، نبایستی، نمی‌بایست
* `Part` (536; 45% of non-empty `VerbForm`): شده، بوده، شده‌اند، نشده، بوده‌اند، نبوده، نشده‌اند، گردیده، شده‌ایم، بوده‌ایم
* `EMPTY` (4252): است، بود، شد، می‌شود، نیست، شود، باشد، هستند، ست، بودند

### `ADJ`

239 <tt><a href="fa-pos-ADJ.html">ADJ</a></tt> tokens (2% of all `ADJ` tokens) have a non-empty value of `VerbForm`.

The most frequent other feature values with which `ADJ` and `VerbForm` co-occurred: <tt><a href="fa-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (239; 100%).

`ADJ` tokens may have the following values of `VerbForm`:

* `Part` (239; 100% of non-empty `VerbForm`): آمده، ساخته، یادشده، بسته، توقیف‌شده، ناخواسته، انجام‌شده، عقب‌افتاده، کاسته، کشته
* `EMPTY` (13435): اسلامی، دیگر، سیاسی، دوم، گذشته، فرهنگی، جدید، چند، بیشتر، ملی

`VerbForm` seems to be **lexical feature** of `ADJ`. 100% lemmas (75) occur only with one value of `VerbForm`.

## Relations with Agreement in `VerbForm`

The 10 most frequent relations where parent and child node agree in `VerbForm`:
<tt>AUX --[<tt><a href="fa-dep-conj.html">conj</a></tt>]--> VERB</tt> (1; 100%).

