---
layout: base
title:  'Statistics of PronType in UD_Greek-Cretan'
udver: '2'
---

## Treebank Statistics: UD_Greek-Cretan: Features: `PronType`

This feature is universal but the values `Emp` are language-specific.
It occurs with 9 different values: `Art`, `Dem`, `Emp`, `Ind`, `Int`, `Neg`, `Prs`, `Rel`, `Tot`.

1099 tokens (25%) have a non-empty value of `PronType`.
119 types (9%) occur at least once with a non-empty value of `PronType`.
50 lemmas (5%) occur at least once with a non-empty value of `PronType`.
The feature is used with 4 part-of-speech tags: <tt><a href="el_cretan-pos-DET.html">DET</a></tt> (636; 15% instances), <tt><a href="el_cretan-pos-PRON.html">PRON</a></tt> (417; 10% instances), <tt><a href="el_cretan-pos-ADV.html">ADV</a></tt> (40; 1% instances), <tt><a href="el_cretan-pos-SCONJ.html">SCONJ</a></tt> (6; 0% instances).

### `DET`

636 <tt><a href="el_cretan-pos-DET.html">DET</a></tt> tokens (99% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="el_cretan-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (544; 86%), <tt><a href="el_cretan-feat-Number.html">Number</a></tt><tt>=Sing</tt> (491; 77%).

`DET` tokens may have the following values of `PronType`:

* `Art` (572; 90% of non-empty `PronType`): <em>ο, το, τα, η, του, τη, την, τ', τον, μια</em>
* `Dem` (24; 4% of non-empty `PronType`): <em>αυτός, αυτό, εκείνη, 'κείνη, 'κεινά, αυτές, αυτή, εκειοσές, εκειουνά, κειονέ</em>
* `Emp` (1; 0% of non-empty `PronType`): <em>ίδια</em>
* `Ind` (18; 3% of non-empty `PronType`): <em>κάτι, άλλα, άλλες, άλλη, Τάδε, άλλον, άλλος, μερικούς, μόνο, πολλούς</em>
* `Neg` (4; 1% of non-empty `PronType`): <em>καμιά, κανείς, κιαμιά</em>
* `Rel` (12; 2% of non-empty `PronType`): <em>ό,τι, όποιος, όσοι, όποια</em>
* `Tot` (5; 1% of non-empty `PronType`): <em>Κάθε, όλη, όλο, όλοι, όλους</em>
* `EMPTY` (5): <em>τα, το, τ'</em>

<table>
  <tr><th>Paradigm <i>τα</i></th><th><tt>Art</tt></th><th><tt>Ind</tt></th></tr>
  <tr><td><tt><tt><a href="el_cretan-feat-Definite.html">Definite</a></tt><tt>=Def</tt></tt></td><td><em>τα</em></td><td></td></tr>
  <tr><td><tt></tt></td><td></td><td><em>τα</em></td></tr>
</table>

`PronType` seems to be **lexical feature** of `DET`. 96% lemmas (27) occur only with one value of `PronType`.

### `PRON`

417 <tt><a href="el_cretan-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="el_cretan-feat-Number.html">Number</a></tt><tt>=Sing</tt> (342; 82%), <tt><a href="el_cretan-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (285; 68%), <tt><a href="el_cretan-feat-Person.html">Person</a></tt><tt>=3</tt> (223; 53%), <tt><a href="el_cretan-feat-Case.html">Case</a></tt><tt>=Gen</tt> (218; 52%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (2; 0% of non-empty `PronType`): <em>έτονα, τόσοσας</em>
* `Int` (19; 5% of non-empty `PronType`): <em>είντα, Ποιος, ποιο, Ποιανού, τι</em>
* `Neg` (2; 0% of non-empty `PronType`): <em>κιάνα, κιαμιά</em>
* `Prs` (365; 88% of non-empty `PronType`): <em>μου, ντου, του, το, τηνε, τζη, μας, σου, την, τονε</em>
* `Rel` (28; 7% of non-empty `PronType`): <em>που, ό,τι</em>
* `Tot` (1; 0% of non-empty `PronType`): <em>καθένας</em>
* `EMPTY` (1): <em>τόσηνα</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (18) occur only with one value of `PronType`.

### `ADV`

40 <tt><a href="el_cretan-pos-ADV.html">ADV</a></tt> tokens (23% of all `ADV` tokens) have a non-empty value of `PronType`.

`ADV` tokens may have the following values of `PronType`:

* `Int` (16; 40% of non-empty `PronType`): <em>πού, πώς, Γιάντα, Γιατ'</em>
* `Rel` (23; 57% of non-empty `PronType`): <em>που, ως, καταπώς, απου, όπου</em>
* `Tot` (1; 3% of non-empty `PronType`): <em>όλο</em>
* `EMPTY` (133): <em>μέσα, ύστερα, σαν, γύρου, καλά, ίσα, μόνο, περίσσα, πιο, Κοντό</em>

`PronType` seems to be **lexical feature** of `ADV`. 100% lemmas (10) occur only with one value of `PronType`.

### `SCONJ`

6 <tt><a href="el_cretan-pos-SCONJ.html">SCONJ</a></tt> tokens (5% of all `SCONJ` tokens) have a non-empty value of `PronType`.

`SCONJ` tokens may have the following values of `PronType`:

* `Rel` (6; 100% of non-empty `PronType`): <em>ως, που</em>
* `EMPTY` (123): <em>να, πως, άνε, που, αν, γιατί, σαν, Ανε, για, επειδή</em>

