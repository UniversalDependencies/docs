---
layout: base
title:  'Statistics of PronType in UD_Italian-KIParlaForest'
udver: '2'
---

## Treebank Statistics: UD_Italian-KIParlaForest: Features: `PronType`

This feature is universal but the values `Exc` are language-specific.
It occurs with 9 different values: `Art`, `Dem`, `Exc`, `Ind`, `Int`, `Neg`, `Prs`, `Rel`, `Tot`.

2191 tokens (23%) have a non-empty value of `PronType`.
129 types (8%) occur at least once with a non-empty value of `PronType`.
83 lemmas (7%) occur at least once with a non-empty value of `PronType`.
The feature is used with 6 part-of-speech tags: <tt><a href="it_kiparlaforest-pos-PRON.html">PRON</a></tt> (998; 11% instances), <tt><a href="it_kiparlaforest-pos-DET.html">DET</a></tt> (869; 9% instances), <tt><a href="it_kiparlaforest-pos-ADV.html">ADV</a></tt> (234; 3% instances), <tt><a href="it_kiparlaforest-pos-ADJ.html">ADJ</a></tt> (54; 1% instances), <tt><a href="it_kiparlaforest-pos-SCONJ.html">SCONJ</a></tt> (34; 0% instances), <tt><a href="it_kiparlaforest-pos-NOUN.html">NOUN</a></tt> (2; 0% instances).

### `PRON`

998 <tt><a href="it_kiparlaforest-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="it_kiparlaforest-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (709; 71%), <tt><a href="it_kiparlaforest-feat-Number.html">Number</a></tt><tt>=Sing</tt> (541; 54%).

`PRON` tokens may have the following values of `PronType`:

* `Art` (8; 1% of non-empty `PronType`): <em>l', le, lo, la, uno</em>
* `Dem` (68; 7% of non-empty `PronType`): <em>quello, questo, quella, questa, quelli, 'sta, 'ste, quelle, ciò, quel</em>
* `Ind` (85; 9% of non-empty `PronType`): <em>niente, tutti, uno, altra, altro, nessuno, tutto, qualcosa, tutta, altri</em>
* `Int` (16; 2% of non-empty `PronType`): <em>che, cosa, cos', co~, quale, qual, quante</em>
* `Prs` (731; 73% of non-empty `PronType`): <em>c', ci, lo, io, mi, me, ti, si, ne, te</em>
* `Rel` (88; 9% of non-empty `PronType`): <em>che, chi, cui, io, quanto, chiunque, quali</em>
* `Tot` (2; 0% of non-empty `PronType`): <em>quanti, tutta</em>
* `EMPTY` (4): <em>altra, ce, l', quanto</em>

<table>
  <tr><th>Paradigm <i>lo</i></th><th><tt>Prs</tt></th><th><tt>Art</tt></th><th><tt>Int</tt></th></tr>
  <tr><td><tt>_</tt></td><td><em>lo</em></td><td></td><td></td></tr>
  <tr><td><tt><tt><a href="it_kiparlaforest-feat-Definite.html">Definite</a></tt><tt>=Def</tt>|<tt><a href="it_kiparlaforest-feat-Gender.html">Gender</a></tt><tt>=Masc</tt>|<tt><a href="it_kiparlaforest-feat-Number.html">Number</a></tt><tt>=Sing</tt></tt></td><td></td><td><em>lo</em></td><td></td></tr>
  <tr><td><tt><tt><a href="it_kiparlaforest-feat-Definite.html">Definite</a></tt><tt>=Def</tt>|<tt><a href="it_kiparlaforest-feat-Number.html">Number</a></tt><tt>=Sing</tt></tt></td><td></td><td><em>l'</em></td><td></td></tr>
  <tr><td><tt><tt><a href="it_kiparlaforest-feat-Gender.html">Gender</a></tt><tt>=Masc</tt>|<tt><a href="it_kiparlaforest-feat-Number.html">Number</a></tt><tt>=Sing</tt>|<tt><a href="it_kiparlaforest-feat-Person.html">Person</a></tt><tt>=3</tt></tt></td><td><em>lo, l', qual</em></td><td></td><td></td></tr>
  <tr><td><tt><tt><a href="it_kiparlaforest-feat-Number.html">Number</a></tt><tt>=Sing</tt></tt></td><td></td><td></td><td><em>qual</em></td></tr>
  <tr><td><tt><tt><a href="it_kiparlaforest-feat-Number.html">Number</a></tt><tt>=Sing</tt>|<tt><a href="it_kiparlaforest-feat-Person.html">Person</a></tt><tt>=3</tt></tt></td><td><em>l'</em></td><td></td><td></td></tr>
</table>

### `DET`

869 <tt><a href="it_kiparlaforest-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="it_kiparlaforest-feat-Number.html">Number</a></tt><tt>=Sing</tt> (620; 71%), <tt><a href="it_kiparlaforest-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (489; 56%).

`DET` tokens may have the following values of `PronType`:

* `Art` (698; 80% of non-empty `PronType`): <em>la, il, un, le, una, l', i, gli, un', lo</em>
* `Dem` (40; 5% of non-empty `PronType`): <em>quella, questa, questo, sta, 'sta, 'sto, quel, 'sti, quell', quello</em>
* `Exc` (11; 1% of non-empty `PronType`): <em>che, quanti</em>
* `Ind` (71; 8% of non-empty `PronType`): <em>dei, delle, altra, tante, altre, tutti, tutto, altri, certi, ogni</em>
* `Int` (8; 1% of non-empty `PronType`): <em>che, quanti</em>
* `Prs` (24; 3% of non-empty `PronType`): <em>mia, lo, mio, tua, la, miei, sua, sue</em>
* `Rel` (1; 0% of non-empty `PronType`): <em>che</em>
* `Tot` (16; 2% of non-empty `PronType`): <em>tutte, tutti, tutta, tutto</em>
* `EMPTY` (3): <em>un~, u</em>

<table>
  <tr><th>Paradigm <i>che</i></th><th><tt>Int</tt></th><th><tt>Rel</tt></th><th><tt>Exc</tt></th></tr>
  <tr><td><tt></tt></td><td><em>che</em></td><td><em>che</em></td><td><em>che</em></td></tr>
</table>

### `ADV`

234 <tt><a href="it_kiparlaforest-pos-ADV.html">ADV</a></tt> tokens (22% of all `ADV` tokens) have a non-empty value of `PronType`.

`ADV` tokens may have the following values of `PronType`:

* `Ind` (3; 1% of non-empty `PronType`): <em>quant', tutte</em>
* `Int` (5; 2% of non-empty `PronType`): <em>cos', cosa, quant', quanti</em>
* `Neg` (225; 96% of non-empty `PronType`): <em>non, no, neanche, nemmeno</em>
* `Prs` (1; 0% of non-empty `PronType`): <em>cos'</em>
* `EMPTY` (810): <em>anche, più, poi, molto, sempre, così, fuori, adesso, mai, però</em>

<table>
  <tr><th>Paradigm <i>quanto</i></th><th><tt>Int</tt></th><th><tt>Ind</tt></th></tr>
  <tr><td><tt>_</tt></td><td><em>quant'</em></td><td><em>quant'</em></td></tr>
  <tr><td><tt><tt><a href="it_kiparlaforest-feat-Gender.html">Gender</a></tt><tt>=Masc</tt>|<tt><a href="it_kiparlaforest-feat-Number.html">Number</a></tt><tt>=Plur</tt></tt></td><td><em>quanti</em></td><td></td></tr>
</table>

### `ADJ`

54 <tt><a href="it_kiparlaforest-pos-ADJ.html">ADJ</a></tt> tokens (13% of all `ADJ` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADJ` and `PronType` co-occurred: <tt><a href="it_kiparlaforest-feat-Number.html">Number</a></tt><tt>=Sing</tt> (29; 54%).

`ADJ` tokens may have the following values of `PronType`:

* `Dem` (2; 4% of non-empty `PronType`): <em>stessa</em>
* `Ind` (11; 20% of non-empty `PronType`): <em>diverse, altro, altra, pochi, vari, pochissimi, tanti, tutto</em>
* `Prs` (41; 76% of non-empty `PronType`): <em>mia, miei, tua, loro, nostro, propria, proprio, suo, suoi, tue</em>
* `EMPTY` (357): <em>esatto, grande, piccola, bella, difficile, strano, altra, mezza, certo, grosso</em>

`PronType` seems to be **lexical feature** of `ADJ`. 100% lemmas (14) occur only with one value of `PronType`.

### `SCONJ`

34 <tt><a href="it_kiparlaforest-pos-SCONJ.html">SCONJ</a></tt> tokens (10% of all `SCONJ` tokens) have a non-empty value of `PronType`.

`SCONJ` tokens may have the following values of `PronType`:

* `Rel` (34; 100% of non-empty `PronType`): <em>che</em>
* `EMPTY` (295): <em>che, perché, se, quando, come, mentre, ovunque, sia</em>

### `NOUN`

2 <tt><a href="it_kiparlaforest-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `NOUN` and `PronType` co-occurred: <tt><a href="it_kiparlaforest-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (2; 100%).

`NOUN` tokens may have the following values of `PronType`:

* `Ind` (2; 100% of non-empty `PronType`): <em>granché, po'</em>
* `EMPTY` (1216): <em>tipo, casa, centro, realtà, città, anni, po', cosa, parte, senso</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="it_kiparlaforest-dep-reparandum.html">reparandum</a></tt>]--> PRON</tt> (13; 100%),
<tt>DET --[<tt><a href="it_kiparlaforest-dep-reparandum.html">reparandum</a></tt>]--> DET</tt> (11; 85%),
<tt>ADV --[<tt><a href="it_kiparlaforest-dep-reparandum.html">reparandum</a></tt>]--> ADV</tt> (4; 100%),
<tt>DET --[<tt><a href="it_kiparlaforest-dep-conj.html">conj</a></tt>]--> DET</tt> (1; 100%),
<tt>DET --[<tt><a href="it_kiparlaforest-dep-parataxis.html">parataxis</a></tt>]--> DET</tt> (1; 100%),
<tt>PRON --[<tt><a href="it_kiparlaforest-dep-conj.html">conj</a></tt>]--> PRON</tt> (1; 100%),
<tt>PRON --[<tt><a href="it_kiparlaforest-dep-iobj.html">iobj</a></tt>]--> PRON</tt> (1; 100%).

