---
layout: base
title:  'Statistics of NumType in UD_Western_Armenian-ArmTDP'
udver: '2'
---

## Treebank Statistics: UD_Western_Armenian-ArmTDP: Features: `NumType`

This feature is universal.
It occurs with 4 different values: `Card`, `Dist`, `Frac`, `Ord`.

242 tokens (1%) have a non-empty value of `NumType`.
92 types (1%) occur at least once with a non-empty value of `NumType`.
75 lemmas (1%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="hyw_armtdp-pos-NUM.html">NUM</a></tt> (183; 1% instances), <tt><a href="hyw_armtdp-pos-ADJ.html">ADJ</a></tt> (59; 0% instances).

### `NUM`

183 <tt><a href="hyw_armtdp-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="hyw_armtdp-feat-NumForm.html">NumForm</a></tt><tt>=Word</tt> (138; 75%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (177; 97% of non-empty `NumType`): <em>երկու, մէկ, երեք, չորս, հազար, հինգ, վեց, 2, 500, մէյ</em>
* `Dist` (1; 1% of non-empty `NumType`): <em>մէյմէկ</em>
* `Frac` (5; 3% of non-empty `NumType`): <em>կէս, քառորդ</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (57) occur only with one value of `NumType`.

### `ADJ`

59 <tt><a href="hyw_armtdp-pos-ADJ.html">ADJ</a></tt> tokens (2% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="hyw_armtdp-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (59; 100%).

`ADJ` tokens may have the following values of `NumType`:

* `Card` (2; 3% of non-empty `NumType`): <em>եօթանասուննոց, ութսուննոց</em>
* `Ord` (57; 97% of non-empty `NumType`): <em>առաջին, առջի, Բ, Ա, Գ, երկրորդ, Դ, Ե, Զ, Է</em>
* `EMPTY` (2713): <em>մեծ, նոր, կարելի, կարող, ընդհանուր, աղուոր, գեղեցիկ, երկար, շատ, պզտիկ</em>

`NumType` seems to be **lexical feature** of `ADJ`. 100% lemmas (18) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="hyw_armtdp-dep-flat-range.html">flat:range</a></tt>]--> NUM</tt> (6; 100%),
<tt>NUM --[<tt><a href="hyw_armtdp-dep-appos.html">appos</a></tt>]--> NUM</tt> (4; 100%),
<tt>NUM --[<tt><a href="hyw_armtdp-dep-flat.html">flat</a></tt>]--> NUM</tt> (4; 100%),
<tt>NUM --[<tt><a href="hyw_armtdp-dep-conj.html">conj</a></tt>]--> NUM</tt> (3; 100%),
<tt>NUM --[<tt><a href="hyw_armtdp-dep-flat-dist.html">flat:dist</a></tt>]--> NUM</tt> (3; 100%).

