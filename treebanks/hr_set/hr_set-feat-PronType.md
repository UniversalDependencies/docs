---
layout: base
title:  'Statistics of PronType in UD_Croatian-SET'
udver: '2'
---

## Treebank Statistics: UD_Croatian-SET: Features: `PronType`

This feature is universal.
It occurs with 7 different values: `Dem`, `Ind`, `Int`, `Neg`, `Prs`, `Rel`, `Tot`.
Some words have combined values of the feature; 1 combinations have been observed: `Int|Rel`.

14292 tokens (7%) have a non-empty value of `PronType`.
521 types (1%) occur at least once with a non-empty value of `PronType`.
96 lemmas (1%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="hr_set-pos-DET.html">DET</a></tt> (7495; 4% instances), <tt><a href="hr_set-pos-PRON.html">PRON</a></tt> (5334; 3% instances), <tt><a href="hr_set-pos-ADV.html">ADV</a></tt> (1463; 1% instances).

### `DET`

7495 <tt><a href="hr_set-pos-DET.html">DET</a></tt> tokens (97% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="hr_set-feat-Number-psor.html">Number[psor]</a></tt><tt>=EMPTY</tt> (6666; 89%), <tt><a href="hr_set-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (6666; 89%), <tt><a href="hr_set-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (5913; 79%), <tt><a href="hr_set-feat-Number.html">Number</a></tt><tt>=Sing</tt> (5100; 68%).

`DET` tokens may have the following values of `PronType`:

* `Dem` (2478; 33% of non-empty `PronType`): <em>to, ove, toga, tome, taj, te, ovog, tom, ovaj, ovo</em>
* `Ind` (441; 6% of non-empty `PronType`): <em>nekoliko, neki, neke, nekim, neka, nekog, nekih, neku, neko, nekoga</em>
* `Int,Rel` (2636; 35% of non-empty `PronType`): <em>koji, koje, koja, kojima, koju, kojoj, kojem, kojim, kojeg, kojih</em>
* `Neg` (23; 0% of non-empty `PronType`): <em>nikakve, nikakvu, nikakvih, nikakvo, nikakvi, Nikakva, nikakav, ničiju</em>
* `Prs` (1499; 20% of non-empty `PronType`): <em>svoje, svoj, svoju, svojim, njegova, svojih, njegov, njihov, naše, svojoj</em>
* `Tot` (418; 6% of non-empty `PronType`): <em>sve, svaki, svih, svaka, svakog, svega, svake, svi, svakom, svim</em>
* `EMPTY` (199): <em>više, mnogo, pola, puno, posto, manje, malo, dosta, dovoljno, najviše</em>

`PronType` seems to be **lexical feature** of `DET`. 100% lemmas (38) occur only with one value of `PronType`.

### `PRON`

5334 <tt><a href="hr_set-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="hr_set-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (3785; 71%), <tt><a href="hr_set-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (3746; 70%), <tt><a href="hr_set-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (3745; 70%), <tt><a href="hr_set-feat-Case.html">Case</a></tt><tt>=Acc</tt> (3504; 66%), <tt><a href="hr_set-feat-Reflex.html">Reflex</a></tt><tt>=Yes</tt> (2899; 54%).

`PRON` tokens may have the following values of `PronType`:

* `Ind` (78; 1% of non-empty `PronType`): <em>nešto, netko, itko, išta, nečemu, nekome, nečeg, nečega, nekog, nečem</em>
* `Int,Rel` (656; 12% of non-empty `PronType`): <em>što, tko, čime, čega, čemu, koga, kim, kome, kime, čim</em>
* `Neg` (100; 2% of non-empty `PronType`): <em>ništa, nitko, nikome, nikoga, ničeg, NIKOG</em>
* `Prs` (4487; 84% of non-empty `PronType`): <em>se, ih, mi, ga, im, mu, on, njih, nam, vam</em>
* `Tot` (13; 0% of non-empty `PronType`): <em>svatko, svačemu, svašta</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (22) occur only with one value of `PronType`.

### `ADV`

1463 <tt><a href="hr_set-pos-ADV.html">ADV</a></tt> tokens (17% of all `ADV` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADV` and `PronType` co-occurred: <tt><a href="hr_set-feat-Degree.html">Degree</a></tt><tt>=Pos</tt> (1463; 100%).

`ADV` tokens may have the following values of `PronType`:

* `Dem` (617; 42% of non-empty `PronType`): <em>sada, tako, tada, onda, tu, zato, ovdje, stoga, tamo, toliko</em>
* `Ind` (56; 4% of non-empty `PronType`): <em>ponekad, ikad, nekada, nekoliko, negdje, ikada, nekad, nekako, odnekud</em>
* `Int,Rel` (570; 39% of non-empty `PronType`): <em>kada, kad, gdje, kako, koliko, zašto, odakle</em>
* `Neg` (69; 5% of non-empty `PronType`): <em>nikad, nikada, nikako, nigdje</em>
* `Tot` (151; 10% of non-empty `PronType`): <em>uvijek, svakako, svuda, svugdje, posvuda</em>
* `EMPTY` (6965): <em>samo, još, više, također, već, posto, međutim, oko, vrlo, danas</em>

`PronType` seems to be **lexical feature** of `ADV`. 100% lemmas (41) occur only with one value of `PronType`.

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>ADV --[<tt><a href="hr_set-dep-conj.html">conj</a></tt>]--> ADV</tt> (10; 56%),
<tt>DET --[<tt><a href="hr_set-dep-conj.html">conj</a></tt>]--> DET</tt> (7; 100%),
<tt>DET --[<tt><a href="hr_set-dep-nsubj.html">nsubj</a></tt>]--> DET</tt> (4; 67%),
<tt>PRON --[<tt><a href="hr_set-dep-nsubj.html">nsubj</a></tt>]--> PRON</tt> (3; 75%),
<tt>PRON --[<tt><a href="hr_set-dep-conj.html">conj</a></tt>]--> PRON</tt> (2; 67%),
<tt>DET --[<tt><a href="hr_set-dep-appos.html">appos</a></tt>]--> DET</tt> (1; 100%),
<tt>DET --[<tt><a href="hr_set-dep-conj.html">conj</a></tt>]--> PRON</tt> (1; 100%),
<tt>DET --[<tt><a href="hr_set-dep-fixed.html">fixed</a></tt>]--> DET</tt> (1; 100%),
<tt>DET --[<tt><a href="hr_set-dep-flat.html">flat</a></tt>]--> DET</tt> (1; 100%),
<tt>DET --[<tt><a href="hr_set-dep-nmod.html">nmod</a></tt>]--> DET</tt> (1; 100%).

