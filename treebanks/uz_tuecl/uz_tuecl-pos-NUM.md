---
layout: base
title:  'Statistics of NUM in UD_Uzbek-TueCL'
udver: '2'
---

## Treebank Statistics: UD_Uzbek-TueCL: POS Tags: `NUM`

There are 3 `NUM` lemmas (1%), 4 `NUM` types (1%) and 5 `NUM` tokens (1%).
Out of 15 observed tags, the rank of `NUM` is: 13 in number of lemmas, 12 in number of types and 12 in number of tokens.

The 10 most frequent `NUM` lemmas: <em>uch, besh, ikki</em>

The 10 most frequent `NUM` types:  <em>uch, beshta, ikki, uchta</em>

The 10 most frequent ambiguous lemmas: 

The 10 most frequent ambiguous types:  



## Morphology

The form / lemma ratio of `NUM` is 1.333333 (the average of all parts of speech is 1.489437).

The 1st highest number of forms (2) was observed with the lemma “uch”: <em>uch, uchta</em>.

The 2nd highest number of forms (1) was observed with the lemma “besh”: <em>beshta</em>.

The 3rd highest number of forms (1) was observed with the lemma “ikki”: <em>ikki</em>.

`NUM` occurs with 1 features: <tt><a href="uz_tuecl-feat-NumType.html">NumType</a></tt> (5; 100% instances)

`NUM` occurs with 1 feature-value pairs: `NumType=Card`

`NUM` occurs with 1 feature combinations.
The most frequent feature combination is `NumType=Card` (5 tokens).
Examples: <em>uch, beshta, ikki, uchta</em>


## Relations

`NUM` nodes are attached to their parents using 1 different relations: <tt><a href="uz_tuecl-dep-nummod.html">nummod</a></tt> (5; 100% instances)

Parents of `NUM` nodes belong to 1 different parts of speech: <tt><a href="uz_tuecl-pos-NOUN.html">NOUN</a></tt> (5; 100% instances)

5 (100%) `NUM` nodes are leaves.

The highest child degree of a `NUM` node is 0.

