---
layout: base
title:  'Statistics of Polarity in UD_English-GENTLE'
udver: '2'
---

## Treebank Statistics: UD_English-GENTLE: Features: `Polarity`

This feature is universal.
It occurs with 2 different values: `Neg`, `Pos`.

102 tokens (1%) have a non-empty value of `Polarity`.
11 types (0%) occur at least once with a non-empty value of `Polarity`.
6 lemmas (0%) occur at least once with a non-empty value of `Polarity`.
The feature is used with 4 part-of-speech tags: <tt><a href="en_gentle-pos-PART.html">PART</a></tt> (90; 1% instances), <tt><a href="en_gentle-pos-ADV.html">ADV</a></tt> (5; 0% instances), <tt><a href="en_gentle-pos-CCONJ.html">CCONJ</a></tt> (4; 0% instances), <tt><a href="en_gentle-pos-INTJ.html">INTJ</a></tt> (3; 0% instances).

### `PART`

90 <tt><a href="en_gentle-pos-PART.html">PART</a></tt> tokens (26% of all `PART` tokens) have a non-empty value of `Polarity`.

`PART` tokens may have the following values of `Polarity`:

* `Neg` (90; 100% of non-empty `Polarity`): <em>not, n't, n’t</em>
* `EMPTY` (259): <em>to, 's, na, ', ’s, 'ta, too</em>

### `ADV`

5 <tt><a href="en_gentle-pos-ADV.html">ADV</a></tt> tokens (1% of all `ADV` tokens) have a non-empty value of `Polarity`.

The most frequent other feature values with which `ADV` and `Polarity` co-occurred: <tt><a href="en_gentle-feat-Degree.html">Degree</a></tt><tt>=Pos</tt> (5; 100%), <tt><a href="en_gentle-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (5; 100%).

`ADV` tokens may have the following values of `Polarity`:

* `Neg` (5; 100% of non-empty `Polarity`): <em>no</em>
* `EMPTY` (724): <em>then, just, so, here, also, thus, well, how, where, now</em>

### `CCONJ`

4 <tt><a href="en_gentle-pos-CCONJ.html">CCONJ</a></tt> tokens (1% of all `CCONJ` tokens) have a non-empty value of `Polarity`.

`CCONJ` tokens may have the following values of `Polarity`:

* `Neg` (4; 100% of non-empty `Polarity`): <em>neither, nor</em>
* `EMPTY` (614): <em>and, or, but, &, either, /, plus, yet, both, +</em>

### `INTJ`

3 <tt><a href="en_gentle-pos-INTJ.html">INTJ</a></tt> tokens (5% of all `INTJ` tokens) have a non-empty value of `Polarity`.

`INTJ` tokens may have the following values of `Polarity`:

* `Pos` (3; 100% of non-empty `Polarity`): <em>Yes, Yeah</em>
* `EMPTY` (57): <em>fucking, please, ah, well, So, oh, okay, uh, Ha, Welcome</em>

