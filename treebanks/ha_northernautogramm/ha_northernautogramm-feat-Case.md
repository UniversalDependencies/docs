---
layout: base
title:  'Statistics of Case in UD_Hausa-NorthernAutogramm'
udver: '2'
---

## Treebank Statistics: UD_Hausa-NorthernAutogramm: Features: `Case`

This feature is universal.
It occurs with 3 different values: `Ben`, `Gen`, `Nom`.

132 tokens (3%) have a non-empty value of `Case`.
24 types (3%) occur at least once with a non-empty value of `Case`.
26 lemmas (5%) occur at least once with a non-empty value of `Case`.
The feature is used with 2 part-of-speech tags: <tt><a href="ha_northernautogramm-pos-PRON.html">PRON</a></tt> (116; 3% instances), <tt><a href="ha_northernautogramm-pos-ADP.html">ADP</a></tt> (16; 0% instances).

### `PRON`

116 <tt><a href="ha_northernautogramm-pos-PRON.html">PRON</a></tt> tokens (41% of all `PRON` tokens) have a non-empty value of `Case`.

The most frequent other feature values with which `PRON` and `Case` co-occurred: <tt><a href="ha_northernautogramm-feat-PronType.html">PronType</a></tt><tt>=Prs</tt> (116; 100%), <tt><a href="ha_northernautogramm-feat-Person.html">Person</a></tt><tt>=3</tt> (84; 72%), <tt><a href="ha_northernautogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (81; 70%).

`PRON` tokens may have the following values of `Case`:

* `Ben` (31; 27% of non-empty `Case`): <em>mai, musù, maː, min, matà, maw</em>
* `Gen` (28; 24% of non-empty `Case`): <em>=nai, =tà, =tai, =kà, =su, =kù, =ta, naːshì, =kì, naːkù</em>
* `Nom` (57; 49% of non-empty `Case`): <em>shiː, ita, niː, suː, kai, keː, shi</em>
* `EMPTY` (166): <em>shì, shi, =tai, =sù, =tà, tà, =ka, =nai, sù, =kà</em>

`Case` seems to be **lexical feature** of `PRON`. 100% lemmas (25) occur only with one value of `Case`.

### `ADP`

16 <tt><a href="ha_northernautogramm-pos-ADP.html">ADP</a></tt> tokens (12% of all `ADP` tokens) have a non-empty value of `Case`.

`ADP` tokens may have the following values of `Case`:

* `Ben` (16; 100% of non-empty `Case`): <em>mà</em>
* `EMPTY` (117): <em>dà, cikin, gà, tun, dàc, dàn, bàːkin, dan, dàb, dàw</em>

## Relations with Agreement in `Case`

The 10 most frequent relations where parent and child node agree in `Case`:
<tt>PRON --[<tt><a href="ha_northernautogramm-dep-nmod.html">nmod</a></tt>]--> PRON</tt> (1; 100%).

