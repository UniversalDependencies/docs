---
layout: base
title:  'Statistics of PronType in UD_Swedish-PUD'
udver: '2'
---

## Treebank Statistics: UD_Swedish-PUD: Features: `PronType`

This feature is universal.
It occurs with 4 different values: `Art`, `Ind`, `Int`, `Rel`.
Some words have combined values of the feature; 1 combinations have been observed: `Int|Rel`.

396 tokens (2%) have a non-empty value of `PronType`.
25 types (0%) occur at least once with a non-empty value of `PronType`.
18 lemmas (0%) occur at least once with a non-empty value of `PronType`.
The feature is used with 4 part-of-speech tags: <tt><a href="sv_pud-pos-PRON.html">PRON</a></tt> (299; 2% instances), <tt><a href="sv_pud-pos-ADV.html">ADV</a></tt> (46; 0% instances), <tt><a href="sv_pud-pos-SCONJ.html">SCONJ</a></tt> (46; 0% instances), <tt><a href="sv_pud-pos-DET.html">DET</a></tt> (5; 0% instances).

### `PRON`

299 <tt><a href="sv_pud-pos-PRON.html">PRON</a></tt> tokens (23% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="sv_pud-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (299; 100%), <tt><a href="sv_pud-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (294; 98%), <tt><a href="sv_pud-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (253; 85%), <tt><a href="sv_pud-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (240; 80%), <tt><a href="sv_pud-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (235; 79%).

`PRON` tokens may have the following values of `PronType`:

* `Ind` (1; 0% of non-empty `PronType`): <em>mycket</em>
* `Int,Rel` (298; 100% of non-empty `PronType`): <em>som, vad, vilket, vilka, vem, vilken, vars, vilkas</em>
* `EMPTY` (1012): <em>det, han, de, sig, hans, jag, sin, hon, den, sina</em>

### `ADV`

46 <tt><a href="sv_pud-pos-ADV.html">ADV</a></tt> tokens (5% of all `ADV` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADV` and `PronType` co-occurred: <tt><a href="sv_pud-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (46; 100%).

`ADV` tokens may have the following values of `PronType`:

* `Int,Rel` (46; 100% of non-empty `PronType`): <em>där, hur, varför, när, dit, var, varav, då</em>
* `EMPTY` (966): <em>så, mer, in, ut, också, upp, bara, fram, här, även</em>

### `SCONJ`

46 <tt><a href="sv_pud-pos-SCONJ.html">SCONJ</a></tt> tokens (13% of all `SCONJ` tokens) have a non-empty value of `PronType`.

`SCONJ` tokens may have the following values of `PronType`:

* `Int,Rel` (46; 100% of non-empty `PronType`): <em>när, då</em>
* `EMPTY` (322): <em>att, som, om, än, medan, eftersom, innan, fastän, huruvida, sedan</em>

### `DET`

5 <tt><a href="sv_pud-pos-DET.html">DET</a></tt> tokens (0% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="sv_pud-feat-Number.html">Number</a></tt><tt>=Sing</tt> (5; 100%), <tt><a href="sv_pud-feat-Definite.html">Definite</a></tt><tt>=Ind</tt> (3; 60%), <tt><a href="sv_pud-feat-Gender.html">Gender</a></tt><tt>=Neut</tt> (3; 60%).

`DET` tokens may have the following values of `PronType`:

* `Art` (2; 40% of non-empty `PronType`): <em>den, det</em>
* `Int,Rel` (3; 60% of non-empty `PronType`): <em>vilket, vilken</em>
* `EMPTY` (1018): <em>en, den, ett, de, det, denna, varje, dessa, alla, detta</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="sv_pud-dep-obl.html">obl</a></tt>]--> PRON</tt> (1; 100%).

