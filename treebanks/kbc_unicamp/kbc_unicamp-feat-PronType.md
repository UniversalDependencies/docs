---
layout: base
title:  'Statistics of PronType in UD_Kadiweu-Unicamp'
udver: '2'
---

## Treebank Statistics: UD_Kadiweu-Unicamp: Features: `PronType`

This feature is universal.
It occurs with 4 different values: `Dem`, `Ind`, `Prs`, `Rel`.

55 tokens (17%) have a non-empty value of `PronType`.
22 types (23%) occur at least once with a non-empty value of `PronType`.
16 lemmas (21%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="kbc_unicamp-pos-DET.html">DET</a></tt> (33; 10% instances), <tt><a href="kbc_unicamp-pos-PRON.html">PRON</a></tt> (18; 6% instances), <tt><a href="kbc_unicamp-pos-ADV.html">ADV</a></tt> (4; 1% instances).

### `DET`

33 <tt><a href="kbc_unicamp-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="kbc_unicamp-feat-Number.html">Number</a></tt><tt>=Sing</tt> (31; 94%), <tt><a href="kbc_unicamp-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (19; 58%).

`DET` tokens may have the following values of `PronType`:

* `Dem` (32; 97% of non-empty `PronType`): <em>ica, ajo, NaGani, NiGida, adi, ijo, NaGajo, NiGijo, NiGinoa, naGana</em>
* `Ind` (1; 3% of non-empty `PronType`): <em>eliodi</em>

### `PRON`

18 <tt><a href="kbc_unicamp-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="kbc_unicamp-feat-Gender.html">Gender</a></tt><tt>=Fem</tt> (10; 56%), <tt><a href="kbc_unicamp-feat-Number.html">Number</a></tt><tt>=Sing</tt> (10; 56%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (10; 56% of non-empty `PronType`): <em>naGada, naGadi, Ada, Adi, Ani, NaGajo, NaGana, naGajo</em>
* `Prs` (1; 6% of non-empty `PronType`): <em>ee</em>
* `Rel` (7; 39% of non-empty `PronType`): <em>ane</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (10) occur only with one value of `PronType`.

### `ADV`

4 <tt><a href="kbc_unicamp-pos-ADV.html">ADV</a></tt> tokens (80% of all `ADV` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADV` and `PronType` co-occurred: <tt><a href="kbc_unicamp-feat-Deixis.html">Deixis</a></tt><tt>=EMPTY</tt> (3; 75%).

`ADV` tokens may have the following values of `PronType`:

* `Dem` (2; 50% of non-empty `PronType`): <em>digoida</em>
* `Ind` (2; 50% of non-empty `PronType`): <em>eliodi</em>
* `EMPTY` (1): <em>eliodi</em>

