---
layout: base
title:  'Statistics of NumType in UD_Kurmanji'
udver: '2'
---

## Treebank Statistics: UD_Kurmanji: Features: `NumType`

This feature is universal.
It occurs with 1 different values: `Card`.

213 tokens (2%) have a non-empty value of `NumType`.
150 types (5%) occur at least once with a non-empty value of `NumType`.
113 lemmas (6%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="kmr-pos-NUM.html">NUM</a></tt> (213; 2% instances).

### `NUM`

213 <tt><a href="kmr-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="kmr-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (154; 72%), <tt><a href="kmr-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (117; 55%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (213; 100% of non-empty `NumType`): du yek yekê 4 siseyan yekem sê 1 10 15'ê

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (113) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="kmr-dep-conj.html">conj</a></tt>]--> NUM</tt> (19; 100%),
<tt>NUM --[<tt><a href="kmr-dep-flat.html">flat</a></tt>]--> NUM</tt> (14; 100%),
<tt>NUM --[<tt><a href="kmr-dep-compound.html">compound</a></tt>]--> NUM</tt> (1; 100%),
<tt>NUM --[<tt><a href="kmr-dep-nmod-poss.html">nmod:poss</a></tt>]--> NUM</tt> (1; 100%),
<tt>NUM --[<tt><a href="kmr-dep-nmod.html">nmod</a></tt>]--> NUM</tt> (1; 100%).

