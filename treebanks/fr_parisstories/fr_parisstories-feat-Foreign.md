---
layout: base
title:  'Statistics of Foreign in UD_French-ParisStories'
udver: '2'
---

## Treebank Statistics: UD_French-ParisStories: Features: `Foreign`

This feature is universal.
It occurs with 1 different values: `Yes`.

2 tokens (0%) have a non-empty value of `Foreign`.
2 types (0%) occur at least once with a non-empty value of `Foreign`.
2 lemmas (0%) occur at least once with a non-empty value of `Foreign`.
The feature is used with 1 part-of-speech tags: <tt><a href="fr_parisstories-pos-X.html">X</a></tt> (2; 0% instances).

### `X`

2 <tt><a href="fr_parisstories-pos-X.html">X</a></tt> tokens (2% of all `X` tokens) have a non-empty value of `Foreign`.

The most frequent other feature values with which `X` and `Foreign` co-occurred: <tt><a href="fr_parisstories-feat-ExtPos.html">ExtPos</a></tt><tt>=EMPTY</tt> (2; 100%), <tt><a href="fr_parisstories-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (2; 100%).

`X` tokens may have the following values of `Foreign`:

* `Yes` (2; 100% of non-empty `Foreign`): <em>best, sellers</em>
* `EMPTY` (125): <em>XXX, s~, d~, j~, re~, a~, euh~, i~, m~, pl~</em>

## Relations with Agreement in `Foreign`

The 10 most frequent relations where parent and child node agree in `Foreign`:
<tt>X --[<tt><a href="fr_parisstories-dep-flat-foreign.html">flat:foreign</a></tt>]--> X</tt> (1; 100%).

