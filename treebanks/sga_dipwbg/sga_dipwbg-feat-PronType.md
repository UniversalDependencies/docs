---
layout: base
title:  'Statistics of PronType in UD_Old_Irish-DipWBG'
udver: '2'
---

## Treebank Statistics: UD_Old_Irish-DipWBG: Features: `PronType`

This feature is universal but the values `Ana`, `Emp` are language-specific.
It occurs with 8 different values: `Ana`, `Art`, `Dem`, `Emp`, `Ind`, `Int`, `Prs`, `Rel`.

106 tokens (24%) have a non-empty value of `PronType`.
50 types (20%) occur at least once with a non-empty value of `PronType`.
36 lemmas (21%) occur at least once with a non-empty value of `PronType`.
The feature is used with 5 part-of-speech tags: <tt><a href="sga_dipwbg-pos-PRON.html">PRON</a></tt> (68; 16% instances), <tt><a href="sga_dipwbg-pos-ADP.html">ADP</a></tt> (18; 4% instances), <tt><a href="sga_dipwbg-pos-DET.html">DET</a></tt> (9; 2% instances), <tt><a href="sga_dipwbg-pos-PART.html">PART</a></tt> (9; 2% instances), <tt><a href="sga_dipwbg-pos-AUX.html">AUX</a></tt> (2; 0% instances).

### `PRON`

68 <tt><a href="sga_dipwbg-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="sga_dipwbg-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (57; 84%), <tt><a href="sga_dipwbg-feat-PronClass.html">PronClass</a></tt><tt>=EMPTY</tt> (56; 82%), <tt><a href="sga_dipwbg-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (48; 71%), <tt><a href="sga_dipwbg-feat-Number.html">Number</a></tt><tt>=Sing</tt> (41; 60%).

`PRON` tokens may have the following values of `PronType`:

* `Ana` (1; 1% of non-empty `PronType`): <em>són</em>
* `Emp` (24; 35% of non-empty `PronType`): <em>sa, si, se, ni, so, som</em>
* `Ind` (4; 6% of non-empty `PronType`): <em>cách, níi, cáich</em>
* `Int` (2; 3% of non-empty `PronType`): <em>cote, cain</em>
* `Prs` (37; 54% of non-empty `PronType`): <em>mo, a, for, m, ar, t, far, hed, hé, b</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (26) occur only with one value of `PronType`.

### `ADP`

18 <tt><a href="sga_dipwbg-pos-ADP.html">ADP</a></tt> tokens (40% of all `ADP` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADP` and `PronType` co-occurred: <tt><a href="sga_dipwbg-feat-AdpType.html">AdpType</a></tt><tt>=Prep</tt> (18; 100%), <tt><a href="sga_dipwbg-feat-Definite.html">Definite</a></tt><tt>=Ind</tt> (18; 100%), <tt><a href="sga_dipwbg-feat-Person.html">Person</a></tt><tt>=2</tt> (12; 67%), <tt><a href="sga_dipwbg-feat-Number.html">Number</a></tt><tt>=Plur</tt> (10; 56%).

`ADP` tokens may have the following values of `PronType`:

* `Prs` (18; 100% of non-empty `PronType`): <em>dúib, de, dét, frib, fuirib, indib, lib, lim, limm, linn</em>
* `EMPTY` (27): <em>ar, oc, dar, i, di, fri, as, dochum, hi, imm</em>

### `DET`

9 <tt><a href="sga_dipwbg-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="sga_dipwbg-feat-Number.html">Number</a></tt><tt>=Sing</tt> (5; 56%).

`DET` tokens may have the following values of `PronType`:

* `Art` (9; 100% of non-empty `PronType`): <em>inna, a, in, ind, na</em>

### `PART`

9 <tt><a href="sga_dipwbg-pos-PART.html">PART</a></tt> tokens (19% of all `PART` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PART` and `PronType` co-occurred: <tt><a href="sga_dipwbg-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (9; 100%), <tt><a href="sga_dipwbg-feat-PartType.html">PartType</a></tt><tt>=EMPTY</tt> (6; 67%).

`PART` tokens may have the following values of `PronType`:

* `Dem` (8; 89% of non-empty `PronType`): <em>so, a, se, sin, siu</em>
* `Int` (1; 11% of non-empty `PronType`): <em>in</em>
* `EMPTY` (38): <em>no, do, ni, ní, as, nas, a, at, con, d</em>

### `AUX`

2 <tt><a href="sga_dipwbg-pos-AUX.html">AUX</a></tt> tokens (6% of all `AUX` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `AUX` and `PronType` co-occurred: <tt><a href="sga_dipwbg-feat-Number.html">Number</a></tt><tt>=Sing</tt> (2; 100%), <tt><a href="sga_dipwbg-feat-Person.html">Person</a></tt><tt>=3</tt> (2; 100%), <tt><a href="sga_dipwbg-feat-Polarity.html">Polarity</a></tt><tt>=Pos</tt> (2; 100%), <tt><a href="sga_dipwbg-feat-Tense.html">Tense</a></tt><tt>=Pres</tt> (2; 100%), <tt><a href="sga_dipwbg-feat-VerbType.html">VerbType</a></tt><tt>=Cop</tt> (2; 100%).

`AUX` tokens may have the following values of `PronType`:

* `Rel` (2; 100% of non-empty `PronType`): <em>as, bes</em>
* `EMPTY` (30): <em>is, d, ni, ba, bimmi, p, am, bed, i, naba</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="sga_dipwbg-dep-acl.html">acl</a></tt>]--> PRON</tt> (1; 100%).

