---
layout: base
title:  'Statistics of Foreign in UD_Hindi-PUD'
udver: '2'
---

## Treebank Statistics: UD_Hindi-PUD: Features: `Foreign`

This feature is universal.
It occurs with 1 different values: `Yes`.

3 tokens (0%) have a non-empty value of `Foreign`.
3 types (0%) occur at least once with a non-empty value of `Foreign`.
1 lemmas (0) occur at least once with a non-empty value of `Foreign`.
The feature is used with 1 part-of-speech tags: <tt><a href="hi_pud-pos-X.html">X</a></tt> (3; 0% instances).

### `X`

3 <tt><a href="hi_pud-pos-X.html">X</a></tt> tokens (27% of all `X` tokens) have a non-empty value of `Foreign`.

`X` tokens may have the following values of `Foreign`:

* `Yes` (3; 100% of non-empty `Foreign`): थ़ॉट, न्यू, या
* `EMPTY` (8): वीं, आनन, ए, कि, पहला, भरकम, यह

## Relations with Agreement in `Foreign`

The 10 most frequent relations where parent and child node agree in `Foreign`:
<tt>X --[<tt><a href="hi_pud-dep-flat.html">flat</a></tt>]--> X</tt> (2; 100%).

