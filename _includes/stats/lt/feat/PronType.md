

--------------------------------------------------------------------------------

## Treebank Statistics (UD_Lithuanian-Alksnis)

This feature is universal.
It occurs with 1 different values: `Tot`.

3080 tokens (8%) have a non-empty value of `PronType`.
457 types (4%) occur at least once with a non-empty value of `PronType`.
70 lemmas (1%) occur at least once with a non-empty value of `PronType`.
The feature is used with 2 part-of-speech tags: [lt-pos/PRON]() (3077; 8% instances), [lt-pos/DET]() (3; 0% instances).

### `PRON`

3077 [lt-pos/PRON]() tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="Case.html">Case</a>=Nom</tt> (3062; 100%).

`PRON` tokens may have the following values of `PronType`:

* `Tot` (3077; 100% of non-empty `PronType`): <em>tai, savo, jis, jų, jie, aš, jos, kas, jo, ką</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (69) occur only with one value of `PronType`.

### `DET`

3 [lt-pos/DET]() tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="NumType.html">NumType</a>=Card</tt> (3; 100%), <tt><a href="NumForm.html">NumForm</a>=Word</tt> (3; 100%), <tt><a href="Case.html">Case</a>=Acc</tt> (3; 100%).

`DET` tokens may have the following values of `PronType`:

* `Tot` (3; 100% of non-empty `PronType`): <em>trejetą</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<a href="../dep/nmod.html">nmod</a>]--> PRON</tt> (33; 100%),
<tt>PRON --[<a href="../dep/obj.html">obj</a>]--> PRON</tt> (22; 100%),
<tt>PRON --[<a href="../dep/dep:adj.html">dep:adj</a>]--> PRON</tt> (8; 100%),
<tt>PRON --[<a href="../dep/dep:sub.html">dep:sub</a>]--> PRON</tt> (7; 100%),
<tt>PRON --[<a href="../dep/dep:predn_adj.html">dep:predn_adj</a>]--> PRON</tt> (2; 100%),
<tt>PRON --[<a href="../dep/dep:obj_co.html">dep:obj_co</a>]--> PRON</tt> (1; 100%),
<tt>PRON --[<a href="../dep/dep:predn.html">dep:predn</a>]--> PRON</tt> (1; 100%),
<tt>PRON --[<a href="../dep/dep:predn_atr.html">dep:predn_atr</a>]--> PRON</tt> (1; 100%),
<tt>PRON --[<a href="../dep/dep:predn_sub.html">dep:predn_sub</a>]--> PRON</tt> (1; 100%).

