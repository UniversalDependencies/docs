---
layout: base
title:  'Statistics of PronType in UD_Mbya_Guarani-Thomas'
udver: '2'
---

## Treebank Statistics: UD_Mbya_Guarani-Thomas: Features: `PronType`

This feature is universal.
It occurs with 5 different values: `Dem`, `Ind`, `Int`, `Prs`, `Tot`.

122 tokens (9%) have a non-empty value of `PronType`.
35 types (9%) occur at least once with a non-empty value of `PronType`.
26 lemmas (10%) occur at least once with a non-empty value of `PronType`.
The feature is used with 2 part-of-speech tags: <tt><a href="gun_thomas-pos-PRON.html">PRON</a></tt> (111; 8% instances), <tt><a href="gun_thomas-pos-DET.html">DET</a></tt> (11; 1% instances).

### `PRON`

111 <tt><a href="gun_thomas-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="gun_thomas-feat-Clusivity.html">Clusivity</a></tt><tt>=EMPTY</tt> (81; 73%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (20; 18% of non-empty `PronType`): <em>upea, upe, kirami'i, kova'e, pea, ko'ava, umia, upeve</em>
* `Ind` (3; 3% of non-empty `PronType`): <em>heta</em>
* `Int` (9; 8% of non-empty `PronType`): <em>mba'e, Mava'e</em>
* `Prs` (78; 70% of non-empty `PronType`): <em>ore, chee, ha'e, ichupe, ñande, che, ñandevy, ndee, pende, pendevy</em>
* `Tot` (1; 1% of non-empty `PronType`): <em>opa</em>

<table>
  <tr><th>Paradigm <i>mba'e</i></th><th><tt>Prs</tt></th><th><tt>Int</tt></th></tr>
  <tr><td><tt>_</tt></td><td></td><td><em>mba'e</em></td></tr>
  <tr><td><tt><tt><a href="gun_thomas-feat-Polarity.html">Polarity</a></tt><tt>=Neg</tt></tt></td><td><em>mba'eve</em></td><td></td></tr>
</table>

`PronType` seems to be **lexical feature** of `PRON`. 96% lemmas (22) occur only with one value of `PronType`.

### `DET`

11 <tt><a href="gun_thomas-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

`DET` tokens may have the following values of `PronType`:

* `Dem` (5; 45% of non-empty `PronType`): <em>kova'e, pe</em>
* `Int` (3; 27% of non-empty `PronType`): <em>mba'e, Mava'e</em>
* `Tot` (3; 27% of non-empty `PronType`): <em>pavẽ, javive</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="gun_thomas-dep-reparandum.html">reparandum</a></tt>]--> PRON</tt> (3; 100%).

