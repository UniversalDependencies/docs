---
layout: base
title:  'Statistics of NumType in UD_Norwegian-NynorskLIA'
udver: '2'
---

## Treebank Statistics: UD_Norwegian-NynorskLIA: Features: `NumType`

This feature is universal.
It occurs with 1 different values: `Card`.

406 tokens (1%) have a non-empty value of `NumType`.
69 types (2%) occur at least once with a non-empty value of `NumType`.
67 lemmas (2%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="no_nynorsklia-pos-NUM.html">NUM</a></tt> (406; 1% instances).

### `NUM`

406 <tt><a href="no_nynorsklia-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="no_nynorsklia-feat-Number.html">Number</a></tt><tt>=Plur</tt> (377; 93%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (406; 100% of non-empty `NumType`): <em>to, tre, hundre, seks, sju, mange, fem, fire, tjue, åtte</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (67) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="no_nynorsklia-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (37; 100%),
<tt>NUM --[<tt><a href="no_nynorsklia-dep-conj.html">conj</a></tt>]--> NUM</tt> (14; 100%),
<tt>NUM --[<tt><a href="no_nynorsklia-dep-reparandum.html">reparandum</a></tt>]--> NUM</tt> (8; 100%),
<tt>NUM --[<tt><a href="no_nynorsklia-dep-nmod.html">nmod</a></tt>]--> NUM</tt> (2; 100%).

