---
layout: base
title:  'Statistics of NumType in UD_Middle_French-PROFITEROLE'
udver: '2'
---

## Treebank Statistics: UD_Middle_French-PROFITEROLE: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

115 tokens (1%) have a non-empty value of `NumType`.
42 types (2%) occur at least once with a non-empty value of `NumType`.
29 lemmas (2%) occur at least once with a non-empty value of `NumType`.
The feature is used with 4 part-of-speech tags: <tt><a href="frm_profiterole-pos-DET.html">DET</a></tt> (62; 1% instances), <tt><a href="frm_profiterole-pos-ADJ.html">ADJ</a></tt> (42; 0% instances), <tt><a href="frm_profiterole-pos-PRON.html">PRON</a></tt> (9; 0% instances), <tt><a href="frm_profiterole-pos-NUM.html">NUM</a></tt> (2; 0% instances).

### `DET`

62 <tt><a href="frm_profiterole-pos-DET.html">DET</a></tt> tokens (5% of all `DET` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `DET` and `NumType` co-occurred: <tt><a href="frm_profiterole-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (62; 100%), <tt><a href="frm_profiterole-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (62; 100%), <tt><a href="frm_profiterole-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (62; 100%).

`DET` tokens may have the following values of `NumType`:

* `Card` (62; 100% of non-empty `NumType`): <em>quatre, cinq, deux, huit, mille, cens, six, trois, dix, quinze</em>
* `EMPTY` (1207): <em>le, la, les, l', son, sa, ledict, ses, leur, ceste</em>

`NumType` seems to be **lexical feature** of `DET`. 100% lemmas (15) occur only with one value of `NumType`.

### `ADJ`

42 <tt><a href="frm_profiterole-pos-ADJ.html">ADJ</a></tt> tokens (11% of all `ADJ` tokens) have a non-empty value of `NumType`.

`ADJ` tokens may have the following values of `NumType`:

* `Card` (34; 81% of non-empty `NumType`): <em>mil, CCC, quatre, LX, LXIIII, II, LXI, LXIV, LXV, LXVI</em>
* `Ord` (8; 19% of non-empty `NumType`): <em>VIIIe, Ve, Viiè, XIIe, XIXe, cinquiesme, premier, unziesme</em>
* `EMPTY` (352): <em>grant, grans, bonne, belle, bon, grand, joyeulx, jeune, noble, puissant</em>

`NumType` seems to be **lexical feature** of `ADJ`. 100% lemmas (18) occur only with one value of `NumType`.

### `PRON`

9 <tt><a href="frm_profiterole-pos-PRON.html">PRON</a></tt> tokens (1% of all `PRON` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `PRON` and `NumType` co-occurred: <tt><a href="frm_profiterole-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (9; 100%).

`PRON` tokens may have the following values of `NumType`:

* `Card` (9; 100% of non-empty `NumType`): <em>cinquante, cinq, deux, huict, mille, neuf, quatre, vingt</em>
* `EMPTY` (1114): <em>il, qui, vous, luy, que, ilz, ce, se, je, le</em>

### `NUM`

2 <tt><a href="frm_profiterole-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (2; 100% of non-empty `NumType`): <em>X, deux</em>

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>ADJ --[<tt><a href="frm_profiterole-dep-flat.html">flat</a></tt>]--> ADJ</tt> (21; 100%),
<tt>DET --[<tt><a href="frm_profiterole-dep-flat.html">flat</a></tt>]--> DET</tt> (16; 100%),
<tt>DET --[<tt><a href="frm_profiterole-dep-conj.html">conj</a></tt>]--> DET</tt> (3; 100%),
<tt>NUM --[<tt><a href="frm_profiterole-dep-flat.html">flat</a></tt>]--> NUM</tt> (1; 100%),
<tt>PRON --[<tt><a href="frm_profiterole-dep-conj.html">conj</a></tt>]--> PRON</tt> (1; 100%),
<tt>PRON --[<tt><a href="frm_profiterole-dep-flat.html">flat</a></tt>]--> PRON</tt> (1; 100%).

