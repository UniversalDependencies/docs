---
layout: base
title:  'Statistics of Foc in UD_Bororo-BDT'
udver: '2'
---

## Treebank Statistics: UD_Bororo-BDT: Features: `Foc`

This feature is language-specific.
It occurs with 1 different values: `Yes`.

48 tokens (0%) have a non-empty value of `Foc`.
11 types (0%) occur at least once with a non-empty value of `Foc`.
10 lemmas (0%) occur at least once with a non-empty value of `Foc`.
The feature is used with 1 part-of-speech tags: <tt><a href="bor_bdt-pos-PART.html">PART</a></tt> (48; 0% instances).

### `PART`

48 <tt><a href="bor_bdt-pos-PART.html">PART</a></tt> tokens (24% of all `PART` tokens) have a non-empty value of `Foc`.

The most frequent other feature values with which `PART` and `Foc` co-occurred: <tt><a href="bor_bdt-feat-Int.html">Int</a></tt><tt>=Yes</tt> (48; 100%), <tt><a href="bor_bdt-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (48; 100%).

`PART` tokens may have the following values of `Foc`:

* `Yes` (48; 100% of non-empty `Foc`): <em>kana, na, lmana, modukana, rena, una, kuborewumana, marena, rakamoduna</em>

`Foc` seems to be **lexical feature** of `PART`. 100% lemmas (10) occur only with one value of `Foc`.

## Relations with Agreement in `Foc`

The 10 most frequent relations where parent and child node agree in `Foc`:
<tt>PART --[<tt><a href="bor_bdt-dep-dep.html">dep</a></tt>]--> PART</tt> (1; 100%).

