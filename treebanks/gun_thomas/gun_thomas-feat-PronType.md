---
layout: base
title:  'Statistics of PronType in UD_Mbya_Guarani-Thomas'
udver: '2'
---

## Treebank Statistics: UD_Mbya_Guarani-Thomas: Features: `PronType`

This feature is universal.
It occurs with 6 different values: `Dem`, `Ind`, `Int`, `Neg`, `Prs`, `Tot`.

126 tokens (10%) have a non-empty value of `PronType`.
36 types (9%) occur at least once with a non-empty value of `PronType`.
26 lemmas (9%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="gun_thomas-pos-PRON.html">PRON</a></tt> (111; 8% instances), <tt><a href="gun_thomas-pos-DET.html">DET</a></tt> (8; 1% instances), <tt><a href="gun_thomas-pos-ADV.html">ADV</a></tt> (7; 1% instances).

### `PRON`

111 <tt><a href="gun_thomas-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="gun_thomas-feat-Clusivity.html">Clusivity</a></tt><tt>=EMPTY</tt> (81; 73%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (20; 18% of non-empty `PronType`): <em>upea, upe, kirami'i, kova'e, pea, ko'ava, umia, upeve</em>
* `Ind` (3; 3% of non-empty `PronType`): <em>heta</em>
* `Int` (9; 8% of non-empty `PronType`): <em>mba'e, Mava'e</em>
* `Neg` (1; 1% of non-empty `PronType`): <em>mba'eve</em>
* `Prs` (77; 69% of non-empty `PronType`): <em>ore, chee, ha'e, ichupe, ñande, che, ñandevy, ndee, pende, pendevy</em>
* `Tot` (1; 1% of non-empty `PronType`): <em>opa</em>

<table>
  <tr><th>Paradigm <i>mba'e</i></th><th><tt>Int</tt></th><th><tt>Neg</tt></th></tr>
  <tr><td><tt>_</tt></td><td><em>mba'e</em></td><td></td></tr>
  <tr><td><tt><tt><a href="gun_thomas-feat-Polarity.html">Polarity</a></tt><tt>=Neg</tt></tt></td><td></td><td><em>mba'eve</em></td></tr>
</table>

`PronType` seems to be **lexical feature** of `PRON`. 95% lemmas (21) occur only with one value of `PronType`.

### `DET`

8 <tt><a href="gun_thomas-pos-DET.html">DET</a></tt> tokens (73% of all `DET` tokens) have a non-empty value of `PronType`.

`DET` tokens may have the following values of `PronType`:

* `Dem` (5; 63% of non-empty `PronType`): <em>kova'e, pe</em>
* `Int` (3; 38% of non-empty `PronType`): <em>mba'e, Mava'e</em>
* `EMPTY` (3): <em>pavẽ, pavẽ'i</em>

### `ADV`

7 <tt><a href="gun_thomas-pos-ADV.html">ADV</a></tt> tokens (7% of all `ADV` tokens) have a non-empty value of `PronType`.

`ADV` tokens may have the following values of `PronType`:

* `Int` (7; 100% of non-empty `PronType`): <em>mba'eicha, Mba'echa, mamo</em>
* `EMPTY` (96): <em>pecha, agã, upecha, upei, ko'agã, kuri, peicha, jepe, ko'ape, porã</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="gun_thomas-dep-reparandum.html">reparandum</a></tt>]--> PRON</tt> (3; 100%).

