---
layout: base
title:  'Statistics of NumType in UD_Italian-KIParlaForest'
udver: '2'
---

## Treebank Statistics: UD_Italian-KIParlaForest: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

88 tokens (1%) have a non-empty value of `NumType`.
35 types (2%) occur at least once with a non-empty value of `NumType`.
34 lemmas (3%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="it_kiparlaforest-pos-NUM.html">NUM</a></tt> (84; 1% instances), <tt><a href="it_kiparlaforest-pos-ADJ.html">ADJ</a></tt> (4; 0% instances).

### `NUM`

84 <tt><a href="it_kiparlaforest-pos-NUM.html">NUM</a></tt> tokens (91% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="it_kiparlaforest-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (72; 86%), <tt><a href="it_kiparlaforest-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (72; 86%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (71; 85% of non-empty `NumType`): <em>due, quattro, tre, dodici, quattordici, undici, venti, cinquanta, cinque, dieci</em>
* `Ord` (13; 15% of non-empty `NumType`): <em>prima, primi, primo, secondo</em>
* `EMPTY` (8): <em>qua~, cinquanta, seconda, secron~, settantasei, undici, venticinque</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (31) occur only with one value of `NumType`.

### `ADJ`

4 <tt><a href="it_kiparlaforest-pos-ADJ.html">ADJ</a></tt> tokens (1% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="it_kiparlaforest-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (4; 100%), <tt><a href="it_kiparlaforest-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (3; 75%).

`ADJ` tokens may have the following values of `NumType`:

* `Card` (1; 25% of non-empty `NumType`): <em>mezz'</em>
* `Ord` (3; 75% of non-empty `NumType`): <em>quarti, ultimi, ultimo</em>
* `EMPTY` (407): <em>mia, esatto, grande, miei, piccola, bella, altra, difficile, strano, tua</em>

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="it_kiparlaforest-dep-conj.html">conj</a></tt>]--> NUM</tt> (2; 100%).

