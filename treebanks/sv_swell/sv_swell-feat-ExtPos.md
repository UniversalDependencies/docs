---
layout: base
title:  'Statistics of ExtPos in UD_Swedish-SweLL'
udver: '2'
---

## Treebank Statistics: UD_Swedish-SweLL: Features: `ExtPos`

This feature is language-specific.
It occurs with 4 different values: `ADP`, `ADV`, `CCONJ`, `SCONJ`.

16 tokens (0%) have a non-empty value of `ExtPos`.
8 types (0%) occur at least once with a non-empty value of `ExtPos`.
6 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 3 part-of-speech tags: <tt><a href="sv_swell-pos-ADV.html">ADV</a></tt> (14; 0% instances), <tt><a href="sv_swell-pos-ADP.html">ADP</a></tt> (1; 0% instances), <tt><a href="sv_swell-pos-CCONJ.html">CCONJ</a></tt> (1; 0% instances).

### `ADV`

14 <tt><a href="sv_swell-pos-ADV.html">ADV</a></tt> tokens (2% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="sv_swell-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (13; 93%).

`ADV` tokens may have the following values of `ExtPos`:

* `ADV` (3; 21% of non-empty `ExtPos`): <em>Till, fram, länge</em>
* `SCONJ` (11; 79% of non-empty `ExtPos`): <em>därför, darför</em>

### `ADP`

1 <tt><a href="sv_swell-pos-ADP.html">ADP</a></tt> tokens (0% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (1; 100% of non-empty `ExtPos`): <em>För</em>

### `CCONJ`

1 <tt><a href="sv_swell-pos-CCONJ.html">CCONJ</a></tt> tokens (0% of all `CCONJ` tokens) have a non-empty value of `ExtPos`.

`CCONJ` tokens may have the following values of `ExtPos`:

* `CCONJ` (1; 100% of non-empty `ExtPos`): <em>vare</em>

