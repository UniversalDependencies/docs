---
layout: base
title:  'Statistics of NumType in UD_Middle_French-PROFITEROLE'
udver: '2'
---

## Treebank Statistics: UD_Middle_French-PROFITEROLE: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

913 tokens (1%) have a non-empty value of `NumType`.
123 types (1%) occur at least once with a non-empty value of `NumType`.
64 lemmas (2%) occur at least once with a non-empty value of `NumType`.
The feature is used with 4 part-of-speech tags: <tt><a href="frm_profiterole-pos-NUM.html">NUM</a></tt> (693; 1% instances), <tt><a href="frm_profiterole-pos-ADJ.html">ADJ</a></tt> (143; 0% instances), <tt><a href="frm_profiterole-pos-PRON.html">PRON</a></tt> (70; 0% instances), <tt><a href="frm_profiterole-pos-DET.html">DET</a></tt> (7; 0% instances).

### `NUM`

693 <tt><a href="frm_profiterole-pos-NUM.html">NUM</a></tt> tokens (71% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (693; 100% of non-empty `NumType`): <em>deux, cens, trois, quatre, cinq, mil, CCC, vingt, cent, six</em>
* `EMPTY` (285): <em>deux, trois, quatre, cent, .VJ., .IXe., .XL., .V., cinq, .IIIJ.</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (36) occur only with one value of `NumType`.

### `ADJ`

143 <tt><a href="frm_profiterole-pos-ADJ.html">ADJ</a></tt> tokens (3% of all `ADJ` tokens) have a non-empty value of `NumType`.

`ADJ` tokens may have the following values of `NumType`:

* `Card` (30; 21% of non-empty `NumType`): <em>deux, trois, I, II, III, IV, mil, IX, V, VIII</em>
* `Ord` (113; 79% of non-empty `NumType`): <em>premier, première, seconde, second, tiers, IXe, VIIe, Ve, cinquiesme, premiere</em>
* `EMPTY` (4123): <em>grant, saint, belle, grans, bonne, saincte, bon, beau, grande, grand</em>

`NumType` seems to be **lexical feature** of `ADJ`. 100% lemmas (37) occur only with one value of `NumType`.

### `PRON`

70 <tt><a href="frm_profiterole-pos-PRON.html">PRON</a></tt> tokens (1% of all `PRON` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `PRON` and `NumType` co-occurred: <tt><a href="frm_profiterole-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (70; 100%).

`PRON` tokens may have the following values of `NumType`:

* `Card` (53; 76% of non-empty `NumType`): <em>deux, quatre, dix, trois, vingt, 1, 2, V, cinq, douse</em>
* `Ord` (17; 24% of non-empty `NumType`): <em>premier, première, premiers, premiere, second</em>
* `EMPTY` (11475): <em>il, qui, ilz, nous, vous, que, ce, se, luy, je</em>

`NumType` seems to be **lexical feature** of `PRON`. 100% lemmas (13) occur only with one value of `NumType`.

### `DET`

7 <tt><a href="frm_profiterole-pos-DET.html">DET</a></tt> tokens (0% of all `DET` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `DET` and `NumType` co-occurred: <tt><a href="frm_profiterole-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (7; 100%), <tt><a href="frm_profiterole-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (7; 100%), <tt><a href="frm_profiterole-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (7; 100%).

`DET` tokens may have the following values of `NumType`:

* `Card` (7; 100% of non-empty `NumType`): <em>un, une, ung</em>
* `EMPTY` (13431): <em>le, la, les, l', ung, une, son, nostre, ses, sa</em>

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="frm_profiterole-dep-flat.html">flat</a></tt>]--> NUM</tt> (189; 100%),
<tt>NUM --[<tt><a href="frm_profiterole-dep-conj.html">conj</a></tt>]--> NUM</tt> (52; 100%),
<tt>ADJ --[<tt><a href="frm_profiterole-dep-flat.html">flat</a></tt>]--> NUM</tt> (4; 100%),
<tt>NUM --[<tt><a href="frm_profiterole-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (3; 75%),
<tt>PRON --[<tt><a href="frm_profiterole-dep-flat.html">flat</a></tt>]--> PRON</tt> (2; 100%),
<tt>NUM --[<tt><a href="frm_profiterole-dep-nmod.html">nmod</a></tt>]--> NUM</tt> (1; 100%),
<tt>PRON --[<tt><a href="frm_profiterole-dep-conj.html">conj</a></tt>]--> PRON</tt> (1; 100%).

