---
layout: base
title:  'Statistics of AdvType in UD_Georgian-GNC'
udver: '2'
---

## Treebank Statistics: UD_Georgian-GNC: Features: `AdvType`

This feature is language-specific.
It occurs with 3 different values: `Loc`, `Rel`, `Tim`.

442 tokens (2%) have a non-empty value of `AdvType`.
106 types (2%) occur at least once with a non-empty value of `AdvType`.
89 lemmas (2%) occur at least once with a non-empty value of `AdvType`.
The feature is used with 1 part-of-speech tags: <tt><a href="ka_gnc-pos-ADV.html">ADV</a></tt> (442; 2% instances).

### `ADV`

442 <tt><a href="ka_gnc-pos-ADV.html">ADV</a></tt> tokens (23% of all `ADV` tokens) have a non-empty value of `AdvType`.

`ADV` tokens may have the following values of `AdvType`:

* `Loc` (150; 34% of non-empty `AdvType`): <em>აქ, იქ, სად, შინ, აქედან, საიდან, უკან, აქამდე, შორს, ამქვეყნად</em>
* `Rel` (10; 2% of non-empty `AdvType`): <em>სადაც, საიდანაც</em>
* `Tim` (282; 64% of non-empty `AdvType`): <em>ახლა, მაშინ, მერე, ბოლოს, არასოდეს, შემდეგ, დღეს, ყოველთვის, ამჯერად, ერთხანს</em>

<table>
  <tr><th>Paradigm <i>სად</i></th><th><tt>Loc</tt></th><th><tt>Rel</tt></th></tr>
  <tr><td><tt></tt></td><td><em>სად</em></td><td><em>სადაც</em></td></tr>
</table>

`AdvType` seems to be **lexical feature** of `ADV`. 98% lemmas (87) occur only with one value of `AdvType`.

## Relations with Agreement in `AdvType`

The 10 most frequent relations where parent and child node agree in `AdvType`:
<tt>ADV --[<tt><a href="ka_gnc-dep-conj.html">conj</a></tt>]--> ADV</tt> (6; 75%).

