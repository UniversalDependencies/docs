---
layout: base
title:  'Statistics of Aspect in UD_Zaar-Autogramm'
udver: '2'
---

## Treebank Statistics: UD_Zaar-Autogramm: Features: `Aspect`

This feature is universal but the values `Aor`, `Conc`, `ImpIter`, `Iter` are language-specific.
It occurs with 7 different values: `Aor`, `Conc`, `Imp`, `ImpIter`, `Iter`, `Perf`, `Prog`.

1705 tokens (8%) have a non-empty value of `Aspect`.
95 types (3%) occur at least once with a non-empty value of `Aspect`.
2 lemmas (0%) occur at least once with a non-empty value of `Aspect`.
The feature is used with 2 part-of-speech tags: <tt><a href="say_autogramm-pos-AUX.html">AUX</a></tt> (1611; 8% instances), <tt><a href="say_autogramm-pos-PART.html">PART</a></tt> (94; 0% instances).

### `AUX`

1611 <tt><a href="say_autogramm-pos-AUX.html">AUX</a></tt> tokens (52% of all `AUX` tokens) have a non-empty value of `Aspect`.

The most frequent other feature values with which `AUX` and `Aspect` co-occurred: <tt><a href="say_autogramm-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (1606; 100%), <tt><a href="say_autogramm-feat-Tense.html">Tense</a></tt><tt>=EMPTY</tt> (1508; 94%), <tt><a href="say_autogramm-feat-Number.html">Number</a></tt><tt>=Sing</tt> (997; 62%), <tt><a href="say_autogramm-feat-Person.html">Person</a></tt><tt>=3</tt> (882; 55%).

`AUX` tokens may have the following values of `Aspect`:

* `Aor` (938; 58% of non-empty `Aspect`): <em>á, tə́, kə, mə, mə́, kə́, ɣə́, kə̀, _́, gə́</em>
* `Conc` (7; 0% of non-empty `Aspect`): <em>myáːnaː, ʧáːnaː, myàːnaː, ʧínaː</em>
* `Imp` (346; 21% of non-empty `Aspect`): <em>ʧáː, ʧǎː, myǎː, myáː, mə̀tàyáː, kyáː, kə̀tàyáː, ʧàː, tə̀tàyáː, myàː</em>
* `ImpIter` (17; 1% of non-empty `Aspect`): <em>ʧàːyí, ʧàːyi, kyàːyi, myàːyí, ʧáːyi, myàːyi, myáːyi, myǎːyi</em>
* `Iter` (19; 1% of non-empty `Aspect`): <em>yi, táyi, kîː, mayi, yiː, k~, káyi, miː, mîː, mə̀yi</em>
* `Perf` (248; 15% of non-empty `Aspect`): <em>àː, káː, máː, tàː, màː, tàːtá, àːtá, máːtá, máːyí, àːyí</em>
* `Prog` (36; 2% of non-empty `Aspect`): <em>ʧàː, ʧìɣá, ʧǎː, myǎː, kiɣá, myaː, kyǎː, kìɣá, miɣá, myàː</em>
* `EMPTY` (1482): <em>wò, tə̀, tá, yáː, nə, ka, mə̀, ʧi, má, ma</em>

### `PART`

94 <tt><a href="say_autogramm-pos-PART.html">PART</a></tt> tokens (5% of all `PART` tokens) have a non-empty value of `Aspect`.

The most frequent other feature values with which `PART` and `Aspect` co-occurred: <tt><a href="say_autogramm-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (94; 100%), <tt><a href="say_autogramm-feat-PartType.html">PartType</a></tt><tt>=Adv</tt> (94; 100%), <tt><a href="say_autogramm-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (94; 100%).

`PART` tokens may have the following values of `Aspect`:

* `Iter` (94; 100% of non-empty `Aspect`): <em>ni, nì, nəni, ní</em>
* `EMPTY` (1713): <em>tôː, hŋ́, ɗi, bàː, tòː, oː, aː, kúmá, máː, mə́n</em>

## Relations with Agreement in `Aspect`

The 10 most frequent relations where parent and child node agree in `Aspect`:
<tt>AUX --[<tt><a href="say_autogramm-dep-reparandum.html">reparandum</a></tt>]--> AUX</tt> (1; 100%).

