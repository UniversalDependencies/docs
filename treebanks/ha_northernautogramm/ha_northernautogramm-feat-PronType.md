---
layout: base
title:  'Statistics of PronType in UD_Hausa-NorthernAutogramm'
udver: '2'
---

## Treebank Statistics: UD_Hausa-NorthernAutogramm: Features: `PronType`

This feature is universal.
It occurs with 6 different values: `Dem`, `Ind`, `Int`, `Prs`, `Rel`, `Tot`.

1222 tokens (8%) have a non-empty value of `PronType`.
120 types (7%) occur at least once with a non-empty value of `PronType`.
82 lemmas (8%) occur at least once with a non-empty value of `PronType`.
The feature is used with 4 part-of-speech tags: <tt><a href="ha_northernautogramm-pos-PRON.html">PRON</a></tt> (770; 5% instances), <tt><a href="ha_northernautogramm-pos-DET.html">DET</a></tt> (282; 2% instances), <tt><a href="ha_northernautogramm-pos-ADV.html">ADV</a></tt> (169; 1% instances), <tt><a href="ha_northernautogramm-pos-ADJ.html">ADJ</a></tt> (1; 0% instances).

### `PRON`

770 <tt><a href="ha_northernautogramm-pos-PRON.html">PRON</a></tt> tokens (98% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="ha_northernautogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (499; 65%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (35; 5% of non-empty `PronType`): <em>waɗànga, wàccan, wànga, wànnan, wàgga, wàncân, waɗàncân, wâggàːshi, wannàn, waɗànnan</em>
* `Ind` (41; 5% of non-empty `PronType`): <em>koːwaː, koːmiː, wani, wasu, koːwanèː, suwaːnè, wata, waːnèː</em>
* `Int` (19; 2% of non-empty `PronType`): <em>miː, waː, mìː, koːwanèː, suwàː, wanèː, koːwaː, wàː</em>
* `Prs` (625; 81% of non-empty `PronType`): <em>mai, shiː, niː, shì, sù, shi, musù, kai, min, suː</em>
* `Rel` (42; 5% of non-empty `PronType`): <em>indà, wandà, waɗàndà, koːmiː, sà'addà, wa'ànda, wàdà, waɗànda</em>
* `Tot` (8; 1% of non-empty `PronType`): <em>dukà, duk, duy, dûn, koːwanèː</em>
* `EMPTY` (12): <em>kâinai, ", ., kà, kânka, kêː, kù, maidà, shiː, wàndonkà</em>

<table>
  <tr><th>Paradigm <i>koːwanèː</i></th><th><tt>Int</tt></th><th><tt>Tot</tt></th><th><tt>Ind</tt></th></tr>
  <tr><td><tt>_</tt></td><td></td><td></td><td><em>koːwanèː</em></td></tr>
  <tr><td><tt><tt><a href="ha_northernautogramm-feat-Gender.html">Gender</a></tt><tt>=Masc</tt></tt></td><td><em>koːwanèː</em></td><td><em>koːwanèː</em></td><td></td></tr>
</table>

`PronType` seems to be **lexical feature** of `PRON`. 96% lemmas (67) occur only with one value of `PronType`.

### `DET`

282 <tt><a href="ha_northernautogramm-pos-DET.html">DET</a></tt> tokens (93% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="ha_northernautogramm-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (231; 82%), <tt><a href="ha_northernautogramm-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (215; 76%).

`DET` tokens may have the following values of `PronType`:

* `Dem` (176; 62% of non-empty `PronType`): <em>nan, ga, tan, wànga, waɗànga, waɗànnan, wàccân, can, ceːnìyaː, wàncân</em>
* `Ind` (51; 18% of non-empty `PronType`): <em>wani, wasu, wata</em>
* `Int` (6; 2% of non-empty `PronType`): <em>wanèː, waccè, wacèː, wàcceː</em>
* `Tot` (49; 17% of non-empty `PronType`): <em>dukà, duk, dug, duy, du', dus, dub, dukàn, dun, dut</em>
* `EMPTY` (22): <em>wata, wasu, nan, su, ", ., wàndon</em>

`PronType` seems to be **lexical feature** of `DET`. 100% lemmas (10) occur only with one value of `PronType`.

### `ADV`

169 <tt><a href="ha_northernautogramm-pos-ADV.html">ADV</a></tt> tokens (26% of all `ADV` tokens) have a non-empty value of `PronType`.

`ADV` tokens may have the following values of `PronType`:

* `Dem` (116; 69% of non-empty `PronType`): <em>nan, nân, can, cân, nanânga, ceːnìyaː</em>
* `Ind` (3; 2% of non-empty `PronType`): <em>koː'ìnaː</em>
* `Int` (23; 14% of non-empty `PronType`): <em>ìnaː, ƙàːƙàː, yaushèː</em>
* `Rel` (25; 15% of non-empty `PronType`): <em>indà, wàdà, indàduk, duwwàdà, indàdun, indàdut, koːìnaː, koːƙàːƙàː, kóːkòːindà</em>
* `Tot` (2; 1% of non-empty `PronType`): <em>dukà, duh</em>
* `EMPTY` (487): <em>sai, sa'ànnan, yànzu, hakànan, gàba, baːya, hakà, kusa, dâːmaː, dâː</em>

`PronType` seems to be **lexical feature** of `ADV`. 100% lemmas (16) occur only with one value of `PronType`.

### `ADJ`

1 <tt><a href="ha_northernautogramm-pos-ADJ.html">ADJ</a></tt> tokens (1% of all `ADJ` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADJ` and `PronType` co-occurred: <tt><a href="ha_northernautogramm-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (1; 100%), <tt><a href="ha_northernautogramm-feat-Gender.html">Gender</a></tt><tt>=Fem</tt> (1; 100%), <tt><a href="ha_northernautogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (1; 100%).

`ADJ` tokens may have the following values of `PronType`:

* `Int` (1; 100% of non-empty `PronType`): <em>wacèː</em>
* `EMPTY` (99): <em>ɗan, hàlle-hàllan, ɗam, daban, hwarin, jaː, jàkkai, maːtaː, namijì, 'yab</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>ADV --[<tt><a href="ha_northernautogramm-dep-conj.html">conj</a></tt>]--> ADV</tt> (7; 100%),
<tt>ADV --[<tt><a href="ha_northernautogramm-dep-parataxis.html">parataxis</a></tt>]--> ADV</tt> (7; 100%),
<tt>PRON --[<tt><a href="ha_northernautogramm-dep-conj.html">conj</a></tt>]--> PRON</tt> (3; 100%),
<tt>PRON --[<tt><a href="ha_northernautogramm-dep-reparandum.html">reparandum</a></tt>]--> PRON</tt> (1; 100%),
<tt>PRON --[<tt><a href="ha_northernautogramm-dep-vocative.html">vocative</a></tt>]--> PRON</tt> (1; 100%).

