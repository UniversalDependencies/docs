---
layout: base
title:  'Statistics of NumType in UD_Esperanto-Prago'
udver: '2'
---

## Treebank Statistics: UD_Esperanto-Prago: Features: `NumType`

This feature is universal.
It occurs with 1 different values: `Card`.

20 tokens (1%) have a non-empty value of `NumType`.
15 types (2%) occur at least once with a non-empty value of `NumType`.
14 lemmas (2%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="eo_prago-pos-NUM.html">NUM</a></tt> (19; 1% instances), <tt><a href="eo_prago-pos-ADV.html">ADV</a></tt> (1; 0% instances).

### `NUM`

19 <tt><a href="eo_prago-pos-NUM.html">NUM</a></tt> tokens (45% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (19; 100% of non-empty `NumType`): <em>unu, 1, 1913, 2, I, II, III, IV, IX, V</em>
* `EMPTY` (23): <em>1, 2, 3, 4, 5, 6, 7, 8, unu, 10</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (14) occur only with one value of `NumType`.

### `ADV`

1 <tt><a href="eo_prago-pos-ADV.html">ADV</a></tt> tokens (0% of all `ADV` tokens) have a non-empty value of `NumType`.

`ADV` tokens may have the following values of `NumType`:

* `Card` (1; 100% of non-empty `NumType`): <em>unuj</em>
* `EMPTY` (204): <em>ne, nur, kiel, plej, pli, tute, kiam, tamen, eĉ, speciale</em>

