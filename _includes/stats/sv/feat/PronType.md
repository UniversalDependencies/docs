

--------------------------------------------------------------------------------

## Treebank Statistics (UD_Swedish)

This feature is universal.
It occurs with 2 different values: `Int`, `Rel`.
Some words have combined values of the feature; 1 combinations have been observed: `Int|Rel`.

1401 tokens (1%) have a non-empty value of `PronType`.
14 types (0%) occur at least once with a non-empty value of `PronType`.
7 lemmas (0%) occur at least once with a non-empty value of `PronType`.
The feature is used with 2 part-of-speech tags: [sv-pos/PRON]() (1310; 1% instances), [sv-pos/DET]() (91; 0% instances).

### `PRON`

1310 [sv-pos/PRON]() tokens (22% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="Case.html">Case</a>=EMPTY</tt> (1310; 100%), <tt><a href="Gender.html">Gender</a>=EMPTY</tt> (1130; 86%), <tt><a href="Definite.html">Definite</a>=EMPTY</tt> (1111; 85%), <tt><a href="Number.html">Number</a>=EMPTY</tt> (1111; 85%).

`PRON` tokens may have the following values of `PronType`:

* `Int,Rel` (1310; 100% of non-empty `PronType`): <em>som, vad, vilket, vilka, vem, vilken</em>
* `EMPTY` (4665): <em>det, man, de, sig, vi, den, du, detta, dem, han</em>

### `DET`

91 [sv-pos/DET]() tokens (2% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="Poss.html">Poss</a>=EMPTY</tt> (75; 82%), <tt><a href="Definite.html">Definite</a>=Ind</tt> (75; 82%), <tt><a href="Gender.html">Gender</a>=EMPTY</tt> (56; 62%).

`DET` tokens may have the following values of `PronType`:

* `Int,Rel` (91; 100% of non-empty `PronType`): <em>vilka, vilken, vars, vilket, vilkas, Vems</em>
* `EMPTY` (5565): <em>en, den, ett, de, det, sin, alla, denna, varje, dessa</em>

