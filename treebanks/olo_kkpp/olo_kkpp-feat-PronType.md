---
layout: base
title:  'Statistics of PronType in UD_Livvi-KKPP'
udver: '2'
---

## Treebank Statistics: UD_Livvi-KKPP: Features: `PronType`

This feature is universal.
It occurs with 6 different values: `Dem`, `Ind`, `Int`, `Prs`, `Rel`, `Tot`.

102 tokens (6%) have a non-empty value of `PronType`.
57 types (8%) occur at least once with a non-empty value of `PronType`.
30 lemmas (5%) occur at least once with a non-empty value of `PronType`.
The feature is used with 1 part-of-speech tags: <tt><a href="olo_kkpp-pos-PRON.html">PRON</a></tt> (102; 6% instances).

### `PRON`

102 <tt><a href="olo_kkpp-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="olo_kkpp-feat-Number.html">Number</a></tt><tt>=Sing</tt> (77; 75%), <tt><a href="olo_kkpp-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (65; 64%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (31; 30% of non-empty `PronType`): <em>tämän, se, tämä, net, sidä, Niilöin, Tädä, niidy, nämmis, sen</em>
* `Ind` (14; 14% of non-empty `PronType`): <em>toine, toizel, toizen, mondu, nimidä</em>
* `Int` (10; 10% of non-empty `PronType`): <em>midä, Midäbo, ken, kus, mis, mit</em>
* `Prs` (42; 41% of non-empty `PronType`): <em>häi, minun, minul, hyö, iče, minä, Heil, Hänel, heidy, händy</em>
* `Rel` (4; 4% of non-empty `PronType`): <em>kudamat, kudamua, kuduan, kuduat</em>
* `Tot` (1; 1% of non-empty `PronType`): <em>kai</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (30) occur only with one value of `PronType`.

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="olo_kkpp-dep-orphan.html">orphan</a></tt>]--> PRON</tt> (3; 100%),
<tt>PRON --[<tt><a href="olo_kkpp-dep-nmod.html">nmod</a></tt>]--> PRON</tt> (1; 100%).

