---
layout: base
title:  'Statistics of PronType in UD_Naija-NSC'
udver: '2'
---

## Treebank Statistics: UD_Naija-NSC: Features: `PronType`

This feature is universal.
It occurs with 5 different values: `Art`, `Dem`, `Int`, `Prs`, `Rel`.

18519 tokens (15%) have a non-empty value of `PronType`.
66 types (1%) occur at least once with a non-empty value of `PronType`.
43 lemmas (1%) occur at least once with a non-empty value of `PronType`.
The feature is used with 7 part-of-speech tags: <tt><a href="pcm_nsc-pos-PRON.html">PRON</a></tt> (13626; 11% instances), <tt><a href="pcm_nsc-pos-DET.html">DET</a></tt> (4413; 3% instances), <tt><a href="pcm_nsc-pos-ADV.html">ADV</a></tt> (429; 0% instances), <tt><a href="pcm_nsc-pos-PART.html">PART</a></tt> (45; 0% instances), <tt><a href="pcm_nsc-pos-SCONJ.html">SCONJ</a></tt> (3; 0% instances), <tt><a href="pcm_nsc-pos-AUX.html">AUX</a></tt> (2; 0% instances), <tt><a href="pcm_nsc-pos-PUNCT.html">PUNCT</a></tt> (1; 0% instances).

### `PRON`

13626 <tt><a href="pcm_nsc-pos-PRON.html">PRON</a></tt> tokens (86% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="pcm_nsc-feat-Case.html">Case</a></tt><tt>=Nom</tt> (10282; 75%), <tt><a href="pcm_nsc-feat-Number.html">Number</a></tt><tt>=Sing</tt> (7363; 54%).

`PRON` tokens may have the following values of `PronType`:

* `Art` (3; 0% of non-empty `PronType`): <em>dem</em>
* `Dem` (369; 3% of non-empty `PronType`): <em>dat, dis, dose, dese, those</em>
* `Int` (437; 3% of non-empty `PronType`): <em>wetin, who, what</em>
* `Prs` (12804; 94% of non-empty `PronType`): <em>I, you, e, we, am, de, me, dem, im, us</em>
* `Rel` (13; 0% of non-empty `PronType`): <em>dat, which, that</em>
* `EMPTY` (2279): <em>my, your, our, deir, im, everybody, everyting, some, anyting, someting</em>

<table>
  <tr><th>Paradigm <i>dem</i></th><th><tt>Prs</tt></th><th><tt>Art</tt></th></tr>
  <tr><td><tt><tt><a href="pcm_nsc-feat-Case.html">Case</a></tt><tt>=Acc</tt>|<tt><a href="pcm_nsc-feat-Person.html">Person</a></tt><tt>=3</tt></tt></td><td><em>dem, them</em></td><td></td></tr>
  <tr><td><tt><tt><a href="pcm_nsc-feat-Case.html">Case</a></tt><tt>=Nom</tt></tt></td><td></td><td><em>dem</em></td></tr>
  <tr><td><tt><tt><a href="pcm_nsc-feat-Case.html">Case</a></tt><tt>=Nom</tt>|<tt><a href="pcm_nsc-feat-Person.html">Person</a></tt><tt>=3</tt></tt></td><td><em>de, dem, den, they</em></td><td></td></tr>
</table>

`PronType` seems to be **lexical feature** of `PRON`. 97% lemmas (30) occur only with one value of `PronType`.

### `DET`

4413 <tt><a href="pcm_nsc-pos-DET.html">DET</a></tt> tokens (91% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="pcm_nsc-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (2990; 68%), <tt><a href="pcm_nsc-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (2617; 59%).

`DET` tokens may have the following values of `PronType`:

* `Art` (2952; 67% of non-empty `PronType`): <em>di, one, a, the, an, dem</em>
* `Dem` (1422; 32% of non-empty `PronType`): <em>dis, dat, dose, dese, that, those, this</em>
* `Int` (39; 1% of non-empty `PronType`): <em>which</em>
* `EMPTY` (414): <em>some, any, anoder, every, anoda, each, no, both, few, a</em>

`PronType` seems to be **lexical feature** of `DET`. 100% lemmas (10) occur only with one value of `PronType`.

### `ADV`

429 <tt><a href="pcm_nsc-pos-ADV.html">ADV</a></tt> tokens (8% of all `ADV` tokens) have a non-empty value of `PronType`.

`ADV` tokens may have the following values of `PronType`:

* `Int` (429; 100% of non-empty `PronType`): <em>how, where, why, when</em>
* `EMPTY` (5156): <em>so, now, just, all, even, well, dere, den, still, very</em>

### `PART`

45 <tt><a href="pcm_nsc-pos-PART.html">PART</a></tt> tokens (2% of all `PART` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PART` and `PronType` co-occurred: <tt><a href="pcm_nsc-feat-PartType.html">PartType</a></tt><tt>=EMPTY</tt> (45; 100%), <tt><a href="pcm_nsc-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (45; 100%).

`PART` tokens may have the following values of `PronType`:

* `Art` (32; 71% of non-empty `PronType`): <em>dem</em>
* `Int` (13; 29% of non-empty `PronType`): <em>shey</em>
* `EMPTY` (2315): <em>no, o, sef, sha, not, ni, n't, ma, don't, self</em>

### `SCONJ`

3 <tt><a href="pcm_nsc-pos-SCONJ.html">SCONJ</a></tt> tokens (0% of all `SCONJ` tokens) have a non-empty value of `PronType`.

`SCONJ` tokens may have the following values of `PronType`:

* `Dem` (1; 33% of non-empty `PronType`): <em>dat</em>
* `Int` (1; 33% of non-empty `PronType`): <em>when</em>
* `Rel` (1; 33% of non-empty `PronType`): <em>dat</em>
* `EMPTY` (4555): <em>sey, wey, if, because, when, so, wen, dat, wheder, once</em>

### `AUX`

2 <tt><a href="pcm_nsc-pos-AUX.html">AUX</a></tt> tokens (0% of all `AUX` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `AUX` and `PronType` co-occurred: <tt><a href="pcm_nsc-feat-Aspect.html">Aspect</a></tt><tt>=EMPTY</tt> (2; 100%), <tt><a href="pcm_nsc-feat-PartType.html">PartType</a></tt><tt>=EMPTY</tt> (2; 100%).

`AUX` tokens may have the following values of `PronType`:

* `Prs` (2; 100% of non-empty `PronType`): <em>'ll, 're</em>
* `EMPTY` (10750): <em>dey, go, na, con, don, be, make, fit, come, meh</em>

### `PUNCT`

1 <tt><a href="pcm_nsc-pos-PUNCT.html">PUNCT</a></tt> tokens (0% of all `PUNCT` tokens) have a non-empty value of `PronType`.

`PUNCT` tokens may have the following values of `PronType`:

* `Int` (1; 100% of non-empty `PronType`): <em>||</em>
* `EMPTY` (36614): <em>#, //, <, {, }, [, |c, ||, ], >+</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="pcm_nsc-dep-conj-dicto.html">conj:dicto</a></tt>]--> PRON</tt> (118; 96%),
<tt>DET --[<tt><a href="pcm_nsc-dep-conj-dicto.html">conj:dicto</a></tt>]--> DET</tt> (43; 80%),
<tt>PRON --[<tt><a href="pcm_nsc-dep-conj.html">conj</a></tt>]--> PRON</tt> (28; 88%),
<tt>ADV --[<tt><a href="pcm_nsc-dep-conj-dicto.html">conj:dicto</a></tt>]--> ADV</tt> (13; 100%),
<tt>ADV --[<tt><a href="pcm_nsc-dep-parataxis-conj.html">parataxis:conj</a></tt>]--> ADV</tt> (11; 92%),
<tt>PRON --[<tt><a href="pcm_nsc-dep-parataxis-conj.html">parataxis:conj</a></tt>]--> PRON</tt> (7; 100%),
<tt>ADV --[<tt><a href="pcm_nsc-dep-conj.html">conj</a></tt>]--> ADV</tt> (6; 86%),
<tt>PRON --[<tt><a href="pcm_nsc-dep-compound-redup.html">compound:redup</a></tt>]--> PRON</tt> (4; 100%),
<tt>PRON --[<tt><a href="pcm_nsc-dep-appos.html">appos</a></tt>]--> PRON</tt> (3; 100%),
<tt>PRON --[<tt><a href="pcm_nsc-dep-conj.html">conj</a></tt>]--> ADV</tt> (3; 75%).

