---
layout: base
title:  'Statistics of Voice in UD_Norwegian-NynorskLIA'
udver: '2'
---

## Treebank Statistics: UD_Norwegian-NynorskLIA: Features: `Voice`

This feature is universal.
It occurs with 1 different values: `Pass`.

43 tokens (0%) have a non-empty value of `Voice`.
24 types (1%) occur at least once with a non-empty value of `Voice`.
23 lemmas (1%) occur at least once with a non-empty value of `Voice`.
The feature is used with 1 part-of-speech tags: <tt><a href="no_nynorsklia-pos-VERB.html">VERB</a></tt> (43; 0% instances).

### `VERB`

43 <tt><a href="no_nynorsklia-pos-VERB.html">VERB</a></tt> tokens (1% of all `VERB` tokens) have a non-empty value of `Voice`.

The most frequent other feature values with which `VERB` and `Voice` co-occurred: <tt><a href="no_nynorsklia-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (43; 100%), <tt><a href="no_nynorsklia-feat-Tense.html">Tense</a></tt><tt>=EMPTY</tt> (43; 100%), <tt><a href="no_nynorsklia-feat-VerbForm.html">VerbForm</a></tt><tt>=Inf</tt> (43; 100%).

`VERB` tokens may have the following values of `Voice`:

* `Pass` (43; 100% of non-empty `Voice`): <em>kallast, gjerast, samlast, settest, deltest, skytast, boltast, bygdest, bytast, dyrkast</em>
* `EMPTY` (5458): <em>var, er, hadde, har, veit, kom, blir, trur, gjekk, sa</em>

`Voice` seems to be **lexical feature** of `VERB`. 100% lemmas (23) occur only with one value of `Voice`.

