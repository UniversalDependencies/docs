

--------------------------------------------------------------------------------

## Treebank Statistics (UD_Croatian)

This feature is universal.
It occurs with 1 different values: `Imp`.

97 tokens (0%) have a non-empty value of `Mood`.
93 types (0%) occur at least once with a non-empty value of `Mood`.
80 lemmas (1%) occur at least once with a non-empty value of `Mood`.
The feature is used with 2 part-of-speech tags: [hr-pos/VERB]() (96; 0% instances), [hr-pos/AUX]() (1; 0% instances).

### `VERB`

96 [hr-pos/VERB]() tokens (1% of all `VERB` tokens) have a non-empty value of `Mood`.

The most frequent other feature values with which `VERB` and `Mood` co-occurred: <tt><a href="Tense.html">Tense</a>=EMPTY</tt> (96; 100%), <tt><a href="VerbForm.html">VerbForm</a>=EMPTY</tt> (96; 100%), <tt><a href="Gender.html">Gender</a>=EMPTY</tt> (96; 100%), <tt><a href="Person.html">Person</a>=2</tt> (84; 88%), <tt><a href="Number.html">Number</a>=Plur</tt> (72; 75%).

`VERB` tokens may have the following values of `Mood`:

* `Imp` (96; 100% of non-empty `Mood`): <em>izbjegavajte, recimo, dodajte, oprostite, slušajte, Dajte, Dodajmo, Dođi, Ispisujte, Koristite</em>
* `EMPTY` (11660): <em>izjavio, rekao, kazao, ima, može, kaže, mogu, treba, mora, nema</em>

`Mood` seems to be **lexical feature** of `VERB`. 100% lemmas (79) occur only with one value of `Mood`.

### `AUX`

1 [hr-pos/AUX]() tokens (0% of all `AUX` tokens) have a non-empty value of `Mood`.

The most frequent other feature values with which `AUX` and `Mood` co-occurred: <tt><a href="Person.html">Person</a>=2</tt> (1; 100%), <tt><a href="Number.html">Number</a>=Plur</tt> (1; 100%), <tt><a href="Tense.html">Tense</a>=EMPTY</tt> (1; 100%).

`AUX` tokens may have the following values of `Mood`:

* `Imp` (1; 100% of non-empty `Mood`): <em>budite</em>
* `EMPTY` (8988): <em>je, su, će, bi, biti, nije, bilo, smo, bio, sam</em>

## Relations with Agreement in `Mood`

The 10 most frequent relations where parent and child node agree in `Mood`:
<tt>VERB --[<a href="../dep/conj.html">conj</a>]--> VERB</tt> (18; 64%).

