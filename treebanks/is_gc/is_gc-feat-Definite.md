---
layout: base
title:  'Statistics of Definite in UD_Icelandic-GC'
udver: '2'
---

## Treebank Statistics: UD_Icelandic-GC: Features: `Definite`

This feature is universal.
It occurs with 1 different values: `Def`.

5358 tokens (5%) have a non-empty value of `Definite`.
3183 types (16%) occur at least once with a non-empty value of `Definite`.
2234 lemmas (16%) occur at least once with a non-empty value of `Definite`.
The feature is used with 4 part-of-speech tags: <tt><a href="is_gc-pos-NOUN.html">NOUN</a></tt> (4785; 5% instances), <tt><a href="is_gc-pos-PROPN.html">PROPN</a></tt> (361; 0% instances), <tt><a href="is_gc-pos-ADV.html">ADV</a></tt> (167; 0% instances), <tt><a href="is_gc-pos-DET.html">DET</a></tt> (45; 0% instances).

### `NOUN`

4785 <tt><a href="is_gc-pos-NOUN.html">NOUN</a></tt> tokens (23% of all `NOUN` tokens) have a non-empty value of `Definite`.

The most frequent other feature values with which `NOUN` and `Definite` co-occurred: <tt><a href="is_gc-feat-Number.html">Number</a></tt><tt>=Sing</tt> (3975; 83%).

`NOUN` tokens may have the following values of `Definite`:

* `Def` (4785; 100% of non-empty `Definite`): <em>málið, landsins, liðið, landinu, leiknum, fyrirtækið, málinu, félagið, lögreglan, félagsins</em>
* `EMPTY` (15779): <em>ára, fólk, sæti, maður, og, stað, tíma, menn, mál, áhrif</em>

`Definite` seems to be **lexical feature** of `NOUN`. 100% lemmas (2058) occur only with one value of `Definite`.

### `PROPN`

361 <tt><a href="is_gc-pos-PROPN.html">PROPN</a></tt> tokens (6% of all `PROPN` tokens) have a non-empty value of `Definite`.

The most frequent other feature values with which `PROPN` and `Definite` co-occurred: <tt><a href="is_gc-feat-Number.html">Number</a></tt><tt>=Sing</tt> (299; 83%).

`PROPN` tokens may have the following values of `Definite`:

* `Def` (361; 100% of non-empty `Definite`): <em>Bandaríkjunum, Bandaríkjanna, Morgunblaðinu, Sjálfstæðisflokksins, Framsóknarflokksins, Landsbankans, Fréttablaðinu, Fréttablaðsins, Landhelgisgæslunnar, Samfylkingarinnar</em>
* `EMPTY` (5764): <em>Trump, Íslands, Íslandi, þór, Jón, Reykjavík, Ísland, Guðmundur, 2, Facebook</em>

`Definite` seems to be **lexical feature** of `PROPN`. 100% lemmas (192) occur only with one value of `Definite`.

### `ADV`

167 <tt><a href="is_gc-pos-ADV.html">ADV</a></tt> tokens (2% of all `ADV` tokens) have a non-empty value of `Definite`.

`ADV` tokens may have the following values of `Definite`:

* `Def` (167; 100% of non-empty `Definite`): <em>daginn, árið, laugardaginn, sunnudaginn, tímanum, dagana, helgina, lokin, árin, árinu</em>
* `EMPTY` (10132): <em>ekki, þar, þá, í, á, fram, svo, upp, dag, til</em>

`Definite` seems to be **lexical feature** of `ADV`. 100% lemmas (63) occur only with one value of `Definite`.

### `DET`

45 <tt><a href="is_gc-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `Definite`.

The most frequent other feature values with which `DET` and `Definite` co-occurred: <tt><a href="is_gc-feat-Number.html">Number</a></tt><tt>=Sing</tt> (36; 80%).

`DET` tokens may have the following values of `Definite`:

* `Def` (45; 100% of non-empty `Definite`): <em>hið, hin, hinn, hinum, hinu, Hinir, hina, WOW-ið, hinar, hinnar</em>

## Relations with Agreement in `Definite`

The 10 most frequent relations where parent and child node agree in `Definite`:
<tt>NOUN --[<tt><a href="is_gc-dep-flat.html">flat</a></tt>]--> NOUN</tt> (31; 100%),
<tt>ADV --[<tt><a href="is_gc-dep-flat.html">flat</a></tt>]--> NOUN</tt> (4; 100%),
<tt>NOUN --[<tt><a href="is_gc-dep-xcomp.html">xcomp</a></tt>]--> NOUN</tt> (3; 75%),
<tt>NOUN --[<tt><a href="is_gc-dep-dep.html">dep</a></tt>]--> NOUN</tt> (1; 100%).

