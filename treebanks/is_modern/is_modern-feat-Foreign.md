---
layout: base
title:  'Statistics of Foreign in UD_Icelandic-Modern'
udver: '2'
---

## Treebank Statistics: UD_Icelandic-Modern: Features: `Foreign`

This feature is universal.
It occurs with 1 different values: `Yes`.

118 tokens (0%) have a non-empty value of `Foreign`.
53 types (1%) occur at least once with a non-empty value of `Foreign`.
50 lemmas (1%) occur at least once with a non-empty value of `Foreign`.
The feature is used with 1 part-of-speech tags: <tt><a href="is_modern-pos-X.html">X</a></tt> (118; 0% instances).

### `X`

118 <tt><a href="is_modern-pos-X.html">X</a></tt> tokens (64% of all `X` tokens) have a non-empty value of `Foreign`.

`X` tokens may have the following values of `Foreign`:

* `Yes` (118; 100% of non-empty `Foreign`): <em>Molde, 2016, Eidur, FK, i, se, your, 22, 3, 8</em>
* `EMPTY` (66): <em>að, Damörku, Danmörki, EM, Hafnfirðingur, a.m.k., brimbreti, horfið, hryðjuverkaárásana, keppt</em>

`Foreign` seems to be **lexical feature** of `X`. 100% lemmas (50) occur only with one value of `Foreign`.

## Relations with Agreement in `Foreign`

The 10 most frequent relations where parent and child node agree in `Foreign`:
<tt>X --[<tt><a href="is_modern-dep-flat-foreign.html">flat:foreign</a></tt>]--> X</tt> (66; 100%),
<tt>X --[<tt><a href="is_modern-dep-dep.html">dep</a></tt>]--> X</tt> (8; 100%).

