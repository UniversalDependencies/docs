---
layout: base
title:  'Statistics of PronType in UD_Tupinamba-TuDeT'
udver: '2'
---

## Treebank Statistics: UD_Tupinamba-TuDeT: Features: `PronType`

This feature is universal.
It occurs with 3 different values: `Ind`, `Int`, `Prs`.

53 tokens (3%) have a non-empty value of `PronType`.
19 types (3%) occur at least once with a non-empty value of `PronType`.
10 lemmas (2%) occur at least once with a non-empty value of `PronType`.
The feature is used with 1 part-of-speech tags: <tt><a href="tpn_tudet-pos-PRON.html">PRON</a></tt> (53; 3% instances).

### `PRON`

53 <tt><a href="tpn_tudet-pos-PRON.html">PRON</a></tt> tokens (79% of all `PRON` tokens) have a non-empty value of `PronType`.

`PRON` tokens may have the following values of `PronType`:

* `Ind` (16; 30% of non-empty `PronType`): <em>amõ, paβẽ, amõamõ</em>
* `Int` (14; 26% of non-empty `PronType`): <em>marã, aβá, Aβápe, Mbaʔepe, sumarã, ʃesumarã</em>
* `Prs` (23; 43% of non-empty `PronType`): <em>asé, iʃé, aséβe, jandé, peẽ, ʃe</em>
* `EMPTY` (14): <em>o, asé, jandé, jepé, mbaʔepe, Aβáaβápe, Cirineu, Opa</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (10) occur only with one value of `PronType`.

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="tpn_tudet-dep-conj.html">conj</a></tt>]--> PRON</tt> (1; 100%).

