---
layout: base
title:  'Statistics of ExtPos in UD_Pashto-Sikaram'
udver: '2'
---

## Treebank Statistics: UD_Pashto-Sikaram: Features: `ExtPos`

This feature is language-specific.
It occurs with 3 different values: `ADJ`, `ADV`, `SCONJ`.

24 tokens (0%) have a non-empty value of `ExtPos`.
10 types (1%) occur at least once with a non-empty value of `ExtPos`.
10 lemmas (1%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 5 part-of-speech tags: <tt><a href="ps_sikaram-pos-ADV.html">ADV</a></tt> (11; 0% instances), <tt><a href="ps_sikaram-pos-ADJ.html">ADJ</a></tt> (5; 0% instances), <tt><a href="ps_sikaram-pos-SCONJ.html">SCONJ</a></tt> (5; 0% instances), <tt><a href="ps_sikaram-pos-NOUN.html">NOUN</a></tt> (2; 0% instances), <tt><a href="ps_sikaram-pos-X.html">X</a></tt> (1; 0% instances).

### `ADV`

11 <tt><a href="ps_sikaram-pos-ADV.html">ADV</a></tt> tokens (5% of all `ADV` tokens) have a non-empty value of `ExtPos`.

`ADV` tokens may have the following values of `ExtPos`:

* `ADV` (7; 64% of non-empty `ExtPos`): <em>کله, همدا</em>
* `SCONJ` (4; 36% of non-empty `ExtPos`): <em>کله</em>

<table>
  <tr><th>Paradigm <i>کله</i></th><th><tt>ADV</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>کله</em></td><td><em>کله</em></td></tr>
</table>

### `ADJ`

5 <tt><a href="ps_sikaram-pos-ADJ.html">ADJ</a></tt> tokens (1% of all `ADJ` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADJ` and `ExtPos` co-occurred: <tt><a href="ps_sikaram-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (3; 60%), <tt><a href="ps_sikaram-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (3; 60%), <tt><a href="ps_sikaram-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (3; 60%).

`ADJ` tokens may have the following values of `ExtPos`:

* `ADJ` (3; 60% of non-empty `ExtPos`): <em>هـ</em>
* `ADV` (2; 40% of non-empty `ExtPos`): <em>لږ</em>

### `SCONJ`

5 <tt><a href="ps_sikaram-pos-SCONJ.html">SCONJ</a></tt> tokens (3% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

`SCONJ` tokens may have the following values of `ExtPos`:

* `SCONJ` (5; 100% of non-empty `ExtPos`): <em>که, لکه, ځکه, چې</em>

### `NOUN`

2 <tt><a href="ps_sikaram-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `NOUN` and `ExtPos` co-occurred: <tt><a href="ps_sikaram-feat-Case.html">Case</a></tt><tt>=Loc</tt> (2; 100%), <tt><a href="ps_sikaram-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (2; 100%), <tt><a href="ps_sikaram-feat-Number.html">Number</a></tt><tt>=Sing</tt> (2; 100%).

`NOUN` tokens may have the following values of `ExtPos`:

* `ADV` (2; 100% of non-empty `ExtPos`): <em>ټکي</em>

### `X`

1 <tt><a href="ps_sikaram-pos-X.html">X</a></tt> tokens (2% of all `X` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `X` and `ExtPos` co-occurred: <tt><a href="ps_sikaram-feat-Foreign.html">Foreign</a></tt><tt>=EMPTY</tt> (1; 100%).

`X` tokens may have the following values of `ExtPos`:

* `ADV` (1; 100% of non-empty `ExtPos`): <em>کټ</em>

