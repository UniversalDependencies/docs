---
layout: base
title:  'Statistics of ExtPos in UD_Old_Occitan-CorAG'
udver: '2'
---

## Treebank Statistics: UD_Old_Occitan-CorAG: Features: `ExtPos`

This feature is language-specific.
It occurs with 4 different values: `ADP`, `ADV`, `CCONJ`, `SCONJ`.

445 tokens (1%) have a non-empty value of `ExtPos`.
118 types (2%) occur at least once with a non-empty value of `ExtPos`.
1 lemmas (0) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 7 part-of-speech tags: <tt><a href="pro_corag-pos-ADV.html">ADV</a></tt> (203; 0% instances), <tt><a href="pro_corag-pos-ADP.html">ADP</a></tt> (155; 0% instances), <tt><a href="pro_corag-pos-SCONJ.html">SCONJ</a></tt> (47; 0% instances), <tt><a href="pro_corag-pos-PRON.html">PRON</a></tt> (29; 0% instances), <tt><a href="pro_corag-pos-VERB.html">VERB</a></tt> (8; 0% instances), <tt><a href="pro_corag-pos-NOUN.html">NOUN</a></tt> (2; 0% instances), <tt><a href="pro_corag-pos-CCONJ.html">CCONJ</a></tt> (1; 0% instances).

### `ADV`

203 <tt><a href="pro_corag-pos-ADV.html">ADV</a></tt> tokens (9% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="pro_corag-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (191; 94%).

`ADV` tokens may have the following values of `ExtPos`:

* `ADP` (19; 9% of non-empty `ExtPos`): <em>fore, quant, no, abant, afin, comme, ensemps, for, sino, senes</em>
* `ADV` (32; 16% of non-empty `ExtPos`): <em>tant, ayssi, no, aixi, assi, ayssi-, certas, cum, si, sino</em>
* `CCONJ` (11; 5% of non-empty `ExtPos`): <em>ayssi, tout, aixi, ayssi-, ensemps</em>
* `SCONJ` (141; 69% of non-empty `ExtPos`): <em>tant, ayssi, sino, aixi, sinon, apres, puix, auparauant, ont, aissi</em>

### `ADP`

155 <tt><a href="pro_corag-pos-ADP.html">ADP</a></tt> tokens (2% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (56; 36% of non-empty `ExtPos`): <em>entro, per, d', saub, contra, ob, ses, Seis, ab, am</em>
* `ADV` (3; 2% of non-empty `ExtPos`): <em>per, de</em>
* `SCONJ` (96; 62% of non-empty `ExtPos`): <em>entro, per, en, ab, de, segont, à, saub, senhs, a</em>

### `SCONJ`

47 <tt><a href="pro_corag-pos-SCONJ.html">SCONJ</a></tt> tokens (2% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

`SCONJ` tokens may have the following values of `ExtPos`:

* `ADP` (2; 4% of non-empty `ExtPos`): <em>cum, si</em>
* `ADV` (13; 28% of non-empty `ExtPos`): <em>si, cum, -si, ayssicum, se</em>
* `SCONJ` (32; 68% of non-empty `ExtPos`): <em>si, jassia, jassie, affin, cum, mentra, dequé, que, sinauque</em>

### `PRON`

29 <tt><a href="pro_corag-pos-PRON.html">PRON</a></tt> tokens (1% of all `PRON` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PRON` and `ExtPos` co-occurred: <tt><a href="pro_corag-feat-Number.html">Number</a></tt><tt>=Sing</tt> (29; 100%), <tt><a href="pro_corag-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (29; 100%), <tt><a href="pro_corag-feat-PronType.html">PronType</a></tt><tt>=Dem</tt> (29; 100%), <tt><a href="pro_corag-feat-Gender.html">Gender</a></tt><tt>=Neut</tt> (25; 86%).

`PRON` tokens may have the following values of `ExtPos`:

* `ADV` (29; 100% of non-empty `ExtPos`): <em>so, so-, ço</em>

### `VERB`

8 <tt><a href="pro_corag-pos-VERB.html">VERB</a></tt> tokens (0% of all `VERB` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `VERB` and `ExtPos` co-occurred: <tt><a href="pro_corag-feat-Number.html">Number</a></tt><tt>=Sing</tt> (8; 100%), <tt><a href="pro_corag-feat-Voice.html">Voice</a></tt><tt>=EMPTY</tt> (7; 88%), <tt><a href="pro_corag-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (5; 63%), <tt><a href="pro_corag-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (5; 63%), <tt><a href="pro_corag-feat-Tense.html">Tense</a></tt><tt>=Past</tt> (5; 63%), <tt><a href="pro_corag-feat-VerbForm.html">VerbForm</a></tt><tt>=Part</tt> (5; 63%).

`VERB` tokens may have the following values of `ExtPos`:

* `ADV` (3; 38% of non-empty `ExtPos`): <em>es</em>
* `SCONJ` (5; 63% of non-empty `ExtPos`): <em>pausat, actendut, expausat</em>

### `NOUN`

2 <tt><a href="pro_corag-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `ExtPos`.

`NOUN` tokens may have the following values of `ExtPos`:

* `SCONJ` (2; 100% of non-empty `ExtPos`): <em>rason, sobres</em>

### `CCONJ`

1 <tt><a href="pro_corag-pos-CCONJ.html">CCONJ</a></tt> tokens (0% of all `CCONJ` tokens) have a non-empty value of `ExtPos`.

`CCONJ` tokens may have the following values of `ExtPos`:

* `SCONJ` (1; 100% of non-empty `ExtPos`): <em>pero</em>

