---
layout: base
title:  'Statistics of Foreign in UD_Serbian-SET'
udver: '2'
---

## Treebank Statistics: UD_Serbian-SET: Features: `Foreign`

This feature is universal.
It occurs with 1 different values: `Yes`.

265 tokens (0%) have a non-empty value of `Foreign`.
130 types (1%) occur at least once with a non-empty value of `Foreign`.
133 lemmas (1%) occur at least once with a non-empty value of `Foreign`.
The feature is used with 1 part-of-speech tags: <tt><a href="sr_set-pos-X.html">X</a></tt> (265; 0% instances).

### `X`

265 <tt><a href="sr_set-pos-X.html">X</a></tt> tokens (85% of all `X` tokens) have a non-empty value of `Foreign`.

`X` tokens may have the following values of `Foreign`:

* `Yes` (265; 100% of non-empty `Foreign`): <em>SETimes, Setimes, A1, European, Windows, Southeast, XP, Cro-a-Porter, Microsoft, SETimesu</em>
* `EMPTY` (47): <em>al, de, bin, B92, Ali, B, H1N1, da, D, G17</em>

`Foreign` seems to be **lexical feature** of `X`. 100% lemmas (133) occur only with one value of `Foreign`.

## Relations with Agreement in `Foreign`

The 10 most frequent relations where parent and child node agree in `Foreign`:
<tt>X --[<tt><a href="sr_set-dep-flat.html">flat</a></tt>]--> X</tt> (76; 100%),
<tt>X --[<tt><a href="sr_set-dep-conj.html">conj</a></tt>]--> X</tt> (7; 100%),
<tt>X --[<tt><a href="sr_set-dep-fixed.html">fixed</a></tt>]--> X</tt> (1; 100%),
<tt>X --[<tt><a href="sr_set-dep-parataxis.html">parataxis</a></tt>]--> X</tt> (1; 100%).

