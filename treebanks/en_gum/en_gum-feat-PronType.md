---
layout: base
title:  'Statistics of PronType in UD_English-GUM'
udver: '2'
---

## Treebank Statistics: UD_English-GUM: Features: `PronType`

This feature is universal but the values `Emp` are language-specific.
It occurs with 10 different values: `Art`, `Dem`, `Emp`, `Ind`, `Int`, `Neg`, `Prs`, `Rcp`, `Rel`, `Tot`.

40969 tokens (18%) have a non-empty value of `PronType`.
175 types (1%) occur at least once with a non-empty value of `PronType`.
75 lemmas (1%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="en_gum-pos-PRON.html">PRON</a></tt> (19979; 9% instances), <tt><a href="en_gum-pos-DET.html">DET</a></tt> (19057; 8% instances), <tt><a href="en_gum-pos-ADV.html">ADV</a></tt> (1933; 1% instances).

### `PRON`

19979 <tt><a href="en_gum-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="en_gum-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (16827; 84%), <tt><a href="en_gum-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (14796; 74%), <tt><a href="en_gum-feat-Number.html">Number</a></tt><tt>=Sing</tt> (13316; 67%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (1830; 9% of non-empty `PronType`): <em>there, that, this, those, these</em>
* `Emp` (35; 0% of non-empty `PronType`): <em>itself, themselves, himself, yourself</em>
* `Ind` (311; 2% of non-empty `PronType`): <em>something, anything, someone, anyone, somebody, anybody</em>
* `Int` (298; 1% of non-empty `PronType`): <em>what, who, whatever, which, Whoever, whose</em>
* `Neg` (67; 0% of non-empty `PronType`): <em>nothing, one, nobody</em>
* `Prs` (15367; 77% of non-empty `PronType`): <em>i, it, you, we, they, he, his, your, my, our</em>
* `Rcp` (4; 0% of non-empty `PronType`): <em>one</em>
* `Rel` (1939; 10% of non-empty `PronType`): <em>that, which, what, who, whatever, whom, whose, whoever, Whosoever, wish</em>
* `Tot` (128; 1% of non-empty `PronType`): <em>everything, everyone, everybody, ev</em>
* `EMPTY` (1): <em>t</em>

<table>
  <tr><th>Paradigm <i>one</i></th><th><tt>Prs</tt></th><th><tt>Rcp</tt></th><th><tt>Neg</tt></th></tr>
  <tr><td><tt><tt><a href="en_gum-feat-ExtPos.html">ExtPos</a></tt><tt>=PRON</tt></tt></td><td></td><td><em>one</em></td><td></td></tr>
  <tr><td><tt><tt><a href="en_gum-feat-Number.html">Number</a></tt><tt>=Sing</tt></tt></td><td></td><td></td><td><em>one</em></td></tr>
  <tr><td><tt><tt><a href="en_gum-feat-Number.html">Number</a></tt><tt>=Sing</tt>|<tt><a href="en_gum-feat-Person.html">Person</a></tt><tt>=3</tt></tt></td><td><em>one</em></td><td></td><td></td></tr>
</table>

### `DET`

19057 <tt><a href="en_gum-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="en_gum-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (10947; 57%).

`DET` tokens may have the following values of `PronType`:

* `Art` (15772; 83% of non-empty `PronType`): <em>the, a, an</em>
* `Dem` (1527; 8% of non-empty `PronType`): <em>this, that, these, those, such, yonder</em>
* `Ind` (1064; 6% of non-empty `PronType`): <em>some, all, any, every, another, each, both, half, such, Mat</em>
* `Int` (55; 0% of non-empty `PronType`): <em>which, what, whatever</em>
* `Neg` (200; 1% of non-empty `PronType`): <em>no, neither</em>
* `Rcp` (20; 0% of non-empty `PronType`): <em>each</em>
* `Rel` (19; 0% of non-empty `PronType`): <em>what, whatever</em>
* `Tot` (400; 2% of non-empty `PronType`): <em>all, both, each, every</em>
* `EMPTY` (21): <em>either, quite</em>

<table>
  <tr><th>Paradigm <i>each</i></th><th><tt>Rcp</tt></th><th><tt>Tot</tt></th><th><tt>Ind</tt></th></tr>
  <tr><td><tt>_</tt></td><td></td><td><em>each</em></td><td><em>each</em></td></tr>
  <tr><td><tt><tt><a href="en_gum-feat-ExtPos.html">ExtPos</a></tt><tt>=PRON</tt></tt></td><td><em>each</em></td><td></td><td></td></tr>
</table>

### `ADV`

1933 <tt><a href="en_gum-pos-ADV.html">ADV</a></tt> tokens (17% of all `ADV` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADV` and `PronType` co-occurred: <tt><a href="en_gum-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (1933; 100%).

`ADV` tokens may have the following values of `PronType`:

* `Dem` (824; 43% of non-empty `PronType`): <em>then, here, there</em>
* `Int` (744; 38% of non-empty `PronType`): <em>when, how, why, where, whither, whenever</em>
* `Rel` (365; 19% of non-empty `PronType`): <em>where, how, why, when, wherever, whenever, however, whereby</em>
* `EMPTY` (9259): <em>so, just, also, now, more, really, very, as, even, only</em>

<table>
  <tr><th>Paradigm <i>when</i></th><th><tt>Int</tt></th><th><tt>Rel</tt></th></tr>
  <tr><td><tt></tt></td><td><em>when</em></td><td><em>when</em></td></tr>
</table>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="en_gum-dep-reparandum.html">reparandum</a></tt>]--> PRON</tt> (46; 84%),
<tt>PRON --[<tt><a href="en_gum-dep-conj.html">conj</a></tt>]--> PRON</tt> (29; 88%),
<tt>DET --[<tt><a href="en_gum-dep-reparandum.html">reparandum</a></tt>]--> DET</tt> (27; 96%),
<tt>ADV --[<tt><a href="en_gum-dep-conj.html">conj</a></tt>]--> ADV</tt> (13; 68%),
<tt>PRON --[<tt><a href="en_gum-dep-conj.html">conj</a></tt>]--> ADV</tt> (9; 69%),
<tt>DET --[<tt><a href="en_gum-dep-reparandum.html">reparandum</a></tt>]--> PRON</tt> (6; 100%),
<tt>PRON --[<tt><a href="en_gum-dep-appos.html">appos</a></tt>]--> PRON</tt> (6; 86%),
<tt>DET --[<tt><a href="en_gum-dep-conj.html">conj</a></tt>]--> DET</tt> (5; 63%),
<tt>PRON --[<tt><a href="en_gum-dep-parataxis.html">parataxis</a></tt>]--> PRON</tt> (4; 80%),
<tt>DET --[<tt><a href="en_gum-dep-nmod.html">nmod</a></tt>]--> DET</tt> (2; 100%).

