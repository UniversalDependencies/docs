---
layout: base
title:  'Statistics of NumType in UD_Classical_Armenian-CAVaL'
udver: '2'
---

## Treebank Statistics: UD_Classical_Armenian-CAVaL: Features: `NumType`

This feature is universal.
It occurs with 3 different values: `Card`, `Ord`, `Sets`.

80 tokens (1%) have a non-empty value of `NumType`.
36 types (1%) occur at least once with a non-empty value of `NumType`.
23 lemmas (2%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="xcl_caval-pos-NUM.html">NUM</a></tt> (72; 1% instances), <tt><a href="xcl_caval-pos-ADJ.html">ADJ</a></tt> (8; 0% instances).

### `NUM`

72 <tt><a href="xcl_caval-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="xcl_caval-feat-Number.html">Number</a></tt><tt>=Plur</tt> (45; 63%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (68; 94% of non-empty `NumType`): <em>երկուս, հինգ, մի, երիս, երկոտասան, երկու, եւթն, յիսուն, երից, երկոտասանից</em>
* `Sets` (4; 6% of non-empty `NumType`): <em>երկոքին</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (17) occur only with one value of `NumType`.

### `ADJ`

8 <tt><a href="xcl_caval-pos-ADJ.html">ADJ</a></tt> tokens (2% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="xcl_caval-feat-Number.html">Number</a></tt><tt>=Sing</tt> (7; 88%), <tt><a href="xcl_caval-feat-Case.html">Case</a></tt><tt>=Loc</tt> (6; 75%).

`ADJ` tokens may have the following values of `NumType`:

* `Ord` (8; 100% of non-empty `NumType`): <em>երիր, երկրորդ, երկրորդում, երրորդ, հինգետասաներորդի, ութերորդի, վեցերորդ, վեցերորդի</em>
* `EMPTY` (383): <em>մեծ, բարի, երանի, սուրբ, չար, նոր, սրբով, այլոց, բարձրելոյ, լի</em>

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="xcl_caval-dep-conj.html">conj</a></tt>]--> NUM</tt> (3; 100%),
<tt>NUM --[<tt><a href="xcl_caval-dep-compound-redup.html">compound:redup</a></tt>]--> NUM</tt> (2; 100%),
<tt>NUM --[<tt><a href="xcl_caval-dep-compound.html">compound</a></tt>]--> NUM</tt> (2; 100%),
<tt>NUM --[<tt><a href="xcl_caval-dep-orphan.html">orphan</a></tt>]--> NUM</tt> (2; 100%),
<tt>ADJ --[<tt><a href="xcl_caval-dep-conj.html">conj</a></tt>]--> ADJ</tt> (1; 100%).

