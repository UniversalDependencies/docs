---
layout: base
title:  'Statistics of ExtPos in UD_French-FQB'
udver: '2'
---

## Treebank Statistics: UD_French-FQB: Features: `ExtPos`

This feature is language-specific.
It occurs with 8 different values: `ADJ`, `ADP`, `ADV`, `CCONJ`, `DET`, `NOUN`, `PRON`, `SCONJ`.

89 tokens (0%) have a non-empty value of `ExtPos`.
29 types (1%) occur at least once with a non-empty value of `ExtPos`.
42 lemmas (1%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 6 part-of-speech tags: <tt><a href="fr_fqb-pos-ADP.html">ADP</a></tt> (43; 0% instances), <tt><a href="fr_fqb-pos-ADV.html">ADV</a></tt> (26; 0% instances), <tt><a href="fr_fqb-pos-DET.html">DET</a></tt> (16; 0% instances), <tt><a href="fr_fqb-pos-PRON.html">PRON</a></tt> (2; 0% instances), <tt><a href="fr_fqb-pos-CCONJ.html">CCONJ</a></tt> (1; 0% instances), <tt><a href="fr_fqb-pos-NOUN.html">NOUN</a></tt> (1; 0% instances).

### `ADP`

43 <tt><a href="fr_fqb-pos-ADP.html">ADP</a></tt> tokens (1% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADJ` (1; 2% of non-empty `ExtPos`): <em>d'</em>
* `ADP` (27; 63% of non-empty `ExtPos`): <em>à, en, Par, d'</em>
* `ADV` (6; 14% of non-empty `ExtPos`): <em>en, pour</em>
* `DET` (7; 16% of non-empty `ExtPos`): <em>de, d'</em>
* `SCONJ` (2; 5% of non-empty `ExtPos`): <em>pour</em>

`ExtPos` seems to be **lexical feature** of `ADP`. 100% lemmas (20) occur only with one value of `ExtPos`.

### `ADV`

26 <tt><a href="fr_fqb-pos-ADV.html">ADV</a></tt> tokens (3% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="fr_fqb-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (26; 100%).

`ADV` tokens may have the following values of `ExtPos`:

* `ADP` (15; 58% of non-empty `ExtPos`): <em>lord, près, auprès, afin, autour</em>
* `ADV` (2; 8% of non-empty `ExtPos`): <em>aujourd', ne</em>
* `NOUN` (1; 4% of non-empty `ExtPos`): <em>trop</em>
* `PRON` (2; 8% of non-empty `ExtPos`): <em>moins, plus</em>
* `SCONJ` (6; 23% of non-empty `ExtPos`): <em>afin, alors, après, avant, dès</em>

`ExtPos` seems to be **lexical feature** of `ADV`. 100% lemmas (15) occur only with one value of `ExtPos`.

### `DET`

16 <tt><a href="fr_fqb-pos-DET.html">DET</a></tt> tokens (0% of all `DET` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `DET` and `ExtPos` co-occurred: <tt><a href="fr_fqb-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (15; 94%), <tt><a href="fr_fqb-feat-Number.html">Number</a></tt><tt>=Sing</tt> (15; 94%), <tt><a href="fr_fqb-feat-PronType.html">PronType</a></tt><tt>=Art</tt> (15; 94%), <tt><a href="fr_fqb-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (14; 88%).

`DET` tokens may have the following values of `ExtPos`:

* `PRON` (16; 100% of non-empty `ExtPos`): <em>le, quelqu', un</em>

### `PRON`

2 <tt><a href="fr_fqb-pos-PRON.html">PRON</a></tt> tokens (0% of all `PRON` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PRON` and `ExtPos` co-occurred: <tt><a href="fr_fqb-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (2; 100%), <tt><a href="fr_fqb-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (2; 100%), <tt><a href="fr_fqb-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (2; 100%), <tt><a href="fr_fqb-feat-PronType.html">PronType</a></tt><tt>=Prs</tt> (2; 100%).

`PRON` tokens may have the following values of `ExtPos`:

* `ADP` (1; 50% of non-empty `ExtPos`): <em>Il</em>
* `CCONJ` (1; 50% of non-empty `ExtPos`): <em>y</em>

### `CCONJ`

1 <tt><a href="fr_fqb-pos-CCONJ.html">CCONJ</a></tt> tokens (1% of all `CCONJ` tokens) have a non-empty value of `ExtPos`.

`CCONJ` tokens may have the following values of `ExtPos`:

* `CCONJ` (1; 100% of non-empty `ExtPos`): <em>et</em>

### `NOUN`

1 <tt><a href="fr_fqb-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `NOUN` and `ExtPos` co-occurred: <tt><a href="fr_fqb-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (1; 100%), <tt><a href="fr_fqb-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (1; 100%).

`NOUN` tokens may have the following values of `ExtPos`:

* `ADP` (1; 100% of non-empty `ExtPos`): <em>suite</em>

