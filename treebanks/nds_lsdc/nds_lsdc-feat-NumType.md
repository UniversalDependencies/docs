---
layout: base
title:  'Statistics of NumType in UD_Low_Saxon-LSDC'
udver: '2'
---

## Treebank Statistics: UD_Low_Saxon-LSDC: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

11 tokens (0%) have a non-empty value of `NumType`.
8 types (1%) occur at least once with a non-empty value of `NumType`.
7 lemmas (1%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="nds_lsdc-pos-NUM.html">NUM</a></tt> (8; 0% instances), <tt><a href="nds_lsdc-pos-ADJ.html">ADJ</a></tt> (3; 0% instances).

### `NUM`

8 <tt><a href="nds_lsdc-pos-NUM.html">NUM</a></tt> tokens (36% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="nds_lsdc-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (7; 88%), <tt><a href="nds_lsdc-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (7; 88%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (8; 100% of non-empty `NumType`): <em>eyn, dree, acht, twey, vyv</em>
* `EMPTY` (14): <em>eynen, tein, 10, 14, 1817, dörtig, eyn, eyne, hunderd, twey</em>

### `ADJ`

3 <tt><a href="nds_lsdc-pos-ADJ.html">ADJ</a></tt> tokens (1% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="nds_lsdc-feat-Degree.html">Degree</a></tt><tt>=Pos</tt> (2; 67%), <tt><a href="nds_lsdc-feat-Number.html">Number</a></tt><tt>=Sing</tt> (2; 67%).

`ADJ` tokens may have the following values of `NumType`:

* `Ord` (3; 100% of non-empty `NumType`): <em>eyrste, eyrsten, tweyde</em>
* `EMPTY` (262): <em>good, ander, anderen, leve, olden, veal, byster, eyrste, goden, grout</em>

