---
layout: base
title:  'Statistics of PronType in UD_Persian-PerDT'
udver: '2'
---

## Treebank Statistics: UD_Persian-PerDT: Features: `PronType`

This feature is universal.
It occurs with 1 different values: `Prs`.

5697 tokens (1%) have a non-empty value of `PronType`.
15 types (0%) occur at least once with a non-empty value of `PronType`.
6 lemmas (0%) occur at least once with a non-empty value of `PronType`.
The feature is used with 1 part-of-speech tags: <tt><a href="fa_perdt-pos-PRON.html">PRON</a></tt> (5697; 1% instances).

### `PRON`

5697 <tt><a href="fa_perdt-pos-PRON.html">PRON</a></tt> tokens (24% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="fa_perdt-feat-Number.html">Number</a></tt><tt>=Sing</tt> (4050; 71%).

`PRON` tokens may have the following values of `PronType`:

* `Prs` (5697; 100% of non-empty `PronType`): ش، م، شان، تان، مان، ت، اش، ام، یش، ات
* `EMPTY` (18443): خود، او، آن، ما، من، شما، آنها، این، تو، هم

