---
layout: base
title:  'Statistics of ExtPos in UD_Finnish-FTB'
udver: '2'
---

## Treebank Statistics: UD_Finnish-FTB: Features: `ExtPos`

This feature is language-specific.
It occurs with 8 different values: `ADJ`, `ADP`, `ADV`, `CCONJ`, `DET`, `INTJ`, `PRON`, `SCONJ`.

395 tokens (0%) have a non-empty value of `ExtPos`.
115 types (0%) occur at least once with a non-empty value of `ExtPos`.
86 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 12 part-of-speech tags: <tt><a href="fi_ftb-pos-ADV.html">ADV</a></tt> (220; 0% instances), <tt><a href="fi_ftb-pos-PRON.html">PRON</a></tt> (54; 0% instances), <tt><a href="fi_ftb-pos-PART.html">PART</a></tt> (43; 0% instances), <tt><a href="fi_ftb-pos-NOUN.html">NOUN</a></tt> (40; 0% instances), <tt><a href="fi_ftb-pos-SCONJ.html">SCONJ</a></tt> (10; 0% instances), <tt><a href="fi_ftb-pos-VERB.html">VERB</a></tt> (9; 0% instances), <tt><a href="fi_ftb-pos-INTJ.html">INTJ</a></tt> (6; 0% instances), <tt><a href="fi_ftb-pos-ADP.html">ADP</a></tt> (5; 0% instances), <tt><a href="fi_ftb-pos-CCONJ.html">CCONJ</a></tt> (4; 0% instances), <tt><a href="fi_ftb-pos-ADJ.html">ADJ</a></tt> (2; 0% instances), <tt><a href="fi_ftb-pos-AUX.html">AUX</a></tt> (1; 0% instances), <tt><a href="fi_ftb-pos-NUM.html">NUM</a></tt> (1; 0% instances).

### `ADV`

220 <tt><a href="fi_ftb-pos-ADV.html">ADV</a></tt> tokens (2% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="fi_ftb-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (122; 55%).

`ADV` tokens may have the following values of `ExtPos`:

* `ADJ` (4; 2% of non-empty `ExtPos`): <em>niin, itsestään</em>
* `ADV` (48; 22% of non-empty `ExtPos`): <em>ikään, niin, silloin, näin, enemmän, ennen, pitkin, taas, vähän, ylös</em>
* `CCONJ` (1; 0% of non-empty `ExtPos`): <em>mukaan</em>
* `INTJ` (2; 1% of non-empty `ExtPos`): <em>Johan, Jopas</em>
* `SCONJ` (165; 75% of non-empty `ExtPos`): <em>ennen, niin, ilman, paitsi, vasta, silloin, aivan, ihan, ikään, siksi</em>

<table>
  <tr><th>Paradigm <i>niin</i></th><th><tt>ADJ</tt></th><th><tt>ADV</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt>_</tt></td><td></td><td><em>niin</em></td><td><em>niin</em></td></tr>
  <tr><td><tt><tt><a href="fi_ftb-feat-PronType.html">PronType</a></tt><tt>=Dem</tt></tt></td><td><em>niin</em></td><td><em>niin</em></td><td><em>niin</em></td></tr>
</table>

### `PRON`

54 <tt><a href="fi_ftb-pos-PRON.html">PRON</a></tt> tokens (0% of all `PRON` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PRON` and `ExtPos` co-occurred: <tt><a href="fi_ftb-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (54; 100%), <tt><a href="fi_ftb-feat-Number.html">Number</a></tt><tt>=Sing</tt> (51; 94%).

`PRON` tokens may have the following values of `ExtPos`:

* `ADV` (11; 20% of non-empty `ExtPos`): <em>saman, sen, tätä, samaa, tämän</em>
* `DET` (2; 4% of non-empty `ExtPos`): <em>joka</em>
* `PRON` (22; 41% of non-empty `ExtPos`): <em>toinen, itse, Siitä, niitä</em>
* `SCONJ` (19; 35% of non-empty `ExtPos`): <em>sen, sitä, siitä, sillä, siltä</em>

<table>
  <tr><th>Paradigm <i>se</i></th><th><tt>ADV</tt></th><th><tt>PRON</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt><tt><a href="fi_ftb-feat-Case.html">Case</a></tt><tt>=Abl</tt></tt></td><td></td><td></td><td><em>siltä</em></td></tr>
  <tr><td><tt><tt><a href="fi_ftb-feat-Case.html">Case</a></tt><tt>=Ade</tt></tt></td><td></td><td></td><td><em>sillä</em></td></tr>
  <tr><td><tt><tt><a href="fi_ftb-feat-Case.html">Case</a></tt><tt>=Ela</tt></tt></td><td></td><td><em>Siitä</em></td><td><em>siitä</em></td></tr>
  <tr><td><tt><tt><a href="fi_ftb-feat-Case.html">Case</a></tt><tt>=Gen</tt></tt></td><td><em>sen</em></td><td></td><td><em>sen</em></td></tr>
  <tr><td><tt><tt><a href="fi_ftb-feat-Case.html">Case</a></tt><tt>=Par</tt></tt></td><td></td><td></td><td><em>sitä</em></td></tr>
</table>

### `PART`

43 <tt><a href="fi_ftb-pos-PART.html">PART</a></tt> tokens (2% of all `PART` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PART` and `ExtPos` co-occurred: <tt><a href="fi_ftb-feat-Style.html">Style</a></tt><tt>=EMPTY</tt> (36; 84%).

`PART` tokens may have the following values of `ExtPos`:

* `ADV` (24; 56% of non-empty `ExtPos`): <em>totta, aika, sen, hädin, juuri, nipin, Kas, melko, niin, ties</em>
* `INTJ` (7; 16% of non-empty `ExtPos`): <em>tota, Kas</em>
* `SCONJ` (12; 28% of non-empty `ExtPos`): <em>niin, Entä, sen</em>

<table>
  <tr><th>Paradigm <i>niin</i></th><th><tt>ADV</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt>_</tt></td><td><em>niin</em></td><td><em>niin</em></td></tr>
  <tr><td><tt><tt><a href="fi_ftb-feat-Style.html">Style</a></tt><tt>=Coll</tt></tt></td><td><em>Ni</em></td><td></td></tr>
</table>

### `NOUN`

40 <tt><a href="fi_ftb-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `NOUN` and `ExtPos` co-occurred: <tt><a href="fi_ftb-feat-Number.html">Number</a></tt><tt>=Sing</tt> (27; 68%).

`NOUN` tokens may have the following values of `ExtPos`:

* `ADP` (2; 5% of non-empty `ExtPos`): <em>lukuun</em>
* `ADV` (38; 95% of non-empty `ExtPos`): <em>alun, loppujen, iät, vuosi, kerta, kesät, päivä, rinta, yötä, Ihme</em>

`ExtPos` seems to be **lexical feature** of `NOUN`. 100% lemmas (22) occur only with one value of `ExtPos`.

### `SCONJ`

10 <tt><a href="fi_ftb-pos-SCONJ.html">SCONJ</a></tt> tokens (0% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `SCONJ` and `ExtPos` co-occurred: <tt><a href="fi_ftb-feat-Style.html">Style</a></tt><tt>=EMPTY</tt> (9; 90%).

`SCONJ` tokens may have the following values of `ExtPos`:

* `ADV` (1; 10% of non-empty `ExtPos`): <em>ett</em>
* `SCONJ` (9; 90% of non-empty `ExtPos`): <em>kun, jos</em>

### `VERB`

9 <tt><a href="fi_ftb-pos-VERB.html">VERB</a></tt> tokens (0% of all `VERB` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `VERB` and `ExtPos` co-occurred: <tt><a href="fi_ftb-feat-PartForm.html">PartForm</a></tt><tt>=EMPTY</tt> (9; 100%), <tt><a href="fi_ftb-feat-Voice.html">Voice</a></tt><tt>=Act</tt> (9; 100%), <tt><a href="fi_ftb-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (8; 89%), <tt><a href="fi_ftb-feat-InfForm.html">InfForm</a></tt><tt>=EMPTY</tt> (8; 89%), <tt><a href="fi_ftb-feat-Number.html">Number</a></tt><tt>=Sing</tt> (8; 89%), <tt><a href="fi_ftb-feat-VerbForm.html">VerbForm</a></tt><tt>=Fin</tt> (8; 89%), <tt><a href="fi_ftb-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (7; 78%), <tt><a href="fi_ftb-feat-Person.html">Person</a></tt><tt>=3</tt> (7; 78%), <tt><a href="fi_ftb-feat-Tense.html">Tense</a></tt><tt>=EMPTY</tt> (7; 78%).

`VERB` tokens may have the following values of `ExtPos`:

* `ADV` (8; 89% of non-empty `ExtPos`): <em>ei, mennen, tiiät, vuosi</em>
* `INTJ` (1; 11% of non-empty `ExtPos`): <em>ei</em>

<table>
  <tr><th>Paradigm <i>ei</i></th><th><tt>ADV</tt></th><th><tt>INTJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>ei</em></td><td><em>ei</em></td></tr>
</table>

### `INTJ`

6 <tt><a href="fi_ftb-pos-INTJ.html">INTJ</a></tt> tokens (2% of all `INTJ` tokens) have a non-empty value of `ExtPos`.

`INTJ` tokens may have the following values of `ExtPos`:

* `INTJ` (6; 100% of non-empty `ExtPos`): <em>voi, No</em>

### `ADP`

5 <tt><a href="fi_ftb-pos-ADP.html">ADP</a></tt> tokens (0% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADV` (5; 100% of non-empty `ExtPos`): <em>ilman</em>

### `CCONJ`

4 <tt><a href="fi_ftb-pos-CCONJ.html">CCONJ</a></tt> tokens (0% of all `CCONJ` tokens) have a non-empty value of `ExtPos`.

`CCONJ` tokens may have the following values of `ExtPos`:

* `CCONJ` (4; 100% of non-empty `ExtPos`): <em>saati</em>

### `ADJ`

2 <tt><a href="fi_ftb-pos-ADJ.html">ADJ</a></tt> tokens (0% of all `ADJ` tokens) have a non-empty value of `ExtPos`.

`ADJ` tokens may have the following values of `ExtPos`:

* `ADV` (2; 100% of non-empty `ExtPos`): <em>pikku, kumma</em>

### `AUX`

1 <tt><a href="fi_ftb-pos-AUX.html">AUX</a></tt> tokens (0% of all `AUX` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `AUX` and `ExtPos` co-occurred: <tt><a href="fi_ftb-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (1; 100%), <tt><a href="fi_ftb-feat-Number.html">Number</a></tt><tt>=Sing</tt> (1; 100%), <tt><a href="fi_ftb-feat-Person.html">Person</a></tt><tt>=3</tt> (1; 100%), <tt><a href="fi_ftb-feat-Polarity.html">Polarity</a></tt><tt>=Neg</tt> (1; 100%), <tt><a href="fi_ftb-feat-Tense.html">Tense</a></tt><tt>=EMPTY</tt> (1; 100%), <tt><a href="fi_ftb-feat-VerbForm.html">VerbForm</a></tt><tt>=Fin</tt> (1; 100%), <tt><a href="fi_ftb-feat-Voice.html">Voice</a></tt><tt>=Act</tt> (1; 100%).

`AUX` tokens may have the following values of `ExtPos`:

* `CCONJ` (1; 100% of non-empty `ExtPos`): <em>ei</em>

### `NUM`

1 <tt><a href="fi_ftb-pos-NUM.html">NUM</a></tt> tokens (0% of all `NUM` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `NUM` and `ExtPos` co-occurred: <tt><a href="fi_ftb-feat-Case.html">Case</a></tt><tt>=Nom</tt> (1; 100%), <tt><a href="fi_ftb-feat-NumType.html">NumType</a></tt><tt>=Card</tt> (1; 100%), <tt><a href="fi_ftb-feat-Number.html">Number</a></tt><tt>=Sing</tt> (1; 100%).

`NUM` tokens may have the following values of `ExtPos`:

* `ADV` (1; 100% of non-empty `ExtPos`): <em>yksi</em>

