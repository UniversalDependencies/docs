---
layout: base
title:  'Statistics of Voice in UD_English-GENTLE'
udver: '2'
---

## Treebank Statistics: UD_English-GENTLE: Features: `Voice`

This feature is universal.
It occurs with 1 different values: `Pass`.

110 tokens (1%) have a non-empty value of `Voice`.
80 types (2%) occur at least once with a non-empty value of `Voice`.
80 lemmas (3%) occur at least once with a non-empty value of `Voice`.
The feature is used with 1 part-of-speech tags: <tt><a href="en_gentle-pos-VERB.html">VERB</a></tt> (110; 1% instances).

### `VERB`

110 <tt><a href="en_gentle-pos-VERB.html">VERB</a></tt> tokens (7% of all `VERB` tokens) have a non-empty value of `Voice`.

The most frequent other feature values with which `VERB` and `Voice` co-occurred: <tt><a href="en_gentle-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (110; 100%), <tt><a href="en_gentle-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (110; 100%), <tt><a href="en_gentle-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (110; 100%), <tt><a href="en_gentle-feat-Tense.html">Tense</a></tt><tt>=Past</tt> (110; 100%), <tt><a href="en_gentle-feat-VerbForm.html">VerbForm</a></tt><tt>=Part</tt> (110; 100%).

`VERB` tokens may have the following values of `Voice`:

* `Pass` (110; 100% of non-empty `Voice`): <em>applied, assigned, connected, given, colored, done, expected, noted, allowed, caught</em>
* `EMPTY` (1553): <em>have, gon, has, see, let, do, get, make, take, know</em>

`Voice` seems to be **lexical feature** of `VERB`. 100% lemmas (80) occur only with one value of `Voice`.

