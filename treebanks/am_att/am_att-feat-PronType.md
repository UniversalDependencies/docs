---
layout: base
title:  'Statistics of PronType in UD_Amharic-ATT'
udver: '2'
---

## Treebank Statistics: UD_Amharic-ATT: Features: `PronType`

This feature is universal but the values `Exc` are language-specific.
It occurs with 9 different values: `Art`, `Dem`, `Exc`, `Ind`, `Int`, `Neg`, `Prs`, `Rcp`, `Tot`.

3032 tokens (30%) have a non-empty value of `PronType`.
84 types (6%) occur at least once with a non-empty value of `PronType`.
48 lemmas (3%) occur at least once with a non-empty value of `PronType`.
The feature is used with 2 part-of-speech tags: <tt><a href="am_att-pos-PRON.html">PRON</a></tt> (2550; 25% instances), <tt><a href="am_att-pos-DET.html">DET</a></tt> (482; 5% instances).

### `PRON`

2550 <tt><a href="am_att-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="am_att-feat-Number.html">Number</a></tt><tt>=Sing</tt> (2191; 86%), <tt><a href="am_att-feat-Person.html">Person</a></tt><tt>=3</tt> (1858; 73%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (1; 0% of non-empty `PronType`): እንግዲያ
* `Exc` (6; 0% of non-empty `PronType`): ምነው
* `Ind` (6; 0% of non-empty `PronType`): ማንም, የትም
* `Int` (88; 3% of non-empty `PronType`): ምን, ማን, የት, መቼ, እንዴት, ምንድን, የትኛ, ምንድ, ለምን, መች
* `Neg` (5; 0% of non-empty `PronType`): ምንም, ምን
* `Prs` (2442; 96% of non-empty `PronType`): ኧ, ይ, ኡ, ት, ኧው, ኧች, ው, ኦ, ኝ, ኣት
* `Rcp` (2; 0% of non-empty `PronType`): እርስ

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (30) occur only with one value of `PronType`.

### `DET`

482 <tt><a href="am_att-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

`DET` tokens may have the following values of `PronType`:

* `Art` (414; 86% of non-empty `PronType`): ኡ, ው, ዋ, ዬ_ው, ዬ
* `Dem` (56; 12% of non-empty `PronType`): ይህ, እዚህ, ይህን, እዚያ, ያ, ይቺ, እንዲህ, ይች, ይኸው
* `Exc` (1; 0% of non-empty `PronType`): ምነው
* `Ind` (1; 0% of non-empty `PronType`): ብዙ
* `Int` (1; 0% of non-empty `PronType`): እነ
* `Prs` (6; 1% of non-empty `PronType`): ኤ, ዋን, ውን
* `Tot` (3; 1% of non-empty `PronType`): እየ

`PronType` seems to be **lexical feature** of `DET`. 100% lemmas (21) occur only with one value of `PronType`.

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="am_att-dep-conj.html">conj</a></tt>]--> PRON</tt> (2; 100%),
<tt>PRON --[<tt><a href="am_att-dep-det.html">det</a></tt>]--> PRON</tt> (1; 100%),
<tt>PRON --[<tt><a href="am_att-dep-nmod.html">nmod</a></tt>]--> PRON</tt> (1; 100%).

