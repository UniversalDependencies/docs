---
layout: base
title:  'Statistics of ExtPos in UD_Georgian-GNC'
udver: '2'
---

## Treebank Statistics: UD_Georgian-GNC: Features: `ExtPos`

This feature is language-specific.
It occurs with 3 different values: `ADV`, `INTJ`, `SCONJ`.

39 tokens (0%) have a non-empty value of `ExtPos`.
11 types (0%) occur at least once with a non-empty value of `ExtPos`.
10 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 5 part-of-speech tags: <tt><a href="ka_gnc-pos-ADV.html">ADV</a></tt> (11; 0% instances), <tt><a href="ka_gnc-pos-CCONJ.html">CCONJ</a></tt> (11; 0% instances), <tt><a href="ka_gnc-pos-PRON.html">PRON</a></tt> (10; 0% instances), <tt><a href="ka_gnc-pos-SCONJ.html">SCONJ</a></tt> (6; 0% instances), <tt><a href="ka_gnc-pos-INTJ.html">INTJ</a></tt> (1; 0% instances).

### `ADV`

11 <tt><a href="ka_gnc-pos-ADV.html">ADV</a></tt> tokens (1% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="ka_gnc-feat-AdvType.html">AdvType</a></tt><tt>=EMPTY</tt> (11; 100%).

`ADV` tokens may have the following values of `ExtPos`:

* `ADV` (11; 100% of non-empty `ExtPos`): <em>ჯერ, ესე, არც</em>

### `CCONJ`

11 <tt><a href="ka_gnc-pos-CCONJ.html">CCONJ</a></tt> tokens (1% of all `CCONJ` tokens) have a non-empty value of `ExtPos`.

`CCONJ` tokens may have the following values of `ExtPos`:

* `SCONJ` (11; 100% of non-empty `ExtPos`): <em>თუ</em>

### `PRON`

10 <tt><a href="ka_gnc-pos-PRON.html">PRON</a></tt> tokens (1% of all `PRON` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PRON` and `ExtPos` co-occurred: <tt><a href="ka_gnc-feat-Case.html">Case</a></tt><tt>=Nom</tt> (10; 100%), <tt><a href="ka_gnc-feat-Number-psor.html">Number[psor]</a></tt><tt>=EMPTY</tt> (10; 100%), <tt><a href="ka_gnc-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (10; 100%), <tt><a href="ka_gnc-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (10; 100%), <tt><a href="ka_gnc-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (7; 70%).

`PRON` tokens may have the following values of `ExtPos`:

* `ADV` (7; 70% of non-empty `ExtPos`): <em>რა, ის, ეს</em>
* `SCONJ` (3; 30% of non-empty `ExtPos`): <em>რაც</em>

<table>
  <tr><th>Paradigm <i>რა</i></th><th><tt>ADV</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>რა</em></td><td></td></tr>
  <tr><td><tt><tt><a href="ka_gnc-feat-PronType.html">PronType</a></tt><tt>=Rel</tt></tt></td><td></td><td><em>რაც</em></td></tr>
</table>

### `SCONJ`

6 <tt><a href="ka_gnc-pos-SCONJ.html">SCONJ</a></tt> tokens (2% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

`SCONJ` tokens may have the following values of `ExtPos`:

* `SCONJ` (6; 100% of non-empty `ExtPos`): <em>როგორც, თუმცა</em>

### `INTJ`

1 <tt><a href="ka_gnc-pos-INTJ.html">INTJ</a></tt> tokens (4% of all `INTJ` tokens) have a non-empty value of `ExtPos`.

`INTJ` tokens may have the following values of `ExtPos`:

* `INTJ` (1; 100% of non-empty `ExtPos`): <em>კარგი</em>

