---
layout: base
title:  'Statistics of NumType in UD_Brahui-Kholum'
udver: '2'
---

## Treebank Statistics: UD_Brahui-Kholum: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

28 tokens (3%) have a non-empty value of `NumType`.
15 types (3%) occur at least once with a non-empty value of `NumType`.
14 lemmas (4%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="brh_kholum-pos-NUM.html">NUM</a></tt> (28; 3% instances).

### `NUM`

28 <tt><a href="brh_kholum-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (26; 93% of non-empty `NumType`): <em>asi, musi, 13, 2020, Asiṭ, irā, اسہ, دو, سدآ, مُسہ</em>
* `Ord` (2; 7% of non-empty `NumType`): <em>musiṭṭamīko</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (14) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="brh_kholum-dep-compound.html">compound</a></tt>]--> NUM</tt> (1; 100%).

