---
layout: base
title:  'Statistics of ExtPos in UD_Hebrew-IAHLTknesset'
udver: '2'
---

## Treebank Statistics: UD_Hebrew-IAHLTknesset: Features: `ExtPos`

This feature is language-specific.
It occurs with 5 different values: `ADP`, `ADV`, `CCONJ`, `DET`, `SCONJ`.

446 tokens (1%) have a non-empty value of `ExtPos`.
66 types (1%) occur at least once with a non-empty value of `ExtPos`.
66 lemmas (1%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 8 part-of-speech tags: <tt><a href="he_iahltknesset-pos-ADP.html">ADP</a></tt> (243; 0% instances), <tt><a href="he_iahltknesset-pos-ADV.html">ADV</a></tt> (86; 0% instances), <tt><a href="he_iahltknesset-pos-SCONJ.html">SCONJ</a></tt> (70; 0% instances), <tt><a href="he_iahltknesset-pos-DET.html">DET</a></tt> (35; 0% instances), <tt><a href="he_iahltknesset-pos-PRON.html">PRON</a></tt> (5; 0% instances), <tt><a href="he_iahltknesset-pos-CCONJ.html">CCONJ</a></tt> (4; 0% instances), <tt><a href="he_iahltknesset-pos-NOUN.html">NOUN</a></tt> (2; 0% instances), <tt><a href="he_iahltknesset-pos-ADJ.html">ADJ</a></tt> (1; 0% instances).

### `ADP`

243 <tt><a href="he_iahltknesset-pos-ADP.html">ADP</a></tt> tokens (3% of all `ADP` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADP` and `ExtPos` co-occurred: <tt><a href="he_iahltknesset-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (243; 100%), <tt><a href="he_iahltknesset-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (243; 100%), <tt><a href="he_iahltknesset-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (243; 100%).

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (146; 60% of non-empty `ExtPos`): על, ב, מעבר, תוך, עד, מחוץ, אחר, בניגוד, מ, קודם
* `SCONJ` (97; 40% of non-empty `ExtPos`): כדי, עד, בגלל, כמו, למרות, אחרי, לפני, על, בזמן, בעוד

<table>
  <tr><th>Paradigm <i>על</i></th><th><tt>ADP</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td>על</td><td>על</td></tr>
</table>

### `ADV`

86 <tt><a href="he_iahltknesset-pos-ADV.html">ADV</a></tt> tokens (2% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="he_iahltknesset-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (84; 98%).

`ADV` tokens may have the following values of `ExtPos`:

* `ADP` (29; 34% of non-empty `ExtPos`): יחד, אף, מסביב, ביחד, פחות
* `ADV` (50; 58% of non-empty `ExtPos`): יותר, קודם, ב, למעלה, סוף, אט, עוד, פחות, אף, בוודאי
* `SCONJ` (7; 8% of non-empty `ExtPos`): כך, בייחוד, מה, היכן

<table>
  <tr><th>Paradigm <i>פחות</i></th><th><tt>ADP</tt></th><th><tt>ADV</tt></th></tr>
  <tr><td><tt></tt></td><td>פחות</td><td>פחות</td></tr>
</table>

### `SCONJ`

70 <tt><a href="he_iahltknesset-pos-SCONJ.html">SCONJ</a></tt> tokens (2% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

`SCONJ` tokens may have the following values of `ExtPos`:

* `SCONJ` (70; 100% of non-empty `ExtPos`): כפי, משום, ככל, מכיוון, היות, אם, כיוון, מאחר, בהינתן, בזמן

`ExtPos` seems to be **lexical feature** of `SCONJ`. 100% lemmas (13) occur only with one value of `ExtPos`.

### `DET`

35 <tt><a href="he_iahltknesset-pos-DET.html">DET</a></tt> tokens (1% of all `DET` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `DET` and `ExtPos` co-occurred: <tt><a href="he_iahltknesset-feat-Definite.html">Definite</a></tt><tt>=Cons</tt> (34; 97%), <tt><a href="he_iahltknesset-feat-PronType.html">PronType</a></tt><tt>=Tot</tt> (33; 94%).

`DET` tokens may have the following values of `ExtPos`:

* `ADV` (30; 86% of non-empty `ExtPos`): כל
* `SCONJ` (5; 14% of non-empty `ExtPos`): כל, כמה

<table>
  <tr><th>Paradigm <i>כול</i></th><th><tt>ADV</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td>כל</td><td>כל</td></tr>
</table>

### `PRON`

5 <tt><a href="he_iahltknesset-pos-PRON.html">PRON</a></tt> tokens (0% of all `PRON` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PRON` and `ExtPos` co-occurred: <tt><a href="he_iahltknesset-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (5; 100%), <tt><a href="he_iahltknesset-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (5; 100%), <tt><a href="he_iahltknesset-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (5; 100%), <tt><a href="he_iahltknesset-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (5; 100%), <tt><a href="he_iahltknesset-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (5; 100%), <tt><a href="he_iahltknesset-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (3; 60%), <tt><a href="he_iahltknesset-feat-PronType.html">PronType</a></tt><tt>=Rel</tt> (3; 60%).

`PRON` tokens may have the following values of `ExtPos`:

* `DET` (3; 60% of non-empty `ExtPos`): איזה
* `SCONJ` (2; 40% of non-empty `ExtPos`): מה

### `CCONJ`

4 <tt><a href="he_iahltknesset-pos-CCONJ.html">CCONJ</a></tt> tokens (0% of all `CCONJ` tokens) have a non-empty value of `ExtPos`.

`CCONJ` tokens may have the following values of `ExtPos`:

* `CCONJ` (2; 50% of non-empty `ExtPos`): ו
* `SCONJ` (2; 50% of non-empty `ExtPos`): אף

### `NOUN`

2 <tt><a href="he_iahltknesset-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `NOUN` and `ExtPos` co-occurred: <tt><a href="he_iahltknesset-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (2; 100%), <tt><a href="he_iahltknesset-feat-Gender.html">Gender</a></tt><tt>=Fem</tt> (2; 100%), <tt><a href="he_iahltknesset-feat-Number.html">Number</a></tt><tt>=Sing</tt> (2; 100%).

`NOUN` tokens may have the following values of `ExtPos`:

* `ADV` (2; 100% of non-empty `ExtPos`): דרך

### `ADJ`

1 <tt><a href="he_iahltknesset-pos-ADJ.html">ADJ</a></tt> tokens (0% of all `ADJ` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADJ` and `ExtPos` co-occurred: <tt><a href="he_iahltknesset-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (1; 100%), <tt><a href="he_iahltknesset-feat-Number.html">Number</a></tt><tt>=Sing</tt> (1; 100%).

`ADJ` tokens may have the following values of `ExtPos`:

* `ADP` (1; 100% of non-empty `ExtPos`): נכון

