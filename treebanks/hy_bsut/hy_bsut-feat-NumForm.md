---
layout: base
title:  'Statistics of NumForm in UD_Armenian-BSUT'
udver: '2'
---

## Treebank Statistics: UD_Armenian-BSUT: Features: `NumForm`

This feature is language-specific.
It occurs with 2 different values: `Digit`, `Word`.

27 tokens (1%) have a non-empty value of `NumForm`.
18 types (1%) occur at least once with a non-empty value of `NumForm`.
17 lemmas (2%) occur at least once with a non-empty value of `NumForm`.
The feature is used with 2 part-of-speech tags: <tt><a href="hy_bsut-pos-NUM.html">NUM</a></tt> (20; 1% instances), <tt><a href="hy_bsut-pos-ADJ.html">ADJ</a></tt> (7; 0% instances).

### `NUM`

20 <tt><a href="hy_bsut-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumForm`.

The most frequent other feature values with which `NUM` and `NumForm` co-occurred: <tt><a href="hy_bsut-feat-NumType.html">NumType</a></tt><tt>=Card</tt> (20; 100%).

`NUM` tokens may have the following values of `NumForm`:

* `Digit` (13; 65% of non-empty `NumForm`): <em>2008, 9, 1, 10, 12, 16, 1991, 1998, 2000, 2020</em>
* `Word` (7; 35% of non-empty `NumForm`): <em>մի, երկու, մեկ</em>

`NumForm` seems to be **lexical feature** of `NUM`. 100% lemmas (14) occur only with one value of `NumForm`.

### `ADJ`

7 <tt><a href="hy_bsut-pos-ADJ.html">ADJ</a></tt> tokens (3% of all `ADJ` tokens) have a non-empty value of `NumForm`.

The most frequent other feature values with which `ADJ` and `NumForm` co-occurred: <tt><a href="hy_bsut-feat-Degree.html">Degree</a></tt><tt>=Pos</tt> (5; 71%).

`ADJ` tokens may have the following values of `NumForm`:

* `Word` (7; 100% of non-empty `NumForm`): <em>առաջին, երկրորդ, երրորդ</em>

## Relations with Agreement in `NumForm`

The 10 most frequent relations where parent and child node agree in `NumForm`:
<tt>NUM --[<tt><a href="hy_bsut-dep-flat.html">flat</a></tt>]--> NUM</tt> (2; 100%).

