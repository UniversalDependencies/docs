

--------------------------------------------------------------------------------

## Treebank Statistics (UD_Polish)

This feature is universal.
It occurs with 7 different values: `Dem`, `Ind`, `Int`, `Neg`, `Prs`, `Rel`, `Tot`.
Some words have combined values of the feature; 1 combinations have been observed: `Int|Rel`.

6076 tokens (7%) have a non-empty value of `PronType`.
254 types (1%) occur at least once with a non-empty value of `PronType`.
38 lemmas (0%) occur at least once with a non-empty value of `PronType`.
The feature is used with 2 part-of-speech tags: [pl-pos/PRON]() (5023; 6% instances), [pl-pos/DET]() (1053; 1% instances).

### `PRON`

5023 [pl-pos/PRON]() tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="PrepCase.html">PrepCase</a>=EMPTY</tt> (3809; 76%), <tt><a href="Variant.html">Variant</a>=EMPTY</tt> (3527; 70%), <tt><a href="Animacy.html">Animacy</a>=EMPTY</tt> (3449; 69%), <tt><a href="Person.html">Person</a>=EMPTY</tt> (3319; 66%), <tt><a href="Reflex.html">Reflex</a>=EMPTY</tt> (3143; 63%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (463; 9% of non-empty `PronType`): <em>to, tym, tego, temu, ten, takie, takiego, tych, ta, Ci</em>
* `Ind` (141; 3% of non-empty `PronType`): <em>coś, ktoś, czegoś, czymś, kilku, Niektórzy, kogoś, cokolwiek, kilkoma, kilkunastu</em>
* `Int,Rel` (607; 12% of non-empty `PronType`): <em>co, które, który, która, kto, którzy, których, którym, której, czym</em>
* `Neg` (100; 2% of non-empty `PronType`): <em>nic, nikt, niczego, nikogo, nikomu, żaden, niczym, Żadne, żadna, żadnego</em>
* `Prs` (3584; 71% of non-empty `PronType`): <em>się, jego, go, ich, jej, mnie, mu, sobie, mi, ją</em>
* `Tot` (128; 3% of non-empty `PronType`): <em>wszystko, wszystkim, wszyscy, każdy, wszystkiego, wszystkich, każdego, każdym, wszystkie, Każda</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (32) occur only with one value of `PronType`.

### `DET`

1053 [pl-pos/DET]() tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="Degree.html">Degree</a>=Pos</tt> (955; 91%), <tt><a href="Number.html">Number</a>=Sing</tt> (661; 63%), <tt><a href="Gender.html">Gender</a>=Masc</tt> (530; 50%).

`DET` tokens may have the following values of `PronType`:

* `Dem` (682; 65% of non-empty `PronType`): <em>tej, ten, tym, tego, te, tych, to, ta, takie, tę</em>
* `Ind` (179; 17% of non-empty `PronType`): <em>kilka, jakieś, jakiś, kilku, kilkanaście, kilkadziesiąt, jakąś, kilkuset, niektóre, niektórych</em>
* `Int,Rel` (28; 3% of non-empty `PronType`): <em>jakie, jaki, jakich, Jakim, Które, jaka, jakiej, jaką, który, którym</em>
* `Neg` (36; 3% of non-empty `PronType`): <em>żadnych, żadnego, żaden, żadnej, żadne, żadna, żadnym, żadną</em>
* `Tot` (128; 12% of non-empty `PronType`): <em>wszystkie, wszystkich, każdy, wszelki, każde, każdym, wszelkie, każdego, każdą, wszelkich</em>

`PronType` seems to be **lexical feature** of `DET`. 100% lemmas (20) occur only with one value of `PronType`.

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<a href="../dep/conj.html">conj</a>]--> PRON</tt> (6; 100%).

