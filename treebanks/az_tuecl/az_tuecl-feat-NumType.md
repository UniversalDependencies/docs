---
layout: base
title:  'Statistics of NumType in UD_Azerbaijani-TueCL'
udver: '2'
---

## Treebank Statistics: UD_Azerbaijani-TueCL: Features: `NumType`

This feature is universal.
It occurs with 1 different values: `Card`.

4 tokens (0%) have a non-empty value of `NumType`.
4 types (1%) occur at least once with a non-empty value of `NumType`.
4 lemmas (2%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="az_tuecl-pos-NUM.html">NUM</a></tt> (4; 0% instances).

### `NUM`

4 <tt><a href="az_tuecl-pos-NUM.html">NUM</a></tt> tokens (50% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="az_tuecl-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (4; 100%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (4; 100% of non-empty `NumType`): <em>beş, bir, ikki, üç</em>
* `EMPTY` (4): <em>neçə, üç</em>

