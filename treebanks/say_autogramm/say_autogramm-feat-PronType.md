---
layout: base
title:  'Statistics of PronType in UD_Zaar-Autogramm'
udver: '2'
---

## Treebank Statistics: UD_Zaar-Autogramm: Features: `PronType`

This feature is universal.
It occurs with 6 different values: `Dem`, `Ind`, `Int`, `Prs`, `Rel`, `Tot`.

1543 tokens (9%) have a non-empty value of `PronType`.
162 types (7%) occur at least once with a non-empty value of `PronType`.
100 lemmas (7%) occur at least once with a non-empty value of `PronType`.
The feature is used with 4 part-of-speech tags: <tt><a href="say_autogramm-pos-PRON.html">PRON</a></tt> (1252; 7% instances), <tt><a href="say_autogramm-pos-ADV.html">ADV</a></tt> (155; 1% instances), <tt><a href="say_autogramm-pos-SCONJ.html">SCONJ</a></tt> (71; 0% instances), <tt><a href="say_autogramm-pos-DET.html">DET</a></tt> (65; 0% instances).

### `PRON`

1252 <tt><a href="say_autogramm-pos-PRON.html">PRON</a></tt> tokens (94% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="say_autogramm-feat-Number.html">Number</a></tt><tt>=Sing</tt> (691; 55%), <tt><a href="say_autogramm-feat-Person.html">Person</a></tt><tt>=3</tt> (680; 54%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (64; 5% of non-empty `PronType`): <em>gíː, gín, giní, ginês, ginín</em>
* `Ind` (12; 1% of non-empty `PronType`): <em>wáːni, gón, kóːnaːgyòː</em>
* `Int` (38; 3% of non-empty `PronType`): <em>níː, niː, nîː, núː, nûː, gyòː, kóːnúː, míː, níːwáːni, níːwóneː</em>
* `Prs` (1114; 89% of non-empty `PronType`): <em>=tə, ɣáy, =ʃí, gòs, =mí, =mə, =tə̀, gáy, =kə, yâːn</em>
* `Rel` (18; 1% of non-empty `PronType`): <em>yóːɗan, gyóːɗan, dàːsóːɗa, yáddà, yóːɗaŋ</em>
* `Tot` (6; 0% of non-empty `PronType`): <em>kóːnúː, kóːgón</em>
* `EMPTY` (86): <em>gón, tə, gyaːgón, tə̀, gwàːn, ɣáy, káp, kóːníː, kóːnúː, yàːʃí</em>

<table>
  <tr><th>Paradigm <i>kóːnúː</i></th><th><tt>Int</tt></th><th><tt>Tot</tt></th></tr>
  <tr><td><tt></tt></td><td><em>kóːnúː</em></td><td><em>kóːnúː</em></td></tr>
</table>

`PronType` seems to be **lexical feature** of `PRON`. 99% lemmas (68) occur only with one value of `PronType`.

### `ADV`

155 <tt><a href="say_autogramm-pos-ADV.html">ADV</a></tt> tokens (20% of all `ADV` tokens) have a non-empty value of `PronType`.

`ADV` tokens may have the following values of `PronType`:

* `Dem` (120; 77% of non-empty `PronType`): <em>yáːwón, ɗáni, ɗân, ɗangəní, ɗûːn, ɗúːni, ɗaɗân, ʤǎːn, ɗangənín, ɗaɗáni</em>
* `Int` (26; 17% of non-empty `PronType`): <em>wuriː, wúr, gyóː, téːɗôː, wuríː, yǎː, ìnáː, ɗòː, ɗôː</em>
* `Rel` (9; 6% of non-empty `PronType`): <em>yáddiyóːɗan, yandìyóːɗan, yádda, yáddà, yándiyóːɗan, yə́ddà, ɗa, ɗan</em>
* `EMPTY` (618): <em>séː, sòːséy, ʧík, ɗan, káwây, káwêy, tsə́n, ɣəndá, tá, əndá</em>

`PronType` seems to be **lexical feature** of `ADV`. 100% lemmas (28) occur only with one value of `PronType`.

### `SCONJ`

71 <tt><a href="say_autogramm-pos-SCONJ.html">SCONJ</a></tt> tokens (12% of all `SCONJ` tokens) have a non-empty value of `PronType`.

`SCONJ` tokens may have the following values of `PronType`:

* `Prs` (10; 14% of non-empty `PronType`): <em>yâːn</em>
* `Rel` (61; 86% of non-empty `PronType`): <em>ɗan, ɗa</em>
* `EMPTY` (541): <em>tu, ɗan, yâːn, ín, dón, dùmín, séː, ɗa, dòmín, kàmán</em>

### `DET`

65 <tt><a href="say_autogramm-pos-DET.html">DET</a></tt> tokens (21% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="say_autogramm-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (65; 100%), <tt><a href="say_autogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (65; 100%).

`DET` tokens may have the following values of `PronType`:

* `Dem` (52; 80% of non-empty `PronType`): <em>gíː, gín, giní, ɗányâːlíː, XX</em>
* `Int` (12; 18% of non-empty `PronType`): <em>gyòː, gyôː, wuriː, gyóː, níː</em>
* `Tot` (1; 2% of non-empty `PronType`): <em>káp</em>
* `EMPTY` (250): <em>gyáː, wón, gyaː, gón, gíː, kóː, gyòː, gyǎː, wàːné, gyaːgón</em>

`PronType` seems to be **lexical feature** of `DET`. 100% lemmas (14) occur only with one value of `PronType`.

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="say_autogramm-dep-nmod.html">nmod</a></tt>]--> PRON</tt> (7; 58%),
<tt>PRON --[<tt><a href="say_autogramm-dep-conj.html">conj</a></tt>]--> PRON</tt> (2; 100%).

