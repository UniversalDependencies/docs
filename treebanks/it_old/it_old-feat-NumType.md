---
layout: base
title:  'Statistics of NumType in UD_Italian-Old'
udver: '2'
---

## Treebank Statistics: UD_Italian-Old: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

199 tokens (0%) have a non-empty value of `NumType`.
49 types (1%) occur at least once with a non-empty value of `NumType`.
32 lemmas (1%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="it_old-pos-NUM.html">NUM</a></tt> (133; 0% instances), <tt><a href="it_old-pos-ADJ.html">ADJ</a></tt> (66; 0% instances).

### `NUM`

133 <tt><a href="it_old-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (133; 100% of non-empty `NumType`): <em>due, tre, mille, ambo, cento, sette, diece, quattro, cinque, sei</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (20) occur only with one value of `NumType`.

### `ADJ`

66 <tt><a href="it_old-pos-ADJ.html">ADJ</a></tt> tokens (4% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="it_old-feat-Number.html">Number</a></tt><tt>=Sing</tt> (59; 89%), <tt><a href="it_old-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (44; 67%).

`ADJ` tokens may have the following values of `NumType`:

* `Ord` (66; 100% of non-empty `NumType`): <em>primo, secondo, prime, quarto, quinto, sesto, terzo, ottava, prima, primaio</em>
* `EMPTY` (1549): <em>gran, tal, alto, buon, dolce, grande, lunga, trista, mal, tristo</em>

`NumType` seems to be **lexical feature** of `ADJ`. 100% lemmas (12) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="it_old-dep-flat.html">flat</a></tt>]--> NUM</tt> (3; 100%),
<tt>ADJ --[<tt><a href="it_old-dep-conj.html">conj</a></tt>]--> ADJ</tt> (2; 67%),
<tt>ADJ --[<tt><a href="it_old-dep-advcl.html">advcl</a></tt>]--> ADJ</tt> (1; 100%),
<tt>NUM --[<tt><a href="it_old-dep-flat-redup.html">flat:redup</a></tt>]--> NUM</tt> (1; 100%),
<tt>NUM --[<tt><a href="it_old-dep-nmod.html">nmod</a></tt>]--> NUM</tt> (1; 100%).

