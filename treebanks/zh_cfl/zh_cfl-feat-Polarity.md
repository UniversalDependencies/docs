---
layout: base
title:  'Statistics of Polarity in UD_Chinese-CFL'
udver: '2'
---

## Treebank Statistics: UD_Chinese-CFL: Features: `Polarity`

This feature is universal.
It occurs with 1 different values: `Neg`.

28 tokens (0%) have a non-empty value of `Polarity`.
7 types (0%) occur at least once with a non-empty value of `Polarity`.
7 lemmas (0%) occur at least once with a non-empty value of `Polarity`.
The feature is used with 3 part-of-speech tags: <tt><a href="zh_cfl-pos-VERB.html">VERB</a></tt> (16; 0% instances), <tt><a href="zh_cfl-pos-PART.html">PART</a></tt> (9; 0% instances), <tt><a href="zh_cfl-pos-AUX.html">AUX</a></tt> (3; 0% instances).

### `VERB`

16 <tt><a href="zh_cfl-pos-VERB.html">VERB</a></tt> tokens (1% of all `VERB` tokens) have a non-empty value of `Polarity`.

`VERB` tokens may have the following values of `Polarity`:

* `Neg` (16; 100% of non-empty `Polarity`): 没有、 不好意思、 不见、 不通、 不顾
* `EMPTY` (1311): 到、 去、 有、 是、 看、 说、 让、 来、 见、 吃

### `PART`

9 <tt><a href="zh_cfl-pos-PART.html">PART</a></tt> tokens (2% of all `PART` tokens) have a non-empty value of `Polarity`.

`PART` tokens may have the following values of `Polarity`:

* `Neg` (9; 100% of non-empty `Polarity`): 没
* `EMPTY` (536): 的、 了、 地、 得、 吧、 吗、 呢、 啊、 和、 嗬

### `AUX`

3 <tt><a href="zh_cfl-pos-AUX.html">AUX</a></tt> tokens (1% of all `AUX` tokens) have a non-empty value of `Polarity`.

`AUX` tokens may have the following values of `Polarity`:

* `Neg` (3; 100% of non-empty `Polarity`): 没有、 不得
* `EMPTY` (333): 了、 是、 着、 要、 会、 能、 想、 过、 可以、 应该

## Relations with Agreement in `Polarity`

The 10 most frequent relations where parent and child node agree in `Polarity`:
<tt>VERB --[<tt><a href="zh_cfl-dep-conj.html">conj</a></tt>]--> VERB</tt> (2; 67%).

