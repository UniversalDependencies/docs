---
layout: base
title:  'Statistics of PronType in UD_Malayalam-UFAL'
udver: '2'
---

## Treebank Statistics: UD_Malayalam-UFAL: Features: `PronType`

This feature is universal.
It occurs with 6 different values: `Art`, `Dem`, `Ind`, `Int`, `Prs`, `Tot`.

122 tokens (5%) have a non-empty value of `PronType`.
51 types (3%) occur at least once with a non-empty value of `PronType`.
30 lemmas (2%) occur at least once with a non-empty value of `PronType`.
The feature is used with 2 part-of-speech tags: <tt><a href="ml_ufal-pos-PRON.html">PRON</a></tt> (84; 3% instances), <tt><a href="ml_ufal-pos-DET.html">DET</a></tt> (38; 2% instances).

### `PRON`

84 <tt><a href="ml_ufal-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="ml_ufal-feat-Number.html">Number</a></tt><tt>=Sing</tt> (57; 68%), <tt><a href="ml_ufal-feat-Animacy.html">Animacy</a></tt><tt>=EMPTY</tt> (56; 67%), <tt><a href="ml_ufal-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (54; 64%), <tt><a href="ml_ufal-feat-Case.html">Case</a></tt><tt>=Nom</tt> (48; 57%), <tt><a href="ml_ufal-feat-Deixis.html">Deixis</a></tt><tt>=EMPTY</tt> (43; 51%).

`PRON` tokens may have the following values of `PronType`:

* `Ind` (2; 2% of non-empty `PronType`): <em>ആരായാലഉം, ആരെങ്കിലും</em>
* `Int` (6; 7% of non-empty `PronType`): <em>എന്ത്, ആര്</em>
* `Prs` (76; 90% of non-empty `PronType`): <em>ഇത്, അവൾ, ഞാൻ, അവർ, നിങ്ങൾ, അത്, അവൻ, എന്റെ, അവളുടെ, അവർക്ക്</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (21) occur only with one value of `PronType`.

### `DET`

38 <tt><a href="ml_ufal-pos-DET.html">DET</a></tt> tokens (97% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="ml_ufal-feat-Deixis.html">Deixis</a></tt><tt>=EMPTY</tt> (26; 68%), <tt><a href="ml_ufal-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (22; 58%).

`DET` tokens may have the following values of `PronType`:

* `Art` (16; 42% of non-empty `PronType`): <em>ഒരു, ഒന്നും, ഒരേ</em>
* `Dem` (12; 32% of non-empty `PronType`): <em>ഈ, ആ</em>
* `Ind` (3; 8% of non-empty `PronType`): <em>ഏതാനും, ഓരോ</em>
* `Tot` (7; 18% of non-empty `PronType`): <em>എല്ലാ, എല്ലാം, ഓരോ, ഒക്കെ</em>
* `EMPTY` (1): <em>ദ</em>

<table>
  <tr><th>Paradigm <i>ഓരോ</i></th><th><tt>Tot</tt></th><th><tt>Ind</tt></th></tr>
  <tr><td><tt></tt></td><td><em>ഓരോ</em></td><td><em>ഓരോ</em></td></tr>
</table>

