---
layout: base
title:  'Statistics of NumForm in UD_English-GUM'
udver: '2'
---

## Treebank Statistics: UD_English-GUM: Features: `NumForm`

This feature is language-specific.
It occurs with 3 different values: `Digit`, `Roman`, `Word`.

2695 tokens (2%) have a non-empty value of `NumForm`.
558 types (4%) occur at least once with a non-empty value of `NumForm`.
539 lemmas (5%) occur at least once with a non-empty value of `NumForm`.
The feature is used with 1 part-of-speech tags: <tt><a href="en_gum-pos-NUM.html">NUM</a></tt> (2695; 2% instances).

### `NUM`

2695 <tt><a href="en_gum-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumForm`.

The most frequent other feature values with which `NUM` and `NumForm` co-occurred: <tt><a href="en_gum-feat-NumType.html">NumType</a></tt><tt>=Card</tt> (2695; 100%).

`NUM` tokens may have the following values of `NumForm`:

* `Digit` (1916; 71% of non-empty `NumForm`): <em>1, 2, 3, 6, 15, 4, 5, 10, 24, 17</em>
* `Roman` (15; 1% of non-empty `NumForm`): <em>II, I, XV, XVII</em>
* `Word` (764; 28% of non-empty `NumForm`): <em>one, two, three, four, six, five, million, ten, hundred, eight</em>

`NumForm` seems to be **lexical feature** of `NUM`. 100% lemmas (539) occur only with one value of `NumForm`.

## Relations with Agreement in `NumForm`

The 10 most frequent relations where parent and child node agree in `NumForm`:
<tt>NUM --[<tt><a href="en_gum-dep-nmod-tmod.html">nmod:tmod</a></tt>]--> NUM</tt> (118; 100%),
<tt>NUM --[<tt><a href="en_gum-dep-nmod.html">nmod</a></tt>]--> NUM</tt> (85; 98%),
<tt>NUM --[<tt><a href="en_gum-dep-conj.html">conj</a></tt>]--> NUM</tt> (69; 95%),
<tt>NUM --[<tt><a href="en_gum-dep-parataxis.html">parataxis</a></tt>]--> NUM</tt> (7; 100%),
<tt>NUM --[<tt><a href="en_gum-dep-dep.html">dep</a></tt>]--> NUM</tt> (3; 100%),
<tt>NUM --[<tt><a href="en_gum-dep-goeswith.html">goeswith</a></tt>]--> NUM</tt> (1; 100%),
<tt>NUM --[<tt><a href="en_gum-dep-reparandum.html">reparandum</a></tt>]--> NUM</tt> (1; 100%).

