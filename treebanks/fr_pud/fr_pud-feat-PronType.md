---
layout: base
title:  'Statistics of PronType in UD_French-PUD'
udver: '2'
---

## Treebank Statistics: UD_French-PUD: Features: `PronType`

This feature is universal.
It occurs with 1 different values: `Prs`.

278 tokens (1%) have a non-empty value of `PronType`.
23 types (0%) occur at least once with a non-empty value of `PronType`.
1 lemmas (25%) occur at least once with a non-empty value of `PronType`.
The feature is used with 2 part-of-speech tags: <tt><a href="fr_pud-pos-DET.html">DET</a></tt> (276; 1% instances), <tt><a href="fr_pud-pos-PRON.html">PRON</a></tt> (2; 0% instances).

### `DET`

276 <tt><a href="fr_pud-pos-DET.html">DET</a></tt> tokens (7% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="fr_pud-feat-Number.html">Number</a></tt><tt>=Sing</tt> (201; 73%), <tt><a href="fr_pud-feat-Gender.html">Gender</a></tt><tt>=Fem</tt> (146; 53%).

`DET` tokens may have the following values of `PronType`:

* `Prs` (276; 100% of non-empty `PronType`): <em>son, sa, ses, leur, leurs, notre, nos, mon, votre, ma</em>
* `EMPTY` (3612): <em>le, la, les, l', un, une, des, l’, cette, de</em>

### `PRON`

2 <tt><a href="fr_pud-pos-PRON.html">PRON</a></tt> tokens (0% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="fr_pud-feat-Number.html">Number</a></tt><tt>=Sing</tt> (2; 100%).

`PRON` tokens may have the following values of `PronType`:

* `Prs` (2; 100% of non-empty `PronType`): <em>My, sienne</em>
* `EMPTY` (1055): <em>il, qui, se, elle, ils, y, s', on, je, ce</em>

