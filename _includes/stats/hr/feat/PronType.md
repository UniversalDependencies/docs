

--------------------------------------------------------------------------------

## Treebank Statistics (UD_Croatian)

This feature is universal.
It occurs with 3 different values: `Dem`, `Ind`, `Prs`.

2875 tokens (3%) have a non-empty value of `PronType`.
195 types (1%) occur at least once with a non-empty value of `PronType`.
29 lemmas (0%) occur at least once with a non-empty value of `PronType`.
The feature is used with 1 part-of-speech tags: [hr-pos/PRON]() (2875; 3% instances).

### `PRON`

2875 [hr-pos/PRON]() tokens (62% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="Reflex.html">Reflex</a>=EMPTY</tt> (2875; 100%), <tt><a href="Animacy.html">Animacy</a>=EMPTY</tt> (2442; 85%), <tt><a href="Person.html">Person</a>=EMPTY</tt> (2180; 76%), <tt><a href="Number.html">Number</a>=Sing</tt> (1692; 59%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (926; 32% of non-empty `PronType`): <em>to, ove, toga, ovog, tom, te, taj, tome, tog, ovaj</em>
* `Ind` (1612; 56% of non-empty `PronType`): <em>koji, što, koje, koja, kojima, koju, kojem, neki, kojeg, kojim</em>
* `Prs` (337; 12% of non-empty `PronType`): <em>ga, on, ih, mi, im, oni, ona, njih, je, njega</em>
* `EMPTY` (1749): <em>se, svoje, svoj, svoju, njegova, svojeg, svojih, svojim, njegov, svojem</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (29) occur only with one value of `PronType`.

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<a href="../dep/conj.html">conj</a>]--> PRON</tt> (3; 100%),
<tt>PRON --[<a href="../dep/mark.html">mark</a>]--> PRON</tt> (2; 67%).

