---
layout: base
title:  'Statistics of PronType in UD_Old_Occitan-CorAG'
udver: '2'
---

## Treebank Statistics: UD_Old_Occitan-CorAG: Features: `PronType`

This feature is universal.
It occurs with 6 different values: `Dem`, `Ind`, `Int`, `Neg`, `Prs`, `Rel`.

2839 tokens (6%) have a non-empty value of `PronType`.
222 types (4%) occur at least once with a non-empty value of `PronType`.
1 lemmas (0) occur at least once with a non-empty value of `PronType`.
The feature is used with 1 part-of-speech tags: <tt><a href="pro_corag-pos-PRON.html">PRON</a></tt> (2839; 6% instances).

### `PRON`

2839 <tt><a href="pro_corag-pos-PRON.html">PRON</a></tt> tokens (81% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="pro_corag-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (1582; 56%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (368; 13% of non-empty `PronType`): <em>so, ac, asso, aqueg, aquero, a-, aqued, aquegs, a, aquetz</em>
* `Ind` (265; 9% of non-empty `PronType`): <em>autre, hom, autres, un, cascun, tot, totz, aute, mes, arres</em>
* `Int` (2; 0% of non-empty `PronType`): <em>qui</em>
* `Neg` (57; 2% of non-empty `PronType`): <em>augu, augun, negun, auguns, degun, degune, nulhs, aucun, neg, negu</em>
* `Prs` (1556; 55% of non-empty `PronType`): <em>y, lo, en, se, ne, -n, n', i, s', nos</em>
* `Rel` (591; 21% of non-empty `PronType`): <em>qui, que, ont, qui-, on, loquoau, losquoaus, que-, cui, losquaus</em>
* `EMPTY` (654): <em>nos, que, qui, vos, luy, edz, se, so, ed, los</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="pro_corag-dep-conj.html">conj</a></tt>]--> PRON</tt> (11; 58%),
<tt>PRON --[<tt><a href="pro_corag-dep-nmod.html">nmod</a></tt>]--> PRON</tt> (9; 60%),
<tt>PRON --[<tt><a href="pro_corag-dep-advmod.html">advmod</a></tt>]--> PRON</tt> (1; 100%),
<tt>PRON --[<tt><a href="pro_corag-dep-orphan.html">orphan</a></tt>]--> PRON</tt> (1; 100%).

