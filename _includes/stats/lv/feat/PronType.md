

--------------------------------------------------------------------------------

## Treebank Statistics (UD_Latvian)

This feature is universal.
It occurs with 8 different values: `Dem`, `Ind`, `Int`, `Neg`, `Prs`, `Rcp`, `Rel`, `Tot`.
Some words have combined values of the feature; 2 combinations have been observed: `Ind|Neg`, `Int|Neg`.

1330 tokens (7%) have a non-empty value of `PronType`.
258 types (4%) occur at least once with a non-empty value of `PronType`.
76 lemmas (2%) occur at least once with a non-empty value of `PronType`.
The feature is used with 5 part-of-speech tags: [lv-pos/PRON]() (545; 3% instances), [lv-pos/DET]() (377; 2% instances), [lv-pos/ADV]() (227; 1% instances), [lv-pos/SCONJ]() (180; 1% instances), [lv-pos/NOUN]() (1; 0% instances).

### `PRON`

545 [lv-pos/PRON]() tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="Number.html">Number</a>=Sing</tt> (370; 68%), <tt><a href="Person.html">Person</a>=EMPTY</tt> (362; 66%), <tt><a href="Gender.html">Gender</a>=Masc</tt> (321; 59%), <tt><a href="Case.html">Case</a>=Nom</tt> (278; 51%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (226; 41% of non-empty `PronType`): <em>tas, to, tā, tam, tie, tās, tajā, šim, šajā, tos</em>
* `Ind` (49; 9% of non-empty `PronType`): <em>kas, ko, kādas, kāds, kādā, citi, kādam, citas, citos, cits</em>
* `Ind,Neg` (6; 1% of non-empty `PronType`): <em>neviens, neko</em>
* `Int` (33; 6% of non-empty `PronType`): <em>kas, ko, Kāda, kurā, kurš, Kuras, Kuri, Kādas, Kādi, kuram</em>
* `Prs` (186; 34% of non-empty `PronType`): <em>viņš, es, viņa, man, mēs, mums, jūs, viņi, viņu, mūsu</em>
* `Rcp` (8; 1% of non-empty `PronType`): <em>sev, sevi, sevis</em>
* `Tot` (37; 7% of non-empty `PronType`): <em>viss, pats, abi, katram, visi, pašiem, visā, abiem, paši, pašā</em>

<table>
  <tr><th>Paradigm <i>kas</i></th><th><tt>Int</tt></th><th><tt>Ind</tt></th></tr>
  <tr><td><tt><a href="Case.html">Case</a>=Acc</tt></td><td><em>ko</em></td><td><em>ko</em></td></tr>
  <tr><td><tt><a href="Case.html">Case</a>=Nom</tt></td><td><em>kas</em></td><td><em>kas</em></td></tr>
</table>

### `DET`

377 [lv-pos/DET]() tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="Number.html">Number</a>=Sing</tt> (235; 62%), <tt><a href="Gender.html">Gender</a>=Masc</tt> (211; 56%).

`DET` tokens may have the following values of `PronType`:

* `Dem` (192; 51% of non-empty `PronType`): <em>šo, tā, šī, šajā, šāda, šādu, šīs, šis, to, tāda</em>
* `Ind` (78; 21% of non-empty `PronType`): <em>cits, citus, citām, kādu, jebkurā, kādas, vairāku, citiem, citu, daudzi</em>
* `Ind,Neg` (7; 2% of non-empty `PronType`): <em>nekādas, neviena, Neviens, nekāds, nekādā</em>
* `Int` (1; 0% of non-empty `PronType`): <em>Kādas</em>
* `Prs` (37; 10% of non-empty `PronType`): <em>savu, savā, savas, saviem, mana, manas, manu, savus, Mans, Savās</em>
* `Tot` (62; 16% of non-empty `PronType`): <em>visas, visā, katru, visos, visu, katrā, pats, pašā, visa, abu</em>

<table>
  <tr><th>Paradigm <i>kāda</i></th><th><tt>Int</tt></th><th><tt>Ind</tt></th></tr>
  <tr><td><tt><a href="Case.html">Case</a>=Acc|<a href="Number.html">Number</a>=Sing</tt></td><td></td><td><em>kādu</em></td></tr>
  <tr><td><tt><a href="Case.html">Case</a>=Acc|<a href="Number.html">Number</a>=Plur</tt></td><td><em>Kādas</em></td><td><em>kādas</em></td></tr>
  <tr><td><tt><a href="Case.html">Case</a>=Loc|<a href="Number.html">Number</a>=Sing</tt></td><td></td><td><em>kādā</em></td></tr>
  <tr><td><tt><a href="Case.html">Case</a>=Loc|<a href="Number.html">Number</a>=Plur</tt></td><td></td><td><em>kādās</em></td></tr>
  <tr><td><tt><a href="Case.html">Case</a>=Nom|<a href="Number.html">Number</a>=Plur</tt></td><td></td><td><em>kādas</em></td></tr>
</table>

`PronType` seems to be **lexical feature** of `DET`. 98% lemmas (39) occur only with one value of `PronType`.

### `ADV`

227 [lv-pos/ADV]() tokens (27% of all `ADV` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADV` and `PronType` co-occurred: <tt><a href="Degree.html">Degree</a>=EMPTY</tt> (227; 100%).

`ADV` tokens may have the following values of `PronType`:

* `Dem` (75; 33% of non-empty `PronType`): <em>tā, tad, tur, tik, tagad, Šeit</em>
* `Ind` (1; 0% of non-empty `PronType`): <em>kur</em>
* `Int` (127; 56% of non-empty `PronType`): <em>kā, kur, kad, cik, kāpēc, kādēļ</em>
* `Int,Neg` (6; 3% of non-empty `PronType`): <em>nekad, nekur</em>
* `Neg` (14; 6% of non-empty `PronType`): <em>nepamatoti, neiespējami, nelietderīgi, nereti, nesen, neatkarīgi, nemaz, nesalīdzināmi</em>
* `Tot` (4; 2% of non-empty `PronType`): <em>vienmēr, visur</em>
* `EMPTY` (615): <em>jau, vēl, vairāk, savukārt, ļoti, pašlaik, patlaban, daudz, līdz, tāpēc</em>

<table>
  <tr><th>Paradigm <i>kur</i></th><th><tt>Int</tt></th><th><tt>Ind</tt></th></tr>
  <tr><td><tt></tt></td><td><em>kur</em></td><td><em>kur</em></td></tr>
</table>

`PronType` seems to be **lexical feature** of `ADV`. 96% lemmas (23) occur only with one value of `PronType`.

### `SCONJ`

180 [lv-pos/SCONJ]() tokens (31% of all `SCONJ` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `SCONJ` and `PronType` co-occurred: <tt><a href="Case.html">Case</a>=Nom</tt> (108; 60%), <tt><a href="Number.html">Number</a>=EMPTY</tt> (106; 59%), <tt><a href="Gender.html">Gender</a>=EMPTY</tt> (106; 59%).

`SCONJ` tokens may have the following values of `PronType`:

* `Rel` (180; 100% of non-empty `PronType`): <em>kas, ko, kurā, kuru, kuras, kuriem, kurš, kuri, kura, kuram</em>
* `EMPTY` (394): <em>ka, lai, kā, jo, ja, nekā, vai, gan, līdz, kamēr</em>

### `NOUN`

1 [lv-pos/NOUN]() tokens (0% of all `NOUN` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `NOUN` and `PronType` co-occurred: <tt><a href="Case.html">Case</a>=Gen</tt> (1; 100%), <tt><a href="Gender.html">Gender</a>=Fem</tt> (1; 100%), <tt><a href="Number.html">Number</a>=Sing</tt> (1; 100%).

`NOUN` tokens may have the following values of `PronType`:

* `Int` (1; 100% of non-empty `PronType`): <em>kurienes</em>
* `EMPTY` (6003): <em>gada, gadā, valsts, atkritumu, latu, finanšu, uzņēmuma, laikā, pašvaldības, valdes</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>DET --[<a href="../dep/det.html">det</a>]--> DET</tt> (1; 100%),
<tt>SCONJ --[<a href="../dep/conj.html">conj</a>]--> SCONJ</tt> (1; 100%),
<tt>PRON --[<a href="../dep/det.html">det</a>]--> DET</tt> (1; 100%).

