---
layout: base
title:  'Statistics of Foreign in UD_Polish-PDB'
udver: '2'
---

## Treebank Statistics: UD_Polish-PDB: Features: `Foreign`

This feature is universal.
It occurs with 1 different values: `Yes`.

167 tokens (0%) have a non-empty value of `Foreign`.
105 types (0%) occur at least once with a non-empty value of `Foreign`.
103 lemmas (0%) occur at least once with a non-empty value of `Foreign`.
The feature is used with 1 part-of-speech tags: <tt><a href="pl_pdb-pos-X.html">X</a></tt> (167; 0% instances).

### `X`

167 <tt><a href="pl_pdb-pos-X.html">X</a></tt> tokens (16% of all `X` tokens) have a non-empty value of `Foreign`.

The most frequent other feature values with which `X` and `Foreign` co-occurred: <tt><a href="pl_pdb-feat-NumForm.html">NumForm</a></tt><tt>=EMPTY</tt> (167; 100%).

`X` tokens may have the following values of `Foreign`:

* `Yes` (167; 100% of non-empty `Foreign`): <em>de, San, of, von, New, Don, a, bin, di, the</em>
* `EMPTY` (889): <em>2, 1, 3, 4, 5, 6, 7, 8, 14, 9</em>

`Foreign` seems to be **lexical feature** of `X`. 100% lemmas (103) occur only with one value of `Foreign`.

## Relations with Agreement in `Foreign`

The 10 most frequent relations where parent and child node agree in `Foreign`:
<tt>X --[<tt><a href="pl_pdb-dep-flat-foreign.html">flat:foreign</a></tt>]--> X</tt> (42; 100%),
<tt>X --[<tt><a href="pl_pdb-dep-fixed.html">fixed</a></tt>]--> X</tt> (7; 100%),
<tt>X --[<tt><a href="pl_pdb-dep-conj.html">conj</a></tt>]--> X</tt> (6; 100%),
<tt>X --[<tt><a href="pl_pdb-dep-flat.html">flat</a></tt>]--> X</tt> (1; 100%).

