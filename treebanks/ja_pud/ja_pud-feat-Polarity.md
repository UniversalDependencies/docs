---
layout: base
title:  'Statistics of Polarity in UD_Japanese-PUD'
udver: '2'
---

## Treebank Statistics: UD_Japanese-PUD: Features: `Polarity`

This feature is universal.
It occurs with 1 different values: `Neg`.

177 tokens (1%) have a non-empty value of `Polarity`.
11 types (0%) occur at least once with a non-empty value of `Polarity`.
7 lemmas (0%) occur at least once with a non-empty value of `Polarity`.
The feature is used with 3 part-of-speech tags: <tt><a href="ja_pud-pos-AUX.html">AUX</a></tt> (144; 1% instances), <tt><a href="ja_pud-pos-NOUN.html">NOUN</a></tt> (27; 0% instances), <tt><a href="ja_pud-pos-SCONJ.html">SCONJ</a></tt> (6; 0% instances).

### `AUX`

144 <tt><a href="ja_pud-pos-AUX.html">AUX</a></tt> tokens (4% of all `AUX` tokens) have a non-empty value of `Polarity`.

`AUX` tokens may have the following values of `Polarity`:

* `Neg` (144; 100% of non-empty `Polarity`): ない, なかっ, ず, ん, なけれ, なく
* `EMPTY` (3178): た, し, で, れ, な, さ, する, に, だ, ます

### `NOUN`

27 <tt><a href="ja_pud-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `Polarity`.

`NOUN` tokens may have the following values of `Polarity`:

* `Neg` (27; 100% of non-empty `Polarity`): 不, 未, 非, 反, 無
* `EMPTY` (7397): こと, 年, 人, 者, 月, ら, ため, 後, 日, つ

### `SCONJ`

6 <tt><a href="ja_pud-pos-SCONJ.html">SCONJ</a></tt> tokens (1% of all `SCONJ` tokens) have a non-empty value of `Polarity`.

`SCONJ` tokens may have the following values of `Polarity`:

* `Neg` (6; 100% of non-empty `Polarity`): ず
* `EMPTY` (984): て, が, の, と, に, ば, ため, で, ながら, から

## Relations with Agreement in `Polarity`

The 10 most frequent relations where parent and child node agree in `Polarity`:
<tt>AUX --[<tt><a href="ja_pud-dep-fixed.html">fixed</a></tt>]--> AUX</tt> (7; 70%).

