---
layout: base
title:  'Statistics of NumType in UD_Classical_Chinese-Kyoto'
udver: '2'
---

## Treebank Statistics: UD_Classical_Chinese-Kyoto: Features: `NumType`

This feature is universal.
It occurs with 1 different values: `Ord`.

137 tokens (0%) have a non-empty value of `NumType`.
65 types (1%) occur at least once with a non-empty value of `NumType`.
65 lemmas (1%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="lzh_kyoto-pos-NUM.html">NUM</a></tt> (137; 0% instances).

### `NUM`

137 <tt><a href="lzh_kyoto-pos-NUM.html">NUM</a></tt> tokens (3% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Ord` (137; 100% of non-empty `NumType`): 丁、 己、 庚申、 戊、 己卯、 戊寅、 甲、 甲子、 辛、 丁丑
* `EMPTY` (5340): 三、 一、 五、 二、 四、 百、 六、 十、 九、 萬

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (65) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="lzh_kyoto-dep-flat.html">flat</a></tt>]--> NUM</tt> (13; 100%).

