---
layout: base
title:  'Statistics of ExtPos in UD_Indonesian-PUD'
udver: '2'
---

## Treebank Statistics: UD_Indonesian-PUD: Features: `ExtPos`

This feature is language-specific.
It occurs with 3 different values: `ADP`, `ADV`, `SCONJ`.

53 tokens (0%) have a non-empty value of `ExtPos`.
12 types (0%) occur at least once with a non-empty value of `ExtPos`.
11 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 4 part-of-speech tags: <tt><a href="id_pud-pos-ADV.html">ADV</a></tt> (28; 0% instances), <tt><a href="id_pud-pos-ADP.html">ADP</a></tt> (21; 0% instances), <tt><a href="id_pud-pos-ADJ.html">ADJ</a></tt> (3; 0% instances), <tt><a href="id_pud-pos-PRON.html">PRON</a></tt> (1; 0% instances).

### `ADV`

28 <tt><a href="id_pud-pos-ADV.html">ADV</a></tt> tokens (4% of all `ADV` tokens) have a non-empty value of `ExtPos`.

`ADV` tokens may have the following values of `ExtPos`:

* `ADV` (28; 100% of non-empty `ExtPos`): <em>lebih, sering, Sementara, Lagi, kurang, Kadang</em>

### `ADP`

21 <tt><a href="id_pud-pos-ADP.html">ADP</a></tt> tokens (1% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (2; 10% of non-empty `ExtPos`): <em>antara</em>
* `ADV` (1; 5% of non-empty `ExtPos`): <em>Oleh</em>
* `SCONJ` (18; 86% of non-empty `ExtPos`): <em>di</em>

### `ADJ`

3 <tt><a href="id_pud-pos-ADJ.html">ADJ</a></tt> tokens (0% of all `ADJ` tokens) have a non-empty value of `ExtPos`.

`ADJ` tokens may have the following values of `ExtPos`:

* `ADV` (3; 100% of non-empty `ExtPos`): <em>sama</em>

### `PRON`

1 <tt><a href="id_pud-pos-PRON.html">PRON</a></tt> tokens (0% of all `PRON` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PRON` and `ExtPos` co-occurred: <tt><a href="id_pud-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (1; 100%), <tt><a href="id_pud-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (1; 100%), <tt><a href="id_pud-feat-PronType.html">PronType</a></tt><tt>=Rel</tt> (1; 100%).

`PRON` tokens may have the following values of `ExtPos`:

* `SCONJ` (1; 100% of non-empty `ExtPos`): <em>yang</em>

