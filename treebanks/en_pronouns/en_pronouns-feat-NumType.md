---
layout: base
title:  'Statistics of NumType in UD_English-Pronouns'
udver: '2'
---

## Treebank Statistics: UD_English-Pronouns: Features: `NumType`

This feature is universal.
It occurs with 1 different values: `Card`.

5 tokens (0%) have a non-empty value of `NumType`.
1 types (1%) occur at least once with a non-empty value of `NumType`.
1 lemmas (2%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="en_pronouns-pos-NUM.html">NUM</a></tt> (5; 0% instances).

### `NUM`

5 <tt><a href="en_pronouns-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (5; 100% of non-empty `NumType`): <em>One</em>

