---
layout: base
title:  'Statistics of ExtPos in UD_Swedish-PUD'
udver: '2'
---

## Treebank Statistics: UD_Swedish-PUD: Features: `ExtPos`

This feature is language-specific.
It occurs with 3 different values: `ADJ`, `ADP`, `ADV`.

17 tokens (0%) have a non-empty value of `ExtPos`.
10 types (0%) occur at least once with a non-empty value of `ExtPos`.
7 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 4 part-of-speech tags: <tt><a href="sv_pud-pos-ADP.html">ADP</a></tt> (7; 0% instances), <tt><a href="sv_pud-pos-ADV.html">ADV</a></tt> (7; 0% instances), <tt><a href="sv_pud-pos-PART.html">PART</a></tt> (2; 0% instances), <tt><a href="sv_pud-pos-CCONJ.html">CCONJ</a></tt> (1; 0% instances).

### `ADP`

7 <tt><a href="sv_pud-pos-ADP.html">ADP</a></tt> tokens (0% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADJ` (2; 29% of non-empty `ExtPos`): <em>före</em>
* `ADP` (5; 71% of non-empty `ExtPos`): <em>för, i</em>

### `ADV`

7 <tt><a href="sv_pud-pos-ADV.html">ADV</a></tt> tokens (1% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="sv_pud-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (7; 100%).

`ADV` tokens may have the following values of `ExtPos`:

* `ADV` (7; 100% of non-empty `ExtPos`): <em>så, till</em>

### `PART`

2 <tt><a href="sv_pud-pos-PART.html">PART</a></tt> tokens (0% of all `PART` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PART` and `ExtPos` co-occurred: <tt><a href="sv_pud-feat-Polarity.html">Polarity</a></tt><tt>=Neg</tt> (2; 100%).

`PART` tokens may have the following values of `ExtPos`:

* `ADV` (2; 100% of non-empty `ExtPos`): <em>icke</em>

### `CCONJ`

1 <tt><a href="sv_pud-pos-CCONJ.html">CCONJ</a></tt> tokens (0% of all `CCONJ` tokens) have a non-empty value of `ExtPos`.

`CCONJ` tokens may have the following values of `ExtPos`:

* `ADV` (1; 100% of non-empty `ExtPos`): <em>som</em>

