---
layout: base
title:  'Statistics of ExtPos in UD_Skolt_Sami-Giellagas'
udver: '2'
---

## Treebank Statistics: UD_Skolt_Sami-Giellagas: Features: `ExtPos`

This feature is language-specific.
It occurs with 5 different values: `ADV`, `CCONJ`, `INTJ`, `PRON`, `SCONJ`.

14 tokens (0%) have a non-empty value of `ExtPos`.
10 types (1%) occur at least once with a non-empty value of `ExtPos`.
10 lemmas (2%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 5 part-of-speech tags: <tt><a href="sms_giellagas-pos-ADV.html">ADV</a></tt> (5; 0% instances), <tt><a href="sms_giellagas-pos-INTJ.html">INTJ</a></tt> (4; 0% instances), <tt><a href="sms_giellagas-pos-PART.html">PART</a></tt> (2; 0% instances), <tt><a href="sms_giellagas-pos-PRON.html">PRON</a></tt> (2; 0% instances), <tt><a href="sms_giellagas-pos-CCONJ.html">CCONJ</a></tt> (1; 0% instances).

### `ADV`

5 <tt><a href="sms_giellagas-pos-ADV.html">ADV</a></tt> tokens (2% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="sms_giellagas-feat-AdvType.html">AdvType</a></tt><tt>=EMPTY</tt> (4; 80%).

`ADV` tokens may have the following values of `ExtPos`:

* `ADV` (4; 80% of non-empty `ExtPos`): <em>koozz, kuuʹǩǩ</em>
* `SCONJ` (1; 20% of non-empty `ExtPos`): <em>ouddâl</em>

### `INTJ`

4 <tt><a href="sms_giellagas-pos-INTJ.html">INTJ</a></tt> tokens (7% of all `INTJ` tokens) have a non-empty value of `ExtPos`.

`INTJ` tokens may have the following values of `ExtPos`:

* `CCONJ` (1; 25% of non-empty `ExtPos`): <em>Ij</em>
* `INTJ` (3; 75% of non-empty `ExtPos`): <em>Piʒʒ, Na</em>

### `PART`

2 <tt><a href="sms_giellagas-pos-PART.html">PART</a></tt> tokens (8% of all `PART` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PART` and `ExtPos` co-occurred: <tt><a href="sms_giellagas-feat-Polarity.html">Polarity</a></tt><tt>=Neg</tt> (2; 100%).

`PART` tokens may have the following values of `ExtPos`:

* `ADV` (2; 100% of non-empty `ExtPos`): <em>ni</em>

### `PRON`

2 <tt><a href="sms_giellagas-pos-PRON.html">PRON</a></tt> tokens (1% of all `PRON` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PRON` and `ExtPos` co-occurred: <tt><a href="sms_giellagas-feat-Number.html">Number</a></tt><tt>=Sing</tt> (2; 100%), <tt><a href="sms_giellagas-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (2; 100%), <tt><a href="sms_giellagas-feat-PronType.html">PronType</a></tt><tt>=Rel</tt> (2; 100%).

`PRON` tokens may have the following values of `ExtPos`:

* `PRON` (2; 100% of non-empty `ExtPos`): <em>Mii, mõin</em>

### `CCONJ`

1 <tt><a href="sms_giellagas-pos-CCONJ.html">CCONJ</a></tt> tokens (1% of all `CCONJ` tokens) have a non-empty value of `ExtPos`.

`CCONJ` tokens may have the following values of `ExtPos`:

* `CCONJ` (1; 100% of non-empty `ExtPos`): <em>a</em>

