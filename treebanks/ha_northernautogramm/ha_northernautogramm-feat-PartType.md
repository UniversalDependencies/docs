---
layout: base
title:  'Statistics of PartType in UD_Hausa-NorthernAutogramm'
udver: '2'
---

## Treebank Statistics: UD_Hausa-NorthernAutogramm: Features: `PartType`

This feature is language-specific.
It occurs with 1 different values: `Int`.

8 tokens (0%) have a non-empty value of `PartType`.
4 types (1%) occur at least once with a non-empty value of `PartType`.
4 lemmas (1%) occur at least once with a non-empty value of `PartType`.
The feature is used with 1 part-of-speech tags: <tt><a href="ha_northernautogramm-pos-PART.html">PART</a></tt> (8; 0% instances).

### `PART`

8 <tt><a href="ha_northernautogramm-pos-PART.html">PART</a></tt> tokens (5% of all `PART` tokens) have a non-empty value of `PartType`.

The most frequent other feature values with which `PART` and `PartType` co-occurred: <tt><a href="ha_northernautogramm-feat-Aspect.html">Aspect</a></tt><tt>=EMPTY</tt> (8; 100%), <tt><a href="ha_northernautogramm-feat-Polarity.html">Polarity</a></tt><tt>=Neg</tt> (5; 63%).

`PART` tokens may have the following values of `PartType`:

* `Int` (8; 100% of non-empty `PartType`): <em>ba, hwa, koː, shìn</em>

