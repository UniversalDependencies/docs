---
layout: base
title:  'Statistics of ExtPos in UD_Czech-PUD'
udver: '2'
---

## Treebank Statistics: UD_Czech-PUD: Features: `ExtPos`

This feature is language-specific.
It occurs with 4 different values: `ADP`, `ADV`, `CCONJ`, `SCONJ`.

59 tokens (0%) have a non-empty value of `ExtPos`.
20 types (0%) occur at least once with a non-empty value of `ExtPos`.
13 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 3 part-of-speech tags: <tt><a href="cs_pud-pos-ADP.html">ADP</a></tt> (35; 0% instances), <tt><a href="cs_pud-pos-ADV.html">ADV</a></tt> (16; 0% instances), <tt><a href="cs_pud-pos-CCONJ.html">CCONJ</a></tt> (8; 0% instances).

### `ADP`

35 <tt><a href="cs_pud-pos-ADP.html">ADP</a></tt> tokens (2% of all `ADP` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADP` and `ExtPos` co-occurred: <tt><a href="cs_pud-feat-AdpType.html">AdpType</a></tt><tt>=Prep</tt> (30; 86%), <tt><a href="cs_pud-feat-Case.html">Case</a></tt><tt>=Loc</tt> (26; 74%).

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (34; 97% of non-empty `ExtPos`): <em>v, na, vzhledem, s, ve, bez, z</em>
* `ADV` (1; 3% of non-empty `ExtPos`): <em>mimo</em>

### `ADV`

16 <tt><a href="cs_pud-pos-ADV.html">ADV</a></tt> tokens (2% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="cs_pud-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (16; 100%), <tt><a href="cs_pud-feat-Polarity.html">Polarity</a></tt><tt>=Pos</tt> (14; 88%), <tt><a href="cs_pud-feat-Degree.html">Degree</a></tt><tt>=Cmp</tt> (11; 69%).

`ADV` tokens may have the following values of `ExtPos`:

* `ADP` (4; 25% of non-empty `ExtPos`): <em>spolu, společně</em>
* `ADV` (12; 75% of non-empty `ExtPos`): <em>více, víc, stejně</em>

### `CCONJ`

8 <tt><a href="cs_pud-pos-CCONJ.html">CCONJ</a></tt> tokens (1% of all `CCONJ` tokens) have a non-empty value of `ExtPos`.

`CCONJ` tokens may have the following values of `ExtPos`:

* `CCONJ` (6; 75% of non-empty `ExtPos`): <em>a</em>
* `SCONJ` (2; 25% of non-empty `ExtPos`): <em>i</em>

