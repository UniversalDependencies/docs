---
layout: base
title:  'Statistics of PronType in UD_Maltese'
udver: '2'
---

## Treebank Statistics: UD_Maltese: Features: `PronType`

This feature is universal.
It occurs with 5 different values: `Art`, `Dem`, `Ind`, `Int`, `Prs`.

450 tokens (19%) have a non-empty value of `PronType`.
92 types (10%) occur at least once with a non-empty value of `PronType`.
1 lemmas (0) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="mt-pos-DET.html">DET</a></tt> (266; 11% instances), <tt><a href="mt-pos-ADP.html">ADP</a></tt> (165; 7% instances), <tt><a href="mt-pos-PRON.html">PRON</a></tt> (19; 1% instances).

### `DET`

266 <tt><a href="mt-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="mt-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (190; 71%).

`DET` tokens may have the following values of `PronType`:

* `Art` (190; 71% of non-empty `PronType`): l- il- is- it- t- d- id- iż- s- -il
* `Dem` (32; 12% of non-empty `PronType`): dan din dawn dawk dak hekk
* `Ind` (20; 8% of non-empty `PronType`): kollha kollu kull ftit kollox kontroll xejn ħaddieħor Għaldaqstant Għalhekk
* `Int` (1; 0% of non-empty `PronType`): min
* `Prs` (23; 9% of non-empty `PronType`): tagħhom tagħna tiegħu

### `ADP`

165 <tt><a href="mt-pos-ADP.html">ADP</a></tt> tokens (46% of all `ADP` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADP` and `PronType` co-occurred: <tt><a href="mt-feat-AdpType.html">AdpType</a></tt><tt>=Prep</tt> (165; 100%), <tt><a href="mt-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (165; 100%).

`ADP` tokens may have the following values of `PronType`:

* `Art` (165; 100% of non-empty `PronType`): tal- mill- fl- fil- għall- lill- tat- bil- għat- mal-
* `EMPTY` (190): ta' f' b' fuq għal ma' minn dwar fi lil

### `PRON`

19 <tt><a href="mt-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="mt-feat-Reflex.html">Reflex</a></tt><tt>=EMPTY</tt> (17; 89%).

`PRON` tokens may have the following values of `PronType`:

* `Prs` (19; 100% of non-empty `PronType`): hu huma hi aħna hija ġejjien magħkom ruħhom

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>ADP --[<tt><a href="mt-dep-case.html">case</a></tt>]--> ADP</tt> (1; 100%).

