---
layout: base
title:  'Statistics of ExtPos in UD_Swedish-Old'
udver: '2'
---

## Treebank Statistics: UD_Swedish-Old: Features: `ExtPos`

This feature is language-specific.
It occurs with 1 different values: `ADV`.

2 tokens (0%) have a non-empty value of `ExtPos`.
2 types (1%) occur at least once with a non-empty value of `ExtPos`.
2 lemmas (1%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 1 part-of-speech tags: <tt><a href="sv_old-pos-ADV.html">ADV</a></tt> (2; 0% instances).

### `ADV`

2 <tt><a href="sv_old-pos-ADV.html">ADV</a></tt> tokens (4% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="sv_old-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (2; 100%).

`ADV` tokens may have the following values of `ExtPos`:

* `ADV` (2; 100% of non-empty `ExtPos`): <em>om, wtan</em>

