---
layout: base
title:  'Statistics of NumType in UD_Italian-ParlaMint'
udver: '2'
---

## Treebank Statistics: UD_Italian-ParlaMint: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

293 tokens (1%) have a non-empty value of `NumType`.
117 types (3%) occur at least once with a non-empty value of `NumType`.
111 lemmas (5%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="it_parlamint-pos-NUM.html">NUM</a></tt> (243; 1% instances), <tt><a href="it_parlamint-pos-ADJ.html">ADJ</a></tt> (50; 0% instances).

### `NUM`

243 <tt><a href="it_parlamint-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (243; 100% of non-empty `NumType`): <em>5, due, 184, 6, ventiquattro, 1983, 2020, 4, 1, 1.100</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (96) occur only with one value of `NumType`.

### `ADJ`

50 <tt><a href="it_parlamint-pos-ADJ.html">ADJ</a></tt> tokens (4% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="it_parlamint-feat-Number.html">Number</a></tt><tt>=Sing</tt> (31; 62%).

`ADJ` tokens may have the following values of `NumType`:

* `Ord` (50; 100% of non-empty `NumType`): <em>bis, primo, prima, quater, ultimi, II, prime, secondo, ter, ultimo</em>
* `EMPTY` (1081): <em>penitenziaria, possibile, affidataria, contrario, altri, familiare, prolungato, altre, sociali, presente</em>

`NumType` seems to be **lexical feature** of `ADJ`. 100% lemmas (15) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="it_parlamint-dep-conj.html">conj</a></tt>]--> NUM</tt> (19; 100%),
<tt>NUM --[<tt><a href="it_parlamint-dep-flat.html">flat</a></tt>]--> NUM</tt> (11; 100%),
<tt>NUM --[<tt><a href="it_parlamint-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (2; 100%),
<tt>ADJ --[<tt><a href="it_parlamint-dep-conj.html">conj</a></tt>]--> ADJ</tt> (1; 100%).

