---
layout: base
title:  'Statistics of NumType in UD_Japanese'
udver: '2'
---

## Treebank Statistics: UD_Japanese: Features: `NumType`

This feature is universal.
It occurs with 1 different values: `Card`.

4401 tokens (2%) have a non-empty value of `NumType`.
717 types (3%) occur at least once with a non-empty value of `NumType`.
717 lemmas (3%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="ja-pos-NUM.html">NUM</a></tt> (4401; 2% instances).

### `NUM`

4401 <tt><a href="ja-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (4401; 100% of non-empty `NumType`): 1, 2, 3, 4, 一, 5, 10, 6, 二, 7

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (717) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="ja-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (339; 100%),
<tt>NUM --[<tt><a href="ja-dep-obl.html">obl</a></tt>]--> NUM</tt> (62; 100%),
<tt>NUM --[<tt><a href="ja-dep-nmod.html">nmod</a></tt>]--> NUM</tt> (5; 100%),
<tt>NUM --[<tt><a href="ja-dep-acl.html">acl</a></tt>]--> NUM</tt> (4; 100%),
<tt>NUM --[<tt><a href="ja-dep-nsubj.html">nsubj</a></tt>]--> NUM</tt> (1; 100%).

