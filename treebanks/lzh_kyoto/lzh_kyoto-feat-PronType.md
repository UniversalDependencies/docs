---
layout: base
title:  'Statistics of PronType in UD_Classical_Chinese-Kyoto'
udver: '2'
---

## Treebank Statistics: UD_Classical_Chinese-Kyoto: Features: `PronType`

This feature is universal.
It occurs with 3 different values: `Dem`, `Int`, `Prs`.

4200 tokens (8%) have a non-empty value of `PronType`.
38 types (1%) occur at least once with a non-empty value of `PronType`.
38 lemmas (1%) occur at least once with a non-empty value of `PronType`.
The feature is used with 1 part-of-speech tags: <tt><a href="lzh_kyoto-pos-PRON.html">PRON</a></tt> (4200; 8% instances).

### `PRON`

4200 <tt><a href="lzh_kyoto-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="lzh_kyoto-feat-Person.html">Person</a></tt><tt>=3</tt> (2218; 53%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (699; 17% of non-empty `PronType`): <em>是, 此, 斯, 彼, 焉, 他, 夫, 惟, 茲, 爰</em>
* `Int` (321; 8% of non-empty `PronType`): <em>何, 孰, 誰, 奚, 害</em>
* `Prs` (3180; 76% of non-empty `PronType`): <em>之, 其, 吾, 我, 諸, 或, 子, 予, 己, 自</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (38) occur only with one value of `PronType`.

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="lzh_kyoto-dep-nsubj.html">nsubj</a></tt>]--> PRON</tt> (4; 67%),
<tt>PRON --[<tt><a href="lzh_kyoto-dep-conj.html">conj</a></tt>]--> PRON</tt> (3; 100%).

