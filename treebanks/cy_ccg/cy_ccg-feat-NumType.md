---
layout: base
title:  'Statistics of NumType in UD_Welsh-CCG'
udver: '2'
---

## Treebank Statistics: UD_Welsh-CCG: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

734 tokens (2%) have a non-empty value of `NumType`.
277 types (4%) occur at least once with a non-empty value of `NumType`.
245 lemmas (5%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="cy_ccg-pos-NUM.html">NUM</a></tt> (635; 1% instances), <tt><a href="cy_ccg-pos-ADJ.html">ADJ</a></tt> (99; 0% instances).

### `NUM`

635 <tt><a href="cy_ccg-pos-NUM.html">NUM</a></tt> tokens (99% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="cy_ccg-feat-Mutation.html">Mutation</a></tt><tt>=EMPTY</tt> (571; 90%), <tt><a href="cy_ccg-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (547; 86%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (635; 100% of non-empty `NumType`): <em>un, 4, dau, ddau, chwe, tair, tri, ddwy, pedair, dwy</em>
* `EMPTY` (4): <em>chwech, 150, un</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (219) occur only with one value of `NumType`.

### `ADJ`

99 <tt><a href="cy_ccg-pos-ADJ.html">ADJ</a></tt> tokens (3% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="cy_ccg-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (99; 100%), <tt><a href="cy_ccg-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (91; 92%), <tt><a href="cy_ccg-feat-Mutation.html">Mutation</a></tt><tt>=EMPTY</tt> (83; 84%), <tt><a href="cy_ccg-feat-Degree.html">Degree</a></tt><tt>=Pos</tt> (68; 69%).

`ADJ` tokens may have the following values of `NumType`:

* `Ord` (99; 100% of non-empty `NumType`): <em>cyntaf, ail, gyntaf, trydydd, drydedd, 19, 17, 18, 19eg, 21ain</em>
* `EMPTY` (3223): <em>newydd, Cymraeg, Gymraeg, bob, mwy, lleol, eraill, arall, pob, fawr</em>

`NumType` seems to be **lexical feature** of `ADJ`. 100% lemmas (26) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="cy_ccg-dep-conj.html">conj</a></tt>]--> NUM</tt> (13; 100%),
<tt>NUM --[<tt><a href="cy_ccg-dep-nmod.html">nmod</a></tt>]--> NUM</tt> (4; 100%),
<tt>NUM --[<tt><a href="cy_ccg-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (4; 100%),
<tt>ADJ --[<tt><a href="cy_ccg-dep-conj.html">conj</a></tt>]--> ADJ</tt> (1; 100%),
<tt>NUM --[<tt><a href="cy_ccg-dep-obl.html">obl</a></tt>]--> NUM</tt> (1; 100%).

