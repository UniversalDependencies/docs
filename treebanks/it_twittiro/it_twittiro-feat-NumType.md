---
layout: base
title:  'Statistics of NumType in UD_Italian-TWITTIRO'
udver: '2'
---

## Treebank Statistics: UD_Italian-TWITTIRO: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

351 tokens (1%) have a non-empty value of `NumType`.
152 types (2%) occur at least once with a non-empty value of `NumType`.
139 lemmas (3%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="it_twittiro-pos-NUM.html">NUM</a></tt> (298; 1% instances), <tt><a href="it_twittiro-pos-ADJ.html">ADJ</a></tt> (53; 0% instances).

### `NUM`

298 <tt><a href="it_twittiro-pos-NUM.html">NUM</a></tt> tokens (99% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (298; 100% of non-empty `NumType`): <em>due, 3, 2, mila, tre, 1, 12, 5, 7, 10</em>
* `EMPTY` (2): <em>3), 5</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (129) occur only with one value of `NumType`.

### `ADJ`

53 <tt><a href="it_twittiro-pos-ADJ.html">ADJ</a></tt> tokens (4% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="it_twittiro-feat-Number.html">Number</a></tt><tt>=Sing</tt> (41; 77%).

`ADJ` tokens may have the following values of `NumType`:

* `Card` (1; 2% of non-empty `NumType`): <em>1'</em>
* `Ord` (52; 98% of non-empty `NumType`): <em>primo, prima, prime, primi, terza, 1', 1mo, 3, ennesima, seconda</em>
* `EMPTY` (1184): <em>buona, nuovo, buon, bella, grande, italiana, pubblica, italiano, bel, unica</em>

<table>
  <tr><th>Paradigm <i>1'</i></th><th><tt>Card</tt></th><th><tt>Ord</tt></th></tr>
  <tr><td><tt></tt></td><td><em>1'</em></td><td><em>1'</em></td></tr>
</table>

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="it_twittiro-dep-flat.html">flat</a></tt>]--> NUM</tt> (17; 100%),
<tt>NUM --[<tt><a href="it_twittiro-dep-conj.html">conj</a></tt>]--> NUM</tt> (6; 100%),
<tt>NUM --[<tt><a href="it_twittiro-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (1; 100%).

