---
layout: base
title:  'Statistics of NumType in UD_Chinese'
udver: '2'
---

## Treebank Statistics: UD_Chinese: Features: `NumType`

This feature is universal.
It occurs with 1 different values: `Card`.

6659 tokens (5%) have a non-empty value of `NumType`.
1257 types (6%) occur at least once with a non-empty value of `NumType`.
1257 lemmas (6%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="zh-pos-NUM.html">NUM</a></tt> (6659; 5% instances).

### `NUM`

6659 <tt><a href="zh-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (6659; 100% of non-empty `NumType`): 一、 兩、 三、 1、 第一、 3、 12、 5、 2、 8

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (1257) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="zh-dep-conj.html">conj</a></tt>]--> NUM</tt> (49; 100%),
<tt>NUM --[<tt><a href="zh-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (11; 100%),
<tt>NUM --[<tt><a href="zh-dep-dep.html">dep</a></tt>]--> NUM</tt> (10; 100%).

