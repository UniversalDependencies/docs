---
layout: base
title:  'Statistics of ExtPos in UD_Arabic-PUD'
udver: '2'
---

## Treebank Statistics: UD_Arabic-PUD: Features: `ExtPos`

This feature is language-specific.
It occurs with 4 different values: `ADP`, `ADV`, `CCONJ`, `SCONJ`.

167 tokens (1%) have a non-empty value of `ExtPos`.
15 types (0%) occur at least once with a non-empty value of `ExtPos`.
15 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 3 part-of-speech tags: <tt><a href="ar_pud-pos-ADP.html">ADP</a></tt> (142; 1% instances), <tt><a href="ar_pud-pos-PART.html">PART</a></tt> (24; 0% instances), <tt><a href="ar_pud-pos-PRON.html">PRON</a></tt> (1; 0% instances).

### `ADP`

142 <tt><a href="ar_pud-pos-ADP.html">ADP</a></tt> tokens (4% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (111; 78% of non-empty `ExtPos`): من، ب، حتى، ك، ل
* `ADV` (11; 8% of non-empty `ExtPos`): ب، على، من
* `SCONJ` (20; 14% of non-empty `ExtPos`): ب، في، كما، حيث، بما، حتى، لو، ما

<table>
  <tr><th>Paradigm <i>بِ</i></th><th><tt>ADP</tt></th><th><tt>ADV</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td>ب</td><td>ب</td><td>ب</td></tr>
</table>

### `PART`

24 <tt><a href="ar_pud-pos-PART.html">PART</a></tt> tokens (5% of all `PART` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PART` and `ExtPos` co-occurred: <tt><a href="ar_pud-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (24; 100%).

`PART` tokens may have the following values of `ExtPos`:

* `ADP` (2; 8% of non-empty `ExtPos`): ما
* `ADV` (6; 25% of non-empty `ExtPos`): لا
* `CCONJ` (5; 21% of non-empty `ExtPos`): غير، كما
* `SCONJ` (11; 46% of non-empty `ExtPos`): ما، إلا، غير، حتى

<table>
  <tr><th>Paradigm <i>غَير</i></th><th><tt>CCONJ</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td>غير</td><td>غير</td></tr>
</table>

### `PRON`

1 <tt><a href="ar_pud-pos-PRON.html">PRON</a></tt> tokens (0% of all `PRON` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PRON` and `ExtPos` co-occurred: <tt><a href="ar_pud-feat-Case.html">Case</a></tt><tt>=Gen</tt> (1; 100%), <tt><a href="ar_pud-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (1; 100%), <tt><a href="ar_pud-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (1; 100%), <tt><a href="ar_pud-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (1; 100%), <tt><a href="ar_pud-feat-PronType.html">PronType</a></tt><tt>=Int</tt> (1; 100%).

`PRON` tokens may have the following values of `ExtPos`:

* `ADV` (1; 100% of non-empty `ExtPos`): ما

