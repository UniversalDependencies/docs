---
layout: base
title:  'Statistics of AdvType in UD_Latin-ITTB'
udver: '2'
---

## Treebank Statistics: UD_Latin-ITTB: Features: `AdvType`

This feature is language-specific.
It occurs with 2 different values: `Loc`, `Tim`.

4423 tokens (1%) have a non-empty value of `AdvType`.
33 types (0%) occur at least once with a non-empty value of `AdvType`.
33 lemmas (1%) occur at least once with a non-empty value of `AdvType`.
The feature is used with 1 part-of-speech tags: <tt><a href="la_ittb-pos-ADV.html">ADV</a></tt> (4423; 1% instances).

### `ADV`

4423 <tt><a href="la_ittb-pos-ADV.html">ADV</a></tt> tokens (20% of all `ADV` tokens) have a non-empty value of `AdvType`.

The most frequent other feature values with which `ADV` and `AdvType` co-occurred: <tt><a href="la_ittb-feat-Compound.html">Compound</a></tt><tt>=EMPTY</tt> (4371; 99%), <tt><a href="la_ittb-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (2780; 63%).

`ADV` tokens may have the following values of `AdvType`:

* `Loc` (2651; 60% of non-empty `AdvType`): <em>ergo, unde, hinc, ibi, inde, ubi, ibidem, ubique, hic, illuc</em>
* `Tim` (1772; 40% of non-empty `AdvType`): <em>adhuc, semper, iam, nunc, iterum, tunc, quandoque, prius, postea, ante</em>

`AdvType` seems to be **lexical feature** of `ADV`. 100% lemmas (33) occur only with one value of `AdvType`.

## Relations with Agreement in `AdvType`

The 10 most frequent relations where parent and child node agree in `AdvType`:
<tt>ADV --[<tt><a href="la_ittb-dep-discourse.html">discourse</a></tt>]--> ADV</tt> (2; 67%).

