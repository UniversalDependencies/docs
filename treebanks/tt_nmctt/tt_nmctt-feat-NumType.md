---
layout: base
title:  'Statistics of NumType in UD_Tatar-NMCTT'
udver: '2'
---

## Treebank Statistics: UD_Tatar-NMCTT: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

19 tokens (1%) have a non-empty value of `NumType`.
17 types (2%) occur at least once with a non-empty value of `NumType`.
17 lemmas (3%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="tt_nmctt-pos-NUM.html">NUM</a></tt> (19; 1% instances).

### `NUM`

19 <tt><a href="tt_nmctt-pos-NUM.html">NUM</a></tt> tokens (66% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (15; 79% of non-empty `NumType`): <em>бер, 1, 11, 11318, 2022, 23, 2500, 27, 5, 500</em>
* `Ord` (4; 21% of non-empty `NumType`): <em>10, 1942, 27, 28</em>
* `EMPTY` (10): <em>15, 20, 11, 163, 26, 5, 6, 7</em>

<table>
  <tr><th>Paradigm <i>27</i></th><th><tt>Card</tt></th><th><tt>Ord</tt></th></tr>
  <tr><td><tt></tt></td><td><em>27</em></td><td><em>27</em></td></tr>
</table>

`NumType` seems to be **lexical feature** of `NUM`. 94% lemmas (16) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="tt_nmctt-dep-flat.html">flat</a></tt>]--> NUM</tt> (1; 100%).

