---
layout: base
title:  'Statistics of ExtPos in UD_German-PUD'
udver: '2'
---

## Treebank Statistics: UD_German-PUD: Features: `ExtPos`

This feature is language-specific.
It occurs with 1 different values: `ADV`.

3 tokens (0%) have a non-empty value of `ExtPos`.
2 types (0%) occur at least once with a non-empty value of `ExtPos`.
2 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 1 part-of-speech tags: <tt><a href="de_pud-pos-ADV.html">ADV</a></tt> (3; 0% instances).

### `ADV`

3 <tt><a href="de_pud-pos-ADV.html">ADV</a></tt> tokens (0% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="de_pud-feat-Degree.html">Degree</a></tt><tt>=Pos</tt> (3; 100%).

`ADV` tokens may have the following values of `ExtPos`:

* `ADV` (3; 100% of non-empty `ExtPos`): <em>mehr, so</em>

