---
layout: base
title:  'Statistics of Polarity in UD_Vietnamese-TueCL'
udver: '2'
---

## Treebank Statistics: UD_Vietnamese-TueCL: Features: `Polarity`

This feature is universal.
It occurs with 2 different values: `Neg`, `Pos`.

42 tokens (2%) have a non-empty value of `Polarity`.
11 types (2%) occur at least once with a non-empty value of `Polarity`.
9 lemmas (1%) occur at least once with a non-empty value of `Polarity`.
The feature is used with 5 part-of-speech tags: <tt><a href="vi_tuecl-pos-ADV.html">ADV</a></tt> (34; 2% instances), <tt><a href="vi_tuecl-pos-AUX.html">AUX</a></tt> (3; 0% instances), <tt><a href="vi_tuecl-pos-PART.html">PART</a></tt> (3; 0% instances), <tt><a href="vi_tuecl-pos-ADJ.html">ADJ</a></tt> (1; 0% instances), <tt><a href="vi_tuecl-pos-INTJ.html">INTJ</a></tt> (1; 0% instances).

### `ADV`

34 <tt><a href="vi_tuecl-pos-ADV.html">ADV</a></tt> tokens (18% of all `ADV` tokens) have a non-empty value of `Polarity`.

The most frequent other feature values with which `ADV` and `Polarity` co-occurred: <tt><a href="vi_tuecl-feat-Tense.html">Tense</a></tt><tt>=EMPTY</tt> (34; 100%), <tt><a href="vi_tuecl-feat-AdvType.html">AdvType</a></tt><tt>=EMPTY</tt> (32; 94%).

`ADV` tokens may have the following values of `Polarity`:

* `Neg` (34; 100% of non-empty `Polarity`): <em>không, chưa, chẳng, đừng, ko</em>
* `EMPTY` (153): <em>đã, sẽ, chỉ, khi, cũng, nhất, đang, đều, hãy, ngay</em>

### `AUX`

3 <tt><a href="vi_tuecl-pos-AUX.html">AUX</a></tt> tokens (4% of all `AUX` tokens) have a non-empty value of `Polarity`.

The most frequent other feature values with which `AUX` and `Polarity` co-occurred: <tt><a href="vi_tuecl-feat-Mood.html">Mood</a></tt><tt>=Pot</tt> (3; 100%), <tt><a href="vi_tuecl-feat-VerbType.html">VerbType</a></tt><tt>=Mod</tt> (3; 100%), <tt><a href="vi_tuecl-feat-Voice.html">Voice</a></tt><tt>=EMPTY</tt> (3; 100%).

`AUX` tokens may have the following values of `Polarity`:

* `Neg` (3; 100% of non-empty `Polarity`): <em>không thể</em>
* `EMPTY` (80): <em>là, được, phải, có thể, bị, có vẻ, nghĩa là</em>

### `PART`

3 <tt><a href="vi_tuecl-pos-PART.html">PART</a></tt> tokens (16% of all `PART` tokens) have a non-empty value of `Polarity`.

`PART` tokens may have the following values of `Polarity`:

* `Neg` (2; 67% of non-empty `Polarity`): <em>hề, phi</em>
* `Pos` (1; 33% of non-empty `Polarity`): <em>phải</em>
* `EMPTY` (16): <em>vậy, đấy, Bộ, Mà, chứ, nhé, ra, thi, thì, tụi</em>

### `ADJ`

1 <tt><a href="vi_tuecl-pos-ADJ.html">ADJ</a></tt> tokens (1% of all `ADJ` tokens) have a non-empty value of `Polarity`.

`ADJ` tokens may have the following values of `Polarity`:

* `Neg` (1; 100% of non-empty `Polarity`): <em>không thể</em>
* `EMPTY` (81): <em>khác, khác nhau, mới, phải, lớn, từ bi, tự do, đen, Anh, an toàn</em>

### `INTJ`

1 <tt><a href="vi_tuecl-pos-INTJ.html">INTJ</a></tt> tokens (25% of all `INTJ` tokens) have a non-empty value of `Polarity`.

The most frequent other feature values with which `INTJ` and `Polarity` co-occurred: <tt><a href="vi_tuecl-feat-Foreign.html">Foreign</a></tt><tt>=EMPTY</tt> (1; 100%).

`INTJ` tokens may have the following values of `Polarity`:

* `Neg` (1; 100% of non-empty `Polarity`): <em>không</em>
* `EMPTY` (3): <em>Dù sao, Rồi, Yeah</em>

