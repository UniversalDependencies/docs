---
layout: base
title:  'Statistics of PunctType in UD_Polish-MPDT'
udver: '2'
---

## Treebank Statistics: UD_Polish-MPDT: Features: `PunctType`

This feature is language-specific.
It occurs with 11 different values: `Brck`, `Colo`, `Comm`, `Dash`, `Elip`, `Excl`, `Peri`, `Qest`, `Quot`, `Semi`, `Slsh`.

7959 tokens (17%) have a non-empty value of `PunctType`.
18 types (0%) occur at least once with a non-empty value of `PunctType`.
18 lemmas (0%) occur at least once with a non-empty value of `PunctType`.
The feature is used with 1 part-of-speech tags: <tt><a href="pl_mpdt-pos-PUNCT.html">PUNCT</a></tt> (7959; 17% instances).

### `PUNCT`

7959 <tt><a href="pl_mpdt-pos-PUNCT.html">PUNCT</a></tt> tokens (100% of all `PUNCT` tokens) have a non-empty value of `PunctType`.

`PUNCT` tokens may have the following values of `PunctType`:

* `Brck` (105; 1% of non-empty `PunctType`): <em>), (</em>
* `Colo` (332; 4% of non-empty `PunctType`): <em>:</em>
* `Comm` (3430; 43% of non-empty `PunctType`): <em>,</em>
* `Dash` (57; 1% of non-empty `PunctType`): <em>-, —, –</em>
* `Elip` (1; 0% of non-empty `PunctType`): <em>…</em>
* `Excl` (52; 1% of non-empty `PunctType`): <em>!</em>
* `Peri` (2532; 32% of non-empty `PunctType`): <em>.</em>
* `Qest` (115; 1% of non-empty `PunctType`): <em>?</em>
* `Quot` (29; 0% of non-empty `PunctType`): <em>", », ”, «, „</em>
* `Semi` (206; 3% of non-empty `PunctType`): <em>;</em>
* `Slsh` (1100; 14% of non-empty `PunctType`): <em>/</em>

`PunctType` seems to be **lexical feature** of `PUNCT`. 100% lemmas (18) occur only with one value of `PunctType`.

## Relations with Agreement in `PunctType`

The 10 most frequent relations where parent and child node agree in `PunctType`:
<tt>PUNCT --[<tt><a href="pl_mpdt-dep-punct.html">punct</a></tt>]--> PUNCT</tt> (37; 86%).

