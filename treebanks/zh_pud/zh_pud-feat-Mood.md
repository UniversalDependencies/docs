---
layout: base
title:  'Statistics of Mood in UD_Chinese-PUD'
udver: '2'
---

## Treebank Statistics: UD_Chinese-PUD: Features: `Mood`

This feature is universal but the values `Int` are language-specific.
It occurs with 1 different values: `Int`.

2 tokens (0%) have a non-empty value of `Mood`.
2 types (0%) occur at least once with a non-empty value of `Mood`.
2 lemmas (0%) occur at least once with a non-empty value of `Mood`.
The feature is used with 1 part-of-speech tags: <tt><a href="zh_pud-pos-AUX.html">AUX</a></tt> (2; 0% instances).

### `AUX`

2 <tt><a href="zh_pud-pos-AUX.html">AUX</a></tt> tokens (0% of all `AUX` tokens) have a non-empty value of `Mood`.

The most frequent other feature values with which `AUX` and `Mood` co-occurred: <tt><a href="zh_pud-feat-Aspect.html">Aspect</a></tt><tt>=EMPTY</tt> (2; 100%).

`AUX` tokens may have the following values of `Mood`:

* `Int` (2; 100% of non-empty `Mood`): 會不會、 要不要
* `EMPTY` (953): 了、 是、 被、 會、 著、 能、 可以、 為、 可能、 要

