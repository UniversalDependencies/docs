---
layout: base
title:  'Statistics of NumType in UD_Nayini-AHA'
udver: '2'
---

## Treebank Statistics: UD_Nayini-AHA: Features: `NumType`

This feature is universal.
It occurs with 1 different values: `Card`.

5 tokens (6%) have a non-empty value of `NumType`.
2 types (3%) occur at least once with a non-empty value of `NumType`.
2 lemmas (4%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="nyq_aha-pos-NUM.html">NUM</a></tt> (5; 6% instances).

### `NUM`

5 <tt><a href="nyq_aha-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (5; 100% of non-empty `NumType`): <em>دِه, یک</em>

