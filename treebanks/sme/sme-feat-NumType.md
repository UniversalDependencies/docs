---
layout: base
title:  'Statistics of NumType in UD_North_Sami'
udver: '2'
---

## Treebank Statistics: UD_North_Sami: Features: `NumType`

This feature is universal.
It occurs with 1 different values: `Card`.

130 tokens (1%) have a non-empty value of `NumType`.
75 types (2%) occur at least once with a non-empty value of `NumType`.
63 lemmas (2%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="sme-pos-NUM.html">NUM</a></tt> (130; 1% instances).

### `NUM`

130 <tt><a href="sme-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="sme-feat-Number.html">Number</a></tt><tt>=Sing</tt> (130; 100%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (130; 100% of non-empty `NumType`): guokte njeallje okta ovtta máŋga guovtti 12 20 golmma guoktenuppelot

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (63) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="sme-dep-conj.html">conj</a></tt>]--> NUM</tt> (1; 100%).

