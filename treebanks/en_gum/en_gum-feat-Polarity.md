---
layout: base
title:  'Statistics of Polarity in UD_English-GUM'
udver: '2'
---

## Treebank Statistics: UD_English-GUM: Features: `Polarity`

This feature is universal.
It occurs with 1 different values: `Neg`.

866 tokens (1%) have a non-empty value of `Polarity`.
136 types (1%) occur at least once with a non-empty value of `Polarity`.
123 lemmas (1%) occur at least once with a non-empty value of `Polarity`.
The feature is used with 7 part-of-speech tags: <tt><a href="en_gum-pos-PART.html">PART</a></tt> (537; 1% instances), <tt><a href="en_gum-pos-ADJ.html">ADJ</a></tt> (132; 0% instances), <tt><a href="en_gum-pos-DET.html">DET</a></tt> (78; 0% instances), <tt><a href="en_gum-pos-ADV.html">ADV</a></tt> (53; 0% instances), <tt><a href="en_gum-pos-VERB.html">VERB</a></tt> (37; 0% instances), <tt><a href="en_gum-pos-NOUN.html">NOUN</a></tt> (27; 0% instances), <tt><a href="en_gum-pos-PROPN.html">PROPN</a></tt> (2; 0% instances).

### `PART`

537 <tt><a href="en_gum-pos-PART.html">PART</a></tt> tokens (24% of all `PART` tokens) have a non-empty value of `Polarity`.

`PART` tokens may have the following values of `Polarity`:

* `Neg` (537; 100% of non-empty `Polarity`): <em>not, n't, n’t, ’t</em>
* `EMPTY` (1736): <em>to, 's, ’s, ', n’t, ’, not, n`t, s</em>

### `ADJ`

132 <tt><a href="en_gum-pos-ADJ.html">ADJ</a></tt> tokens (2% of all `ADJ` tokens) have a non-empty value of `Polarity`.

The most frequent other feature values with which `ADJ` and `Polarity` co-occurred: <tt><a href="en_gum-feat-Degree.html">Degree</a></tt><tt>=Pos</tt> (132; 100%).

`ADJ` tokens may have the following values of `Polarity`:

* `Neg` (132; 100% of non-empty `Polarity`): <em>unlikely, unknown, impossible, unable, unprecedented, incomprehensible, inevitable, unfamiliar, inadequate, non-profit</em>
* `EMPTY` (6396): <em>other, many, first, new, more, good, such, own, most, different</em>

`Polarity` seems to be **lexical feature** of `ADJ`. 100% lemmas (87) occur only with one value of `Polarity`.

### `DET`

78 <tt><a href="en_gum-pos-DET.html">DET</a></tt> tokens (1% of all `DET` tokens) have a non-empty value of `Polarity`.

The most frequent other feature values with which `DET` and `Polarity` co-occurred: <tt><a href="en_gum-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (78; 100%), <tt><a href="en_gum-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (78; 100%).

`DET` tokens may have the following values of `Polarity`:

* `Neg` (78; 100% of non-empty `Polarity`): <em>no</em>
* `EMPTY` (8148): <em>the, a, an, this, some, these, all, that, any, each</em>

### `ADV`

53 <tt><a href="en_gum-pos-ADV.html">ADV</a></tt> tokens (1% of all `ADV` tokens) have a non-empty value of `Polarity`.

`ADV` tokens may have the following values of `Polarity`:

* `Neg` (53; 100% of non-empty `Polarity`): <em>never, no, Unfortunately, nowhere, indirectly, Ne, pas, unambiguously, unawares, uncomprehendingly</em>
* `EMPTY` (3606): <em>also, so, then, just, more, as, very, even, well, most</em>

`Polarity` seems to be **lexical feature** of `ADV`. 100% lemmas (12) occur only with one value of `Polarity`.

### `VERB`

37 <tt><a href="en_gum-pos-VERB.html">VERB</a></tt> tokens (0% of all `VERB` tokens) have a non-empty value of `Polarity`.

The most frequent other feature values with which `VERB` and `Polarity` co-occurred: <tt><a href="en_gum-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (22; 59%), <tt><a href="en_gum-feat-Tense.html">Tense</a></tt><tt>=Past</tt> (22; 59%).

`VERB` tokens may have the following values of `Polarity`:

* `Neg` (37; 100% of non-empty `Polarity`): <em>involved, involve, involving, dislike, disliked, dismounted, undo, Uncover, disbanded, disentangle</em>
* `EMPTY` (10378): <em>have, make, said, get, see, are, is, want, do, know</em>

`Polarity` seems to be **lexical feature** of `VERB`. 100% lemmas (11) occur only with one value of `Polarity`.

### `NOUN`

27 <tt><a href="en_gum-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `Polarity`.

The most frequent other feature values with which `NOUN` and `Polarity` co-occurred: <tt><a href="en_gum-feat-Number.html">Number</a></tt><tt>=Sing</tt> (22; 81%).

`NOUN` tokens may have the following values of `Polarity`:

* `Neg` (27; 100% of non-empty `Polarity`): <em>nothing, involvement, disadvantages, discomfort, dislikes, disrespect, dysphoria, inadequacies, intolerance, involvements</em>
* `EMPTY` (17814): <em>people, time, city, years, way, language, world, something, research, work</em>

`Polarity` seems to be **lexical feature** of `NOUN`. 100% lemmas (12) occur only with one value of `Polarity`.

### `PROPN`

2 <tt><a href="en_gum-pos-PROPN.html">PROPN</a></tt> tokens (0% of all `PROPN` tokens) have a non-empty value of `Polarity`.

The most frequent other feature values with which `PROPN` and `Polarity` co-occurred: <tt><a href="en_gum-feat-Number.html">Number</a></tt><tt>=Plur</tt> (2; 100%).

`PROPN` tokens may have the following values of `Polarity`:

* `Neg` (2; 100% of non-empty `Polarity`): <em>Immortals</em>
* `EMPTY` (7252): <em>New, United, University, Scientology, States, York, lee, Party, fort, September</em>

## Relations with Agreement in `Polarity`

The 10 most frequent relations where parent and child node agree in `Polarity`:
<tt>NOUN --[<tt><a href="en_gum-dep-appos.html">appos</a></tt>]--> NOUN</tt> (1; 100%).

