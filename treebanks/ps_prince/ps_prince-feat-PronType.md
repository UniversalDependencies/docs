---
layout: base
title:  'Statistics of PronType in UD_Pashto-Prince'
udver: '2'
---

## Treebank Statistics: UD_Pashto-Prince: Features: `PronType`

This feature is universal.
It occurs with 7 different values: `Dem`, `Ind`, `Int`, `Neg`, `Prs`, `Rel`, `Tot`.

169 tokens (14%) have a non-empty value of `PronType`.
45 types (9%) occur at least once with a non-empty value of `PronType`.
28 lemmas (8%) occur at least once with a non-empty value of `PronType`.
The feature is used with 2 part-of-speech tags: <tt><a href="ps_prince-pos-PRON.html">PRON</a></tt> (119; 10% instances), <tt><a href="ps_prince-pos-DET.html">DET</a></tt> (50; 4% instances).

### `PRON`

119 <tt><a href="ps_prince-pos-PRON.html">PRON</a></tt> tokens (98% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="ps_prince-feat-Deixis.html">Deixis</a></tt><tt>=EMPTY</tt> (102; 86%), <tt><a href="ps_prince-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (95; 80%), <tt><a href="ps_prince-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (92; 77%), <tt><a href="ps_prince-feat-Number.html">Number</a></tt><tt>=Sing</tt> (78; 66%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (20; 17% of non-empty `PronType`): <em>دا, دې, هغه, هغې</em>
* `Ind` (5; 4% of non-empty `PronType`): <em>څوک, څه</em>
* `Int` (6; 5% of non-empty `PronType`): <em>څه, څوک</em>
* `Prs` (85; 71% of non-empty `PronType`): <em>ما, زما, زه, یې, مو, مې, خپل, دوی, هغه, ورته</em>
* `Rel` (2; 2% of non-empty `PronType`): <em>چا, کوم</em>
* `Tot` (1; 1% of non-empty `PronType`): <em>ټولو</em>
* `EMPTY` (3): <em>تاسو, ما, ماته</em>

### `DET`

50 <tt><a href="ps_prince-pos-DET.html">DET</a></tt> tokens (96% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="ps_prince-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (44; 88%), <tt><a href="ps_prince-feat-Reflex.html">Reflex</a></tt><tt>=EMPTY</tt> (44; 88%), <tt><a href="ps_prince-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (39; 78%).

`DET` tokens may have the following values of `PronType`:

* `Dem` (17; 34% of non-empty `PronType`): <em>دا, هغه, داسې, داگھہ, دغه</em>
* `Ind` (18; 36% of non-empty `PronType`): <em>يو, یو, يوه, یوه</em>
* `Neg` (2; 4% of non-empty `PronType`): <em>هېڅ</em>
* `Prs` (6; 12% of non-empty `PronType`): <em>خپله, خپل, خپلو</em>
* `Rel` (2; 4% of non-empty `PronType`): <em>څو</em>
* `Tot` (5; 10% of non-empty `PronType`): <em>هر, ټول</em>
* `EMPTY` (2): <em>يو, يوه</em>

`PronType` seems to be **lexical feature** of `DET`. 92% lemmas (12) occur only with one value of `PronType`.

