---
layout: base
title:  'Statistics of PronType in UD_Scottish_Gaelic-ARCOSG'
udver: '2'
---

## Treebank Statistics: UD_Scottish_Gaelic-ARCOSG: Features: `PronType`

This feature is universal.
It occurs with 2 different values: `Int`, `Rel`.

2413 tokens (3%) have a non-empty value of `PronType`.
40 types (1%) occur at least once with a non-empty value of `PronType`.
32 lemmas (1%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="gd_arcosg-pos-PART.html">PART</a></tt> (2091; 3% instances), <tt><a href="gd_arcosg-pos-PRON.html">PRON</a></tt> (258; 0% instances), <tt><a href="gd_arcosg-pos-AUX.html">AUX</a></tt> (64; 0% instances).

### `PART`

2091 <tt><a href="gd_arcosg-pos-PART.html">PART</a></tt> tokens (30% of all `PART` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PART` and `PronType` co-occurred: <tt><a href="gd_arcosg-feat-PartType.html">PartType</a></tt><tt>=Vb</tt> (2091; 100%), <tt><a href="gd_arcosg-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (2034; 97%).

`PART` tokens may have the following values of `PronType`:

* `Int` (363; 17% of non-empty `PronType`): <em>an, a, am, 'm, na, 'n, ‘n</em>
* `Rel` (1728; 83% of non-empty `PronType`): <em>a, nach, na, a'</em>
* `EMPTY` (4963): <em>a', a, gu, ag, cha, air, gun, chan, nach, do</em>

<table>
  <tr><th>Paradigm <i>a</i></th><th><tt>Int</tt></th><th><tt>Rel</tt></th></tr>
  <tr><td><tt></tt></td><td><em>a</em></td><td><em>a</em></td></tr>
</table>

### `PRON`

258 <tt><a href="gd_arcosg-pos-PRON.html">PRON</a></tt> tokens (3% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="gd_arcosg-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (258; 100%), <tt><a href="gd_arcosg-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (258; 100%), <tt><a href="gd_arcosg-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (258; 100%).

`PRON` tokens may have the following values of `PronType`:

* `Int` (258; 100% of non-empty `PronType`): <em>dè, cò, dé, gu, có, mheud, co, cà, mar, Ge</em>
* `EMPTY` (7367): <em>e, iad, i, mi, a, thu, sin, sinn, esan, fhèin</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (23) occur only with one value of `PronType`.

### `AUX`

64 <tt><a href="gd_arcosg-pos-AUX.html">AUX</a></tt> tokens (7% of all `AUX` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `AUX` and `PronType` co-occurred: <tt><a href="gd_arcosg-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (64; 100%), <tt><a href="gd_arcosg-feat-Tense.html">Tense</a></tt><tt>=Pres</tt> (64; 100%).

`AUX` tokens may have the following values of `PronType`:

* `Rel` (64; 100% of non-empty `PronType`): <em>as, is</em>
* `EMPTY` (869): <em>'s, is, b', gur, bu, chan, an, nach, b’, cha</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="gd_arcosg-dep-fixed.html">fixed</a></tt>]--> PRON</tt> (25; 100%),
<tt>PRON --[<tt><a href="gd_arcosg-dep-reparandum.html">reparandum</a></tt>]--> PRON</tt> (1; 100%).

