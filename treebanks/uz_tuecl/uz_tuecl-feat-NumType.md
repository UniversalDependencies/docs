---
layout: base
title:  'Statistics of NumType in UD_Uzbek-TueCL'
udver: '2'
---

## Treebank Statistics: UD_Uzbek-TueCL: Features: `NumType`

This feature is universal.
It occurs with 1 different values: `Card`.

5 tokens (1%) have a non-empty value of `NumType`.
4 types (1%) occur at least once with a non-empty value of `NumType`.
3 lemmas (1%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="uz_tuecl-pos-NUM.html">NUM</a></tt> (5; 1% instances).

### `NUM`

5 <tt><a href="uz_tuecl-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (5; 100% of non-empty `NumType`): <em>uch, beshta, ikki, uchta</em>

