---
layout: base
title:  'Statistics of Definite in UD_Zaar-Autogramm'
udver: '2'
---

## Treebank Statistics: UD_Zaar-Autogramm: Features: `Definite`

This feature is universal but the values `Cons`, `Spec` are language-specific.
It occurs with 3 different values: `Cons`, `Ind`, `Spec`.

88 tokens (1%) have a non-empty value of `Definite`.
35 types (3%) occur at least once with a non-empty value of `Definite`.
36 lemmas (4%) occur at least once with a non-empty value of `Definite`.
The feature is used with 4 part-of-speech tags: <tt><a href="say_autogramm-pos-NOUN.html">NOUN</a></tt> (59; 1% instances), <tt><a href="say_autogramm-pos-DET.html">DET</a></tt> (22; 0% instances), <tt><a href="say_autogramm-pos-ADJ.html">ADJ</a></tt> (6; 0% instances), <tt><a href="say_autogramm-pos-PRON.html">PRON</a></tt> (1; 0% instances).

### `NOUN`

59 <tt><a href="say_autogramm-pos-NOUN.html">NOUN</a></tt> tokens (8% of all `NOUN` tokens) have a non-empty value of `Definite`.

`NOUN` tokens may have the following values of `Definite`:

* `Cons` (41; 69% of non-empty `Definite`): <em>lǎː, məːrí, zǎːr, ɮǐː, dondə́, kətə́r, gút, awré, gàrí, kafá</em>
* `Ind` (18; 31% of non-empty `Definite`): <em>də̀nì, màːʃíni, námʧi, náɣɗi, sə̀kéːɗi, gə̀ɗə̌, tə́pi, vwàːy, vìːnì, ŋây</em>
* `EMPTY` (639): <em>laː, də̀n, dzàn, mə́n, lə̂p, vìː, dzàŋ, ngə́tn, ŋáː, gə̀t</em>

<table>
  <tr><th>Paradigm <i>màːʃîn</i></th><th><tt>Ind</tt></th><th><tt>Cons</tt></th></tr>
  <tr><td><tt></tt></td><td><em>màːʃíni</em></td><td><em>màːʃîn</em></td></tr>
</table>

`Definite` seems to be **lexical feature** of `NOUN`. 97% lemmas (30) occur only with one value of `Definite`.

### `DET`

22 <tt><a href="say_autogramm-pos-DET.html">DET</a></tt> tokens (24% of all `DET` tokens) have a non-empty value of `Definite`.

The most frequent other feature values with which `DET` and `Definite` co-occurred: <tt><a href="say_autogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (22; 100%), <tt><a href="say_autogramm-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (22; 100%).

`DET` tokens may have the following values of `Definite`:

* `Spec` (22; 100% of non-empty `Definite`): <em>wón, gón, gòn</em>
* `EMPTY` (71): <em>gyaː, gyáː, gíː, gyǎː, gyòː, kotá, XX, dúk, sú, wannan</em>

### `ADJ`

6 <tt><a href="say_autogramm-pos-ADJ.html">ADJ</a></tt> tokens (12% of all `ADJ` tokens) have a non-empty value of `Definite`.

`ADJ` tokens may have the following values of `Definite`:

* `Cons` (6; 100% of non-empty `Definite`): <em>mərə́, ŋǎː</em>
* `EMPTY` (44): <em>ŋaː, ʧólák, mari, nʤwâːtn, ŋǎː, ʒèlì, ʧolak, banza, bàptàk, mərə</em>

### `PRON`

1 <tt><a href="say_autogramm-pos-PRON.html">PRON</a></tt> tokens (0% of all `PRON` tokens) have a non-empty value of `Definite`.

The most frequent other feature values with which `PRON` and `Definite` co-occurred: <tt><a href="say_autogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (1; 100%), <tt><a href="say_autogramm-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (1; 100%), <tt><a href="say_autogramm-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (1; 100%).

`PRON` tokens may have the following values of `Definite`:

* `Spec` (1; 100% of non-empty `Definite`): <em>gón</em>
* `EMPTY` (560): <em>=tə, =mí, =ʃí, ɣáy, =m, =tə̀, gòs, myâːn, =ɣə, ʧì</em>

