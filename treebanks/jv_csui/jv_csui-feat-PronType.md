---
layout: base
title:  'Statistics of PronType in UD_Javanese-CSUI'
udver: '2'
---

## Treebank Statistics: UD_Javanese-CSUI: Features: `PronType`

This feature is universal but the values `Emp` are language-specific.
It occurs with 8 different values: `Art`, `Dem`, `Emp`, `Ind`, `Int`, `Prs`, `Rel`, `Tot`.

184 tokens (9%) have a non-empty value of `PronType`.
51 types (5%) occur at least once with a non-empty value of `PronType`.
41 lemmas (4%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="jv_csui-pos-PRON.html">PRON</a></tt> (110; 5% instances), <tt><a href="jv_csui-pos-DET.html">DET</a></tt> (70; 3% instances), <tt><a href="jv_csui-pos-ADV.html">ADV</a></tt> (4; 0% instances).

### `PRON`

110 <tt><a href="jv_csui-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="jv_csui-feat-Number.html">Number</a></tt><tt>=Sing</tt> (56; 51%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (7; 6% of non-empty `PronType`): <em>gene, ngene, Iki, Punika, iku, kana, kuwi</em>
* `Prs` (57; 52% of non-empty `PronType`): <em>ipun, é, piyambakipun, né, dhèwèké, panjenenganipun, aku, nipun, Piyambaké, Sira</em>
* `Rel` (44; 40% of non-empty `PronType`): <em>ingkang, kang, apa, apa-apa, sing</em>
* `Tot` (2; 2% of non-empty `PronType`): <em>kabèh</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (25) occur only with one value of `PronType`.

### `DET`

70 <tt><a href="jv_csui-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="jv_csui-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (58; 83%), <tt><a href="jv_csui-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (52; 74%).

`DET` tokens may have the following values of `PronType`:

* `Art` (18; 26% of non-empty `PronType`): <em>é, ipun, sawijining, nipun, satunggaling, né, sing</em>
* `Dem` (36; 51% of non-empty `PronType`): <em>punika, iki, mau, iku, kana, semana</em>
* `Emp` (1; 1% of non-empty `PronType`): <em>piyambak</em>
* `Ind` (12; 17% of non-empty `PronType`): <em>para, akèh, sapérangan, Akèh-akèhe, Pira-pira, pinten-pinten</em>
* `Tot` (3; 4% of non-empty `PronType`): <em>sedaya, Saben</em>

`PronType` seems to be **lexical feature** of `DET`. 100% lemmas (21) occur only with one value of `PronType`.

### `ADV`

4 <tt><a href="jv_csui-pos-ADV.html">ADV</a></tt> tokens (6% of all `ADV` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADV` and `PronType` co-occurred: <tt><a href="jv_csui-feat-Polite.html">Polite</a></tt><tt>=EMPTY</tt> (3; 75%).

`ADV` tokens may have the following values of `PronType`:

* `Int` (4; 100% of non-empty `PronType`): <em>Kena, sepira</em>
* `EMPTY` (61): <em>paling, lajeng, uga, ugi, banjur, kadang, kurang, malih, mligi, namung</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="jv_csui-dep-nmod-poss.html">nmod:poss</a></tt>]--> PRON</tt> (1; 100%).

