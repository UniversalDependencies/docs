---
layout: base
title:  'Statistics of ExtPos in UD_Turkish-Atis'
udver: '2'
---

## Treebank Statistics: UD_Turkish-Atis: Features: `ExtPos`

This feature is language-specific.
It occurs with 2 different values: `ADJ`, `ADV`.

9 tokens (0%) have a non-empty value of `ExtPos`.
3 types (0%) occur at least once with a non-empty value of `ExtPos`.
3 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 2 part-of-speech tags: <tt><a href="tr_atis-pos-ADJ.html">ADJ</a></tt> (5; 0% instances), <tt><a href="tr_atis-pos-ADV.html">ADV</a></tt> (4; 0% instances).

### `ADJ`

5 <tt><a href="tr_atis-pos-ADJ.html">ADJ</a></tt> tokens (0% of all `ADJ` tokens) have a non-empty value of `ExtPos`.

`ADJ` tokens may have the following values of `ExtPos`:

* `ADJ` (5; 100% of non-empty `ExtPos`): <em>herhangi, tek</em>

### `ADV`

4 <tt><a href="tr_atis-pos-ADV.html">ADV</a></tt> tokens (0% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="tr_atis-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (4; 100%).

`ADV` tokens may have the following values of `ExtPos`:

* `ADV` (4; 100% of non-empty `ExtPos`): <em>Peki</em>

