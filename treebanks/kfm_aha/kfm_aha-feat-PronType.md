---
layout: base
title:  'Statistics of PronType in UD_Khunsari-AHA'
udver: '2'
---

## Treebank Statistics: UD_Khunsari-AHA: Features: `PronType`

This feature is universal.
It occurs with 2 different values: `Dem`, `Prs`.

8 tokens (11%) have a non-empty value of `PronType`.
7 types (12%) occur at least once with a non-empty value of `PronType`.
7 lemmas (13%) occur at least once with a non-empty value of `PronType`.
The feature is used with 1 part-of-speech tags: <tt><a href="kfm_aha-pos-PRON.html">PRON</a></tt> (8; 11% instances).

### `PRON`

8 <tt><a href="kfm_aha-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="kfm_aha-feat-Number.html">Number</a></tt><tt>=Sing</tt> (8; 100%), <tt><a href="kfm_aha-feat-Person.html">Person</a></tt><tt>=1</tt> (5; 63%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (1; 13% of non-empty `PronType`): <em>مون</em>
* `Prs` (7; 88% of non-empty `PronType`): <em>م, او, ش, ما, مُن, نا</em>

