---
layout: base
title:  'Statistics of Degree in UD_Veps-VWT'
udver: '2'
---

## Treebank Statistics: UD_Veps-VWT: Features: `Degree`

This feature is universal.
It occurs with 2 different values: `Cmp`, `Pos`.

54 tokens (4%) have a non-empty value of `Degree`.
43 types (7%) occur at least once with a non-empty value of `Degree`.
33 lemmas (9%) occur at least once with a non-empty value of `Degree`.
The feature is used with 1 part-of-speech tags: <tt><a href="vep_vwt-pos-ADJ.html">ADJ</a></tt> (54; 4% instances).

### `ADJ`

54 <tt><a href="vep_vwt-pos-ADJ.html">ADJ</a></tt> tokens (69% of all `ADJ` tokens) have a non-empty value of `Degree`.

The most frequent other feature values with which `ADJ` and `Degree` co-occurred: <tt><a href="vep_vwt-feat-Number.html">Number</a></tt><tt>=Sing</tt> (43; 80%).

`ADJ` tokens may have the following values of `Degree`:

* `Cmp` (1; 2% of non-empty `Degree`): <em>lämembad</em>
* `Pos` (53; 98% of non-empty `Degree`): <em>hüvä, surel, čomal, erazvuiččed, jüged, kebn, tärged, äi, čoma, aktivižen</em>
* `EMPTY` (24): <em>vepsän, ezmäižid, toižen, vepsläine, vepsläižes, vepsläižid, vepsläšt</em>

`Degree` seems to be **lexical feature** of `ADJ`. 100% lemmas (33) occur only with one value of `Degree`.

## Relations with Agreement in `Degree`

The 10 most frequent relations where parent and child node agree in `Degree`:
<tt>ADJ --[<tt><a href="vep_vwt-dep-conj.html">conj</a></tt>]--> ADJ</tt> (5; 100%).

