---
layout: base
title:  'Statistics of PronType in UD_Assamese-AiW'
udver: '2'
---

## Treebank Statistics: UD_Assamese-AiW: Features: `PronType`

This feature is universal.
It occurs with 5 different values: `Dem`, `Ind`, `Int`, `Prs`, `Tot`.

83 tokens (10%) have a non-empty value of `PronType`.
37 types (7%) occur at least once with a non-empty value of `PronType`.
24 lemmas (6%) occur at least once with a non-empty value of `PronType`.
The feature is used with 2 part-of-speech tags: <tt><a href="as_aiw-pos-PRON.html">PRON</a></tt> (58; 7% instances), <tt><a href="as_aiw-pos-DET.html">DET</a></tt> (25; 3% instances).

### `PRON`

58 <tt><a href="as_aiw-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="as_aiw-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (49; 84%), <tt><a href="as_aiw-feat-Number.html">Number</a></tt><tt>=Sing</tt> (39; 67%), <tt><a href="as_aiw-feat-Gender.html">Gender</a></tt><tt>=Fem</tt> (33; 57%), <tt><a href="as_aiw-feat-Person.html">Person</a></tt><tt>=3</tt> (31; 53%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (4; 7% of non-empty `PronType`): <em>এইজনী, সেইটো, সেয়ে, এইটো</em>
* `Ind` (5; 9% of non-empty `PronType`): <em>কোনো, একো, একোকে, একোটোকে</em>
* `Int` (4; 7% of non-empty `PronType`): <em>কি</em>
* `Prs` (44; 76% of non-empty `PronType`): <em>তাই, তাইৰ, মই, তাৰ, তই, তায়ো, তাহাঁতৰ, তেওঁ, নিজকে, মোক</em>
* `Tot` (1; 2% of non-empty `PronType`): <em>সকলোৱে</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (15) occur only with one value of `PronType`.

### `DET`

25 <tt><a href="as_aiw-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

`DET` tokens may have the following values of `PronType`:

* `Dem` (8; 32% of non-empty `PronType`): <em>এই, এনে, এনেকুৱা, এইসমূহ</em>
* `Ind` (9; 36% of non-empty `PronType`): <em>কোনো, একো, এটা, কিছুমান, কেতবোৰ, নানা</em>
* `Int` (5; 20% of non-empty `PronType`): <em>কিমান, কি, কিবা</em>
* `Tot` (3; 12% of non-empty `PronType`): <em>সকলো, প্রতিখন</em>

`PronType` seems to be **lexical feature** of `DET`. 100% lemmas (14) occur only with one value of `PronType`.

