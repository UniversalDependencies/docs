---
layout: base
title:  'Statistics of PartType in UD_Hausa-NorthernAutogramm'
udver: '2'
---

## Treebank Statistics: UD_Hausa-NorthernAutogramm: Features: `PartType`

This feature is language-specific.
It occurs with 4 different values: `Adv`, `Neg`, `Pred`, `Top`.

139 tokens (3%) have a non-empty value of `PartType`.
14 types (2%) occur at least once with a non-empty value of `PartType`.
12 lemmas (2%) occur at least once with a non-empty value of `PartType`.
The feature is used with 1 part-of-speech tags: <tt><a href="ha_northernautogramm-pos-PART.html">PART</a></tt> (139; 3% instances).

### `PART`

139 <tt><a href="ha_northernautogramm-pos-PART.html">PART</a></tt> tokens (100% of all `PART` tokens) have a non-empty value of `PartType`.

The most frequent other feature values with which `PART` and `PartType` co-occurred: <tt><a href="ha_northernautogramm-feat-Aspect.html">Aspect</a></tt><tt>=EMPTY</tt> (113; 81%), <tt><a href="ha_northernautogramm-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (89; 64%).

`PART` tokens may have the following values of `PartType`:

* `Adv` (26; 19% of non-empty `PartType`): <em>ta</em>
* `Neg` (25; 18% of non-empty `PartType`): <em>ba, bàː</em>
* `Pred` (60; 43% of non-empty `PartType`): <em>gàː, àkwai, baːbù, bâː, dà, zâː, à~</em>
* `Top` (28; 20% of non-empty `PartType`): <em>kòː, dai, kuma, hwa</em>

`PartType` seems to be **lexical feature** of `PART`. 100% lemmas (12) occur only with one value of `PartType`.

## Relations with Agreement in `PartType`

The 10 most frequent relations where parent and child node agree in `PartType`:
<tt>PART --[<tt><a href="ha_northernautogramm-dep-parataxis.html">parataxis</a></tt>]--> PART</tt> (4; 100%),
<tt>PART --[<tt><a href="ha_northernautogramm-dep-conj.html">conj</a></tt>]--> PART</tt> (2; 100%),
<tt>PART --[<tt><a href="ha_northernautogramm-dep-reparandum.html">reparandum</a></tt>]--> PART</tt> (2; 100%),
<tt>PART --[<tt><a href="ha_northernautogramm-dep-advmod.html">advmod</a></tt>]--> PART</tt> (1; 100%).

