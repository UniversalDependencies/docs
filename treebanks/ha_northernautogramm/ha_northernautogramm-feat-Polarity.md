---
layout: base
title:  'Statistics of Polarity in UD_Hausa-NorthernAutogramm'
udver: '2'
---

## Treebank Statistics: UD_Hausa-NorthernAutogramm: Features: `Polarity`

This feature is universal.
It occurs with 1 different values: `Neg`.

76 tokens (2%) have a non-empty value of `Polarity`.
15 types (2%) occur at least once with a non-empty value of `Polarity`.
5 lemmas (1%) occur at least once with a non-empty value of `Polarity`.
The feature is used with 2 part-of-speech tags: <tt><a href="ha_northernautogramm-pos-PART.html">PART</a></tt> (50; 1% instances), <tt><a href="ha_northernautogramm-pos-AUX.html">AUX</a></tt> (26; 1% instances).

### `PART`

50 <tt><a href="ha_northernautogramm-pos-PART.html">PART</a></tt> tokens (28% of all `PART` tokens) have a non-empty value of `Polarity`.

The most frequent other feature values with which `PART` and `Polarity` co-occurred: <tt><a href="ha_northernautogramm-feat-Aspect.html">Aspect</a></tt><tt>=EMPTY</tt> (50; 100%).

`PART` tokens may have the following values of `Polarity`:

* `Neg` (50; 100% of non-empty `Polarity`): <em>baːbù, ba, bâː, bàː</em>
* `EMPTY` (131): <em>ta, gàː, nàː, kòː, dai, àkwai, naː, mài, wai, dà</em>

### `AUX`

26 <tt><a href="ha_northernautogramm-pos-AUX.html">AUX</a></tt> tokens (4% of all `AUX` tokens) have a non-empty value of `Polarity`.

The most frequent other feature values with which `AUX` and `Polarity` co-occurred: <tt><a href="ha_northernautogramm-feat-Aspect.html">Aspect</a></tt><tt>=Perf</tt> (16; 62%), <tt><a href="ha_northernautogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (16; 62%).

`AUX` tokens may have the following values of `Polarity`:

* `Neg` (26; 100% of non-empty `Polarity`): <em>bài, baː'à, baːkà, bàkà, bàkù, baːmù, bàn, bàtà, baːkù, bàsù</em>
* `EMPTY` (664): <em>yac, ankà, sunkà, yaː, shinàː, kaː, shì, à, ìn, tac</em>

## Relations with Agreement in `Polarity`

The 10 most frequent relations where parent and child node agree in `Polarity`:
<tt>PART --[<tt><a href="ha_northernautogramm-dep-conj.html">conj</a></tt>]--> PART</tt> (5; 100%).

