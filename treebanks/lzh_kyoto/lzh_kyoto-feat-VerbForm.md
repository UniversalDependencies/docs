---
layout: base
title:  'Statistics of VerbForm in UD_Classical_Chinese-Kyoto'
udver: '2'
---

## Treebank Statistics: UD_Classical_Chinese-Kyoto: Features: `VerbForm`

This feature is universal but the values `Conv` are language-specific.
It occurs with 2 different values: `Conv`, `Part`.

19815 tokens (6%) have a non-empty value of `VerbForm`.
1649 types (18%) occur at least once with a non-empty value of `VerbForm`.
1623 lemmas (18%) occur at least once with a non-empty value of `VerbForm`.
The feature is used with 2 part-of-speech tags: <tt><a href="lzh_kyoto-pos-VERB.html">VERB</a></tt> (10157; 3% instances), <tt><a href="lzh_kyoto-pos-ADV.html">ADV</a></tt> (9658; 3% instances).

### `VERB`

10157 <tt><a href="lzh_kyoto-pos-VERB.html">VERB</a></tt> tokens (10% of all `VERB` tokens) have a non-empty value of `VerbForm`.

The most frequent other feature values with which `VERB` and `VerbForm` co-occurred: <tt><a href="lzh_kyoto-feat-Degree.html">Degree</a></tt><tt>=Pos</tt> (5866; 58%).

`VERB` tokens may have the following values of `VerbForm`:

* `Part` (10157; 100% of non-empty `VerbForm`): 大、 太、 主、 皇、 小、 有、 長、 以、 庶、 孝
* `EMPTY` (90637): 曰、 有、 以、 無、 至、 如、 立、 為、 在、 行

`VerbForm` seems to be **lexical feature** of `VERB`. 100% lemmas (1145) occur only with one value of `VerbForm`.

### `ADV`

9658 <tt><a href="lzh_kyoto-pos-ADV.html">ADV</a></tt> tokens (33% of all `ADV` tokens) have a non-empty value of `VerbForm`.

The most frequent other feature values with which `ADV` and `VerbForm` co-occurred: <tt><a href="lzh_kyoto-feat-AdvType.html">AdvType</a></tt><tt>=EMPTY</tt> (9658; 100%), <tt><a href="lzh_kyoto-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (9326; 97%), <tt><a href="lzh_kyoto-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (6220; 64%).

`ADV` tokens may have the following values of `VerbForm`:

* `Conv` (9658; 100% of non-empty `VerbForm`): 以、 大、 始、 無、 然、 當、 親、 凡、 對、 共
* `EMPTY` (19710): 不、 則、 皆、 必、 故、 亦、 未、 非、 乃、 相

`VerbForm` seems to be **lexical feature** of `ADV`. 100% lemmas (1092) occur only with one value of `VerbForm`.

