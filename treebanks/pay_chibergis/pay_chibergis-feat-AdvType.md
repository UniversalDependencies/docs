---
layout: base
title:  'Statistics of AdvType in UD_Pesh-ChibErgIS'
udver: '2'
---

## Treebank Statistics: UD_Pesh-ChibErgIS: Features: `AdvType`

This feature is language-specific.
It occurs with 1 different values: `Ideoph`.

11 tokens (0%) have a non-empty value of `AdvType`.
6 types (1%) occur at least once with a non-empty value of `AdvType`.
6 lemmas (1%) occur at least once with a non-empty value of `AdvType`.
The feature is used with 1 part-of-speech tags: <tt><a href="pay_chibergis-pos-ADV.html">ADV</a></tt> (11; 0% instances).

### `ADV`

11 <tt><a href="pay_chibergis-pos-ADV.html">ADV</a></tt> tokens (8% of all `ADV` tokens) have a non-empty value of `AdvType`.

`ADV` tokens may have the following values of `AdvType`:

* `Ideoph` (11; 100% of non-empty `AdvType`): <em>tõʃ, kluk, roh, teʔne, tukuluk, tʃas</em>

## Relations with Agreement in `AdvType`

The 10 most frequent relations where parent and child node agree in `AdvType`:
<tt>ADV --[<tt><a href="pay_chibergis-dep-conj.html">conj</a></tt>]--> ADV</tt> (6; 100%).

