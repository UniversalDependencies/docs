---
layout: base
title:  'Statistics of ExtPos in UD_Dutch-Alpino'
udver: '2'
---

## Treebank Statistics: UD_Dutch-Alpino: Features: `ExtPos`

This feature is language-specific.
It occurs with 8 different values: `ADJ`, `ADP`, `ADV`, `CCONJ`, `DET`, `PRON`, `PROPN`, `SCONJ`.

2173 tokens (1%) have a non-empty value of `ExtPos`.
442 types (2%) occur at least once with a non-empty value of `ExtPos`.
395 lemmas (2%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 14 part-of-speech tags: <tt><a href="nl_alpino-pos-ADP.html">ADP</a></tt> (944; 0% instances), <tt><a href="nl_alpino-pos-NUM.html">NUM</a></tt> (321; 0% instances), <tt><a href="nl_alpino-pos-SYM.html">SYM</a></tt> (170; 0% instances), <tt><a href="nl_alpino-pos-X.html">X</a></tt> (144; 0% instances), <tt><a href="nl_alpino-pos-DET.html">DET</a></tt> (140; 0% instances), <tt><a href="nl_alpino-pos-ADJ.html">ADJ</a></tt> (124; 0% instances), <tt><a href="nl_alpino-pos-NOUN.html">NOUN</a></tt> (108; 0% instances), <tt><a href="nl_alpino-pos-ADV.html">ADV</a></tt> (90; 0% instances), <tt><a href="nl_alpino-pos-PRON.html">PRON</a></tt> (67; 0% instances), <tt><a href="nl_alpino-pos-SCONJ.html">SCONJ</a></tt> (23; 0% instances), <tt><a href="nl_alpino-pos-PROPN.html">PROPN</a></tt> (16; 0% instances), <tt><a href="nl_alpino-pos-VERB.html">VERB</a></tt> (11; 0% instances), <tt><a href="nl_alpino-pos-CCONJ.html">CCONJ</a></tt> (10; 0% instances), <tt><a href="nl_alpino-pos-INTJ.html">INTJ</a></tt> (5; 0% instances).

### `ADP`

944 <tt><a href="nl_alpino-pos-ADP.html">ADP</a></tt> tokens (4% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADJ` (49; 5% of non-empty `ExtPos`): <em>in, op, aan, van, te, achter, ter, voor</em>
* `ADP` (654; 69% of non-empty `ExtPos`): <em>in, op, ten, van, met, ter, aan, tot, onder, naar</em>
* `ADV` (193; 20% of non-empty `ExtPos`): <em>voor, ten, onder, op, met, in, tot, bij, ter, af</em>
* `CCONJ` (5; 1% of non-empty `ExtPos`): <em>tot, Ten</em>
* `PRON` (2; 0% of non-empty `ExtPos`): <em>van</em>
* `PROPN` (5; 1% of non-empty `ExtPos`): <em>van, Achterlangs, Door, Op</em>
* `SCONJ` (36; 4% of non-empty `ExtPos`): <em>aan, voor, in, te</em>

<table>
  <tr><th>Paradigm <i>van</i></th><th><tt>ADJ</tt></th><th><tt>ADP</tt></th><th><tt>ADV</tt></th><th><tt>PRON</tt></th><th><tt>PROPN</tt></th></tr>
  <tr><td><tt></tt></td><td><em>van</em></td><td><em>van</em></td><td><em>van</em></td><td><em>van</em></td><td><em>van</em></td></tr>
</table>

### `NUM`

321 <tt><a href="nl_alpino-pos-NUM.html">NUM</a></tt> tokens (9% of all `NUM` tokens) have a non-empty value of `ExtPos`.

`NUM` tokens may have the following values of `ExtPos`:

* `ADP` (1; 0% of non-empty `ExtPos`): <em>comite-31</em>
* `PRON` (317; 99% of non-empty `ExtPos`): <em>1, 2, 3, 15, 12, 30, 8, een, 6, 10</em>
* `PROPN` (3; 1% of non-empty `ExtPos`): <em>Drie, Duizend, Vijf</em>

<table>
  <tr><th>Paradigm <i>drie</i></th><th><tt>PRON</tt></th><th><tt>PROPN</tt></th></tr>
  <tr><td><tt></tt></td><td><em>drie</em></td><td><em>Drie</em></td></tr>
</table>

`ExtPos` seems to be **lexical feature** of `NUM`. 97% lemmas (63) occur only with one value of `ExtPos`.

### `SYM`

170 <tt><a href="nl_alpino-pos-SYM.html">SYM</a></tt> tokens (31% of all `SYM` tokens) have a non-empty value of `ExtPos`.

`SYM` tokens may have the following values of `ExtPos`:

* `PRON` (170; 100% of non-empty `ExtPos`): <em>flo, -, 10.000,-, 130.000,-, 5,-, 1/2, 10,-, 100.000,-, 1500,-, 20.000,-</em>

`ExtPos` seems to be **lexical feature** of `SYM`. 100% lemmas (26) occur only with one value of `ExtPos`.

### `X`

144 <tt><a href="nl_alpino-pos-X.html">X</a></tt> tokens (15% of all `X` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `X` and `ExtPos` co-occurred: <tt><a href="nl_alpino-feat-Abbr.html">Abbr</a></tt><tt>=EMPTY</tt> (141; 98%), <tt><a href="nl_alpino-feat-Foreign.html">Foreign</a></tt><tt>=Yes</tt> (140; 97%).

`X` tokens may have the following values of `ExtPos`:

* `ADJ` (1; 1% of non-empty `ExtPos`): <em>total</em>
* `ADP` (5; 3% of non-empty `ExtPos`): <em>in, a, ad, uit</em>
* `ADV` (8; 6% of non-empty `ExtPos`): <em>nota, En, ad, con, im, linea, per</em>
* `CCONJ` (2; 1% of non-empty `ExtPos`): <em>t</em>
* `PROPN` (126; 88% of non-empty `ExtPos`): <em>the, Grand, Le, A, All, Caland-, Die, Financial, Guinness, I</em>
* `SCONJ` (2; 1% of non-empty `ExtPos`): <em>in</em>

<table>
  <tr><th>Paradigm <i>ad</i></th><th><tt>ADP</tt></th><th><tt>ADV</tt></th></tr>
  <tr><td><tt></tt></td><td><em>ad</em></td><td><em>ad</em></td></tr>
</table>

`ExtPos` seems to be **lexical feature** of `X`. 98% lemmas (106) occur only with one value of `ExtPos`.

### `DET`

140 <tt><a href="nl_alpino-pos-DET.html">DET</a></tt> tokens (1% of all `DET` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `DET` and `ExtPos` co-occurred: <tt><a href="nl_alpino-feat-PronType.html">PronType</a></tt><tt>=Art</tt> (136; 97%), <tt><a href="nl_alpino-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (126; 90%).

`DET` tokens may have the following values of `ExtPos`:

* `ADP` (17; 12% of non-empty `ExtPos`): <em>de, het, een</em>
* `ADV` (93; 66% of non-empty `ExtPos`): <em>het, 's, een, des, 't</em>
* `PRON` (7; 5% of non-empty `ExtPos`): <em>flo, dll, een, de</em>
* `PROPN` (23; 16% of non-empty `ExtPos`): <em>De, Het</em>

<table>
  <tr><th>Paradigm <i>de</i></th><th><tt>ADP</tt></th><th><tt>ADV</tt></th><th><tt>PRON</tt></th><th><tt>PROPN</tt></th></tr>
  <tr><td><tt></tt></td><td><em>de</em></td><td><em>'s, des</em></td><td><em>de</em></td><td><em>De</em></td></tr>
</table>

### `ADJ`

124 <tt><a href="nl_alpino-pos-ADJ.html">ADJ</a></tt> tokens (1% of all `ADJ` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADJ` and `ExtPos` co-occurred: <tt><a href="nl_alpino-feat-Degree.html">Degree</a></tt><tt>=Pos</tt> (117; 94%).

`ADJ` tokens may have the following values of `ExtPos`:

* `ADP` (9; 7% of non-empty `ExtPos`): <em>dicht, korte, makkelijk, nieuw, terwille</em>
* `ADV` (3; 2% of non-empty `ExtPos`): <em>rustig, geleidelijk</em>
* `PRON` (19; 15% of non-empty `ExtPos`): <em>half, Vierde</em>
* `PROPN` (93; 75% of non-empty `ExtPos`): <em>Integraal, Internationale, Binnenlandse, Eerste, Buitenlandse, Economische, Nederlandse, Nieuwe, Sociale, Algemene</em>

<table>
  <tr><th>Paradigm <i>nieuw</i></th><th><tt>ADP</tt></th><th><tt>PROPN</tt></th></tr>
  <tr><td><tt></tt></td><td><em>nieuw</em></td><td><em>Nieuwe</em></td></tr>
</table>

`ExtPos` seems to be **lexical feature** of `ADJ`. 96% lemmas (50) occur only with one value of `ExtPos`.

### `NOUN`

108 <tt><a href="nl_alpino-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `NOUN` and `ExtPos` co-occurred: <tt><a href="nl_alpino-feat-Number.html">Number</a></tt><tt>=Sing</tt> (100; 93%), <tt><a href="nl_alpino-feat-Gender.html">Gender</a></tt><tt>=Com</tt> (56; 52%).

`NOUN` tokens may have the following values of `ExtPos`:

* `ADP` (11; 10% of non-empty `ExtPos`): <em>dank, IJs, paal, preludia, relief</em>
* `ADV` (6; 6% of non-empty `ExtPos`): <em>hals, heden, keer, koste, stap, woord</em>
* `PRON` (10; 9% of non-empty `ExtPos`): <em>kwart, Watvoor</em>
* `PROPN` (80; 74% of non-empty `ExtPos`): <em>Basistakenpakket, Wet, Dm, Oom, verkeer, AVRO's, Akademie, Aktie, Bericht, Bestemming</em>
* `SCONJ` (1; 1% of non-empty `ExtPos`): <em>Inplaats</em>

`ExtPos` seems to be **lexical feature** of `NOUN`. 100% lemmas (76) occur only with one value of `ExtPos`.

### `ADV`

90 <tt><a href="nl_alpino-pos-ADV.html">ADV</a></tt> tokens (1% of all `ADV` tokens) have a non-empty value of `ExtPos`.

`ADV` tokens may have the following values of `ExtPos`:

* `ADJ` (2; 2% of non-empty `ExtPos`): <em>te</em>
* `ADP` (9; 10% of non-empty `ExtPos`): <em>omwille, tengevolge, temidden</em>
* `ADV` (73; 81% of non-empty `ExtPos`): <em>dan, nu, min, niet, al, maar, zo, her, ook, verre</em>
* `CCONJ` (1; 1% of non-empty `ExtPos`): <em>dan</em>
* `DET` (2; 2% of non-empty `ExtPos`): <em>hoe</em>
* `PROPN` (2; 2% of non-empty `ExtPos`): <em>Hier, Vandaag</em>
* `SCONJ` (1; 1% of non-empty `ExtPos`): <em>Zo</em>

<table>
  <tr><th>Paradigm <i>zo</i></th><th><tt>ADV</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>zo</em></td><td><em>Zo</em></td></tr>
</table>

### `PRON`

67 <tt><a href="nl_alpino-pos-PRON.html">PRON</a></tt> tokens (1% of all `PRON` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PRON` and `ExtPos` co-occurred: <tt><a href="nl_alpino-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (64; 96%), <tt><a href="nl_alpino-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (63; 94%).

`PRON` tokens may have the following values of `ExtPos`:

* `ADV` (37; 55% of non-empty `ExtPos`): <em>meer, minder, zijns, ons, wat</em>
* `PRON` (24; 36% of non-empty `ExtPos`): <em>wat, zich</em>
* `SCONJ` (6; 9% of non-empty `ExtPos`): <em>dat</em>

<table>
  <tr><th>Paradigm <i>wat</i></th><th><tt>ADV</tt></th><th><tt>PRON</tt></th></tr>
  <tr><td><tt><tt><a href="nl_alpino-feat-PronType.html">PronType</a></tt><tt>=Exc</tt></tt></td><td></td><td><em>wat</em></td></tr>
  <tr><td><tt><tt><a href="nl_alpino-feat-PronType.html">PronType</a></tt><tt>=Int</tt></tt></td><td><em>wat</em></td><td><em>wat</em></td></tr>
</table>

### `SCONJ`

23 <tt><a href="nl_alpino-pos-SCONJ.html">SCONJ</a></tt> tokens (1% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

`SCONJ` tokens may have the following values of `ExtPos`:

* `ADP` (14; 61% of non-empty `ExtPos`): <em>als</em>
* `ADV` (4; 17% of non-empty `ExtPos`): <em>als</em>
* `CCONJ` (1; 4% of non-empty `ExtPos`): <em>als</em>
* `SCONJ` (4; 17% of non-empty `ExtPos`): <em>of</em>

<table>
  <tr><th>Paradigm <i>als</i></th><th><tt>ADP</tt></th><th><tt>ADV</tt></th><th><tt>CCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>als</em></td><td><em>als</em></td><td><em>als</em></td></tr>
</table>

### `PROPN`

16 <tt><a href="nl_alpino-pos-PROPN.html">PROPN</a></tt> tokens (0% of all `PROPN` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PROPN` and `ExtPos` co-occurred: <tt><a href="nl_alpino-feat-Number.html">Number</a></tt><tt>=Sing</tt> (16; 100%), <tt><a href="nl_alpino-feat-Gender.html">Gender</a></tt><tt>=Com</tt> (10; 63%).

`PROPN` tokens may have the following values of `ExtPos`:

* `ADV` (2; 13% of non-empty `ExtPos`): <em>God</em>
* `PROPN` (14; 88% of non-empty `ExtPos`): <em>Olympia's, Ulster, Adam, Balkum, Fiat, GOD, Josef, Máxima, Porsche, Sydney</em>

<table>
  <tr><th>Paradigm <i>God</i></th><th><tt>ADV</tt></th><th><tt>PROPN</tt></th></tr>
  <tr><td><tt></tt></td><td><em>God</em></td><td><em>GOD</em></td></tr>
</table>

`ExtPos` seems to be **lexical feature** of `PROPN`. 92% lemmas (12) occur only with one value of `ExtPos`.

### `VERB`

11 <tt><a href="nl_alpino-pos-VERB.html">VERB</a></tt> tokens (0% of all `VERB` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `VERB` and `ExtPos` co-occurred: <tt><a href="nl_alpino-feat-Mood.html">Mood</a></tt><tt>=Ind</tt> (9; 82%), <tt><a href="nl_alpino-feat-Number.html">Number</a></tt><tt>=Sing</tt> (9; 82%), <tt><a href="nl_alpino-feat-Tense.html">Tense</a></tt><tt>=Pres</tt> (9; 82%), <tt><a href="nl_alpino-feat-VerbForm.html">VerbForm</a></tt><tt>=Fin</tt> (9; 82%).

`VERB` tokens may have the following values of `ExtPos`:

* `ADV` (1; 9% of non-empty `ExtPos`): <em>Zegge</em>
* `CCONJ` (1; 9% of non-empty `ExtPos`): <em>laat</em>
* `PROPN` (2; 18% of non-empty `ExtPos`): <em>Denken, Verwoeste</em>
* `SCONJ` (7; 64% of non-empty `ExtPos`): <em>zij, laat</em>

<table>
  <tr><th>Paradigm <i>laten</i></th><th><tt>CCONJ</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>laat</em></td><td><em>laat</em></td></tr>
</table>

### `CCONJ`

10 <tt><a href="nl_alpino-pos-CCONJ.html">CCONJ</a></tt> tokens (0% of all `CCONJ` tokens) have a non-empty value of `ExtPos`.

`CCONJ` tokens may have the following values of `ExtPos`:

* `CCONJ` (7; 70% of non-empty `ExtPos`): <em>en, maar</em>
* `SCONJ` (3; 30% of non-empty `ExtPos`): <em>maar, en</em>

<table>
  <tr><th>Paradigm <i>en</i></th><th><tt>CCONJ</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>en</em></td><td><em>en</em></td></tr>
</table>

### `INTJ`

5 <tt><a href="nl_alpino-pos-INTJ.html">INTJ</a></tt> tokens (5% of all `INTJ` tokens) have a non-empty value of `ExtPos`.

`INTJ` tokens may have the following values of `ExtPos`:

* `ADV` (2; 40% of non-empty `ExtPos`): <em>ja, o</em>
* `PROPN` (3; 60% of non-empty `ExtPos`): <em>Ja, hallo</em>

<table>
  <tr><th>Paradigm <i>ja</i></th><th><tt>ADV</tt></th><th><tt>PROPN</tt></th></tr>
  <tr><td><tt></tt></td><td><em>ja</em></td><td><em>Ja</em></td></tr>
</table>

## Relations with Agreement in `ExtPos`

The 10 most frequent relations where parent and child node agree in `ExtPos`:
<tt>SYM --[<tt><a href="nl_alpino-dep-fixed.html">fixed</a></tt>]--> SYM</tt> (26; 100%),
<tt>NUM --[<tt><a href="nl_alpino-dep-conj.html">conj</a></tt>]--> NUM</tt> (7; 70%),
<tt>SYM --[<tt><a href="nl_alpino-dep-conj.html">conj</a></tt>]--> SYM</tt> (5; 100%),
<tt>X --[<tt><a href="nl_alpino-dep-conj.html">conj</a></tt>]--> X</tt> (5; 71%),
<tt>DET --[<tt><a href="nl_alpino-dep-conj.html">conj</a></tt>]--> DET</tt> (2; 100%),
<tt>DET --[<tt><a href="nl_alpino-dep-parataxis.html">parataxis</a></tt>]--> DET</tt> (2; 100%),
<tt>X --[<tt><a href="nl_alpino-dep-conj.html">conj</a></tt>]--> ADJ</tt> (2; 100%),
<tt>ADP --[<tt><a href="nl_alpino-dep-conj.html">conj</a></tt>]--> SCONJ</tt> (1; 100%),
<tt>NOUN --[<tt><a href="nl_alpino-dep-conj.html">conj</a></tt>]--> ADJ</tt> (1; 100%),
<tt>NOUN --[<tt><a href="nl_alpino-dep-conj.html">conj</a></tt>]--> X</tt> (1; 100%).

