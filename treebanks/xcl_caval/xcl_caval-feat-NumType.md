---
layout: base
title:  'Statistics of NumType in UD_Classical_Armenian-CAVaL'
udver: '2'
---

## Treebank Statistics: UD_Classical_Armenian-CAVaL: Features: `NumType`

This feature is universal.
It occurs with 3 different values: `Card`, `Ord`, `Sets`.

596 tokens (1%) have a non-empty value of `NumType`.
79 types (1%) occur at least once with a non-empty value of `NumType`.
43 lemmas (2%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="xcl_caval-pos-NUM.html">NUM</a></tt> (548; 1% instances), <tt><a href="xcl_caval-pos-ADJ.html">ADJ</a></tt> (48; 0% instances).

### `NUM`

548 <tt><a href="xcl_caval-pos-NUM.html">NUM</a></tt> tokens (99% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="xcl_caval-feat-Number.html">Number</a></tt><tt>=Sing</tt> (353; 64%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (538; 98% of non-empty `NumType`): <em>մի, երկուս, հինգ, երկու, եւթն, երիս, երկոտասան, հարիւր, երկոտասանից, երկուց</em>
* `Sets` (10; 2% of non-empty `NumType`): <em>երկոքին, երկոտասանեսին, եւթանեքին, իւթանեքին</em>
* `EMPTY` (8): <em>մի, միում, մետասանք</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (32) occur only with one value of `NumType`.

### `ADJ`

48 <tt><a href="xcl_caval-pos-ADJ.html">ADJ</a></tt> tokens (2% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="xcl_caval-feat-Number.html">Number</a></tt><tt>=Sing</tt> (34; 71%).

`ADJ` tokens may have the following values of `NumType`:

* `Ord` (48; 100% of non-empty `NumType`): <em>երիր, երրորդ, երկրորդ, վեցերորդ, մետասաներորդ, եւթներորդ, երրորդի, իններորդ, չորրորդ, եկրորդ</em>
* `EMPTY` (1992): <em>մեծ, բարի, չար, միւս, այղ, այլ, սուրբ, ճշմարիտ, առաջին, արժան</em>

`NumType` seems to be **lexical feature** of `ADJ`. 100% lemmas (11) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="xcl_caval-dep-conj.html">conj</a></tt>]--> NUM</tt> (21; 100%),
<tt>NUM --[<tt><a href="xcl_caval-dep-compound.html">compound</a></tt>]--> NUM</tt> (13; 100%),
<tt>NUM --[<tt><a href="xcl_caval-dep-nmod.html">nmod</a></tt>]--> NUM</tt> (9; 100%),
<tt>NUM --[<tt><a href="xcl_caval-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (6; 100%),
<tt>NUM --[<tt><a href="xcl_caval-dep-orphan.html">orphan</a></tt>]--> NUM</tt> (6; 100%),
<tt>NUM --[<tt><a href="xcl_caval-dep-compound-redup.html">compound:redup</a></tt>]--> NUM</tt> (4; 100%),
<tt>ADJ --[<tt><a href="xcl_caval-dep-conj.html">conj</a></tt>]--> ADJ</tt> (1; 100%),
<tt>NUM --[<tt><a href="xcl_caval-dep-advcl.html">advcl</a></tt>]--> NUM</tt> (1; 100%).

