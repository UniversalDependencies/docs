

--------------------------------------------------------------------------------

## Treebank Statistics (UD_Persian)

This feature is universal.
It occurs with 2 different values: `Imp`, `Sub`.

2570 tokens (2%) have a non-empty value of `Mood`.
466 types (3%) occur at least once with a non-empty value of `Mood`.
2 lemmas (15%) occur at least once with a non-empty value of `Mood`.
The feature is used with 2 part-of-speech tags: [fa-pos/VERB]() (2566; 2% instances), [fa-pos/AUX]() (4; 0% instances).

### `VERB`

2566 [fa-pos/VERB]() tokens (15% of all `VERB` tokens) have a non-empty value of `Mood`.

The most frequent other feature values with which `VERB` and `Mood` co-occurred: <tt><a href="VerbForm.html">VerbForm</a>=EMPTY</tt> (2536; 99%), <tt><a href="Tense.html">Tense</a>=Pres</tt> (2380; 93%), <tt><a href="Person.html">Person</a>=3</tt> (1808; 70%), <tt><a href="Number.html">Number</a>=Sing</tt> (1507; 59%).

`VERB` tokens may have the following values of `Mood`:

* `Imp` (163; 6% of non-empty `Mood`): کن، گو، بگو، بگیر، باش، بده، ببین، برو، بیا، بدانید
* `Sub` (2403; 94% of non-empty `Mood`): کنند، باشد، کند، شود، باشند، کنیم، کنم، بشود، دهد، کنید
* `EMPTY` (14366): است، بود، کرد، شد، گفت، شده، می‌شود، دارد، می‌کند، کرده

### `AUX`

4 [fa-pos/AUX]() tokens (1% of all `AUX` tokens) have a non-empty value of `Mood`.

The most frequent other feature values with which `AUX` and `Mood` co-occurred: <tt><a href="Person.html">Person</a>=3</tt> (4; 100%), <tt><a href="VerbForm.html">VerbForm</a>=Fin</tt> (4; 100%), <tt><a href="Tense.html">Tense</a>=Pres</tt> (4; 100%), <tt><a href="Number.html">Number</a>=Plur</tt> (4; 100%).

`AUX` tokens may have the following values of `Mood`:

* `Sub` (4; 100% of non-empty `Mood`): نتوانند
* `EMPTY` (738): باید، خواهد، می‌توان، خواهند، نباید، نخواهد، نمی‌توان، بایستی، دارد، داریم

## Relations with Agreement in `Mood`

The 10 most frequent relations where parent and child node agree in `Mood`:
<tt>VERB --[<a href="../dep/conj.html">conj</a>]--> VERB</tt> (267; 59%).

