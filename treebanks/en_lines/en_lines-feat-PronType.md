---
layout: base
title:  'Statistics of PronType in UD_English-LinES'
udver: '2'
---

## Treebank Statistics: UD_English-LinES: Features: `PronType`

This feature is universal but the values `Emp` are language-specific.
It occurs with 10 different values: `Art`, `Dem`, `Emp`, `Ind`, `Int`, `Neg`, `Prs`, `Rcp`, `Rel`, `Tot`.

20025 tokens (19%) have a non-empty value of `PronType`.
135 types (1%) occur at least once with a non-empty value of `PronType`.
66 lemmas (1%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="en_lines-pos-DET.html">DET</a></tt> (9844; 9% instances), <tt><a href="en_lines-pos-PRON.html">PRON</a></tt> (9248; 9% instances), <tt><a href="en_lines-pos-ADV.html">ADV</a></tt> (933; 1% instances).

### `DET`

9844 <tt><a href="en_lines-pos-DET.html">DET</a></tt> tokens (99% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="en_lines-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (5845; 59%).

`DET` tokens may have the following values of `PronType`:

* `Art` (8768; 89% of non-empty `PronType`): <em>the, a, an, Le, what, Tha</em>
* `Dem` (471; 5% of non-empty `PronType`): <em>this, that, these, those</em>
* `Ind` (182; 2% of non-empty `PronType`): <em>some, any, another, either</em>
* `Int` (38; 0% of non-empty `PronType`): <em>what, which, whatever</em>
* `Neg` (160; 2% of non-empty `PronType`): <em>no, none</em>
* `Rcp` (23; 0% of non-empty `PronType`): <em>each</em>
* `Rel` (2; 0% of non-empty `PronType`): <em>what, whose</em>
* `Tot` (200; 2% of non-empty `PronType`): <em>all, each, every, both</em>
* `EMPTY` (77): <em>no, some, all, any, one, either, both, du, each, La</em>

<table>
  <tr><th>Paradigm <i>what</i></th><th><tt>Art</tt></th><th><tt>Int</tt></th><th><tt>Rel</tt></th></tr>
  <tr><td><tt></tt></td><td><em>what</em></td><td><em>what</em></td><td><em>what</em></td></tr>
</table>

### `PRON`

9248 <tt><a href="en_lines-pos-PRON.html">PRON</a></tt> tokens (83% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="en_lines-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (7224; 78%), <tt><a href="en_lines-feat-Number.html">Number</a></tt><tt>=Sing</tt> (6063; 66%), <tt><a href="en_lines-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (5674; 61%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (393; 4% of non-empty `PronType`): <em>that, this, these, those</em>
* `Emp` (113; 1% of non-empty `PronType`): <em>himself</em>
* `Ind` (164; 2% of non-empty `PronType`): <em>something, someone, anything, anyone, one, either, some, ones</em>
* `Int` (395; 4% of non-empty `PronType`): <em>what, who, which, whatever, whom, whose, Those</em>
* `Neg` (104; 1% of non-empty `PronType`): <em>nothing, one, none, neither</em>
* `Prs` (7492; 81% of non-empty `PronType`): <em>he, I, his, you, they, my, him, her, we, she</em>
* `Rcp` (4; 0% of non-empty `PronType`): <em>one</em>
* `Rel` (572; 6% of non-empty `PronType`): <em>that, who, which, what, whom, whose</em>
* `Tot` (11; 0% of non-empty `PronType`): <em>each, both, all</em>
* `EMPTY` (1945): <em>it, there, one, all, you, everything, others, some, everyone, another</em>

<table>
  <tr><th>Paradigm <i>one</i></th><th><tt>Prs</tt></th><th><tt>Rcp</tt></th><th><tt>Neg</tt></th><th><tt>Ind</tt></th></tr>
  <tr><td><tt><tt><a href="en_lines-feat-Case.html">Case</a></tt><tt>=Acc</tt>|<tt><a href="en_lines-feat-Number.html">Number</a></tt><tt>=Sing</tt>|<tt><a href="en_lines-feat-Person.html">Person</a></tt><tt>=3</tt>|<tt><a href="en_lines-feat-Reflex.html">Reflex</a></tt><tt>=Yes</tt></tt></td><td><em>oneself</em></td><td></td><td></td><td></td></tr>
  <tr><td><tt><tt><a href="en_lines-feat-ExtPos.html">ExtPos</a></tt><tt>=PRON</tt></tt></td><td></td><td><em>one</em></td><td></td><td></td></tr>
  <tr><td><tt><tt><a href="en_lines-feat-Number.html">Number</a></tt><tt>=Sing</tt></tt></td><td></td><td></td><td><em>one</em></td><td><em>one</em></td></tr>
  <tr><td><tt><tt><a href="en_lines-feat-Number.html">Number</a></tt><tt>=Plur</tt></tt></td><td></td><td></td><td></td><td><em>ones</em></td></tr>
</table>

### `ADV`

933 <tt><a href="en_lines-pos-ADV.html">ADV</a></tt> tokens (16% of all `ADV` tokens) have a non-empty value of `PronType`.

`ADV` tokens may have the following values of `PronType`:

* `Dem` (452; 48% of non-empty `PronType`): <em>then, now, there, here</em>
* `Ind` (77; 8% of non-empty `PronType`): <em>ever, sometimes, somewhere, anywhere</em>
* `Int` (213; 23% of non-empty `PronType`): <em>how, why, where, when, wherever, whatever</em>
* `Neg` (81; 9% of non-empty `PronType`): <em>never, nowhere</em>
* `Rel` (56; 6% of non-empty `PronType`): <em>where, why</em>
* `Tot` (54; 6% of non-empty `PronType`): <em>always, everywhere</em>
* `EMPTY` (4808): <em>out, up, so, back, very, just, again, too, down, only</em>

<table>
  <tr><th>Paradigm <i>where</i></th><th><tt>Int</tt></th><th><tt>Rel</tt></th></tr>
  <tr><td><tt></tt></td><td><em>where</em></td><td><em>where</em></td></tr>
</table>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="en_lines-dep-conj.html">conj</a></tt>]--> PRON</tt> (24; 80%),
<tt>ADV --[<tt><a href="en_lines-dep-conj.html">conj</a></tt>]--> ADV</tt> (11; 100%),
<tt>PRON --[<tt><a href="en_lines-dep-appos.html">appos</a></tt>]--> PRON</tt> (2; 67%),
<tt>PRON --[<tt><a href="en_lines-dep-conj.html">conj</a></tt>]--> ADV</tt> (2; 100%).

