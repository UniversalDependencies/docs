---
layout: base
title:  'Statistics of ExtPos in UD_Greek-Lesbian'
udver: '2'
---

## Treebank Statistics: UD_Greek-Lesbian: Features: `ExtPos`

This feature is language-specific.
It occurs with 4 different values: `ADP`, `ADV`, `INTJ`, `SCONJ`.

28 tokens (1%) have a non-empty value of `ExtPos`.
23 types (2%) occur at least once with a non-empty value of `ExtPos`.
18 lemmas (2%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 7 part-of-speech tags: <tt><a href="el_lesbian-pos-ADV.html">ADV</a></tt> (16; 0% instances), <tt><a href="el_lesbian-pos-ADP.html">ADP</a></tt> (4; 0% instances), <tt><a href="el_lesbian-pos-PART.html">PART</a></tt> (3; 0% instances), <tt><a href="el_lesbian-pos-SCONJ.html">SCONJ</a></tt> (2; 0% instances), <tt><a href="el_lesbian-pos-NOUN.html">NOUN</a></tt> (1; 0% instances), <tt><a href="el_lesbian-pos-NUM.html">NUM</a></tt> (1; 0% instances), <tt><a href="el_lesbian-pos-X.html">X</a></tt> (1; 0% instances).

### `ADV`

16 <tt><a href="el_lesbian-pos-ADV.html">ADV</a></tt> tokens (10% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="el_lesbian-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (14; 88%).

`ADV` tokens may have the following values of `ExtPos`:

* `ADP` (11; 69% of non-empty `ExtPos`): <em>ίσια, πα, πριν, Ύστιρα, Απάνου, ανάμισα, ιδώ, πά</em>
* `ADV` (5; 31% of non-empty `ExtPos`): <em>Σαν, Σιγά, άλλου, μο</em>

`ExtPos` seems to be **lexical feature** of `ADV`. 100% lemmas (10) occur only with one value of `ExtPos`.

### `ADP`

4 <tt><a href="el_lesbian-pos-ADP.html">ADP</a></tt> tokens (2% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (1; 25% of non-empty `ExtPos`): <em>απ</em>
* `SCONJ` (3; 75% of non-empty `ExtPos`): <em>για</em>

### `PART`

3 <tt><a href="el_lesbian-pos-PART.html">PART</a></tt> tokens (5% of all `PART` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PART` and `ExtPos` co-occurred: <tt><a href="el_lesbian-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (3; 100%).

`PART` tokens may have the following values of `ExtPos`:

* `ADV` (3; 100% of non-empty `ExtPos`): <em>μπά, Μπα</em>

### `SCONJ`

2 <tt><a href="el_lesbian-pos-SCONJ.html">SCONJ</a></tt> tokens (1% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

`SCONJ` tokens may have the following values of `ExtPos`:

* `ADV` (1; 50% of non-empty `ExtPos`): <em>π’</em>
* `SCONJ` (1; 50% of non-empty `ExtPos`): <em>που</em>

<table>
  <tr><th>Paradigm <i>που</i></th><th><tt>ADV</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>π’</em></td><td><em>που</em></td></tr>
</table>

### `NOUN`

1 <tt><a href="el_lesbian-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `NOUN` and `ExtPos` co-occurred: <tt><a href="el_lesbian-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (1; 100%), <tt><a href="el_lesbian-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (1; 100%), <tt><a href="el_lesbian-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (1; 100%).

`NOUN` tokens may have the following values of `ExtPos`:

* `ADV` (1; 100% of non-empty `ExtPos`): <em>καλού</em>

### `NUM`

1 <tt><a href="el_lesbian-pos-NUM.html">NUM</a></tt> tokens (5% of all `NUM` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `NUM` and `ExtPos` co-occurred: <tt><a href="el_lesbian-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (1; 100%), <tt><a href="el_lesbian-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (1; 100%), <tt><a href="el_lesbian-feat-NumType.html">NumType</a></tt><tt>=Card</tt> (1; 100%), <tt><a href="el_lesbian-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (1; 100%).

`NUM` tokens may have the following values of `ExtPos`:

* `ADV` (1; 100% of non-empty `ExtPos`): <em>100</em>

### `X`

1 <tt><a href="el_lesbian-pos-X.html">X</a></tt> tokens (17% of all `X` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `X` and `ExtPos` co-occurred: <tt><a href="el_lesbian-feat-Style.html">Style</a></tt><tt>=EMPTY</tt> (1; 100%).

`X` tokens may have the following values of `ExtPos`:

* `INTJ` (1; 100% of non-empty `ExtPos`): <em>Εις</em>

