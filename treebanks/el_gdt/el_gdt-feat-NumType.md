---
layout: base
title:  'Statistics of NumType in UD_Greek-GDT'
udver: '2'
---

## Treebank Statistics: UD_Greek-GDT: Features: `NumType`

This feature is universal.
It occurs with 4 different values: `Card`, `Mult`, `Ord`, `Sets`.

982 tokens (2%) have a non-empty value of `NumType`.
356 types (3%) occur at least once with a non-empty value of `NumType`.
304 lemmas (5%) occur at least once with a non-empty value of `NumType`.
The feature is used with 3 part-of-speech tags: <tt><a href="el_gdt-pos-NUM.html">NUM</a></tt> (792; 1% instances), <tt><a href="el_gdt-pos-ADJ.html">ADJ</a></tt> (188; 0% instances), <tt><a href="el_gdt-pos-NOUN.html">NOUN</a></tt> (2; 0% instances).

### `NUM`

792 <tt><a href="el_gdt-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="el_gdt-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (470; 59%), <tt><a href="el_gdt-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (470; 59%), <tt><a href="el_gdt-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (470; 59%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (776; 98% of non-empty `NumType`): <em>δύο, τρεις, 1, 15, πέντε, 100, 20, 3, 30, 80</em>
* `Sets` (16; 2% of non-empty `NumType`): <em>δεκάδες, χιλιάδες, εκατοντάδες, χιλιάδων, εκατοντάδων</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (263) occur only with one value of `NumType`.

### `ADJ`

188 <tt><a href="el_gdt-pos-ADJ.html">ADJ</a></tt> tokens (4% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="el_gdt-feat-Number.html">Number</a></tt><tt>=Sing</tt> (173; 92%).

`ADJ` tokens may have the following values of `NumType`:

* `Mult` (6; 3% of non-empty `NumType`): <em>διπλή, διπλάσιο, διπλού, διπλό, τριπλάσιος</em>
* `Ord` (182; 97% of non-empty `NumType`): <em>πρώτη, πρώτο, Β', δεύτερη, δεύτερο, τρίτη, πρώτες, πρώτης, πρώτος, δεύτερος</em>
* `EMPTY` (4921): <em>ευρωπαϊκή, ευρωπαϊκό, νέα, σημαντικό, όλα, ευρωπαϊκής, όλες, μεγάλη, ευρωπαϊκού, πολλές</em>

`NumType` seems to be **lexical feature** of `ADJ`. 100% lemmas (42) occur only with one value of `NumType`.

### `NOUN`

2 <tt><a href="el_gdt-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NOUN` and `NumType` co-occurred: <tt><a href="el_gdt-feat-Case.html">Case</a></tt><tt>=Nom</tt> (2; 100%), <tt><a href="el_gdt-feat-Gender.html">Gender</a></tt><tt>=Fem</tt> (2; 100%), <tt><a href="el_gdt-feat-Number.html">Number</a></tt><tt>=Plur</tt> (2; 100%).

`NOUN` tokens may have the following values of `NumType`:

* `Sets` (2; 100% of non-empty `NumType`): <em>δεκάδες</em>
* `EMPTY` (13538): <em>επιτροπή, ΗΠΑ, κ., κοινοβούλιο, Συμβούλιο, κύριε, χώρες, ένωση, επιτροπής, Πρόεδρε</em>

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="el_gdt-dep-conj.html">conj</a></tt>]--> NUM</tt> (25; 100%),
<tt>NUM --[<tt><a href="el_gdt-dep-compound.html">compound</a></tt>]--> NUM</tt> (3; 60%),
<tt>NUM --[<tt><a href="el_gdt-dep-nmod.html">nmod</a></tt>]--> NUM</tt> (3; 100%),
<tt>NUM --[<tt><a href="el_gdt-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (3; 100%),
<tt>NUM --[<tt><a href="el_gdt-dep-compound.html">compound</a></tt>]--> NOUN</tt> (1; 100%).

