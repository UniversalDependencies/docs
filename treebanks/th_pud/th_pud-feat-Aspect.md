---
layout: base
title:  'Statistics of Aspect in UD_Thai-PUD'
udver: '2'
---

## Treebank Statistics: UD_Thai-PUD: Features: `Aspect`

This feature is universal.
It occurs with 2 different values: `Perf`, `Prog`.

401 tokens (2%) have a non-empty value of `Aspect`.
11 types (0%) occur at least once with a non-empty value of `Aspect`.
11 lemmas (0%) occur at least once with a non-empty value of `Aspect`.
The feature is used with 2 part-of-speech tags: <tt><a href="th_pud-pos-AUX.html">AUX</a></tt> (310; 1% instances), <tt><a href="th_pud-pos-VERB.html">VERB</a></tt> (91; 0% instances).

### `AUX`

310 <tt><a href="th_pud-pos-AUX.html">AUX</a></tt> tokens (27% of all `AUX` tokens) have a non-empty value of `Aspect`.

`AUX` tokens may have the following values of `Aspect`:

* `Perf` (285; 92% of non-empty `Aspect`): ได้, แล้ว, มา, เคย
* `Prog` (25; 8% of non-empty `Aspect`): กำลัง
* `EMPTY` (849): เป็น, จะ, ได้, ถูก, ต้อง, ได้รับ, อย่างเป็น, การเป็น, ความเป็น

### `VERB`

91 <tt><a href="th_pud-pos-VERB.html">VERB</a></tt> tokens (2% of all `VERB` tokens) have a non-empty value of `Aspect`.

`VERB` tokens may have the following values of `Aspect`:

* `Perf` (4; 4% of non-empty `Aspect`): การได้, อยู่, แลัว
* `Prog` (87; 96% of non-empty `Aspect`): อยู่, ยัง, ยังคง, ค่อนข้าง
* `EMPTY` (4560): มี, ให้, ใช้, ทำ, อยู่, รับ, กล่าว, ทำให้, เกิด, คือ

<table>
  <tr><th>Paradigm <i>อยู่</i></th><th><tt>Perf</tt></th><th><tt>Prog</tt></th></tr>
  <tr><td><tt></tt></td><td>อยู่</td><td>อยู่</td></tr>
</table>

