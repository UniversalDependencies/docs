---
layout: base
title:  'Statistics of Foreign in UD_Lithuanian-ALKSNIS'
udver: '2'
---

## Treebank Statistics: UD_Lithuanian-ALKSNIS: Features: `Foreign`

This feature is universal.
It occurs with 1 different values: `Yes`.

97 tokens (0%) have a non-empty value of `Foreign`.
82 types (1%) occur at least once with a non-empty value of `Foreign`.
82 lemmas (1%) occur at least once with a non-empty value of `Foreign`.
The feature is used with 1 part-of-speech tags: <tt><a href="lt_alksnis-pos-X.html">X</a></tt> (97; 0% instances).

### `X`

97 <tt><a href="lt_alksnis-pos-X.html">X</a></tt> tokens (20% of all `X` tokens) have a non-empty value of `Foreign`.

The most frequent other feature values with which `X` and `Foreign` co-occurred: <tt><a href="lt_alksnis-feat-Abbr.html">Abbr</a></tt><tt>=EMPTY</tt> (97; 100%).

`X` tokens may have the following values of `Foreign`:

* `Yes` (97; 100% of non-empty `Foreign`): <em>Baltic, Inter, Via, Cilley, Baltica, Kosmos, Optima, Urbico, le, A Bao A Ku</em>
* `EMPTY` (393): <em>ES, proc, Nr, d, tūkst, A, K, V, GMO, g</em>

`Foreign` seems to be **lexical feature** of `X`. 100% lemmas (82) occur only with one value of `Foreign`.

## Relations with Agreement in `Foreign`

The 10 most frequent relations where parent and child node agree in `Foreign`:
<tt>X --[<tt><a href="lt_alksnis-dep-flat-foreign.html">flat:foreign</a></tt>]--> X</tt> (25; 100%),
<tt>X --[<tt><a href="lt_alksnis-dep-conj.html">conj</a></tt>]--> X</tt> (3; 100%).

