---
layout: base
title:  'Statistics of NumForm in UD_Western_Armenian-ArmTDP'
udver: '2'
---

## Treebank Statistics: UD_Western_Armenian-ArmTDP: Features: `NumForm`

This feature is language-specific.
It occurs with 4 different values: `Armenian`, `Combi`, `Digit`, `Word`.

243 tokens (1%) have a non-empty value of `NumForm`.
93 types (1%) occur at least once with a non-empty value of `NumForm`.
76 lemmas (1%) occur at least once with a non-empty value of `NumForm`.
The feature is used with 2 part-of-speech tags: <tt><a href="hyw_armtdp-pos-NUM.html">NUM</a></tt> (183; 1% instances), <tt><a href="hyw_armtdp-pos-ADJ.html">ADJ</a></tt> (60; 0% instances).

### `NUM`

183 <tt><a href="hyw_armtdp-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumForm`.

The most frequent other feature values with which `NUM` and `NumForm` co-occurred: <tt><a href="hyw_armtdp-feat-NumType.html">NumType</a></tt><tt>=Card</tt> (177; 97%).

`NUM` tokens may have the following values of `NumForm`:

* `Digit` (45; 25% of non-empty `NumForm`): <em>2, 500, 100, 18, 1, 10, 15, 1906, 25, 96</em>
* `Word` (138; 75% of non-empty `NumForm`): <em>երկու, մէկ, երեք, չորս, հազար, հինգ, վեց, կէս, մէյ, հարիւր</em>

`NumForm` seems to be **lexical feature** of `NUM`. 100% lemmas (57) occur only with one value of `NumForm`.

### `ADJ`

60 <tt><a href="hyw_armtdp-pos-ADJ.html">ADJ</a></tt> tokens (2% of all `ADJ` tokens) have a non-empty value of `NumForm`.

The most frequent other feature values with which `ADJ` and `NumForm` co-occurred: <tt><a href="hyw_armtdp-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (60; 100%).

`ADJ` tokens may have the following values of `NumForm`:

* `Armenian` (16; 27% of non-empty `NumForm`): <em>Բ, Ա, Գ, Դ, Ե, Զ, Է, Ը, ԺԷ</em>
* `Combi` (1; 2% of non-empty `NumForm`): <em>36նոց</em>
* `Word` (43; 72% of non-empty `NumForm`): <em>առաջին, առջի, երկրորդ, առաջի, երրորդ, եօթանասուներորդ, եօթանասուննոց, ութսուննոց, տասներորդ</em>

`NumForm` seems to be **lexical feature** of `ADJ`. 100% lemmas (19) occur only with one value of `NumForm`.

## Relations with Agreement in `NumForm`

The 10 most frequent relations where parent and child node agree in `NumForm`:
<tt>NUM --[<tt><a href="hyw_armtdp-dep-flat-range.html">flat:range</a></tt>]--> NUM</tt> (6; 100%),
<tt>NUM --[<tt><a href="hyw_armtdp-dep-appos.html">appos</a></tt>]--> NUM</tt> (4; 100%),
<tt>NUM --[<tt><a href="hyw_armtdp-dep-flat.html">flat</a></tt>]--> NUM</tt> (4; 100%),
<tt>NUM --[<tt><a href="hyw_armtdp-dep-conj.html">conj</a></tt>]--> NUM</tt> (3; 100%),
<tt>NUM --[<tt><a href="hyw_armtdp-dep-flat-dist.html">flat:dist</a></tt>]--> NUM</tt> (3; 100%).

