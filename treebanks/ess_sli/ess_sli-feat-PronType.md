---
layout: base
title:  'Statistics of PronType in UD_Yupik-SLI'
udver: '2'
---

## Treebank Statistics: UD_Yupik-SLI: Features: `PronType`

This feature is universal.
It occurs with 2 different values: `Dem`, `Int`.

62 tokens (2%) have a non-empty value of `PronType`.
27 types (4%) occur at least once with a non-empty value of `PronType`.
26 lemmas (4%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="ess_sli-pos-ADV.html">ADV</a></tt> (37; 1% instances), <tt><a href="ess_sli-pos-PRON.html">PRON</a></tt> (20; 1% instances), <tt><a href="ess_sli-pos-DET.html">DET</a></tt> (5; 0% instances).

### `ADV`

37 <tt><a href="ess_sli-pos-ADV.html">ADV</a></tt> tokens (57% of all `ADV` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADV` and `PronType` co-occurred: <tt><a href="ess_sli-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (37; 100%).

`ADV` tokens may have the following values of `PronType`:

* `Int` (37; 100% of non-empty `PronType`): <em>Qakun, Sangavek, Qavngaq, Navek, Sangan, Naken, Nani, Sangami, naten, Sangama</em>
* `EMPTY` (28): <em>llu, aagken, Allakagh, Awani, Ingani, Pikegken, Qagani, Qerngagh, an'gani, ellngina</em>

`PronType` seems to be **lexical feature** of `ADV`. 100% lemmas (11) occur only with one value of `PronType`.

### `PRON`

20 <tt><a href="ess_sli-pos-PRON.html">PRON</a></tt> tokens (87% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="ess_sli-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (13; 65%), <tt><a href="ess_sli-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (13; 65%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (9; 45% of non-empty `PronType`): <em>ingku, paamna, piku, Iigna, Kaanyuq, Qaamna, m, pagna</em>
* `Int` (11; 55% of non-empty `PronType`): <em>Kitu, Kina, Kinku, Sameng, Kitumun</em>
* `EMPTY` (3): <em>elpenun, iingku, whangkunnun</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (13) occur only with one value of `PronType`.

### `DET`

5 <tt><a href="ess_sli-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

`DET` tokens may have the following values of `PronType`:

* `Int` (5; 100% of non-empty `PronType`): <em>Nali, Qafsina</em>

