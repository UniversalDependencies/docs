---
layout: base
title:  'Statistics of AdpType in UD_Low_Saxon-LSDC'
udver: '2'
---

## Treebank Statistics: UD_Low_Saxon-LSDC: Features: `AdpType`

This feature is language-specific.
It occurs with 2 different values: `Post`, `Prep`.

276 tokens (6%) have a non-empty value of `AdpType`.
42 types (3%) occur at least once with a non-empty value of `AdpType`.
24 lemmas (2%) occur at least once with a non-empty value of `AdpType`.
The feature is used with 1 part-of-speech tags: <tt><a href="nds_lsdc-pos-ADP.html">ADP</a></tt> (276; 6% instances).

### `ADP`

276 <tt><a href="nds_lsdc-pos-ADP.html">ADP</a></tt> tokens (88% of all `ADP` tokens) have a non-empty value of `AdpType`.

`ADP` tokens may have the following values of `AdpType`:

* `Post` (1; 0% of non-empty `AdpType`): <em>an</em>
* `Prep` (275; 100% of non-empty `AdpType`): <em>in, van, mid, to, an, up, by, vöär, nå, uut</em>

<table>
  <tr><th>Paradigm <i>an</i></th><th><tt>Post</tt></th><th><tt>Prep</tt></th></tr>
  <tr><td><tt></tt></td><td><em>an</em></td><td><em>an</em></td></tr>
</table>

`AdpType` seems to be **lexical feature** of `ADP`. 96% lemmas (23) occur only with one value of `AdpType`.

