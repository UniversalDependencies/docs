---
layout: base
title:  'Statistics of NumType in UD_English-LittlePrince'
udver: '2'
---

## Treebank Statistics: UD_English-LittlePrince: Features: `NumType`

This feature is universal.
It occurs with 3 different values: `Card`, `Mult`, `Ord`.

161 tokens (2%) have a non-empty value of `NumType`.
47 types (4%) occur at least once with a non-empty value of `NumType`.
40 lemmas (4%) occur at least once with a non-empty value of `NumType`.
The feature is used with 3 part-of-speech tags: <tt><a href="en_littleprince-pos-NUM.html">NUM</a></tt> (142; 2% instances), <tt><a href="en_littleprince-pos-ADJ.html">ADJ</a></tt> (14; 0% instances), <tt><a href="en_littleprince-pos-ADV.html">ADV</a></tt> (5; 0% instances).

### `NUM`

142 <tt><a href="en_littleprince-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (142; 100% of non-empty `NumType`): <em>one, hundred, five, two, twenty, four, three, million, seven, six</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (34) occur only with one value of `NumType`.

### `ADJ`

14 <tt><a href="en_littleprince-pos-ADJ.html">ADJ</a></tt> tokens (4% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="en_littleprince-feat-Degree.html">Degree</a></tt><tt>=Pos</tt> (14; 100%).

`ADJ` tokens may have the following values of `NumType`:

* `Ord` (14; 100% of non-empty `NumType`): <em>first, fourth, second, third, fifth</em>
* `EMPTY` (377): <em>little, good, conceited, other, bad, important, last, more, old, same</em>

### `ADV`

5 <tt><a href="en_littleprince-pos-ADV.html">ADV</a></tt> tokens (1% of all `ADV` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADV` and `NumType` co-occurred: <tt><a href="en_littleprince-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (5; 100%).

`ADV` tokens may have the following values of `NumType`:

* `Mult` (4; 80% of non-empty `NumType`): <em>once</em>
* `Ord` (1; 20% of non-empty `NumType`): <em>first</em>
* `EMPTY` (353): <em>very, so, then, never, just, now, too, much, away, again</em>

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="en_littleprince-dep-compound.html">compound</a></tt>]--> NUM</tt> (26; 100%),
<tt>NUM --[<tt><a href="en_littleprince-dep-conj.html">conj</a></tt>]--> NUM</tt> (17; 100%),
<tt>NUM --[<tt><a href="en_littleprince-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (14; 100%),
<tt>NUM --[<tt><a href="en_littleprince-dep-nmod.html">nmod</a></tt>]--> NUM</tt> (4; 100%),
<tt>NUM --[<tt><a href="en_littleprince-dep-appos.html">appos</a></tt>]--> NUM</tt> (1; 100%).

