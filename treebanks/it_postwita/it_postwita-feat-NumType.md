---
layout: base
title:  'Statistics of NumType in UD_Italian-PoSTWITA'
udver: '2'
---

## Treebank Statistics: UD_Italian-PoSTWITA: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

839 tokens (1%) have a non-empty value of `NumType`.
260 types (2%) occur at least once with a non-empty value of `NumType`.
240 lemmas (2%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="it_postwita-pos-NUM.html">NUM</a></tt> (733; 1% instances), <tt><a href="it_postwita-pos-ADJ.html">ADJ</a></tt> (106; 0% instances).

### `NUM`

733 <tt><a href="it_postwita-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (733; 100% of non-empty `NumType`): <em>2, 1, due, 3, 5, 4, 7, 20, 10, 6</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (229) occur only with one value of `NumType`.

### `ADJ`

106 <tt><a href="it_postwita-pos-ADJ.html">ADJ</a></tt> tokens (4% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="it_postwita-feat-Number.html">Number</a></tt><tt>=Sing</tt> (69; 65%).

`ADJ` tokens may have the following values of `NumType`:

* `Ord` (106; 100% of non-empty `NumType`): <em>primo, prima, prime, primi, 1°, ennesima, ennesimo, secondo, terza, seconda</em>
* `EMPTY` (2516): <em>nuovo, grande, buon, buona, bella, vero, giusto, politica, bravo, italiana</em>

`NumType` seems to be **lexical feature** of `ADJ`. 100% lemmas (11) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="it_postwita-dep-flat.html">flat</a></tt>]--> NUM</tt> (19; 100%),
<tt>NUM --[<tt><a href="it_postwita-dep-conj.html">conj</a></tt>]--> NUM</tt> (15; 100%),
<tt>NUM --[<tt><a href="it_postwita-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (3; 100%),
<tt>NUM --[<tt><a href="it_postwita-dep-nmod.html">nmod</a></tt>]--> NUM</tt> (2; 100%),
<tt>NUM --[<tt><a href="it_postwita-dep-obl.html">obl</a></tt>]--> NUM</tt> (2; 100%),
<tt>ADJ --[<tt><a href="it_postwita-dep-conj.html">conj</a></tt>]--> ADJ</tt> (1; 100%).

