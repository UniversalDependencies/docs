---
layout: base
title:  'Statistics of ExtPos in UD_Turkish-FrameNet'
udver: '2'
---

## Treebank Statistics: UD_Turkish-FrameNet: Features: `ExtPos`

This feature is language-specific.
It occurs with 3 different values: `ADJ`, `ADV`, `CCONJ`.

6 tokens (0%) have a non-empty value of `ExtPos`.
6 types (0%) occur at least once with a non-empty value of `ExtPos`.
5 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 3 part-of-speech tags: <tt><a href="tr_framenet-pos-ADV.html">ADV</a></tt> (4; 0% instances), <tt><a href="tr_framenet-pos-ADJ.html">ADJ</a></tt> (1; 0% instances), <tt><a href="tr_framenet-pos-DET.html">DET</a></tt> (1; 0% instances).

### `ADV`

4 <tt><a href="tr_framenet-pos-ADV.html">ADV</a></tt> tokens (0% of all `ADV` tokens) have a non-empty value of `ExtPos`.

`ADV` tokens may have the following values of `ExtPos`:

* `ADV` (4; 100% of non-empty `ExtPos`): <em>öyle, belki, sonra</em>

### `ADJ`

1 <tt><a href="tr_framenet-pos-ADJ.html">ADJ</a></tt> tokens (0% of all `ADJ` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADJ` and `ExtPos` co-occurred: <tt><a href="tr_framenet-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (1; 100%), <tt><a href="tr_framenet-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (1; 100%).

`ADJ` tokens may have the following values of `ExtPos`:

* `ADJ` (1; 100% of non-empty `ExtPos`): <em>Belli</em>

### `DET`

1 <tt><a href="tr_framenet-pos-DET.html">DET</a></tt> tokens (0% of all `DET` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `DET` and `ExtPos` co-occurred: <tt><a href="tr_framenet-feat-Definite.html">Definite</a></tt><tt>=Ind</tt> (1; 100%), <tt><a href="tr_framenet-feat-PronType.html">PronType</a></tt><tt>=Art</tt> (1; 100%).

`DET` tokens may have the following values of `ExtPos`:

* `CCONJ` (1; 100% of non-empty `ExtPos`): <em>bir</em>

