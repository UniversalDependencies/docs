---
layout: base
title:  'Statistics of NumType in UD_Western_Armenian-ArmTDP'
udver: '2'
---

## Treebank Statistics: UD_Western_Armenian-ArmTDP: Features: `NumType`

This feature is universal.
It occurs with 4 different values: `Card`, `Dist`, `Frac`, `Ord`.

895 tokens (1%) have a non-empty value of `NumType`.
230 types (1%) occur at least once with a non-empty value of `NumType`.
203 lemmas (2%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="hyw_armtdp-pos-NUM.html">NUM</a></tt> (684; 1% instances), <tt><a href="hyw_armtdp-pos-ADJ.html">ADJ</a></tt> (211; 0% instances).

### `NUM`

684 <tt><a href="hyw_armtdp-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (662; 97% of non-empty `NumType`): <em>երկու, մէկ, 2021, երեք, չորս, հինգ, վեց, 6, 7, 15</em>
* `Dist` (8; 1% of non-empty `NumType`): <em>8, մէյմէկ</em>
* `Frac` (14; 2% of non-empty `NumType`): <em>կէս, մէկուկէս, քառորդ, երկուքուկէս</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (159) occur only with one value of `NumType`.

### `ADJ`

211 <tt><a href="hyw_armtdp-pos-ADJ.html">ADJ</a></tt> tokens (3% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="hyw_armtdp-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (122; 58%).

`ADJ` tokens may have the following values of `NumType`:

* `Card` (4; 2% of non-empty `NumType`): <em>եօթանասուննոց, երկուքուկէսնոց, ութսուննոց</em>
* `Dist` (2; 1% of non-empty `NumType`): <em>մէկական</em>
* `Ord` (205; 97% of non-empty `NumType`): <em>առաջին, երկրորդ, Բ, Ա, առջի, ԺԹ, Գ, 113-րդ, երրորդ, եօթներորդ</em>
* `EMPTY` (7081): <em>մեծ, նոր, կարելի, վերջին, ազգային, Ս, քաղաքական, հայ, երկար, տարբեր</em>

`NumType` seems to be **lexical feature** of `ADJ`. 100% lemmas (44) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="hyw_armtdp-dep-flat.html">flat</a></tt>]--> NUM</tt> (24; 86%),
<tt>NUM --[<tt><a href="hyw_armtdp-dep-flat-range.html">flat:range</a></tt>]--> NUM</tt> (19; 100%),
<tt>NUM --[<tt><a href="hyw_armtdp-dep-conj.html">conj</a></tt>]--> NUM</tt> (7; 88%),
<tt>NUM --[<tt><a href="hyw_armtdp-dep-appos.html">appos</a></tt>]--> NUM</tt> (4; 100%),
<tt>NUM --[<tt><a href="hyw_armtdp-dep-flat-dist.html">flat:dist</a></tt>]--> NUM</tt> (3; 100%),
<tt>NUM --[<tt><a href="hyw_armtdp-dep-nmod-npmod.html">nmod:npmod</a></tt>]--> NUM</tt> (1; 100%).

