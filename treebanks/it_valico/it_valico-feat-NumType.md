---
layout: base
title:  'Statistics of NumType in UD_Italian-Valico'
udver: '2'
---

## Treebank Statistics: UD_Italian-Valico: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

19 tokens (0%) have a non-empty value of `NumType`.
16 types (1%) occur at least once with a non-empty value of `NumType`.
16 lemmas (2%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="it_valico-pos-NUM.html">NUM</a></tt> (17; 0% instances), <tt><a href="it_valico-pos-ADJ.html">ADJ</a></tt> (2; 0% instances).

### `NUM`

17 <tt><a href="it_valico-pos-NUM.html">NUM</a></tt> tokens (94% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (17; 100% of non-empty `NumType`): <em>due, 1, 10, 28, 30, 700, 9, 95, 99, catordieci</em>
* `EMPTY` (1): <em>diecisette</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (14) occur only with one value of `NumType`.

### `ADJ`

2 <tt><a href="it_valico-pos-ADJ.html">ADJ</a></tt> tokens (1% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="it_valico-feat-Number.html">Number</a></tt><tt>=Sing</tt> (2; 100%).

`ADJ` tokens may have the following values of `NumType`:

* `Ord` (2; 100% of non-empty `NumType`): <em>prima, ventottesimo</em>
* `EMPTY` (308): <em>brutto, bella, grande, forte, simpatico, contenta, felice, giovane, bel, bello</em>

