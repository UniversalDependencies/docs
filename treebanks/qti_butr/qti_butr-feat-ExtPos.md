---
layout: base
title:  'Statistics of ExtPos in UD_Turkish_English-BUTR'
udver: '2'
---

## Treebank Statistics: UD_Turkish_English-BUTR: Features: `ExtPos`

This feature is language-specific.
It occurs with 2 different values: `ADJ`, `ADV`.

3 tokens (1%) have a non-empty value of `ExtPos`.
3 types (1%) occur at least once with a non-empty value of `ExtPos`.
3 lemmas (1%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 3 part-of-speech tags: <tt><a href="qti_butr-pos-ADJ.html">ADJ</a></tt> (1; 0% instances), <tt><a href="qti_butr-pos-ADP.html">ADP</a></tt> (1; 0% instances), <tt><a href="qti_butr-pos-DET.html">DET</a></tt> (1; 0% instances).

### `ADJ`

1 <tt><a href="qti_butr-pos-ADJ.html">ADJ</a></tt> tokens (4% of all `ADJ` tokens) have a non-empty value of `ExtPos`.

`ADJ` tokens may have the following values of `ExtPos`:

* `ADJ` (1; 100% of non-empty `ExtPos`): <em>karşı</em>

### `ADP`

1 <tt><a href="qti_butr-pos-ADP.html">ADP</a></tt> tokens (9% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADV` (1; 100% of non-empty `ExtPos`): <em>Of</em>

### `DET`

1 <tt><a href="qti_butr-pos-DET.html">DET</a></tt> tokens (4% of all `DET` tokens) have a non-empty value of `ExtPos`.

`DET` tokens may have the following values of `ExtPos`:

* `ADV` (1; 100% of non-empty `ExtPos`): <em>All</em>

