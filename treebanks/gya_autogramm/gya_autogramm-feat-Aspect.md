---
layout: base
title:  'Statistics of Aspect in UD_Northwest_Gbaya-Autogramm'
udver: '2'
---

## Treebank Statistics: UD_Northwest_Gbaya-Autogramm: Features: `Aspect`

This feature is universal but the values `Iter` are language-specific.
It occurs with 2 different values: `Imp`, `Iter`.

6 tokens (0%) have a non-empty value of `Aspect`.
3 types (1%) occur at least once with a non-empty value of `Aspect`.
2 lemmas (1%) occur at least once with a non-empty value of `Aspect`.
The feature is used with 2 part-of-speech tags: <tt><a href="gya_autogramm-pos-PART.html">PART</a></tt> (5; 0% instances), <tt><a href="gya_autogramm-pos-SCONJ.html">SCONJ</a></tt> (1; 0% instances).

### `PART`

5 <tt><a href="gya_autogramm-pos-PART.html">PART</a></tt> tokens (3% of all `PART` tokens) have a non-empty value of `Aspect`.

The most frequent other feature values with which `PART` and `Aspect` co-occurred: <tt><a href="gya_autogramm-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (5; 100%).

`PART` tokens may have the following values of `Aspect`:

* `Iter` (5; 100% of non-empty `Aspect`): <em>kàá, kàà</em>
* `EMPTY` (146): <em>ná, nɛ̀, ndé, sɛ̀nɛ́, wó, péínɛ́, wá, yè, nɛ̀nɛ́, nɛ́</em>

### `SCONJ`

1 <tt><a href="gya_autogramm-pos-SCONJ.html">SCONJ</a></tt> tokens (2% of all `SCONJ` tokens) have a non-empty value of `Aspect`.

The most frequent other feature values with which `SCONJ` and `Aspect` co-occurred: <tt><a href="gya_autogramm-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (1; 100%).

`SCONJ` tokens may have the following values of `Aspect`:

* `Imp` (1; 100% of non-empty `Aspect`): <em>hám</em>
* `EMPTY` (48): <em>nɛ̀, kà, há̰, hé, kóó, mɔ̀-nɛ̀, gá, gá,, mɔ̀, wèn</em>

