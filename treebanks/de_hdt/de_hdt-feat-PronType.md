---
layout: base
title:  'Statistics of PronType in UD_German-HDT'
udver: '2'
---

## Treebank Statistics: UD_German-HDT: Features: `PronType`

This feature is universal.
It occurs with 9 different values: `Art`, `Dem`, `Ind`, `Int`, `Neg`, `Prs`, `Rcp`, `Rel`, `Tot`.
Some words have combined values of the feature; 2 combinations have been observed: `Dem|Rel`, `Int|Rel`.

612722 tokens (18%) have a non-empty value of `PronType`.
491 types (0%) occur at least once with a non-empty value of `PronType`.
142 lemmas (0%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="de_hdt-pos-DET.html">DET</a></tt> (494365; 14% instances), <tt><a href="de_hdt-pos-PRON.html">PRON</a></tt> (94847; 3% instances), <tt><a href="de_hdt-pos-ADV.html">ADV</a></tt> (23510; 1% instances).

### `DET`

494365 <tt><a href="de_hdt-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="de_hdt-feat-NumType.html">NumType</a></tt><tt>=EMPTY</tt> (424404; 86%), <tt><a href="de_hdt-feat-Number.html">Number</a></tt><tt>=Sing</tt> (396065; 80%), <tt><a href="de_hdt-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (359941; 73%).

`DET` tokens may have the following values of `PronType`:

* `Art` (428899; 87% of non-empty `PronType`): <em>der, die, dem, den, das, des, eine, ein, einen, einer</em>
* `Dem` (16335; 3% of non-empty `PronType`): <em>diese, dieser, diesem, dies, dieses, diesen, solche, solchen, solcher, diejenigen</em>
* `Ind` (13248; 3% of non-empty `PronType`): <em>anderen, andere, viele, anderem, einige, mehr, einigen, viel, vielen, anderer</em>
* `Int` (54; 0% of non-empty `PronType`): <em>wieviel, wieviele, wievielen</em>
* `Int,Rel` (815; 0% of non-empty `PronType`): <em>welche, welchen, welcher, welches, welchem, welch</em>
* `Neg` (5538; 1% of non-empty `PronType`): <em>keine, kein, keinen, keinerlei, keiner, keinem, keines, keins</em>
* `Prs` (18373; 4% of non-empty `PronType`): <em>ihre, seine, seiner, seinen, ihrer, ihren, sein, seinem, ihr, ihrem</em>
* `Tot` (11103; 2% of non-empty `PronType`): <em>alle, allem, aller, allen, jeder, beide, alles, jede, jeden, jedem</em>
* `EMPTY` (2): <em>das, der</em>

`PronType` seems to be **lexical feature** of `DET`. 100% lemmas (49) occur only with one value of `PronType`.

### `PRON`

94847 <tt><a href="de_hdt-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="de_hdt-feat-Reflex.html">Reflex</a></tt><tt>=EMPTY</tt> (73703; 78%), <tt><a href="de_hdt-feat-Case.html">Case</a></tt><tt>=Nom</tt> (57883; 61%), <tt><a href="de_hdt-feat-Number.html">Number</a></tt><tt>=Sing</tt> (54467; 57%), <tt><a href="de_hdt-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (50734; 53%), <tt><a href="de_hdt-feat-Person.html">Person</a></tt><tt>=3</tt> (48767; 51%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (6; 0% of non-empty `PronType`): <em>sowas</em>
* `Dem,Rel` (28684; 30% of non-empty `PronType`): <em>die, das, der, dem, denen, deren, dessen, den, derer, d.</em>
* `Ind` (7301; 8% of non-empty `PronType`): <em>man, etwas, jemand, jemanden, irgendetwas, irgendjemand, jemandem, irgendwas, irgendwem, E-irgendwas</em>
* `Int,Rel` (3506; 4% of non-empty `PronType`): <em>was, wer, wem, wen, watt, wessen</em>
* `Neg` (1071; 1% of non-empty `PronType`): <em>nichts, niemand, niemanden, nix, niemandem</em>
* `Prs` (54200; 57% of non-empty `PronType`): <em>sich, es, sie, er, wir, ich, uns, ihm, ihnen, ihn</em>
* `Rcp` (8; 0% of non-empty `PronType`): <em>einander</em>
* `Rel` (2; 0% of non-empty `PronType`): <em>der/das, die/der</em>
* `Tot` (69; 0% of non-empty `PronType`): <em>jedermann, jedermanns, alledem, alldem, jedefrau</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (29) occur only with one value of `PronType`.

### `ADV`

23510 <tt><a href="de_hdt-pos-ADV.html">ADV</a></tt> tokens (12% of all `ADV` tokens) have a non-empty value of `PronType`.

`ADV` tokens may have the following values of `PronType`:

* `Dem` (14467; 62% of non-empty `PronType`): <em>damit, dazu, dafür, davon, dagegen, daher, darauf, darüber, dadurch, daran</em>
* `Ind` (4109; 17% of non-empty `PronType`): <em>mehr, etwas, mehrere, meisten, genug, weniger, mehreren, bisschen, mehrerer, bißchen</em>
* `Int` (4934; 21% of non-empty `PronType`): <em>wie, wo, wann, warum, wobei, wonach, wodurch, womit, inwieweit, weshalb</em>
* `EMPTY` (173084): <em>auch, noch, nur, so, aber, bereits, allerdings, mehr, schon, rund</em>

`PronType` seems to be **lexical feature** of `ADV`. 100% lemmas (70) occur only with one value of `PronType`.

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="de_hdt-dep-conj.html">conj</a></tt>]--> PRON</tt> (44; 94%),
<tt>ADV --[<tt><a href="de_hdt-dep-conj.html">conj</a></tt>]--> ADV</tt> (32; 86%),
<tt>DET --[<tt><a href="de_hdt-dep-reparandum.html">reparandum</a></tt>]--> DET</tt> (13; 87%),
<tt>PRON --[<tt><a href="de_hdt-dep-reparandum.html">reparandum</a></tt>]--> PRON</tt> (4; 100%),
<tt>DET --[<tt><a href="de_hdt-dep-ccomp.html">ccomp</a></tt>]--> DET</tt> (1; 100%).

