---
layout: base
title:  'Statistics of Voice in UD_Uzbek-UzUDT'
udver: '2'
---

## Treebank Statistics: UD_Uzbek-UzUDT: Features: `Voice`

This feature is universal.
It occurs with 2 different values: `Act`, `Pass`.

16 tokens (0%) have a non-empty value of `Voice`.
12 types (0%) occur at least once with a non-empty value of `Voice`.
14 lemmas (1%) occur at least once with a non-empty value of `Voice`.
The feature is used with 1 part-of-speech tags: <tt><a href="uz_uzudt-pos-VERB.html">VERB</a></tt> (16; 0% instances).

### `VERB`

16 <tt><a href="uz_uzudt-pos-VERB.html">VERB</a></tt> tokens (1% of all `VERB` tokens) have a non-empty value of `Voice`.

The most frequent other feature values with which `VERB` and `Voice` co-occurred: <tt><a href="uz_uzudt-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (13; 81%), <tt><a href="uz_uzudt-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (13; 81%), <tt><a href="uz_uzudt-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (13; 81%), <tt><a href="uz_uzudt-feat-Aspect.html">Aspect</a></tt><tt>=EMPTY</tt> (10; 63%), <tt><a href="uz_uzudt-feat-Tense.html">Tense</a></tt><tt>=EMPTY</tt> (9; 56%).

`VERB` tokens may have the following values of `Voice`:

* `Act` (2; 13% of non-empty `Voice`): <em>ketgan, qoraygan</em>
* `Pass` (14; 88% of non-empty `Voice`): <em>qilingan, bilinib, tikilib, boshlanar, chayqalishni, hisoblanadigan, ochilmasdan, qilinsa, qoplangan, yetishtirilgan</em>
* `EMPTY` (1562): <em>debdi, dedi, boʻlib, deb, bor, olib, qilib, ketdi, turib, boshladi</em>

`Voice` seems to be **lexical feature** of `VERB`. 100% lemmas (14) occur only with one value of `Voice`.

