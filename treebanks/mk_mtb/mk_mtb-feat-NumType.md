---
layout: base
title:  'Statistics of NumType in UD_Macedonian-MTB'
udver: '2'
---

## Treebank Statistics: UD_Macedonian-MTB: Features: `NumType`

This feature is universal.
It occurs with 1 different values: `Card`.

11 tokens (1%) have a non-empty value of `NumType`.
10 types (2%) occur at least once with a non-empty value of `NumType`.
10 lemmas (2%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="mk_mtb-pos-NUM.html">NUM</a></tt> (7; 1% instances), <tt><a href="mk_mtb-pos-ADV.html">ADV</a></tt> (4; 0% instances).

### `NUM`

7 <tt><a href="mk_mtb-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="mk_mtb-feat-Definite.html">Definite</a></tt><tt>=Ind</tt> (7; 100%), <tt><a href="mk_mtb-feat-Number.html">Number</a></tt><tt>=Plur</tt> (6; 86%), <tt><a href="mk_mtb-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (4; 57%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (7; 100% of non-empty `NumType`): <em>два, 15, една, неколку, пет, три</em>

### `ADV`

4 <tt><a href="mk_mtb-pos-ADV.html">ADV</a></tt> tokens (7% of all `ADV` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADV` and `NumType` co-occurred: <tt><a href="mk_mtb-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (4; 100%).

`ADV` tokens may have the following values of `NumType`:

* `Card` (4; 100% of non-empty `NumType`): <em>колкава, колку, толкава, толку</em>
* `EMPTY` (53): <em>вчера, дома, како, кога, само, многу, овде, пак, потоа, Јасно</em>

