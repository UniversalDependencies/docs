---
layout: base
title:  'Statistics of ExtPos in UD_Finnish-FTB'
udver: '2'
---

## Treebank Statistics: UD_Finnish-FTB: Features: `ExtPos`

This feature is language-specific.
It occurs with 9 different values: `ADJ`, `ADP`, `ADV`, `AUX`, `CCONJ`, `DET`, `INTJ`, `PRON`, `SCONJ`.

442 tokens (0%) have a non-empty value of `ExtPos`.
135 types (0%) occur at least once with a non-empty value of `ExtPos`.
99 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 12 part-of-speech tags: <tt><a href="fi_ftb-pos-ADV.html">ADV</a></tt> (233; 0% instances), <tt><a href="fi_ftb-pos-PRON.html">PRON</a></tt> (68; 0% instances), <tt><a href="fi_ftb-pos-PART.html">PART</a></tt> (46; 0% instances), <tt><a href="fi_ftb-pos-NOUN.html">NOUN</a></tt> (45; 0% instances), <tt><a href="fi_ftb-pos-SCONJ.html">SCONJ</a></tt> (13; 0% instances), <tt><a href="fi_ftb-pos-VERB.html">VERB</a></tt> (11; 0% instances), <tt><a href="fi_ftb-pos-ADP.html">ADP</a></tt> (10; 0% instances), <tt><a href="fi_ftb-pos-INTJ.html">INTJ</a></tt> (6; 0% instances), <tt><a href="fi_ftb-pos-CCONJ.html">CCONJ</a></tt> (4; 0% instances), <tt><a href="fi_ftb-pos-NUM.html">NUM</a></tt> (3; 0% instances), <tt><a href="fi_ftb-pos-ADJ.html">ADJ</a></tt> (2; 0% instances), <tt><a href="fi_ftb-pos-AUX.html">AUX</a></tt> (1; 0% instances).

### `ADV`

233 <tt><a href="fi_ftb-pos-ADV.html">ADV</a></tt> tokens (2% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="fi_ftb-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (128; 55%).

`ADV` tokens may have the following values of `ExtPos`:

* `ADJ` (4; 2% of non-empty `ExtPos`): <em>niin, itsestään</em>
* `ADV` (60; 26% of non-empty `ExtPos`): <em>ikään, niin, silloin, taas, vähän, näin, enemmän, ennen, pitkin, ylös</em>
* `CCONJ` (1; 0% of non-empty `ExtPos`): <em>mukaan</em>
* `INTJ` (2; 1% of non-empty `ExtPos`): <em>Johan, Jopas</em>
* `SCONJ` (166; 71% of non-empty `ExtPos`): <em>ennen, niin, ilman, paitsi, vasta, silloin, aivan, ihan, ikään, siksi</em>

<table>
  <tr><th>Paradigm <i>niin</i></th><th><tt>ADJ</tt></th><th><tt>ADV</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt>_</tt></td><td></td><td><em>niin</em></td><td><em>niin</em></td></tr>
  <tr><td><tt><tt><a href="fi_ftb-feat-PronType.html">PronType</a></tt><tt>=Dem</tt></tt></td><td><em>niin</em></td><td><em>niin</em></td><td><em>niin</em></td></tr>
</table>

### `PRON`

68 <tt><a href="fi_ftb-pos-PRON.html">PRON</a></tt> tokens (1% of all `PRON` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PRON` and `ExtPos` co-occurred: <tt><a href="fi_ftb-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (68; 100%), <tt><a href="fi_ftb-feat-Number.html">Number</a></tt><tt>=Sing</tt> (64; 94%), <tt><a href="fi_ftb-feat-PronType.html">PronType</a></tt><tt>=Dem</tt> (35; 51%).

`PRON` tokens may have the following values of `ExtPos`:

* `ADV` (20; 29% of non-empty `ExtPos`): <em>saman, sen, tätä, Siitä, missään, samaa, sillä, tähän, tämän</em>
* `AUX` (1; 1% of non-empty `ExtPos`): <em>mikä</em>
* `CCONJ` (1; 1% of non-empty `ExtPos`): <em>sen</em>
* `DET` (3; 4% of non-empty `ExtPos`): <em>joka, harva</em>
* `INTJ` (1; 1% of non-empty `ExtPos`): <em>Mikäs</em>
* `PRON` (22; 32% of non-empty `ExtPos`): <em>toinen, itse, Siitä, niitä</em>
* `SCONJ` (20; 29% of non-empty `ExtPos`): <em>sen, sitä, siitä, siinä, sillä, siltä</em>

<table>
  <tr><th>Paradigm <i>se</i></th><th><tt>ADV</tt></th><th><tt>CCONJ</tt></th><th><tt>PRON</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt><tt><a href="fi_ftb-feat-Case.html">Case</a></tt><tt>=Abl</tt></tt></td><td></td><td></td><td></td><td><em>siltä</em></td></tr>
  <tr><td><tt><tt><a href="fi_ftb-feat-Case.html">Case</a></tt><tt>=Ade</tt></tt></td><td><em>sillä</em></td><td></td><td></td><td><em>sillä</em></td></tr>
  <tr><td><tt><tt><a href="fi_ftb-feat-Case.html">Case</a></tt><tt>=Ela</tt></tt></td><td><em>Siitä</em></td><td></td><td><em>Siitä</em></td><td><em>siitä</em></td></tr>
  <tr><td><tt><tt><a href="fi_ftb-feat-Case.html">Case</a></tt><tt>=Gen</tt></tt></td><td><em>sen</em></td><td><em>sen</em></td><td></td><td><em>sen</em></td></tr>
  <tr><td><tt><tt><a href="fi_ftb-feat-Case.html">Case</a></tt><tt>=Ine</tt></tt></td><td></td><td></td><td></td><td><em>siinä</em></td></tr>
  <tr><td><tt><tt><a href="fi_ftb-feat-Case.html">Case</a></tt><tt>=Par</tt></tt></td><td></td><td></td><td></td><td><em>sitä</em></td></tr>
</table>

### `PART`

46 <tt><a href="fi_ftb-pos-PART.html">PART</a></tt> tokens (2% of all `PART` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PART` and `ExtPos` co-occurred: <tt><a href="fi_ftb-feat-Style.html">Style</a></tt><tt>=EMPTY</tt> (38; 83%).

`PART` tokens may have the following values of `ExtPos`:

* `ADV` (26; 57% of non-empty `ExtPos`): <em>totta, aika, nipin, sen, hädin, juuri, niin, Kas, melko, ties</em>
* `INTJ` (8; 17% of non-empty `ExtPos`): <em>tota, Kas</em>
* `SCONJ` (12; 26% of non-empty `ExtPos`): <em>niin, Entä, sen</em>

<table>
  <tr><th>Paradigm <i>niin</i></th><th><tt>ADV</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt>_</tt></td><td><em>niin</em></td><td><em>niin</em></td></tr>
  <tr><td><tt><tt><a href="fi_ftb-feat-Style.html">Style</a></tt><tt>=Coll</tt></tt></td><td><em>Ni</em></td><td></td></tr>
</table>

### `NOUN`

45 <tt><a href="fi_ftb-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `NOUN` and `ExtPos` co-occurred: <tt><a href="fi_ftb-feat-Number.html">Number</a></tt><tt>=Sing</tt> (31; 69%).

`NOUN` tokens may have the following values of `ExtPos`:

* `ADP` (2; 4% of non-empty `ExtPos`): <em>lukuun</em>
* `ADV` (43; 96% of non-empty `ExtPos`): <em>alun, loppujen, iät, vuosi, aika, ikävä, kerta, kesät, puolin, päivä</em>

`ExtPos` seems to be **lexical feature** of `NOUN`. 100% lemmas (24) occur only with one value of `ExtPos`.

### `SCONJ`

13 <tt><a href="fi_ftb-pos-SCONJ.html">SCONJ</a></tt> tokens (0% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `SCONJ` and `ExtPos` co-occurred: <tt><a href="fi_ftb-feat-Style.html">Style</a></tt><tt>=EMPTY</tt> (12; 92%).

`SCONJ` tokens may have the following values of `ExtPos`:

* `ADV` (1; 8% of non-empty `ExtPos`): <em>ett</em>
* `SCONJ` (12; 92% of non-empty `ExtPos`): <em>kun, jos</em>

### `VERB`

11 <tt><a href="fi_ftb-pos-VERB.html">VERB</a></tt> tokens (0% of all `VERB` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `VERB` and `ExtPos` co-occurred: <tt><a href="fi_ftb-feat-PartForm.html">PartForm</a></tt><tt>=EMPTY</tt> (11; 100%), <tt><a href="fi_ftb-feat-Voice.html">Voice</a></tt><tt>=Act</tt> (11; 100%), <tt><a href="fi_ftb-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (9; 82%), <tt><a href="fi_ftb-feat-InfForm.html">InfForm</a></tt><tt>=EMPTY</tt> (9; 82%), <tt><a href="fi_ftb-feat-Number.html">Number</a></tt><tt>=Sing</tt> (9; 82%), <tt><a href="fi_ftb-feat-Tense.html">Tense</a></tt><tt>=EMPTY</tt> (9; 82%), <tt><a href="fi_ftb-feat-VerbForm.html">VerbForm</a></tt><tt>=Fin</tt> (9; 82%), <tt><a href="fi_ftb-feat-Mood.html">Mood</a></tt><tt>=Ind</tt> (8; 73%), <tt><a href="fi_ftb-feat-Person.html">Person</a></tt><tt>=3</tt> (7; 64%).

`VERB` tokens may have the following values of `ExtPos`:

* `ADV` (10; 91% of non-empty `ExtPos`): <em>ei, mennen, tiedä, tiiät, vuosi</em>
* `INTJ` (1; 9% of non-empty `ExtPos`): <em>ei</em>

<table>
  <tr><th>Paradigm <i>ei</i></th><th><tt>ADV</tt></th><th><tt>INTJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>ei</em></td><td><em>ei</em></td></tr>
</table>

### `ADP`

10 <tt><a href="fi_ftb-pos-ADP.html">ADP</a></tt> tokens (0% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADV` (10; 100% of non-empty `ExtPos`): <em>ennen, ilman</em>

### `INTJ`

6 <tt><a href="fi_ftb-pos-INTJ.html">INTJ</a></tt> tokens (2% of all `INTJ` tokens) have a non-empty value of `ExtPos`.

`INTJ` tokens may have the following values of `ExtPos`:

* `INTJ` (6; 100% of non-empty `ExtPos`): <em>voi, No</em>

### `CCONJ`

4 <tt><a href="fi_ftb-pos-CCONJ.html">CCONJ</a></tt> tokens (0% of all `CCONJ` tokens) have a non-empty value of `ExtPos`.

`CCONJ` tokens may have the following values of `ExtPos`:

* `CCONJ` (4; 100% of non-empty `ExtPos`): <em>saati</em>

### `NUM`

3 <tt><a href="fi_ftb-pos-NUM.html">NUM</a></tt> tokens (0% of all `NUM` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `NUM` and `ExtPos` co-occurred: <tt><a href="fi_ftb-feat-NumType.html">NumType</a></tt><tt>=Card</tt> (3; 100%), <tt><a href="fi_ftb-feat-Number.html">Number</a></tt><tt>=Sing</tt> (3; 100%), <tt><a href="fi_ftb-feat-Case.html">Case</a></tt><tt>=Nom</tt> (2; 67%).

`NUM` tokens may have the following values of `ExtPos`:

* `ADV` (2; 67% of non-empty `ExtPos`): <em>yhtä, yksi</em>
* `DET` (1; 33% of non-empty `ExtPos`): <em>kolkyt</em>

### `ADJ`

2 <tt><a href="fi_ftb-pos-ADJ.html">ADJ</a></tt> tokens (0% of all `ADJ` tokens) have a non-empty value of `ExtPos`.

`ADJ` tokens may have the following values of `ExtPos`:

* `ADV` (2; 100% of non-empty `ExtPos`): <em>pikku, kumma</em>

### `AUX`

1 <tt><a href="fi_ftb-pos-AUX.html">AUX</a></tt> tokens (0% of all `AUX` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `AUX` and `ExtPos` co-occurred: <tt><a href="fi_ftb-feat-Mood.html">Mood</a></tt><tt>=Ind</tt> (1; 100%), <tt><a href="fi_ftb-feat-Number.html">Number</a></tt><tt>=Sing</tt> (1; 100%), <tt><a href="fi_ftb-feat-Person.html">Person</a></tt><tt>=3</tt> (1; 100%), <tt><a href="fi_ftb-feat-Polarity.html">Polarity</a></tt><tt>=Neg</tt> (1; 100%), <tt><a href="fi_ftb-feat-Tense.html">Tense</a></tt><tt>=EMPTY</tt> (1; 100%), <tt><a href="fi_ftb-feat-VerbForm.html">VerbForm</a></tt><tt>=Fin</tt> (1; 100%), <tt><a href="fi_ftb-feat-Voice.html">Voice</a></tt><tt>=Act</tt> (1; 100%).

`AUX` tokens may have the following values of `ExtPos`:

* `CCONJ` (1; 100% of non-empty `ExtPos`): <em>ei</em>

