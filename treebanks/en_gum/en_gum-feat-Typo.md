---
layout: base
title:  'Statistics of Typo in UD_English-GUM'
udver: '2'
---

## Treebank Statistics: UD_English-GUM: Features: `Typo`

This feature is language-specific.
It occurs with 1 different values: `Yes`.

340 tokens (0%) have a non-empty value of `Typo`.
201 types (1%) occur at least once with a non-empty value of `Typo`.
167 lemmas (1%) occur at least once with a non-empty value of `Typo`.
The feature is used with 17 part-of-speech tags: <tt><a href="en_gum-pos-PUNCT.html">PUNCT</a></tt> (65; 0% instances), <tt><a href="en_gum-pos-VERB.html">VERB</a></tt> (58; 0% instances), <tt><a href="en_gum-pos-INTJ.html">INTJ</a></tt> (31; 0% instances), <tt><a href="en_gum-pos-DET.html">DET</a></tt> (26; 0% instances), <tt><a href="en_gum-pos-ADV.html">ADV</a></tt> (25; 0% instances), <tt><a href="en_gum-pos-AUX.html">AUX</a></tt> (25; 0% instances), <tt><a href="en_gum-pos-ADP.html">ADP</a></tt> (24; 0% instances), <tt><a href="en_gum-pos-ADJ.html">ADJ</a></tt> (17; 0% instances), <tt><a href="en_gum-pos-NOUN.html">NOUN</a></tt> (16; 0% instances), <tt><a href="en_gum-pos-PART.html">PART</a></tt> (11; 0% instances), <tt><a href="en_gum-pos-X.html">X</a></tt> (10; 0% instances), <tt><a href="en_gum-pos-SCONJ.html">SCONJ</a></tt> (9; 0% instances), <tt><a href="en_gum-pos-PROPN.html">PROPN</a></tt> (7; 0% instances), <tt><a href="en_gum-pos-PRON.html">PRON</a></tt> (6; 0% instances), <tt><a href="en_gum-pos-CCONJ.html">CCONJ</a></tt> (5; 0% instances), <tt><a href="en_gum-pos-NUM.html">NUM</a></tt> (4; 0% instances), <tt><a href="en_gum-pos-SYM.html">SYM</a></tt> (1; 0% instances).

### `PUNCT`

65 <tt><a href="en_gum-pos-PUNCT.html">PUNCT</a></tt> tokens (0% of all `PUNCT` tokens) have a non-empty value of `Typo`.

`PUNCT` tokens may have the following values of `Typo`:

* `Yes` (65; 100% of non-empty `Typo`): <em>—, –, ,, ., [, ", ], ), -, --</em>

`Typo` seems to be **lexical feature** of `PUNCT`. 100% lemmas (12) occur only with one value of `Typo`.

### `VERB`

58 <tt><a href="en_gum-pos-VERB.html">VERB</a></tt> tokens (0% of all `VERB` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `VERB` and `Typo` co-occurred: <tt><a href="en_gum-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (38; 66%), <tt><a href="en_gum-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (37; 64%), <tt><a href="en_gum-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (37; 64%).

`VERB` tokens may have the following values of `Typo`:

* `Yes` (58; 100% of non-empty `Typo`): <em>been, have, l-, 's, Keep, address, begun, breath, cause, coded</em>

`Typo` seems to be **lexical feature** of `VERB`. 100% lemmas (41) occur only with one value of `Typo`.

### `INTJ`

31 <tt><a href="en_gum-pos-INTJ.html">INTJ</a></tt> tokens (5% of all `INTJ` tokens) have a non-empty value of `Typo`.

`INTJ` tokens may have the following values of `Typo`:

* `Yes` (31; 100% of non-empty `Typo`): <em>w-, duh, Th, like, th-, Woo, Y-, a-, alreet, alroot</em>

`Typo` seems to be **lexical feature** of `INTJ`. 100% lemmas (22) occur only with one value of `Typo`.

### `DET`

26 <tt><a href="en_gum-pos-DET.html">DET</a></tt> tokens (0% of all `DET` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `DET` and `Typo` co-occurred: <tt><a href="en_gum-feat-PronType.html">PronType</a></tt><tt>=Art</tt> (18; 69%).

`DET` tokens may have the following values of `Typo`:

* `Yes` (26; 100% of non-empty `Typo`): <em>the, a, a-, all, an, d-, on, to, What, these</em>

### `ADV`

25 <tt><a href="en_gum-pos-ADV.html">ADV</a></tt> tokens (0% of all `ADV` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `ADV` and `Typo` co-occurred: <tt><a href="en_gum-feat-Degree.html">Degree</a></tt><tt>=Pos</tt> (15; 60%).

`ADV` tokens may have the following values of `Typo`:

* `Yes` (25; 100% of non-empty `Typo`): <em>before, then, as, maybe, only, them, well, actually, around, awhile</em>

`Typo` seems to be **lexical feature** of `ADV`. 100% lemmas (22) occur only with one value of `Typo`.

### `AUX`

25 <tt><a href="en_gum-pos-AUX.html">AUX</a></tt> tokens (0% of all `AUX` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `AUX` and `Typo` co-occurred: <tt><a href="en_gum-feat-VerbForm.html">VerbForm</a></tt><tt>=Fin</tt> (20; 80%), <tt><a href="en_gum-feat-Mood.html">Mood</a></tt><tt>=Ind</tt> (13; 52%).

`AUX` tokens may have the following values of `Typo`:

* `Yes` (25; 100% of non-empty `Typo`): <em>can, has, are, be, been, is, 'd, 'm, 's, 've</em>

### `ADP`

24 <tt><a href="en_gum-pos-ADP.html">ADP</a></tt> tokens (0% of all `ADP` tokens) have a non-empty value of `Typo`.

`ADP` tokens may have the following values of `Typo`:

* `Yes` (24; 100% of non-empty `Typo`): <em>of, on, in, to, with, a, as, at, fro, from</em>

`Typo` seems to be **lexical feature** of `ADP`. 100% lemmas (11) occur only with one value of `Typo`.

### `ADJ`

17 <tt><a href="en_gum-pos-ADJ.html">ADJ</a></tt> tokens (0% of all `ADJ` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `ADJ` and `Typo` co-occurred: <tt><a href="en_gum-feat-Degree.html">Degree</a></tt><tt>=Pos</tt> (16; 94%).

`ADJ` tokens may have the following values of `Typo`:

* `Yes` (17; 100% of non-empty `Typo`): <em>I.=, appropriate, authentic, correct, difficult, educative, first, least, non-Muslim, northeastern</em>

`Typo` seems to be **lexical feature** of `ADJ`. 100% lemmas (17) occur only with one value of `Typo`.

### `NOUN`

16 <tt><a href="en_gum-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `NOUN` and `Typo` co-occurred: <tt><a href="en_gum-feat-Number.html">Number</a></tt><tt>=Sing</tt> (16; 100%).

`NOUN` tokens may have the following values of `Typo`:

* `Yes` (16; 100% of non-empty `Typo`): <em>Color, F, air, app-, b-, bag, break, hum-, lotos, migration</em>

`Typo` seems to be **lexical feature** of `NOUN`. 100% lemmas (16) occur only with one value of `Typo`.

### `PART`

11 <tt><a href="en_gum-pos-PART.html">PART</a></tt> tokens (0% of all `PART` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `PART` and `Typo` co-occurred: <tt><a href="en_gum-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (9; 82%).

`PART` tokens may have the following values of `Typo`:

* `Yes` (11; 100% of non-empty `Typo`): <em>s, 's, to, na, not, the</em>

### `X`

10 <tt><a href="en_gum-pos-X.html">X</a></tt> tokens (3% of all `X` tokens) have a non-empty value of `Typo`.

`X` tokens may have the following values of `Typo`:

* `Yes` (10; 100% of non-empty `Typo`): <em>etc, hand, N-, S-, balls, cent, is</em>

### `SCONJ`

9 <tt><a href="en_gum-pos-SCONJ.html">SCONJ</a></tt> tokens (0% of all `SCONJ` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `SCONJ` and `Typo` co-occurred: <tt><a href="en_gum-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (8; 89%).

`SCONJ` tokens may have the following values of `Typo`:

* `Yes` (9; 100% of non-empty `Typo`): <em>cuz, UNLESS, before, despite, that, till, how</em>

### `PROPN`

7 <tt><a href="en_gum-pos-PROPN.html">PROPN</a></tt> tokens (0% of all `PROPN` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `PROPN` and `Typo` co-occurred: <tt><a href="en_gum-feat-Number.html">Number</a></tt><tt>=Sing</tt> (7; 100%).

`PROPN` tokens may have the following values of `Typo`:

* `Yes` (7; 100% of non-empty `Typo`): <em>lotos, F&AM, Chatnam, Hell</em>

### `PRON`

6 <tt><a href="en_gum-pos-PRON.html">PRON</a></tt> tokens (0% of all `PRON` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `PRON` and `Typo` co-occurred: <tt><a href="en_gum-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (6; 100%), <tt><a href="en_gum-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (6; 100%), <tt><a href="en_gum-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (6; 100%), <tt><a href="en_gum-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (5; 83%), <tt><a href="en_gum-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (4; 67%).

`PRON` tokens may have the following values of `Typo`:

* `Yes` (6; 100% of non-empty `Typo`): <em>Th-, what, that, which, ya</em>

### `CCONJ`

5 <tt><a href="en_gum-pos-CCONJ.html">CCONJ</a></tt> tokens (0% of all `CCONJ` tokens) have a non-empty value of `Typo`.

`CCONJ` tokens may have the following values of `Typo`:

* `Yes` (5; 100% of non-empty `Typo`): <em>and</em>

### `NUM`

4 <tt><a href="en_gum-pos-NUM.html">NUM</a></tt> tokens (0% of all `NUM` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `NUM` and `Typo` co-occurred: <tt><a href="en_gum-feat-NumType.html">NumType</a></tt><tt>=Card</tt> (4; 100%).

`NUM` tokens may have the following values of `Typo`:

* `Yes` (4; 100% of non-empty `Typo`): <em>2, 4, thr-, three</em>

### `SYM`

1 <tt><a href="en_gum-pos-SYM.html">SYM</a></tt> tokens (0% of all `SYM` tokens) have a non-empty value of `Typo`.

`SYM` tokens may have the following values of `Typo`:

* `Yes` (1; 100% of non-empty `Typo`): <em>/</em>

## Relations with Agreement in `Typo`

The 10 most frequent relations where parent and child node agree in `Typo`:
<tt>INTJ --[<tt><a href="en_gum-dep-punct.html">punct</a></tt>]--> PUNCT</tt> (8; 53%),
<tt>DET --[<tt><a href="en_gum-dep-case.html">case</a></tt>]--> ADP</tt> (3; 100%),
<tt>ADV --[<tt><a href="en_gum-dep-advmod.html">advmod</a></tt>]--> ADV</tt> (2; 67%),
<tt>ADV --[<tt><a href="en_gum-dep-cc.html">cc</a></tt>]--> CCONJ</tt> (2; 100%),
<tt>ADV --[<tt><a href="en_gum-dep-goeswith.html">goeswith</a></tt>]--> X</tt> (2; 100%),
<tt>ADJ --[<tt><a href="en_gum-dep-cc.html">cc</a></tt>]--> CCONJ</tt> (1; 100%),
<tt>ADJ --[<tt><a href="en_gum-dep-cc.html">cc</a></tt>]--> SYM</tt> (1; 100%),
<tt>ADP --[<tt><a href="en_gum-dep-punct.html">punct</a></tt>]--> PUNCT</tt> (1; 100%),
<tt>ADV --[<tt><a href="en_gum-dep-conj.html">conj</a></tt>]--> ADV</tt> (1; 100%),
<tt>ADV --[<tt><a href="en_gum-dep-cop.html">cop</a></tt>]--> AUX</tt> (1; 100%).

