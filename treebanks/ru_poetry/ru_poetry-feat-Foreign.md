---
layout: base
title:  'Statistics of Foreign in UD_Russian-Poetry'
udver: '2'
---

## Treebank Statistics: UD_Russian-Poetry: Features: `Foreign`

This feature is universal.
It occurs with 1 different values: `Yes`.

55 tokens (0%) have a non-empty value of `Foreign`.
49 types (0%) occur at least once with a non-empty value of `Foreign`.
48 lemmas (0%) occur at least once with a non-empty value of `Foreign`.
The feature is used with 2 part-of-speech tags: <tt><a href="ru_poetry-pos-X.html">X</a></tt> (53; 0% instances), <tt><a href="ru_poetry-pos-PART.html">PART</a></tt> (2; 0% instances).

### `X`

53 <tt><a href="ru_poetry-pos-X.html">X</a></tt> tokens (96% of all `X` tokens) have a non-empty value of `Foreign`.

`X` tokens may have the following values of `Foreign`:

* `Yes` (53; 100% of non-empty `Foreign`): <em>е, и, сань, цю, Dànse, EIFEL, FANDANGO, IMPROMPTU, In, Jesekiila</em>
* `EMPTY` (2): <em>нибудь, се</em>

`Foreign` seems to be **lexical feature** of `X`. 100% lemmas (46) occur only with one value of `Foreign`.

### `PART`

2 <tt><a href="ru_poetry-pos-PART.html">PART</a></tt> tokens (0% of all `PART` tokens) have a non-empty value of `Foreign`.

The most frequent other feature values with which `PART` and `Foreign` co-occurred: <tt><a href="ru_poetry-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (2; 100%).

`PART` tokens may have the following values of `Foreign`:

* `Yes` (2; 100% of non-empty `Foreign`): <em>Ван, ДА</em>
* `EMPTY` (1820): <em>не, же, и, вот, ли, только, лишь, уж, пусть, ль</em>

## Relations with Agreement in `Foreign`

The 10 most frequent relations where parent and child node agree in `Foreign`:
<tt>X --[<tt><a href="ru_poetry-dep-flat-foreign.html">flat:foreign</a></tt>]--> X</tt> (34; 100%),
<tt>X --[<tt><a href="ru_poetry-dep-conj.html">conj</a></tt>]--> X</tt> (2; 100%).

