

--------------------------------------------------------------------------------

## Treebank Statistics (UD_Latvian)

This feature is universal.
It occurs with 1 different values: `Yes`.

40 tokens (0%) have a non-empty value of `Poss`.
16 types (0%) occur at least once with a non-empty value of `Poss`.
5 lemmas (0%) occur at least once with a non-empty value of `Poss`.
The feature is used with 2 part-of-speech tags: [lv-pos/DET]() (37; 0% instances), [lv-pos/PRON]() (3; 0% instances).

### `DET`

37 [lv-pos/DET]() tokens (10% of all `DET` tokens) have a non-empty value of `Poss`.

The most frequent other feature values with which `DET` and `Poss` co-occurred: <tt><a href="PronType.html">PronType</a>=Prs</tt> (37; 100%), <tt><a href="Number.html">Number</a>=Sing</tt> (26; 70%), <tt><a href="Case.html">Case</a>=Acc</tt> (22; 59%), <tt><a href="Gender.html">Gender</a>=Masc</tt> (19; 51%).

`DET` tokens may have the following values of `Poss`:

* `Yes` (37; 100% of non-empty `Poss`): <em>savu, savā, savas, saviem, mana, manas, manu, savus, Mans, Savās</em>
* `EMPTY` (340): <em>šo, tā, šī, šajā, šāda, šādu, šīs, šis, to, visas</em>

### `PRON`

3 [lv-pos/PRON]() tokens (1% of all `PRON` tokens) have a non-empty value of `Poss`.

The most frequent other feature values with which `PRON` and `Poss` co-occurred: <tt><a href="Person.html">Person</a>=EMPTY</tt> (3; 100%), <tt><a href="PronType.html">PronType</a>=Prs</tt> (3; 100%), <tt><a href="Gender.html">Gender</a>=Fem</tt> (2; 67%), <tt><a href="Number.html">Number</a>=Sing</tt> (2; 67%).

`PRON` tokens may have the following values of `Poss`:

* `Yes` (3; 100% of non-empty `Poss`): <em>mans, savu, savās</em>
* `EMPTY` (542): <em>tas, to, viņš, tā, kas, es, viņa, ko, man, mēs</em>

