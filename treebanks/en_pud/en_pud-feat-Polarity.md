---
layout: base
title:  'Statistics of Polarity in UD_English-PUD'
udver: '2'
---

## Treebank Statistics: UD_English-PUD: Features: `Polarity`

This feature is universal.
It occurs with 1 different values: `Neg`.

100 tokens (0%) have a non-empty value of `Polarity`.
7 types (0%) occur at least once with a non-empty value of `Polarity`.
4 lemmas (0%) occur at least once with a non-empty value of `Polarity`.
The feature is used with 3 part-of-speech tags: <tt><a href="en_pud-pos-PART.html">PART</a></tt> (56; 0% instances), <tt><a href="en_pud-pos-ADV.html">ADV</a></tt> (34; 0% instances), <tt><a href="en_pud-pos-DET.html">DET</a></tt> (10; 0% instances).

### `PART`

56 <tt><a href="en_pud-pos-PART.html">PART</a></tt> tokens (13% of all `PART` tokens) have a non-empty value of `Polarity`.

`PART` tokens may have the following values of `Polarity`:

* `Neg` (56; 100% of non-empty `Polarity`): <em>not, n't, n’t</em>
* `EMPTY` (370): <em>to, 's, ’s, ', not, up</em>

### `ADV`

34 <tt><a href="en_pud-pos-ADV.html">ADV</a></tt> tokens (4% of all `ADV` tokens) have a non-empty value of `Polarity`.

The most frequent other feature values with which `ADV` and `Polarity` co-occurred: <tt><a href="en_pud-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (34; 100%).

`ADV` tokens may have the following values of `Polarity`:

* `Neg` (34; 100% of non-empty `Polarity`): <em>not, no, n’t, never, n't</em>
* `EMPTY` (814): <em>also, when, so, only, more, where, however, most, well, as</em>

### `DET`

10 <tt><a href="en_pud-pos-DET.html">DET</a></tt> tokens (0% of all `DET` tokens) have a non-empty value of `Polarity`.

The most frequent other feature values with which `DET` and `Polarity` co-occurred: <tt><a href="en_pud-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (10; 100%), <tt><a href="en_pud-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (10; 100%).

`DET` tokens may have the following values of `Polarity`:

* `Neg` (10; 100% of non-empty `Polarity`): <em>no</em>
* `EMPTY` (2076): <em>the, a, an, this, all, that, some, these, each, both</em>

