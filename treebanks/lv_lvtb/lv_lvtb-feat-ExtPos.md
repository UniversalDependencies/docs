---
layout: base
title:  'Statistics of ExtPos in UD_Latvian-LVTB'
udver: '2'
---

## Treebank Statistics: UD_Latvian-LVTB: Features: `ExtPos`

This feature is language-specific.
It occurs with 5 different values: `ADV`, `CCONJ`, `DET`, `PART`, `SCONJ`.

1205 tokens (0%) have a non-empty value of `ExtPos`.
52 types (0%) occur at least once with a non-empty value of `ExtPos`.
31 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 5 part-of-speech tags: <tt><a href="lv_lvtb-pos-PART.html">PART</a></tt> (513; 0% instances), <tt><a href="lv_lvtb-pos-CCONJ.html">CCONJ</a></tt> (359; 0% instances), <tt><a href="lv_lvtb-pos-SCONJ.html">SCONJ</a></tt> (212; 0% instances), <tt><a href="lv_lvtb-pos-ADV.html">ADV</a></tt> (97; 0% instances), <tt><a href="lv_lvtb-pos-DET.html">DET</a></tt> (24; 0% instances).

### `PART`

513 <tt><a href="lv_lvtb-pos-PART.html">PART</a></tt> tokens (7% of all `PART` tokens) have a non-empty value of `ExtPos`.

`PART` tokens may have the following values of `ExtPos`:

* `CCONJ` (271; 53% of non-empty `ExtPos`): <em>kā, it, tāpat, Ne, ka, tikai</em>
* `PART` (197; 38% of non-empty `ExtPos`): <em>it, droši, ne, vai, kaut, tā, diez, Nu, gan, tikpat</em>
* `SCONJ` (45; 9% of non-empty `ExtPos`): <em>it, tā, itin, tāpat</em>

<table>
  <tr><th>Paradigm <i>it</i></th><th><tt>CCONJ</tt></th><th><tt>PART</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>it</em></td><td><em>it</em></td><td><em>it</em></td></tr>
</table>

### `CCONJ`

359 <tt><a href="lv_lvtb-pos-CCONJ.html">CCONJ</a></tt> tokens (3% of all `CCONJ` tokens) have a non-empty value of `ExtPos`.

`CCONJ` tokens may have the following values of `ExtPos`:

* `CCONJ` (359; 100% of non-empty `ExtPos`): <em>bet, vai, ne, gan, kā, un</em>

### `SCONJ`

212 <tt><a href="lv_lvtb-pos-SCONJ.html">SCONJ</a></tt> tokens (3% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

`SCONJ` tokens may have the following values of `ExtPos`:

* `SCONJ` (212; 100% of non-empty `ExtPos`): <em>lai, tā, kaut, tāpat, ja, tāpēc, kā</em>

### `ADV`

97 <tt><a href="lv_lvtb-pos-ADV.html">ADV</a></tt> tokens (1% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="lv_lvtb-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (97; 100%), <tt><a href="lv_lvtb-feat-Degree.html">Degree</a></tt><tt>=Cmp</tt> (93; 96%).

`ADV` tokens may have the following values of `ExtPos`:

* `ADV` (97; 100% of non-empty `ExtPos`): <em>vairāk, tikpat, mazāk, ilgāk</em>

### `DET`

24 <tt><a href="lv_lvtb-pos-DET.html">DET</a></tt> tokens (0% of all `DET` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `DET` and `ExtPos` co-occurred: <tt><a href="lv_lvtb-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (24; 100%), <tt><a href="lv_lvtb-feat-PronType.html">PronType</a></tt><tt>=Dem</tt> (24; 100%), <tt><a href="lv_lvtb-feat-Number.html">Number</a></tt><tt>=Sing</tt> (19; 79%), <tt><a href="lv_lvtb-feat-Gender.html">Gender</a></tt><tt>=Fem</tt> (15; 63%).

`DET` tokens may have the following values of `ExtPos`:

* `DET` (24; 100% of non-empty `ExtPos`): <em>tādu, tāds, tādā, tāda, tādas, tādi, tādās</em>

