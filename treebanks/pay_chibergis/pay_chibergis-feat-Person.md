---
layout: base
title:  'Statistics of Person in UD_Pesh-ChibErgIS'
udver: '2'
---

## Treebank Statistics: UD_Pesh-ChibErgIS: Features: `Person`

This feature is universal.
It occurs with 3 different values: `1`, `2`, `3`.

41 tokens (1%) have a non-empty value of `Person`.
29 types (3%) occur at least once with a non-empty value of `Person`.
22 lemmas (4%) occur at least once with a non-empty value of `Person`.
The feature is used with 2 part-of-speech tags: <tt><a href="pay_chibergis-pos-VERB.html">VERB</a></tt> (35; 1% instances), <tt><a href="pay_chibergis-pos-AUX.html">AUX</a></tt> (6; 0% instances).

### `VERB`

35 <tt><a href="pay_chibergis-pos-VERB.html">VERB</a></tt> tokens (4% of all `VERB` tokens) have a non-empty value of `Person`.

The most frequent other feature values with which `VERB` and `Person` co-occurred: <tt><a href="pay_chibergis-feat-Clusivity.html">Clusivity</a></tt><tt>=EMPTY</tt> (35; 100%), <tt><a href="pay_chibergis-feat-Voice.html">Voice</a></tt><tt>=EMPTY</tt> (33; 94%).

`VERB` tokens may have the following values of `Person`:

* `1` (28; 80% of non-empty `Person`): <em>piãpa, kaporpa, teʔpa, tʃãpa, yãhpa, akonapa, artʃuiʃpa, iʃpa, kapai, kawiʃpa</em>
* `2` (6; 17% of non-empty `Person`): <em>nẽya, kaya, takaya, tʃãya</em>
* `3` (1; 3% of non-empty `Person`): <em>kawiʃkawa</em>
* `EMPTY` (921): <em>tVki, teʔ, tVerwa, tVktaiiwã, nã, nẽ, nãi, artʃuiʃkari, taõʃi, nẽerwa</em>

<table>
  <tr><th>Paradigm <i>ka</i></th><th><tt>1</tt></th><th><tt>2</tt></th></tr>
  <tr><td><tt></tt></td><td><em>kapai</em></td><td><em>kaya, takaya</em></td></tr>
</table>

### `AUX`

6 <tt><a href="pay_chibergis-pos-AUX.html">AUX</a></tt> tokens (5% of all `AUX` tokens) have a non-empty value of `Person`.

The most frequent other feature values with which `AUX` and `Person` co-occurred: <tt><a href="pay_chibergis-feat-Clusivity.html">Clusivity</a></tt><tt>=EMPTY</tt> (5; 83%).

`AUX` tokens may have the following values of `Person`:

* `1` (2; 33% of non-empty `Person`): <em>tʃatVpa, =bartVwa</em>
* `2` (4; 67% of non-empty `Person`): <em>=rya</em>
* `EMPTY` (113): <em>=ri, =hi, =i, tʃaawa, tʃabaruri, =leri, =ĩ, aki, akuh, tʃawa</em>

<table>
  <tr><th>Paradigm <i>r</i></th><th><tt>1</tt></th><th><tt>2</tt></th></tr>
  <tr><td><tt>_</tt></td><td></td><td><em>=rya</em></td></tr>
  <tr><td><tt><tt><a href="pay_chibergis-feat-Clusivity.html">Clusivity</a></tt><tt>=Ex</tt></tt></td><td><em>=bartVwa</em></td><td></td></tr>
</table>

