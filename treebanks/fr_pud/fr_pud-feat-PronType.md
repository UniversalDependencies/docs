---
layout: base
title:  'Statistics of PronType in UD_French-PUD'
udver: '2'
---

## Treebank Statistics: UD_French-PUD: Features: `PronType`

This feature is universal.
It occurs with 2 different values: `Neg`, `Prs`.

286 tokens (1%) have a non-empty value of `PronType`.
24 types (0%) occur at least once with a non-empty value of `PronType`.
3 lemmas (0%) occur at least once with a non-empty value of `PronType`.
The feature is used with 2 part-of-speech tags: <tt><a href="fr_pud-pos-DET.html">DET</a></tt> (283; 1% instances), <tt><a href="fr_pud-pos-PRON.html">PRON</a></tt> (3; 0% instances).

### `DET`

283 <tt><a href="fr_pud-pos-DET.html">DET</a></tt> tokens (7% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="fr_pud-feat-Number.html">Number</a></tt><tt>=Sing</tt> (208; 73%), <tt><a href="fr_pud-feat-Gender.html">Gender</a></tt><tt>=Fem</tt> (149; 53%).

`DET` tokens may have the following values of `PronType`:

* `Neg` (8; 3% of non-empty `PronType`): <em>aucun, aucune</em>
* `Prs` (275; 97% of non-empty `PronType`): <em>son, sa, ses, leur, leurs, notre, nos, mon, votre, ma</em>
* `EMPTY` (3597): <em>le, la, les, l', un, une, des, l’, cette, ce</em>

### `PRON`

3 <tt><a href="fr_pud-pos-PRON.html">PRON</a></tt> tokens (0% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="fr_pud-feat-Number.html">Number</a></tt><tt>=Sing</tt> (3; 100%), <tt><a href="fr_pud-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (2; 67%), <tt><a href="fr_pud-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (2; 67%).

`PRON` tokens may have the following values of `PronType`:

* `Neg` (2; 67% of non-empty `PronType`): <em>aucun</em>
* `Prs` (1; 33% of non-empty `PronType`): <em>sienne</em>
* `EMPTY` (1060): <em>il, qui, se, elle, ils, y, s', on, je, ce</em>

