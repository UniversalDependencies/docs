---
layout: base
title:  'Statistics of ExtPos in UD_Ancient_Greek-PTNK'
udver: '2'
---

## Treebank Statistics: UD_Ancient_Greek-PTNK: Features: `ExtPos`

This feature is language-specific.
It occurs with 3 different values: `ADV`, `CCONJ`, `SCONJ`.

6 tokens (0%) have a non-empty value of `ExtPos`.
3 types (0%) occur at least once with a non-empty value of `ExtPos`.
3 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 2 part-of-speech tags: <tt><a href="grc_ptnk-pos-CCONJ.html">CCONJ</a></tt> (4; 0% instances), <tt><a href="grc_ptnk-pos-SCONJ.html">SCONJ</a></tt> (2; 0% instances).

### `CCONJ`

4 <tt><a href="grc_ptnk-pos-CCONJ.html">CCONJ</a></tt> tokens (0% of all `CCONJ` tokens) have a non-empty value of `ExtPos`.

`CCONJ` tokens may have the following values of `ExtPos`:

* `ADV` (1; 25% of non-empty `ExtPos`): <em>καὶ</em>
* `CCONJ` (3; 75% of non-empty `ExtPos`): <em>καὶ</em>

<table>
  <tr><th>Paradigm <i>καί</i></th><th><tt>ADV</tt></th><th><tt>CCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>καὶ</em></td><td><em>καὶ</em></td></tr>
</table>

### `SCONJ`

2 <tt><a href="grc_ptnk-pos-SCONJ.html">SCONJ</a></tt> tokens (0% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

`SCONJ` tokens may have the following values of `ExtPos`:

* `SCONJ` (2; 100% of non-empty `ExtPos`): <em>εἰ, ἕως</em>

