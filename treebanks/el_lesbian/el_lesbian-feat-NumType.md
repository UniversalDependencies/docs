---
layout: base
title:  'Statistics of NumType in UD_Greek-Lesbian'
udver: '2'
---

## Treebank Statistics: UD_Greek-Lesbian: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

59 tokens (1%) have a non-empty value of `NumType`.
29 types (1%) occur at least once with a non-empty value of `NumType`.
25 lemmas (2%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="el_lesbian-pos-NUM.html">NUM</a></tt> (58; 1% instances), <tt><a href="el_lesbian-pos-ADJ.html">ADJ</a></tt> (1; 0% instances).

### `NUM`

58 <tt><a href="el_lesbian-pos-NUM.html">NUM</a></tt> tokens (95% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="el_lesbian-feat-Case.html">Case</a></tt><tt>=Acc</tt> (32; 55%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (58; 100% of non-empty `NumType`): <em>δυο, ένα, μια, 100, 1912, 9, τρία, 12, 120, 13</em>
* `EMPTY` (3): <em>1954, 1970, να</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (24) occur only with one value of `NumType`.

### `ADJ`

1 <tt><a href="el_lesbian-pos-ADJ.html">ADJ</a></tt> tokens (1% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="el_lesbian-feat-Case.html">Case</a></tt><tt>=Nom</tt> (1; 100%), <tt><a href="el_lesbian-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (1; 100%), <tt><a href="el_lesbian-feat-Number.html">Number</a></tt><tt>=Sing</tt> (1; 100%).

`ADJ` tokens may have the following values of `NumType`:

* `Ord` (1; 100% of non-empty `NumType`): <em>πρώτους</em>
* `EMPTY` (173): <em>καλή, καλό, καημένους, Τούρκους, αγιά, μαύρα, μεγάλ’, αρχαία, θκα, θκο</em>

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="el_lesbian-dep-conj.html">conj</a></tt>]--> NUM</tt> (4; 100%),
<tt>NUM --[<tt><a href="el_lesbian-dep-compound.html">compound</a></tt>]--> NUM</tt> (1; 100%),
<tt>NUM --[<tt><a href="el_lesbian-dep-fixed.html">fixed</a></tt>]--> NUM</tt> (1; 100%),
<tt>NUM --[<tt><a href="el_lesbian-dep-obl.html">obl</a></tt>]--> NUM</tt> (1; 100%).

