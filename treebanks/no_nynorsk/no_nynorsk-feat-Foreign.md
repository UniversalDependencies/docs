---
layout: base
title:  'Statistics of Foreign in UD_Norwegian-Nynorsk'
udver: '2'
---

## Treebank Statistics: UD_Norwegian-Nynorsk: Features: `Foreign`

This feature is universal.
It occurs with 1 different values: `Yes`.

1021 tokens (0%) have a non-empty value of `Foreign`.
637 types (2%) occur at least once with a non-empty value of `Foreign`.
637 lemmas (3%) occur at least once with a non-empty value of `Foreign`.
The feature is used with 1 part-of-speech tags: <tt><a href="no_nynorsk-pos-X.html">X</a></tt> (1021; 0% instances).

### `X`

1021 <tt><a href="no_nynorsk-pos-X.html">X</a></tt> tokens (83% of all `X` tokens) have a non-empty value of `Foreign`.

`X` tokens may have the following values of `Foreign`:

* `Yes` (1021; 100% of non-empty `Foreign`): <em>det, en, i, og, the, of, som, til, den, for</em>
* `EMPTY` (211): <em>of, the, for, barn, og, and, in, cup, to, X</em>

`Foreign` seems to be **lexical feature** of `X`. 100% lemmas (637) occur only with one value of `Foreign`.

## Relations with Agreement in `Foreign`

The 10 most frequent relations where parent and child node agree in `Foreign`:
<tt>X --[<tt><a href="no_nynorsk-dep-flat-foreign.html">flat:foreign</a></tt>]--> X</tt> (874; 100%),
<tt>X --[<tt><a href="no_nynorsk-dep-conj.html">conj</a></tt>]--> X</tt> (3; 100%).

