---
layout: base
title:  'Statistics of AdvType in UD_Vietnamese-TueCL'
udver: '2'
---

## Treebank Statistics: UD_Vietnamese-TueCL: Features: `AdvType`

This feature is language-specific.
It occurs with 6 different values: `Cau`, `Deg`, `Loc`, `Man`, `Mod`, `Tim`.

86 tokens (5%) have a non-empty value of `AdvType`.
30 types (4%) occur at least once with a non-empty value of `AdvType`.
27 lemmas (4%) occur at least once with a non-empty value of `AdvType`.
The feature is used with 1 part-of-speech tags: <tt><a href="vi_tuecl-pos-ADV.html">ADV</a></tt> (86; 5% instances).

### `ADV`

86 <tt><a href="vi_tuecl-pos-ADV.html">ADV</a></tt> tokens (46% of all `ADV` tokens) have a non-empty value of `AdvType`.

The most frequent other feature values with which `ADV` and `AdvType` co-occurred: <tt><a href="vi_tuecl-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (84; 98%).

`ADV` tokens may have the following values of `AdvType`:

* `Cau` (2; 2% of non-empty `AdvType`): <em>vì sao, vì thế</em>
* `Deg` (13; 15% of non-empty `AdvType`): <em>nhất, rất, khá, nhiều, quá, vô cùng</em>
* `Loc` (2; 2% of non-empty `AdvType`): <em>ngoài, trong</em>
* `Man` (1; 1% of non-empty `AdvType`): <em>sao</em>
* `Mod` (17; 20% of non-empty `AdvType`): <em>chỉ, hãy, được, đừng</em>
* `Tim` (51; 59% of non-empty `AdvType`): <em>đã, sẽ, đang, bao giờ, Hôm nay, bây giờ, nay, sắp, trước đây, mới</em>

`AdvType` seems to be **lexical feature** of `ADV`. 100% lemmas (27) occur only with one value of `AdvType`.

