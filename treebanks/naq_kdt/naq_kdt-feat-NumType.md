---
layout: base
title:  'Statistics of NumType in UD_Khoekhoe-KDT'
udver: '2'
---

## Treebank Statistics: UD_Khoekhoe-KDT: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

225 tokens (1%) have a non-empty value of `NumType`.
92 types (3%) occur at least once with a non-empty value of `NumType`.
59 lemmas (3%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="naq_kdt-pos-NUM.html">NUM</a></tt> (188; 1% instances), <tt><a href="naq_kdt-pos-ADJ.html">ADJ</a></tt> (37; 0% instances).

### `NUM`

188 <tt><a href="naq_kdt-pos-NUM.html">NUM</a></tt> tokens (98% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="naq_kdt-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (147; 78%), <tt><a href="naq_kdt-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (146; 78%), <tt><a href="naq_kdt-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (146; 78%), <tt><a href="naq_kdt-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (146; 78%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (188; 100% of non-empty `NumType`): <em>ǀgui, ǀgam, koro, ǀguis, ǃnona, dīsi, haka, korodīsi, ǃnanidīsi, hakadīsi</em>
* `EMPTY` (3): <em>ǃnani, khoeseǀab</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (50) occur only with one value of `NumType`.

### `ADJ`

37 <tt><a href="naq_kdt-pos-ADJ.html">ADJ</a></tt> tokens (5% of all `ADJ` tokens) have a non-empty value of `NumType`.

`ADJ` tokens may have the following values of `NumType`:

* `Ord` (37; 100% of non-empty `NumType`): <em>ǂguro, ǀgamǁî, ǃnonaǁî, koroǁî, hakaǁî, ǀgamǀaǁî, ǀguiǀaǁî, ǁkhaisadīsiǀgamǀâǁî, ǂkhî, ǃNaniǁî</em>
* `EMPTY` (737): <em>ǃgâi, kai, ǂhanu, ǀkhara, ǂoaǂamsa, ama, ǂhâǂhâsa, American, ǀgui, ǃgom</em>

`NumType` seems to be **lexical feature** of `ADJ`. 100% lemmas (11) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="naq_kdt-dep-flat-num.html">flat:num</a></tt>]--> NUM</tt> (18; 100%),
<tt>NUM --[<tt><a href="naq_kdt-dep-parataxis.html">parataxis</a></tt>]--> NUM</tt> (8; 100%),
<tt>NUM --[<tt><a href="naq_kdt-dep-conj.html">conj</a></tt>]--> NUM</tt> (7; 100%).

