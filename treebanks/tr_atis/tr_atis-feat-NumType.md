---
layout: base
title:  'Statistics of NumType in UD_Turkish-Atis'
udver: '2'
---

## Treebank Statistics: UD_Turkish-Atis: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

961 tokens (2%) have a non-empty value of `NumType`.
167 types (8%) occur at least once with a non-empty value of `NumType`.
153 lemmas (16%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="tr_atis-pos-NUM.html">NUM</a></tt> (889; 2% instances), <tt><a href="tr_atis-pos-ADJ.html">ADJ</a></tt> (72; 0% instances).

### `NUM`

889 <tt><a href="tr_atis-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (880; 99% of non-empty `NumType`): <em>7, on, 1, 27, 2, 6, 5, yirmi, bir, 12</em>
* `Ord` (9; 1% of non-empty `NumType`): <em>altıncı, 19:00, 28., 5:55, 7:00, 8., birinci, yedinci</em>

<table>
  <tr><th>Paradigm <i>8</i></th><th><tt>Card</tt></th><th><tt>Ord</tt></th></tr>
  <tr><td><tt></tt></td><td><em>8</em></td><td><em>8.</em></td></tr>
</table>

`NumType` seems to be **lexical feature** of `NUM`. 99% lemmas (148) occur only with one value of `NumType`.

### `ADJ`

72 <tt><a href="tr_atis-pos-ADJ.html">ADJ</a></tt> tokens (1% of all `ADJ` tokens) have a non-empty value of `NumType`.

`ADJ` tokens may have the following values of `NumType`:

* `Card` (72; 100% of non-empty `NumType`): <em>kaç, 1991'deki, 1992'deki, 1993'teki, 420'deki, altısındaki, sekizindeki</em>
* `EMPTY` (5671): <em>olan, var, hangi, kalkan, giden, ucuz, arasındaki, varan, aktarmasız, tek</em>

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="tr_atis-dep-conj.html">conj</a></tt>]--> NUM</tt> (48; 98%),
<tt>NUM --[<tt><a href="tr_atis-dep-compound.html">compound</a></tt>]--> NUM</tt> (41; 98%),
<tt>NUM --[<tt><a href="tr_atis-dep-clf.html">clf</a></tt>]--> NUM</tt> (1; 100%).

