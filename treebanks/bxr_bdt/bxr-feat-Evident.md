---
layout: base
title:  'Statistics of Evident in UD_Buryat'
udver: '2'
---

## Treebank Statistics: UD_Buryat: Features: `Evident`

This feature is universal but the values `Fh` are language-specific.
It occurs with 1 different values: `Fh`.

65 tokens (1%) have a non-empty value of `Evident`.
1 types (0%) occur at least once with a non-empty value of `Evident`.
1 lemmas (0%) occur at least once with a non-empty value of `Evident`.
The feature is used with 1 part-of-speech tags: <tt><a href="bxr-pos-AUX.html">AUX</a></tt> (65; 1% instances).

### `AUX`

65 <tt><a href="bxr-pos-AUX.html">AUX</a></tt> tokens (34% of all `AUX` tokens) have a non-empty value of `Evident`.

The most frequent other feature values with which `AUX` and `Evident` co-occurred: <tt><a href="bxr-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (65; 100%), <tt><a href="bxr-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (65; 100%), <tt><a href="bxr-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (65; 100%), <tt><a href="bxr-feat-Tense.html">Tense</a></tt><tt>=EMPTY</tt> (65; 100%), <tt><a href="bxr-feat-VerbForm.html">VerbForm</a></tt><tt>=EMPTY</tt> (65; 100%).

`AUX` tokens may have the following values of `Evident`:

* `Fh` (65; 100% of non-empty `Evident`): юм
* `EMPTY` (127): байна байгаа байһан байгааб гээшэб бай бэлэй байба байдаг шадаха

