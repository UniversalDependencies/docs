---
layout: base
title:  'Statistics of NumForm in UD_Spanish-PUD'
udver: '2'
---

## Treebank Statistics: UD_Spanish-PUD: Features: `NumForm`

This feature is language-specific.
It occurs with 3 different values: `Digit`, `Roman`, `Word`.

435 tokens (2%) have a non-empty value of `NumForm`.
234 types (4%) occur at least once with a non-empty value of `NumForm`.
228 lemmas (5%) occur at least once with a non-empty value of `NumForm`.
The feature is used with 1 part-of-speech tags: <tt><a href="es_pud-pos-NUM.html">NUM</a></tt> (435; 2% instances).

### `NUM`

435 <tt><a href="es_pud-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumForm`.

The most frequent other feature values with which `NUM` and `NumForm` co-occurred: <tt><a href="es_pud-feat-NumType.html">NumType</a></tt><tt>=Card</tt> (435; 100%), <tt><a href="es_pud-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (375; 86%).

`NUM` tokens may have the following values of `NumForm`:

* `Digit` (313; 72% of non-empty `NumForm`): <em>1, 10, 3, 70, 100, 1492, 20, 2010, 2014, 2015</em>
* `Roman` (11; 3% of non-empty `NumForm`): <em>III, XVI, XX, V, VIII, XIII, XV</em>
* `Word` (111; 26% of non-empty `NumForm`): <em>dos, tres, cuatro, mil, seis, cinco, diez, siete, treinta, un</em>

`NumForm` seems to be **lexical feature** of `NUM`. 100% lemmas (228) occur only with one value of `NumForm`.

## Relations with Agreement in `NumForm`

The 10 most frequent relations where parent and child node agree in `NumForm`:
<tt>NUM --[<tt><a href="es_pud-dep-conj.html">conj</a></tt>]--> NUM</tt> (11; 100%).

