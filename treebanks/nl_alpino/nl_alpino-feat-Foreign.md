---
layout: base
title:  'Statistics of Foreign in UD_Dutch-Alpino'
udver: '2'
---

## Treebank Statistics: UD_Dutch-Alpino: Features: `Foreign`

This feature is universal.
It occurs with 1 different values: `Yes`.

588 tokens (0%) have a non-empty value of `Foreign`.
411 types (2%) occur at least once with a non-empty value of `Foreign`.
410 lemmas (2%) occur at least once with a non-empty value of `Foreign`.
The feature is used with 1 part-of-speech tags: <tt><a href="nl_alpino-pos-X.html">X</a></tt> (588; 0% instances).

### `X`

588 <tt><a href="nl_alpino-pos-X.html">X</a></tt> tokens (62% of all `X` tokens) have a non-empty value of `Foreign`.

The most frequent other feature values with which `X` and `Foreign` co-occurred: <tt><a href="nl_alpino-feat-Abbr.html">Abbr</a></tt><tt>=EMPTY</tt> (588; 100%).

`X` tokens may have the following values of `Foreign`:

* `Yes` (588; 100% of non-empty `Foreign`): <em>the, fancy, ad, a, in, of, Grand, fiction, to, world</em>
* `EMPTY` (368): <em>pct., o.a., jl., o.m., pct, mln., a.s., binnen-, rk, v.j.</em>

`Foreign` seems to be **lexical feature** of `X`. 100% lemmas (410) occur only with one value of `Foreign`.

## Relations with Agreement in `Foreign`

The 10 most frequent relations where parent and child node agree in `Foreign`:
<tt>X --[<tt><a href="nl_alpino-dep-fixed.html">fixed</a></tt>]--> X</tt> (339; 100%),
<tt>X --[<tt><a href="nl_alpino-dep-conj.html">conj</a></tt>]--> X</tt> (7; 100%).

