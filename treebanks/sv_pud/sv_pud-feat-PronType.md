---
layout: base
title:  'Statistics of PronType in UD_Swedish-PUD'
udver: '2'
---

## Treebank Statistics: UD_Swedish-PUD: Features: `PronType`

This feature is universal.
It occurs with 9 different values: `Art`, `Dem`, `Ind`, `Int`, `Neg`, `Prs`, `Rcp`, `Rel`, `Tot`.
Some words have combined values of the feature; 1 combinations have been observed: `Int|Rel`.

2446 tokens (13%) have a non-empty value of `PronType`.
132 types (2%) occur at least once with a non-empty value of `PronType`.
53 lemmas (1%) occur at least once with a non-empty value of `PronType`.
The feature is used with 4 part-of-speech tags: <tt><a href="sv_pud-pos-PRON.html">PRON</a></tt> (1338; 7% instances), <tt><a href="sv_pud-pos-DET.html">DET</a></tt> (1016; 5% instances), <tt><a href="sv_pud-pos-SCONJ.html">SCONJ</a></tt> (50; 0% instances), <tt><a href="sv_pud-pos-ADV.html">ADV</a></tt> (42; 0% instances).

### `PRON`

1338 <tt><a href="sv_pud-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="sv_pud-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (1080; 81%), <tt><a href="sv_pud-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (946; 71%), <tt><a href="sv_pud-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (911; 68%).

`PRON` tokens may have the following values of `PronType`:

* `Art` (1; 0% of non-empty `PronType`): <em>de</em>
* `Dem` (35; 3% of non-empty `PronType`): <em>detta, dessa, denna, det, den, dennes</em>
* `Ind` (46; 3% of non-empty `PronType`): <em>mer, mycket, många, någon, andra, något, Fler, annat, mesta, mångt</em>
* `Int,Rel` (301; 22% of non-empty `PronType`): <em>som, vad, vilket, vilka, vem, vilken, vars, vilkas</em>
* `Neg` (4; 0% of non-empty `PronType`): <em>inget, ingen, intet</em>
* `Prs` (927; 69% of non-empty `PronType`): <em>det, han, de, sig, hans, jag, sin, hon, den, sina</em>
* `Rcp` (1; 0% of non-empty `PronType`): <em>varandra</em>
* `Tot` (23; 2% of non-empty `PronType`): <em>alla, allt, båda, allting, alltihop, alltsammans, bådas</em>

<table>
  <tr><th>Paradigm <i>den</i></th><th><tt>Prs</tt></th><th><tt>Dem</tt></th></tr>
  <tr><td><tt><tt><a href="sv_pud-feat-Gender.html">Gender</a></tt><tt>=Com</tt>|<tt><a href="sv_pud-feat-Number.html">Number</a></tt><tt>=Sing</tt></tt></td><td><em>den</em></td><td><em>den</em></td></tr>
  <tr><td><tt><tt><a href="sv_pud-feat-Gender.html">Gender</a></tt><tt>=Neut</tt>|<tt><a href="sv_pud-feat-Number.html">Number</a></tt><tt>=Sing</tt></tt></td><td><em>det</em></td><td><em>det</em></td></tr>
  <tr><td><tt><tt><a href="sv_pud-feat-Poss.html">Poss</a></tt><tt>=Yes</tt></tt></td><td><em>dess</em></td><td></td></tr>
</table>

`PronType` seems to be **lexical feature** of `PRON`. 94% lemmas (34) occur only with one value of `PronType`.

### `DET`

1016 <tt><a href="sv_pud-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="sv_pud-feat-Number.html">Number</a></tt><tt>=Sing</tt> (838; 82%), <tt><a href="sv_pud-feat-Gender.html">Gender</a></tt><tt>=Com</tt> (577; 57%).

`DET` tokens may have the following values of `PronType`:

* `Art` (876; 86% of non-empty `PronType`): <em>en, den, ett, de, det, the, a, Die, El, Le</em>
* `Dem` (65; 6% of non-empty `PronType`): <em>denna, den, dessa, detta, det, That</em>
* `Ind` (26; 3% of non-empty `PronType`): <em>några, någon, något, lite</em>
* `Int,Rel` (3; 0% of non-empty `PronType`): <em>vilket, vilken</em>
* `Neg` (9; 1% of non-empty `PronType`): <em>ingen, inget, inga</em>
* `Tot` (37; 4% of non-empty `PronType`): <em>varje, alla, båda, all, allt</em>

<table>
  <tr><th>Paradigm <i>den</i></th><th><tt>Art</tt></th><th><tt>Dem</tt></th></tr>
  <tr><td><tt><tt><a href="sv_pud-feat-Gender.html">Gender</a></tt><tt>=Com</tt></tt></td><td><em>den</em></td><td><em>den</em></td></tr>
  <tr><td><tt><tt><a href="sv_pud-feat-Gender.html">Gender</a></tt><tt>=Neut</tt></tt></td><td><em>det</em></td><td><em>det</em></td></tr>
</table>

`PronType` seems to be **lexical feature** of `DET`. 94% lemmas (17) occur only with one value of `PronType`.

### `SCONJ`

50 <tt><a href="sv_pud-pos-SCONJ.html">SCONJ</a></tt> tokens (13% of all `SCONJ` tokens) have a non-empty value of `PronType`.

`SCONJ` tokens may have the following values of `PronType`:

* `Int,Rel` (50; 100% of non-empty `PronType`): <em>när, då</em>
* `EMPTY` (334): <em>att, som, än, om, medan, eftersom, innan, fastän, huruvida, då</em>

### `ADV`

42 <tt><a href="sv_pud-pos-ADV.html">ADV</a></tt> tokens (4% of all `ADV` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADV` and `PronType` co-occurred: <tt><a href="sv_pud-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (42; 100%).

`ADV` tokens may have the following values of `PronType`:

* `Int,Rel` (42; 100% of non-empty `PronType`): <em>där, hur, varför, dit, var, varav, då</em>
* `EMPTY` (948): <em>så, in, ut, också, bara, upp, fram, här, även, mer</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="sv_pud-dep-conj.html">conj</a></tt>]--> PRON</tt> (3; 100%).

