---
layout: base
title:  'Statistics of Polarity in UD_Hausa-NorthernAutogramm'
udver: '2'
---

## Treebank Statistics: UD_Hausa-NorthernAutogramm: Features: `Polarity`

This feature is universal.
It occurs with 1 different values: `Neg`.

356 tokens (2%) have a non-empty value of `Polarity`.
26 types (1%) occur at least once with a non-empty value of `Polarity`.
6 lemmas (1%) occur at least once with a non-empty value of `Polarity`.
The feature is used with 2 part-of-speech tags: <tt><a href="ha_northernautogramm-pos-PART.html">PART</a></tt> (199; 1% instances), <tt><a href="ha_northernautogramm-pos-AUX.html">AUX</a></tt> (157; 1% instances).

### `PART`

199 <tt><a href="ha_northernautogramm-pos-PART.html">PART</a></tt> tokens (30% of all `PART` tokens) have a non-empty value of `Polarity`.

The most frequent other feature values with which `PART` and `Polarity` co-occurred: <tt><a href="ha_northernautogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (199; 100%).

`PART` tokens may have the following values of `Polarity`:

* `Neg` (199; 100% of non-empty `Polarity`): <em>ba, baːbù, bàː, bâː, dàk, dàsh</em>
* `EMPTY` (473): <em>ta, mài, nàː, gàː, màːsu, dai, kuma, kòː, na, àkwai</em>

### `AUX`

157 <tt><a href="ha_northernautogramm-pos-AUX.html">AUX</a></tt> tokens (6% of all `AUX` tokens) have a non-empty value of `Polarity`.

The most frequent other feature values with which `AUX` and `Polarity` co-occurred: <tt><a href="ha_northernautogramm-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (157; 100%), <tt><a href="ha_northernautogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (97; 62%), <tt><a href="ha_northernautogramm-feat-Aspect.html">Aspect</a></tt><tt>=Perf</tt> (89; 57%), <tt><a href="ha_northernautogramm-feat-Person.html">Person</a></tt><tt>=3</tt> (84; 54%).

`AUX` tokens may have the following values of `Polarity`:

* `Neg` (157; 100% of non-empty `Polarity`): <em>bài, baːnì, bàn, baːshì, bâi, bàkà, bàsù, baːkà, bàkù, bàmù</em>
* `EMPTY` (2381): <em>yac, sunkà, yaː, shì, kà, shinàː, ankà, yat, naː, yay</em>

## Relations with Agreement in `Polarity`

The 10 most frequent relations where parent and child node agree in `Polarity`:
<tt>PART --[<tt><a href="ha_northernautogramm-dep-parataxis.html">parataxis</a></tt>]--> PART</tt> (4; 80%),
<tt>PART --[<tt><a href="ha_northernautogramm-dep-conj.html">conj</a></tt>]--> PART</tt> (2; 100%),
<tt>AUX --[<tt><a href="ha_northernautogramm-dep-parataxis.html">parataxis</a></tt>]--> AUX</tt> (1; 100%),
<tt>PART --[<tt><a href="ha_northernautogramm-dep-advmod.html">advmod</a></tt>]--> PART</tt> (1; 100%).

