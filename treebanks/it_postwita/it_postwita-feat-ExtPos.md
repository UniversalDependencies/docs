---
layout: base
title:  'Statistics of ExtPos in UD_Italian-PoSTWITA'
udver: '2'
---

## Treebank Statistics: UD_Italian-PoSTWITA: Features: `ExtPos`

This feature is language-specific.
It occurs with 2 different values: `ADV`, `NOUN`.

13 tokens (0%) have a non-empty value of `ExtPos`.
10 types (0%) occur at least once with a non-empty value of `ExtPos`.
9 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 2 part-of-speech tags: <tt><a href="it_postwita-pos-ADP.html">ADP</a></tt> (10; 0% instances), <tt><a href="it_postwita-pos-SCONJ.html">SCONJ</a></tt> (3; 0% instances).

### `ADP`

10 <tt><a href="it_postwita-pos-ADP.html">ADP</a></tt> tokens (0% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADV` (8; 80% of non-empty `ExtPos`): <em>fino, OLTRE, davanti, fuori, intorno, prima</em>
* `NOUN` (2; 20% of non-empty `ExtPos`): <em>grazie, rispetto</em>

### `SCONJ`

3 <tt><a href="it_postwita-pos-SCONJ.html">SCONJ</a></tt> tokens (0% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

`SCONJ` tokens may have the following values of `ExtPos`:

* `ADV` (3; 100% of non-empty `ExtPos`): <em>invece, oltre, prima</em>

