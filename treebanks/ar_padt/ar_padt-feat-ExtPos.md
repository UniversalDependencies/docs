---
layout: base
title:  'Statistics of ExtPos in UD_Arabic-PADT'
udver: '2'
---

## Treebank Statistics: UD_Arabic-PADT: Features: `ExtPos`

This feature is language-specific.
It occurs with 3 different values: `ADP`, `ADV`, `SCONJ`.

2149 tokens (1%) have a non-empty value of `ExtPos`.
52 types (0%) occur at least once with a non-empty value of `ExtPos`.
47 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 8 part-of-speech tags: <tt><a href="ar_padt-pos-ADP.html">ADP</a></tt> (1548; 1% instances), <tt><a href="ar_padt-pos-SCONJ.html">SCONJ</a></tt> (334; 0% instances), <tt><a href="ar_padt-pos-CCONJ.html">CCONJ</a></tt> (199; 0% instances), <tt><a href="ar_padt-pos-DET.html">DET</a></tt> (18; 0% instances), <tt><a href="ar_padt-pos-X.html">X</a></tt> (16; 0% instances), <tt><a href="ar_padt-pos-ADV.html">ADV</a></tt> (14; 0% instances), <tt><a href="ar_padt-pos-PART.html">PART</a></tt> (14; 0% instances), <tt><a href="ar_padt-pos-ADJ.html">ADJ</a></tt> (6; 0% instances).

### `ADP`

1548 <tt><a href="ar_padt-pos-ADP.html">ADP</a></tt> tokens (4% of all `ADP` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADP` and `ExtPos` co-occurred: <tt><a href="ar_padt-feat-AdpType.html">AdpType</a></tt><tt>=Prep</tt> (1548; 100%), <tt><a href="ar_padt-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (1534; 99%).

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (1384; 89% of non-empty `ExtPos`): ب، من، في، على، ل، فى، عن، إلى، الى، امام
* `SCONJ` (164; 11% of non-empty `ExtPos`): في، على، ب، من، رغم، ل، منذ، دون، فى، ك

<table>
  <tr><th>Paradigm <i>فِي</i></th><th><tt>ADP</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td>في, فى</td><td>في, فى</td></tr>
</table>

### `SCONJ`

334 <tt><a href="ar_padt-pos-SCONJ.html">SCONJ</a></tt> tokens (6% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

`SCONJ` tokens may have the following values of `ExtPos`:

* `SCONJ` (334; 100% of non-empty `ExtPos`): أن، إن، ان

### `CCONJ`

199 <tt><a href="ar_padt-pos-CCONJ.html">CCONJ</a></tt> tokens (1% of all `CCONJ` tokens) have a non-empty value of `ExtPos`.

`CCONJ` tokens may have the following values of `ExtPos`:

* `ADP` (112; 56% of non-empty `ExtPos`): و، ف، لكن، اما، كما
* `SCONJ` (87; 44% of non-empty `ExtPos`): و، اذا، ف، إذا، حيث، طالما، لأن، أي، طال، لذا

<table>
  <tr><th>Paradigm <i>وَ</i></th><th><tt>ADP</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td>و</td><td>و</td></tr>
</table>

### `DET`

18 <tt><a href="ar_padt-pos-DET.html">DET</a></tt> tokens (0% of all `DET` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `DET` and `ExtPos` co-occurred: <tt><a href="ar_padt-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (18; 100%), <tt><a href="ar_padt-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (18; 100%), <tt><a href="ar_padt-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (18; 100%), <tt><a href="ar_padt-feat-PronType.html">PronType</a></tt><tt>=Int</tt> (18; 100%).

`DET` tokens may have the following values of `ExtPos`:

* `SCONJ` (18; 100% of non-empty `ExtPos`): ما

### `X`

16 <tt><a href="ar_padt-pos-X.html">X</a></tt> tokens (0% of all `X` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `X` and `ExtPos` co-occurred: <tt><a href="ar_padt-feat-Foreign.html">Foreign</a></tt><tt>=EMPTY</tt> (16; 100%).

`X` tokens may have the following values of `ExtPos`:

* `ADP` (12; 75% of non-empty `ExtPos`): بناء، لكن، هما، هم
* `SCONJ` (4; 25% of non-empty `ExtPos`): لكن، ما

<table>
  <tr><th>Paradigm <i>لكن</i></th><th><tt>ADP</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td>لكن</td><td>لكن</td></tr>
</table>

### `ADV`

14 <tt><a href="ar_padt-pos-ADV.html">ADV</a></tt> tokens (1% of all `ADV` tokens) have a non-empty value of `ExtPos`.

`ADV` tokens may have the following values of `ExtPos`:

* `ADP` (9; 64% of non-empty `ExtPos`): بدلاً، بدلا
* `SCONJ` (5; 36% of non-empty `ExtPos`): حتى

### `PART`

14 <tt><a href="ar_padt-pos-PART.html">PART</a></tt> tokens (1% of all `PART` tokens) have a non-empty value of `ExtPos`.

`PART` tokens may have the following values of `ExtPos`:

* `ADP` (3; 21% of non-empty `ExtPos`): ربما، لا، ناهيك
* `SCONJ` (11; 79% of non-empty `ExtPos`): إلا، الا، إن، ل، لا

<table>
  <tr><th>Paradigm <i>لَا</i></th><th><tt>ADP</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td>لا</td><td>لا</td></tr>
</table>

### `ADJ`

6 <tt><a href="ar_padt-pos-ADJ.html">ADJ</a></tt> tokens (0% of all `ADJ` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADJ` and `ExtPos` co-occurred: <tt><a href="ar_padt-feat-Case.html">Case</a></tt><tt>=Acc</tt> (6; 100%), <tt><a href="ar_padt-feat-Definite.html">Definite</a></tt><tt>=Ind</tt> (6; 100%), <tt><a href="ar_padt-feat-Number.html">Number</a></tt><tt>=Sing</tt> (6; 100%), <tt><a href="ar_padt-feat-Gender.html">Gender</a></tt><tt>=Fem</tt> (4; 67%).

`ADJ` tokens may have the following values of `ExtPos`:

* `ADV` (6; 100% of non-empty `ExtPos`): خاصة، بعيداً، غالبا

