---
layout: base
title:  'Statistics of Foreign in UD_Dutch'
udver: '2'
---

## Treebank Statistics: UD_Dutch: Features: `Foreign`

This feature is universal.
It occurs with 1 different values: `Yes`.

503 tokens (0%) have a non-empty value of `Foreign`.
353 types (1%) occur at least once with a non-empty value of `Foreign`.
353 lemmas (2%) occur at least once with a non-empty value of `Foreign`.
The feature is used with 1 part-of-speech tags: <tt><a href="nl-pos-X.html">X</a></tt> (503; 0% instances).

### `X`

503 <tt><a href="nl-pos-X.html">X</a></tt> tokens (67% of all `X` tokens) have a non-empty value of `Foreign`.

The most frequent other feature values with which `X` and `Foreign` co-occurred: <tt><a href="nl-feat-Abbr.html">Abbr</a></tt><tt>=EMPTY</tt> (503; 100%).

`X` tokens may have the following values of `Foreign`:

* `Yes` (503; 100% of non-empty `Foreign`): <em>fancy, a, the, and, front, to, National, be, fiction, flo</em>
* `EMPTY` (248): <em>o.a., jl., o.m., Ad, a.s., binnen-, etc., v.j., enz., spraak-</em>

`Foreign` seems to be **lexical feature** of `X`. 100% lemmas (353) occur only with one value of `Foreign`.

## Relations with Agreement in `Foreign`

The 10 most frequent relations where parent and child node agree in `Foreign`:
<tt>X --[<tt><a href="nl-dep-fixed.html">fixed</a></tt>]--> X</tt> (168; 100%),
<tt>X --[<tt><a href="nl-dep-flat-name.html">flat:name</a></tt>]--> X</tt> (5; 100%),
<tt>X --[<tt><a href="nl-dep-nmod.html">nmod</a></tt>]--> X</tt> (4; 100%),
<tt>X --[<tt><a href="nl-dep-conj.html">conj</a></tt>]--> X</tt> (2; 100%),
<tt>X --[<tt><a href="nl-dep-case.html">case</a></tt>]--> X</tt> (1; 100%).

