---
layout: base
title:  'Statistics of Foreign in UD_Greek'
udver: '2'
---

## Treebank Statistics: UD_Greek: Features: `Foreign`

This feature is universal.
It occurs with 1 different values: `Yes`.

972 tokens (2%) have a non-empty value of `Foreign`.
497 types (4%) occur at least once with a non-empty value of `Foreign`.
497 lemmas (8%) occur at least once with a non-empty value of `Foreign`.
The feature is used with 1 part-of-speech tags: <tt><a href="el-pos-X.html">X</a></tt> (972; 2% instances).

### `X`

972 <tt><a href="el-pos-X.html">X</a></tt> tokens (100% of all `X` tokens) have a non-empty value of `Foreign`.

`X` tokens may have the following values of `Foreign`:

* `Yes` (972; 100% of non-empty `Foreign`): <em>αλ, Κάστρο, σκορ, Αφγανιστάν, Ουάσιγκτον, Σαντόρουμ, Μιτ, Ρικ, Ρόμνεϊ, Θιβέτ</em>

`Foreign` seems to be **lexical feature** of `X`. 100% lemmas (497) occur only with one value of `Foreign`.

## Relations with Agreement in `Foreign`

The 10 most frequent relations where parent and child node agree in `Foreign`:
<tt>X --[<tt><a href="el-dep-nmod.html">nmod</a></tt>]--> X</tt> (251; 100%),
<tt>X --[<tt><a href="el-dep-conj.html">conj</a></tt>]--> X</tt> (36; 100%),
<tt>X --[<tt><a href="el-dep-appos.html">appos</a></tt>]--> X</tt> (1; 100%),
<tt>X --[<tt><a href="el-dep-case.html">case</a></tt>]--> X</tt> (1; 100%),
<tt>X --[<tt><a href="el-dep-compound.html">compound</a></tt>]--> X</tt> (1; 100%),
<tt>X --[<tt><a href="el-dep-orphan.html">orphan</a></tt>]--> X</tt> (1; 100%).

