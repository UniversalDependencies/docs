---
layout: base
title:  'Statistics of PronType in UD_Middle_French-PROFITEROLE'
udver: '2'
---

## Treebank Statistics: UD_Middle_French-PROFITEROLE: Features: `PronType`

This feature is universal.
It occurs with 6 different values: `Art`, `Dem`, `Ind`, `Int`, `Prs`, `Rel`.

18130 tokens (15%) have a non-empty value of `PronType`.
225 types (2%) occur at least once with a non-empty value of `PronType`.
61 lemmas (2%) occur at least once with a non-empty value of `PronType`.
The feature is used with 4 part-of-speech tags: <tt><a href="frm_profiterole-pos-PRON.html">PRON</a></tt> (9390; 8% instances), <tt><a href="frm_profiterole-pos-DET.html">DET</a></tt> (8565; 7% instances), <tt><a href="frm_profiterole-pos-ADJ.html">ADJ</a></tt> (152; 0% instances), <tt><a href="frm_profiterole-pos-ADV.html">ADV</a></tt> (23; 0% instances).

### `PRON`

9390 <tt><a href="frm_profiterole-pos-PRON.html">PRON</a></tt> tokens (81% of all `PRON` tokens) have a non-empty value of `PronType`.

`PRON` tokens may have the following values of `PronType`:

* `Dem` (838; 9% of non-empty `PronType`): <em>ce, ceulx, c', celuy, cela, cellui, cecy, ceuls, celle, icelle</em>
* `Ind` (1072; 11% of non-empty `PronType`): <em>on, autres, tous, autre, chascun, riens, un, en, tout, aucuns</em>
* `Int` (33; 0% of non-empty `PronType`): <em>que, qui, qu', ou, quoy</em>
* `Prs` (5534; 59% of non-empty `PronType`): <em>il, ilz, vous, se, luy, je, nous, s', le, les</em>
* `Rel` (1913; 20% of non-empty `PronType`): <em>qui, que, qu', dont, où, quoy, lequel, ou, laquelle, lesquelz</em>
* `EMPTY` (2155): <em>qui, nous, il, ou, en, que, c', ce, ilz, laquelle</em>

<table>
  <tr><th>Paradigm <i>qui1</i></th><th><tt>Int</tt></th><th><tt>Rel</tt></th></tr>
  <tr><td><tt></tt></td><td><em>qui</em></td><td><em>qui</em></td></tr>
</table>

`PronType` seems to be **lexical feature** of `PRON`. 92% lemmas (47) occur only with one value of `PronType`.

### `DET`

8565 <tt><a href="frm_profiterole-pos-DET.html">DET</a></tt> tokens (64% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="frm_profiterole-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (8565; 100%), <tt><a href="frm_profiterole-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (6494; 76%).

`DET` tokens may have the following values of `PronType`:

* `Art` (7028; 82% of non-empty `PronType`): <em>le, la, les, l', ung, une, ledit, un, des, lesdits</em>
* `Dem` (593; 7% of non-empty `PronType`): <em>ce, ceste, ces, celle, cest, celui, ycelui, celuy, icellui, celi</em>
* `Ind` (883; 10% of non-empty `PronType`): <em>tous, toutes, tout, plusieurs, autres, toute, quelque, pluseurs, aucune, aucuns</em>
* `Int` (7; 0% of non-empty `PronType`): <em>quel, quelz, quelle</em>
* `Rel` (54; 1% of non-empty `PronType`): <em>quel, laquelle, lequel, lesquelles, quelz, quelle, quelles, lesquiex, lesquelz, quelque</em>
* `EMPTY` (4873): <em>le, la, son, nostre, l', ses, les, une, sa, leur</em>

<table>
  <tr><th>Paradigm <i>quelque</i></th><th><tt>Rel</tt></th><th><tt>Ind</tt></th></tr>
  <tr><td><tt></tt></td><td><em>quelque</em></td><td><em>quelque, quelques</em></td></tr>
</table>

`PronType` seems to be **lexical feature** of `DET`. 90% lemmas (19) occur only with one value of `PronType`.

### `ADJ`

152 <tt><a href="frm_profiterole-pos-ADJ.html">ADJ</a></tt> tokens (4% of all `ADJ` tokens) have a non-empty value of `PronType`.

`ADJ` tokens may have the following values of `PronType`:

* `Ind` (152; 100% of non-empty `PronType`): <em>autres, aultres, autre, telle, meismes, mesmes, aultre, tel, telz, mesme</em>
* `EMPTY` (4114): <em>grant, saint, belle, grans, bonne, saincte, bon, beau, grande, grand</em>

### `ADV`

23 <tt><a href="frm_profiterole-pos-ADV.html">ADV</a></tt> tokens (0% of all `ADV` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADV` and `PronType` co-occurred: <tt><a href="frm_profiterole-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (23; 100%).

`ADV` tokens may have the following values of `PronType`:

* `Int` (23; 100% of non-empty `PronType`): <em>comment, que, combien, Pourquoi</em>
* `EMPTY` (9414): <em>ne, si, y, en, bien, plus, n', moult, ainsi, pas</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="frm_profiterole-dep-conj.html">conj</a></tt>]--> PRON</tt> (29; 73%),
<tt>PRON --[<tt><a href="frm_profiterole-dep-appos.html">appos</a></tt>]--> PRON</tt> (3; 60%).

