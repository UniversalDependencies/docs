---
layout: base
title:  'Statistics of ExtPos in UD_Old_French-ALTM'
udver: '2'
---

## Treebank Statistics: UD_Old_French-ALTM: Features: `ExtPos`

This feature is language-specific.
It occurs with 4 different values: `ADP`, `ADV`, `PRON`, `SCONJ`.

120 tokens (1%) have a non-empty value of `ExtPos`.
31 types (2%) occur at least once with a non-empty value of `ExtPos`.
25 lemmas (3%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 4 part-of-speech tags: <tt><a href="fro_altm-pos-ADV.html">ADV</a></tt> (67; 0% instances), <tt><a href="fro_altm-pos-ADP.html">ADP</a></tt> (49; 0% instances), <tt><a href="fro_altm-pos-DET.html">DET</a></tt> (2; 0% instances), <tt><a href="fro_altm-pos-PRON.html">PRON</a></tt> (2; 0% instances).

### `ADV`

67 <tt><a href="fro_altm-pos-ADV.html">ADV</a></tt> tokens (7% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="fro_altm-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (67; 100%).

`ADV` tokens may have the following values of `ExtPos`:

* `ADP` (7; 10% of non-empty `ExtPos`): <em>hors, Non, ains, quant</em>
* `SCONJ` (60; 90% of non-empty `ExtPos`): <em>si, tant, anciés, puis, Anceis, Ausi, ainceis, ains, ainçois, combien</em>

<table>
  <tr><th>Paradigm <i>ains</i></th><th><tt>ADP</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>ains</em></td><td><em>ains</em></td></tr>
</table>

### `ADP`

49 <tt><a href="fro_altm-pos-ADP.html">ADP</a></tt> tokens (3% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (2; 4% of non-empty `ExtPos`): <em>juques, jusques</em>
* `ADV` (2; 4% of non-empty `ExtPos`): <em>de</em>
* `SCONJ` (45; 92% of non-empty `ExtPos`): <em>por, devant, par, puis, sans, depuis, fors, avant, des</em>

`ExtPos` seems to be **lexical feature** of `ADP`. 100% lemmas (11) occur only with one value of `ExtPos`.

### `DET`

2 <tt><a href="fro_altm-pos-DET.html">DET</a></tt> tokens (0% of all `DET` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `DET` and `ExtPos` co-occurred: <tt><a href="fro_altm-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (2; 100%), <tt><a href="fro_altm-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (2; 100%), <tt><a href="fro_altm-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (2; 100%).

`DET` tokens may have the following values of `ExtPos`:

* `PRON` (2; 100% of non-empty `ExtPos`): <em>la</em>

### `PRON`

2 <tt><a href="fro_altm-pos-PRON.html">PRON</a></tt> tokens (0% of all `PRON` tokens) have a non-empty value of `ExtPos`.

`PRON` tokens may have the following values of `ExtPos`:

* `ADV` (1; 50% of non-empty `ExtPos`): <em>c'</em>
* `SCONJ` (1; 50% of non-empty `ExtPos`): <em>tout</em>

