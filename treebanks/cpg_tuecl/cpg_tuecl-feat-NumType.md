---
layout: base
title:  'Statistics of NumType in UD_Cappadocian-TueCL'
udver: '2'
---

## Treebank Statistics: UD_Cappadocian-TueCL: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

15 tokens (0%) have a non-empty value of `NumType`.
11 types (1%) occur at least once with a non-empty value of `NumType`.
12 lemmas (2%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="cpg_tuecl-pos-NUM.html">NUM</a></tt> (13; 0% instances), <tt><a href="cpg_tuecl-pos-ADJ.html">ADJ</a></tt> (2; 0% instances).

### `NUM`

13 <tt><a href="cpg_tuecl-pos-NUM.html">NUM</a></tt> tokens (22% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="cpg_tuecl-feat-Number.html">Number</a></tt><tt>=Plur</tt> (10; 77%), <tt><a href="cpg_tuecl-feat-Case.html">Case</a></tt><tt>=Acc</tt> (8; 62%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (13; 100% of non-empty `NumType`): <em>τρία, δύο, έξι, α, ε, εννέ, πένdε, τέσσερα, ‘τζα</em>
* `EMPTY` (45): <em>μία, αν, τρία, α, δύο, δεύτερο, εν, οφτά, πρωτινά, πρωτινό</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (10) occur only with one value of `NumType`.

### `ADJ`

2 <tt><a href="cpg_tuecl-pos-ADJ.html">ADJ</a></tt> tokens (3% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="cpg_tuecl-feat-Case.html">Case</a></tt><tt>=Acc</tt> (2; 100%), <tt><a href="cpg_tuecl-feat-Number.html">Number</a></tt><tt>=Sing</tt> (2; 100%).

`ADJ` tokens may have the following values of `NumType`:

* `Ord` (2; 100% of non-empty `NumType`): <em>μπρο, πώτο</em>
* `EMPTY` (58): <em>άβου, καό, δομαίνο, ζόρι, ιράστα, λειψό, πίσι, παλό, πουά, τίπκε</em>

