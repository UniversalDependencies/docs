---
layout: base
title:  'Statistics of ExtPos in UD_Scottish_Gaelic-ARCOSG'
udver: '2'
---

## Treebank Statistics: UD_Scottish_Gaelic-ARCOSG: Features: `ExtPos`

This feature is language-specific.
It occurs with 11 different values: `ADJ`, `ADP`, `ADV`, `AUX`, `CCONJ`, `DET`, `INTJ`, `PART`, `PRON`, `PROPN`, `SCONJ`.

2608 tokens (3%) have a non-empty value of `ExtPos`.
70 types (1%) occur at least once with a non-empty value of `ExtPos`.
42 lemmas (1%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 11 part-of-speech tags: <tt><a href="gd_arcosg-pos-ADP.html">ADP</a></tt> (977; 1% instances), <tt><a href="gd_arcosg-pos-ADV.html">ADV</a></tt> (555; 1% instances), <tt><a href="gd_arcosg-pos-AUX.html">AUX</a></tt> (535; 1% instances), <tt><a href="gd_arcosg-pos-ADJ.html">ADJ</a></tt> (220; 0% instances), <tt><a href="gd_arcosg-pos-DET.html">DET</a></tt> (100; 0% instances), <tt><a href="gd_arcosg-pos-PRON.html">PRON</a></tt> (84; 0% instances), <tt><a href="gd_arcosg-pos-PART.html">PART</a></tt> (71; 0% instances), <tt><a href="gd_arcosg-pos-SCONJ.html">SCONJ</a></tt> (60; 0% instances), <tt><a href="gd_arcosg-pos-CCONJ.html">CCONJ</a></tt> (4; 0% instances), <tt><a href="gd_arcosg-pos-INTJ.html">INTJ</a></tt> (1; 0% instances), <tt><a href="gd_arcosg-pos-VERB.html">VERB</a></tt> (1; 0% instances).

### `ADP`

977 <tt><a href="gd_arcosg-pos-ADP.html">ADP</a></tt> tokens (8% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (971; 99% of non-empty `ExtPos`): <em>ann, anns, air, an, a, as, bho, mu, am, o</em>
* `ADV` (5; 1% of non-empty `ExtPos`): <em>bho, a, an, mu</em>
* `CCONJ` (1; 0% of non-empty `ExtPos`): <em>air</em>

<table>
  <tr><th>Paradigm <i>an</i></th><th><tt>ADP</tt></th><th><tt>ADV</tt></th></tr>
  <tr><td><tt></tt></td><td><em>ann, anns, an, am, 'n</em></td><td><em>an</em></td></tr>
</table>

### `ADV`

555 <tt><a href="gd_arcosg-pos-ADV.html">ADV</a></tt> tokens (12% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="gd_arcosg-feat-AdvType.html">AdvType</a></tt><tt>=Loc</tt> (314; 57%).

`ADV` tokens may have the following values of `ExtPos`:

* `ADV` (555; 100% of non-empty `ExtPos`): <em>an, ann, a, air, mu, 'n, am, ma, dè, mar</em>

`ExtPos` seems to be **lexical feature** of `ADV`. 100% lemmas (15) occur only with one value of `ExtPos`.

### `AUX`

535 <tt><a href="gd_arcosg-pos-AUX.html">AUX</a></tt> tokens (39% of all `AUX` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `AUX` and `ExtPos` co-occurred: <tt><a href="gd_arcosg-feat-VerbForm.html">VerbForm</a></tt><tt>=EMPTY</tt> (535; 100%), <tt><a href="gd_arcosg-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (508; 95%), <tt><a href="gd_arcosg-feat-Tense.html">Tense</a></tt><tt>=Pres</tt> (463; 87%), <tt><a href="gd_arcosg-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (365; 68%).

`AUX` tokens may have the following values of `ExtPos`:

* `AUX` (535; 100% of non-empty `ExtPos`): <em>is, 's, gur, b', chan, an, nach, b’, gun, ‘s</em>

### `ADJ`

220 <tt><a href="gd_arcosg-pos-ADJ.html">ADJ</a></tt> tokens (6% of all `ADJ` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADJ` and `ExtPos` co-occurred: <tt><a href="gd_arcosg-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (214; 97%), <tt><a href="gd_arcosg-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (214; 97%), <tt><a href="gd_arcosg-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (214; 97%).

`ADJ` tokens may have the following values of `ExtPos`:

* `ADJ` (220; 100% of non-empty `ExtPos`): <em>sam, a, an, mu, air, fo, ma, Iar, am, fa</em>

`ExtPos` seems to be **lexical feature** of `ADJ`. 100% lemmas (10) occur only with one value of `ExtPos`.

### `DET`

100 <tt><a href="gd_arcosg-pos-DET.html">DET</a></tt> tokens (2% of all `DET` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `DET` and `ExtPos` co-occurred: <tt><a href="gd_arcosg-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (100; 100%), <tt><a href="gd_arcosg-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (100; 100%), <tt><a href="gd_arcosg-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (100; 100%), <tt><a href="gd_arcosg-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (97; 97%), <tt><a href="gd_arcosg-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (97; 97%), <tt><a href="gd_arcosg-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (97; 97%), <tt><a href="gd_arcosg-feat-PronType.html">PronType</a></tt><tt>=Art</tt> (97; 97%).

`DET` tokens may have the following values of `ExtPos`:

* `DET` (100; 100% of non-empty `ExtPos`): <em>a, mo, an</em>

### `PRON`

84 <tt><a href="gd_arcosg-pos-PRON.html">PRON</a></tt> tokens (1% of all `PRON` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PRON` and `ExtPos` co-occurred: <tt><a href="gd_arcosg-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (84; 100%), <tt><a href="gd_arcosg-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (81; 96%), <tt><a href="gd_arcosg-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (81; 96%).

`PRON` tokens may have the following values of `ExtPos`:

* `ADV` (8; 10% of non-empty `ExtPos`): <em>dè, có</em>
* `PRON` (76; 90% of non-empty `ExtPos`): <em>a, an, gu, car, cia, cò, Ge, ann, dè, dé</em>

<table>
  <tr><th>Paradigm <i>dè</i></th><th><tt>ADV</tt></th><th><tt>PRON</tt></th></tr>
  <tr><td><tt></tt></td><td><em>dè</em></td><td><em>dè</em></td></tr>
</table>

`ExtPos` seems to be **lexical feature** of `PRON`. 92% lemmas (12) occur only with one value of `ExtPos`.

### `PART`

71 <tt><a href="gd_arcosg-pos-PART.html">PART</a></tt> tokens (1% of all `PART` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PART` and `ExtPos` co-occurred: <tt><a href="gd_arcosg-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (71; 100%), <tt><a href="gd_arcosg-feat-PartType.html">PartType</a></tt><tt>=Comp</tt> (70; 99%), <tt><a href="gd_arcosg-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (70; 99%).

`PART` tokens may have the following values of `ExtPos`:

* `PART` (71; 100% of non-empty `ExtPos`): <em>na, a</em>

### `SCONJ`

60 <tt><a href="gd_arcosg-pos-SCONJ.html">SCONJ</a></tt> tokens (6% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

`SCONJ` tokens may have the following values of `ExtPos`:

* `ADV` (1; 2% of non-empty `ExtPos`): <em>mar</em>
* `SCONJ` (59; 98% of non-empty `ExtPos`): <em>an, fhad, a, ge, fad, aon, ri, 'n, Chionn, air</em>

`ExtPos` seems to be **lexical feature** of `SCONJ`. 100% lemmas (15) occur only with one value of `ExtPos`.

### `CCONJ`

4 <tt><a href="gd_arcosg-pos-CCONJ.html">CCONJ</a></tt> tokens (0% of all `CCONJ` tokens) have a non-empty value of `ExtPos`.

`CCONJ` tokens may have the following values of `ExtPos`:

* `CCONJ` (4; 100% of non-empty `ExtPos`): <em>a, air</em>

### `INTJ`

1 <tt><a href="gd_arcosg-pos-INTJ.html">INTJ</a></tt> tokens (0% of all `INTJ` tokens) have a non-empty value of `ExtPos`.

`INTJ` tokens may have the following values of `ExtPos`:

* `INTJ` (1; 100% of non-empty `ExtPos`): <em>'N</em>

### `VERB`

1 <tt><a href="gd_arcosg-pos-VERB.html">VERB</a></tt> tokens (0% of all `VERB` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `VERB` and `ExtPos` co-occurred: <tt><a href="gd_arcosg-feat-Mood.html">Mood</a></tt><tt>=Ind</tt> (1; 100%), <tt><a href="gd_arcosg-feat-Tense.html">Tense</a></tt><tt>=Fut</tt> (1; 100%), <tt><a href="gd_arcosg-feat-VerbForm.html">VerbForm</a></tt><tt>=Fin</tt> (1; 100%).

`VERB` tokens may have the following values of `ExtPos`:

* `PROPN` (1; 100% of non-empty `ExtPos`): <em>Théid</em>

