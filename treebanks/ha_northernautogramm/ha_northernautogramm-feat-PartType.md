---
layout: base
title:  'Statistics of PartType in UD_Hausa-NorthernAutogramm'
udver: '2'
---

## Treebank Statistics: UD_Hausa-NorthernAutogramm: Features: `PartType`

This feature is language-specific.
It occurs with 9 different values: `Aspect`, `Case`, `Der`, `Disc`, `Evident`, `Foc`, `Neg`, `Pred`, `Top`.

566 tokens (4%) have a non-empty value of `PartType`.
31 types (2%) occur at least once with a non-empty value of `PartType`.
26 lemmas (2%) occur at least once with a non-empty value of `PartType`.
The feature is used with 1 part-of-speech tags: <tt><a href="ha_northernautogramm-pos-PART.html">PART</a></tt> (566; 4% instances).

### `PART`

566 <tt><a href="ha_northernautogramm-pos-PART.html">PART</a></tt> tokens (84% of all `PART` tokens) have a non-empty value of `PartType`.

The most frequent other feature values with which `PART` and `PartType` co-occurred: <tt><a href="ha_northernautogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (481; 85%), <tt><a href="ha_northernautogramm-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (466; 82%).

`PART` tokens may have the following values of `PartType`:

* `Aspect` (53; 9% of non-empty `PartType`): <em>ta, kan, zan</em>
* `Case` (41; 7% of non-empty `PartType`): <em>na, ta</em>
* `Der` (92; 16% of non-empty `PartType`): <em>mài, màːsu</em>
* `Disc` (17; 3% of non-empty `PartType`): <em>kuma, shîn</em>
* `Evident` (11; 2% of non-empty `PartType`): <em>wai</em>
* `Foc` (64; 11% of non-empty `PartType`): <em>nàː, naː, tàː, kèːnan, taː</em>
* `Neg` (61; 11% of non-empty `PartType`): <em>bàː, ba, dàk, dàsh</em>
* `Pred` (118; 21% of non-empty `PartType`): <em>gàː, àkwai, baːbù, zâː, bâː, dà, à~</em>
* `Top` (109; 19% of non-empty `PartType`): <em>dai, kòː, kuma, hwa, maː, koː, kâm</em>

<table>
  <tr><th>Paradigm <i>kuma</i></th><th><tt>Disc</tt></th><th><tt>Top</tt></th></tr>
  <tr><td><tt></tt></td><td><em>kuma</em></td><td><em>kuma</em></td></tr>
</table>

`PartType` seems to be **lexical feature** of `PART`. 96% lemmas (25) occur only with one value of `PartType`.

## Relations with Agreement in `PartType`

The 10 most frequent relations where parent and child node agree in `PartType`:
<tt>PART --[<tt><a href="ha_northernautogramm-dep-parataxis.html">parataxis</a></tt>]--> PART</tt> (6; 100%),
<tt>PART --[<tt><a href="ha_northernautogramm-dep-conj.html">conj</a></tt>]--> PART</tt> (2; 100%),
<tt>PART --[<tt><a href="ha_northernautogramm-dep-reparandum.html">reparandum</a></tt>]--> PART</tt> (2; 100%),
<tt>PART --[<tt><a href="ha_northernautogramm-dep-advmod.html">advmod</a></tt>]--> PART</tt> (1; 100%),
<tt>PART --[<tt><a href="ha_northernautogramm-dep-appos.html">appos</a></tt>]--> PART</tt> (1; 100%).

