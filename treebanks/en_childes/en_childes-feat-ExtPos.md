---
layout: base
title:  'Statistics of ExtPos in UD_English-CHILDES'
udver: '2'
---

## Treebank Statistics: UD_English-CHILDES: Features: `ExtPos`

This feature is language-specific.
It occurs with 4 different values: `ADP`, `ADV`, `PRON`, `SCONJ`.

209 tokens (0%) have a non-empty value of `ExtPos`.
31 types (1%) occur at least once with a non-empty value of `ExtPos`.
22 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 6 part-of-speech tags: <tt><a href="en_childes-pos-ADV.html">ADV</a></tt> (88; 0% instances), <tt><a href="en_childes-pos-ADP.html">ADP</a></tt> (65; 0% instances), <tt><a href="en_childes-pos-NOUN.html">NOUN</a></tt> (40; 0% instances), <tt><a href="en_childes-pos-SCONJ.html">SCONJ</a></tt> (9; 0% instances), <tt><a href="en_childes-pos-PRON.html">PRON</a></tt> (4; 0% instances), <tt><a href="en_childes-pos-ADJ.html">ADJ</a></tt> (3; 0% instances).

### `ADV`

88 <tt><a href="en_childes-pos-ADV.html">ADV</a></tt> tokens (1% of all `ADV` tokens) have a non-empty value of `ExtPos`.

`ADV` tokens may have the following values of `ExtPos`:

* `ADP` (7; 8% of non-empty `ExtPos`): <em>instead, what</em>
* `ADV` (80; 91% of non-empty `ExtPos`): <em>how, as, each, kind, at, instead, of, upside, 'd, Less</em>
* `SCONJ` (1; 1% of non-empty `ExtPos`): <em>So</em>

<table>
  <tr><th>Paradigm <i>so</i></th><th><tt>ADV</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>So</em></td><td><em>So</em></td></tr>
</table>

### `ADP`

65 <tt><a href="en_childes-pos-ADP.html">ADP</a></tt> tokens (0% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (47; 72% of non-empty `ExtPos`): <em>out, because, of, up</em>
* `ADV` (17; 26% of non-empty `ExtPos`): <em>of, at, As, because, in, out</em>
* `SCONJ` (1; 2% of non-empty `ExtPos`): <em>in</em>

<table>
  <tr><th>Paradigm <i>in</i></th><th><tt>ADV</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>in</em></td><td><em>in</em></td></tr>
</table>

### `NOUN`

40 <tt><a href="en_childes-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `ExtPos`.

`NOUN` tokens may have the following values of `ExtPos`:

* `ADV` (40; 100% of non-empty `ExtPos`): <em>upside, kind, sort</em>

### `SCONJ`

9 <tt><a href="en_childes-pos-SCONJ.html">SCONJ</a></tt> tokens (0% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

`SCONJ` tokens may have the following values of `ExtPos`:

* `ADP` (1; 11% of non-empty `ExtPos`): <em>Because</em>
* `SCONJ` (8; 89% of non-empty `ExtPos`): <em>so, as</em>

### `PRON`

4 <tt><a href="en_childes-pos-PRON.html">PRON</a></tt> tokens (0% of all `PRON` tokens) have a non-empty value of `ExtPos`.

`PRON` tokens may have the following values of `ExtPos`:

* `ADV` (1; 25% of non-empty `ExtPos`): <em>what</em>
* `PRON` (3; 75% of non-empty `ExtPos`): <em>what</em>

<table>
  <tr><th>Paradigm <i>what</i></th><th><tt>ADV</tt></th><th><tt>PRON</tt></th></tr>
  <tr><td><tt></tt></td><td><em>what</em></td><td><em>what</em></td></tr>
</table>

### `ADJ`

3 <tt><a href="en_childes-pos-ADJ.html">ADJ</a></tt> tokens (0% of all `ADJ` tokens) have a non-empty value of `ExtPos`.

`ADJ` tokens may have the following values of `ExtPos`:

* `ADV` (3; 100% of non-empty `ExtPos`): <em>more</em>

## Relations with Agreement in `ExtPos`

The 10 most frequent relations where parent and child node agree in `ExtPos`:
<tt>NOUN --[<tt><a href="en_childes-dep-conj.html">conj</a></tt>]--> NOUN</tt> (2; 100%),
<tt>NOUN --[<tt><a href="en_childes-dep-reparandum.html">reparandum</a></tt>]--> NOUN</tt> (2; 100%).

