---
layout: base
title:  'Statistics of AdvType in UD_Latin-ITTB'
udver: '2'
---

## Treebank Statistics: UD_Latin-ITTB: Features: `AdvType`

This feature is language-specific.
It occurs with 2 different values: `Loc`, `Tim`.

3921 tokens (1%) have a non-empty value of `AdvType`.
12 types (0%) occur at least once with a non-empty value of `AdvType`.
12 lemmas (0%) occur at least once with a non-empty value of `AdvType`.
The feature is used with 1 part-of-speech tags: <tt><a href="la_ittb-pos-ADV.html">ADV</a></tt> (3921; 1% instances).

### `ADV`

3921 <tt><a href="la_ittb-pos-ADV.html">ADV</a></tt> tokens (15% of all `ADV` tokens) have a non-empty value of `AdvType`.

The most frequent other feature values with which `ADV` and `AdvType` co-occurred: <tt><a href="la_ittb-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (2278; 58%).

`ADV` tokens may have the following values of `AdvType`:

* `Loc` (2437; 62% of non-empty `AdvType`): <em>ergo, unde, hinc, hic</em>
* `Tim` (1484; 38% of non-empty `AdvType`): <em>adhuc, semper, iam, nunc, iterum, quandoque, denique, quando</em>

`AdvType` seems to be **lexical feature** of `ADV`. 100% lemmas (12) occur only with one value of `AdvType`.

