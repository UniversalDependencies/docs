---
layout: base
title:  'Statistics of PronType in UD_Northwest_Gbaya-Autogramm'
udver: '2'
---

## Treebank Statistics: UD_Northwest_Gbaya-Autogramm: Features: `PronType`

This feature is universal.
It occurs with 5 different values: `Ind`, `Int`, `Prs`, `Rcp`, `Rel`.

418 tokens (16%) have a non-empty value of `PronType`.
37 types (7%) occur at least once with a non-empty value of `PronType`.
15 lemmas (4%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="gya_autogramm-pos-PRON.html">PRON</a></tt> (398; 15% instances), <tt><a href="gya_autogramm-pos-SCONJ.html">SCONJ</a></tt> (18; 1% instances), <tt><a href="gya_autogramm-pos-ADV.html">ADV</a></tt> (2; 0% instances).

### `PRON`

398 <tt><a href="gya_autogramm-pos-PRON.html">PRON</a></tt> tokens (87% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="gya_autogramm-feat-Number.html">Number</a></tt><tt>=Sing</tt> (281; 71%).

`PRON` tokens may have the following values of `PronType`:

* `Ind` (9; 2% of non-empty `PronType`): <em>ʔéí, =éí, ndèì, ʔèì</em>
* `Prs` (382; 96% of non-empty `PronType`): <em>=à̰, =ám, ʔà̰, wà, =wà, =éí, =m, mɛ́, =mɛ́, mɛ̀</em>
* `Rcp` (7; 2% of non-empty `PronType`): <em>màá</em>
* `EMPTY` (60): <em>=à, =à̰, ʔɛ̰̀, kɔ́, mà, mɛ́, wà, wàn, ʔɛ̀nɛ̀, =nɛ́-ìí</em>

<table>
  <tr><th>Paradigm <i>ʔéí</i></th><th><tt>Prs</tt></th><th><tt>Ind</tt></th></tr>
  <tr><td><tt>_</tt></td><td><em>=éí</em></td><td><em>ʔéí, =éí, ndèì, ʔèì</em></td></tr>
  <tr><td><tt><tt><a href="gya_autogramm-feat-Number.html">Number</a></tt><tt>=Sing</tt></tt></td><td><em>=éí, ʔéí, ʔèì</em></td><td></td></tr>
</table>

`PronType` seems to be **lexical feature** of `PRON`. 92% lemmas (11) occur only with one value of `PronType`.

### `SCONJ`

18 <tt><a href="gya_autogramm-pos-SCONJ.html">SCONJ</a></tt> tokens (37% of all `SCONJ` tokens) have a non-empty value of `PronType`.

`SCONJ` tokens may have the following values of `PronType`:

* `Rel` (18; 100% of non-empty `PronType`): <em>nɛ̀</em>
* `EMPTY` (31): <em>kà, há̰, hé, kóó, mɔ̀-nɛ̀, gá, gá,, hám, mɔ̀, nɛ̀</em>

### `ADV`

2 <tt><a href="gya_autogramm-pos-ADV.html">ADV</a></tt> tokens (1% of all `ADV` tokens) have a non-empty value of `PronType`.

`ADV` tokens may have the following values of `PronType`:

* `Int` (2; 100% of non-empty `PronType`): <em>díì, díí-gè-ndé</em>
* `EMPTY` (167): <em>ʔèá, hɛ̰̀, ʔé, ʔòé, mɛ̀, hḭ̀ḭ́, bèè, sí-ɗòŋ, ɗòŋ, ʔèááá</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="gya_autogramm-dep-conj-redup.html">conj:redup</a></tt>]--> PRON</tt> (4; 100%),
<tt>PRON --[<tt><a href="gya_autogramm-dep-reparandum.html">reparandum</a></tt>]--> PRON</tt> (2; 100%).

