---
layout: base
title:  'Statistics of Voice in UD_Hausa-NorthernAutogramm'
udver: '2'
---

## Treebank Statistics: UD_Hausa-NorthernAutogramm: Features: `Voice`

This feature is universal but the values `Stat` are language-specific.
It occurs with 2 different values: `Cau`, `Stat`.

30 tokens (1%) have a non-empty value of `Voice`.
15 types (2%) occur at least once with a non-empty value of `Voice`.
13 lemmas (2%) occur at least once with a non-empty value of `Voice`.
The feature is used with 1 part-of-speech tags: <tt><a href="ha_northernautogramm-pos-VERB.html">VERB</a></tt> (30; 1% instances).

### `VERB`

30 <tt><a href="ha_northernautogramm-pos-VERB.html">VERB</a></tt> tokens (4% of all `VERB` tokens) have a non-empty value of `Voice`.

The most frequent other feature values with which `VERB` and `Voice` co-occurred: <tt><a href="ha_northernautogramm-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (29; 97%), <tt><a href="ha_northernautogramm-feat-VerbForm.html">VerbForm</a></tt><tt>=Part</tt> (21; 70%), <tt><a href="ha_northernautogramm-feat-ExtPos.html">ExtPos</a></tt><tt>=ADV</tt> (20; 67%).

`VERB` tokens may have the following values of `Voice`:

* `Cau` (9; 30% of non-empty `Voice`): <em>tassheː, bâsshee, hîrkassheː, s'aisà, tassà, tâːkassà, jèːwàyèsshi, tàssa</em>
* `Stat` (21; 70% of non-empty `Voice`): <em>bìye, tàhe, kwànce, màːlìye, tsàye, tàushe, zàmne</em>
* `EMPTY` (697): <em>cèː, yi, cêː, zakà, zoː, ga, tàhi, tàhiyàː, yîː, ji</em>

`Voice` seems to be **lexical feature** of `VERB`. 100% lemmas (13) occur only with one value of `Voice`.

## Relations with Agreement in `Voice`

The 10 most frequent relations where parent and child node agree in `Voice`:
<tt>VERB --[<tt><a href="ha_northernautogramm-dep-conj.html">conj</a></tt>]--> VERB</tt> (5; 63%),
<tt>VERB --[<tt><a href="ha_northernautogramm-dep-parataxis.html">parataxis</a></tt>]--> VERB</tt> (2; 67%).

