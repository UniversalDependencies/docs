---
layout: base
title:  'Statistics of PronType in UD_Nepali-BK'
udver: '2'
---

## Treebank Statistics: UD_Nepali-BK: Features: `PronType`

This feature is universal.
It occurs with 4 different values: `Dem`, `Int`, `Prs`, `Rel`.

63 tokens (8%) have a non-empty value of `PronType`.
27 types (8%) occur at least once with a non-empty value of `PronType`.
23 lemmas (9%) occur at least once with a non-empty value of `PronType`.
The feature is used with 2 part-of-speech tags: <tt><a href="ne_bk-pos-DET.html">DET</a></tt> (33; 4% instances), <tt><a href="ne_bk-pos-PRON.html">PRON</a></tt> (30; 4% instances).

### `DET`

33 <tt><a href="ne_bk-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="ne_bk-feat-Number.html">Number</a></tt><tt>=Sing</tt> (25; 76%).

`DET` tokens may have the following values of `PronType`:

* `Dem` (30; 91% of non-empty `PronType`): <em>त्यो, एउटा, त्यही, यस, यो, त्यस, यत्रो</em>
* `Int` (2; 6% of non-empty `PronType`): <em>कति</em>
* `Prs` (1; 3% of non-empty `PronType`): <em>आआफ्नो</em>

### `PRON`

30 <tt><a href="ne_bk-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="ne_bk-feat-Number.html">Number</a></tt><tt>=Sing</tt> (19; 63%), <tt><a href="ne_bk-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (18; 60%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (13; 43% of non-empty `PronType`): <em>त्यो, एउटा, त्यसलाई, यस, यसबाट, यस्ता, यी, यस्तो, यही</em>
* `Int` (2; 7% of non-empty `PronType`): <em>के</em>
* `Prs` (14; 47% of non-empty `PronType`): <em>उ, मलाई, आफु, आफ्ना, उलाइ, उले, उसको, कसैलाई, तँ, हाम्रो</em>
* `Rel` (1; 3% of non-empty `PronType`): <em>जुन</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (18) occur only with one value of `PronType`.

