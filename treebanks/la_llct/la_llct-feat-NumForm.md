---
layout: base
title:  'Statistics of NumForm in UD_Latin-LLCT'
udver: '2'
---

## Treebank Statistics: UD_Latin-LLCT: Features: `NumForm`

This feature is language-specific.
It occurs with 2 different values: `Roman`, `Word`.

1501 tokens (1%) have a non-empty value of `NumForm`.
82 types (1%) occur at least once with a non-empty value of `NumForm`.
31 lemmas (1%) occur at least once with a non-empty value of `NumForm`.
The feature is used with 1 part-of-speech tags: <tt><a href="la_llct-pos-NUM.html">NUM</a></tt> (1501; 1% instances).

### `NUM`

1501 <tt><a href="la_llct-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumForm`.

The most frequent other feature values with which `NUM` and `NumForm` co-occurred: <tt><a href="la_llct-feat-NumType.html">NumType</a></tt><tt>=Card</tt> (1501; 100%), <tt><a href="la_llct-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (872; 58%), <tt><a href="la_llct-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (872; 58%), <tt><a href="la_llct-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (872; 58%).

`NUM` tokens may have the following values of `NumForm`:

* `Roman` (2; 0% of non-empty `NumForm`): <em>XIIII, XX</em>
* `Word` (1499; 100% of non-empty `NumForm`): <em>duas, duo, viginti, triginta, tres, quinquaginta, decem, sex, quattuor, quinque</em>

`NumForm` seems to be **lexical feature** of `NUM`. 100% lemmas (31) occur only with one value of `NumForm`.

## Relations with Agreement in `NumForm`

The 10 most frequent relations where parent and child node agree in `NumForm`:
<tt>NUM --[<tt><a href="la_llct-dep-conj.html">conj</a></tt>]--> NUM</tt> (71; 100%),
<tt>NUM --[<tt><a href="la_llct-dep-flat.html">flat</a></tt>]--> NUM</tt> (59; 100%).

