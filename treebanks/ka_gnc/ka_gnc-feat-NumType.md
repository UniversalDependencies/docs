---
layout: base
title:  'Statistics of NumType in UD_Georgian-GNC'
udver: '2'
---

## Treebank Statistics: UD_Georgian-GNC: Features: `NumType`

This feature is universal.
It occurs with 3 different values: `Card`, `Frac`, `Ord`.

182 tokens (1%) have a non-empty value of `NumType`.
62 types (1%) occur at least once with a non-empty value of `NumType`.
42 lemmas (1%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="ka_gnc-pos-NUM.html">NUM</a></tt> (182; 1% instances).

### `NUM`

182 <tt><a href="ka_gnc-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (153; 84% of non-empty `NumType`): <em>ერთი, ერთ, ორი, სამი, ოცი, ორივე, ათასი, ოთხი, ორ, რვა</em>
* `Frac` (1; 1% of non-empty `NumType`): <em>მეოთხედ</em>
* `Ord` (28; 15% of non-empty `NumType`): <em>მეორე, პირველი, მე-15, მეორეს, მეოთხი, მეორეც, მეოცე, მერვე, მესამე, მესამეს</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (42) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="ka_gnc-dep-obl.html">obl</a></tt>]--> NUM</tt> (2; 100%),
<tt>NUM --[<tt><a href="ka_gnc-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (1; 100%).

