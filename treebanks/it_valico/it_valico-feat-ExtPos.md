---
layout: base
title:  'Statistics of ExtPos in UD_Italian-Valico'
udver: '2'
---

## Treebank Statistics: UD_Italian-Valico: Features: `ExtPos`

This feature is language-specific.
It occurs with 4 different values: `ADP`, `ADV`, `INTJ`, `SCONJ`.

22 tokens (0%) have a non-empty value of `ExtPos`.
17 types (1%) occur at least once with a non-empty value of `ExtPos`.
17 lemmas (2%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 4 part-of-speech tags: <tt><a href="it_valico-pos-ADP.html">ADP</a></tt> (8; 0% instances), <tt><a href="it_valico-pos-SCONJ.html">SCONJ</a></tt> (8; 0% instances), <tt><a href="it_valico-pos-ADV.html">ADV</a></tt> (5; 0% instances), <tt><a href="it_valico-pos-INTJ.html">INTJ</a></tt> (1; 0% instances).

### `ADP`

8 <tt><a href="it_valico-pos-ADP.html">ADP</a></tt> tokens (1% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (8; 100% of non-empty `ExtPos`): <em>a, cerca, davanti, dietro, per, sopra, vecino, vicino</em>

### `SCONJ`

8 <tt><a href="it_valico-pos-SCONJ.html">SCONJ</a></tt> tokens (5% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

`SCONJ` tokens may have the following values of `ExtPos`:

* `ADP` (2; 25% of non-empty `ExtPos`): <em>a</em>
* `ADV` (2; 25% of non-empty `ExtPos`): <em>Non, così</em>
* `SCONJ` (4; 50% of non-empty `ExtPos`): <em>come, Anche, tale</em>

### `ADV`

5 <tt><a href="it_valico-pos-ADV.html">ADV</a></tt> tokens (1% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="it_valico-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (5; 100%).

`ADV` tokens may have the following values of `ExtPos`:

* `ADV` (5; 100% of non-empty `ExtPos`): <em>prima, a, fino, invece</em>

### `INTJ`

1 <tt><a href="it_valico-pos-INTJ.html">INTJ</a></tt> tokens (10% of all `INTJ` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `INTJ` and `ExtPos` co-occurred: <tt><a href="it_valico-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (1; 100%).

`INTJ` tokens may have the following values of `ExtPos`:

* `INTJ` (1; 100% of non-empty `ExtPos`): <em>mama</em>

