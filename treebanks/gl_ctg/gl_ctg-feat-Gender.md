---
layout: base
title:  'Statistics of Gender in UD_Galician-CTG'
udver: '2'
---

## Treebank Statistics: UD_Galician-CTG: Features: `Gender`

This feature is universal.
It occurs with 1 different values: `Fem`.

285 tokens (0%) have a non-empty value of `Gender`.
3 types (0%) occur at least once with a non-empty value of `Gender`.
2 lemmas (0%) occur at least once with a non-empty value of `Gender`.
The feature is used with 1 part-of-speech tags: <tt><a href="gl_ctg-pos-DET.html">DET</a></tt> (285; 0% instances).

### `DET`

285 <tt><a href="gl_ctg-pos-DET.html">DET</a></tt> tokens (1% of all `DET` tokens) have a non-empty value of `Gender`.

The most frequent other feature values with which `DET` and `Gender` co-occurred: <tt><a href="gl_ctg-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (285; 100%), <tt><a href="gl_ctg-feat-PronType.html">PronType</a></tt><tt>=Art</tt> (285; 100%).

`DET` tokens may have the following values of `Gender`:

* `Fem` (285; 100% of non-empty `Gender`): <em>as, a</em>
* `EMPTY` (23900): <em>o, a, os, as, un, unha, este, súa, esta, seu</em>

