---
layout: base
title:  'Statistics of PronType in UD_Uzbek-TueCL'
udver: '2'
---

## Treebank Statistics: UD_Uzbek-TueCL: Features: `PronType`

This feature is universal.
It occurs with 5 different values: `Dem`, `Int`, `Neg`, `Prs`, `Tot`.

54 tokens (6%) have a non-empty value of `PronType`.
35 types (8%) occur at least once with a non-empty value of `PronType`.
18 lemmas (7%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="uz_tuecl-pos-PRON.html">PRON</a></tt> (43; 5% instances), <tt><a href="uz_tuecl-pos-DET.html">DET</a></tt> (9; 1% instances), <tt><a href="uz_tuecl-pos-ADV.html">ADV</a></tt> (2; 0% instances).

### `PRON`

43 <tt><a href="uz_tuecl-pos-PRON.html">PRON</a></tt> tokens (84% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="uz_tuecl-feat-Number.html">Number</a></tt><tt>=Sing</tt> (33; 77%), <tt><a href="uz_tuecl-feat-Case.html">Case</a></tt><tt>=Nom</tt> (26; 60%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (8; 19% of non-empty `PronType`): <em>Buning, bu, shuki, shunday, shundan, shuni</em>
* `Int` (6; 14% of non-empty `PronType`): <em>kim, nimaga, qanday, qaysisi</em>
* `Neg` (1; 2% of non-empty `PronType`): <em>Hechqisi</em>
* `Prs` (28; 65% of non-empty `PronType`): <em>u, Sen, biz, Mening, Ular, oʻzi, Senga, Siz, Uning, men</em>
* `EMPTY` (8): <em>menga, nimani, bir-birlarini, biz, qayerda</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (14) occur only with one value of `PronType`.

### `DET`

9 <tt><a href="uz_tuecl-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

`DET` tokens may have the following values of `PronType`:

* `Dem` (6; 67% of non-empty `PronType`): <em>bu, oʻsha, shunday</em>
* `Neg` (2; 22% of non-empty `PronType`): <em>hech</em>
* `Tot` (1; 11% of non-empty `PronType`): <em>barcha</em>

### `ADV`

2 <tt><a href="uz_tuecl-pos-ADV.html">ADV</a></tt> tokens (5% of all `ADV` tokens) have a non-empty value of `PronType`.

`ADV` tokens may have the following values of `PronType`:

* `Int` (2; 100% of non-empty `PronType`): <em>qachon</em>
* `EMPTY` (37): <em>ertalab, ham, juda, kech, Menimcha, Umuman, faqat, hali, negadir, avval</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="uz_tuecl-dep-compound.html">compound</a></tt>]--> PRON</tt> (1; 100%).

