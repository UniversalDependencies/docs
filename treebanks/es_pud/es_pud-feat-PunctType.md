---
layout: base
title:  'Statistics of PunctType in UD_Spanish-PUD'
udver: '2'
---

## Treebank Statistics: UD_Spanish-PUD: Features: `PunctType`

This feature is language-specific.
It occurs with 9 different values: `Brck`, `Colo`, `Comm`, `Dash`, `Elip`, `Peri`, `Qest`, `Quot`, `Semi`.

2289 tokens (10%) have a non-empty value of `PunctType`.
16 types (0%) occur at least once with a non-empty value of `PunctType`.
16 lemmas (0%) occur at least once with a non-empty value of `PunctType`.
The feature is used with 1 part-of-speech tags: <tt><a href="es_pud-pos-PUNCT.html">PUNCT</a></tt> (2289; 10% instances).

### `PUNCT`

2289 <tt><a href="es_pud-pos-PUNCT.html">PUNCT</a></tt> tokens (100% of all `PUNCT` tokens) have a non-empty value of `PunctType`.

`PUNCT` tokens may have the following values of `PunctType`:

* `Brck` (82; 4% of non-empty `PunctType`): <em>(, )</em>
* `Colo` (27; 1% of non-empty `PunctType`): <em>:, /</em>
* `Comm` (998; 44% of non-empty `PunctType`): <em>,</em>
* `Dash` (18; 1% of non-empty `PunctType`): <em>-, —</em>
* `Elip` (2; 0% of non-empty `PunctType`): <em>…</em>
* `Peri` (983; 43% of non-empty `PunctType`): <em>.</em>
* `Qest` (28; 1% of non-empty `PunctType`): <em>?, ¿</em>
* `Quot` (131; 6% of non-empty `PunctType`): <em>", ', «, »</em>
* `Semi` (20; 1% of non-empty `PunctType`): <em>;</em>

`PunctType` seems to be **lexical feature** of `PUNCT`. 100% lemmas (16) occur only with one value of `PunctType`.

