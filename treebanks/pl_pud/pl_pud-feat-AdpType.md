---
layout: base
title:  'Statistics of AdpType in UD_Polish-PUD'
udver: '2'
---

## Treebank Statistics: UD_Polish-PUD: Features: `AdpType`

This feature is language-specific.
It occurs with 2 different values: `Post`, `Prep`.

2050 tokens (11%) have a non-empty value of `AdpType`.
75 types (1%) occur at least once with a non-empty value of `AdpType`.
47 lemmas (1%) occur at least once with a non-empty value of `AdpType`.
The feature is used with 1 part-of-speech tags: <tt><a href="pl_pud-pos-ADP.html">ADP</a></tt> (2050; 11% instances).

### `ADP`

2050 <tt><a href="pl_pud-pos-ADP.html">ADP</a></tt> tokens (100% of all `ADP` tokens) have a non-empty value of `AdpType`.

The most frequent other feature values with which `ADP` and `AdpType` co-occurred: <tt><a href="pl_pud-feat-Variant.html">Variant</a></tt><tt>=Short</tt> (1146; 56%).

`ADP` tokens may have the following values of `AdpType`:

* `Post` (3; 0% of non-empty `AdpType`): <em>temu</em>
* `Prep` (2047; 100% of non-empty `AdpType`): <em>w, na, do, z, o, po, dla, za, przez, od</em>

`AdpType` seems to be **lexical feature** of `ADP`. 100% lemmas (47) occur only with one value of `AdpType`.

## Relations with Agreement in `AdpType`

The 10 most frequent relations where parent and child node agree in `AdpType`:
<tt>ADP --[<tt><a href="pl_pud-dep-fixed.html">fixed</a></tt>]--> ADP</tt> (11; 100%).

