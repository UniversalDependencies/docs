---
layout: base
title:  'Statistics of NumType in UD_Armenian-ArmTDP'
udver: '2'
---

## Treebank Statistics: UD_Armenian-ArmTDP: Features: `NumType`

This feature is universal.
It occurs with 4 different values: `Card`, `Frac`, `Ord`, `Range`.

360 tokens (2%) have a non-empty value of `NumType`.
167 types (2%) occur at least once with a non-empty value of `NumType`.
159 lemmas (4%) occur at least once with a non-empty value of `NumType`.
The feature is used with 3 part-of-speech tags: <tt><a href="hy_armtdp-pos-NUM.html">NUM</a></tt> (287; 1% instances), <tt><a href="hy_armtdp-pos-ADJ.html">ADJ</a></tt> (67; 0% instances), <tt><a href="hy_armtdp-pos-ADV.html">ADV</a></tt> (6; 0% instances).

### `NUM`

287 <tt><a href="hy_armtdp-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="hy_armtdp-feat-NumForm.html">NumForm</a></tt><tt>=Digit</tt> (177; 62%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (250; 87% of non-empty `NumType`): <em>երկու, հազար, մեկ, մլն, 2017, 2016, երեք, մի, 2, հարյուր</em>
* `Frac` (29; 10% of non-empty `NumType`): <em>3.2, կես, 0.2, 13,99, 14,07, 176.7, 2,3, 2,7, 201.5, 210.4</em>
* `Range` (8; 3% of non-empty `NumType`): <em>12-13, 17-23, 1878-1943, 1990-1999, 2008-2012, 65-68, 7-10, մեկ-երկու</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (143) occur only with one value of `NumType`.

### `ADJ`

67 <tt><a href="hy_armtdp-pos-ADJ.html">ADJ</a></tt> tokens (4% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="hy_armtdp-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (34; 51%).

`ADJ` tokens may have the following values of `NumType`:

* `Card` (2; 3% of non-empty `NumType`): <em>մեկ, յոթերորդ</em>
* `Frac` (1; 1% of non-empty `NumType`): <em>⅛</em>
* `Ord` (64; 96% of non-empty `NumType`): <em>առաջին, երկրորդ, 5-րդ, Բ, 100-րդ, 11-րդ, 20-րդ, XX, երրորդ, 113-րդ</em>
* `EMPTY` (1839): <em>նոր, քաղաքական, մեծ, կարող, հայ, ազգային, սուրբ, հնարավոր, պետք, կարևոր</em>

`NumType` seems to be **lexical feature** of `ADJ`. 100% lemmas (17) occur only with one value of `NumType`.

### `ADV`

6 <tt><a href="hy_armtdp-pos-ADV.html">ADV</a></tt> tokens (1% of all `ADV` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADV` and `NumType` co-occurred: <tt><a href="hy_armtdp-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (6; 100%), <tt><a href="hy_armtdp-feat-PronType.html">PronType</a></tt><tt>=Tot</tt> (6; 100%).

`ADV` tokens may have the following values of `NumType`:

* `Card` (6; 100% of non-empty `NumType`): <em>մեկ</em>
* `EMPTY` (1052): <em>էլ, ավելի, միայն, արդեն, երբ, հիմա, դեռ, շատ, տեղի, դուրս</em>

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="hy_armtdp-dep-flat.html">flat</a></tt>]--> NUM</tt> (42; 74%),
<tt>NUM --[<tt><a href="hy_armtdp-dep-conj.html">conj</a></tt>]--> NUM</tt> (12; 100%),
<tt>ADJ --[<tt><a href="hy_armtdp-dep-conj.html">conj</a></tt>]--> ADJ</tt> (1; 100%),
<tt>ADV --[<tt><a href="hy_armtdp-dep-compound-redup.html">compound:redup</a></tt>]--> ADV</tt> (1; 100%).

