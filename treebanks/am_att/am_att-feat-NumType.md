---
layout: base
title:  'Statistics of NumType in UD_Amharic-ATT'
udver: '2'
---

## Treebank Statistics: UD_Amharic-ATT: Features: `NumType`

This feature is universal.
It occurs with 1 different values: `Card`.

40 tokens (0%) have a non-empty value of `NumType`.
17 types (1%) occur at least once with a non-empty value of `NumType`.
17 lemmas (1%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="am_att-pos-NUM.html">NUM</a></tt> (40; 0% instances).

### `NUM`

40 <tt><a href="am_att-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (40; 100% of non-empty `NumType`): አንድ, ሁለት, ሦስት, ብዙ, አስር, 1.85, ስምንት, ሶስት, ሺህ, አምስት

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (17) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="am_att-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (1; 100%).

