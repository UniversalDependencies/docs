---
layout: base
title:  'Statistics of ExtPos in UD_Zaar-Autogramm'
udver: '2'
---

## Treebank Statistics: UD_Zaar-Autogramm: Features: `ExtPos`

This feature is language-specific.
It occurs with 4 different values: `ADJ`, `ADP`, `NOUN`, `SCONJ`.

309 tokens (1%) have a non-empty value of `ExtPos`.
171 types (6%) occur at least once with a non-empty value of `ExtPos`.
114 lemmas (7%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 3 part-of-speech tags: <tt><a href="say_autogramm-pos-VERB.html">VERB</a></tt> (260; 1% instances), <tt><a href="say_autogramm-pos-ADP.html">ADP</a></tt> (34; 0% instances), <tt><a href="say_autogramm-pos-SCONJ.html">SCONJ</a></tt> (15; 0% instances).

### `VERB`

260 <tt><a href="say_autogramm-pos-VERB.html">VERB</a></tt> tokens (7% of all `VERB` tokens) have a non-empty value of `ExtPos`.

`VERB` tokens may have the following values of `ExtPos`:

* `ADJ` (4; 2% of non-empty `ExtPos`): <em>kúŋni, məʃí, məʃíː, ɲâŋnês</em>
* `ADP` (1; 0% of non-empty `ExtPos`): <em>mop</em>
* `NOUN` (255; 98% of non-empty `ExtPos`): <em>ɗə́ːlɣə́n, láːpkə̂n, mbútkə̂n, yélɣə̂n, fíɣə̂n, láːpkə́nì, vyàːɣə̀n, ɬyáɣə̂n, ʧáːɣə̂n, ʧíɣə̂n</em>

<table>
  <tr><th>Paradigm <i>məs</i></th><th><tt>ADJ</tt></th><th><tt>NOUN</tt></th></tr>
  <tr><td><tt><tt><a href="say_autogramm-feat-Number.html">Number</a></tt><tt>=Plur</tt>|<tt><a href="say_autogramm-feat-VerbForm.html">VerbForm</a></tt><tt>=Vnoun</tt></tt></td><td></td><td><em>mə́ːskə̂n</em></td></tr>
  <tr><td><tt><tt><a href="say_autogramm-feat-VerbForm.html">VerbForm</a></tt><tt>=Part</tt>|<tt><a href="say_autogramm-feat-Voice.html">Voice</a></tt><tt>=Pass</tt></tt></td><td><em>məʃí, məʃíː</em></td><td></td></tr>
  <tr><td><tt><tt><a href="say_autogramm-feat-VerbForm.html">VerbForm</a></tt><tt>=Vnoun</tt></tt></td><td></td><td><em>mə́skə́nì</em></td></tr>
</table>

`ExtPos` seems to be **lexical feature** of `VERB`. 99% lemmas (99) occur only with one value of `ExtPos`.

### `ADP`

34 <tt><a href="say_autogramm-pos-ADP.html">ADP</a></tt> tokens (3% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (27; 79% of non-empty `ExtPos`): <em>á, nə́, teː, ɣá, kàmán, séː, ɗa, ɗan, hár</em>
* `SCONJ` (7; 21% of non-empty `ExtPos`): <em>nə́, ɗan</em>

<table>
  <tr><th>Paradigm <i>nə́</i></th><th><tt>ADP</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>nə́</em></td><td><em>nə́</em></td></tr>
</table>

### `SCONJ`

15 <tt><a href="say_autogramm-pos-SCONJ.html">SCONJ</a></tt> tokens (2% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

`SCONJ` tokens may have the following values of `ExtPos`:

* `SCONJ` (15; 100% of non-empty `ExtPos`): <em>kàmán, séː, ín, ɗan, dàʧêːwáː, kóː, tu, ìdán, ɓanɗan</em>

## Relations with Agreement in `ExtPos`

The 10 most frequent relations where parent and child node agree in `ExtPos`:
<tt>VERB --[<tt><a href="say_autogramm-dep-nmod.html">nmod</a></tt>]--> VERB</tt> (1; 100%).

