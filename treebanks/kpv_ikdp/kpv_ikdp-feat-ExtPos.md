---
layout: base
title:  'Statistics of ExtPos in UD_Komi_Zyrian-IKDP'
udver: '2'
---

## Treebank Statistics: UD_Komi_Zyrian-IKDP: Features: `ExtPos`

This feature is language-specific.
It occurs with 3 different values: `ADV`, `CCONJ`, `SCONJ`.

11 tokens (0%) have a non-empty value of `ExtPos`.
8 types (1%) occur at least once with a non-empty value of `ExtPos`.
7 lemmas (1%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 3 part-of-speech tags: <tt><a href="kpv_ikdp-pos-SCONJ.html">SCONJ</a></tt> (7; 0% instances), <tt><a href="kpv_ikdp-pos-PART.html">PART</a></tt> (3; 0% instances), <tt><a href="kpv_ikdp-pos-CCONJ.html">CCONJ</a></tt> (1; 0% instances).

### `SCONJ`

7 <tt><a href="kpv_ikdp-pos-SCONJ.html">SCONJ</a></tt> tokens (26% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

`SCONJ` tokens may have the following values of `ExtPos`:

* `ADV` (3; 43% of non-empty `ExtPos`): <em>как</em>
* `SCONJ` (4; 57% of non-empty `ExtPos`): <em>потому, так</em>

### `PART`

3 <tt><a href="kpv_ikdp-pos-PART.html">PART</a></tt> tokens (5% of all `PART` tokens) have a non-empty value of `ExtPos`.

`PART` tokens may have the following values of `ExtPos`:

* `ADV` (2; 67% of non-empty `ExtPos`): <em>да, то</em>
* `SCONJ` (1; 33% of non-empty `ExtPos`): <em>Так</em>

### `CCONJ`

1 <tt><a href="kpv_ikdp-pos-CCONJ.html">CCONJ</a></tt> tokens (1% of all `CCONJ` tokens) have a non-empty value of `ExtPos`.

`CCONJ` tokens may have the following values of `ExtPos`:

* `CCONJ` (1; 100% of non-empty `ExtPos`): <em>но</em>

