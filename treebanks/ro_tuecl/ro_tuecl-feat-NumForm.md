---
layout: base
title:  'Statistics of NumForm in UD_Romanian-TueCL'
udver: '2'
---

## Treebank Statistics: UD_Romanian-TueCL: Features: `NumForm`

This feature is language-specific.
It occurs with 2 different values: `Digit`, `Word`.

30 tokens (1%) have a non-empty value of `NumForm`.
21 types (1%) occur at least once with a non-empty value of `NumForm`.
20 lemmas (2%) occur at least once with a non-empty value of `NumForm`.
The feature is used with 1 part-of-speech tags: <tt><a href="ro_tuecl-pos-NUM.html">NUM</a></tt> (30; 1% instances).

### `NUM`

30 <tt><a href="ro_tuecl-pos-NUM.html">NUM</a></tt> tokens (97% of all `NUM` tokens) have a non-empty value of `NumForm`.

The most frequent other feature values with which `NUM` and `NumForm` co-occurred: <tt><a href="ro_tuecl-feat-NumType.html">NumType</a></tt><tt>=Card</tt> (28; 93%), <tt><a href="ro_tuecl-feat-Number.html">Number</a></tt><tt>=Sing</tt> (26; 87%), <tt><a href="ro_tuecl-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (25; 83%).

`NUM` tokens may have the following values of `NumForm`:

* `Digit` (24; 80% of non-empty `NumForm`): <em>10, 2, 3, 9, 1, 112, 12, 12000, 2,5, 20</em>
* `Word` (6; 20% of non-empty `NumForm`): <em>doi, doua, prima, trei, întâi</em>

`NumForm` seems to be **lexical feature** of `NUM`. 100% lemmas (20) occur only with one value of `NumForm`.

## Relations with Agreement in `NumForm`

The 10 most frequent relations where parent and child node agree in `NumForm`:
<tt>NUM --[<tt><a href="ro_tuecl-dep-conj.html">conj</a></tt>]--> NUM</tt> (2; 100%).

