---
layout: base
title:  'Statistics of ExtPos in UD_Assamese-AiW'
udver: '2'
---

## Treebank Statistics: UD_Assamese-AiW: Features: `ExtPos`

This feature is language-specific.
It occurs with 1 different values: `ADV`.

2 tokens (0%) have a non-empty value of `ExtPos`.
2 types (0%) occur at least once with a non-empty value of `ExtPos`.
2 lemmas (1%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 1 part-of-speech tags: <tt><a href="as_aiw-pos-ADV.html">ADV</a></tt> (2; 0% instances).

### `ADV`

2 <tt><a href="as_aiw-pos-ADV.html">ADV</a></tt> tokens (4% of all `ADV` tokens) have a non-empty value of `ExtPos`.

`ADV` tokens may have the following values of `ExtPos`:

* `ADV` (2; 100% of non-empty `ExtPos`): <em>অ'ত, ত'ত</em>

## Relations with Agreement in `ExtPos`

The 10 most frequent relations where parent and child node agree in `ExtPos`:
<tt>ADV --[<tt><a href="as_aiw-dep-fixed.html">fixed</a></tt>]--> ADV</tt> (1; 100%).

