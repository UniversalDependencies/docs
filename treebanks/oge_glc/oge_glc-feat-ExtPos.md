---
layout: base
title:  'Statistics of ExtPos in UD_Old_Georgian-GLC'
udver: '2'
---

## Treebank Statistics: UD_Old_Georgian-GLC: Features: `ExtPos`

This feature is language-specific.
It occurs with 2 different values: `ADV`, `PRON`.

29 tokens (0%) have a non-empty value of `ExtPos`.
15 types (1%) occur at least once with a non-empty value of `ExtPos`.
11 lemmas (1%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 4 part-of-speech tags: <tt><a href="oge_glc-pos-PRON.html">PRON</a></tt> (25; 0% instances), <tt><a href="oge_glc-pos-ADJ.html">ADJ</a></tt> (2; 0% instances), <tt><a href="oge_glc-pos-ADV.html">ADV</a></tt> (1; 0% instances), <tt><a href="oge_glc-pos-PART.html">PART</a></tt> (1; 0% instances).

### `PRON`

25 <tt><a href="oge_glc-pos-PRON.html">PRON</a></tt> tokens (3% of all `PRON` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PRON` and `ExtPos` co-occurred: <tt><a href="oge_glc-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (24; 96%), <tt><a href="oge_glc-feat-Number.html">Number</a></tt><tt>=Sing</tt> (20; 80%), <tt><a href="oge_glc-feat-PronType.html">PronType</a></tt><tt>=Rel</tt> (18; 72%).

`PRON` tokens may have the following values of `ExtPos`:

* `PRON` (25; 100% of non-empty `ExtPos`): <em>ესე, რომელმან, რომელთა, ვინაჲ, რომელი, ვინცა, თავს, რაოდენისა, რაჲ, რაჲცა</em>

### `ADJ`

2 <tt><a href="oge_glc-pos-ADJ.html">ADJ</a></tt> tokens (1% of all `ADJ` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADJ` and `ExtPos` co-occurred: <tt><a href="oge_glc-feat-Case.html">Case</a></tt><tt>=Gen</tt> (2; 100%), <tt><a href="oge_glc-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (2; 100%), <tt><a href="oge_glc-feat-Number.html">Number</a></tt><tt>=Sing</tt> (2; 100%).

`ADJ` tokens may have the following values of `ExtPos`:

* `ADV` (2; 100% of non-empty `ExtPos`): <em>შემდგომითი</em>

### `ADV`

1 <tt><a href="oge_glc-pos-ADV.html">ADV</a></tt> tokens (0% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="oge_glc-feat-AdvType.html">AdvType</a></tt><tt>=Man</tt> (1; 100%).

`ADV` tokens may have the following values of `ExtPos`:

* `ADV` (1; 100% of non-empty `ExtPos`): <em>მცირედ</em>

### `PART`

1 <tt><a href="oge_glc-pos-PART.html">PART</a></tt> tokens (1% of all `PART` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PART` and `ExtPos` co-occurred: <tt><a href="oge_glc-feat-PartType.html">PartType</a></tt><tt>=Neg</tt> (1; 100%).

`PART` tokens may have the following values of `ExtPos`:

* `ADV` (1; 100% of non-empty `ExtPos`): <em>არა</em>

