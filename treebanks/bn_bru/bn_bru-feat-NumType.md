---
layout: base
title:  'Statistics of NumType in UD_Bengali-BRU'
udver: '2'
---

## Treebank Statistics: UD_Bengali-BRU: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

3 tokens (1%) have a non-empty value of `NumType`.
3 types (2%) occur at least once with a non-empty value of `NumType`.
3 lemmas (3%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="bn_bru-pos-NUM.html">NUM</a></tt> (2; 1% instances), <tt><a href="bn_bru-pos-ADJ.html">ADJ</a></tt> (1; 0% instances).

### `NUM`

2 <tt><a href="bn_bru-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (2; 100% of non-empty `NumType`): <em>একজন, চার</em>

### `ADJ`

1 <tt><a href="bn_bru-pos-ADJ.html">ADJ</a></tt> tokens (8% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="bn_bru-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (1; 100%).

`ADJ` tokens may have the following values of `NumType`:

* `Ord` (1; 100% of non-empty `NumType`): <em>প্রথম</em>
* `EMPTY` (12): <em>কতজন, ক্ষুধার্ত, খারাপ, খুব, জাতীয়, প্রিয়, ভাল, ভালো, মজার, সবুজ</em>

