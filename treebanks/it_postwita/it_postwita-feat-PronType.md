---
layout: base
title:  'Statistics of PronType in UD_Italian-PoSTWITA'
udver: '2'
---

## Treebank Statistics: UD_Italian-PoSTWITA: Features: `PronType`

This feature is universal but the values `Exc` are language-specific.
It occurs with 9 different values: `Art`, `Dem`, `Exc`, `Ind`, `Int`, `Neg`, `Prs`, `Rel`, `Tot`.

11589 tokens (18%) have a non-empty value of `PronType`.
311 types (2%) occur at least once with a non-empty value of `PronType`.
81 lemmas (1%) occur at least once with a non-empty value of `PronType`.
The feature is used with 5 part-of-speech tags: <tt><a href="it_postwita-pos-DET.html">DET</a></tt> (7413; 11% instances), <tt><a href="it_postwita-pos-PRON.html">PRON</a></tt> (3402; 5% instances), <tt><a href="it_postwita-pos-ADV.html">ADV</a></tt> (770; 1% instances), <tt><a href="it_postwita-pos-X.html">X</a></tt> (3; 0% instances), <tt><a href="it_postwita-pos-CCONJ.html">CCONJ</a></tt> (1; 0% instances).

### `DET`

7413 <tt><a href="it_postwita-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="it_postwita-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (5659; 76%), <tt><a href="it_postwita-feat-Number.html">Number</a></tt><tt>=Sing</tt> (5474; 74%), <tt><a href="it_postwita-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (4076; 55%).

`DET` tokens may have the following values of `PronType`:

* `Art` (6466; 87% of non-empty `PronType`): <em>il, la, i, l', un, le, una, gli, lo, un'</em>
* `Dem` (200; 3% of non-empty `PronType`): <em>questo, questa, quel, sta, sto, sti, questi, quei, ste, stessa</em>
* `Exc` (48; 1% of non-empty `PronType`): <em>che, quanto, Quanta, quante</em>
* `Ind` (312; 4% of non-empty `PronType`): <em>tutti, tutto, tutta, qualche, tutte, ogni, tanti, altri, altro, nessun</em>
* `Int` (33; 0% of non-empty `PronType`): <em>che, quale, quali, quanta, quante, quanti, quanto</em>
* `Prs` (343; 5% of non-empty `PronType`): <em>mio, mia, suo, suoi, loro, tua, sua, tuo, miei, nostra</em>
* `Tot` (11; 0% of non-empty `PronType`): <em>tutti, tutto, Tutta, tutte</em>

<table>
  <tr><th>Paradigm <i>tutto</i></th><th><tt>Tot</tt></th><th><tt>Ind</tt></th></tr>
  <tr><td><tt>_</tt></td><td><em>tutti, tutto, Tutta, tutte</em></td><td><em>tutti, tutto, tutta, tutte</em></td></tr>
  <tr><td><tt><tt><a href="it_postwita-feat-Gender.html">Gender</a></tt><tt>=Masc</tt>|<tt><a href="it_postwita-feat-Number.html">Number</a></tt><tt>=Plur</tt></tt></td><td></td><td><em>tutti</em></td></tr>
</table>

`PronType` seems to be **lexical feature** of `DET`. 92% lemmas (36) occur only with one value of `PronType`.

### `PRON`

3402 <tt><a href="it_postwita-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="it_postwita-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (2385; 70%), <tt><a href="it_postwita-feat-Clitic.html">Clitic</a></tt><tt>=EMPTY</tt> (1716; 50%).

`PRON` tokens may have the following values of `PronType`:

* `Art` (1; 0% of non-empty `PronType`): <em>le</em>
* `Dem` (170; 5% of non-empty `PronType`): <em>quello, questo, quelli, questa, quella, ciò, quelle, questi, stesso, quel</em>
* `Ind` (461; 14% of non-empty `PronType`): <em>tutti, tutto, niente, nulla, qualcosa, uno, altro, nessuno, qualcuno, altri</em>
* `Int` (115; 3% of non-empty `PronType`): <em>cosa, quanto, che, chi, cos', qnti, qual', quale, QUANDO, Quando</em>
* `Prs` (2137; 63% of non-empty `PronType`): <em>si, mi, ci, lo, ti, io, la, me, c', ne</em>
* `Rel` (518; 15% of non-empty `PronType`): <em>che, chi, cui, quanto, ke, dove, quale, cha, chiunque, k</em>

<table>
  <tr><th>Paradigm <i>che</i></th><th><tt>Int</tt></th><th><tt>Rel</tt></th></tr>
  <tr><td><tt></tt></td><td><em>che</em></td><td><em>che, ke, k</em></td></tr>
</table>

`PronType` seems to be **lexical feature** of `PRON`. 92% lemmas (59) occur only with one value of `PronType`.

### `ADV`

770 <tt><a href="it_postwita-pos-ADV.html">ADV</a></tt> tokens (23% of all `ADV` tokens) have a non-empty value of `PronType`.

`ADV` tokens may have the following values of `PronType`:

* `Neg` (770; 100% of non-empty `PronType`): <em>non, nn, no</em>
* `EMPTY` (2629): <em>anche, più, bene, ancora, solo, oggi, ora, poi, sempre, così</em>

### `X`

3 <tt><a href="it_postwita-pos-X.html">X</a></tt> tokens (0% of all `X` tokens) have a non-empty value of `PronType`.

`X` tokens may have the following values of `PronType`:

* `Int` (1; 33% of non-empty `PronType`): <em>quanto</em>
* `Prs` (2; 67% of non-empty `PronType`): <em>me, te</em>
* `EMPTY` (755): <em>rt, of, Prince, live, a, and, i, cc, on, the</em>

### `CCONJ`

1 <tt><a href="it_postwita-pos-CCONJ.html">CCONJ</a></tt> tokens (0% of all `CCONJ` tokens) have a non-empty value of `PronType`.

`CCONJ` tokens may have the following values of `PronType`:

* `Rel` (1; 100% of non-empty `PronType`): <em>che</em>
* `EMPTY` (1569): <em>e, ma, o, ed, ne, cioè, però, quindi, né, pero'</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="it_postwita-dep-conj.html">conj</a></tt>]--> PRON</tt> (15; 71%),
<tt>PRON --[<tt><a href="it_postwita-dep-obl.html">obl</a></tt>]--> PRON</tt> (3; 100%),
<tt>DET --[<tt><a href="it_postwita-dep-conj.html">conj</a></tt>]--> DET</tt> (1; 100%),
<tt>PRON --[<tt><a href="it_postwita-dep-acl.html">acl</a></tt>]--> PRON</tt> (1; 100%),
<tt>PRON --[<tt><a href="it_postwita-dep-appos.html">appos</a></tt>]--> PRON</tt> (1; 100%),
<tt>PRON --[<tt><a href="it_postwita-dep-compound.html">compound</a></tt>]--> PRON</tt> (1; 100%),
<tt>PRON --[<tt><a href="it_postwita-dep-det-poss.html">det:poss</a></tt>]--> DET</tt> (1; 100%),
<tt>PRON --[<tt><a href="it_postwita-dep-orphan.html">orphan</a></tt>]--> PRON</tt> (1; 100%),
<tt>PRON --[<tt><a href="it_postwita-dep-parataxis.html">parataxis</a></tt>]--> PRON</tt> (1; 100%).

