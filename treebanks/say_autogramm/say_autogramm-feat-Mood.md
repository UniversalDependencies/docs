---
layout: base
title:  'Statistics of Mood in UD_Zaar-Autogramm'
udver: '2'
---

## Treebank Statistics: UD_Zaar-Autogramm: Features: `Mood`

This feature is universal but the values `Irr` are language-specific.
It occurs with 5 different values: `Cnd`, `Imp`, `Irr`, `Qot`, `Sub`.

382 tokens (5%) have a non-empty value of `Mood`.
35 types (3%) occur at least once with a non-empty value of `Mood`.
16 lemmas (2%) occur at least once with a non-empty value of `Mood`.
The feature is used with 4 part-of-speech tags: <tt><a href="say_autogramm-pos-AUX.html">AUX</a></tt> (243; 3% instances), <tt><a href="say_autogramm-pos-SCONJ.html">SCONJ</a></tt> (94; 1% instances), <tt><a href="say_autogramm-pos-PART.html">PART</a></tt> (36; 0% instances), <tt><a href="say_autogramm-pos-VERB.html">VERB</a></tt> (9; 0% instances).

### `AUX`

243 <tt><a href="say_autogramm-pos-AUX.html">AUX</a></tt> tokens (23% of all `AUX` tokens) have a non-empty value of `Mood`.

The most frequent other feature values with which `AUX` and `Mood` co-occurred: <tt><a href="say_autogramm-feat-Aspect.html">Aspect</a></tt><tt>=EMPTY</tt> (243; 100%), <tt><a href="say_autogramm-feat-Tense.html">Tense</a></tt><tt>=EMPTY</tt> (242; 100%), <tt><a href="say_autogramm-feat-Number.html">Number</a></tt><tt>=Sing</tt> (137; 56%).

`AUX` tokens may have the following values of `Mood`:

* `Cnd` (105; 43% of non-empty `Mood`): <em>yáː, kyáː, myàː, myáː, yǎː, kyàː, mǎː, myǎː</em>
* `Irr` (23; 9% of non-empty `Mood`): <em>mìː, mîː, kîː, tíː, kìː, míyí, mîtà, tíyí, ʧí, ʧíyí</em>
* `Sub` (115; 47% of non-empty `Mood`): <em>mə̀, tə̀, àː</em>
* `EMPTY` (796): <em>mə́, wò, mə, á, àː, má, kə, myáː, ma, ʧáː</em>

### `SCONJ`

94 <tt><a href="say_autogramm-pos-SCONJ.html">SCONJ</a></tt> tokens (36% of all `SCONJ` tokens) have a non-empty value of `Mood`.

The most frequent other feature values with which `SCONJ` and `Mood` co-occurred: <tt><a href="say_autogramm-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (94; 100%).

`SCONJ` tokens may have the following values of `Mood`:

* `Qot` (94; 100% of non-empty `Mood`): <em>tu</em>
* `EMPTY` (167): <em>ɗan, yâːn, dón, ɗa, ín, kàmán, sábòːtu, ìdán, kutu, kóː</em>

### `PART`

36 <tt><a href="say_autogramm-pos-PART.html">PART</a></tt> tokens (4% of all `PART` tokens) have a non-empty value of `Mood`.

The most frequent other feature values with which `PART` and `Mood` co-occurred: <tt><a href="say_autogramm-feat-PartType.html">PartType</a></tt><tt>=EMPTY</tt> (36; 100%), <tt><a href="say_autogramm-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (36; 100%), <tt><a href="say_autogramm-feat-VerbType.html">VerbType</a></tt><tt>=EMPTY</tt> (36; 100%).

`PART` tokens may have the following values of `Mood`:

* `Irr` (7; 19% of non-empty `Mood`): <em>dàː, kóːdàː</em>
* `Qot` (29; 81% of non-empty `Mood`): <em>wéy</em>
* `EMPTY` (800): <em>tòː, bàː, máː, tôː, oː, ɗi, hŋ́, nə, ni, n</em>

### `VERB`

9 <tt><a href="say_autogramm-pos-VERB.html">VERB</a></tt> tokens (1% of all `VERB` tokens) have a non-empty value of `Mood`.

The most frequent other feature values with which `VERB` and `Mood` co-occurred: <tt><a href="say_autogramm-feat-VerbForm.html">VerbForm</a></tt><tt>=Fin</tt> (9; 100%), <tt><a href="say_autogramm-feat-Aspect.html">Aspect</a></tt><tt>=EMPTY</tt> (8; 89%).

`VERB` tokens may have the following values of `Mood`:

* `Imp` (9; 100% of non-empty `Mood`): <em>sǔn, fi, kaɓíː, kon, máːn, tá, ɲangás, ʤáː</em>
* `EMPTY` (1211): <em>ɬə́, mán, fi, súː, wul, yel, tu, yi, ɬə, ɬǐː</em>

## Relations with Agreement in `Mood`

The 10 most frequent relations where parent and child node agree in `Mood`:
<tt>PART --[<tt><a href="say_autogramm-dep-reparandum.html">reparandum</a></tt>]--> PART</tt> (3; 100%),
<tt>SCONJ --[<tt><a href="say_autogramm-dep-fixed.html">fixed</a></tt>]--> PART</tt> (3; 100%),
<tt>AUX --[<tt><a href="say_autogramm-dep-conj.html">conj</a></tt>]--> AUX</tt> (2; 100%),
<tt>AUX --[<tt><a href="say_autogramm-dep-mark.html">mark</a></tt>]--> PART</tt> (1; 100%).

