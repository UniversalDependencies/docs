---
layout: base
title:  'Statistics of PronType in UD_Naija-NSC'
udver: '2'
---

## Treebank Statistics: UD_Naija-NSC: Features: `PronType`

This feature is universal.
It occurs with 5 different values: `Art`, `Dem`, `Int`, `Prs`, `Rel`.

21350 tokens (15%) have a non-empty value of `PronType`.
80 types (2%) occur at least once with a non-empty value of `PronType`.
49 lemmas (1%) occur at least once with a non-empty value of `PronType`.
The feature is used with 7 part-of-speech tags: <tt><a href="pcm_nsc-pos-PRON.html">PRON</a></tt> (15685; 11% instances), <tt><a href="pcm_nsc-pos-DET.html">DET</a></tt> (4844; 3% instances), <tt><a href="pcm_nsc-pos-ADV.html">ADV</a></tt> (507; 0% instances), <tt><a href="pcm_nsc-pos-SCONJ.html">SCONJ</a></tt> (277; 0% instances), <tt><a href="pcm_nsc-pos-PART.html">PART</a></tt> (25; 0% instances), <tt><a href="pcm_nsc-pos-X.html">X</a></tt> (8; 0% instances), <tt><a href="pcm_nsc-pos-AUX.html">AUX</a></tt> (4; 0% instances).

### `PRON`

15685 <tt><a href="pcm_nsc-pos-PRON.html">PRON</a></tt> tokens (86% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="pcm_nsc-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (15643; 100%), <tt><a href="pcm_nsc-feat-Case.html">Case</a></tt><tt>=Nom</tt> (11876; 76%), <tt><a href="pcm_nsc-feat-Number.html">Number</a></tt><tt>=Sing</tt> (8548; 54%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (419; 3% of non-empty `PronType`): <em>dat, dis, dose, dese, those</em>
* `Int` (513; 3% of non-empty `PronType`): <em>wetin, who, what</em>
* `Prs` (14739; 94% of non-empty `PronType`): <em>I, you, e, we, am, de, me, dem, im, us</em>
* `Rel` (14; 0% of non-empty `PronType`): <em>dat, which, that</em>
* `EMPTY` (2574): <em>my, your, our, deir, everybody, some, everyting, anyting, her, someting</em>

<table>
  <tr><th>Paradigm <i>dat</i></th><th><tt>Rel</tt></th><th><tt>Dem</tt></th></tr>
  <tr><td><tt>_</tt></td><td><em>that</em></td><td></td></tr>
  <tr><td><tt><tt><a href="pcm_nsc-feat-Number.html">Number</a></tt><tt>=Sing</tt></tt></td><td></td><td><em>dat</em></td></tr>
</table>

`PronType` seems to be **lexical feature** of `PRON`. 97% lemmas (32) occur only with one value of `PronType`.

### `DET`

4844 <tt><a href="pcm_nsc-pos-DET.html">DET</a></tt> tokens (86% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="pcm_nsc-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (3235; 67%), <tt><a href="pcm_nsc-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (2812; 58%).

`DET` tokens may have the following values of `PronType`:

* `Art` (3173; 66% of non-empty `PronType`): <em>di, one, a, the, an</em>
* `Dem` (1609; 33% of non-empty `PronType`): <em>dis, dat, dose, dese, those, that, da, this</em>
* `Int` (62; 1% of non-empty `PronType`): <em>which</em>
* `EMPTY` (814): <em>all, some, any, anoder, every, anoda, each, no, one, another</em>

### `ADV`

507 <tt><a href="pcm_nsc-pos-ADV.html">ADV</a></tt> tokens (9% of all `ADV` tokens) have a non-empty value of `PronType`.

`ADV` tokens may have the following values of `PronType`:

* `Int` (507; 100% of non-empty `PronType`): <em>how, where, why, when</em>
* `EMPTY` (5359): <em>so, now, just, even, well, dere, den, still, very, here</em>

### `SCONJ`

277 <tt><a href="pcm_nsc-pos-SCONJ.html">SCONJ</a></tt> tokens (6% of all `SCONJ` tokens) have a non-empty value of `PronType`.

`SCONJ` tokens may have the following values of `PronType`:

* `Int` (276; 100% of non-empty `PronType`): <em>when</em>
* `Prs` (1; 0% of non-empty `PronType`): <em>sey</em>
* `EMPTY` (4704): <em>sey, wey, if, because, so, wen, dat, wheder, once, weh</em>

### `PART`

25 <tt><a href="pcm_nsc-pos-PART.html">PART</a></tt> tokens (1% of all `PART` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PART` and `PronType` co-occurred: <tt><a href="pcm_nsc-feat-PartType.html">PartType</a></tt><tt>=EMPTY</tt> (25; 100%), <tt><a href="pcm_nsc-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (25; 100%).

`PART` tokens may have the following values of `PronType`:

* `Int` (24; 96% of non-empty `PronType`): <em>shey</em>
* `Prs` (1; 4% of non-empty `PronType`): <em>masef</em>
* `EMPTY` (2993): <em>no, o, sef, sha, not, dem, ma, n't, ni, ba</em>

### `X`

8 <tt><a href="pcm_nsc-pos-X.html">X</a></tt> tokens (0% of all `X` tokens) have a non-empty value of `PronType`.

`X` tokens may have the following values of `PronType`:

* `Dem` (2; 25% of non-empty `PronType`): <em>{, }</em>
* `Prs` (6; 75% of non-empty `PronType`): <em>>+, [, de, {, ||, }</em>
* `EMPTY` (40268): <em>#, //, <, {, }, [, |c, ||, ], >+</em>

<table>
  <tr><th>Paradigm <i>{</i></th><th><tt>Prs</tt></th><th><tt>Dem</tt></th></tr>
  <tr><td><tt><tt><a href="pcm_nsc-feat-Case.html">Case</a></tt><tt>=Nom</tt>|<tt><a href="pcm_nsc-feat-Person.html">Person</a></tt><tt>=2</tt></tt></td><td><em>{</em></td><td></td></tr>
  <tr><td><tt><tt><a href="pcm_nsc-feat-Number.html">Number</a></tt><tt>=Sing</tt></tt></td><td></td><td><em>{</em></td></tr>
</table>

### `AUX`

4 <tt><a href="pcm_nsc-pos-AUX.html">AUX</a></tt> tokens (0% of all `AUX` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `AUX` and `PronType` co-occurred: <tt><a href="pcm_nsc-feat-Aspect.html">Aspect</a></tt><tt>=EMPTY</tt> (3; 75%), <tt><a href="pcm_nsc-feat-PartType.html">PartType</a></tt><tt>=EMPTY</tt> (3; 75%).

`AUX` tokens may have the following values of `PronType`:

* `Prs` (4; 100% of non-empty `PronType`): <em>'m, Don, be</em>
* `EMPTY` (12266): <em>dey, go, na, con, don, be, make, fit, come, meh</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="pcm_nsc-dep-reparandum.html">reparandum</a></tt>]--> PRON</tt> (124; 95%),
<tt>DET --[<tt><a href="pcm_nsc-dep-reparandum.html">reparandum</a></tt>]--> DET</tt> (50; 75%),
<tt>PRON --[<tt><a href="pcm_nsc-dep-conj.html">conj</a></tt>]--> PRON</tt> (38; 88%),
<tt>ADV --[<tt><a href="pcm_nsc-dep-parataxis-conj.html">parataxis:conj</a></tt>]--> ADV</tt> (14; 93%),
<tt>ADV --[<tt><a href="pcm_nsc-dep-reparandum.html">reparandum</a></tt>]--> ADV</tt> (13; 100%),
<tt>PRON --[<tt><a href="pcm_nsc-dep-parataxis-conj.html">parataxis:conj</a></tt>]--> PRON</tt> (13; 87%),
<tt>ADV --[<tt><a href="pcm_nsc-dep-conj.html">conj</a></tt>]--> ADV</tt> (6; 86%),
<tt>PRON --[<tt><a href="pcm_nsc-dep-appos.html">appos</a></tt>]--> PRON</tt> (4; 100%),
<tt>PRON --[<tt><a href="pcm_nsc-dep-compound-redup.html">compound:redup</a></tt>]--> PRON</tt> (4; 100%),
<tt>PRON --[<tt><a href="pcm_nsc-dep-conj.html">conj</a></tt>]--> ADV</tt> (3; 75%).

