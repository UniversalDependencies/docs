---
layout: base
title:  'Statistics of Aspect in UD_Zaar-Autogramm'
udver: '2'
---

## Treebank Statistics: UD_Zaar-Autogramm: Features: `Aspect`

This feature is universal but the values `Aor`, `Conc`, `ImpIter`, `Inch`, `Iter`, `Res` are language-specific.
It occurs with 9 different values: `Aor`, `Conc`, `Imp`, `ImpIter`, `Inch`, `Iter`, `Perf`, `Prog`, `Res`.

803 tokens (11%) have a non-empty value of `Aspect`.
179 types (13%) occur at least once with a non-empty value of `Aspect`.
105 lemmas (13%) occur at least once with a non-empty value of `Aspect`.
The feature is used with 9 part-of-speech tags: <tt><a href="say_autogramm-pos-AUX.html">AUX</a></tt> (522; 7% instances), <tt><a href="say_autogramm-pos-VERB.html">VERB</a></tt> (170; 2% instances), <tt><a href="say_autogramm-pos-PART.html">PART</a></tt> (42; 1% instances), <tt><a href="say_autogramm-pos-NOUN.html">NOUN</a></tt> (35; 0% instances), <tt><a href="say_autogramm-pos-PRON.html">PRON</a></tt> (27; 0% instances), <tt><a href="say_autogramm-pos-ADV.html">ADV</a></tt> (3; 0% instances), <tt><a href="say_autogramm-pos-NUM.html">NUM</a></tt> (2; 0% instances), <tt><a href="say_autogramm-pos-ADJ.html">ADJ</a></tt> (1; 0% instances), <tt><a href="say_autogramm-pos-PROPN.html">PROPN</a></tt> (1; 0% instances).

### `AUX`

522 <tt><a href="say_autogramm-pos-AUX.html">AUX</a></tt> tokens (50% of all `AUX` tokens) have a non-empty value of `Aspect`.

The most frequent other feature values with which `AUX` and `Aspect` co-occurred: <tt><a href="say_autogramm-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (522; 100%), <tt><a href="say_autogramm-feat-Tense.html">Tense</a></tt><tt>=EMPTY</tt> (479; 92%), <tt><a href="say_autogramm-feat-Number.html">Number</a></tt><tt>=Sing</tt> (341; 65%).

`AUX` tokens may have the following values of `Aspect`:

* `Aor` (254; 49% of non-empty `Aspect`): <em>mə́, mə, á, kə, tə́, kə́, mə̀</em>
* `Conc` (2; 0% of non-empty `Aspect`): <em>myáːnaː</em>
* `Imp` (126; 24% of non-empty `Aspect`): <em>myáː, ʧáː, myǎː, átâyáː, kə̀tàyáː, mətáyáː, áyǎː, mənáːyáː, mə̀tàyáː, tə̀tàyáː</em>
* `ImpIter` (2; 0% of non-empty `Aspect`): <em>myàːyi, myáːyi</em>
* `Iter` (8; 2% of non-empty `Aspect`): <em>yi, mayi, miː, mə̀yi, átâyi</em>
* `Perf` (111; 21% of non-empty `Aspect`): <em>àː, káː, máː, tàː, màː, àːtá, máːtá, máːyí, âː, màːtá</em>
* `Prog` (19; 4% of non-empty `Aspect`): <em>ʧǎː, myǎː, ʧàː, ʧìɣá, miɣá, kiɣá, myaː, mìɣá, mə̀tàyiɣá</em>
* `EMPTY` (517): <em>wò, mə̀, tə̀, má, yáː, ma, ka, tá, kyáː, átâ</em>

`Aspect` seems to be **lexical feature** of `AUX`. 100% lemmas (15) occur only with one value of `Aspect`.

### `VERB`

170 <tt><a href="say_autogramm-pos-VERB.html">VERB</a></tt> tokens (14% of all `VERB` tokens) have a non-empty value of `Aspect`.

The most frequent other feature values with which `VERB` and `Aspect` co-occurred: <tt><a href="say_autogramm-feat-VerbForm.html">VerbForm</a></tt><tt>=Fin</tt> (167; 98%).

`VERB` tokens may have the following values of `Aspect`:

* `Inch` (6; 4% of non-empty `Aspect`): <em>fin, fín, fîn, yin, yìn, ɬyan</em>
* `Res` (164; 96% of non-empty `Aspect`): <em>ɬǐː, ɬíː, ɬǎːy, laːtsə́y, ndáy, ndǎy, ɓâníː, mbûɗíː, fíː, kaɓíː</em>
* `EMPTY` (1050): <em>ɬə́, fi, mán, súː, wul, yel, tu, yi, ɬə, fuː</em>

<table>
  <tr><th>Paradigm <i>fi</i></th><th><tt>Inch</tt></th><th><tt>Res</tt></th></tr>
  <tr><td><tt></tt></td><td><em>fin, fín, fîn</em></td><td><em>fíː, fǐː, fîː</em></td></tr>
</table>

`Aspect` seems to be **lexical feature** of `VERB`. 96% lemmas (49) occur only with one value of `Aspect`.

### `PART`

42 <tt><a href="say_autogramm-pos-PART.html">PART</a></tt> tokens (5% of all `PART` tokens) have a non-empty value of `Aspect`.

The most frequent other feature values with which `PART` and `Aspect` co-occurred: <tt><a href="say_autogramm-feat-PartType.html">PartType</a></tt><tt>=EMPTY</tt> (42; 100%), <tt><a href="say_autogramm-feat-VerbType.html">VerbType</a></tt><tt>=EMPTY</tt> (42; 100%), <tt><a href="say_autogramm-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (41; 98%).

`PART` tokens may have the following values of `Aspect`:

* `Inch` (38; 90% of non-empty `Aspect`): <em>ni, nì, n</em>
* `Res` (4; 10% of non-empty `Aspect`): <em>məníː, kwêy, ə̌níː</em>
* `EMPTY` (794): <em>tòː, bàː, máː, tôː, oː, ɗi, hŋ́, nə, n, mə́n</em>

### `NOUN`

35 <tt><a href="say_autogramm-pos-NOUN.html">NOUN</a></tt> tokens (5% of all `NOUN` tokens) have a non-empty value of `Aspect`.

`NOUN` tokens may have the following values of `Aspect`:

* `Res` (35; 100% of non-empty `Aspect`): <em>gə̀ɗíː, lə́ɓíː, bóːlǐː, kuríː, lǎːy, kàlàːʧíː, kə́lâːʃíː, ləpíː, náɣɗêʃíː, náɣɗíː</em>
* `EMPTY` (663): <em>laː, də̀n, dzàn, mə́n, lə̂p, vìː, dzàŋ, ngə́tn, ŋáː, gə̀t</em>

`Aspect` seems to be **lexical feature** of `NOUN`. 100% lemmas (20) occur only with one value of `Aspect`.

### `PRON`

27 <tt><a href="say_autogramm-pos-PRON.html">PRON</a></tt> tokens (5% of all `PRON` tokens) have a non-empty value of `Aspect`.

The most frequent other feature values with which `PRON` and `Aspect` co-occurred: <tt><a href="say_autogramm-feat-PronType.html">PronType</a></tt><tt>=Prs</tt> (27; 100%), <tt><a href="say_autogramm-feat-Number.html">Number</a></tt><tt>=Sing</tt> (17; 63%), <tt><a href="say_autogramm-feat-Person.html">Person</a></tt><tt>=3</tt> (14; 52%).

`PRON` tokens may have the following values of `Aspect`:

* `Res` (27; 100% of non-empty `Aspect`): <em>=tíː, =míː, =wôpíː, =gə̀tíː, =kə́y, =tə́y, =wàːsə̀ŋə́y, =wòpíː, =âtíː, =ʃíː</em>
* `EMPTY` (534): <em>=tə, =mí, =ʃí, ɣáy, =m, =tə̀, gòs, myâːn, =ɣə, ʧì</em>

`Aspect` seems to be **lexical feature** of `PRON`. 100% lemmas (10) occur only with one value of `Aspect`.

### `ADV`

3 <tt><a href="say_autogramm-pos-ADV.html">ADV</a></tt> tokens (1% of all `ADV` tokens) have a non-empty value of `Aspect`.

The most frequent other feature values with which `ADV` and `Aspect` co-occurred: <tt><a href="say_autogramm-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (2; 67%).

`ADV` tokens may have the following values of `Aspect`:

* `Res` (3; 100% of non-empty `Aspect`): <em>ʧiɣə́y, ʧíɣə́y, ɗúːníː</em>
* `EMPTY` (328): <em>séː, sòːséy, káwêy, káwây, ɗangəní, ʧík, yáːwón, ɗan, wuriː, tá</em>

### `NUM`

2 <tt><a href="say_autogramm-pos-NUM.html">NUM</a></tt> tokens (11% of all `NUM` tokens) have a non-empty value of `Aspect`.

`NUM` tokens may have the following values of `Aspect`:

* `Res` (2; 100% of non-empty `Aspect`): <em>nàmbóŋə́y, nàmbóɲíː</em>
* `EMPTY` (17): <em>nàmbón, lim, ɗàrí, dubu, nandam, hàmsə́n, limês, wupsə</em>

### `ADJ`

1 <tt><a href="say_autogramm-pos-ADJ.html">ADJ</a></tt> tokens (2% of all `ADJ` tokens) have a non-empty value of `Aspect`.

The most frequent other feature values with which `ADJ` and `Aspect` co-occurred: <tt><a href="say_autogramm-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (1; 100%).

`ADJ` tokens may have the following values of `Aspect`:

* `Res` (1; 100% of non-empty `Aspect`): <em>vàrèʃíː</em>
* `EMPTY` (49): <em>ŋaː, mərə́, ŋǎː, ʧólák, mari, nʤwâːtn, ʒèlì, ʧolak, banza, bàptàk</em>

### `PROPN`

1 <tt><a href="say_autogramm-pos-PROPN.html">PROPN</a></tt> tokens (1% of all `PROPN` tokens) have a non-empty value of `Aspect`.

`PROPN` tokens may have the following values of `Aspect`:

* `Res` (1; 100% of non-empty `Aspect`): <em>Kullây</em>
* `EMPTY` (195): <em>Kímsə, Bàːbá, Malâːr, Púʤì, Tʃôkn, Zəgì, Féːlêks, Gôː, Kámʃâk, Làːdí</em>

## Relations with Agreement in `Aspect`

The 10 most frequent relations where parent and child node agree in `Aspect`:
<tt>VERB --[<tt><a href="say_autogramm-dep-reparandum.html">reparandum</a></tt>]--> VERB</tt> (2; 100%),
<tt>AUX --[<tt><a href="say_autogramm-dep-reparandum.html">reparandum</a></tt>]--> AUX</tt> (1; 100%).

