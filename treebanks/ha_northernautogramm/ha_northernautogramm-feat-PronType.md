---
layout: base
title:  'Statistics of PronType in UD_Hausa-NorthernAutogramm'
udver: '2'
---

## Treebank Statistics: UD_Hausa-NorthernAutogramm: Features: `PronType`

This feature is universal.
It occurs with 6 different values: `Dem`, `Ind`, `Int`, `Prs`, `Rel`, `Tot`.

330 tokens (8%) have a non-empty value of `PronType`.
81 types (10%) occur at least once with a non-empty value of `PronType`.
64 lemmas (12%) occur at least once with a non-empty value of `PronType`.
The feature is used with 4 part-of-speech tags: <tt><a href="ha_northernautogramm-pos-PRON.html">PRON</a></tt> (192; 5% instances), <tt><a href="ha_northernautogramm-pos-ADV.html">ADV</a></tt> (85; 2% instances), <tt><a href="ha_northernautogramm-pos-DET.html">DET</a></tt> (52; 1% instances), <tt><a href="ha_northernautogramm-pos-ADJ.html">ADJ</a></tt> (1; 0% instances).

### `PRON`

192 <tt><a href="ha_northernautogramm-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="ha_northernautogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (143; 74%), <tt><a href="ha_northernautogramm-feat-Person.html">Person</a></tt><tt>=3</tt> (111; 58%), <tt><a href="ha_northernautogramm-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (97; 51%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (16; 8% of non-empty `PronType`): <em>wàccan, wàgga, wànnan, wàncân, wâggàːshi, wàncéːnìyaː, wànga</em>
* `Ind` (13; 7% of non-empty `PronType`): <em>koːmiː, koːwaː, wani, wasu, wata</em>
* `Int` (4; 2% of non-empty `PronType`): <em>miː, wanèː</em>
* `Prs` (153; 80% of non-empty `PronType`): <em>shiː, ita, shì, shi, mai, niː, suː, musù, tà, sù</em>
* `Rel` (5; 3% of non-empty `PronType`): <em>sà'addà, wa'ànda, wandà</em>
* `Tot` (1; 1% of non-empty `PronType`): <em>dûn</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (42) occur only with one value of `PronType`.

### `ADV`

85 <tt><a href="ha_northernautogramm-pos-ADV.html">ADV</a></tt> tokens (33% of all `ADV` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADV` and `PronType` co-occurred: <tt><a href="ha_northernautogramm-feat-Deixis.html">Deixis</a></tt><tt>=EMPTY</tt> (53; 62%).

`ADV` tokens may have the following values of `PronType`:

* `Dem` (51; 60% of non-empty `PronType`): <em>nan, nân, cân, nanânga, can, ceːnìyaː, nân.</em>
* `Ind` (2; 2% of non-empty `PronType`): <em>koː'ìnaː</em>
* `Int` (9; 11% of non-empty `PronType`): <em>ƙàːƙàː, ìnaː</em>
* `Rel` (22; 26% of non-empty `PronType`): <em>indà, indàduk, duwwàdà, indàdun, indàdut, koːìnaː, koːƙàːƙàː, kóːkòːindà, wàdà</em>
* `Tot` (1; 1% of non-empty `PronType`): <em>duh</em>
* `EMPTY` (172): <em>sai, hakànan, sa'ànnan, dâːmaː, kullun, dâː, sa’ànnan, wàje, yànzu, wai</em>

`PronType` seems to be **lexical feature** of `ADV`. 100% lemmas (18) occur only with one value of `PronType`.

### `DET`

52 <tt><a href="ha_northernautogramm-pos-DET.html">DET</a></tt> tokens (60% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="ha_northernautogramm-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (44; 85%), <tt><a href="ha_northernautogramm-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (36; 69%), <tt><a href="ha_northernautogramm-feat-Deixis.html">Deixis</a></tt><tt>=EMPTY</tt> (35; 67%).

`DET` tokens may have the following values of `PronType`:

* `Dem` (19; 37% of non-empty `PronType`): <em>ga, wàccân, can, nan, tan</em>
* `Ind` (14; 27% of non-empty `PronType`): <em>wata, wani, wasu</em>
* `Int` (3; 6% of non-empty `PronType`): <em>wanèː, waccè</em>
* `Tot` (16; 31% of non-empty `PronType`): <em>dug, du', dus, duy, dub, dukà, dun, dut, duk</em>
* `EMPTY` (34): <em>nan, su, tan</em>

### `ADJ`

1 <tt><a href="ha_northernautogramm-pos-ADJ.html">ADJ</a></tt> tokens (3% of all `ADJ` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADJ` and `PronType` co-occurred: <tt><a href="ha_northernautogramm-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (1; 100%), <tt><a href="ha_northernautogramm-feat-Gender.html">Gender</a></tt><tt>=Fem</tt> (1; 100%).

`ADJ` tokens may have the following values of `PronType`:

* `Int` (1; 100% of non-empty `PronType`): <em>wacèː</em>
* `EMPTY` (29): <em>ɗan, daban, hwarin, jaː, maːtaː, baƙiː, jan, baƙin, hwariː, hìyayyem</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>ADV --[<tt><a href="ha_northernautogramm-dep-parataxis.html">parataxis</a></tt>]--> ADV</tt> (6; 100%),
<tt>ADV --[<tt><a href="ha_northernautogramm-dep-conj.html">conj</a></tt>]--> ADV</tt> (5; 100%),
<tt>PRON --[<tt><a href="ha_northernautogramm-dep-conj.html">conj</a></tt>]--> PRON</tt> (2; 100%).

