

--------------------------------------------------------------------------------

## Treebank Statistics (UD_Estonian)

This feature is universal.
It occurs with 2 different values: `Cmp`, `Pos`.

456 tokens (5%) have a non-empty value of `Degree`.
375 types (10%) occur at least once with a non-empty value of `Degree`.
356 lemmas (10%) occur at least once with a non-empty value of `Degree`.
The feature is used with 1 part-of-speech tags: [et-pos/ADJ]() (456; 5% instances).

### `ADJ`

456 [et-pos/ADJ]() tokens (95% of all `ADJ` tokens) have a non-empty value of `Degree`.

The most frequent other feature values with which `ADJ` and `Degree` co-occurred: <tt><a href="VerbForm.html">VerbForm</a>=EMPTY</tt> (394; 86%), <tt><a href="Number.html">Number</a>=Sing</tt> (332; 73%).

`ADJ` tokens may have the following values of `Degree`:

* `Cmp` (19; 4% of non-empty `Degree`): <em>parema, parem, tugevam, vaiksemaks, enam, kaugema, paremad, paremaid, parematel, suurem</em>
* `Pos` (437; 96% of non-empty `Degree`): <em>kogu, suur, tugev, suurest, valged, viimase, hingetuks, musta, noor, raske</em>
* `EMPTY` (24): <em>teise, kolmandale, kolmandat, Esimene, Inglise, Kolmas, Teisel, esimese, esimesest, esimest</em>

`Degree` seems to be **lexical feature** of `ADJ`. 100% lemmas (356) occur only with one value of `Degree`.

## Relations with Agreement in `Degree`

The 10 most frequent relations where parent and child node agree in `Degree`:
<tt>ADJ --[<a href="../dep/conj.html">conj</a>]--> ADJ</tt> (10; 91%),
<tt>ADJ --[<a href="../dep/nmod.html">nmod</a>]--> ADJ</tt> (1; 100%).

