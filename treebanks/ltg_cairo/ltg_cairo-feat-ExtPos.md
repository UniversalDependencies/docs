---
layout: base
title:  'Statistics of ExtPos in UD_Latgalian-Cairo'
udver: '2'
---

## Treebank Statistics: UD_Latgalian-Cairo: Features: `ExtPos`

This feature is language-specific.
It occurs with 1 different values: `SCONJ`.

1 tokens (1%) have a non-empty value of `ExtPos`.
1 types (1%) occur at least once with a non-empty value of `ExtPos`.
1 lemmas (1%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 1 part-of-speech tags: <tt><a href="ltg_cairo-pos-ADV.html">ADV</a></tt> (1; 1% instances).

### `ADV`

1 <tt><a href="ltg_cairo-pos-ADV.html">ADV</a></tt> tokens (13% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="ltg_cairo-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (1; 100%).

`ADV` tokens may have the following values of `ExtPos`:

* `SCONJ` (1; 100% of non-empty `ExtPos`): <em>partū</em>

