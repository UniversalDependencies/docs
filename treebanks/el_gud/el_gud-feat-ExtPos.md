---
layout: base
title:  'Statistics of ExtPos in UD_Greek-GUD'
udver: '2'
---

## Treebank Statistics: UD_Greek-GUD: Features: `ExtPos`

This feature is language-specific.
It occurs with 2 different values: `ADV`, `SCONJ`.

18 tokens (0%) have a non-empty value of `ExtPos`.
8 types (0%) occur at least once with a non-empty value of `ExtPos`.
6 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 2 part-of-speech tags: <tt><a href="el_gud-pos-ADP.html">ADP</a></tt> (10; 0% instances), <tt><a href="el_gud-pos-PART.html">PART</a></tt> (8; 0% instances).

### `ADP`

10 <tt><a href="el_gud-pos-ADP.html">ADP</a></tt> tokens (1% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADV` (10; 100% of non-empty `ExtPos`): <em>σ, ως, εκ, επ', μετά</em>

### `PART`

8 <tt><a href="el_gud-pos-PART.html">PART</a></tt> tokens (2% of all `PART` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PART` and `ExtPos` co-occurred: <tt><a href="el_gud-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (8; 100%).

`PART` tokens may have the following values of `ExtPos`:

* `SCONJ` (8; 100% of non-empty `ExtPos`): <em>μπας</em>

