---
layout: base
title:  'Statistics of Person in UD_Neapolitan-RB'
udver: '2'
---

## Treebank Statistics: UD_Neapolitan-RB: Features: `Person`

This feature is universal.
It occurs with 3 different values: `1`, `2`, `3`.

38 tokens (19%) have a non-empty value of `Person`.
30 types (24%) occur at least once with a non-empty value of `Person`.
23 lemmas (21%) occur at least once with a non-empty value of `Person`.
The feature is used with 3 part-of-speech tags: <tt><a href="nap_rb-pos-AUX.html">AUX</a></tt> (14; 7% instances), <tt><a href="nap_rb-pos-PRON.html">PRON</a></tt> (12; 6% instances), <tt><a href="nap_rb-pos-VERB.html">VERB</a></tt> (12; 6% instances).

### `AUX`

14 <tt><a href="nap_rb-pos-AUX.html">AUX</a></tt> tokens (93% of all `AUX` tokens) have a non-empty value of `Person`.

The most frequent other feature values with which `AUX` and `Person` co-occurred: <tt><a href="nap_rb-feat-VerbForm.html">VerbForm</a></tt><tt>=Fin</tt> (14; 100%), <tt><a href="nap_rb-feat-Mood.html">Mood</a></tt><tt>=Ind</tt> (12; 86%), <tt><a href="nap_rb-feat-Tense.html">Tense</a></tt><tt>=Pres</tt> (8; 57%).

`AUX` tokens may have the following values of `Person`:

* `2` (1; 7% of non-empty `Person`): <em>stajĕ</em>
* `3` (13; 93% of non-empty `Person`): <em>è, so, afossĕ, avia, avissĕ, ha, hannĕ, putettĕrĕ, sta, vvĕnettĕ</em>
* `EMPTY` (1): <em>avutŏ</em>

<table>
  <tr><th>Paradigm <i>stà</i></th><th><tt>2</tt></th><th><tt>3</tt></th></tr>
  <tr><td><tt></tt></td><td><em>stajĕ</em></td><td><em>sta</em></td></tr>
</table>

### `PRON`

12 <tt><a href="nap_rb-pos-PRON.html">PRON</a></tt> tokens (63% of all `PRON` tokens) have a non-empty value of `Person`.

The most frequent other feature values with which `PRON` and `Person` co-occurred: <tt><a href="nap_rb-feat-PronType.html">PronType</a></tt><tt>=Prs</tt> (12; 100%), <tt><a href="nap_rb-feat-Reflex.html">Reflex</a></tt><tt>=EMPTY</tt> (10; 83%), <tt><a href="nap_rb-feat-Number.html">Number</a></tt><tt>=Sing</tt> (9; 75%).

`PRON` tokens may have the following values of `Person`:

* `2` (2; 17% of non-empty `Person`): <em>Tĕ, tuojŏ</em>
* `3` (10; 83% of non-empty `Person`): <em>Essă, issŏ, l', s'</em>
* `EMPTY` (7): <em>Chĕ, ată, c', chi, cĕ, n', nĕ</em>

### `VERB`

12 <tt><a href="nap_rb-pos-VERB.html">VERB</a></tt> tokens (41% of all `VERB` tokens) have a non-empty value of `Person`.

The most frequent other feature values with which `VERB` and `Person` co-occurred: <tt><a href="nap_rb-feat-VerbForm.html">VerbForm</a></tt><tt>=Fin</tt> (12; 100%), <tt><a href="nap_rb-feat-Mood.html">Mood</a></tt><tt>=Ind</tt> (11; 92%).

`VERB` tokens may have the following values of `Person`:

* `1` (1; 8% of non-empty `Person`): <em>Penzĕ</em>
* `2` (3; 25% of non-empty `Person`): <em>arapĕ, pienzĕ, vuò</em>
* `3` (8; 67% of non-empty `Person`): <em>accattajĕ, criscettĕ, currevĕ, facettĕ, pittajĕ, scrivettĕ, tenĕnĕ, vincettĕ</em>
* `EMPTY` (17): <em>abbracciatĕ, accuncià, asciutĕ, chiuvennĕ, distribbuită, fattŏ, fummà, guaddannĕ, jì, lavà</em>

<table>
  <tr><th>Paradigm <i>penzà</i></th><th><tt>1</tt></th><th><tt>2</tt></th></tr>
  <tr><td><tt></tt></td><td><em>Penzĕ</em></td><td><em>pienzĕ</em></td></tr>
</table>

`Person` seems to be **lexical feature** of `VERB`. 91% lemmas (10) occur only with one value of `Person`.

## Relations with Agreement in `Person`

The 10 most frequent relations where parent and child node agree in `Person`:
<tt>VERB --[<tt><a href="nap_rb-dep-nsubj.html">nsubj</a></tt>]--> PRON</tt> (4; 67%).

