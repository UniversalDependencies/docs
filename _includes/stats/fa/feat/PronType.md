

--------------------------------------------------------------------------------

## Treebank Statistics (UD_Persian)

This feature is universal.
It occurs with 8 different values: `Dem`, `Ind`, `Int`, `Neg`, `Prs`, `Rcp`, `Rel`, `Tot`.

6127 tokens (4%) have a non-empty value of `PronType`.
90 types (1%) occur at least once with a non-empty value of `PronType`.
1 lemmas (0) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: [fa-pos/PRON]() (5765; 4% instances), [fa-pos/ADV]() (343; 0% instances), [fa-pos/DET]() (19; 0% instances).

### `PRON`

5765 [fa-pos/PRON]() tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="Reflex.html">Reflex</a>=EMPTY</tt> (4970; 86%), <tt><a href="Number.html">Number</a>=Sing</tt> (3928; 68%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (1619; 28% of non-empty `PronType`): آن، این، آن‌ها، آنان، این‌ها، همین، آنرا، همان، اینان، دان
* `Ind` (151; 3% of non-empty `PronType`): بعضی، برخی، دیگران، هرکس، بعضی‌ها، تک‌تک، هرکه، بعضیهای
* `Int` (4; 0% of non-empty `PronType`): هرکه، کی
* `Neg` (9; 0% of non-empty `PronType`): هیچکدام، هیچکس، هیچیک، هیچ‌یک
* `Prs` (3604; 63% of non-empty `PronType`): خود، او، ما، ش، من، وی، تو، شان، م، شما
* `Rcp` (102; 2% of non-empty `PronType`): هم، یکدیگر، همدیگر
* `Rel` (47; 1% of non-empty `PronType`): آنچه
* `Tot` (229; 4% of non-empty `PronType`): همه، همگی، همگان، همه‌، تمام، هرکدام
* `EMPTY` (7): جنابعالی، حضرتعالی

### `ADV`

343 [fa-pos/ADV]() tokens (8% of all `ADV` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADV` and `PronType` co-occurred: <tt><a href="Case.html">Case</a>=EMPTY</tt> (343; 100%).

`ADV` tokens may have the following values of `PronType`:

* `Int` (343; 100% of non-empty `PronType`): چه، چرا، چی، چگونه، مگر، کجا، چقدر، چطور، کی، چه‌طور
* `EMPTY` (3807): پس، هیچ، خیلی، پیش، نه، بعد، حتی، بسیار، مثل، البته

### `DET`

19 [fa-pos/DET]() tokens (1% of all `DET` tokens) have a non-empty value of `PronType`.

`DET` tokens may have the following values of `PronType`:

* `Ind` (19; 100% of non-empty `PronType`): بعضی
* `EMPTY` (3542): این، آن، هر، همان، همین، برخی، تمام، تمامی، دیگر، همهٔ

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<a href="../dep/nmod:poss.html">nmod:poss</a>]--> PRON</tt> (149; 91%),
<tt>PRON --[<a href="../dep/conj.html">conj</a>]--> PRON</tt> (10; 77%),
<tt>PRON --[<a href="../dep/nsubj.html">nsubj</a>]--> PRON</tt> (5; 63%),
<tt>ADV --[<a href="../dep/conj.html">conj</a>]--> ADV</tt> (2; 100%).

