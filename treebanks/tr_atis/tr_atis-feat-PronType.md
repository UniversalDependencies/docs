---
layout: base
title:  'Statistics of PronType in UD_Turkish-Atis'
udver: '2'
---

## Treebank Statistics: UD_Turkish-Atis: Features: `PronType`

This feature is universal.
It occurs with 5 different values: `Art`, `Dem`, `Ind`, `Int`, `Prs`.

2887 tokens (7%) have a non-empty value of `PronType`.
41 types (2%) occur at least once with a non-empty value of `PronType`.
14 lemmas (2%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="tr_atis-pos-DET.html">DET</a></tt> (1282; 3% instances), <tt><a href="tr_atis-pos-PRON.html">PRON</a></tt> (1173; 3% instances), <tt><a href="tr_atis-pos-AUX.html">AUX</a></tt> (432; 1% instances).

### `DET`

1282 <tt><a href="tr_atis-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="tr_atis-feat-Definite.html">Definite</a></tt><tt>=Ind</tt> (669; 52%).

`DET` tokens may have the following values of `PronType`:

* `Art` (669; 52% of non-empty `PronType`): <em>bir</em>
* `Dem` (25; 2% of non-empty `PronType`): <em>bu, o, şu</em>
* `Ind` (588; 46% of non-empty `PronType`): <em>tüm, her</em>

### `PRON`

1173 <tt><a href="tr_atis-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="tr_atis-feat-Number.html">Number</a></tt><tt>=Sing</tt> (1132; 97%), <tt><a href="tr_atis-feat-Person.html">Person</a></tt><tt>=1</tt> (944; 80%), <tt><a href="tr_atis-feat-Case.html">Case</a></tt><tt>=Dat</tt> (935; 80%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (11; 1% of non-empty `PronType`): <em>bu, bunu, bunların, şu</em>
* `Ind` (6; 1% of non-empty `PronType`): <em>biri, hepsi, hepsini, çoğunu</em>
* `Int` (212; 18% of non-empty `PronType`): <em>hangi, ne, hangisi, hangileri, neyin, neyi, hangisinin, nerede</em>
* `Prs` (944; 80% of non-empty `PronType`): <em>bana, beni, benim</em>

### `AUX`

432 <tt><a href="tr_atis-pos-AUX.html">AUX</a></tt> tokens (99% of all `AUX` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `AUX` and `PronType` co-occurred: <tt><a href="tr_atis-feat-Tense.html">Tense</a></tt><tt>=Pres</tt> (432; 100%), <tt><a href="tr_atis-feat-Number.html">Number</a></tt><tt>=Sing</tt> (396; 92%), <tt><a href="tr_atis-feat-Person.html">Person</a></tt><tt>=3</tt> (285; 66%).

`AUX` tokens may have the following values of `PronType`:

* `Int` (432; 100% of non-empty `PronType`): <em>mı, misin, mu, misiniz, miyim, mü, mi, musunuz, mısın, mıyım</em>
* `EMPTY` (3): <em>değil</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>DET --[<tt><a href="tr_atis-dep-fixed.html">fixed</a></tt>]--> PRON</tt> (1; 100%).

