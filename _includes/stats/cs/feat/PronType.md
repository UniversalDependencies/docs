

--------------------------------------------------------------------------------

## Treebank Statistics (UD_Czech)

This feature is universal.
It occurs with 7 different values: `Dem`, `Ind`, `Int`, `Neg`, `Prs`, `Rel`, `Tot`.
Some words have combined values of the feature; 2 combinations have been observed: `Dem|Ind`, `Int|Rel`.

100361 tokens (7%) have a non-empty value of `PronType`.
842 types (1%) occur at least once with a non-empty value of `PronType`.
136 lemmas (0%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: [cs-pos/PRON]() (72419; 5% instances), [cs-pos/DET]() (27819; 2% instances), [cs-pos/ADV]() (123; 0% instances).

### `PRON`

72419 [cs-pos/PRON]() tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="Person.html">Person</a>=EMPTY</tt> (61150; 84%), <tt><a href="Reflex.html">Reflex</a>=EMPTY</tt> (46518; 64%), <tt><a href="Variant.html">Variant</a>=EMPTY</tt> (45238; 62%), <tt><a href="Gender.html">Gender</a>=EMPTY</tt> (38811; 54%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (12410; 17% of non-empty `PronType`): <em>to, tím, tom, tomu, toho, ten, ty, ti, ta, těch</em>
* `Dem,Ind` (197; 0% of non-empty `PronType`): <em>mnoho, tolik, málo, moc, několik, několika, mnoha, nemálo, mn, míň</em>
* `Ind` (1398; 2% of non-empty `PronType`): <em>něco, někdo, některé, někteří, někoho, cosi, něčeho, cokoli, něčím, někomu</em>
* `Int,Rel` (13689; 19% of non-empty `PronType`): <em>které, který, která, co, kteří, kdo, kterou, kterým, čím, kterých</em>
* `Neg` (1101; 2% of non-empty `PronType`): <em>nic, nikdo, nikoho, nikomu, ničím, žádný, žádná, ničem, ničemu, žádné</em>
* `Prs` (37160; 51% of non-empty `PronType`): <em>se, si, nás, je, nám, nich, mu, ji, ho, jim</em>
* `Rel` (2960; 4% of non-empty `PronType`): <em>což, nichž, jež, níž, němž, jehož, jenž, něhož, něž, jemuž</em>
* `Tot` (3504; 5% of non-empty `PronType`): <em>všechny, všech, vše, sám, všichni, všechno, sama, sami, všem, veškeré</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (112) occur only with one value of `PronType`.

### `DET`

27819 [cs-pos/DET]() tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="Gender[psor].html">Gender[psor]</a>=EMPTY</tt> (23487; 84%), <tt><a href="Reflex.html">Reflex</a>=EMPTY</tt> (23051; 83%), <tt><a href="Number[psor].html">Number[psor]</a>=EMPTY</tt> (18541; 67%), <tt><a href="Person.html">Person</a>=EMPTY</tt> (18541; 67%), <tt><a href="Number.html">Number</a>=Sing</tt> (14995; 54%), <tt><a href="Poss.html">Poss</a>=Yes</tt> (14046; 50%).

`DET` tokens may have the following values of `PronType`:

* `Dem` (8864; 32% of non-empty `PronType`): <em>této, tento, tohoto, tato, těchto, tyto, tomto, tuto, toto, té</em>
* `Dem,Ind` (1455; 5% of non-empty `PronType`): <em>několik, několika, mnoho, mnoha, málo, tolik, mála, moc, tolika, nemálo</em>
* `Ind` (2075; 7% of non-empty `PronType`): <em>některých, některé, někteří, nějaké, nějaký, nějakou, některá, jakékoli, nějaká, některým</em>
* `Int,Rel` (634; 2% of non-empty `PronType`): <em>jaké, kolik, jaký, jakou, které, jakým, který, jaká, jakých, jakém</em>
* `Neg` (745; 3% of non-empty `PronType`): <em>žádné, žádný, žádná, žádnou, žádném, žádným, žádného, žádnému, žádných, žádnými</em>
* `Prs` (13398; 48% of non-empty `PronType`): <em>jeho, jejich, své, její, svou, svého, svých, svůj, naše, svým</em>
* `Rel` (648; 2% of non-empty `PronType`): <em>jejichž, jehož, jejíž, jejímž, jejíchž, jejíhož, jejímiž, jejímuž</em>

`PronType` seems to be **lexical feature** of `DET`. 100% lemmas (55) occur only with one value of `PronType`.

### `ADV`

123 [cs-pos/ADV]() tokens (0% of all `ADV` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADV` and `PronType` co-occurred: <tt><a href="Degree.html">Degree</a>=EMPTY</tt> (123; 100%), <tt><a href="Negative.html">Negative</a>=EMPTY</tt> (123; 100%).

`ADV` tokens may have the following values of `PronType`:

* `Dem,Ind` (120; 98% of non-empty `PronType`): <em>několikrát, mnohokrát, vícekrát, tolikrát, kolikrát, mnohokráte, bezpočtukrát, nesčíslněkrát, několikráte</em>
* `Int,Rel` (3; 2% of non-empty `PronType`): <em>kolikrát</em>
* `EMPTY` (79874): <em>tak, už, také, jak, včera, ještě, již, tedy, dnes, pak</em>

<table>
  <tr><th>Paradigm <i>kolikrát</i></th><th><tt>Dem,Ind</tt></th><th><tt>Int,Rel</tt></th></tr>
  <tr><td><tt></tt></td><td><em>kolikrát</em></td><td><em>kolikrát</em></td></tr>
</table>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<a href="../dep/conj.html">conj</a>]--> PRON</tt> (104; 79%),
<tt>DET --[<a href="../dep/conj.html">conj</a>]--> PRON</tt> (53; 100%),
<tt>PRON --[<a href="../dep/cc.html">cc</a>]--> PRON</tt> (10; 91%),
<tt>PRON --[<a href="../dep/appos.html">appos</a>]--> PRON</tt> (9; 69%),
<tt>PRON --[<a href="../dep/foreign.html">foreign</a>]--> PRON</tt> (4; 80%),
<tt>DET --[<a href="../dep/det:nummod.html">det:nummod</a>]--> DET</tt> (2; 100%).



--------------------------------------------------------------------------------

## Treebank Statistics (UD_Czech-CAC)

This feature is universal.
It occurs with 7 different values: `Dem`, `Ind`, `Int`, `Neg`, `Prs`, `Rel`, `Tot`.
Some words have combined values of the feature; 2 combinations have been observed: `Dem|Ind`, `Int|Rel`.

35523 tokens (7%) have a non-empty value of `PronType`.
621 types (1%) occur at least once with a non-empty value of `PronType`.
90 lemmas (0%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: [cs-pos/PRON]() (24389; 5% instances), [cs-pos/DET]() (11088; 2% instances), [cs-pos/ADV]() (46; 0% instances).

### `PRON`

24389 [cs-pos/PRON]() tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="Person.html">Person</a>=EMPTY</tt> (20889; 86%), <tt><a href="Reflex.html">Reflex</a>=EMPTY</tt> (15323; 63%), <tt><a href="Variant.html">Variant</a>=EMPTY</tt> (15194; 62%), <tt><a href="Gender.html">Gender</a>=EMPTY</tt> (13520; 55%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (4022; 16% of non-empty `PronType`): <em>to, tím, tomu, tom, toho, ten, ty, ti, těch, ta</em>
* `Dem,Ind` (59; 0% of non-empty `PronType`): <em>mnoho, tolik, několik, mnoha, málo, mála, několika</em>
* `Ind` (351; 1% of non-empty `PronType`): <em>něco, někdo, některé, někteří, někomu, něčím, některých, některá, některý, něčeho</em>
* `Int,Rel` (4343; 18% of non-empty `PronType`): <em>které, který, která, co, kteří, kterou, kdo, kterých, kterým, čím</em>
* `Neg` (184; 1% of non-empty `PronType`): <em>nic, nikdo, nikoho, ničím, nikomu, žádná, ničeho, ničemu, žádné, žádný</em>
* `Prs` (12556; 51% of non-empty `PronType`): <em>se, si, je, nás, nám, nich, ji, jim, mu, sebe</em>
* `Rel` (1180; 5% of non-empty `PronType`): <em>jež, nichž, což, níž, němž, jimiž, něhož, čímž, jenž, nimiž</em>
* `Tot` (1694; 7% of non-empty `PronType`): <em>všech, všechny, všechno, sám, všichni, všem, sama, sami, vše, veškeré</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (74) occur only with one value of `PronType`.

### `DET`

11088 [cs-pos/DET]() tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="Reflex.html">Reflex</a>=EMPTY</tt> (9695; 87%), <tt><a href="Gender[psor].html">Gender[psor]</a>=EMPTY</tt> (9529; 86%), <tt><a href="Number[psor].html">Number[psor]</a>=EMPTY</tt> (7202; 65%), <tt><a href="Person.html">Person</a>=EMPTY</tt> (7202; 65%), <tt><a href="Poss.html">Poss</a>=EMPTY</tt> (5809; 52%), <tt><a href="Number.html">Number</a>=Sing</tt> (5772; 52%).

`DET` tokens may have the following values of `PronType`:

* `Dem` (4116; 37% of non-empty `PronType`): <em>této, těchto, tyto, tento, tohoto, tato, tomto, tuto, takové, toto</em>
* `Dem,Ind` (521; 5% of non-empty `PronType`): <em>několik, mnoho, několika, mnoha, málo, tolik, mála, tolika, Nemálo, kdovíkolik</em>
* `Ind` (800; 7% of non-empty `PronType`): <em>některých, některé, nějaké, někteří, nějakou, některá, některým, jakékoli, jakékoliv, jakési</em>
* `Int,Rel` (251; 2% of non-empty `PronType`): <em>jaké, kolik, které, jakým, jaký, jakou, jakých, jakém, jaká, kterou</em>
* `Neg` (121; 1% of non-empty `PronType`): <em>žádné, žádný, žádná, žádnou, žádném, žádných, žádným, žádného, žádnému, ničím</em>
* `Prs` (4940; 45% of non-empty `PronType`): <em>jejich, jeho, své, její, naší, svou, naše, našeho, svých, našich</em>
* `Rel` (339; 3% of non-empty `PronType`): <em>jehož, jejichž, jejíž, jejímž, jejímuž, jejíchž</em>

`PronType` seems to be **lexical feature** of `DET`. 100% lemmas (44) occur only with one value of `PronType`.

### `ADV`

46 [cs-pos/ADV]() tokens (0% of all `ADV` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADV` and `PronType` co-occurred: <tt><a href="Negative.html">Negative</a>=EMPTY</tt> (46; 100%), <tt><a href="Degree.html">Degree</a>=EMPTY</tt> (46; 100%).

`ADV` tokens may have the following values of `PronType`:

* `Dem,Ind` (44; 96% of non-empty `PronType`): <em>několikrát, mnohokrát, kolikrát, tolikrát, nesčíslněkrát, několikráte, vícekrát</em>
* `Int,Rel` (2; 4% of non-empty `PronType`): <em>kolikrát</em>
* `EMPTY` (27444): <em>tak, také, jak, již, už, ještě, pak, kde, tedy, velmi</em>

<table>
  <tr><th>Paradigm <i>kolikrát</i></th><th><tt>Dem,Ind</tt></th><th><tt>Int,Rel</tt></th></tr>
  <tr><td><tt></tt></td><td><em>kolikrát</em></td><td><em>kolikrát</em></td></tr>
</table>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<a href="../dep/conj.html">conj</a>]--> PRON</tt> (38; 78%),
<tt>DET --[<a href="../dep/conj.html">conj</a>]--> PRON</tt> (28; 93%),
<tt>PRON --[<a href="../dep/cc.html">cc</a>]--> PRON</tt> (5; 71%),
<tt>PRON --[<a href="../dep/appos.html">appos</a>]--> PRON</tt> (3; 100%),
<tt>DET --[<a href="../dep/cc.html">cc</a>]--> PRON</tt> (2; 100%),
<tt>PRON --[<a href="../dep/advcl.html">advcl</a>]--> PRON</tt> (1; 100%),
<tt>DET --[<a href="../dep/nmod.html">nmod</a>]--> PRON</tt> (1; 100%).



--------------------------------------------------------------------------------

## Treebank Statistics (UD_Czech-CLTT)

This feature is universal.
It occurs with 7 different values: `Dem`, `Ind`, `Int`, `Neg`, `Prs`, `Rel`, `Tot`.
Some words have combined values of the feature; 2 combinations have been observed: `Dem|Ind`, `Int|Rel`.

1807 tokens (5%) have a non-empty value of `PronType`.
128 types (3%) occur at least once with a non-empty value of `PronType`.
20 lemmas (1%) occur at least once with a non-empty value of `PronType`.
The feature is used with 2 part-of-speech tags: [cs-pos/PRON]() (1212; 3% instances), [cs-pos/DET]() (595; 2% instances).

### `PRON`

1212 [cs-pos/PRON]() tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="Variant.html">Variant</a>=EMPTY</tt> (743; 61%), <tt><a href="Reflex.html">Reflex</a>=EMPTY</tt> (737; 61%), <tt><a href="Case.html">Case</a>=Acc</tt> (607; 50%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (85; 7% of non-empty `PronType`): <em>to, tím, toho, tomu, tom, ty, tímto, t, takové, tato</em>
* `Dem,Ind` (1; 0% of non-empty `PronType`): <em>několika</em>
* `Ind` (2; 0% of non-empty `PronType`): <em>kterýkoli, některý</em>
* `Int,Rel` (452; 37% of non-empty `PronType`): <em>které, která, který, kterých, kterým, kterém, kterému, kterou, kterého, jakém</em>
* `Neg` (4; 0% of non-empty `PronType`): <em>žádném, žádnou, žádná</em>
* `Prs` (546; 45% of non-empty `PronType`): <em>se, jej, je, jim, jí, ji, nich, ní, sobě, jimi</em>
* `Rel` (73; 6% of non-empty `PronType`): <em>nichž, němž, němuž, jimiž, jehož, níž, nějž, jenž, jež, jímž</em>
* `Tot` (49; 4% of non-empty `PronType`): <em>všech, všechny, veškeré, všechna, sama, veškerého, veškerých, veškerými, samo, samy</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (15) occur only with one value of `PronType`.

### `DET`

595 [cs-pos/DET]() tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="Gender[psor].html">Gender[psor]</a>=EMPTY</tt> (484; 81%), <tt><a href="Person.html">Person</a>=EMPTY</tt> (379; 64%), <tt><a href="Number[psor].html">Number[psor]</a>=EMPTY</tt> (379; 64%), <tt><a href="Poss.html">Poss</a>=EMPTY</tt> (355; 60%), <tt><a href="Number.html">Number</a>=Sing</tt> (306; 51%).

`DET` tokens may have the following values of `PronType`:

* `Dem` (341; 57% of non-empty `PronType`): <em>této, tohoto, těchto, tyto, tato, tento, tomto, tuto, toto, tímto</em>
* `Dem,Ind` (1; 0% of non-empty `PronType`): <em>několika</em>
* `Ind` (8; 1% of non-empty `PronType`): <em>NĚKTERÝCH, některá, některé, jakékoli, jakýchkoliv</em>
* `Int,Rel` (3; 1% of non-empty `PronType`): <em>jakou, jaké, kterým</em>
* `Neg` (2; 0% of non-empty `PronType`): <em>žádná, žádný</em>
* `Prs` (219; 37% of non-empty `PronType`): <em>jejich, jeho, její, své, jejího, jejím, svého, svých, svůj, svoji</em>
* `Rel` (21; 4% of non-empty `PronType`): <em>jejichž, jehož, jejímž, jejíž</em>

`PronType` seems to be **lexical feature** of `DET`. 100% lemmas (13) occur only with one value of `PronType`.

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<a href="../dep/conj.html">conj</a>]--> PRON</tt> (4; 100%),
<tt>PRON --[<a href="../dep/cc.html">cc</a>]--> PRON</tt> (1; 100%).

