---
layout: base
title:  'Statistics of NumType in UD_Galician-PUD'
udver: '2'
---

## Treebank Statistics: UD_Galician-PUD: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

487 tokens (2%) have a non-empty value of `NumType`.
251 types (4%) occur at least once with a non-empty value of `NumType`.
221 lemmas (5%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="gl_pud-pos-NUM.html">NUM</a></tt> (487; 2% instances).

### `NUM`

487 <tt><a href="gl_pud-pos-NUM.html">NUM</a></tt> tokens (97% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (422; 87% of non-empty `NumType`): <em>dous, tres, dúas, 1, catro, seis, 10, 3, dez, 100</em>
* `Ord` (65; 13% of non-empty `NumType`): <em>primeira, primeiro, segunda, primeiros, I, segundo, terceiro, III, IV, primeiras</em>
* `EMPTY` (15): <em>III, vinte, 1,4, 1,5, 5,7, II, XIV, cinco, dous, oito</em>

<table>
  <tr><th>Paradigm <i>iii</i></th><th><tt>Card</tt></th><th><tt>Ord</tt></th></tr>
  <tr><td><tt></tt></td><td><em>III</em></td><td><em>III</em></td></tr>
</table>

`NumType` seems to be **lexical feature** of `NUM`. 98% lemmas (217) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="gl_pud-dep-conj.html">conj</a></tt>]--> NUM</tt> (8; 100%),
<tt>NUM --[<tt><a href="gl_pud-dep-compound.html">compound</a></tt>]--> NUM</tt> (5; 100%),
<tt>NUM --[<tt><a href="gl_pud-dep-nmod.html">nmod</a></tt>]--> NUM</tt> (2; 100%),
<tt>NUM --[<tt><a href="gl_pud-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (2; 100%).

