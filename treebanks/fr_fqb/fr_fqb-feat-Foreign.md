---
layout: base
title:  'Statistics of Foreign in UD_French-FQB'
udver: '2'
---

## Treebank Statistics: UD_French-FQB: Features: `Foreign`

This feature is universal.
It occurs with 1 different values: `Yes`.

244 tokens (1%) have a non-empty value of `Foreign`.
178 types (4%) occur at least once with a non-empty value of `Foreign`.
178 lemmas (5%) occur at least once with a non-empty value of `Foreign`.
The feature is used with 2 part-of-speech tags: <tt><a href="fr_fqb-pos-X.html">X</a></tt> (243; 1% instances), <tt><a href="fr_fqb-pos-NUM.html">NUM</a></tt> (1; 0% instances).

### `X`

243 <tt><a href="fr_fqb-pos-X.html">X</a></tt> tokens (100% of all `X` tokens) have a non-empty value of `Foreign`.

`X` tokens may have the following values of `Foreign`:

* `Yes` (243; 100% of non-empty `Foreign`): <em>the, of, 's, and, caliente, is, west, hazmat, in, to</em>
* `EMPTY` (1): <em>Ricains</em>

`Foreign` seems to be **lexical feature** of `X`. 100% lemmas (177) occur only with one value of `Foreign`.

### `NUM`

1 <tt><a href="fr_fqb-pos-NUM.html">NUM</a></tt> tokens (1% of all `NUM` tokens) have a non-empty value of `Foreign`.

The most frequent other feature values with which `NUM` and `Foreign` co-occurred: <tt><a href="fr_fqb-feat-NumType.html">NumType</a></tt><tt>=Card</tt> (1; 100%).

`NUM` tokens may have the following values of `Foreign`:

* `Yes` (1; 100% of non-empty `Foreign`): <em>1</em>
* `EMPTY` (114): <em>deux, 1994, 1993, 1989, 1990, 1991, 1992, 1988, 10, 1929</em>

## Relations with Agreement in `Foreign`

The 10 most frequent relations where parent and child node agree in `Foreign`:
<tt>X --[<tt><a href="fr_fqb-dep-flat-foreign.html">flat:foreign</a></tt>]--> X</tt> (131; 100%),
<tt>NUM --[<tt><a href="fr_fqb-dep-dep.html">dep</a></tt>]--> X</tt> (3; 100%).

