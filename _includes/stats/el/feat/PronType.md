

--------------------------------------------------------------------------------

## Treebank Statistics (UD_Greek)

This feature is universal.
It occurs with 6 different values: `Art`, `Dem`, `Ind`, `Int`, `Prs`, `Rel`.

12240 tokens (21%) have a non-empty value of `PronType`.
169 types (2%) occur at least once with a non-empty value of `PronType`.
32 lemmas (1%) occur at least once with a non-empty value of `PronType`.
The feature is used with 2 part-of-speech tags: [el-pos/DET]() (9465; 16% instances), [el-pos/PRON]() (2775; 5% instances).

### `DET`

9465 [el-pos/DET]() tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="Definite.html">Definite</a>=Def</tt> (9017; 95%), <tt><a href="Number.html">Number</a>=Sing</tt> (7192; 76%), <tt><a href="Gender.html">Gender</a>=Fem</tt> (5108; 54%).

`DET` tokens may have the following values of `PronType`:

* `Art` (9465; 100% of non-empty `PronType`): <em>την, το, της, η, του, των, ο, οι, τα, τη</em>

### `PRON`

2775 [el-pos/PRON]() tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="Person.html">Person</a>=3</tt> (2430; 88%), <tt><a href="Number.html">Number</a>=Sing</tt> (1652; 60%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (599; 22% of non-empty `PronType`): <em>αυτό, αυτή, αυτά, αυτές, αυτής, αυτού, αυτών, αυτόν, αυτήν, αυτούς</em>
* `Ind` (293; 11% of non-empty `PronType`): <em>κάθε, κάτι, άλλες, άλλη, άλλων, άλλα, κανείς, κάποια, άλλο, καμία</em>
* `Int` (19; 1% of non-empty `PronType`): <em>τι, ποιο, Τίνος, ποιες, ποιος</em>
* `Prs` (890; 32% of non-empty `PronType`): <em>τους, μας, του, της, μου, σας, το, εμείς, την, τον</em>
* `Rel` (974; 35% of non-empty `PronType`): <em>που, οποία, οποίο, οποίες, οποίος, οποίων, οποίοι, όσα, οποίας, οποίους</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (30) occur only with one value of `PronType`.

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<a href="../dep/conj.html">conj</a>]--> PRON</tt> (1; 100%).

