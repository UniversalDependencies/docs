---
layout: base
title:  'Statistics of NumType in UD_Italian-KIParlaForest'
udver: '2'
---

## Treebank Statistics: UD_Italian-KIParlaForest: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

173 tokens (1%) have a non-empty value of `NumType`.
50 types (2%) occur at least once with a non-empty value of `NumType`.
47 lemmas (2%) occur at least once with a non-empty value of `NumType`.
The feature is used with 3 part-of-speech tags: <tt><a href="it_kiparlaforest-pos-NUM.html">NUM</a></tt> (145; 1% instances), <tt><a href="it_kiparlaforest-pos-ADJ.html">ADJ</a></tt> (27; 0% instances), <tt><a href="it_kiparlaforest-pos-NOUN.html">NOUN</a></tt> (1; 0% instances).

### `NUM`

145 <tt><a href="it_kiparlaforest-pos-NUM.html">NUM</a></tt> tokens (84% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="it_kiparlaforest-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (126; 87%), <tt><a href="it_kiparlaforest-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (126; 87%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (125; 86% of non-empty `NumType`): <em>due, quattro, tre, cinque, quattordici, sette, dieci, mille, dodici, nove</em>
* `Ord` (20; 14% of non-empty `NumType`): <em>prima, primi, primo, secondo, terza</em>
* `EMPTY` (27): <em>seicento, sedici, trecentoventotto, cinquanta, duecento, duemiladiciotto, milleseicento, ottocento, ottocentocinquantatre, prima</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (40) occur only with one value of `NumType`.

### `ADJ`

27 <tt><a href="it_kiparlaforest-pos-ADJ.html">ADJ</a></tt> tokens (3% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="it_kiparlaforest-feat-Number.html">Number</a></tt><tt>=Sing</tt> (23; 85%), <tt><a href="it_kiparlaforest-feat-Gender.html">Gender</a></tt><tt>=Fem</tt> (14; 52%).

`ADJ` tokens may have the following values of `NumType`:

* `Card` (1; 4% of non-empty `NumType`): <em>mezz'</em>
* `Ord` (26; 96% of non-empty `NumType`): <em>prima, seconda, primo, terzo, i, ottavo, primi, quarti, ultima, ultimi</em>
* `EMPTY` (920): <em>esatto, araba, arabo, grande, mia, miei, piccola, difficile, proto, stessa</em>

### `NOUN`

1 <tt><a href="it_kiparlaforest-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NOUN` and `NumType` co-occurred: <tt><a href="it_kiparlaforest-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (1; 100%), <tt><a href="it_kiparlaforest-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (1; 100%).

`NOUN` tokens may have the following values of `NumType`:

* `Card` (1; 100% of non-empty `NumType`): <em>enne</em>
* `EMPTY` (2667): <em>tipo, lingua, casa, cosa, arabo, parte, realtà, lingue, centro, città</em>

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="it_kiparlaforest-dep-conj.html">conj</a></tt>]--> NUM</tt> (8; 100%),
<tt>NUM --[<tt><a href="it_kiparlaforest-dep-obl.html">obl</a></tt>]--> NUM</tt> (1; 100%).

