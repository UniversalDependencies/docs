---
layout: base
title:  'Statistics of NumType in UD_Abkhaz-AbNC'
udver: '2'
---

## Treebank Statistics: UD_Abkhaz-AbNC: Features: `NumType`

This feature is universal.
It occurs with 3 different values: `Card`, `Ord`, `Range`.

60 tokens (0%) have a non-empty value of `NumType`.
45 types (1%) occur at least once with a non-empty value of `NumType`.
28 lemmas (1%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="ab_abnc-pos-NUM.html">NUM</a></tt> (60; 0% instances).

### `NUM`

60 <tt><a href="ab_abnc-pos-NUM.html">NUM</a></tt> tokens (95% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="ab_abnc-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (53; 88%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (53; 88% of non-empty `NumType`): <em>аҩбагьы, ҩыџьа, хҩык, хәҩык, ҳҩыџьагь, 12, 1931, 21, 400, акы</em>
* `Ord` (6; 10% of non-empty `NumType`): <em>аҩбатәи, 33-тәи, 35-тәи, Аҧшьбатәи</em>
* `Range` (1; 2% of non-empty `NumType`): <em>Знык-ҩынтә</em>
* `EMPTY` (3): <em>Ҩажәи, ҩынтә, ҩынҩажәантә</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (28) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="ab_abnc-dep-conj.html">conj</a></tt>]--> NUM</tt> (1; 100%),
<tt>NUM --[<tt><a href="ab_abnc-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (1; 100%).

