

--------------------------------------------------------------------------------

## Treebank Statistics (UD_Romanian)

This feature is language-specific.
It occurs with 1 different values: `Yes`.

44 tokens (0%) have a non-empty value of `Abbr`.
33 types (1%) occur at least once with a non-empty value of `Abbr`.
34 lemmas (1%) occur at least once with a non-empty value of `Abbr`.
The feature is used with 3 part-of-speech tags: [ro-pos/NOUN]() (38; 0% instances), [ro-pos/ADV]() (3; 0% instances), [ro-pos/X]() (3; 0% instances).

### `NOUN`

38 [ro-pos/NOUN]() tokens (1% of all `NOUN` tokens) have a non-empty value of `Abbr`.

The most frequent other feature values with which `NOUN` and `Abbr` co-occurred: <tt><a href="Gender.html">Gender</a>=EMPTY</tt> (38; 100%), <tt><a href="Definite.html">Definite</a>=EMPTY</tt> (38; 100%), <tt><a href="Number.html">Number</a>=EMPTY</tt> (38; 100%), <tt><a href="Case.html">Case</a>=EMPTY</tt> (38; 100%).

`NOUN` tokens may have the following values of `Abbr`:

* `Yes` (38; 100% of non-empty `Abbr`): <em>C, CDR, F., PD, PIB, PNL, a, km, Al, CUG</em>

`Abbr` seems to be **lexical feature** of `NOUN`. 100% lemmas (30) occur only with one value of `Abbr`.

### `ADV`

3 [ro-pos/ADV]() tokens (1% of all `ADV` tokens) have a non-empty value of `Abbr`.

The most frequent other feature values with which `ADV` and `Abbr` co-occurred: <tt><a href="Degree.html">Degree</a>=EMPTY</tt> (3; 100%), <tt><a href="PronType.html">PronType</a>=EMPTY</tt> (3; 100%).

`ADV` tokens may have the following values of `Abbr`:

* `Yes` (3; 100% of non-empty `Abbr`): <em>etc.</em>

### `X`

3 [ro-pos/X]() tokens (75% of all `X` tokens) have a non-empty value of `Abbr`.

`X` tokens may have the following values of `Abbr`:

* `Yes` (3; 100% of non-empty `Abbr`): <em>O405, Rev, mp</em>

## Relations with Agreement in `Abbr`

The 10 most frequent relations where parent and child node agree in `Abbr`:
<tt>NOUN --[<a href="../dep/conj.html">conj</a>]--> NOUN</tt> (3; 100%).

