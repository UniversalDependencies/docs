---
layout: base
title:  'Statistics of AdpType in UD_Sinhala-STB'
udver: '2'
---

## Treebank Statistics: UD_Sinhala-STB: Features: `AdpType`

This feature is language-specific.
It occurs with 2 different values: `Post`, `Prep`.

96 tokens (11%) have a non-empty value of `AdpType`.
21 types (4%) occur at least once with a non-empty value of `AdpType`.
22 lemmas (5%) occur at least once with a non-empty value of `AdpType`.
The feature is used with 2 part-of-speech tags: <tt><a href="si_stb-pos-PART.html">PART</a></tt> (76; 9% instances), <tt><a href="si_stb-pos-ADP.html">ADP</a></tt> (20; 2% instances).

### `PART`

76 <tt><a href="si_stb-pos-PART.html">PART</a></tt> tokens (82% of all `PART` tokens) have a non-empty value of `AdpType`.

`PART` tokens may have the following values of `AdpType`:

* `Post` (74; 97% of non-empty `AdpType`): <em>ය, ම, ද, ගැන, දී, ගේ, බව, යනු, ලෙස, සඳහා</em>
* `Prep` (2; 3% of non-empty `AdpType`): <em>නො</em>

`AdpType` seems to be **lexical feature** of `PART`. 100% lemmas (17) occur only with one value of `AdpType`.

### `ADP`

20 <tt><a href="si_stb-pos-ADP.html">ADP</a></tt> tokens (83% of all `ADP` tokens) have a non-empty value of `AdpType`.

`ADP` tokens may have the following values of `AdpType`:

* `Post` (20; 100% of non-empty `AdpType`): <em>පිළිබඳ, සඳහා, තුළ, බව, ලෙස, වෙනුවෙන්, ගැන, දී, වැනි, සේ</em>

`AdpType` seems to be **lexical feature** of `ADP`. 100% lemmas (10) occur only with one value of `AdpType`.

