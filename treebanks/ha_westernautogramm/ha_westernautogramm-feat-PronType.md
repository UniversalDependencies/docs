---
layout: base
title:  'Statistics of PronType in UD_Hausa-WesternAutogramm'
udver: '2'
---

## Treebank Statistics: UD_Hausa-WesternAutogramm: Features: `PronType`

This feature is universal.
It occurs with 5 different values: `Dem`, `Ind`, `Prs`, `Rel`, `Tot`.

357 tokens (3%) have a non-empty value of `PronType`.
40 types (2%) occur at least once with a non-empty value of `PronType`.
19 lemmas (2%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="ha_westernautogramm-pos-DET.html">DET</a></tt> (177; 1% instances), <tt><a href="ha_westernautogramm-pos-PRON.html">PRON</a></tt> (114; 1% instances), <tt><a href="ha_westernautogramm-pos-ADV.html">ADV</a></tt> (66; 0% instances).

### `DET`

177 <tt><a href="ha_westernautogramm-pos-DET.html">DET</a></tt> tokens (88% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="ha_westernautogramm-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (138; 78%), <tt><a href="ha_westernautogramm-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (125; 71%), <tt><a href="ha_westernautogramm-feat-Deixis.html">Deixis</a></tt><tt>=ProxH</tt> (99; 56%).

`DET` tokens may have the following values of `PronType`:

* `Dem` (113; 64% of non-empty `PronType`): <em>nan, wânnam, wa’ànnan, ga, wânga, wânnan, wa’ànga, can, wannàn</em>
* `Ind` (39; 22% of non-empty `PronType`): <em>wani, wasu, wata</em>
* `Tot` (25; 14% of non-empty `PronType`): <em>du', du’, dum, duk, dul, duw, duy, dus, dut, duz</em>
* `EMPTY` (24): <em>wata, koːdàwane, koːwacè, koːwanè, koːdàwanè, koːmiː, koːwàcè, waɗànnan</em>

### `PRON`

114 <tt><a href="ha_westernautogramm-pos-PRON.html">PRON</a></tt> tokens (14% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="ha_westernautogramm-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (114; 100%), <tt><a href="ha_westernautogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (98; 86%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (4; 4% of non-empty `PronType`): <em>wânnan, wa’ànnàn, wâncân</em>
* `Ind` (8; 7% of non-empty `PronType`): <em>wani, koːmiː, koːwanè</em>
* `Prs` (12; 11% of non-empty `PronType`): <em>naːtà, taːshì, taːsù, naːmù, naːsù, taːtà</em>
* `Rel` (84; 74% of non-empty `PronType`): <em>wandà, waddà, wa’àndà, indà, yaddà</em>
* `Tot` (6; 5% of non-empty `PronType`): <em>dus, dud, dum</em>
* `EMPTY` (720): <em>shiː, shi, ita, ta, tà, mutà, suː, su, shì, mai</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (18) occur only with one value of `PronType`.

### `ADV`

66 <tt><a href="ha_westernautogramm-pos-ADV.html">ADV</a></tt> tokens (11% of all `ADV` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADV` and `PronType` co-occurred: <tt><a href="ha_westernautogramm-feat-Deixis.html">Deixis</a></tt><tt>=ProxH</tt> (37; 56%).

`ADV` tokens may have the following values of `PronType`:

* `Dem` (63; 95% of non-empty `PronType`): <em>nan, can, nân, nam</em>
* `Tot` (3; 5% of non-empty `PronType`): <em>dus, duk</em>
* `EMPTY` (556): <em>sai, hakànan, indà, kullun, tàːre, dàban, kàɗannà, cikiː, ɗan, ƙwar̃ai</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>ADV --[<tt><a href="ha_westernautogramm-dep-conj.html">conj</a></tt>]--> ADV</tt> (1; 100%),
<tt>ADV --[<tt><a href="ha_westernautogramm-dep-fixed.html">fixed</a></tt>]--> ADV</tt> (1; 100%),
<tt>ADV --[<tt><a href="ha_westernautogramm-dep-parataxis.html">parataxis</a></tt>]--> ADV</tt> (1; 100%),
<tt>PRON --[<tt><a href="ha_westernautogramm-dep-conj.html">conj</a></tt>]--> PRON</tt> (1; 100%).

