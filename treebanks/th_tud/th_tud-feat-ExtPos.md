---
layout: base
title:  'Statistics of ExtPos in UD_Thai-TUD'
udver: '2'
---

## Treebank Statistics: UD_Thai-TUD: Features: `ExtPos`

This feature is language-specific.
It occurs with 8 different values: `ADJ`, `ADP`, `ADV`, `AUX`, `CCONJ`, `DET`, `PRON`, `SCONJ`.

401 tokens (1%) have a non-empty value of `ExtPos`.
98 types (2%) occur at least once with a non-empty value of `ExtPos`.
1 lemmas (0) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 10 part-of-speech tags: <tt><a href="th_tud-pos-SCONJ.html">SCONJ</a></tt> (130; 0% instances), <tt><a href="th_tud-pos-CCONJ.html">CCONJ</a></tt> (94; 0% instances), <tt><a href="th_tud-pos-ADV.html">ADV</a></tt> (85; 0% instances), <tt><a href="th_tud-pos-ADP.html">ADP</a></tt> (44; 0% instances), <tt><a href="th_tud-pos-AUX.html">AUX</a></tt> (18; 0% instances), <tt><a href="th_tud-pos-ADJ.html">ADJ</a></tt> (15; 0% instances), <tt><a href="th_tud-pos-PART.html">PART</a></tt> (9; 0% instances), <tt><a href="th_tud-pos-DET.html">DET</a></tt> (3; 0% instances), <tt><a href="th_tud-pos-PRON.html">PRON</a></tt> (2; 0% instances), <tt><a href="th_tud-pos-VERB.html">VERB</a></tt> (1; 0% instances).

### `SCONJ`

130 <tt><a href="th_tud-pos-SCONJ.html">SCONJ</a></tt> tokens (4% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

`SCONJ` tokens may have the following values of `ExtPos`:

* `SCONJ` (130; 100% of non-empty `ExtPos`): เนื่อง, หลัง, ไม่, ถึง, เป็น, ตัวอย่าง, แม้, ขณะ, นอก, ภาย

### `CCONJ`

94 <tt><a href="th_tud-pos-CCONJ.html">CCONJ</a></tt> tokens (4% of all `CCONJ` tokens) have a non-empty value of `ExtPos`.

`CCONJ` tokens may have the following values of `ExtPos`:

* `CCONJ` (94; 100% of non-empty `ExtPos`): อย่างไร, ขณะ, รวม, นอก, แต่, ดัง, ทั้ง, ใน, พร้อม, อย่าง

### `ADV`

85 <tt><a href="th_tud-pos-ADV.html">ADV</a></tt> tokens (1% of all `ADV` tokens) have a non-empty value of `ExtPos`.

`ADV` tokens may have the following values of `ExtPos`:

* `ADV` (85; 100% of non-empty `ExtPos`): ต่อ, ก็, แต่, ใน, ตลอด, ทรง, ทัน, มาก, เพียง, ค่อน

### `ADP`

44 <tt><a href="th_tud-pos-ADP.html">ADP</a></tt> tokens (1% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (44; 100% of non-empty `ExtPos`): จน, ตั้ง, ท่าม, โดย, พร้อม, ภาย, หลัง, เช่น, ให้, ของ

### `AUX`

18 <tt><a href="th_tud-pos-AUX.html">AUX</a></tt> tokens (0% of all `AUX` tokens) have a non-empty value of `ExtPos`.

`AUX` tokens may have the following values of `ExtPos`:

* `AUX` (18; 100% of non-empty `ExtPos`): ยัง, ควร, จะ, อาจ, คง, ที่, อยู่, เพิ่ง

### `ADJ`

15 <tt><a href="th_tud-pos-ADJ.html">ADJ</a></tt> tokens (1% of all `ADJ` tokens) have a non-empty value of `ExtPos`.

`ADJ` tokens may have the following values of `ExtPos`:

* `ADJ` (15; 100% of non-empty `ExtPos`): ต่อ, ทั่ว, เดียว, ขยัน, ถัด, ที่, เขียว, แวด

### `PART`

9 <tt><a href="th_tud-pos-PART.html">PART</a></tt> tokens (1% of all `PART` tokens) have a non-empty value of `ExtPos`.

`PART` tokens may have the following values of `ExtPos`:

* `ADP` (3; 33% of non-empty `ExtPos`): อาทิ
* `ADV` (3; 33% of non-empty `ExtPos`): นะ, เลย, แต่
* `SCONJ` (3; 33% of non-empty `ExtPos`): ไม่

### `DET`

3 <tt><a href="th_tud-pos-DET.html">DET</a></tt> tokens (0% of all `DET` tokens) have a non-empty value of `ExtPos`.

`DET` tokens may have the following values of `ExtPos`:

* `DET` (3; 100% of non-empty `ExtPos`): ดัง, นั่น, เหล่า

### `PRON`

2 <tt><a href="th_tud-pos-PRON.html">PRON</a></tt> tokens (0% of all `PRON` tokens) have a non-empty value of `ExtPos`.

`PRON` tokens may have the following values of `ExtPos`:

* `PRON` (2; 100% of non-empty `ExtPos`): ที่, ฝ่า

### `VERB`

1 <tt><a href="th_tud-pos-VERB.html">VERB</a></tt> tokens (0% of all `VERB` tokens) have a non-empty value of `ExtPos`.

`VERB` tokens may have the following values of `ExtPos`:

* `SCONJ` (1; 100% of non-empty `ExtPos`): ยก

