---
layout: base
title:  'Statistics of PronType in UD_Scottish_Gaelic-ARCOSG'
udver: '2'
---

## Treebank Statistics: UD_Scottish_Gaelic-ARCOSG: Features: `PronType`

This feature is universal but the values `Emp` are language-specific.
It occurs with 3 different values: `Emp`, `Int`, `Rel`.

1540 tokens (4%) have a non-empty value of `PronType`.
54 types (1%) occur at least once with a non-empty value of `PronType`.
39 lemmas (1%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="gd_arcosg-pos-PART.html">PART</a></tt> (1224; 3% instances), <tt><a href="gd_arcosg-pos-PRON.html">PRON</a></tt> (257; 1% instances), <tt><a href="gd_arcosg-pos-ADP.html">ADP</a></tt> (59; 0% instances).

### `PART`

1224 <tt><a href="gd_arcosg-pos-PART.html">PART</a></tt> tokens (29% of all `PART` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PART` and `PronType` co-occurred: <tt><a href="gd_arcosg-feat-PartType.html">PartType</a></tt><tt>=Vb</tt> (1224; 100%), <tt><a href="gd_arcosg-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (1180; 96%).

`PART` tokens may have the following values of `PronType`:

* `Int` (217; 18% of non-empty `PronType`): <em>an, a, am, 'm, na, ‘n</em>
* `Rel` (1007; 82% of non-empty `PronType`): <em>a, nach, na</em>
* `EMPTY` (2952): <em>a, a', gu, cha, air, gun, nach, chan, ag, do</em>

<table>
  <tr><th>Paradigm <i>a</i></th><th><tt>Int</tt></th><th><tt>Rel</tt></th></tr>
  <tr><td><tt><tt><a href="gd_arcosg-feat-Mood.html">Mood</a></tt><tt>=Int</tt></tt></td><td><em>a</em></td><td></td></tr>
  <tr><td><tt></tt></td><td></td><td><em>a</em></td></tr>
</table>

### `PRON`

257 <tt><a href="gd_arcosg-pos-PRON.html">PRON</a></tt> tokens (10% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="gd_arcosg-feat-Number.html">Number</a></tt><tt>=Sing</tt> (238; 93%), <tt><a href="gd_arcosg-feat-Person.html">Person</a></tt><tt>=3</tt> (191; 74%).

`PRON` tokens may have the following values of `PronType`:

* `Emp` (257; 100% of non-empty `PronType`): <em>esan, ise, mise, thusa, iadsan, sinne, tusa, mis', sibhse, thus'</em>
* `EMPTY` (2389): <em>e, iad, mi, i, sin, thu, fhèin, seo, sinn, dè</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (10) occur only with one value of `PronType`.

### `ADP`

59 <tt><a href="gd_arcosg-pos-ADP.html">ADP</a></tt> tokens (1% of all `ADP` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADP` and `PronType` co-occurred: <tt><a href="gd_arcosg-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (50; 85%), <tt><a href="gd_arcosg-feat-Number.html">Number</a></tt><tt>=Sing</tt> (40; 68%).

`ADP` tokens may have the following values of `PronType`:

* `Emp` (59; 100% of non-empty `PronType`): <em>agamsa, agadsa, dhòmhsa, acasan, agaibhse, dhaibhsan, dhuinne, dhutsa, aca-san, aige-san</em>
* `EMPTY` (5877): <em>air, ann, an, aig, gu, de, ri, a, le, do</em>

`PronType` seems to be **lexical feature** of `ADP`. 100% lemmas (22) occur only with one value of `PronType`.

