---
layout: base
title:  'Statistics of PartType in UD_Old_Irish-DipWBG'
udver: '2'
---

## Treebank Statistics: UD_Old_Irish-DipWBG: Features: `PartType`

This feature is language-specific.
It occurs with 3 different values: `Dct`, `Rel`, `Vb`.

34 tokens (8%) have a non-empty value of `PartType`.
16 types (7%) occur at least once with a non-empty value of `PartType`.
10 lemmas (6%) occur at least once with a non-empty value of `PartType`.
The feature is used with 1 part-of-speech tags: <tt><a href="sga_dipwbg-pos-PART.html">PART</a></tt> (34; 8% instances).

### `PART`

34 <tt><a href="sga_dipwbg-pos-PART.html">PART</a></tt> tokens (72% of all `PART` tokens) have a non-empty value of `PartType`.

The most frequent other feature values with which `PART` and `PartType` co-occurred: <tt><a href="sga_dipwbg-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (34; 100%), <tt><a href="sga_dipwbg-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (31; 91%).

`PART` tokens may have the following values of `PartType`:

* `Dct` (2; 6% of non-empty `PartType`): <em>hí, ni</em>
* `Rel` (3; 9% of non-empty `PartType`): <em>a</em>
* `Vb` (29; 85% of non-empty `PartType`): <em>no, do, as, nas, a, at, con, d, eter, int</em>

`PartType` seems to be **lexical feature** of `PART`. 100% lemmas (10) occur only with one value of `PartType`.

