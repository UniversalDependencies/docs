---
layout: base
title:  'Statistics of PronType in UD_Norwegian-NynorskLIA'
udver: '2'
---

## Treebank Statistics: UD_Norwegian-NynorskLIA: Features: `PronType`

This feature is universal.
It occurs with 9 different values: `Art`, `Dem`, `Ind`, `Int`, `Neg`, `Prs`, `Rcp`, `Rel`, `Tot`.
Some words have combined values of the feature; 4 combinations have been observed: `Art|Prs`, `Ind|Prs`, `Neg|Prs`, `Prs|Tot`.

10294 tokens (19%) have a non-empty value of `PronType`.
87 types (2%) occur at least once with a non-empty value of `PronType`.
55 lemmas (2%) occur at least once with a non-empty value of `PronType`.
The feature is used with 5 part-of-speech tags: <tt><a href="no_nynorsklia-pos-PRON.html">PRON</a></tt> (8377; 15% instances), <tt><a href="no_nynorsklia-pos-DET.html">DET</a></tt> (1913; 3% instances), <tt><a href="no_nynorsklia-pos-NOUN.html">NOUN</a></tt> (2; 0% instances), <tt><a href="no_nynorsklia-pos-ADV.html">ADV</a></tt> (1; 0% instances), <tt><a href="no_nynorsklia-pos-SCONJ.html">SCONJ</a></tt> (1; 0% instances).

### `PRON`

8377 <tt><a href="no_nynorsklia-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="no_nynorsklia-feat-Number.html">Number</a></tt><tt>=Sing</tt> (5905; 70%), <tt><a href="no_nynorsklia-feat-Animacy.html">Animacy</a></tt><tt>=EMPTY</tt> (5253; 63%), <tt><a href="no_nynorsklia-feat-Person.html">Person</a></tt><tt>=3</tt> (5124; 61%), <tt><a href="no_nynorsklia-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (4251; 51%).

`PRON` tokens may have the following values of `PronType`:

* `Art` (1; 0% of non-empty `PronType`): <em>ein</em>
* `Art,Prs` (131; 2% of non-empty `PronType`): <em>ein</em>
* `Dem` (7; 0% of non-empty `PronType`): <em>dei, det</em>
* `Ind,Prs` (128; 2% of non-empty `PronType`): <em>noko, nokon</em>
* `Int` (137; 2% of non-empty `PronType`): <em>kva, kven</em>
* `Neg` (8; 0% of non-empty `PronType`): <em>ingenting</em>
* `Neg,Prs` (7; 0% of non-empty `PronType`): <em>ingen</em>
* `Prs` (7377; 88% of non-empty `PronType`): <em>det, dei, eg, du, han, vi, den, der, ho, de</em>
* `Prs,Tot` (7; 0% of non-empty `PronType`): <em>alle</em>
* `Rcp` (5; 0% of non-empty `PronType`): <em>kvarandre, einannan</em>
* `Rel` (569; 7% of non-empty `PronType`): <em>som</em>

<table>
  <tr><th>Paradigm <i>det</i></th><th><tt>Prs</tt></th><th><tt>Dem</tt></th></tr>
  <tr><td><tt><tt><a href="no_nynorsklia-feat-Case.html">Case</a></tt><tt>=Acc</tt>|<tt><a href="no_nynorsklia-feat-Person.html">Person</a></tt><tt>=3</tt></tt></td><td><em>det</em></td><td></td></tr>
  <tr><td><tt><tt><a href="no_nynorsklia-feat-Case.html">Case</a></tt><tt>=Nom</tt>|<tt><a href="no_nynorsklia-feat-Person.html">Person</a></tt><tt>=3</tt></tt></td><td><em>det</em></td><td></td></tr>
  <tr><td><tt></tt></td><td></td><td><em>det</em></td></tr>
  <tr><td><tt><tt><a href="no_nynorsklia-feat-Person.html">Person</a></tt><tt>=3</tt></tt></td><td><em>det, det?</em></td><td></td></tr>
</table>

`PronType` seems to be **lexical feature** of `PRON`. 92% lemmas (36) occur only with one value of `PronType`.

### `DET`

1913 <tt><a href="no_nynorsklia-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="no_nynorsklia-feat-Number.html">Number</a></tt><tt>=Sing</tt> (1454; 76%).

`DET` tokens may have the following values of `PronType`:

* `Art` (662; 35% of non-empty `PronType`): <em>ein, ei, eit, eitt</em>
* `Art,Prs` (2; 0% of non-empty `PronType`): <em>ein</em>
* `Dem` (804; 42% of non-empty `PronType`): <em>den, dei, det, andre, sånn, anna, denne, same, desse, sånne</em>
* `Ind` (206; 11% of non-empty `PronType`): <em>noko, nokon, nokre, noka</em>
* `Ind,Prs` (4; 0% of non-empty `PronType`): <em>noko</em>
* `Int` (43; 2% of non-empty `PronType`): <em>kva</em>
* `Neg` (6; 0% of non-empty `PronType`): <em>ingen, inga</em>
* `Prs` (59; 3% of non-empty `PronType`): <em>sjølve, det, eigen, der, eige, han, mi, sånn, -atten, me</em>
* `Tot` (127; 7% of non-empty `PronType`): <em>alle, alt, kvar, kvart, begge, all</em>
* `EMPTY` (1): <em>nittenfemogtjue</em>

<table>
  <tr><th>Paradigm <i>ein</i></th><th><tt>Art,Prs</tt></th><th><tt>Art</tt></th></tr>
  <tr><td><tt><tt><a href="no_nynorsklia-feat-Animacy.html">Animacy</a></tt><tt>=Hum</tt>|<tt><a href="no_nynorsklia-feat-Case.html">Case</a></tt><tt>=Acc</tt></tt></td><td><em>ein</em></td><td></td></tr>
  <tr><td><tt><tt><a href="no_nynorsklia-feat-Gender.html">Gender</a></tt><tt>=Masc</tt></tt></td><td></td><td><em>ein</em></td></tr>
  <tr><td><tt><tt><a href="no_nynorsklia-feat-Gender.html">Gender</a></tt><tt>=Fem</tt></tt></td><td></td><td><em>ei</em></td></tr>
  <tr><td><tt><tt><a href="no_nynorsklia-feat-Gender.html">Gender</a></tt><tt>=Neut</tt></tt></td><td></td><td><em>eit, eitt</em></td></tr>
</table>

### `NOUN`

2 <tt><a href="no_nynorsklia-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `NOUN` and `PronType` co-occurred: <tt><a href="no_nynorsklia-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (2; 100%).

`NOUN` tokens may have the following values of `PronType`:

* `Dem` (1; 50% of non-empty `PronType`): <em>andre</em>
* `Prs` (1; 50% of non-empty `PronType`): <em>det</em>
* `EMPTY` (4860): <em>år, byen, del, folk, slags, tida, eksempel, tid, dag, øya</em>

### `ADV`

1 <tt><a href="no_nynorsklia-pos-ADV.html">ADV</a></tt> tokens (0% of all `ADV` tokens) have a non-empty value of `PronType`.

`ADV` tokens may have the following values of `PronType`:

* `Prs` (1; 100% of non-empty `PronType`): <em>sånt</em>
* `EMPTY` (4960): <em>så, da, nå, her, der, jo, vel, sånn, altså, berre</em>

### `SCONJ`

1 <tt><a href="no_nynorsklia-pos-SCONJ.html">SCONJ</a></tt> tokens (0% of all `SCONJ` tokens) have a non-empty value of `PronType`.

`SCONJ` tokens may have the following values of `PronType`:

* `Dem` (1; 100% of non-empty `PronType`): <em>anna</em>
* `EMPTY` (949): <em>at, når, om, til, viss, for, på, da, enn, med</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="no_nynorsklia-dep-reparandum.html">reparandum</a></tt>]--> PRON</tt> (251; 100%),
<tt>PRON --[<tt><a href="no_nynorsklia-dep-appos.html">appos</a></tt>]--> PRON</tt> (173; 99%),
<tt>PRON --[<tt><a href="no_nynorsklia-dep-expl.html">expl</a></tt>]--> PRON</tt> (90; 63%),
<tt>DET --[<tt><a href="no_nynorsklia-dep-reparandum.html">reparandum</a></tt>]--> DET</tt> (77; 99%),
<tt>PRON --[<tt><a href="no_nynorsklia-dep-nsubj.html">nsubj</a></tt>]--> PRON</tt> (44; 73%),
<tt>PRON --[<tt><a href="no_nynorsklia-dep-nmod.html">nmod</a></tt>]--> PRON</tt> (32; 74%),
<tt>PRON --[<tt><a href="no_nynorsklia-dep-conj.html">conj</a></tt>]--> PRON</tt> (12; 86%),
<tt>PRON --[<tt><a href="no_nynorsklia-dep-parataxis-deletion.html">parataxis:deletion</a></tt>]--> PRON</tt> (9; 64%),
<tt>PRON --[<tt><a href="no_nynorsklia-dep-acl-relcl.html">acl:relcl</a></tt>]--> PRON</tt> (1; 100%),
<tt>PRON --[<tt><a href="no_nynorsklia-dep-obj.html">obj</a></tt>]--> PRON</tt> (1; 100%).

