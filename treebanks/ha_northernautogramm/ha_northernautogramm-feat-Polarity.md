---
layout: base
title:  'Statistics of Polarity in UD_Hausa-NorthernAutogramm'
udver: '2'
---

## Treebank Statistics: UD_Hausa-NorthernAutogramm: Features: `Polarity`

This feature is universal.
It occurs with 1 different values: `Neg`.

75 tokens (2%) have a non-empty value of `Polarity`.
14 types (2%) occur at least once with a non-empty value of `Polarity`.
4 lemmas (1%) occur at least once with a non-empty value of `Polarity`.
The feature is used with 2 part-of-speech tags: <tt><a href="ha_northernautogramm-pos-PART.html">PART</a></tt> (50; 1% instances), <tt><a href="ha_northernautogramm-pos-AUX.html">AUX</a></tt> (25; 1% instances).

### `PART`

50 <tt><a href="ha_northernautogramm-pos-PART.html">PART</a></tt> tokens (36% of all `PART` tokens) have a non-empty value of `Polarity`.

The most frequent other feature values with which `PART` and `Polarity` co-occurred: <tt><a href="ha_northernautogramm-feat-Aspect.html">Aspect</a></tt><tt>=EMPTY</tt> (50; 100%).

`PART` tokens may have the following values of `Polarity`:

* `Neg` (50; 100% of non-empty `Polarity`): <em>ba, baːbù, bâː, bàː</em>
* `EMPTY` (89): <em>ta, gàː, kòː, dai, àkwai, kuma, hwa, dà, zâː, à~</em>

### `AUX`

25 <tt><a href="ha_northernautogramm-pos-AUX.html">AUX</a></tt> tokens (3% of all `AUX` tokens) have a non-empty value of `Polarity`.

The most frequent other feature values with which `AUX` and `Polarity` co-occurred: <tt><a href="ha_northernautogramm-feat-Aspect.html">Aspect</a></tt><tt>=PerfNeg</tt> (16; 64%), <tt><a href="ha_northernautogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (16; 64%), <tt><a href="ha_northernautogramm-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (13; 52%).

`AUX` tokens may have the following values of `Polarity`:

* `Neg` (25; 100% of non-empty `Polarity`): <em>bài, baːkà, baː'à, bàkà, bàkù, baːmù, bàn, bàtà, baːkù, bàsù</em>
* `EMPTY` (693): <em>yac, ankà, sunkà, yaː, shinàː, kaː, shì, nàː, à, ìn</em>

## Relations with Agreement in `Polarity`

The 10 most frequent relations where parent and child node agree in `Polarity`:
<tt>PART --[<tt><a href="ha_northernautogramm-dep-parataxis.html">parataxis</a></tt>]--> PART</tt> (3; 100%),
<tt>PART --[<tt><a href="ha_northernautogramm-dep-conj.html">conj</a></tt>]--> PART</tt> (2; 100%),
<tt>PART --[<tt><a href="ha_northernautogramm-dep-advmod.html">advmod</a></tt>]--> PART</tt> (1; 100%).

