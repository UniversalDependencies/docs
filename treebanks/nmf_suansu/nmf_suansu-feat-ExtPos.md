---
layout: base
title:  'Statistics of ExtPos in UD_Naga-Suansu'
udver: '2'
---

## Treebank Statistics: UD_Naga-Suansu: Features: `ExtPos`

This feature is language-specific.
It occurs with 2 different values: `ADV`, `VERB`.

12 tokens (0%) have a non-empty value of `ExtPos`.
2 types (0%) occur at least once with a non-empty value of `ExtPos`.
2 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 2 part-of-speech tags: <tt><a href="nmf_suansu-pos-INTJ.html">INTJ</a></tt> (10; 0% instances), <tt><a href="nmf_suansu-pos-NUM.html">NUM</a></tt> (2; 0% instances).

### `INTJ`

10 <tt><a href="nmf_suansu-pos-INTJ.html">INTJ</a></tt> tokens (12% of all `INTJ` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `INTJ` and `ExtPos` co-occurred: <tt><a href="nmf_suansu-feat-Polarity.html">Polarity</a></tt><tt>=Pos</tt> (10; 100%).

`INTJ` tokens may have the following values of `ExtPos`:

* `VERB` (10; 100% of non-empty `ExtPos`): <em>ay</em>

### `NUM`

2 <tt><a href="nmf_suansu-pos-NUM.html">NUM</a></tt> tokens (4% of all `NUM` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `NUM` and `ExtPos` co-occurred: <tt><a href="nmf_suansu-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (2; 100%), <tt><a href="nmf_suansu-feat-NumForm.html">NumForm</a></tt><tt>=Word</tt> (2; 100%), <tt><a href="nmf_suansu-feat-NumType.html">NumType</a></tt><tt>=Card</tt> (2; 100%).

`NUM` tokens may have the following values of `ExtPos`:

* `ADV` (2; 100% of non-empty `ExtPos`): <em>ska</em>

## Relations with Agreement in `ExtPos`

The 10 most frequent relations where parent and child node agree in `ExtPos`:
<tt>INTJ --[<tt><a href="nmf_suansu-dep-conj.html">conj</a></tt>]--> INTJ</tt> (1; 100%).

