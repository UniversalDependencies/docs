---
layout: base
title:  'Statistics of Poss in UD_Czech-CAC'
udver: '2'
---

## Treebank Statistics: UD_Czech-CAC: Features: `Poss`

This feature is universal.
It occurs with 1 different values: `Yes`.

6020 tokens (1%) have a non-empty value of `Poss`.
567 types (1%) occur at least once with a non-empty value of `Poss`.
312 lemmas (1%) occur at least once with a non-empty value of `Poss`.
The feature is used with 2 part-of-speech tags: [DET](cs_cac-pos-DET.html) (5371; 1% instances), [ADJ](cs_cac-pos-ADJ.html) (649; 0% instances).

### `DET`

5371 [DET](cs_cac-pos-DET.html) tokens (28% of all `DET` tokens) have a non-empty value of `Poss`.

The most frequent other feature values with which `DET` and `Poss` co-occurred: <tt><a href="Animacy.html">Animacy</a>=EMPTY</tt> (5105; 95%), <tt><a href="PronType.html">PronType</a>=Prs</tt> (5003; 93%).

`DET` tokens may have the following values of `Poss`:

* `Yes` (5371; 100% of non-empty `Poss`): jejich jeho své její naší svou naše našeho svých našich
* `EMPTY` (14074): to které který která tím této těchto tyto tom kteří

### `ADJ`

649 [ADJ](cs_cac-pos-ADJ.html) tokens (1% of all `ADJ` tokens) have a non-empty value of `Poss`.

The most frequent other feature values with which `ADJ` and `Poss` co-occurred: <tt><a href="Polarity.html">Polarity</a>=EMPTY</tt> (649; 100%), <tt><a href="Degree.html">Degree</a>=EMPTY</tt> (649; 100%), <tt><a href="Number.html">Number</a>=Sing</tt> (510; 79%), <tt><a href="Animacy.html">Animacy</a>=EMPTY</tt> (373; 57%).

`ADJ` tokens may have the following values of `Poss`:

* `Yes` (649; 100% of non-empty `Poss`): Karlovy Erbenových Erbenovy Fučíkova Mohorovičićovy Bohrův Fučíkovy Erbenova Erbenově Fučíkův
* `EMPTY` (74145): další pracovní první jednotlivých základní nové možno socialistické různých každý

`Poss` seems to be **lexical feature** of `ADJ`. 100% lemmas (304) occur only with one value of `Poss`.

## Relations with Agreement in `Poss`

The 10 most frequent relations where parent and child node agree in `Poss`:
<tt>ADJ --[<a href="../dep/conj.html">conj</a>]--> ADJ</tt> (20; 80%).

