---
layout: base
title:  'Statistics of NumForm in UD_Malayalam-UFAL'
udver: '2'
---

## Treebank Statistics: UD_Malayalam-UFAL: Features: `NumForm`

This feature is language-specific.
It occurs with 2 different values: `Digit`, `Word`.

42 tokens (2%) have a non-empty value of `NumForm`.
39 types (3%) occur at least once with a non-empty value of `NumForm`.
38 lemmas (3%) occur at least once with a non-empty value of `NumForm`.
The feature is used with 1 part-of-speech tags: <tt><a href="ml_ufal-pos-NUM.html">NUM</a></tt> (42; 2% instances).

### `NUM`

42 <tt><a href="ml_ufal-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumForm`.

The most frequent other feature values with which `NUM` and `NumForm` co-occurred: <tt><a href="ml_ufal-feat-NumType.html">NumType</a></tt><tt>=Card</tt> (41; 98%), <tt><a href="ml_ufal-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (24; 57%).

`NUM` tokens may have the following values of `NumForm`:

* `Digit` (24; 57% of non-empty `NumForm`): <em>1, 0,7-0,8, 12, 15.56, 156, 16, 18, 19.2, 1950, 2004</em>
* `Word` (18; 43% of non-empty `NumForm`): <em>മൂന്ന്, രണ്ട്, ആറ്, എട്ടിന്, ഏഴിന്, ഒന്ന്, തൊണ്ണൂറു, നാലു, നാല്, നാൽപ്പതു</em>

`NumForm` seems to be **lexical feature** of `NUM`. 100% lemmas (38) occur only with one value of `NumForm`.

## Relations with Agreement in `NumForm`

The 10 most frequent relations where parent and child node agree in `NumForm`:
<tt>NUM --[<tt><a href="ml_ufal-dep-flat.html">flat</a></tt>]--> NUM</tt> (1; 100%).

