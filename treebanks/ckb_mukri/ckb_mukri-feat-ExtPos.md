---
layout: base
title:  'Statistics of ExtPos in UD_Central_Kurdish-Mukri'
udver: '2'
---

## Treebank Statistics: UD_Central_Kurdish-Mukri: Features: `ExtPos`

This feature is language-specific.
It occurs with 1 different values: `ADP`.

59 tokens (8%) have a non-empty value of `ExtPos`.
17 types (6%) occur at least once with a non-empty value of `ExtPos`.
14 lemmas (7%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 1 part-of-speech tags: <tt><a href="ckb_mukri-pos-ADP.html">ADP</a></tt> (59; 8% instances).

### `ADP`

59 <tt><a href="ckb_mukri-pos-ADP.html">ADP</a></tt> tokens (98% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (59; 100% of non-empty `ExtPos`): <em>be, lē, le, l, we, b, bo, de, pē, d</em>

`ExtPos` seems to be **lexical feature** of `ADP`. 100% lemmas (14) occur only with one value of `ExtPos`.

