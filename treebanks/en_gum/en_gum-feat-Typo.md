---
layout: base
title:  'Statistics of Typo in UD_English-GUM'
udver: '2'
---

## Treebank Statistics: UD_English-GUM: Features: `Typo`

This feature is language-specific.
It occurs with 1 different values: `Yes`.

334 tokens (0%) have a non-empty value of `Typo`.
199 types (1%) occur at least once with a non-empty value of `Typo`.
166 lemmas (1%) occur at least once with a non-empty value of `Typo`.
The feature is used with 17 part-of-speech tags: <tt><a href="en_gum-pos-PUNCT.html">PUNCT</a></tt> (64; 0% instances), <tt><a href="en_gum-pos-VERB.html">VERB</a></tt> (59; 0% instances), <tt><a href="en_gum-pos-INTJ.html">INTJ</a></tt> (31; 0% instances), <tt><a href="en_gum-pos-AUX.html">AUX</a></tt> (25; 0% instances), <tt><a href="en_gum-pos-ADP.html">ADP</a></tt> (24; 0% instances), <tt><a href="en_gum-pos-ADV.html">ADV</a></tt> (24; 0% instances), <tt><a href="en_gum-pos-ADJ.html">ADJ</a></tt> (18; 0% instances), <tt><a href="en_gum-pos-NOUN.html">NOUN</a></tt> (18; 0% instances), <tt><a href="en_gum-pos-PRON.html">PRON</a></tt> (17; 0% instances), <tt><a href="en_gum-pos-DET.html">DET</a></tt> (15; 0% instances), <tt><a href="en_gum-pos-PART.html">PART</a></tt> (11; 0% instances), <tt><a href="en_gum-pos-SCONJ.html">SCONJ</a></tt> (9; 0% instances), <tt><a href="en_gum-pos-CCONJ.html">CCONJ</a></tt> (5; 0% instances), <tt><a href="en_gum-pos-X.html">X</a></tt> (5; 0% instances), <tt><a href="en_gum-pos-NUM.html">NUM</a></tt> (4; 0% instances), <tt><a href="en_gum-pos-PROPN.html">PROPN</a></tt> (4; 0% instances), <tt><a href="en_gum-pos-SYM.html">SYM</a></tt> (1; 0% instances).

### `PUNCT`

64 <tt><a href="en_gum-pos-PUNCT.html">PUNCT</a></tt> tokens (0% of all `PUNCT` tokens) have a non-empty value of `Typo`.

`PUNCT` tokens may have the following values of `Typo`:

* `Yes` (64; 100% of non-empty `Typo`): <em>—, –, ,, ., [, ], ", ), -, --</em>

`Typo` seems to be **lexical feature** of `PUNCT`. 100% lemmas (12) occur only with one value of `Typo`.

### `VERB`

59 <tt><a href="en_gum-pos-VERB.html">VERB</a></tt> tokens (0% of all `VERB` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `VERB` and `Typo` co-occurred: <tt><a href="en_gum-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (37; 63%), <tt><a href="en_gum-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (36; 61%), <tt><a href="en_gum-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (35; 59%).

`VERB` tokens may have the following values of `Typo`:

* `Yes` (59; 100% of non-empty `Typo`): <em>been, have, is, l-, 's, Keep, address, begun, breath, cause</em>

`Typo` seems to be **lexical feature** of `VERB`. 100% lemmas (41) occur only with one value of `Typo`.

### `INTJ`

31 <tt><a href="en_gum-pos-INTJ.html">INTJ</a></tt> tokens (6% of all `INTJ` tokens) have a non-empty value of `Typo`.

`INTJ` tokens may have the following values of `Typo`:

* `Yes` (31; 100% of non-empty `Typo`): <em>w-, duh, Th, like, th-, Woo, Y-, a-, alreet, alroot</em>

`Typo` seems to be **lexical feature** of `INTJ`. 100% lemmas (22) occur only with one value of `Typo`.

### `AUX`

25 <tt><a href="en_gum-pos-AUX.html">AUX</a></tt> tokens (0% of all `AUX` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `AUX` and `Typo` co-occurred: <tt><a href="en_gum-feat-VerbForm.html">VerbForm</a></tt><tt>=Fin</tt> (20; 80%), <tt><a href="en_gum-feat-Mood.html">Mood</a></tt><tt>=Ind</tt> (13; 52%), <tt><a href="en_gum-feat-Number.html">Number</a></tt><tt>=Sing</tt> (13; 52%).

`AUX` tokens may have the following values of `Typo`:

* `Yes` (25; 100% of non-empty `Typo`): <em>can, has, are, be, been, is, 'd, 'm, 's, 've</em>

### `ADP`

24 <tt><a href="en_gum-pos-ADP.html">ADP</a></tt> tokens (0% of all `ADP` tokens) have a non-empty value of `Typo`.

`ADP` tokens may have the following values of `Typo`:

* `Yes` (24; 100% of non-empty `Typo`): <em>of, on, in, to, with, a, as, at, fro, from</em>

`Typo` seems to be **lexical feature** of `ADP`. 100% lemmas (11) occur only with one value of `Typo`.

### `ADV`

24 <tt><a href="en_gum-pos-ADV.html">ADV</a></tt> tokens (0% of all `ADV` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `ADV` and `Typo` co-occurred: <tt><a href="en_gum-feat-Degree.html">Degree</a></tt><tt>=Pos</tt> (14; 58%).

`ADV` tokens may have the following values of `Typo`:

* `Yes` (24; 100% of non-empty `Typo`): <em>before, then, as, maybe, only, them, actually, around, awhile, commonly</em>

`Typo` seems to be **lexical feature** of `ADV`. 100% lemmas (22) occur only with one value of `Typo`.

### `ADJ`

18 <tt><a href="en_gum-pos-ADJ.html">ADJ</a></tt> tokens (0% of all `ADJ` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `ADJ` and `Typo` co-occurred: <tt><a href="en_gum-feat-Degree.html">Degree</a></tt><tt>=Pos</tt> (17; 94%).

`ADJ` tokens may have the following values of `Typo`:

* `Yes` (18; 100% of non-empty `Typo`): <em>I.=, Well-food, appropriate, authentic, correct, difficult, educative, first, least, non-Muslim</em>

`Typo` seems to be **lexical feature** of `ADJ`. 100% lemmas (18) occur only with one value of `Typo`.

### `NOUN`

18 <tt><a href="en_gum-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `NOUN` and `Typo` co-occurred: <tt><a href="en_gum-feat-Number.html">Number</a></tt><tt>=Sing</tt> (17; 94%).

`NOUN` tokens may have the following values of `Typo`:

* `Yes` (18; 100% of non-empty `Typo`): <em>hand, Color, F, app-, b-, bag, balls, break, cent, hum-</em>

`Typo` seems to be **lexical feature** of `NOUN`. 100% lemmas (17) occur only with one value of `Typo`.

### `PRON`

17 <tt><a href="en_gum-pos-PRON.html">PRON</a></tt> tokens (0% of all `PRON` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `PRON` and `Typo` co-occurred: <tt><a href="en_gum-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (17; 100%), <tt><a href="en_gum-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (17; 100%), <tt><a href="en_gum-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (17; 100%), <tt><a href="en_gum-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (16; 94%), <tt><a href="en_gum-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (15; 88%).

`PRON` tokens may have the following values of `Typo`:

* `Yes` (17; 100% of non-empty `Typo`): <em>the, a, Th-, a-, all, d-, what, that, which, ya</em>

### `DET`

15 <tt><a href="en_gum-pos-DET.html">DET</a></tt> tokens (0% of all `DET` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `DET` and `Typo` co-occurred: <tt><a href="en_gum-feat-PronType.html">PronType</a></tt><tt>=Art</tt> (10; 67%).

`DET` tokens may have the following values of `Typo`:

* `Yes` (15; 100% of non-empty `Typo`): <em>a, the, an, on, to, What, these, this</em>

### `PART`

11 <tt><a href="en_gum-pos-PART.html">PART</a></tt> tokens (0% of all `PART` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `PART` and `Typo` co-occurred: <tt><a href="en_gum-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (9; 82%).

`PART` tokens may have the following values of `Typo`:

* `Yes` (11; 100% of non-empty `Typo`): <em>s, 's, to, na, not, the</em>

### `SCONJ`

9 <tt><a href="en_gum-pos-SCONJ.html">SCONJ</a></tt> tokens (0% of all `SCONJ` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `SCONJ` and `Typo` co-occurred: <tt><a href="en_gum-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (8; 89%).

`SCONJ` tokens may have the following values of `Typo`:

* `Yes` (9; 100% of non-empty `Typo`): <em>cuz, UNLESS, before, despite, that, till, how</em>

### `CCONJ`

5 <tt><a href="en_gum-pos-CCONJ.html">CCONJ</a></tt> tokens (0% of all `CCONJ` tokens) have a non-empty value of `Typo`.

`CCONJ` tokens may have the following values of `Typo`:

* `Yes` (5; 100% of non-empty `Typo`): <em>and</em>

### `X`

5 <tt><a href="en_gum-pos-X.html">X</a></tt> tokens (2% of all `X` tokens) have a non-empty value of `Typo`.

`X` tokens may have the following values of `Typo`:

* `Yes` (5; 100% of non-empty `Typo`): <em>etc, N-, S-</em>

### `NUM`

4 <tt><a href="en_gum-pos-NUM.html">NUM</a></tt> tokens (0% of all `NUM` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `NUM` and `Typo` co-occurred: <tt><a href="en_gum-feat-NumType.html">NumType</a></tt><tt>=Card</tt> (4; 100%).

`NUM` tokens may have the following values of `Typo`:

* `Yes` (4; 100% of non-empty `Typo`): <em>2, 4, thr-, three</em>

### `PROPN`

4 <tt><a href="en_gum-pos-PROPN.html">PROPN</a></tt> tokens (0% of all `PROPN` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `PROPN` and `Typo` co-occurred: <tt><a href="en_gum-feat-Number.html">Number</a></tt><tt>=Sing</tt> (4; 100%).

`PROPN` tokens may have the following values of `Typo`:

* `Yes` (4; 100% of non-empty `Typo`): <em>F&AM, Chatnam, Hell</em>

### `SYM`

1 <tt><a href="en_gum-pos-SYM.html">SYM</a></tt> tokens (0% of all `SYM` tokens) have a non-empty value of `Typo`.

`SYM` tokens may have the following values of `Typo`:

* `Yes` (1; 100% of non-empty `Typo`): <em>/</em>

## Relations with Agreement in `Typo`

The 10 most frequent relations where parent and child node agree in `Typo`:
<tt>INTJ --[<tt><a href="en_gum-dep-punct.html">punct</a></tt>]--> PUNCT</tt> (8; 53%),
<tt>PRON --[<tt><a href="en_gum-dep-case.html">case</a></tt>]--> ADP</tt> (3; 75%),
<tt>ADV --[<tt><a href="en_gum-dep-advmod.html">advmod</a></tt>]--> ADV</tt> (2; 67%),
<tt>ADV --[<tt><a href="en_gum-dep-cc.html">cc</a></tt>]--> CCONJ</tt> (2; 100%),
<tt>ADV --[<tt><a href="en_gum-dep-goeswith.html">goeswith</a></tt>]--> NOUN</tt> (2; 100%),
<tt>ADJ --[<tt><a href="en_gum-dep-cc.html">cc</a></tt>]--> CCONJ</tt> (1; 100%),
<tt>ADJ --[<tt><a href="en_gum-dep-cc.html">cc</a></tt>]--> SYM</tt> (1; 100%),
<tt>ADP --[<tt><a href="en_gum-dep-punct.html">punct</a></tt>]--> PUNCT</tt> (1; 100%),
<tt>ADV --[<tt><a href="en_gum-dep-conj.html">conj</a></tt>]--> ADV</tt> (1; 100%),
<tt>ADV --[<tt><a href="en_gum-dep-cop.html">cop</a></tt>]--> AUX</tt> (1; 100%).

