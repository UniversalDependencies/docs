---
layout: base
title:  'Statistics of ExtPos in UD_Maltese-MUDT'
udver: '2'
---

## Treebank Statistics: UD_Maltese-MUDT: Features: `ExtPos`

This feature is language-specific.
It occurs with 5 different values: `ADP`, `ADV`, `CCONJ`, `PART`, `SCONJ`.

186 tokens (0%) have a non-empty value of `ExtPos`.
40 types (1%) occur at least once with a non-empty value of `ExtPos`.
1 lemmas (0) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 6 part-of-speech tags: <tt><a href="mt_mudt-pos-ADP.html">ADP</a></tt> (97; 0% instances), <tt><a href="mt_mudt-pos-ADV.html">ADV</a></tt> (30; 0% instances), <tt><a href="mt_mudt-pos-SCONJ.html">SCONJ</a></tt> (25; 0% instances), <tt><a href="mt_mudt-pos-PRON.html">PRON</a></tt> (16; 0% instances), <tt><a href="mt_mudt-pos-NOUN.html">NOUN</a></tt> (12; 0% instances), <tt><a href="mt_mudt-pos-CCONJ.html">CCONJ</a></tt> (6; 0% instances).

### `ADP`

97 <tt><a href="mt_mudt-pos-ADP.html">ADP</a></tt> tokens (2% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (5; 5% of non-empty `ExtPos`): <em>qrib, favur</em>
* `ADV` (1; 1% of non-empty `ExtPos`): <em>in</em>
* `SCONJ` (91; 94% of non-empty `ExtPos`): <em>wara, waqt, minkejja, peress, qabel, mingħajr, bla, minħabba, sa, dment</em>

### `ADV`

30 <tt><a href="mt_mudt-pos-ADV.html">ADV</a></tt> tokens (2% of all `ADV` tokens) have a non-empty value of `ExtPos`.

`ADV` tokens may have the following values of `ExtPos`:

* `ADP` (6; 20% of non-empty `ExtPos`): <em>flimkien</em>
* `ADV` (9; 30% of non-empty `ExtPos`): <em>x', kif</em>
* `CCONJ` (1; 3% of non-empty `ExtPos`): <em>kif</em>
* `PART` (1; 3% of non-empty `ExtPos`): <em>lanqas</em>
* `SCONJ` (13; 43% of non-empty `ExtPos`): <em>hekk, aktar</em>

### `SCONJ`

25 <tt><a href="mt_mudt-pos-SCONJ.html">SCONJ</a></tt> tokens (1% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

`SCONJ` tokens may have the following values of `ExtPos`:

* `SCONJ` (25; 100% of non-empty `ExtPos`): <em>filwaqt, li, Kull</em>

### `PRON`

16 <tt><a href="mt_mudt-pos-PRON.html">PRON</a></tt> tokens (1% of all `PRON` tokens) have a non-empty value of `ExtPos`.

`PRON` tokens may have the following values of `ExtPos`:

* `ADV` (2; 13% of non-empty `ExtPos`): <em>x'</em>
* `CCONJ` (11; 69% of non-empty `ExtPos`): <em>kif</em>
* `SCONJ` (3; 19% of non-empty `ExtPos`): <em>jien, huma</em>

### `NOUN`

12 <tt><a href="mt_mudt-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `ExtPos`.

`NOUN` tokens may have the following values of `ExtPos`:

* `ADP` (12; 100% of non-empty `ExtPos`): <em>permezz</em>

### `CCONJ`

6 <tt><a href="mt_mudt-pos-CCONJ.html">CCONJ</a></tt> tokens (0% of all `CCONJ` tokens) have a non-empty value of `ExtPos`.

`CCONJ` tokens may have the following values of `ExtPos`:

* `CCONJ` (6; 100% of non-empty `ExtPos`): <em>kif</em>

