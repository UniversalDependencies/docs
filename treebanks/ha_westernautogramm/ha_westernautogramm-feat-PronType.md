---
layout: base
title:  'Statistics of PronType in UD_Hausa-WesternAutogramm'
udver: '2'
---

## Treebank Statistics: UD_Hausa-WesternAutogramm: Features: `PronType`

This feature is universal.
It occurs with 4 different values: `Dem`, `Ind`, `Rel`, `Tot`.

267 tokens (2%) have a non-empty value of `PronType`.
21 types (1%) occur at least once with a non-empty value of `PronType`.
20 lemmas (2%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="ha_westernautogramm-pos-DET.html">DET</a></tt> (113; 1% instances), <tt><a href="ha_westernautogramm-pos-PRON.html">PRON</a></tt> (91; 1% instances), <tt><a href="ha_westernautogramm-pos-ADV.html">ADV</a></tt> (63; 0% instances).

### `DET`

113 <tt><a href="ha_westernautogramm-pos-DET.html">DET</a></tt> tokens (56% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="ha_westernautogramm-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (94; 83%), <tt><a href="ha_westernautogramm-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (78; 69%), <tt><a href="ha_westernautogramm-feat-Deixis.html">Deixis</a></tt><tt>=EMPTY</tt> (78; 69%).

`DET` tokens may have the following values of `PronType`:

* `Dem` (113; 100% of non-empty `PronType`): <em>nan, wânnam, wa’ànnan, ga, wânga, wânnan, wa’ànga, can, wannàn</em>
* `EMPTY` (88): <em>wani, wata, du', wasu, dum, du’, duk, dul, duw, duy</em>

### `PRON`

91 <tt><a href="ha_westernautogramm-pos-PRON.html">PRON</a></tt> tokens (11% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="ha_westernautogramm-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (91; 100%), <tt><a href="ha_westernautogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (75; 82%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (4; 4% of non-empty `PronType`): <em>wânnan, wa’ànnàn, wâncân</em>
* `Ind` (2; 2% of non-empty `PronType`): <em>koːmiː, koːwanè</em>
* `Rel` (84; 92% of non-empty `PronType`): <em>wandà, waddà, wa’àndà, indà, yaddà</em>
* `Tot` (1; 1% of non-empty `PronType`): <em>dum</em>
* `EMPTY` (742): <em>shiː, shi, ita, ta, tà, mutà, suː, su, shì, mai</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (11) occur only with one value of `PronType`.

### `ADV`

63 <tt><a href="ha_westernautogramm-pos-ADV.html">ADV</a></tt> tokens (10% of all `ADV` tokens) have a non-empty value of `PronType`.

`ADV` tokens may have the following values of `PronType`:

* `Dem` (63; 100% of non-empty `PronType`): <em>nan, can, nân, nam</em>
* `EMPTY` (559): <em>sai, hakànan, indà, kullun, tàːre, dàban, kàɗannà, cikiː, ɗan, ƙwar̃ai</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>ADV --[<tt><a href="ha_westernautogramm-dep-parataxis.html">parataxis</a></tt>]--> ADV</tt> (2; 100%),
<tt>ADV --[<tt><a href="ha_westernautogramm-dep-fixed.html">fixed</a></tt>]--> ADV</tt> (1; 100%),
<tt>PRON --[<tt><a href="ha_westernautogramm-dep-conj.html">conj</a></tt>]--> PRON</tt> (1; 100%).

