---
layout: base
title:  'Statistics of Voice in UD_Chinese-GSDSimp'
udver: '2'
---

## Treebank Statistics: UD_Chinese-GSDSimp: Features: `Voice`

This feature is universal.
It occurs with 2 different values: `Cau`, `Pass`.

918 tokens (1%) have a non-empty value of `Voice`.
135 types (1%) occur at least once with a non-empty value of `Voice`.
135 lemmas (1%) occur at least once with a non-empty value of `Voice`.
The feature is used with 3 part-of-speech tags: <tt><a href="zh_gsdsimp-pos-VERB.html">VERB</a></tt> (492; 0% instances), <tt><a href="zh_gsdsimp-pos-AUX.html">AUX</a></tt> (425; 0% instances), <tt><a href="zh_gsdsimp-pos-ADP.html">ADP</a></tt> (1; 0% instances).

### `VERB`

492 <tt><a href="zh_gsdsimp-pos-VERB.html">VERB</a></tt> tokens (3% of all `VERB` tokens) have a non-empty value of `Voice`.

`VERB` tokens may have the following values of `Voice`:

* `Cau` (492; 100% of non-empty `Voice`): 以、 使、 让、 使得、 令、 导致、 要求、 派、 派遣、 任命
* `EMPTY` (17727): 有、 在、 于、 为、 是、 由、 成、 到、 位、 开始

`Voice` seems to be **lexical feature** of `VERB`. 100% lemmas (133) occur only with one value of `Voice`.

### `AUX`

425 <tt><a href="zh_gsdsimp-pos-AUX.html">AUX</a></tt> tokens (11% of all `AUX` tokens) have a non-empty value of `Voice`.

The most frequent other feature values with which `AUX` and `Voice` co-occurred: <tt><a href="zh_gsdsimp-feat-Aspect.html">Aspect</a></tt><tt>=EMPTY</tt> (425; 100%).

`AUX` tokens may have the following values of `Voice`:

* `Pass` (425; 100% of non-empty `Voice`): 被、 为
* `EMPTY` (3467): 是、 了、 为、 会、 可以、 着、 可、 也是、 能、 要

### `ADP`

1 <tt><a href="zh_gsdsimp-pos-ADP.html">ADP</a></tt> tokens (0% of all `ADP` tokens) have a non-empty value of `Voice`.

`ADP` tokens may have the following values of `Voice`:

* `Cau` (1; 100% of non-empty `Voice`): 以
* `EMPTY` (5628): 在、 于、 中、 与、 后、 时、 上、 对、 因、 从

