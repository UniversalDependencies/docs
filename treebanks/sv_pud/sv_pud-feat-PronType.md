---
layout: base
title:  'Statistics of PronType in UD_Swedish-PUD'
udver: '2'
---

## Treebank Statistics: UD_Swedish-PUD: Features: `PronType`

This feature is universal.
It occurs with 4 different values: `Art`, `Ind`, `Int`, `Rel`.
Some words have combined values of the feature; 1 combinations have been observed: `Int|Rel`.

400 tokens (2%) have a non-empty value of `PronType`.
26 types (0%) occur at least once with a non-empty value of `PronType`.
18 lemmas (0%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="sv_pud-pos-PRON.html">PRON</a></tt> (294; 2% instances), <tt><a href="sv_pud-pos-ADV.html">ADV</a></tt> (96; 1% instances), <tt><a href="sv_pud-pos-DET.html">DET</a></tt> (10; 0% instances).

### `PRON`

294 <tt><a href="sv_pud-pos-PRON.html">PRON</a></tt> tokens (28% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="sv_pud-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (294; 100%), <tt><a href="sv_pud-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (248; 84%), <tt><a href="sv_pud-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (235; 80%), <tt><a href="sv_pud-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (235; 80%).

`PRON` tokens may have the following values of `PronType`:

* `Ind` (1; 0% of non-empty `PronType`): <em>mycket</em>
* `Int,Rel` (293; 100% of non-empty `PronType`): <em>som, vad, vilket, vilka, vem, vilken</em>
* `EMPTY` (763): <em>det, han, de, sig, jag, hon, den, vi, detta, honom</em>

### `ADV`

96 <tt><a href="sv_pud-pos-ADV.html">ADV</a></tt> tokens (8% of all `ADV` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADV` and `PronType` co-occurred: <tt><a href="sv_pud-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (96; 100%).

`ADV` tokens may have the following values of `PronType`:

* `Int,Rel` (96; 100% of non-empty `PronType`): <em>när, där, då, hur, varför, som, dit, var, varav</em>
* `EMPTY` (1064): <em>inte, så, mer, in, ut, också, upp, bara, fram, här</em>

### `DET`

10 <tt><a href="sv_pud-pos-DET.html">DET</a></tt> tokens (1% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="sv_pud-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (7; 70%).

`DET` tokens may have the following values of `PronType`:

* `Art` (2; 20% of non-empty `PronType`): <em>den, det</em>
* `Int,Rel` (8; 80% of non-empty `PronType`): <em>vars, vilket, vilkas, vilken</em>
* `EMPTY` (1267): <em>en, den, ett, de, det, hans, sin, sina, dess, deras</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="sv_pud-dep-obl.html">obl</a></tt>]--> PRON</tt> (1; 100%).

