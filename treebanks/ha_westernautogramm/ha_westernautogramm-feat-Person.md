---
layout: base
title:  'Statistics of Person in UD_Hausa-WesternAutogramm'
udver: '2'
---

## Treebank Statistics: UD_Hausa-WesternAutogramm: Features: `Person`

This feature is universal but the values `4` are language-specific.
It occurs with 4 different values: `1`, `2`, `3`, `4`.

3374 tokens (24%) have a non-empty value of `Person`.
336 types (21%) occur at least once with a non-empty value of `Person`.
206 lemmas (18%) occur at least once with a non-empty value of `Person`.
The feature is used with 5 part-of-speech tags: <tt><a href="ha_westernautogramm-pos-AUX.html">AUX</a></tt> (2364; 17% instances), <tt><a href="ha_westernautogramm-pos-PRON.html">PRON</a></tt> (663; 5% instances), <tt><a href="ha_westernautogramm-pos-NOUN.html">NOUN</a></tt> (246; 2% instances), <tt><a href="ha_westernautogramm-pos-VERB.html">VERB</a></tt> (87; 1% instances), <tt><a href="ha_westernautogramm-pos-ADP.html">ADP</a></tt> (14; 0% instances).

### `AUX`

2364 <tt><a href="ha_westernautogramm-pos-AUX.html">AUX</a></tt> tokens (95% of all `AUX` tokens) have a non-empty value of `Person`.

The most frequent other feature values with which `AUX` and `Person` co-occurred: <tt><a href="ha_westernautogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (2131; 90%).

`AUX` tokens may have the following values of `Person`:

* `1` (53; 2% of non-empty `Person`): <em>ìn, naː, bàn, inàː, munkà, mun, munàː, mù, nay, nis</em>
* `2` (143; 6% of non-empty `Person`): <em>kà, kì, kaː, kakà, kin, kanàː, kas, kaɗ, kac, kah</em>
* `3` (1547; 65% of non-empty `Person`): <em>shì, tà, sù, sunkà, yaː, yac, bâːshi, sunàː, shinàː, taː</em>
* `4` (621; 26% of non-empty `Person`): <em>à, ankà, an, anàː, akà, zâː'a, bâː’a, bà’à, am, anà</em>
* `EMPTY` (112): <em>neː, kà, nàː, ceː, cèː, ad, nèː, ab, as', as</em>

### `PRON`

663 <tt><a href="ha_westernautogramm-pos-PRON.html">PRON</a></tt> tokens (80% of all `PRON` tokens) have a non-empty value of `Person`.

The most frequent other feature values with which `PRON` and `Person` co-occurred: <tt><a href="ha_westernautogramm-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (663; 100%), <tt><a href="ha_westernautogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (588; 89%).

`PRON` tokens may have the following values of `Person`:

* `1` (31; 5% of non-empty `Person`): <em>mun, niː, nì, minì, ni, muː, mukà, munà, mù, naːmù</em>
* `2` (42; 6% of non-empty `Person`): <em>kai, kì, kà, makà, mikì, keː, kèː, kù, ka, kuː</em>
* `3` (588; 89% of non-empty `Person`): <em>shiː, shi, su, ita, ta, tà, mutà, suː, shì, mai</em>
* `4` (2; 0% of non-empty `Person`): <em>a, à</em>
* `EMPTY` (170): <em>wandà, waddà, indà, koːwaː, wa’àndà, koːmiː, wani, koː’ìnaː, miː, wancè</em>

`Person` seems to be **lexical feature** of `PRON`. 100% lemmas (41) occur only with one value of `Person`.

### `NOUN`

246 <tt><a href="ha_westernautogramm-pos-NOUN.html">NOUN</a></tt> tokens (10% of all `NOUN` tokens) have a non-empty value of `Person`.

The most frequent other feature values with which `NOUN` and `Person` co-occurred: <tt><a href="ha_westernautogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (199; 81%), <tt><a href="ha_westernautogramm-feat-Gender.html">Gender</a></tt><tt>=Fem</tt> (132; 54%), <tt><a href="ha_westernautogramm-feat-Definite.html">Definite</a></tt><tt>=Cons</tt> (130; 53%).

`NOUN` tokens may have the following values of `Person`:

* `1` (8; 3% of non-empty `Person`): <em>gidaːna, kissàmmù, kâina, màːtaːta, sàːƙoːna, ìdàːnuːnaː</em>
* `2` (23; 9% of non-empty `Person`): <em>jìkinkà, sàràkkuwakkà, bàːkinkà, gidankà, kânkà, màːtakkà, gòːshinkà, kissàkkù, laːdakkì, zaːzakkà</em>
* `3` (215; 87% of non-empty `Person`): <em>màːtaːtai, jìkintà, wuriːnai, doːkìːnai, mijìntà, gidaːnai, uwattà, àboːkiːnai, sàràkkuwaːtai, gidansù</em>
* `EMPTY` (2274): <em>mùtun, sarkiː, maːlàm, maːlàmiː, maːgàniː, maːtaː, bùdurwaː, gidaː, maːgànîn, ƙarhèː</em>

`Person` seems to be **lexical feature** of `NOUN`. 100% lemmas (108) occur only with one value of `Person`.

### `VERB`

87 <tt><a href="ha_westernautogramm-pos-VERB.html">VERB</a></tt> tokens (4% of all `VERB` tokens) have a non-empty value of `Person`.

The most frequent other feature values with which `VERB` and `Person` co-occurred: <tt><a href="ha_westernautogramm-feat-ExtPos.html">ExtPos</a></tt><tt>=NOUN</tt> (79; 91%), <tt><a href="ha_westernautogramm-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (74; 85%).

`VERB` tokens may have the following values of `Person`:

* `1` (1; 1% of non-empty `Person`): <em>ganiːna</em>
* `2` (5; 6% of non-empty `Person`): <em>aikìnkì, ganinkì, kirànkì, sônkà, sônkì</em>
* `3` (81; 93% of non-empty `Person`): <em>tàhiyàːtai, sôːnai, sôntà, ɗiːbàːtai, tàhiyàttà, ganai, ɗiːbàttà, ganiːnai, kiràntà, rèːnontà</em>
* `EMPTY` (2347): <em>yi, cêː, ajèː, zoː, tàhi, sâː, sàːmu, baː, kwaːnaː, ci</em>

`Person` seems to be **lexical feature** of `VERB`. 100% lemmas (46) occur only with one value of `Person`.

### `ADP`

14 <tt><a href="ha_westernautogramm-pos-ADP.html">ADP</a></tt> tokens (2% of all `ADP` tokens) have a non-empty value of `Person`.

`ADP` tokens may have the following values of `Person`:

* `1` (1; 7% of non-empty `Person`): <em>wuriːna</em>
* `2` (3; 21% of non-empty `Person`): <em>s'akaːninkà, wurinkà, wurinkì</em>
* `3` (10; 71% of non-empty `Person`): <em>dabràssù, gàr̃ai, wuriːnai, s'àkaːninsù, s'àkaːnintà, wajentà, wurintà</em>
* `EMPTY` (736): <em>dà, cikin, gà, na, mà, mài, wurin, wurim, sai, cikim</em>

`Person` seems to be **lexical feature** of `ADP`. 100% lemmas (11) occur only with one value of `Person`.

## Relations with Agreement in `Person`

The 10 most frequent relations where parent and child node agree in `Person`:
<tt>AUX --[<tt><a href="ha_westernautogramm-dep-iobj.html">iobj</a></tt>]--> PRON</tt> (8; 67%),
<tt>PRON --[<tt><a href="ha_westernautogramm-dep-dislocated.html">dislocated</a></tt>]--> PRON</tt> (4; 67%),
<tt>AUX --[<tt><a href="ha_westernautogramm-dep-dislocated.html">dislocated</a></tt>]--> PRON</tt> (3; 75%),
<tt>AUX --[<tt><a href="ha_westernautogramm-dep-parataxis.html">parataxis</a></tt>]--> AUX</tt> (1; 100%),
<tt>AUX --[<tt><a href="ha_westernautogramm-dep-vocative.html">vocative</a></tt>]--> PRON</tt> (1; 100%),
<tt>PRON --[<tt><a href="ha_westernautogramm-dep-parataxis.html">parataxis</a></tt>]--> AUX</tt> (1; 100%),
<tt>PRON --[<tt><a href="ha_westernautogramm-dep-reparandum.html">reparandum</a></tt>]--> PRON</tt> (1; 100%).

