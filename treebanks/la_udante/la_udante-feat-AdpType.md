---
layout: base
title:  'Statistics of AdpType in UD_Latin-UDante'
udver: '2'
---

## Treebank Statistics: UD_Latin-UDante: Features: `AdpType`

This feature is language-specific.
It occurs with 2 different values: `Post`, `Prep`.

4131 tokens (7%) have a non-empty value of `AdpType`.
58 types (0%) occur at least once with a non-empty value of `AdpType`.
39 lemmas (1%) occur at least once with a non-empty value of `AdpType`.
The feature is used with 1 part-of-speech tags: <tt><a href="la_udante-pos-ADP.html">ADP</a></tt> (4131; 7% instances).

### `ADP`

4131 <tt><a href="la_udante-pos-ADP.html">ADP</a></tt> tokens (100% of all `ADP` tokens) have a non-empty value of `AdpType`.

`ADP` tokens may have the following values of `AdpType`:

* `Post` (21; 1% of non-empty `AdpType`): <em>cum</em>
* `Prep` (4110; 99% of non-empty `AdpType`): <em>in, ad, de, per, a, ab, propter, ex, cum, sub</em>

<table>
  <tr><th>Paradigm <i>cum</i></th><th><tt>Post</tt></th><th><tt>Prep</tt></th></tr>
  <tr><td><tt>_</tt></td><td></td><td><em>cum</em></td></tr>
  <tr><td><tt><tt><a href="la_udante-feat-Clitic.html">Clitic</a></tt><tt>=Yes</tt></tt></td><td><em>cum</em></td><td></td></tr>
</table>

`AdpType` seems to be **lexical feature** of `ADP`. 97% lemmas (38) occur only with one value of `AdpType`.

## Relations with Agreement in `AdpType`

The 10 most frequent relations where parent and child node agree in `AdpType`:
<tt>ADP --[<tt><a href="la_udante-dep-conj.html">conj</a></tt>]--> ADP</tt> (2; 100%),
<tt>ADP --[<tt><a href="la_udante-dep-advcl.html">advcl</a></tt>]--> ADP</tt> (1; 100%).

