---
layout: base
title:  'Statistics of PronType in UD_Zaar-Autogramm'
udver: '2'
---

## Treebank Statistics: UD_Zaar-Autogramm: Features: `PronType`

This feature is universal.
It occurs with 6 different values: `Dem`, `Ind`, `Int`, `Prs`, `Rel`, `Tot`.

1381 tokens (7%) have a non-empty value of `PronType`.
116 types (4%) occur at least once with a non-empty value of `PronType`.
72 lemmas (5%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="say_autogramm-pos-PRON.html">PRON</a></tt> (1263; 6% instances), <tt><a href="say_autogramm-pos-DET.html">DET</a></tt> (83; 0% instances), <tt><a href="say_autogramm-pos-ADV.html">ADV</a></tt> (35; 0% instances).

### `PRON`

1263 <tt><a href="say_autogramm-pos-PRON.html">PRON</a></tt> tokens (91% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="say_autogramm-feat-Deixis.html">Deixis</a></tt><tt>=EMPTY</tt> (1079; 85%), <tt><a href="say_autogramm-feat-Number.html">Number</a></tt><tt>=Sing</tt> (879; 70%), <tt><a href="say_autogramm-feat-Person.html">Person</a></tt><tt>=3</tt> (724; 57%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (78; 6% of non-empty `PronType`): <em>gíː, gín, giní, gêːs, ginês, ginín</em>
* `Ind` (12; 1% of non-empty `PronType`): <em>wáːni, gón, kóːnaːgyòː</em>
* `Int` (45; 4% of non-empty `PronType`): <em>níː, niː, nîː, núː, nûː, nǐː, gyòː, gyóː, kóːnúː, míː</em>
* `Prs` (1087; 86% of non-empty `PronType`): <em>tə, ɣáy, gòs, ʃí, mí, tíː, gáy, ɣə, tə̀, yâːn</em>
* `Rel` (25; 2% of non-empty `PronType`): <em>yóːɗan, gyóːɗan, yáddà, dàːsóːɗa, yandìyóːɗan, yádda, yáddìyóːɗan</em>
* `Tot` (16; 1% of non-empty `PronType`): <em>kóːnúː, kóːgón, kóːníː</em>
* `EMPTY` (119): <em>gón, gyaːgón, kóːnúː, ɣáy, gwàːn, gamaʃi, gáy, ɗûːn, gamaː, kóːnîː</em>

<table>
  <tr><th>Paradigm <i>kóːnúː</i></th><th><tt>Int</tt></th><th><tt>Tot</tt></th></tr>
  <tr><td><tt></tt></td><td><em>kóːnúː</em></td><td><em>kóːnúː</em></td></tr>
</table>

`PronType` seems to be **lexical feature** of `PRON`. 97% lemmas (56) occur only with one value of `PronType`.

### `DET`

83 <tt><a href="say_autogramm-pos-DET.html">DET</a></tt> tokens (23% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="say_autogramm-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (83; 100%), <tt><a href="say_autogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (82; 99%), <tt><a href="say_autogramm-feat-Deixis.html">Deixis</a></tt><tt>=Remt</tt> (61; 73%).

`DET` tokens may have the following values of `PronType`:

* `Dem` (71; 86% of non-empty `PronType`): <em>gíː, gêːs, gín, giní</em>
* `Int` (12; 14% of non-empty `PronType`): <em>gyòː, gyôː, wuriː, gyóː</em>
* `EMPTY` (277): <em>gyáː, wón, gyaː, gón, ɓurúk, kóː, ɓét, gyòː, wàːné, gyǎː</em>

### `ADV`

35 <tt><a href="say_autogramm-pos-ADV.html">ADV</a></tt> tokens (3% of all `ADV` tokens) have a non-empty value of `PronType`.

`ADV` tokens may have the following values of `PronType`:

* `Dem` (2; 6% of non-empty `PronType`): <em>gíː</em>
* `Int` (26; 74% of non-empty `PronType`): <em>wuriː, ɗòː, téːɗóː, téːɗôː, wuríː, wúr, yǎː, ìnáː, ɗôː</em>
* `Rel` (7; 20% of non-empty `PronType`): <em>yáddiyóːɗan, yáddà, yándiyóːɗan, yə́ddà, ɗa, ɗan</em>
* `EMPTY` (991): <em>séː, séy, sòːséy, ʧík, ɗan, tá, káwây, káwêy, ɗa, máː</em>

`PronType` seems to be **lexical feature** of `ADV`. 100% lemmas (14) occur only with one value of `PronType`.

