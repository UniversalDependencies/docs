

--------------------------------------------------------------------------------

## Treebank Statistics (UD_Irish)

This feature is universal.
It occurs with 6 different values: `Art`, `Dem`, `Ind`, `Int`, `Prs`, `Rel`.

3259 tokens (14%) have a non-empty value of `PronType`.
95 types (2%) occur at least once with a non-empty value of `PronType`.
42 lemmas (1%) occur at least once with a non-empty value of `PronType`.
The feature is used with 7 part-of-speech tags: [ga-pos/DET]() (1836; 8% instances), [ga-pos/PART]() (611; 3% instances), [ga-pos/ADP]() (434; 2% instances), [ga-pos/PRON]() (191; 1% instances), [ga-pos/VERB]() (150; 1% instances), [ga-pos/X]() (27; 0% instances), [ga-pos/ADV]() (10; 0% instances).

### `DET`

1836 [ga-pos/DET]() tokens (86% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="Person.html">Person</a>=EMPTY</tt> (1836; 100%), <tt><a href="Poss.html">Poss</a>=EMPTY</tt> (1836; 100%), <tt><a href="Case.html">Case</a>=EMPTY</tt> (1602; 87%), <tt><a href="Gender.html">Gender</a>=EMPTY</tt> (1601; 87%), <tt><a href="Definite.html">Definite</a>=Def</tt> (1447; 79%), <tt><a href="Number.html">Number</a>=Sing</tt> (1253; 68%).

`DET` tokens may have the following values of `PronType`:

* `Art` (1448; 79% of non-empty `PronType`): <em>an, na, a, a', 'n</em>
* `Dem` (299; 16% of non-empty `PronType`): <em>seo, sin, eile, úd, s', siúd</em>
* `Ind` (86; 5% of non-empty `PronType`): <em>aon, cibé, uile, haon, n-uile</em>
* `Int` (3; 0% of non-empty `PronType`): <em>cad, cén</em>
* `EMPTY` (298): <em>a, gach, mo, do, m', ár, chuile, uilig, ngach, d'</em>

`PronType` seems to be **lexical feature** of `DET`. 100% lemmas (13) occur only with one value of `PronType`.

### `PART`

611 [ga-pos/PART]() tokens (40% of all `PART` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PART` and `PronType` co-occurred: <tt><a href="PartType.html">PartType</a>=Vb</tt> (582; 95%).

`PART` tokens may have the following values of `PronType`:

* `Rel` (611; 100% of non-empty `PronType`): <em>a, ina, nach, inar, ar, lena, n-a, nár, faoina, lenar</em>
* `EMPTY` (923): <em>a, go, ní, d', gur, nach, is, Ó, do, níor</em>

### `ADP`

434 [ga-pos/ADP]() tokens (12% of all `ADP` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADP` and `PronType` co-occurred: <tt><a href="Number.html">Number</a>=Sing</tt> (408; 94%), <tt><a href="Gender.html">Gender</a>=EMPTY</tt> (405; 93%), <tt><a href="Person.html">Person</a>=EMPTY</tt> (397; 91%).

`ADP` tokens may have the following values of `PronType`:

* `Art` (397; 91% of non-empty `PronType`): <em>sa, den, san, ón, don, faoin, sna, fén, ins</em>
* `Prs` (37; 9% of non-empty `PronType`): <em>á, dhá</em>
* `EMPTY` (3193): <em>ar, i, ag, le, de, leis, ina, mar, ann, chun</em>

<table>
  <tr><th>Paradigm <i>do</i></th><th><tt>Prs</tt></th><th><tt>Art</tt></th></tr>
  <tr><td><tt><a href="Gender.html">Gender</a>=Masc|<a href="Number.html">Number</a>=Sing|<a href="Person.html">Person</a>=3|<a href="Poss.html">Poss</a>=Yes</tt></td><td><em>á</em></td><td></td></tr>
  <tr><td><tt><a href="Gender.html">Gender</a>=Fem|<a href="Number.html">Number</a>=Sing|<a href="Person.html">Person</a>=3|<a href="Poss.html">Poss</a>=Yes</tt></td><td><em>á</em></td><td></td></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Sing</tt></td><td></td><td><em>don</em></td></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Plur|<a href="Person.html">Person</a>=3|<a href="Poss.html">Poss</a>=Yes</tt></td><td><em>á, dhá</em></td><td></td></tr>
</table>

### `PRON`

191 [ga-pos/PRON]() tokens (21% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="Gender.html">Gender</a>=EMPTY</tt> (191; 100%), <tt><a href="Person.html">Person</a>=EMPTY</tt> (191; 100%), <tt><a href="Number.html">Number</a>=EMPTY</tt> (178; 93%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (150; 79% of non-empty `PronType`): <em>sin, seo, siúd, shin, san, in, iúd, súd</em>
* `Ind` (7; 4% of non-empty `PronType`): <em>pé, Cibé, ceachtar, cheachtar</em>
* `Int` (34; 18% of non-empty `PronType`): <em>cad, cé, cén, céard</em>
* `EMPTY` (739): <em>sé, é, sí, féin, mé, iad, siad, í, tú, muid</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (10) occur only with one value of `PronType`.

### `VERB`

150 [ga-pos/VERB]() tokens (7% of all `VERB` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `VERB` and `PronType` co-occurred: <tt><a href="Voice.html">Voice</a>=EMPTY</tt> (150; 100%), <tt><a href="Form.html">Form</a>=EMPTY</tt> (140; 93%), <tt><a href="Tense.html">Tense</a>=Pres</tt> (114; 76%), <tt><a href="VerbForm.html">VerbForm</a>=EMPTY</tt> (95; 63%), <tt><a href="Mood.html">Mood</a>=Ind</tt> (94; 63%).

`VERB` tokens may have the following values of `PronType`:

* `Art` (1; 1% of non-empty `PronType`): <em>Cén</em>
* `Dem` (6; 4% of non-empty `PronType`): <em>Seo, Sin</em>
* `Rel` (143; 95% of non-empty `PronType`): <em>atá, ba, nach, is, ab, leanas, nár, eireos, atáid, atáimse</em>
* `EMPTY` (2151): <em>bhí, is, tá, raibh, bhfuil, gur, bheidh, beidh, ba, ní</em>

`PronType` seems to be **lexical feature** of `VERB`. 100% lemmas (15) occur only with one value of `PronType`.

### `X`

27 [ga-pos/X]() tokens (10% of all `X` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `X` and `PronType` co-occurred: <tt><a href="Dialect.html">Dialect</a>=Munster</tt> (27; 100%).

`X` tokens may have the following values of `PronType`:

* `Dem` (26; 96% of non-empty `PronType`): <em>san, so</em>
* `Rel` (1; 4% of non-empty `PronType`): <em>ná</em>
* `EMPTY` (238): <em>(2), (a), (b), (1), (c), (3), (4), Co., a, Uimh.</em>

### `ADV`

10 [ga-pos/ADV]() tokens (2% of all `ADV` tokens) have a non-empty value of `PronType`.

`ADV` tokens may have the following values of `PronType`:

* `Int` (10; 100% of non-empty `PronType`): <em>conas, cá</em>
* `EMPTY` (452): <em>amach, anois, chomh, ansin, isteach, thart, anseo, arís, síos, anuas</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<a href="../dep/conj.html">conj</a>]--> PRON</tt> (2; 100%).

