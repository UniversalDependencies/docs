---
layout: base
title:  'Statistics of AdvType in UD_Latin-Perseus'
udver: '2'
---

## Treebank Statistics: UD_Latin-Perseus: Features: `AdvType`

This feature is language-specific.
It occurs with 2 different values: `Loc`, `Tim`.

287 tokens (1%) have a non-empty value of `AdvType`.
28 types (0%) occur at least once with a non-empty value of `AdvType`.
28 lemmas (1%) occur at least once with a non-empty value of `AdvType`.
The feature is used with 1 part-of-speech tags: <tt><a href="la_perseus-pos-ADV.html">ADV</a></tt> (287; 1% instances).

### `ADV`

287 <tt><a href="la_perseus-pos-ADV.html">ADV</a></tt> tokens (16% of all `ADV` tokens) have a non-empty value of `AdvType`.

`ADV` tokens may have the following values of `AdvType`:

* `Loc` (56; 20% of non-empty `AdvType`): <em>unde, hinc, ibi, quo, ubi, hic, inde, qua, huc, procul</em>
* `Tim` (231; 80% of non-empty `AdvType`): <em>iam, nunc, semper, tum, ante, saepe, olim, postea, post, pridem</em>

`AdvType` seems to be **lexical feature** of `ADV`. 100% lemmas (28) occur only with one value of `AdvType`.

## Relations with Agreement in `AdvType`

The 10 most frequent relations where parent and child node agree in `AdvType`:
<tt>ADV --[<tt><a href="la_perseus-dep-advmod-tmod.html">advmod:tmod</a></tt>]--> ADV</tt> (8; 53%).

