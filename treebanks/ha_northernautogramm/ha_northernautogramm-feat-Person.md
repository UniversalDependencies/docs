---
layout: base
title:  'Statistics of Person in UD_Hausa-NorthernAutogramm'
udver: '2'
---

## Treebank Statistics: UD_Hausa-NorthernAutogramm: Features: `Person`

This feature is universal but the values `4` are language-specific.
It occurs with 4 different values: `1`, `2`, `3`, `4`.

876 tokens (21%) have a non-empty value of `Person`.
138 types (17%) occur at least once with a non-empty value of `Person`.
46 lemmas (8%) occur at least once with a non-empty value of `Person`.
The feature is used with 2 part-of-speech tags: <tt><a href="ha_northernautogramm-pos-AUX.html">AUX</a></tt> (633; 15% instances), <tt><a href="ha_northernautogramm-pos-PRON.html">PRON</a></tt> (243; 6% instances).

### `AUX`

633 <tt><a href="ha_northernautogramm-pos-AUX.html">AUX</a></tt> tokens (92% of all `AUX` tokens) have a non-empty value of `Person`.

The most frequent other feature values with which `AUX` and `Person` co-occurred: <tt><a href="ha_northernautogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (486; 77%), <tt><a href="ha_northernautogramm-feat-Aspect.html">Aspect</a></tt><tt>=Perf</tt> (413; 65%).

`AUX` tokens may have the following values of `Person`:

* `1` (61; 10% of non-empty `Person`): <em>ìn, naː, inàː, mukà, mù, baːmù, bàn, mun, ani, munkà</em>
* `2` (86; 14% of non-empty `Person`): <em>kaː, kà, kukà, kun, bàkà, bàkù, kanàː, kat, kì, kù</em>
* `3` (398; 63% of non-empty `Person`): <em>yac, sunkà, yaː, shì, shinàː, tac, tà, yaz, tanàː, taː</em>
* `4` (88; 14% of non-empty `Person`): <em>ankà, akà, à, anàː, baː'à, an</em>
* `EMPTY` (57): <em>sunàː, anàː, shinàː, takè, tanàː, ab, kanàː, shikè, ad, am</em>

### `PRON`

243 <tt><a href="ha_northernautogramm-pos-PRON.html">PRON</a></tt> tokens (86% of all `PRON` tokens) have a non-empty value of `Person`.

The most frequent other feature values with which `PRON` and `Person` co-occurred: <tt><a href="ha_northernautogramm-feat-PronType.html">PronType</a></tt><tt>=Prs</tt> (243; 100%), <tt><a href="ha_northernautogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (174; 72%), <tt><a href="ha_northernautogramm-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (127; 52%).

`PRON` tokens may have the following values of `Person`:

* `1` (27; 11% of non-empty `Person`): <em>niː, min, =na, ni, =taː, =naː, nì, shi</em>
* `2` (44; 18% of non-empty `Person`): <em>=kà, =ka, kai, kà, maː, =kì, ka, =kù, keː, =ku</em>
* `3` (172; 71% of non-empty `Person`): <em>shiː, =tai, ita, shì, =tà, shi, =nai, =sù, mai, suː</em>
* `EMPTY` (39): <em>koːmiː, wàccan, miː, wani, wàgga, wànnan, koːwaː, wa'ànda, wandà, wasu</em>

<table>
  <tr><th>Paradigm <i>shi</i></th><th><tt>1</tt></th><th><tt>3</tt></th></tr>
  <tr><td><tt><tt><a href="ha_northernautogramm-feat-Case.html">Case</a></tt><tt>=Nom</tt></tt></td><td></td><td><em>shi</em></td></tr>
  <tr><td><tt></tt></td><td><em>shi</em></td><td><em>shì, shi</em></td></tr>
</table>

`Person` seems to be **lexical feature** of `PRON`. 96% lemmas (43) occur only with one value of `Person`.

## Relations with Agreement in `Person`

The 10 most frequent relations where parent and child node agree in `Person`:
<tt>AUX --[<tt><a href="ha_northernautogramm-dep-dislocated.html">dislocated</a></tt>]--> PRON</tt> (1; 100%),
<tt>AUX --[<tt><a href="ha_northernautogramm-dep-obl.html">obl</a></tt>]--> PRON</tt> (1; 100%),
<tt>PRON --[<tt><a href="ha_northernautogramm-dep-conj.html">conj</a></tt>]--> PRON</tt> (1; 100%),
<tt>PRON --[<tt><a href="ha_northernautogramm-dep-nmod.html">nmod</a></tt>]--> PRON</tt> (1; 100%).

