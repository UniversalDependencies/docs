---
layout: base
title:  'Statistics of Mood in UD_Classical_Chinese-Kyoto'
udver: '2'
---

## Treebank Statistics: UD_Classical_Chinese-Kyoto: Features: `Mood`

This feature is universal.
It occurs with 3 different values: `Des`, `Nec`, `Pot`.

4630 tokens (1%) have a non-empty value of `Mood`.
12 types (0%) occur at least once with a non-empty value of `Mood`.
12 lemmas (0%) occur at least once with a non-empty value of `Mood`.
The feature is used with 1 part-of-speech tags: <tt><a href="lzh_kyoto-pos-AUX.html">AUX</a></tt> (4630; 1% instances).

### `AUX`

4630 <tt><a href="lzh_kyoto-pos-AUX.html">AUX</a></tt> tokens (62% of all `AUX` tokens) have a non-empty value of `Mood`.

The most frequent other feature values with which `AUX` and `Mood` co-occurred: <tt><a href="lzh_kyoto-feat-VerbType.html">VerbType</a></tt><tt>=EMPTY</tt> (4630; 100%).

`AUX` tokens may have the following values of `Mood`:

* `Des` (1505; 33% of non-empty `Mood`): 欲、 敢、 肯
* `Nec` (176; 4% of non-empty `Mood`): 宜、 應、 須、 儀
* `Pot` (2949; 64% of non-empty `Mood`): 可、 能、 足、 得、 堪
* `EMPTY` (2887): 為、 爲、 被、 見、 是

`Mood` seems to be **lexical feature** of `AUX`. 100% lemmas (12) occur only with one value of `Mood`.

## Relations with Agreement in `Mood`

The 10 most frequent relations where parent and child node agree in `Mood`:
<tt>AUX --[<tt><a href="lzh_kyoto-dep-aux.html">aux</a></tt>]--> AUX</tt> (5; 83%).

