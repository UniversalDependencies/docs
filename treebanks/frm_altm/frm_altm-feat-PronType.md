---
layout: base
title:  'Statistics of PronType in UD_Middle_French-ALTM'
udver: '2'
---

## Treebank Statistics: UD_Middle_French-ALTM: Features: `PronType`

This feature is universal.
It occurs with 7 different values: `Art`, `Dem`, `Ind`, `Int`, `Neg`, `Prs`, `Rel`.

1507 tokens (21%) have a non-empty value of `PronType`.
108 types (9%) occur at least once with a non-empty value of `PronType`.
41 lemmas (5%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="frm_altm-pos-DET.html">DET</a></tt> (737; 10% instances), <tt><a href="frm_altm-pos-PRON.html">PRON</a></tt> (669; 9% instances), <tt><a href="frm_altm-pos-ADV.html">ADV</a></tt> (101; 1% instances).

### `DET`

737 <tt><a href="frm_altm-pos-DET.html">DET</a></tt> tokens (88% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="frm_altm-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (737; 100%), <tt><a href="frm_altm-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (569; 77%).

`DET` tokens may have the following values of `PronType`:

* `Art` (603; 82% of non-empty `PronType`): <em>le, la, les, l', ung, une, un, l, lez, -l'</em>
* `Dem` (63; 9% of non-empty `PronType`): <em>icellui, ce, ceste, iceulx, icelle, celle, ces, cestuy, icelles</em>
* `Ind` (57; 8% of non-empty `PronType`): <em>aultre, aucun, quelque, tous, toutes, aucune, tout, aucunes, diverses, nul</em>
* `Int` (7; 1% of non-empty `PronType`): <em>quel, quelle</em>
* `Rel` (7; 1% of non-empty `PronType`): <em>lequel, laquelle, quel</em>
* `EMPTY` (103): <em>son, sa, leurs, ma, ses, nostre, la, leur, mon, mes</em>

<table>
  <tr><th>Paradigm <i>quel</i></th><th><tt>Int</tt></th><th><tt>Rel</tt></th></tr>
  <tr><td><tt></tt></td><td><em>quel, quelle</em></td><td><em>quel</em></td></tr>
</table>

`PronType` seems to be **lexical feature** of `DET`. 94% lemmas (17) occur only with one value of `PronType`.

### `PRON`

669 <tt><a href="frm_altm-pos-PRON.html">PRON</a></tt> tokens (99% of all `PRON` tokens) have a non-empty value of `PronType`.

`PRON` tokens may have the following values of `PronType`:

* `Dem` (107; 16% of non-empty `PronType`): <em>ce, c', cellui, ceulx, icellui, cil, s', -ce, celui, icelle</em>
* `Ind` (59; 9% of non-empty `PronType`): <em>tel, aultres, aucun, aucuns, aultre, autre, chacun, nul, plusieurs, ung</em>
* `Prs` (373; 56% of non-empty `PronType`): <em>il, en, lui, le, y, je, s', se, ilz, nous</em>
* `Rel` (130; 19% of non-empty `PronType`): <em>qui, que, qu', dont, lequel, ou, quoy, lesquels, quieulx, laquelle</em>
* `EMPTY` (5): <em>en, première, qui, siens, simillia</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (29) occur only with one value of `PronType`.

### `ADV`

101 <tt><a href="frm_altm-pos-ADV.html">ADV</a></tt> tokens (25% of all `ADV` tokens) have a non-empty value of `PronType`.

`ADV` tokens may have the following values of `PronType`:

* `Neg` (101; 100% of non-empty `PronType`): <em>ne, n', pas, point, non, mie</em>
* `EMPTY` (301): <em>item, etc., etc, après, plus, si, ainsi, dessus, non, premièrement</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="frm_altm-dep-advmod.html">advmod</a></tt>]--> PRON</tt> (1; 100%),
<tt>PRON --[<tt><a href="frm_altm-dep-appos.html">appos</a></tt>]--> PRON</tt> (1; 100%),
<tt>PRON --[<tt><a href="frm_altm-dep-nsubj.html">nsubj</a></tt>]--> PRON</tt> (1; 100%).

