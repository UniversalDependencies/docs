

--------------------------------------------------------------------------------

## Treebank Statistics (UD_Latvian)

This feature is universal.
It occurs with 8 different values: `Dem`, `Ind`, `Int`, `Neg`, `Prs`, `Rcp`, `Rel`, `Tot`.
Some words have combined values of the feature; 2 combinations have been observed: `Ind|Neg`, `Int|Neg`.

1384 tokens (7%) have a non-empty value of `PronType`.
262 types (4%) occur at least once with a non-empty value of `PronType`.
77 lemmas (2%) occur at least once with a non-empty value of `PronType`.
The feature is used with 5 part-of-speech tags: [lv-pos/PRON]() (565; 3% instances), [lv-pos/DET]() (392; 2% instances), [lv-pos/ADV]() (236; 1% instances), [lv-pos/SCONJ]() (190; 1% instances), [lv-pos/NOUN]() (1; 0% instances).

### `PRON`

565 [lv-pos/PRON]() tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="Number.html">Number</a>=Sing</tt> (380; 67%), <tt><a href="Person.html">Person</a>=EMPTY</tt> (375; 66%), <tt><a href="Gender.html">Gender</a>=Masc</tt> (328; 58%), <tt><a href="Case.html">Case</a>=Nom</tt> (289; 51%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (233; 41% of non-empty `PronType`): <em>tas, to, tā, tam, tās, tie, tajā, šim, šajā, tos</em>
* `Ind` (52; 9% of non-empty `PronType`): <em>kas, ko, kādas, kāds, kādā, citi, citos, kāda, kādam, citas</em>
* `Ind,Neg` (7; 1% of non-empty `PronType`): <em>neviens, neko</em>
* `Int` (34; 6% of non-empty `PronType`): <em>kas, ko, kāda, kurā, kurš, Kuras, Kuri, Kādas, Kādi, kuram</em>
* `Prs` (194; 34% of non-empty `PronType`): <em>viņš, es, viņa, mēs, man, mums, viņi, jūs, viņu, mūsu</em>
* `Rcp` (8; 1% of non-empty `PronType`): <em>sev, sevi, sevis</em>
* `Tot` (37; 7% of non-empty `PronType`): <em>viss, pats, abi, katram, visi, pašiem, visā, abiem, paši, pašā</em>

<table>
  <tr><th>Paradigm <i>kas</i></th><th><tt>Int</tt></th><th><tt>Ind</tt></th></tr>
  <tr><td><tt><a href="Case.html">Case</a>=Acc</tt></td><td><em>ko</em></td><td><em>ko</em></td></tr>
  <tr><td><tt><a href="Case.html">Case</a>=Nom</tt></td><td><em>kas</em></td><td><em>kas</em></td></tr>
</table>

### `DET`

392 [lv-pos/DET]() tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="Poss.html">Poss</a>=EMPTY</tt> (352; 90%), <tt><a href="Number.html">Number</a>=Sing</tt> (248; 63%), <tt><a href="Gender.html">Gender</a>=Masc</tt> (219; 56%).

`DET` tokens may have the following values of `PronType`:

* `Dem` (201; 51% of non-empty `PronType`): <em>šo, šī, tā, šajā, šāda, šādu, šīs, šis, to, tās</em>
* `Ind` (80; 20% of non-empty `PronType`): <em>cits, citus, citām, kādu, jebkurā, kādas, vairāku, citiem, citu, daudzi</em>
* `Ind,Neg` (8; 2% of non-empty `PronType`): <em>nekādas, neviena, Neviens, nekāds, nekādā, nevienai</em>
* `Int` (1; 0% of non-empty `PronType`): <em>Kādas</em>
* `Prs` (40; 10% of non-empty `PronType`): <em>savu, savā, manu, savas, saviem, Mans, mana, manas, savus, Savās</em>
* `Tot` (62; 16% of non-empty `PronType`): <em>visas, visā, katru, visos, visu, katrā, pats, pašā, visa, abu</em>

<table>
  <tr><th>Paradigm <i>kāda</i></th><th><tt>Int</tt></th><th><tt>Ind</tt></th></tr>
  <tr><td><tt><a href="Case.html">Case</a>=Acc|<a href="Number.html">Number</a>=Sing</tt></td><td></td><td><em>kādu</em></td></tr>
  <tr><td><tt><a href="Case.html">Case</a>=Acc|<a href="Number.html">Number</a>=Plur</tt></td><td><em>Kādas</em></td><td><em>kādas</em></td></tr>
  <tr><td><tt><a href="Case.html">Case</a>=Loc|<a href="Number.html">Number</a>=Sing</tt></td><td></td><td><em>kādā</em></td></tr>
  <tr><td><tt><a href="Case.html">Case</a>=Loc|<a href="Number.html">Number</a>=Plur</tt></td><td></td><td><em>kādās</em></td></tr>
  <tr><td><tt><a href="Case.html">Case</a>=Nom|<a href="Number.html">Number</a>=Plur</tt></td><td></td><td><em>kādas</em></td></tr>
</table>

`PronType` seems to be **lexical feature** of `DET`. 98% lemmas (39) occur only with one value of `PronType`.

### `ADV`

236 [lv-pos/ADV]() tokens (27% of all `ADV` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADV` and `PronType` co-occurred: <tt><a href="Degree.html">Degree</a>=EMPTY</tt> (236; 100%).

`ADV` tokens may have the following values of `PronType`:

* `Dem` (78; 33% of non-empty `PronType`): <em>tā, tad, tur, tik, tagad, Šeit</em>
* `Ind` (1; 0% of non-empty `PronType`): <em>kur</em>
* `Int` (132; 56% of non-empty `PronType`): <em>kā, kur, kad, cik, kāpēc, kādēļ</em>
* `Int,Neg` (6; 3% of non-empty `PronType`): <em>nekad, nekur</em>
* `Neg` (14; 6% of non-empty `PronType`): <em>nepamatoti, neiespējami, nelietderīgi, nereti, nesen, neatkarīgi, nemaz, nesalīdzināmi</em>
* `Tot` (5; 2% of non-empty `PronType`): <em>vienmēr, visur</em>
* `EMPTY` (638): <em>jau, vēl, vairāk, savukārt, ļoti, pašlaik, daudz, patlaban, līdz, tāpēc</em>

<table>
  <tr><th>Paradigm <i>kur</i></th><th><tt>Int</tt></th><th><tt>Ind</tt></th></tr>
  <tr><td><tt></tt></td><td><em>kur</em></td><td><em>kur</em></td></tr>
</table>

`PronType` seems to be **lexical feature** of `ADV`. 96% lemmas (23) occur only with one value of `PronType`.

### `SCONJ`

190 [lv-pos/SCONJ]() tokens (32% of all `SCONJ` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `SCONJ` and `PronType` co-occurred: <tt><a href="Number.html">Number</a>=EMPTY</tt> (113; 59%), <tt><a href="Case.html">Case</a>=Nom</tt> (113; 59%), <tt><a href="Gender.html">Gender</a>=EMPTY</tt> (113; 59%).

`SCONJ` tokens may have the following values of `PronType`:

* `Rel` (190; 100% of non-empty `PronType`): <em>kas, ko, kurā, kuru, kuras, kuriem, kurš, kuri, kura, kuram</em>
* `EMPTY` (398): <em>ka, kā, lai, jo, ja, nekā, vai, gan, kamēr, līdz</em>

### `NOUN`

1 [lv-pos/NOUN]() tokens (0% of all `NOUN` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `NOUN` and `PronType` co-occurred: <tt><a href="Case.html">Case</a>=Gen</tt> (1; 100%), <tt><a href="Number.html">Number</a>=Sing</tt> (1; 100%), <tt><a href="Gender.html">Gender</a>=Fem</tt> (1; 100%).

`NOUN` tokens may have the following values of `PronType`:

* `Int` (1; 100% of non-empty `PronType`): <em>kurienes</em>
* `EMPTY` (6238): <em>gada, gadā, valsts, atkritumu, latu, finanšu, uzņēmuma, laikā, pašvaldības, bibliotēkas</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<a href="../dep/det.html">det</a>]--> DET</tt> (1; 100%),
<tt>DET --[<a href="../dep/det.html">det</a>]--> DET</tt> (1; 100%),
<tt>SCONJ --[<a href="../dep/conj.html">conj</a>]--> SCONJ</tt> (1; 100%).

