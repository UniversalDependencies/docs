---
layout: base
title:  'Statistics of PronType in UD_Highland_Puebla_Nahuatl-ITML'
udver: '2'
---

## Treebank Statistics: UD_Highland_Puebla_Nahuatl-ITML: Features: `PronType`

This feature is universal.
It occurs with 2 different values: `Prs`, `Rel`.

187 tokens (2%) have a non-empty value of `PronType`.
24 types (1%) occur at least once with a non-empty value of `PronType`.
7 lemmas (1%) occur at least once with a non-empty value of `PronType`.
The feature is used with 2 part-of-speech tags: <tt><a href="azz_itml-pos-PRON.html">PRON</a></tt> (124; 1% instances), <tt><a href="azz_itml-pos-ADV.html">ADV</a></tt> (63; 1% instances).

### `PRON`

124 <tt><a href="azz_itml-pos-PRON.html">PRON</a></tt> tokens (15% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="azz_itml-feat-Number.html">Number</a></tt><tt>=Sing</tt> (109; 88%), <tt><a href="azz_itml-feat-Person.html">Person</a></tt><tt>=1</tt> (75; 60%).

`PRON` tokens may have the following values of `PronType`:

* `Prs` (124; 100% of non-empty `PronType`): <em>neh, yehwa, tehwan, yeh, yejua, teh, tehwatsin, tejuan, Yehua, ne</em>
* `EMPTY` (698): <em>se, in, teyin, seki, yon, tein, nochi, nijon, sekin, nejin</em>

### `ADV`

63 <tt><a href="azz_itml-pos-ADV.html">ADV</a></tt> tokens (5% of all `ADV` tokens) have a non-empty value of `PronType`.

`ADV` tokens may have the following values of `PronType`:

* `Rel` (63; 100% of non-empty `PronType`): <em>kampa, kanpa</em>
* `EMPTY` (1305): <em>amo, no, ya, semi, pos, ne, ompa, iujki, komo, saj</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="azz_itml-dep-reparandum.html">reparandum</a></tt>]--> PRON</tt> (8; 100%).

