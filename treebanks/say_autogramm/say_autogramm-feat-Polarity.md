---
layout: base
title:  'Statistics of Polarity in UD_Zaar-Autogramm'
udver: '2'
---

## Treebank Statistics: UD_Zaar-Autogramm: Features: `Polarity`

This feature is universal.
It occurs with 1 different values: `Neg`.

464 tokens (2%) have a non-empty value of `Polarity`.
33 types (1%) occur at least once with a non-empty value of `Polarity`.
13 lemmas (1%) occur at least once with a non-empty value of `Polarity`.
The feature is used with 2 part-of-speech tags: <tt><a href="say_autogramm-pos-PART.html">PART</a></tt> (462; 2% instances), <tt><a href="say_autogramm-pos-INTJ.html">INTJ</a></tt> (2; 0% instances).

### `PART`

462 <tt><a href="say_autogramm-pos-PART.html">PART</a></tt> tokens (26% of all `PART` tokens) have a non-empty value of `Polarity`.

The most frequent other feature values with which `PART` and `Polarity` co-occurred: <tt><a href="say_autogramm-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (462; 100%), <tt><a href="say_autogramm-feat-PartType.html">PartType</a></tt><tt>=Neg</tt> (437; 95%).

`PART` tokens may have the following values of `Polarity`:

* `Neg` (462; 100% of non-empty `Polarity`): <em>hŋ́, bàː, ŋâː, ń, ŋǎːn, ǎːn, bâː, báː, ə̌n, káɗâ</em>
* `EMPTY` (1345): <em>tôː, ɗi, tòː, oː, ni, aː, kúmá, máː, mə́n, ɗa</em>

`Polarity` seems to be **lexical feature** of `PART`. 100% lemmas (12) occur only with one value of `Polarity`.

### `INTJ`

2 <tt><a href="say_autogramm-pos-INTJ.html">INTJ</a></tt> tokens (0% of all `INTJ` tokens) have a non-empty value of `Polarity`.

`INTJ` tokens may have the following values of `Polarity`:

* `Neg` (2; 100% of non-empty `Polarity`): <em>baːbù</em>
* `EMPTY` (586): <em>èː, m̀ː, áy, ká, yáwwàː, éy, ə̀ːm, m̀ːhm̂ː, tôː, ə̀ː</em>

## Relations with Agreement in `Polarity`

The 10 most frequent relations where parent and child node agree in `Polarity`:
<tt>PART --[<tt><a href="say_autogramm-dep-advmod.html">advmod</a></tt>]--> PART</tt> (4; 80%).

