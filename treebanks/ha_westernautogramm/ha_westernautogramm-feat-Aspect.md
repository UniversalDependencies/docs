---
layout: base
title:  'Statistics of Aspect in UD_Hausa-WesternAutogramm'
udver: '2'
---

## Treebank Statistics: UD_Hausa-WesternAutogramm: Features: `Aspect`

This feature is universal but the values `Aor`, `Iter`, `PerfBkg`, `PerfNeg`, `ProgBkg`, `ProgNeg` are language-specific.
It occurs with 8 different values: `Aor`, `Iter`, `Perf`, `PerfBkg`, `PerfNeg`, `Prog`, `ProgBkg`, `ProgNeg`.

2305 tokens (17%) have a non-empty value of `Aspect`.
125 types (8%) occur at least once with a non-empty value of `Aspect`.
2 lemmas (0%) occur at least once with a non-empty value of `Aspect`.
The feature is used with 2 part-of-speech tags: <tt><a href="ha_westernautogramm-pos-AUX.html">AUX</a></tt> (2274; 16% instances), <tt><a href="ha_westernautogramm-pos-PART.html">PART</a></tt> (31; 0% instances).

### `AUX`

2274 <tt><a href="ha_westernautogramm-pos-AUX.html">AUX</a></tt> tokens (92% of all `AUX` tokens) have a non-empty value of `Aspect`.

The most frequent other feature values with which `AUX` and `Aspect` co-occurred: <tt><a href="ha_westernautogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (2052; 90%), <tt><a href="ha_westernautogramm-feat-Person.html">Person</a></tt><tt>=3</tt> (1460; 64%).

`AUX` tokens may have the following values of `Aspect`:

* `Aor` (861; 38% of non-empty `Aspect`): <em>à, shì, tà, sù, kà, ìn, kì, kù, mù, ìm</em>
* `Perf` (199; 9% of non-empty `Aspect`): <em>yaː, an, taː, kaː, kin, naː, sun, am, kun, mun</em>
* `PerfBkg` (820; 36% of non-empty `Aspect`): <em>ankà, sunkà, yac, tac, yat, yay, yab, yak, yag, yaz</em>
* `PerfNeg` (50; 2% of non-empty `Aspect`): <em>bài, bàtà, bà’à, bàn, bàsù, bàkà</em>
* `Prog` (143; 6% of non-empty `Aspect`): <em>sunàː, shinàː, anàː, nàː, tanàː, yanàː, kanàː, inàː, kinàː</em>
* `ProgBkg` (87; 4% of non-empty `Aspect`): <em>kà, akà, shikà, takà, sukà, kakà, kikà, yakèː</em>
* `ProgNeg` (114; 5% of non-empty `Aspect`): <em>bâːshi, bâːta, bâːsu, bâː’a, bâːka, bâːki, bâː'a, bâːshì</em>
* `EMPTY` (202): <em>neː, zâːshi, zâː'a, sunàː, shinàː, tanàː, bâːshi, nàː, zâːta, ceː</em>

### `PART`

31 <tt><a href="ha_westernautogramm-pos-PART.html">PART</a></tt> tokens (11% of all `PART` tokens) have a non-empty value of `Aspect`.

The most frequent other feature values with which `PART` and `Aspect` co-occurred: <tt><a href="ha_westernautogramm-feat-PartType.html">PartType</a></tt><tt>=Adv</tt> (31; 100%), <tt><a href="ha_westernautogramm-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (31; 100%).

`PART` tokens may have the following values of `Aspect`:

* `Iter` (31; 100% of non-empty `Aspect`): <em>ta</em>
* `EMPTY` (253): <em>kuma, ba, kâu, dai, bâː, maː, àkwai, baːbù, zâː, kât</em>

## Relations with Agreement in `Aspect`

The 10 most frequent relations where parent and child node agree in `Aspect`:
<tt>AUX --[<tt><a href="ha_westernautogramm-dep-conj.html">conj</a></tt>]--> AUX</tt> (1; 100%),
<tt>AUX --[<tt><a href="ha_westernautogramm-dep-parataxis.html">parataxis</a></tt>]--> AUX</tt> (1; 100%).

