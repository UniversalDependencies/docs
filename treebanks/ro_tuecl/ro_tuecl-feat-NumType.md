---
layout: base
title:  'Statistics of NumType in UD_Romanian-TueCL'
udver: '2'
---

## Treebank Statistics: UD_Romanian-TueCL: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

31 tokens (1%) have a non-empty value of `NumType`.
22 types (1%) occur at least once with a non-empty value of `NumType`.
21 lemmas (2%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="ro_tuecl-pos-NUM.html">NUM</a></tt> (31; 1% instances).

### `NUM`

31 <tt><a href="ro_tuecl-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="ro_tuecl-feat-Number.html">Number</a></tt><tt>=Sing</tt> (26; 84%), <tt><a href="ro_tuecl-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (25; 81%), <tt><a href="ro_tuecl-feat-NumForm.html">NumForm</a></tt><tt>=Digit</tt> (24; 77%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (29; 94% of non-empty `NumType`): <em>10, 2, 3, 9, doi, 1, 112, 12, 12000, 2,5</em>
* `Ord` (2; 6% of non-empty `NumType`): <em>prima, întâi</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (21) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="ro_tuecl-dep-conj.html">conj</a></tt>]--> NUM</tt> (2; 100%).

