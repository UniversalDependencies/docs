---
layout: base
title:  'Statistics of Typo in UD_English-GUM'
udver: '2'
---

## Treebank Statistics: UD_English-GUM: Features: `Typo`

This feature is language-specific.
It occurs with 1 different values: `Yes`.

450 tokens (0%) have a non-empty value of `Typo`.
327 types (2%) occur at least once with a non-empty value of `Typo`.
282 lemmas (2%) occur at least once with a non-empty value of `Typo`.
The feature is used with 15 part-of-speech tags: <tt><a href="en_gum-pos-NOUN.html">NOUN</a></tt> (94; 0% instances), <tt><a href="en_gum-pos-VERB.html">VERB</a></tt> (82; 0% instances), <tt><a href="en_gum-pos-PRON.html">PRON</a></tt> (54; 0% instances), <tt><a href="en_gum-pos-PROPN.html">PROPN</a></tt> (35; 0% instances), <tt><a href="en_gum-pos-ADV.html">ADV</a></tt> (33; 0% instances), <tt><a href="en_gum-pos-ADJ.html">ADJ</a></tt> (28; 0% instances), <tt><a href="en_gum-pos-AUX.html">AUX</a></tt> (27; 0% instances), <tt><a href="en_gum-pos-ADP.html">ADP</a></tt> (23; 0% instances), <tt><a href="en_gum-pos-DET.html">DET</a></tt> (15; 0% instances), <tt><a href="en_gum-pos-INTJ.html">INTJ</a></tt> (15; 0% instances), <tt><a href="en_gum-pos-PUNCT.html">PUNCT</a></tt> (14; 0% instances), <tt><a href="en_gum-pos-SCONJ.html">SCONJ</a></tt> (13; 0% instances), <tt><a href="en_gum-pos-PART.html">PART</a></tt> (8; 0% instances), <tt><a href="en_gum-pos-NUM.html">NUM</a></tt> (7; 0% instances), <tt><a href="en_gum-pos-CCONJ.html">CCONJ</a></tt> (2; 0% instances).

### `NOUN`

94 <tt><a href="en_gum-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `NOUN` and `Typo` co-occurred: <tt><a href="en_gum-feat-Number.html">Number</a></tt><tt>=Sing</tt> (74; 79%).

`NOUN` tokens may have the following values of `Typo`:

* `Yes` (94; 100% of non-empty `Typo`): <em>lotos, etc, kind, nite, per, type, dodge, fisherman, m, order</em>

`Typo` seems to be **lexical feature** of `NOUN`. 100% lemmas (76) occur only with one value of `Typo`.

### `VERB`

82 <tt><a href="en_gum-pos-VERB.html">VERB</a></tt> tokens (0% of all `VERB` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `VERB` and `Typo` co-occurred: <tt><a href="en_gum-feat-Voice.html">Voice</a></tt><tt>=EMPTY</tt> (70; 85%), <tt><a href="en_gum-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (48; 59%), <tt><a href="en_gum-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (48; 59%), <tt><a href="en_gum-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (48; 59%).

`VERB` tokens may have the following values of `Typo`:

* `Yes` (82; 100% of non-empty `Typo`): <em>dwibbling, got, Pre, questi, se, set, under, understand, United, Untied</em>

`Typo` seems to be **lexical feature** of `VERB`. 100% lemmas (60) occur only with one value of `Typo`.

### `PRON`

54 <tt><a href="en_gum-pos-PRON.html">PRON</a></tt> tokens (0% of all `PRON` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `PRON` and `Typo` co-occurred: <tt><a href="en_gum-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (48; 89%), <tt><a href="en_gum-feat-PronType.html">PronType</a></tt><tt>=Prs</tt> (48; 89%), <tt><a href="en_gum-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (44; 81%), <tt><a href="en_gum-feat-Person.html">Person</a></tt><tt>=3</tt> (34; 63%), <tt><a href="en_gum-feat-Case.html">Case</a></tt><tt>=Acc</tt> (30; 56%).

`PRON` tokens may have the following values of `Typo`:

* `Yes` (54; 100% of non-empty `Typo`): <em>em, it, you, ya, i, it's, t, we, She, Who</em>

`Typo` seems to be **lexical feature** of `PRON`. 100% lemmas (15) occur only with one value of `Typo`.

### `PROPN`

35 <tt><a href="en_gum-pos-PROPN.html">PROPN</a></tt> tokens (0% of all `PROPN` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `PROPN` and `Typo` co-occurred: <tt><a href="en_gum-feat-Number.html">Number</a></tt><tt>=Sing</tt> (30; 86%).

`PROPN` tokens may have the following values of `Typo`:

* `Yes` (35; 100% of non-empty `Typo`): <em>sea, skittles, #langu, American, Chatnam, Fla., Hutter, JOHN, Misalette, Oija</em>

`Typo` seems to be **lexical feature** of `PROPN`. 100% lemmas (30) occur only with one value of `Typo`.

### `ADV`

33 <tt><a href="en_gum-pos-ADV.html">ADV</a></tt> tokens (0% of all `ADV` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `ADV` and `Typo` co-occurred: <tt><a href="en_gum-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (32; 97%), <tt><a href="en_gum-feat-Degree.html">Degree</a></tt><tt>=Pos</tt> (22; 67%).

`ADV` tokens may have the following values of `Typo`:

* `Yes` (33; 100% of non-empty `Typo`): <em>aka, all, before, Non, a, abaut, p, really, them, alr-</em>

`Typo` seems to be **lexical feature** of `ADV`. 100% lemmas (24) occur only with one value of `Typo`.

### `ADJ`

28 <tt><a href="en_gum-pos-ADJ.html">ADJ</a></tt> tokens (0% of all `ADJ` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `ADJ` and `Typo` co-occurred: <tt><a href="en_gum-feat-Degree.html">Degree</a></tt><tt>=Pos</tt> (26; 93%).

`ADJ` tokens may have the following values of `Typo`:

* `Yes` (28; 100% of non-empty `Typo`): <em>residential, 2D, I.=, Water, beautiful, completed, crowed, digital, first, flashest</em>

`Typo` seems to be **lexical feature** of `ADJ`. 100% lemmas (26) occur only with one value of `Typo`.

### `AUX`

27 <tt><a href="en_gum-pos-AUX.html">AUX</a></tt> tokens (0% of all `AUX` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `AUX` and `Typo` co-occurred: <tt><a href="en_gum-feat-VerbForm.html">VerbForm</a></tt><tt>=Fin</tt> (24; 89%), <tt><a href="en_gum-feat-Mood.html">Mood</a></tt><tt>=Ind</tt> (17; 63%), <tt><a href="en_gum-feat-Person.html">Person</a></tt><tt>=3</tt> (16; 59%).

`AUX` tokens may have the following values of `Typo`:

* `Yes` (27; 100% of non-empty `Typo`): <em>are, is, be, can, ll, was, will, been, get, has</em>

### `ADP`

23 <tt><a href="en_gum-pos-ADP.html">ADP</a></tt> tokens (0% of all `ADP` tokens) have a non-empty value of `Typo`.

`ADP` tokens may have the following values of `Typo`:

* `Yes` (23; 100% of non-empty `Typo`): <em>on, to, of, with, a, as, fro, from, in, than</em>

`Typo` seems to be **lexical feature** of `ADP`. 100% lemmas (10) occur only with one value of `Typo`.

### `DET`

15 <tt><a href="en_gum-pos-DET.html">DET</a></tt> tokens (0% of all `DET` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `DET` and `Typo` co-occurred: <tt><a href="en_gum-feat-PronType.html">PronType</a></tt><tt>=Art</tt> (10; 67%), <tt><a href="en_gum-feat-Definite.html">Definite</a></tt><tt>=Ind</tt> (8; 53%).

`DET` tokens may have the following values of `Typo`:

* `Yes` (15; 100% of non-empty `Typo`): <em>a, the, an, he, on, some, this, to</em>

### `INTJ`

15 <tt><a href="en_gum-pos-INTJ.html">INTJ</a></tt> tokens (1% of all `INTJ` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `INTJ` and `Typo` co-occurred: <tt><a href="en_gum-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (15; 100%).

`INTJ` tokens may have the following values of `Typo`:

* `Yes` (15; 100% of non-empty `Typo`): <em>y-, Ca-, Ro-, T-, alreet, alroot, f-, i-, n-, plo-</em>

`Typo` seems to be **lexical feature** of `INTJ`. 100% lemmas (14) occur only with one value of `Typo`.

### `PUNCT`

14 <tt><a href="en_gum-pos-PUNCT.html">PUNCT</a></tt> tokens (0% of all `PUNCT` tokens) have a non-empty value of `Typo`.

`PUNCT` tokens may have the following values of `Typo`:

* `Yes` (14; 100% of non-empty `Typo`): <em>., ", -, (, ;, [, |, ’</em>

### `SCONJ`

13 <tt><a href="en_gum-pos-SCONJ.html">SCONJ</a></tt> tokens (0% of all `SCONJ` tokens) have a non-empty value of `Typo`.

`SCONJ` tokens may have the following values of `Typo`:

* `Yes` (13; 100% of non-empty `Typo`): <em>cuz, cause, 'til, Altho, despite, that, then, whil, 'cuz, ‘cuz</em>

### `PART`

8 <tt><a href="en_gum-pos-PART.html">PART</a></tt> tokens (0% of all `PART` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `PART` and `Typo` co-occurred: <tt><a href="en_gum-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (7; 88%).

`PART` tokens may have the following values of `Typo`:

* `Yes` (8; 100% of non-empty `Typo`): <em>s, 's, do, the, not</em>

### `NUM`

7 <tt><a href="en_gum-pos-NUM.html">NUM</a></tt> tokens (0% of all `NUM` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `NUM` and `Typo` co-occurred: <tt><a href="en_gum-feat-NumType.html">NumType</a></tt><tt>=Card</tt> (5; 71%), <tt><a href="en_gum-feat-NumForm.html">NumForm</a></tt><tt>=Word</tt> (4; 57%).

`NUM` tokens may have the following values of `Typo`:

* `Yes` (7; 100% of non-empty `Typo`): <em>1, 19, 6:00, fiftyfive, five, one, to</em>

### `CCONJ`

2 <tt><a href="en_gum-pos-CCONJ.html">CCONJ</a></tt> tokens (0% of all `CCONJ` tokens) have a non-empty value of `Typo`.

`CCONJ` tokens may have the following values of `Typo`:

* `Yes` (2; 100% of non-empty `Typo`): <em>and, n</em>

## Relations with Agreement in `Typo`

The 10 most frequent relations where parent and child node agree in `Typo`:
<tt>PROPN --[<tt><a href="en_gum-dep-conj.html">conj</a></tt>]--> PROPN</tt> (2; 67%),
<tt>ADJ --[<tt><a href="en_gum-dep-compound.html">compound</a></tt>]--> PROPN</tt> (1; 100%),
<tt>ADV --[<tt><a href="en_gum-dep-parataxis.html">parataxis</a></tt>]--> ADV</tt> (1; 100%),
<tt>NOUN --[<tt><a href="en_gum-dep-dep.html">dep</a></tt>]--> PRON</tt> (1; 100%),
<tt>PROPN --[<tt><a href="en_gum-dep-mark.html">mark</a></tt>]--> SCONJ</tt> (1; 100%).

