---
layout: base
title:  'Statistics of ExtPos in UD_Classical_Armenian-CAVaL'
udver: '2'
---

## Treebank Statistics: UD_Classical_Armenian-CAVaL: Features: `ExtPos`

This feature is language-specific.
It occurs with 3 different values: `ADP`, `ADV`, `SCONJ`.

647 tokens (1%) have a non-empty value of `ExtPos`.
21 types (0%) occur at least once with a non-empty value of `ExtPos`.
18 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 3 part-of-speech tags: <tt><a href="xcl_caval-pos-ADP.html">ADP</a></tt> (624; 1% instances), <tt><a href="xcl_caval-pos-ADV.html">ADV</a></tt> (22; 0% instances), <tt><a href="xcl_caval-pos-SCONJ.html">SCONJ</a></tt> (1; 0% instances).

### `ADP`

624 <tt><a href="xcl_caval-pos-ADP.html">ADP</a></tt> tokens (5% of all `ADP` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADP` and `ExtPos` co-occurred: <tt><a href="xcl_caval-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (624; 100%).

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (513; 82% of non-empty `ExtPos`): <em>ի, մինչեւ, առ, մերձ, շուրջ, ընդ, մաւտ, մինչ, վասն, բայց</em>
* `ADV` (104; 17% of non-empty `ExtPos`): <em>ի, մինչեւ</em>
* `SCONJ` (7; 1% of non-empty `ExtPos`): <em>վասն, փոխանակ, մինչ</em>

<table>
  <tr><th>Paradigm <i>ի</i></th><th><tt>ADP</tt></th><th><tt>ADV</tt></th></tr>
  <tr><td><tt></tt></td><td><em>ի</em></td><td><em>ի</em></td></tr>
</table>

### `ADV`

22 <tt><a href="xcl_caval-pos-ADV.html">ADV</a></tt> tokens (1% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="xcl_caval-feat-Deixis.html">Deixis</a></tt><tt>=EMPTY</tt> (21; 95%), <tt><a href="xcl_caval-feat-PronType.html">PronType</a></tt><tt>=Rel</tt> (13; 59%).

`ADV` tokens may have the following values of `ExtPos`:

* `ADV` (10; 45% of non-empty `ExtPos`): <em>մանաւանդ, ընդ, Ամէն, շուրջ, աստ, որչափ, որպէս</em>
* `SCONJ` (12; 55% of non-empty `ExtPos`): <em>որպէս</em>

<table>
  <tr><th>Paradigm <i>որպէս</i></th><th><tt>ADV</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt>_</tt></td><td></td><td><em>որպէս</em></td></tr>
  <tr><td><tt><tt><a href="xcl_caval-feat-PronType.html">PronType</a></tt><tt>=Rel</tt></tt></td><td><em>որպէս</em></td><td><em>որպէս</em></td></tr>
</table>

### `SCONJ`

1 <tt><a href="xcl_caval-pos-SCONJ.html">SCONJ</a></tt> tokens (0% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

`SCONJ` tokens may have the following values of `ExtPos`:

* `SCONJ` (1; 100% of non-empty `ExtPos`): <em>զի</em>

## Relations with Agreement in `ExtPos`

The 10 most frequent relations where parent and child node agree in `ExtPos`:
<tt>ADP --[<tt><a href="xcl_caval-dep-conj.html">conj</a></tt>]--> ADP</tt> (1; 100%).

