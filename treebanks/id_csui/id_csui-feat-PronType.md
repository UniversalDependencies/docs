---
layout: base
title:  'Statistics of PronType in UD_Indonesian-CSUI'
udver: '2'
---

## Treebank Statistics: UD_Indonesian-CSUI: Features: `PronType`

This feature is universal.
It occurs with 5 different values: `Dem`, `Ind`, `Prs`, `Rel`, `Tot`.

809 tokens (3%) have a non-empty value of `PronType`.
27 types (1%) occur at least once with a non-empty value of `PronType`.
17 lemmas (0%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="id_csui-pos-PRON.html">PRON</a></tt> (483; 2% instances), <tt><a href="id_csui-pos-DET.html">DET</a></tt> (321; 1% instances), <tt><a href="id_csui-pos-NOUN.html">NOUN</a></tt> (5; 0% instances).

### `PRON`

483 <tt><a href="id_csui-pos-PRON.html">PRON</a></tt> tokens (71% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="id_csui-feat-Person.html">Person</a></tt><tt>=3</tt> (311; 64%), <tt><a href="id_csui-feat-Number.html">Number</a></tt><tt>=Sing</tt> (304; 63%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (89; 18% of non-empty `PronType`): <em>itu, ini, demikian</em>
* `Prs` (385; 80% of non-empty `PronType`): <em>nya, dia, ia, kita, mereka, saya, kami, diri, anda</em>
* `Rel` (9; 2% of non-empty `PronType`): <em>yang</em>
* `EMPTY` (196): <em>nya, apa, Kita, mana, Kami, Saya, masing-masing, Itu, siapa, yang</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (13) occur only with one value of `PronType`.

### `DET`

321 <tt><a href="id_csui-pos-DET.html">DET</a></tt> tokens (44% of all `DET` tokens) have a non-empty value of `PronType`.

`DET` tokens may have the following values of `PronType`:

* `Dem` (289; 90% of non-empty `PronType`): <em>ini, itu, sana</em>
* `Ind` (31; 10% of non-empty `PronType`): <em>sebuah, seorang</em>
* `Tot` (1; 0% of non-empty `PronType`): <em>segala</em>
* `EMPTY` (413): <em>nya, tersebut, beberapa, seluruh, banyak, sendiri, para, berbagai, semua, setiap</em>

### `NOUN`

5 <tt><a href="id_csui-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `NOUN` and `PronType` co-occurred: <tt><a href="id_csui-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (5; 100%).

`NOUN` tokens may have the following values of `PronType`:

* `Dem` (5; 100% of non-empty `PronType`): <em>Demikian</em>
* `EMPTY` (7656): <em>persen, Rp, tahun, dolar, sebesar, saham, perusahaan, pemerintah, negara, pertumbuhan</em>

