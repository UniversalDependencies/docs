---
layout: base
title:  'Statistics of NumType in UD_Armenian-ArmTDP'
udver: '2'
---

## Treebank Statistics: UD_Armenian-ArmTDP: Features: `NumType`

This feature is universal.
It occurs with 5 different values: `Card`, `Dist`, `Frac`, `Ord`, `Range`.

950 tokens (2%) have a non-empty value of `NumType`.
308 types (2%) occur at least once with a non-empty value of `NumType`.
290 lemmas (4%) occur at least once with a non-empty value of `NumType`.
The feature is used with 3 part-of-speech tags: <tt><a href="hy_armtdp-pos-NUM.html">NUM</a></tt> (714; 1% instances), <tt><a href="hy_armtdp-pos-ADJ.html">ADJ</a></tt> (207; 0% instances), <tt><a href="hy_armtdp-pos-ADV.html">ADV</a></tt> (29; 0% instances).

### `NUM`

714 <tt><a href="hy_armtdp-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="hy_armtdp-feat-NumForm.html">NumForm</a></tt><tt>=Digit</tt> (438; 61%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (637; 89% of non-empty `NumType`): <em>մի, երկու, մեկ, 2, մլն, 1, 2019, հազար, երեք, 2017</em>
* `Dist` (2; 0% of non-empty `NumType`): <em>47764653, երկուական</em>
* `Frac` (63; 9% of non-empty `NumType`): <em>կես, 2,5, 3.2, 4,3, 4,5, 8,7, 0.2, 0.863004, 1,2, 1,6</em>
* `Range` (12; 2% of non-empty `NumType`): <em>երկու-երեք, մեկ-երկու, 12-13, 17-23, 18-19, 65-68, 7-10, երեք-չորս, երեք-տասը, հինգ-վեց</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (245) occur only with one value of `NumType`.

### `ADJ`

207 <tt><a href="hy_armtdp-pos-ADJ.html">ADJ</a></tt> tokens (5% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="hy_armtdp-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (155; 75%).

`ADJ` tokens may have the following values of `NumType`:

* `Card` (3; 1% of non-empty `NumType`): <em>մեկ</em>
* `Frac` (1; 0% of non-empty `NumType`): <em>⅛</em>
* `Ord` (203; 98% of non-empty `NumType`): <em>առաջին, 1-ին, երկրորդ, 5-րդ, 3-րդ, երրորդ, 2-րդ, չորրորդ, 16-րդ, 20-րդ</em>
* `EMPTY` (3991): <em>կարող, նոր, մեծ, պետական, ազգային, հայ, պետք, քաղաքական, տնտեսական, արտաքին</em>

`NumType` seems to be **lexical feature** of `ADJ`. 100% lemmas (45) occur only with one value of `NumType`.

### `ADV`

29 <tt><a href="hy_armtdp-pos-ADV.html">ADV</a></tt> tokens (1% of all `ADV` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADV` and `NumType` co-occurred: <tt><a href="hy_armtdp-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (29; 100%), <tt><a href="hy_armtdp-feat-PronType.html">PronType</a></tt><tt>=Tot</tt> (29; 100%).

`ADV` tokens may have the following values of `NumType`:

* `Card` (29; 100% of non-empty `NumType`): <em>մեկ, մին</em>
* `EMPTY` (2264): <em>էլ, ավելի, միայն, արդեն, երբ, հիմա, դուրս, հետո, շատ, հենց</em>

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="hy_armtdp-dep-flat.html">flat</a></tt>]--> NUM</tt> (76; 80%),
<tt>NUM --[<tt><a href="hy_armtdp-dep-conj.html">conj</a></tt>]--> NUM</tt> (21; 100%),
<tt>ADJ --[<tt><a href="hy_armtdp-dep-conj.html">conj</a></tt>]--> ADJ</tt> (11; 92%),
<tt>NUM --[<tt><a href="hy_armtdp-dep-parataxis.html">parataxis</a></tt>]--> NUM</tt> (7; 100%),
<tt>ADV --[<tt><a href="hy_armtdp-dep-compound-redup.html">compound:redup</a></tt>]--> ADV</tt> (4; 100%),
<tt>NUM --[<tt><a href="hy_armtdp-dep-compound.html">compound</a></tt>]--> NUM</tt> (1; 100%).

