---
layout: base
title:  'Statistics of NumType in UD_Vietnamese-TueCL'
udver: '2'
---

## Treebank Statistics: UD_Vietnamese-TueCL: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

52 tokens (3%) have a non-empty value of `NumType`.
10 types (1%) occur at least once with a non-empty value of `NumType`.
9 lemmas (1%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="vi_tuecl-pos-NUM.html">NUM</a></tt> (51; 3% instances), <tt><a href="vi_tuecl-pos-ADJ.html">ADJ</a></tt> (1; 0% instances).

### `NUM`

51 <tt><a href="vi_tuecl-pos-NUM.html">NUM</a></tt> tokens (88% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (51; 100% of non-empty `NumType`): <em>một, hai, sáu, 19, 2004, 50, mười, tám</em>
* `EMPTY` (7): <em>trăm, 10-25, 2010, ngàn, triệu</em>

### `ADJ`

1 <tt><a href="vi_tuecl-pos-ADJ.html">ADJ</a></tt> tokens (1% of all `ADJ` tokens) have a non-empty value of `NumType`.

`ADJ` tokens may have the following values of `NumType`:

* `Ord` (1; 100% of non-empty `NumType`): <em>đầu tiên</em>
* `EMPTY` (81): <em>khác, khác nhau, mới, phải, lớn, từ bi, tự do, đen, Anh, an toàn</em>

