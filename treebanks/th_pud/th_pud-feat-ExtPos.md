---
layout: base
title:  'Statistics of ExtPos in UD_Thai-PUD'
udver: '2'
---

## Treebank Statistics: UD_Thai-PUD: Features: `ExtPos`

This feature is language-specific.
It occurs with 4 different values: `ADP`, `ADV`, `CCONJ`, `SCONJ`.

249 tokens (1%) have a non-empty value of `ExtPos`.
35 types (1%) occur at least once with a non-empty value of `ExtPos`.
35 lemmas (1%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 7 part-of-speech tags: <tt><a href="th_pud-pos-ADP.html">ADP</a></tt> (128; 1% instances), <tt><a href="th_pud-pos-VERB.html">VERB</a></tt> (68; 0% instances), <tt><a href="th_pud-pos-CCONJ.html">CCONJ</a></tt> (24; 0% instances), <tt><a href="th_pud-pos-NOUN.html">NOUN</a></tt> (19; 0% instances), <tt><a href="th_pud-pos-PART.html">PART</a></tt> (6; 0% instances), <tt><a href="th_pud-pos-ADV.html">ADV</a></tt> (3; 0% instances), <tt><a href="th_pud-pos-DET.html">DET</a></tt> (1; 0% instances).

### `ADP`

128 <tt><a href="th_pud-pos-ADP.html">ADP</a></tt> tokens (4% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (52; 41% of non-empty `ExtPos`): หลัง, จน, เนื่อง, โดย, นอก, แม้, จาก, ตั้ง, ตาม, ทาง
* `ADV` (1; 1% of non-empty `ExtPos`): อย่าง
* `CCONJ` (16; 13% of non-empty `ExtPos`): อย่าง, ถึง, นอก, แม้, แล้ว, โดย, ใน
* `SCONJ` (59; 46% of non-empty `ExtPos`): หลัง, เนื่อง, ก่อน, จน, ตาม, แม้, ถึง, ทั้ง, อย่าง, เพื่อ

<table>
  <tr><th>Paradigm <i>อย่าง</i></th><th><tt>ADP</tt></th><th><tt>ADV</tt></th><th><tt>CCONJ</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td>อย่าง</td><td>อย่าง</td><td>อย่าง</td><td>อย่าง</td></tr>
</table>

### `VERB`

68 <tt><a href="th_pud-pos-VERB.html">VERB</a></tt> tokens (1% of all `VERB` tokens) have a non-empty value of `ExtPos`.

`VERB` tokens may have the following values of `ExtPos`:

* `ADP` (50; 74% of non-empty `ExtPos`): เกี่ยว, ตั้ง, ได้, นับ
* `CCONJ` (14; 21% of non-empty `ExtPos`): รวม, พร้อม
* `SCONJ` (4; 6% of non-empty `ExtPos`): ตั้ง, เว้น, เสีย, แทน

<table>
  <tr><th>Paradigm <i>ตั้ง</i></th><th><tt>ADP</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td>ตั้ง</td><td>ตั้ง</td></tr>
</table>

### `CCONJ`

24 <tt><a href="th_pud-pos-CCONJ.html">CCONJ</a></tt> tokens (4% of all `CCONJ` tokens) have a non-empty value of `ExtPos`.

`CCONJ` tokens may have the following values of `ExtPos`:

* `CCONJ` (24; 100% of non-empty `ExtPos`): อย่าง, จาก, นอก, ใน, ดัง, ถึง, หรือ, หลัง

### `NOUN`

19 <tt><a href="th_pud-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `ExtPos`.

`NOUN` tokens may have the following values of `ExtPos`:

* `ADP` (18; 95% of non-empty `ExtPos`): ภาย, เช่น
* `CCONJ` (1; 5% of non-empty `ExtPos`): ท้าย

### `PART`

6 <tt><a href="th_pud-pos-PART.html">PART</a></tt> tokens (1% of all `PART` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PART` and `ExtPos` co-occurred: <tt><a href="th_pud-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (6; 100%).

`PART` tokens may have the following values of `ExtPos`:

* `CCONJ` (1; 17% of non-empty `ExtPos`): ไม่
* `SCONJ` (5; 83% of non-empty `ExtPos`): ไม่

<table>
  <tr><th>Paradigm <i>ไม่</i></th><th><tt>CCONJ</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td>ไม่</td><td>ไม่</td></tr>
</table>

### `ADV`

3 <tt><a href="th_pud-pos-ADV.html">ADV</a></tt> tokens (0% of all `ADV` tokens) have a non-empty value of `ExtPos`.

`ADV` tokens may have the following values of `ExtPos`:

* `CCONJ` (1; 33% of non-empty `ExtPos`): ยิ่ง
* `SCONJ` (2; 67% of non-empty `ExtPos`): ตาม, ทันที

### `DET`

1 <tt><a href="th_pud-pos-DET.html">DET</a></tt> tokens (0% of all `DET` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `DET` and `ExtPos` co-occurred: <tt><a href="th_pud-feat-PronType.html">PronType</a></tt><tt>=Tot</tt> (1; 100%).

`DET` tokens may have the following values of `ExtPos`:

* `CCONJ` (1; 100% of non-empty `ExtPos`): ทั้ง

