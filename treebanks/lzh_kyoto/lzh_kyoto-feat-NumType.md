---
layout: base
title:  'Statistics of NumType in UD_Classical_Chinese-Kyoto'
udver: '2'
---

## Treebank Statistics: UD_Classical_Chinese-Kyoto: Features: `NumType`

This feature is universal.
It occurs with 1 different values: `Ord`.

55 tokens (0%) have a non-empty value of `NumType`.
19 types (0%) occur at least once with a non-empty value of `NumType`.
19 lemmas (0%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="lzh_kyoto-pos-NUM.html">NUM</a></tt> (55; 0% instances).

### `NUM`

55 <tt><a href="lzh_kyoto-pos-NUM.html">NUM</a></tt> tokens (1% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Ord` (55; 100% of non-empty `NumType`): 丁、 己、 戊、 甲、 辛、 丙、 乙、 壬、 庚、 癸
* `EMPTY` (4561): 三、 一、 五、 二、 四、 百、 六、 十、 九、 七

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (19) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="lzh_kyoto-dep-flat.html">flat</a></tt>]--> NUM</tt> (13; 100%),
<tt>NUM --[<tt><a href="lzh_kyoto-dep-conj.html">conj</a></tt>]--> NUM</tt> (3; 100%).

