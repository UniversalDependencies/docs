---
layout: base
title:  'Statistics of PronType in UD_Scottish_Gaelic-ARCOSG'
udver: '2'
---

## Treebank Statistics: UD_Scottish_Gaelic-ARCOSG: Features: `PronType`

This feature is universal.
It occurs with 2 different values: `Int`, `Rel`.

1777 tokens (3%) have a non-empty value of `PronType`.
14 types (0%) occur at least once with a non-empty value of `PronType`.
9 lemmas (0%) occur at least once with a non-empty value of `PronType`.
The feature is used with 2 part-of-speech tags: <tt><a href="gd_arcosg-pos-PART.html">PART</a></tt> (1724; 3% instances), <tt><a href="gd_arcosg-pos-AUX.html">AUX</a></tt> (53; 0% instances).

### `PART`

1724 <tt><a href="gd_arcosg-pos-PART.html">PART</a></tt> tokens (29% of all `PART` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PART` and `PronType` co-occurred: <tt><a href="gd_arcosg-feat-PartType.html">PartType</a></tt><tt>=Vb</tt> (1724; 100%), <tt><a href="gd_arcosg-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (1674; 97%).

`PART` tokens may have the following values of `PronType`:

* `Int` (296; 17% of non-empty `PronType`): <em>an, a, am, 'm, na, 'n, ‘n</em>
* `Rel` (1428; 83% of non-empty `PronType`): <em>a, nach, na, a'</em>
* `EMPTY` (4145): <em>a', a, gu, ag, cha, air, gun, nach, chan, '</em>

<table>
  <tr><th>Paradigm <i>a</i></th><th><tt>Int</tt></th><th><tt>Rel</tt></th></tr>
  <tr><td><tt><tt><a href="gd_arcosg-feat-Mood.html">Mood</a></tt><tt>=Int</tt></tt></td><td><em>a</em></td><td></td></tr>
  <tr><td><tt></tt></td><td></td><td><em>a</em></td></tr>
</table>

### `AUX`

53 <tt><a href="gd_arcosg-pos-AUX.html">AUX</a></tt> tokens (7% of all `AUX` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `AUX` and `PronType` co-occurred: <tt><a href="gd_arcosg-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (53; 100%), <tt><a href="gd_arcosg-feat-Tense.html">Tense</a></tt><tt>=Pres</tt> (53; 100%).

`AUX` tokens may have the following values of `PronType`:

* `Rel` (53; 100% of non-empty `PronType`): <em>as, is</em>
* `EMPTY` (711): <em>is, 's, b', gur, bu, chan, an, nach, b’, cha</em>

