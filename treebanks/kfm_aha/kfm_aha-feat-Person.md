---
layout: base
title:  'Statistics of Person in UD_Khunsari-AHA'
udver: '2'
---

## Treebank Statistics: UD_Khunsari-AHA: Features: `Person`

This feature is universal.
It occurs with 3 different values: `1`, `2`, `3`.

22 tokens (30%) have a non-empty value of `Person`.
21 types (36%) occur at least once with a non-empty value of `Person`.
17 lemmas (31%) occur at least once with a non-empty value of `Person`.
The feature is used with 3 part-of-speech tags: <tt><a href="kfm_aha-pos-VERB.html">VERB</a></tt> (14; 19% instances), <tt><a href="kfm_aha-pos-PRON.html">PRON</a></tt> (7; 9% instances), <tt><a href="kfm_aha-pos-AUX.html">AUX</a></tt> (1; 1% instances).

### `VERB`

14 <tt><a href="kfm_aha-pos-VERB.html">VERB</a></tt> tokens (100% of all `VERB` tokens) have a non-empty value of `Person`.

The most frequent other feature values with which `VERB` and `Person` co-occurred: <tt><a href="kfm_aha-feat-Number.html">Number</a></tt><tt>=Sing</tt> (13; 93%), <tt><a href="kfm_aha-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (12; 86%), <tt><a href="kfm_aha-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (12; 86%), <tt><a href="kfm_aha-feat-VerbForm.html">VerbForm</a></tt><tt>=EMPTY</tt> (12; 86%), <tt><a href="kfm_aha-feat-Tense.html">Tense</a></tt><tt>=Pres</tt> (8; 57%).

`VERB` tokens may have the following values of `Person`:

* `1` (7; 50% of non-empty `Person`): <em>آکَ, اِمِگوا, بشتون, جیر, دَرکَفتُن, مِکِرُن, ندیُ</em>
* `2` (1; 7% of non-empty `Person`): <em>دِ</em>
* `3` (6; 43% of non-empty `Person`): <em>ئُ, اِداجِن, اِچُ, اِکِرُ, نَچو, کَ</em>

<table>
  <tr><th>Paradigm <i>کِر</i></th><th><tt>1</tt></th><th><tt>3</tt></th></tr>
  <tr><td><tt></tt></td><td><em>مِکِرُن</em></td><td><em>اِکِرُ</em></td></tr>
</table>

`Person` seems to be **lexical feature** of `VERB`. 92% lemmas (11) occur only with one value of `Person`.

### `PRON`

7 <tt><a href="kfm_aha-pos-PRON.html">PRON</a></tt> tokens (88% of all `PRON` tokens) have a non-empty value of `Person`.

The most frequent other feature values with which `PRON` and `Person` co-occurred: <tt><a href="kfm_aha-feat-Number.html">Number</a></tt><tt>=Sing</tt> (7; 100%), <tt><a href="kfm_aha-feat-PronType.html">PronType</a></tt><tt>=Prs</tt> (7; 100%).

`PRON` tokens may have the following values of `Person`:

* `1` (5; 71% of non-empty `Person`): <em>م, ما, مُن, نا</em>
* `3` (2; 29% of non-empty `Person`): <em>او, ش</em>
* `EMPTY` (1): <em>مون</em>

### `AUX`

1 <tt><a href="kfm_aha-pos-AUX.html">AUX</a></tt> tokens (100% of all `AUX` tokens) have a non-empty value of `Person`.

The most frequent other feature values with which `AUX` and `Person` co-occurred: <tt><a href="kfm_aha-feat-Number.html">Number</a></tt><tt>=Sing</tt> (1; 100%), <tt><a href="kfm_aha-feat-Tense.html">Tense</a></tt><tt>=Fut</tt> (1; 100%).

`AUX` tokens may have the following values of `Person`:

* `1` (1; 100% of non-empty `Person`): <em>دارُن</em>

## Relations with Agreement in `Person`

The 10 most frequent relations where parent and child node agree in `Person`:
<tt>VERB --[<tt><a href="kfm_aha-dep-nsubj.html">nsubj</a></tt>]--> PRON</tt> (3; 100%),
<tt>VERB --[<tt><a href="kfm_aha-dep-aux.html">aux</a></tt>]--> AUX</tt> (1; 100%).

