---
layout: base
title:  'Statistics of NumType in UD_Galician-TreeGal'
udver: '2'
---

## Treebank Statistics: UD_Galician-TreeGal: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

265 tokens (1%) have a non-empty value of `NumType`.
133 types (2%) occur at least once with a non-empty value of `NumType`.
121 lemmas (3%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="gl_treegal-pos-NUM.html">NUM</a></tt> (265; 1% instances).

### `NUM`

265 <tt><a href="gl_treegal-pos-NUM.html">NUM</a></tt> tokens (99% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="gl_treegal-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (199; 75%), <tt><a href="gl_treegal-feat-Number.html">Number</a></tt><tt>=Sing</tt> (147; 55%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (227; 86% of non-empty `NumType`): <em>catro, dous, tres, un, dúas, cinco, dez, 1990, 21, 25</em>
* `Ord` (38; 14% of non-empty `NumType`): <em>primeira, primeiro, segunda, II, IX, terceira, terceiro, V, primeiras, primeiros</em>
* `EMPTY` (2): <em>2004, 687 614 874</em>

<table>
  <tr><th>Paradigm <i>ii</i></th><th><tt>Card</tt></th><th><tt>Ord</tt></th></tr>
  <tr><td><tt>_</tt></td><td></td><td><em>II</em></td></tr>
  <tr><td><tt><tt><a href="gl_treegal-feat-Gender.html">Gender</a></tt><tt>=Masc</tt>|<tt><a href="gl_treegal-feat-Number.html">Number</a></tt><tt>=Sing</tt></tt></td><td><em>II</em></td><td></td></tr>
</table>

`NumType` seems to be **lexical feature** of `NUM`. 99% lemmas (120) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="gl_treegal-dep-conj.html">conj</a></tt>]--> NUM</tt> (17; 100%),
<tt>NUM --[<tt><a href="gl_treegal-dep-compound.html">compound</a></tt>]--> NUM</tt> (1; 100%),
<tt>NUM --[<tt><a href="gl_treegal-dep-nmod.html">nmod</a></tt>]--> NUM</tt> (1; 100%),
<tt>NUM --[<tt><a href="gl_treegal-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (1; 100%).

