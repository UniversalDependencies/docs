---
layout: base
title:  'Statistics of Style in UD_Greek-Lesbian'
udver: '2'
---

## Treebank Statistics: UD_Greek-Lesbian: Features: `Style`

This feature is language-specific.
It occurs with 1 different values: `Arch`.

1 tokens (0%) have a non-empty value of `Style`.
1 types (0%) occur at least once with a non-empty value of `Style`.
1 lemmas (0%) occur at least once with a non-empty value of `Style`.
The feature is used with 1 part-of-speech tags: <tt><a href="el_lesbian-pos-X.html">X</a></tt> (1; 0% instances).

### `X`

1 <tt><a href="el_lesbian-pos-X.html">X</a></tt> tokens (17% of all `X` tokens) have a non-empty value of `Style`.

The most frequent other feature values with which `X` and `Style` co-occurred: <tt><a href="el_lesbian-feat-ExtPos.html">ExtPos</a></tt><tt>=EMPTY</tt> (1; 100%).

`X` tokens may have the following values of `Style`:

* `Arch` (1; 100% of non-empty `Style`): <em>αναδράμου</em>

