---
layout: base
title:  'Statistics of PronType in UD_Turkish-Atis'
udver: '2'
---

## Treebank Statistics: UD_Turkish-Atis: Features: `PronType`

This feature is universal.
It occurs with 5 different values: `Art`, `Dem`, `Ind`, `Int`, `Prs`.

3107 tokens (7%) have a non-empty value of `PronType`.
42 types (2%) occur at least once with a non-empty value of `PronType`.
15 lemmas (2%) occur at least once with a non-empty value of `PronType`.
The feature is used with 4 part-of-speech tags: <tt><a href="tr_atis-pos-DET.html">DET</a></tt> (1323; 3% instances), <tt><a href="tr_atis-pos-PRON.html">PRON</a></tt> (1223; 3% instances), <tt><a href="tr_atis-pos-AUX.html">AUX</a></tt> (448; 1% instances), <tt><a href="tr_atis-pos-ADV.html">ADV</a></tt> (113; 0% instances).

### `DET`

1323 <tt><a href="tr_atis-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="tr_atis-feat-Definite.html">Definite</a></tt><tt>=Ind</tt> (691; 52%).

`DET` tokens may have the following values of `PronType`:

* `Art` (1323; 100% of non-empty `PronType`): <em>tüm, bir, bu, her, o, şu</em>

### `PRON`

1223 <tt><a href="tr_atis-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

`PRON` tokens may have the following values of `PronType`:

* `Dem` (12; 1% of non-empty `PronType`): <em>bu, bunu, bunların, şu</em>
* `Ind` (7; 1% of non-empty `PronType`): <em>biri, hepsi, hepsini, çoğunu</em>
* `Int` (211; 17% of non-empty `PronType`): <em>hangi, ne, hangisi, hangileri, neyin, hangisinin, nerede, neyi</em>
* `Prs` (993; 81% of non-empty `PronType`): <em>bana, beni, benim</em>

### `AUX`

448 <tt><a href="tr_atis-pos-AUX.html">AUX</a></tt> tokens (99% of all `AUX` tokens) have a non-empty value of `PronType`.

`AUX` tokens may have the following values of `PronType`:

* `Int` (448; 100% of non-empty `PronType`): <em>mı, misin, mu, misiniz, miyim, mü, mi, musunuz, mısın, mıyım</em>
* `EMPTY` (3): <em>değil</em>

### `ADV`

113 <tt><a href="tr_atis-pos-ADV.html">ADV</a></tt> tokens (7% of all `ADV` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADV` and `PronType` co-occurred: <tt><a href="tr_atis-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (113; 100%).

`ADV` tokens may have the following values of `PronType`:

* `Int` (113; 100% of non-empty `PronType`): <em>ne, nasıl</em>
* `EMPTY` (1572): <em>en, lütfen, erken, hakkında, kalkıp, ayrılıp, şimdi, direkt, ilk, sonra</em>

