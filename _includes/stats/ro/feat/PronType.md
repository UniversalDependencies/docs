

--------------------------------------------------------------------------------

## Treebank Statistics (UD_Romanian)

This feature is universal but the values `Emp` are language-specific.
It occurs with 9 different values: `Art`, `Dem`, `Emp`, `Ind`, `Int`, `Neg`, `Prs`, `Rel`, `Tot`.
Some words have combined values of the feature; 1 combinations have been observed: `Int|Rel`.

25256 tokens (12%) have a non-empty value of `PronType`.
456 types (1%) occur at least once with a non-empty value of `PronType`.
80 lemmas (0%) occur at least once with a non-empty value of `PronType`.
The feature is used with 4 part-of-speech tags: [ro-pos/DET]() (12023; 6% instances), [ro-pos/PRON]() (11888; 5% instances), [ro-pos/ADV]() (1297; 1% instances), [ro-pos/NUM]() (48; 0% instances).

### `DET`

12023 [ro-pos/DET]() tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="Position.html">Position</a>=EMPTY</tt> (10095; 84%), <tt><a href="Number.html">Number</a>=Sing</tt> (9049; 75%), <tt><a href="Person.html">Person</a>=EMPTY</tt> (8190; 68%), <tt><a href="Poss.html">Poss</a>=EMPTY</tt> (8176; 68%), <tt><a href="Case.html">Case</a>=Acc,Nom</tt> (6392; 53%), <tt><a href="Gender.html">Gender</a>=Fem</tt> (6268; 52%).

`DET` tokens may have the following values of `PronType`:

* `Art` (405; 3% of non-empty `PronType`): <em>lui, -lea, -ul, -a, -ului, -urilor, -ilor, -urile</em>
* `Dem` (1568; 13% of non-empty `PronType`): <em>acest, cel, această, aceste, cele, cea, același, acestor, cei, aceeași</em>
* `Emp` (53; 0% of non-empty `PronType`): <em>însuși, însăși, înșiși, însumi, însele, înseși</em>
* `Ind` (6006; 50% of non-empty `PronType`): <em>o, un, unei, unui, orice, toate, unor, alte, fiecare, multe</em>
* `Int,Rel` (114; 1% of non-empty `PronType`): <em>ce, căror, care, câte, cărei, cărui, câți, cât, câtă</em>
* `Neg` (30; 0% of non-empty `PronType`): <em>niciun, nicio, niciunui</em>
* `Prs` (3847; 32% of non-empty `PronType`): <em>a, al, ale, lui, lor, ei, său, ai, sale, sa</em>

<table>
  <tr><th>Paradigm <i>lui</i></th><th><tt>Prs</tt></th><th><tt>Art</tt></th></tr>
  <tr><td><tt><a href="Case.html">Case</a>=Dat,Gen|<a href="Definite.html">Definite</a>=Def|<a href="Number.html">Number</a>=Sing</tt></td><td></td><td><em>lui</em></td></tr>
  <tr><td><tt><a href="Number[psor].html">Number[psor]</a>=Plur|<a href="Person.html">Person</a>=3|<a href="Poss.html">Poss</a>=Yes</tt></td><td><em>lor</em></td><td></td></tr>
  <tr><td><tt><a href="Number[psor].html">Number[psor]</a>=Sing|<a href="Person.html">Person</a>=3|<a href="Poss.html">Poss</a>=Yes</tt></td><td><em>lui, ei, -i</em></td><td></td></tr>
</table>

`PronType` seems to be **lexical feature** of `DET`. 98% lemmas (43) occur only with one value of `PronType`.

### `PRON`

11888 [ro-pos/PRON]() tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="Person.html">Person</a>=3</tt> (10714; 90%), <tt><a href="Variant.html">Variant</a>=EMPTY</tt> (9823; 83%), <tt><a href="Gender.html">Gender</a>=EMPTY</tt> (8714; 73%), <tt><a href="Reflex.html">Reflex</a>=EMPTY</tt> (7957; 67%), <tt><a href="Number.html">Number</a>=EMPTY</tt> (6838; 58%), <tt><a href="Strength.html">Strength</a>=Weak</tt> (6199; 52%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (953; 8% of non-empty `PronType`): <em>ceea, aceasta, acestea, cel, acesta, cele, cei, cea, acestuia, aceea</em>
* `Ind` (776; 7% of non-empty `PronType`): <em>unul, una, toate, ceva, fiecare, totul, tot, multe, toți, altul</em>
* `Int,Rel` (2839; 24% of non-empty `PronType`): <em>care, ce, cine, cărora, căruia, căreia, ce-, câte, cui, cari</em>
* `Neg` (70; 1% of non-empty `PronType`): <em>nimic, nimeni, nimica, Niciunul, niciuna, nime, nimenea</em>
* `Prs` (7250; 61% of non-empty `PronType`): <em>se, s-, el, le, o, își, -și, -l, și-, îi</em>
* `EMPTY` (9): <em>ș.a., dvs., Î.P.S.</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (46) occur only with one value of `PronType`.

### `ADV`

1297 [ro-pos/ADV]() tokens (14% of all `ADV` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADV` and `PronType` co-occurred: <tt><a href="Degree.html">Degree</a>=EMPTY</tt> (1297; 100%).

`ADV` tokens may have the following values of `PronType`:

* `Int,Rel` (1060; 82% of non-empty `PronType`): <em>când, cum, unde, cât, precum, oare, ce, cân'</em>
* `Neg` (237; 18% of non-empty `PronType`): <em>nici, niciodată, nicăieri, nicicând</em>
* `EMPTY` (8142): <em>mai, ca, iar, astfel, decât, numai, mult, înainte, chiar, puțin</em>

`PronType` seems to be **lexical feature** of `ADV`. 100% lemmas (11) occur only with one value of `PronType`.

### `NUM`

48 [ro-pos/NUM]() tokens (1% of all `NUM` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `NUM` and `PronType` co-occurred: <tt><a href="NumForm.html">NumForm</a>=EMPTY</tt> (48; 100%), <tt><a href="Number.html">Number</a>=Plur</tt> (48; 100%), <tt><a href="NumType.html">NumType</a>=Card</tt> (48; 100%), <tt><a href="Gender.html">Gender</a>=Fem</tt> (31; 65%).

`NUM` tokens may have the following values of `PronType`:

* `Tot` (48; 100% of non-empty `PronType`): <em>ambele, amândoi, amândouă, ambelor, ambii, tustrei</em>
* `EMPTY` (5491): <em>1, 2, 3, două, 4, trei, 5, 6, primul, doi</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<a href="../dep/conj.html">conj</a>]--> PRON</tt> (16; 67%),
<tt>PRON --[<a href="../dep/conj.html">conj</a>]--> DET</tt> (16; 100%),
<tt>PRON --[<a href="../dep/remnant.html">remnant</a>]--> PRON</tt> (6; 100%),
<tt>PRON --[<a href="../dep/mwe.html">mwe</a>]--> DET</tt> (5; 100%),
<tt>DET --[<a href="../dep/conj.html">conj</a>]--> PRON</tt> (4; 80%),
<tt>DET --[<a href="../dep/advmod.html">advmod</a>]--> PRON</tt> (3; 60%),
<tt>ADV --[<a href="../dep/conj.html">conj</a>]--> ADV</tt> (2; 67%),
<tt>PRON --[<a href="../dep/conj.html">conj</a>]--> ADV</tt> (1; 100%),
<tt>DET --[<a href="../dep/conj.html">conj</a>]--> DET</tt> (1; 100%),
<tt>DET --[<a href="../dep/amod.html">amod</a>]--> DET</tt> (1; 100%).

