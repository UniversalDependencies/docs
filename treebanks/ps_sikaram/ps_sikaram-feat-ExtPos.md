---
layout: base
title:  'Statistics of ExtPos in UD_Pashto-Sikaram'
udver: '2'
---

## Treebank Statistics: UD_Pashto-Sikaram: Features: `ExtPos`

This feature is language-specific.
It occurs with 2 different values: `ADV`, `SCONJ`.

9 tokens (0%) have a non-empty value of `ExtPos`.
4 types (0%) occur at least once with a non-empty value of `ExtPos`.
4 lemmas (1%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 3 part-of-speech tags: <tt><a href="ps_sikaram-pos-ADV.html">ADV</a></tt> (5; 0% instances), <tt><a href="ps_sikaram-pos-ADJ.html">ADJ</a></tt> (2; 0% instances), <tt><a href="ps_sikaram-pos-SCONJ.html">SCONJ</a></tt> (2; 0% instances).

### `ADV`

5 <tt><a href="ps_sikaram-pos-ADV.html">ADV</a></tt> tokens (5% of all `ADV` tokens) have a non-empty value of `ExtPos`.

`ADV` tokens may have the following values of `ExtPos`:

* `ADV` (2; 40% of non-empty `ExtPos`): <em>کله</em>
* `SCONJ` (3; 60% of non-empty `ExtPos`): <em>کله</em>

<table>
  <tr><th>Paradigm <i>کله</i></th><th><tt>ADV</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>کله</em></td><td><em>کله</em></td></tr>
</table>

### `ADJ`

2 <tt><a href="ps_sikaram-pos-ADJ.html">ADJ</a></tt> tokens (1% of all `ADJ` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADJ` and `ExtPos` co-occurred: <tt><a href="ps_sikaram-feat-Case.html">Case</a></tt><tt>=Nom</tt> (2; 100%), <tt><a href="ps_sikaram-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (2; 100%), <tt><a href="ps_sikaram-feat-Number.html">Number</a></tt><tt>=Sing</tt> (2; 100%).

`ADJ` tokens may have the following values of `ExtPos`:

* `ADV` (2; 100% of non-empty `ExtPos`): <em>لږ</em>

### `SCONJ`

2 <tt><a href="ps_sikaram-pos-SCONJ.html">SCONJ</a></tt> tokens (3% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

`SCONJ` tokens may have the following values of `ExtPos`:

* `SCONJ` (2; 100% of non-empty `ExtPos`): <em>ځکه, که</em>

