---
layout: base
title:  'Statistics of Aspect in UD_Chinese-PUD'
udver: '2'
---

## Treebank Statistics: UD_Chinese-PUD: Features: `Aspect`

This feature is universal.
It occurs with 2 different values: `Perf`, `Prog`.

397 tokens (2%) have a non-empty value of `Aspect`.
3 types (0%) occur at least once with a non-empty value of `Aspect`.
3 lemmas (0%) occur at least once with a non-empty value of `Aspect`.
The feature is used with 1 part-of-speech tags: <tt><a href="zh_pud-pos-AUX.html">AUX</a></tt> (397; 2% instances).

### `AUX`

397 <tt><a href="zh_pud-pos-AUX.html">AUX</a></tt> tokens (42% of all `AUX` tokens) have a non-empty value of `Aspect`.

`AUX` tokens may have the following values of `Aspect`:

* `Perf` (357; 90% of non-empty `Aspect`): 了、 過
* `Prog` (40; 10% of non-empty `Aspect`): 著
* `EMPTY` (558): 是、 被、 會、 能、 可以、 為、 可能、 要、 可、 能夠

