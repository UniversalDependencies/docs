---
layout: base
title:  'Statistics of AdpType in UD_Romanian-TueCL'
udver: '2'
---

## Treebank Statistics: UD_Romanian-TueCL: Features: `AdpType`

This feature is language-specific.
It occurs with 1 different values: `Prep`.

477 tokens (11%) have a non-empty value of `AdpType`.
42 types (3%) occur at least once with a non-empty value of `AdpType`.
23 lemmas (2%) occur at least once with a non-empty value of `AdpType`.
The feature is used with 1 part-of-speech tags: <tt><a href="ro_tuecl-pos-ADP.html">ADP</a></tt> (477; 11% instances).

### `ADP`

477 <tt><a href="ro_tuecl-pos-ADP.html">ADP</a></tt> tokens (100% of all `ADP` tokens) have a non-empty value of `AdpType`.

The most frequent other feature values with which `ADP` and `AdpType` co-occurred: <tt><a href="ro_tuecl-feat-Case.html">Case</a></tt><tt>=Acc</tt> (472; 99%).

`ADP` tokens may have the following values of `AdpType`:

* `Prep` (477; 100% of non-empty `AdpType`): <em>de, la, cu, în, pe, in, din, pentru, ca, după</em>

`AdpType` seems to be **lexical feature** of `ADP`. 100% lemmas (23) occur only with one value of `AdpType`.

## Relations with Agreement in `AdpType`

The 10 most frequent relations where parent and child node agree in `AdpType`:
<tt>ADP --[<tt><a href="ro_tuecl-dep-fixed.html">fixed</a></tt>]--> ADP</tt> (13; 100%).

