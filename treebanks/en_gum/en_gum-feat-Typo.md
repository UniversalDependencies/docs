---
layout: base
title:  'Statistics of Typo in UD_English-GUM'
udver: '2'
---

## Treebank Statistics: UD_English-GUM: Features: `Typo`

This feature is language-specific.
It occurs with 1 different values: `Yes`.

362 tokens (0%) have a non-empty value of `Typo`.
260 types (1%) occur at least once with a non-empty value of `Typo`.
223 lemmas (2%) occur at least once with a non-empty value of `Typo`.
The feature is used with 15 part-of-speech tags: <tt><a href="en_gum-pos-VERB.html">VERB</a></tt> (72; 0% instances), <tt><a href="en_gum-pos-NOUN.html">NOUN</a></tt> (70; 0% instances), <tt><a href="en_gum-pos-PRON.html">PRON</a></tt> (45; 0% instances), <tt><a href="en_gum-pos-ADV.html">ADV</a></tt> (27; 0% instances), <tt><a href="en_gum-pos-AUX.html">AUX</a></tt> (26; 0% instances), <tt><a href="en_gum-pos-ADJ.html">ADJ</a></tt> (24; 0% instances), <tt><a href="en_gum-pos-PROPN.html">PROPN</a></tt> (22; 0% instances), <tt><a href="en_gum-pos-ADP.html">ADP</a></tt> (20; 0% instances), <tt><a href="en_gum-pos-INTJ.html">INTJ</a></tt> (14; 0% instances), <tt><a href="en_gum-pos-DET.html">DET</a></tt> (11; 0% instances), <tt><a href="en_gum-pos-PUNCT.html">PUNCT</a></tt> (11; 0% instances), <tt><a href="en_gum-pos-PART.html">PART</a></tt> (7; 0% instances), <tt><a href="en_gum-pos-SCONJ.html">SCONJ</a></tt> (7; 0% instances), <tt><a href="en_gum-pos-NUM.html">NUM</a></tt> (5; 0% instances), <tt><a href="en_gum-pos-CCONJ.html">CCONJ</a></tt> (1; 0% instances).

### `VERB`

72 <tt><a href="en_gum-pos-VERB.html">VERB</a></tt> tokens (0% of all `VERB` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `VERB` and `Typo` co-occurred: <tt><a href="en_gum-feat-Voice.html">Voice</a></tt><tt>=EMPTY</tt> (62; 86%), <tt><a href="en_gum-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (42; 58%), <tt><a href="en_gum-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (42; 58%), <tt><a href="en_gum-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (42; 58%).

`VERB` tokens may have the following values of `Typo`:

* `Yes` (72; 100% of non-empty `Typo`): <em>dwibbling, got, Pre, questi, se, set, understand, United, Untied, address</em>

`Typo` seems to be **lexical feature** of `VERB`. 100% lemmas (52) occur only with one value of `Typo`.

### `NOUN`

70 <tt><a href="en_gum-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `NOUN` and `Typo` co-occurred: <tt><a href="en_gum-feat-Number.html">Number</a></tt><tt>=Sing</tt> (55; 79%).

`NOUN` tokens may have the following values of `Typo`:

* `Yes` (70; 100% of non-empty `Typo`): <em>lotos, etc, per, type, dodge, fisherman, kind, order, thing, while</em>

`Typo` seems to be **lexical feature** of `NOUN`. 100% lemmas (55) occur only with one value of `Typo`.

### `PRON`

45 <tt><a href="en_gum-pos-PRON.html">PRON</a></tt> tokens (0% of all `PRON` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `PRON` and `Typo` co-occurred: <tt><a href="en_gum-feat-PronType.html">PronType</a></tt><tt>=Prs</tt> (41; 91%), <tt><a href="en_gum-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (39; 87%), <tt><a href="en_gum-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (36; 80%), <tt><a href="en_gum-feat-Person.html">Person</a></tt><tt>=3</tt> (30; 67%), <tt><a href="en_gum-feat-Case.html">Case</a></tt><tt>=Acc</tt> (25; 56%).

`PRON` tokens may have the following values of `Typo`:

* `Yes` (45; 100% of non-empty `Typo`): <em>em, it, you, i, it's, t, we, ya, She, Who</em>

`Typo` seems to be **lexical feature** of `PRON`. 100% lemmas (13) occur only with one value of `Typo`.

### `ADV`

27 <tt><a href="en_gum-pos-ADV.html">ADV</a></tt> tokens (0% of all `ADV` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `ADV` and `Typo` co-occurred: <tt><a href="en_gum-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (27; 100%), <tt><a href="en_gum-feat-Degree.html">Degree</a></tt><tt>=Pos</tt> (18; 67%).

`ADV` tokens may have the following values of `Typo`:

* `Yes` (27; 100% of non-empty `Typo`): <em>aka, all, before, Non, p, really, them, then, alr-, any</em>

`Typo` seems to be **lexical feature** of `ADV`. 100% lemmas (18) occur only with one value of `Typo`.

### `AUX`

26 <tt><a href="en_gum-pos-AUX.html">AUX</a></tt> tokens (0% of all `AUX` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `AUX` and `Typo` co-occurred: <tt><a href="en_gum-feat-VerbForm.html">VerbForm</a></tt><tt>=Fin</tt> (23; 88%), <tt><a href="en_gum-feat-Mood.html">Mood</a></tt><tt>=Ind</tt> (16; 62%), <tt><a href="en_gum-feat-Person.html">Person</a></tt><tt>=3</tt> (16; 62%).

`AUX` tokens may have the following values of `Typo`:

* `Yes` (26; 100% of non-empty `Typo`): <em>are, is, be, can, ll, was, will, been, get, has</em>

### `ADJ`

24 <tt><a href="en_gum-pos-ADJ.html">ADJ</a></tt> tokens (0% of all `ADJ` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `ADJ` and `Typo` co-occurred: <tt><a href="en_gum-feat-Degree.html">Degree</a></tt><tt>=Pos</tt> (22; 92%).

`ADJ` tokens may have the following values of `Typo`:

* `Yes` (24; 100% of non-empty `Typo`): <em>residential, 2D, I.=, Water, completed, crowed, digital, first, flashest, late</em>

`Typo` seems to be **lexical feature** of `ADJ`. 100% lemmas (22) occur only with one value of `Typo`.

### `PROPN`

22 <tt><a href="en_gum-pos-PROPN.html">PROPN</a></tt> tokens (0% of all `PROPN` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `PROPN` and `Typo` co-occurred: <tt><a href="en_gum-feat-Number.html">Number</a></tt><tt>=Sing</tt> (17; 77%).

`PROPN` tokens may have the following values of `Typo`:

* `Yes` (22; 100% of non-empty `Typo`): <em>sea, skittles, #langu, American, Chatnam, Fla., Hutter, JOHN, Oija, Ouiji</em>

`Typo` seems to be **lexical feature** of `PROPN`. 100% lemmas (18) occur only with one value of `Typo`.

### `ADP`

20 <tt><a href="en_gum-pos-ADP.html">ADP</a></tt> tokens (0% of all `ADP` tokens) have a non-empty value of `Typo`.

`ADP` tokens may have the following values of `Typo`:

* `Yes` (20; 100% of non-empty `Typo`): <em>on, to, of, With, a, as, fro, from, in, than</em>

`Typo` seems to be **lexical feature** of `ADP`. 100% lemmas (10) occur only with one value of `Typo`.

### `INTJ`

14 <tt><a href="en_gum-pos-INTJ.html">INTJ</a></tt> tokens (1% of all `INTJ` tokens) have a non-empty value of `Typo`.

`INTJ` tokens may have the following values of `Typo`:

* `Yes` (14; 100% of non-empty `Typo`): <em>y-, Ca-, Ro-, T-, alreet, alroot, f-, n-, plo-, reve-</em>

`Typo` seems to be **lexical feature** of `INTJ`. 100% lemmas (13) occur only with one value of `Typo`.

### `DET`

11 <tt><a href="en_gum-pos-DET.html">DET</a></tt> tokens (0% of all `DET` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `DET` and `Typo` co-occurred: <tt><a href="en_gum-feat-PronType.html">PronType</a></tt><tt>=Art</tt> (8; 73%), <tt><a href="en_gum-feat-Definite.html">Definite</a></tt><tt>=Ind</tt> (7; 64%).

`DET` tokens may have the following values of `Typo`:

* `Yes` (11; 100% of non-empty `Typo`): <em>a, an, on, some, the, to</em>

### `PUNCT`

11 <tt><a href="en_gum-pos-PUNCT.html">PUNCT</a></tt> tokens (0% of all `PUNCT` tokens) have a non-empty value of `Typo`.

`PUNCT` tokens may have the following values of `Typo`:

* `Yes` (11; 100% of non-empty `Typo`): <em>", -, ., ,, [, |, ’</em>

### `PART`

7 <tt><a href="en_gum-pos-PART.html">PART</a></tt> tokens (0% of all `PART` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `PART` and `Typo` co-occurred: <tt><a href="en_gum-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (6; 86%).

`PART` tokens may have the following values of `Typo`:

* `Yes` (7; 100% of non-empty `Typo`): <em>s, 's, do, the, not</em>

### `SCONJ`

7 <tt><a href="en_gum-pos-SCONJ.html">SCONJ</a></tt> tokens (0% of all `SCONJ` tokens) have a non-empty value of `Typo`.

`SCONJ` tokens may have the following values of `Typo`:

* `Yes` (7; 100% of non-empty `Typo`): <em>cuz, cause, despite, that</em>

### `NUM`

5 <tt><a href="en_gum-pos-NUM.html">NUM</a></tt> tokens (0% of all `NUM` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `NUM` and `Typo` co-occurred: <tt><a href="en_gum-feat-NumType.html">NumType</a></tt><tt>=Card</tt> (4; 80%), <tt><a href="en_gum-feat-NumForm.html">NumForm</a></tt><tt>=Word</tt> (3; 60%).

`NUM` tokens may have the following values of `Typo`:

* `Yes` (5; 100% of non-empty `Typo`): <em>1, 6:00, fiftyfive, five, one</em>

### `CCONJ`

1 <tt><a href="en_gum-pos-CCONJ.html">CCONJ</a></tt> tokens (0% of all `CCONJ` tokens) have a non-empty value of `Typo`.

`CCONJ` tokens may have the following values of `Typo`:

* `Yes` (1; 100% of non-empty `Typo`): <em>and</em>

## Relations with Agreement in `Typo`

The 10 most frequent relations where parent and child node agree in `Typo`:
<tt>PROPN --[<tt><a href="en_gum-dep-conj.html">conj</a></tt>]--> PROPN</tt> (2; 67%),
<tt>ADV --[<tt><a href="en_gum-dep-parataxis.html">parataxis</a></tt>]--> ADV</tt> (1; 100%).

