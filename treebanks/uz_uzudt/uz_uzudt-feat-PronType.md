---
layout: base
title:  'Statistics of PronType in UD_Uzbek-UzUDT'
udver: '2'
---

## Treebank Statistics: UD_Uzbek-UzUDT: Features: `PronType`

This feature is universal.
It occurs with 7 different values: `Dem`, `Ind`, `Int`, `Neg`, `Prs`, `Rcp`, `Tot`.

415 tokens (6%) have a non-empty value of `PronType`.
54 types (2%) occur at least once with a non-empty value of `PronType`.
150 lemmas (6%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="uz_uzudt-pos-PRON.html">PRON</a></tt> (353; 5% instances), <tt><a href="uz_uzudt-pos-DET.html">DET</a></tt> (48; 1% instances), <tt><a href="uz_uzudt-pos-ADV.html">ADV</a></tt> (14; 0% instances).

### `PRON`

353 <tt><a href="uz_uzudt-pos-PRON.html">PRON</a></tt> tokens (75% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="uz_uzudt-feat-Case.html">Case</a></tt><tt>=Nom</tt> (242; 69%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (102; 29% of non-empty `PronType`): <em>bu, shu, shuning, bundan, shundan, buni</em>
* `Ind` (4; 1% of non-empty `PronType`): <em>kim, biri, birov</em>
* `Int` (6; 2% of non-empty `PronType`): <em>nima, kim, qayerdan</em>
* `Prs` (239; 68% of non-empty `PronType`): <em>u, men, sen, ular, uning, ularning, uni, biz, siz, bizga</em>
* `Rcp` (2; 1% of non-empty `PronType`): <em>bir, biriga</em>
* `EMPTY` (120): <em>eng, oʻz, oʻzi, oʻzini, sizga, oʻziga, undan, bizga, buni, hamma</em>

<table>
  <tr><th>Paradigm <i>kim</i></th><th><tt>Prs</tt></th><th><tt>Int</tt></th><th><tt>Ind</tt></th></tr>
  <tr><td><tt></tt></td><td></td><td><em>kim</em></td><td><em>kim</em></td></tr>
  <tr><td><tt><tt><a href="uz_uzudt-feat-Number.html">Number</a></tt><tt>=Sing</tt>|<tt><a href="uz_uzudt-feat-Person.html">Person</a></tt><tt>=2</tt></tt></td><td><em>sen</em></td><td></td><td></td></tr>
</table>

### `DET`

48 <tt><a href="uz_uzudt-pos-DET.html">DET</a></tt> tokens (41% of all `DET` tokens) have a non-empty value of `PronType`.

`DET` tokens may have the following values of `PronType`:

* `Dem` (20; 42% of non-empty `PronType`): <em>bu, shu, buncha, haligi</em>
* `Ind` (13; 27% of non-empty `PronType`): <em>bir, har, hech, qanday, shuncha</em>
* `Int` (7; 15% of non-empty `PronType`): <em>qaysi, qanday</em>
* `Neg` (4; 8% of non-empty `PronType`): <em>hech</em>
* `Tot` (4; 8% of non-empty `PronType`): <em>har, barcha, hamma</em>
* `EMPTY` (68): <em>bir, necha, qancha, har, hamma, barcha, bunday, koʻplab, nafar, nechi</em>

<table>
  <tr><th>Paradigm <i>qanday</i></th><th><tt>Int</tt></th><th><tt>Ind</tt></th></tr>
  <tr><td><tt></tt></td><td><em>qanday</em></td><td><em>qanday</em></td></tr>
</table>

### `ADV`

14 <tt><a href="uz_uzudt-pos-ADV.html">ADV</a></tt> tokens (7% of all `ADV` tokens) have a non-empty value of `PronType`.

`ADV` tokens may have the following values of `PronType`:

* `Dem` (1; 7% of non-empty `PronType`): <em>shunday</em>
* `Int` (13; 93% of non-empty `PronType`): <em>qanday, nimaga, nega</em>
* `EMPTY` (190): <em>yana, shunday, juda, nihoyatda, darhol, endi, hali, qayta, shunga, dov</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="uz_uzudt-dep-compound-redup.html">compound:redup</a></tt>]--> PRON</tt> (1; 100%).

