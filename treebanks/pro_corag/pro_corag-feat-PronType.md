---
layout: base
title:  'Statistics of PronType in UD_Old_Occitan-CorAG'
udver: '2'
---

## Treebank Statistics: UD_Old_Occitan-CorAG: Features: `PronType`

This feature is universal.
It occurs with 5 different values: `Dem`, `Ind`, `Neg`, `Prs`, `Rel`.

3905 tokens (7%) have a non-empty value of `PronType`.
308 types (4%) occur at least once with a non-empty value of `PronType`.
1 lemmas (0) occur at least once with a non-empty value of `PronType`.
The feature is used with 2 part-of-speech tags: <tt><a href="pro_corag-pos-PRON.html">PRON</a></tt> (3898; 7% instances), <tt><a href="pro_corag-pos-DET.html">DET</a></tt> (7; 0% instances).

### `PRON`

3898 <tt><a href="pro_corag-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="pro_corag-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (2221; 57%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (563; 14% of non-empty `PronType`): <em>so, asso, ac, aqueg, ag, aquetz, aquero, a-, aqued, aquegs</em>
* `Ind` (431; 11% of non-empty `PronType`): <em>autre, hom, autres, c., un, augu, &c., tot, augun, totz</em>
* `Neg` (27; 1% of non-empty `PronType`): <em>negun, res, augun, auguns, ren, degun, degune, aucun, augune, augunes</em>
* `Prs` (2007; 51% of non-empty `PronType`): <em>se, lo, nos, y, los, en, s', l', -los, luy</em>
* `Rel` (870; 22% of non-empty `PronType`): <em>qui, que, losquaus, ont, lasquaus, loquau, qui-, laquau, on, -que</em>
* `EMPTY` (15): <em>sons, nostre, nostres, sonx, lors, soes, sont, sos, vostres</em>

### `DET`

7 <tt><a href="pro_corag-pos-DET.html">DET</a></tt> tokens (0% of all `DET` tokens) have a non-empty value of `PronType`.

`DET` tokens may have the following values of `PronType`:

* `Neg` (7; 100% of non-empty `PronType`): <em>augun, augune, augunes</em>
* `EMPTY` (6328): <em>la, lo, -lo, las, l', -los, los, son, li, un</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="pro_corag-dep-conj.html">conj</a></tt>]--> PRON</tt> (30; 71%),
<tt>PRON --[<tt><a href="pro_corag-dep-nmod.html">nmod</a></tt>]--> PRON</tt> (15; 60%),
<tt>PRON --[<tt><a href="pro_corag-dep-advmod.html">advmod</a></tt>]--> PRON</tt> (2; 100%),
<tt>PRON --[<tt><a href="pro_corag-dep-orphan.html">orphan</a></tt>]--> PRON</tt> (1; 100%).

