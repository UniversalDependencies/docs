---
layout: base
title:  'Statistics of PunctType in UD_Greek-GUD'
udver: '2'
---

## Treebank Statistics: UD_Greek-GUD: Features: `PunctType`

This feature is language-specific.
It occurs with 8 different values: `Colo`, `Comm`, `Dash`, `Elip`, `Excl`, `Peri`, `Qest`, `Quot`.

4389 tokens (17%) have a non-empty value of `PunctType`.
12 types (0%) occur at least once with a non-empty value of `PunctType`.
12 lemmas (0%) occur at least once with a non-empty value of `PunctType`.
The feature is used with 1 part-of-speech tags: <tt><a href="el_gud-pos-PUNCT.html">PUNCT</a></tt> (4389; 17% instances).

### `PUNCT`

4389 <tt><a href="el_gud-pos-PUNCT.html">PUNCT</a></tt> tokens (100% of all `PUNCT` tokens) have a non-empty value of `PunctType`.

`PUNCT` tokens may have the following values of `PunctType`:

* `Colo` (29; 1% of non-empty `PunctType`): <em>:</em>
* `Comm` (1131; 26% of non-empty `PunctType`): <em>,</em>
* `Dash` (8; 0% of non-empty `PunctType`): <em>-</em>
* `Elip` (6; 0% of non-empty `PunctType`): <em>...</em>
* `Excl` (2; 0% of non-empty `PunctType`): <em>!</em>
* `Peri` (1570; 36% of non-empty `PunctType`): <em>.</em>
* `Qest` (219; 5% of non-empty `PunctType`): <em>;</em>
* `Quot` (1424; 32% of non-empty `PunctType`): <em>», «, “, ”, '</em>

`PunctType` seems to be **lexical feature** of `PUNCT`. 100% lemmas (12) occur only with one value of `PunctType`.

