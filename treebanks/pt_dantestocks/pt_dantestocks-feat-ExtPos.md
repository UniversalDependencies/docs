---
layout: base
title:  'Statistics of ExtPos in UD_Portuguese-DANTEStocks'
udver: '2'
---

## Treebank Statistics: UD_Portuguese-DANTEStocks: Features: `ExtPos`

This feature is language-specific.
It occurs with 5 different values: `ADP`, `ADV`, `CCONJ`, `PRON`, `SCONJ`.

242 tokens (0%) have a non-empty value of `ExtPos`.
46 types (0%) occur at least once with a non-empty value of `ExtPos`.
34 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 8 part-of-speech tags: <tt><a href="pt_dantestocks-pos-ADV.html">ADV</a></tt> (80; 0% instances), <tt><a href="pt_dantestocks-pos-ADP.html">ADP</a></tt> (76; 0% instances), <tt><a href="pt_dantestocks-pos-PRON.html">PRON</a></tt> (60; 0% instances), <tt><a href="pt_dantestocks-pos-DET.html">DET</a></tt> (13; 0% instances), <tt><a href="pt_dantestocks-pos-CCONJ.html">CCONJ</a></tt> (7; 0% instances), <tt><a href="pt_dantestocks-pos-SCONJ.html">SCONJ</a></tt> (4; 0% instances), <tt><a href="pt_dantestocks-pos-SYM.html">SYM</a></tt> (1; 0% instances), <tt><a href="pt_dantestocks-pos-X.html">X</a></tt> (1; 0% instances).

### `ADV`

80 <tt><a href="pt_dantestocks-pos-ADV.html">ADV</a></tt> tokens (3% of all `ADV` tokens) have a non-empty value of `ExtPos`.

`ADV` tokens may have the following values of `ExtPos`:

* `ADV` (63; 79% of non-empty `ExtPos`): <em>mais, Assim, cerca, enquanto, +, até, menos, pouco, QUE, Que</em>
* `CCONJ` (2; 3% of non-empty `ExtPos`): <em>não</em>
* `SCONJ` (15; 19% of non-empty `ExtPos`): <em>assim, Quanto, Qto, Sempre, apesar, depois, mesmo, só</em>

<table>
  <tr><th>Paradigm <i>assim</i></th><th><tt>ADV</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>Assim</em></td><td><em>assim</em></td></tr>
</table>

`ExtPos` seems to be **lexical feature** of `ADV`. 93% lemmas (14) occur only with one value of `ExtPos`.

### `ADP`

76 <tt><a href="pt_dantestocks-pos-ADP.html">ADP</a></tt> tokens (1% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (9; 12% of non-empty `ExtPos`): <em>em, por, de</em>
* `ADV` (34; 45% of non-empty `ExtPos`): <em>de, por, a, Até, d</em>
* `CCONJ` (1; 1% of non-empty `ExtPos`): <em>em</em>
* `SCONJ` (32; 42% of non-empty `ExtPos`): <em>de, d, para, a, com, desde, em</em>

<table>
  <tr><th>Paradigm <i>de</i></th><th><tt>ADP</tt></th><th><tt>ADV</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>de</em></td><td><em>de, d</em></td><td><em>de, d</em></td></tr>
</table>

### `PRON`

60 <tt><a href="pt_dantestocks-pos-PRON.html">PRON</a></tt> tokens (5% of all `PRON` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PRON` and `ExtPos` co-occurred: <tt><a href="pt_dantestocks-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (60; 100%), <tt><a href="pt_dantestocks-feat-Number.html">Number</a></tt><tt>=Sing</tt> (59; 98%), <tt><a href="pt_dantestocks-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (58; 97%), <tt><a href="pt_dantestocks-feat-PronType.html">PronType</a></tt><tt>=Dem</tt> (58; 97%), <tt><a href="pt_dantestocks-feat-Person.html">Person</a></tt><tt>=3</tt> (52; 87%).

`PRON` tokens may have the following values of `ExtPos`:

* `CCONJ` (2; 3% of non-empty `ExtPos`): <em>isto</em>
* `PRON` (58; 97% of non-empty `ExtPos`): <em>o, Isso, a, mo, tudo</em>

### `DET`

13 <tt><a href="pt_dantestocks-pos-DET.html">DET</a></tt> tokens (0% of all `DET` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `DET` and `ExtPos` co-occurred: <tt><a href="pt_dantestocks-feat-Number.html">Number</a></tt><tt>=Sing</tt> (13; 100%), <tt><a href="pt_dantestocks-feat-Definite.html">Definite</a></tt><tt>=Ind</tt> (11; 85%), <tt><a href="pt_dantestocks-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (11; 85%), <tt><a href="pt_dantestocks-feat-PronType.html">PronType</a></tt><tt>=Art</tt> (11; 85%).

`DET` tokens may have the following values of `ExtPos`:

* `ADV` (12; 92% of non-empty `ExtPos`): <em>um, cada</em>
* `SCONJ` (1; 8% of non-empty `ExtPos`): <em>Cada</em>

<table>
  <tr><th>Paradigm <i>cada</i></th><th><tt>ADV</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>cada</em></td><td><em>Cada</em></td></tr>
</table>

### `CCONJ`

7 <tt><a href="pt_dantestocks-pos-CCONJ.html">CCONJ</a></tt> tokens (0% of all `CCONJ` tokens) have a non-empty value of `ExtPos`.

`CCONJ` tokens may have the following values of `ExtPos`:

* `CCONJ` (6; 86% of non-empty `ExtPos`): <em>ou</em>
* `SCONJ` (1; 14% of non-empty `ExtPos`): <em>que</em>

### `SCONJ`

4 <tt><a href="pt_dantestocks-pos-SCONJ.html">SCONJ</a></tt> tokens (1% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

`SCONJ` tokens may have the following values of `ExtPos`:

* `SCONJ` (4; 100% of non-empty `ExtPos`): <em>quanto, Como</em>

### `SYM`

1 <tt><a href="pt_dantestocks-pos-SYM.html">SYM</a></tt> tokens (0% of all `SYM` tokens) have a non-empty value of `ExtPos`.

`SYM` tokens may have the following values of `ExtPos`:

* `ADV` (1; 100% of non-empty `ExtPos`): <em>+</em>

### `X`

1 <tt><a href="pt_dantestocks-pos-X.html">X</a></tt> tokens (0% of all `X` tokens) have a non-empty value of `ExtPos`.

`X` tokens may have the following values of `ExtPos`:

* `ADV` (1; 100% of non-empty `ExtPos`): <em>by</em>

