---
layout: base
title:  'Statistics of PunctSide in UD_Polish-MPDT'
udver: '2'
---

## Treebank Statistics: UD_Polish-MPDT: Features: `PunctSide`

This feature is language-specific.
It occurs with 2 different values: `Fin`, `Ini`.

120 tokens (0%) have a non-empty value of `PunctSide`.
4 types (0%) occur at least once with a non-empty value of `PunctSide`.
4 lemmas (0%) occur at least once with a non-empty value of `PunctSide`.
The feature is used with 1 part-of-speech tags: <tt><a href="pl_mpdt-pos-PUNCT.html">PUNCT</a></tt> (120; 0% instances).

### `PUNCT`

120 <tt><a href="pl_mpdt-pos-PUNCT.html">PUNCT</a></tt> tokens (2% of all `PUNCT` tokens) have a non-empty value of `PunctSide`.

The most frequent other feature values with which `PUNCT` and `PunctSide` co-occurred: <tt><a href="pl_mpdt-feat-PunctType.html">PunctType</a></tt><tt>=Brck</tt> (105; 88%).

`PUNCT` tokens may have the following values of `PunctSide`:

* `Fin` (65; 54% of non-empty `PunctSide`): <em>), ”</em>
* `Ini` (55; 46% of non-empty `PunctSide`): <em>(, „</em>

