---
layout: base
title:  'Statistics of VerbForm in UD_Classical_Chinese-Kyoto'
udver: '2'
---

## Treebank Statistics: UD_Classical_Chinese-Kyoto: Features: `VerbForm`

This feature is universal but the values `Conv` are language-specific.
It occurs with 2 different values: `Conv`, `Part`.

17340 tokens (6%) have a non-empty value of `VerbForm`.
1498 types (18%) occur at least once with a non-empty value of `VerbForm`.
1475 lemmas (18%) occur at least once with a non-empty value of `VerbForm`.
The feature is used with 2 part-of-speech tags: <tt><a href="lzh_kyoto-pos-VERB.html">VERB</a></tt> (8716; 3% instances), <tt><a href="lzh_kyoto-pos-ADV.html">ADV</a></tt> (8624; 3% instances).

### `VERB`

8716 <tt><a href="lzh_kyoto-pos-VERB.html">VERB</a></tt> tokens (10% of all `VERB` tokens) have a non-empty value of `VerbForm`.

The most frequent other feature values with which `VERB` and `VerbForm` co-occurred: <tt><a href="lzh_kyoto-feat-Degree.html">Degree</a></tt><tt>=Pos</tt> (5054; 58%).

`VERB` tokens may have the following values of `VerbForm`:

* `Part` (8716; 100% of non-empty `VerbForm`): 大、 太、 皇、 主、 小、 有、 以、 長、 庶、 孝
* `EMPTY` (82822): 曰、 以、 有、 無、 至、 立、 為、 如、 行、 謂

`VerbForm` seems to be **lexical feature** of `VERB`. 100% lemmas (1026) occur only with one value of `VerbForm`.

### `ADV`

8624 <tt><a href="lzh_kyoto-pos-ADV.html">ADV</a></tt> tokens (32% of all `ADV` tokens) have a non-empty value of `VerbForm`.

The most frequent other feature values with which `ADV` and `VerbForm` co-occurred: <tt><a href="lzh_kyoto-feat-AdvType.html">AdvType</a></tt><tt>=EMPTY</tt> (8624; 100%), <tt><a href="lzh_kyoto-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (8329; 97%), <tt><a href="lzh_kyoto-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (5669; 66%).

`ADV` tokens may have the following values of `VerbForm`:

* `Conv` (8624; 100% of non-empty `VerbForm`): 以、 大、 始、 然、 無、 親、 凡、 對、 當、 反
* `EMPTY` (18294): 不、 則、 皆、 必、 故、 亦、 乃、 未、 非、 又

`VerbForm` seems to be **lexical feature** of `ADV`. 100% lemmas (985) occur only with one value of `VerbForm`.

