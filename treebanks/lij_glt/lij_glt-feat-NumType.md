---
layout: base
title:  'Statistics of NumType in UD_Ligurian-GLT'
udver: '2'
---

## Treebank Statistics: UD_Ligurian-GLT: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

51 tokens (1%) have a non-empty value of `NumType`.
42 types (3%) occur at least once with a non-empty value of `NumType`.
35 lemmas (3%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="lij_glt-pos-NUM.html">NUM</a></tt> (42; 1% instances), <tt><a href="lij_glt-pos-ADJ.html">ADJ</a></tt> (9; 0% instances).

### `NUM`

42 <tt><a href="lij_glt-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="lij_glt-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (35; 83%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (42; 100% of non-empty `NumType`): <em>eutto, 1929, quaranta, doe, doî, 1746, 1815, 1847, 1874, 1892</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (31) occur only with one value of `NumType`.

### `ADJ`

9 <tt><a href="lij_glt-pos-ADJ.html">ADJ</a></tt> tokens (4% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="lij_glt-feat-Number.html">Number</a></tt><tt>=Sing</tt> (8; 89%), <tt><a href="lij_glt-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (5; 56%).

`ADJ` tokens may have the following values of `NumType`:

* `Ord` (9; 100% of non-empty `NumType`): <em>primma, primmo, vinteximo, primme, quarto, segonda</em>
* `EMPTY` (210): <em>bello, antigo, cao, mæximo, San, avvoxæ, Basso, antiga, bella, belle</em>

