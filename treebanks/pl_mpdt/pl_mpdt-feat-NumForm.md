---
layout: base
title:  'Statistics of NumForm in UD_Polish-MPDT'
udver: '2'
---

## Treebank Statistics: UD_Polish-MPDT: Features: `NumForm`

This feature is language-specific.
It occurs with 3 different values: `Digit`, `Roman`, `Word`.

748 tokens (2%) have a non-empty value of `NumForm`.
237 types (2%) occur at least once with a non-empty value of `NumForm`.
203 lemmas (3%) occur at least once with a non-empty value of `NumForm`.
The feature is used with 2 part-of-speech tags: <tt><a href="pl_mpdt-pos-NUM.html">NUM</a></tt> (716; 2% instances), <tt><a href="pl_mpdt-pos-X.html">X</a></tt> (32; 0% instances).

### `NUM`

716 <tt><a href="pl_mpdt-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumForm`.

The most frequent other feature values with which `NUM` and `NumForm` co-occurred: <tt><a href="pl_mpdt-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (440; 61%), <tt><a href="pl_mpdt-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (440; 61%), <tt><a href="pl_mpdt-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (440; 61%).

`NUM` tokens may have the following values of `NumForm`:

* `Digit` (439; 61% of non-empty `NumForm`): <em>2, 3, 4, 1, 10, 7, 5, 12, 8, 6</em>
* `Word` (277; 39% of non-empty `NumForm`): <em>dwa, dwie, trzy, pięć, cztery, dwóch, sto, dwoje, dwu, dwadzieścia</em>

`NumForm` seems to be **lexical feature** of `NUM`. 100% lemmas (187) occur only with one value of `NumForm`.

### `X`

32 <tt><a href="pl_mpdt-pos-X.html">X</a></tt> tokens (65% of all `X` tokens) have a non-empty value of `NumForm`.

The most frequent other feature values with which `X` and `NumForm` co-occurred: <tt><a href="pl_mpdt-feat-Foreign.html">Foreign</a></tt><tt>=EMPTY</tt> (32; 100%).

`X` tokens may have the following values of `NumForm`:

* `Roman` (32; 100% of non-empty `NumForm`): <em>I, III, V, II, XXIV, IV, XIII, XIV, LXIV, VI</em>

`NumForm` seems to be **lexical feature** of `X`. 100% lemmas (16) occur only with one value of `NumForm`.

## Relations with Agreement in `NumForm`

The 10 most frequent relations where parent and child node agree in `NumForm`:
<tt>NUM --[<tt><a href="pl_mpdt-dep-conj.html">conj</a></tt>]--> NUM</tt> (33; 100%),
<tt>NUM --[<tt><a href="pl_mpdt-dep-flat.html">flat</a></tt>]--> NUM</tt> (18; 100%),
<tt>NUM --[<tt><a href="pl_mpdt-dep-amod.html">amod</a></tt>]--> NUM</tt> (8; 100%),
<tt>NUM --[<tt><a href="pl_mpdt-dep-appos.html">appos</a></tt>]--> NUM</tt> (1; 100%),
<tt>NUM --[<tt><a href="pl_mpdt-dep-list.html">list</a></tt>]--> NUM</tt> (1; 100%),
<tt>NUM --[<tt><a href="pl_mpdt-dep-nmod.html">nmod</a></tt>]--> NUM</tt> (1; 100%),
<tt>NUM --[<tt><a href="pl_mpdt-dep-obl-arg.html">obl:arg</a></tt>]--> NUM</tt> (1; 100%),
<tt>NUM --[<tt><a href="pl_mpdt-dep-obl-cmp.html">obl:cmp</a></tt>]--> NUM</tt> (1; 100%),
<tt>NUM --[<tt><a href="pl_mpdt-dep-orphan.html">orphan</a></tt>]--> NUM</tt> (1; 100%).

