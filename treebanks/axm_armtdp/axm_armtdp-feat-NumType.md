---
layout: base
title:  'Statistics of NumType in UD_Middle_Armenian-ArmTDP'
udver: '2'
---

## Treebank Statistics: UD_Middle_Armenian-ArmTDP: Features: `NumType`

This feature is universal.
It occurs with 1 different values: `Card`.

16 tokens (1%) have a non-empty value of `NumType`.
9 types (2%) occur at least once with a non-empty value of `NumType`.
9 lemmas (3%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="axm_armtdp-pos-NUM.html">NUM</a></tt> (16; 1% instances).

### `NUM`

16 <tt><a href="axm_armtdp-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (16; 100% of non-empty `NumType`): <em>2, Բ, երկու, երեք, 3, 4, Գ, ԳՃ, Դ</em>

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="axm_armtdp-dep-appos.html">appos</a></tt>]--> NUM</tt> (5; 100%).

