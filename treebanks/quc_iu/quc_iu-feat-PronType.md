---
layout: base
title:  'Statistics of PronType in UD_Kiche-IU'
udver: '2'
---

## Treebank Statistics: UD_Kiche-IU: Features: `PronType`

This feature is universal.
It occurs with 5 different values: `Art`, `Dem`, `Int`, `Prs`, `Rel`.

1602 tokens (16%) have a non-empty value of `PronType`.
54 types (2%) occur at least once with a non-empty value of `PronType`.
28 lemmas (2%) occur at least once with a non-empty value of `PronType`.
The feature is used with 4 part-of-speech tags: <tt><a href="quc_iu-pos-DET.html">DET</a></tt> (1335; 13% instances), <tt><a href="quc_iu-pos-PRON.html">PRON</a></tt> (209; 2% instances), <tt><a href="quc_iu-pos-PART.html">PART</a></tt> (39; 0% instances), <tt><a href="quc_iu-pos-ADV.html">ADV</a></tt> (19; 0% instances).

### `DET`

1335 <tt><a href="quc_iu-pos-DET.html">DET</a></tt> tokens (96% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="quc_iu-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (1246; 93%).

`DET` tokens may have the following values of `PronType`:

* `Art` (1335; 100% of non-empty `PronType`): <em>ri, le, jun, we, r, waʼ, ju, l</em>
* `EMPTY` (62): <em>taq, e</em>

### `PRON`

209 <tt><a href="quc_iu-pos-PRON.html">PRON</a></tt> tokens (95% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="quc_iu-feat-Polite.html">Polite</a></tt><tt>=EMPTY</tt> (178; 85%), <tt><a href="quc_iu-feat-Clitic.html">Clitic</a></tt><tt>=EMPTY</tt> (152; 73%), <tt><a href="quc_iu-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (126; 60%), <tt><a href="quc_iu-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (126; 60%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (40; 19% of non-empty `PronType`): <em>riʼ, laʼ, waʼ, weriʼ, we</em>
* `Int` (40; 19% of non-empty `PronType`): <em>jas, jachin, su, Jachike</em>
* `Prs` (112; 54% of non-empty `PronType`): <em>in, la, alaq, areʼ, at, are, e, oj, ix, Aʼre</em>
* `Rel` (17; 8% of non-empty `PronType`): <em>le, ri</em>
* `EMPTY` (10): <em>e</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (23) occur only with one value of `PronType`.

### `PART`

39 <tt><a href="quc_iu-pos-PART.html">PART</a></tt> tokens (5% of all `PART` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PART` and `PronType` co-occurred: <tt><a href="quc_iu-feat-Focus.html">Focus</a></tt><tt>=EMPTY</tt> (39; 100%), <tt><a href="quc_iu-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (39; 100%).

`PART` tokens may have the following values of `PronType`:

* `Int` (39; 100% of non-empty `PronType`): <em>la</em>
* `EMPTY` (791): <em>ik, kʼo, o, na, man, are, wi, k, oq, aʼ</em>

### `ADV`

19 <tt><a href="quc_iu-pos-ADV.html">ADV</a></tt> tokens (5% of all `ADV` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADV` and `PronType` co-occurred: <tt><a href="quc_iu-feat-AdvType.html">AdvType</a></tt><tt>=EMPTY</tt> (19; 100%).

`ADV` tokens may have the following values of `PronType`:

* `Int` (19; 100% of non-empty `PronType`): <em>jawi, jas, jampaʼ, jawijeʼ, Jampa, Jawchiʼ</em>
* `EMPTY` (375): <em>sibʼalaj, bʼi, kan, loq, kamik, chik, iwir, utz, chweʼq, na</em>

