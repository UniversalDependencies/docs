---
layout: base
title:  'Statistics of ExtPos in UD_Zaar-Autogramm'
udver: '2'
---

## Treebank Statistics: UD_Zaar-Autogramm: Features: `ExtPos`

This feature is language-specific.
It occurs with 6 different values: `ADJ`, `ADP`, `ADV`, `NOUN`, `SCONJ`, `VERB`.

335 tokens (2%) have a non-empty value of `ExtPos`.
185 types (7%) occur at least once with a non-empty value of `ExtPos`.
123 lemmas (8%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 7 part-of-speech tags: <tt><a href="say_autogramm-pos-VERB.html">VERB</a></tt> (263; 1% instances), <tt><a href="say_autogramm-pos-ADP.html">ADP</a></tt> (35; 0% instances), <tt><a href="say_autogramm-pos-SCONJ.html">SCONJ</a></tt> (26; 0% instances), <tt><a href="say_autogramm-pos-NOUN.html">NOUN</a></tt> (4; 0% instances), <tt><a href="say_autogramm-pos-X.html">X</a></tt> (4; 0% instances), <tt><a href="say_autogramm-pos-AUX.html">AUX</a></tt> (2; 0% instances), <tt><a href="say_autogramm-pos-ADV.html">ADV</a></tt> (1; 0% instances).

### `VERB`

263 <tt><a href="say_autogramm-pos-VERB.html">VERB</a></tt> tokens (7% of all `VERB` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `VERB` and `ExtPos` co-occurred: <tt><a href="say_autogramm-feat-Deixis.html">Deixis</a></tt><tt>=EMPTY</tt> (243; 92%).

`VERB` tokens may have the following values of `ExtPos`:

* `ADJ` (4; 2% of non-empty `ExtPos`): <em>kúŋni, məʃí, məʃíː, ɲâŋnês</em>
* `NOUN` (255; 97% of non-empty `ExtPos`): <em>ɗə́ːlɣə́n, láːpkə̂n, mbútkə̂n, yélɣə̂n, fíɣə̂n, láːpkə́nì, vyàːɣə̀n, ɬyáɣə̂n, ʧáːɣə̂n, ʧíɣə̂n</em>
* `SCONJ` (1; 0% of non-empty `ExtPos`): <em>naː</em>
* `VERB` (3; 1% of non-empty `ExtPos`): <em>wul, fí</em>

<table>
  <tr><th>Paradigm <i>fi</i></th><th><tt>NOUN</tt></th><th><tt>VERB</tt></th></tr>
  <tr><td><tt>_</tt></td><td></td><td><em>fí</em></td></tr>
  <tr><td><tt><tt><a href="say_autogramm-feat-VerbForm.html">VerbForm</a></tt><tt>=Vnoun</tt></tt></td><td><em>fíɣə̂n, fíɣə́nì</em></td><td></td></tr>
</table>

`ExtPos` seems to be **lexical feature** of `VERB`. 96% lemmas (95) occur only with one value of `ExtPos`.

### `ADP`

35 <tt><a href="say_autogramm-pos-ADP.html">ADP</a></tt> tokens (4% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (27; 77% of non-empty `ExtPos`): <em>á, nə́, teː, ɣá, kàmán, séː, ɗa, ɗan, hár</em>
* `ADV` (2; 6% of non-empty `ExtPos`): <em>kàmán, nə́</em>
* `SCONJ` (6; 17% of non-empty `ExtPos`): <em>ɗan, séː, nə́</em>

<table>
  <tr><th>Paradigm <i>nə́</i></th><th><tt>ADP</tt></th><th><tt>ADV</tt></th><th><tt>SCONJ</tt></th></tr>
  <tr><td><tt></tt></td><td><em>nə́</em></td><td><em>nə́</em></td><td><em>nə́</em></td></tr>
</table>

### `SCONJ`

26 <tt><a href="say_autogramm-pos-SCONJ.html">SCONJ</a></tt> tokens (3% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

`SCONJ` tokens may have the following values of `ExtPos`:

* `ADP` (2; 8% of non-empty `ExtPos`): <em>sábòːtu, tu</em>
* `SCONJ` (23; 88% of non-empty `ExtPos`): <em>séː, tu, kàmán, ín, ɗan, dàʧêːwáː, kóː, séy, ìdán, ɓanɗan</em>
* `VERB` (1; 4% of non-empty `ExtPos`): <em>tu</em>

<table>
  <tr><th>Paradigm <i>tu</i></th><th><tt>ADP</tt></th><th><tt>SCONJ</tt></th><th><tt>VERB</tt></th></tr>
  <tr><td><tt></tt></td><td><em>tu</em></td><td><em>tu</em></td><td><em>tu</em></td></tr>
</table>

### `NOUN`

4 <tt><a href="say_autogramm-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `NOUN` and `ExtPos` co-occurred: <tt><a href="say_autogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (4; 100%).

`NOUN` tokens may have the following values of `ExtPos`:

* `ADJ` (3; 75% of non-empty `ExtPos`): <em>gwaɬkay, ʃéːgè, ʒèy</em>
* `NOUN` (1; 25% of non-empty `ExtPos`): <em>naːkámʃi</em>

### `X`

4 <tt><a href="say_autogramm-pos-X.html">X</a></tt> tokens (2% of all `X` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `X` and `ExtPos` co-occurred: <tt><a href="say_autogramm-feat-Foreign.html">Foreign</a></tt><tt>=Yes</tt> (4; 100%).

`X` tokens may have the following values of `ExtPos`:

* `ADV` (4; 100% of non-empty `ExtPos`): <em>dǎː, gaba, hár, sosai</em>

### `AUX`

2 <tt><a href="say_autogramm-pos-AUX.html">AUX</a></tt> tokens (0% of all `AUX` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `AUX` and `ExtPos` co-occurred: <tt><a href="say_autogramm-feat-Aspect.html">Aspect</a></tt><tt>=EMPTY</tt> (2; 100%), <tt><a href="say_autogramm-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (2; 100%), <tt><a href="say_autogramm-feat-Number.html">Number</a></tt><tt>=Sing</tt> (2; 100%), <tt><a href="say_autogramm-feat-Tense.html">Tense</a></tt><tt>=EMPTY</tt> (2; 100%).

`AUX` tokens may have the following values of `ExtPos`:

* `ADV` (2; 100% of non-empty `ExtPos`): <em>mi, ʧi</em>

### `ADV`

1 <tt><a href="say_autogramm-pos-ADV.html">ADV</a></tt> tokens (0% of all `ADV` tokens) have a non-empty value of `ExtPos`.

`ADV` tokens may have the following values of `ExtPos`:

* `ADV` (1; 100% of non-empty `ExtPos`): <em>ngaːláy</em>

## Relations with Agreement in `ExtPos`

The 10 most frequent relations where parent and child node agree in `ExtPos`:
<tt>NOUN --[<tt><a href="say_autogramm-dep-compound.html">compound</a></tt>]--> NOUN</tt> (1; 100%),
<tt>VERB --[<tt><a href="say_autogramm-dep-nmod.html">nmod</a></tt>]--> VERB</tt> (1; 100%).

