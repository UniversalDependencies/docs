---
layout: base
title:  'Statistics of Style in UD_Finnish'
udver: '2'
---

## Treebank Statistics: UD_Finnish: Features: `Style`

This feature is language-specific.
It occurs with 2 different values: `Arch`, `Coll`.

1593 tokens (1%) have a non-empty value of `Style`.
774 types (1%) occur at least once with a non-empty value of `Style`.
461 lemmas (2%) occur at least once with a non-empty value of `Style`.
The feature is used with 12 part-of-speech tags: <tt><a href="fi-pos-PRON.html">PRON</a></tt> (371; 0% instances), <tt><a href="fi-pos-VERB.html">VERB</a></tt> (366; 0% instances), <tt><a href="fi-pos-ADV.html">ADV</a></tt> (271; 0% instances), <tt><a href="fi-pos-AUX.html">AUX</a></tt> (172; 0% instances), <tt><a href="fi-pos-NOUN.html">NOUN</a></tt> (158; 0% instances), <tt><a href="fi-pos-SCONJ.html">SCONJ</a></tt> (115; 0% instances), <tt><a href="fi-pos-ADJ.html">ADJ</a></tt> (63; 0% instances), <tt><a href="fi-pos-CCONJ.html">CCONJ</a></tt> (34; 0% instances), <tt><a href="fi-pos-ADP.html">ADP</a></tt> (13; 0% instances), <tt><a href="fi-pos-NUM.html">NUM</a></tt> (13; 0% instances), <tt><a href="fi-pos-PROPN.html">PROPN</a></tt> (10; 0% instances), <tt><a href="fi-pos-INTJ.html">INTJ</a></tt> (7; 0% instances).

### `PRON`

371 <tt><a href="fi-pos-PRON.html">PRON</a></tt> tokens (3% of all `PRON` tokens) have a non-empty value of `Style`.

The most frequent other feature values with which `PRON` and `Style` co-occurred: <tt><a href="fi-feat-Number.html">Number</a></tt><tt>=Sing</tt> (344; 93%), <tt><a href="fi-feat-PronType.html">PronType</a></tt><tt>=Prs</tt> (277; 75%), <tt><a href="fi-feat-Case.html">Case</a></tt><tt>=Nom</tt> (198; 53%).

`PRON` tokens may have the following values of `Style`:

* `Coll` (371; 100% of non-empty `Style`): <em>mä, mun, sä, tää, mua, mulla, sun, mulle, jotku, musta</em>

`Style` seems to be **lexical feature** of `PRON`. 100% lemmas (18) occur only with one value of `Style`.

### `VERB`

366 <tt><a href="fi-pos-VERB.html">VERB</a></tt> tokens (1% of all `VERB` tokens) have a non-empty value of `Style`.

The most frequent other feature values with which `VERB` and `Style` co-occurred: <tt><a href="fi-feat-Voice.html">Voice</a></tt><tt>=Act</tt> (339; 93%), <tt><a href="fi-feat-InfForm.html">InfForm</a></tt><tt>=EMPTY</tt> (322; 88%), <tt><a href="fi-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (299; 82%), <tt><a href="fi-feat-PartForm.html">PartForm</a></tt><tt>=EMPTY</tt> (299; 82%), <tt><a href="fi-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (280; 77%), <tt><a href="fi-feat-VerbForm.html">VerbForm</a></tt><tt>=Fin</tt> (255; 70%), <tt><a href="fi-feat-Number.html">Number</a></tt><tt>=Sing</tt> (204; 56%), <tt><a href="fi-feat-Mood.html">Mood</a></tt><tt>=Ind</tt> (200; 55%).

`VERB` tokens may have the following values of `Style`:

* `Arch` (1; 0% of non-empty `Style`): <em>tuta</em>
* `Coll` (365; 100% of non-empty `Style`): <em>tajuu, emmä, sano, tuu, alko, eikä, nähtiin, saanu, sanoo, alkoi</em>

`Style` seems to be **lexical feature** of `VERB`. 100% lemmas (170) occur only with one value of `Style`.

### `ADV`

271 <tt><a href="fi-pos-ADV.html">ADV</a></tt> tokens (2% of all `ADV` tokens) have a non-empty value of `Style`.

The most frequent other feature values with which `ADV` and `Style` co-occurred: <tt><a href="fi-feat-Derivation.html">Derivation</a></tt><tt>=EMPTY</tt> (258; 95%).

`ADV` tokens may have the following values of `Style`:

* `Coll` (271; 100% of non-empty `Style`): <em>sit, ni, just, niinku, nii, sitte, sillon, ku, siks, paljo</em>

`Style` seems to be **lexical feature** of `ADV`. 100% lemmas (81) occur only with one value of `Style`.

### `AUX`

172 <tt><a href="fi-pos-AUX.html">AUX</a></tt> tokens (1% of all `AUX` tokens) have a non-empty value of `Style`.

The most frequent other feature values with which `AUX` and `Style` co-occurred: <tt><a href="fi-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (155; 90%), <tt><a href="fi-feat-Voice.html">Voice</a></tt><tt>=Act</tt> (151; 88%), <tt><a href="fi-feat-VerbForm.html">VerbForm</a></tt><tt>=Fin</tt> (145; 84%), <tt><a href="fi-feat-Mood.html">Mood</a></tt><tt>=Ind</tt> (94; 55%).

`AUX` tokens may have the following values of `Style`:

* `Coll` (172; 100% of non-empty `Style`): <em>on, ei, ollu, oli, oo, oon, olis, vois, ois, voinu</em>

### `NOUN`

158 <tt><a href="fi-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `Style`.

The most frequent other feature values with which `NOUN` and `Style` co-occurred: <tt><a href="fi-feat-Derivation.html">Derivation</a></tt><tt>=EMPTY</tt> (149; 94%), <tt><a href="fi-feat-Number.html">Number</a></tt><tt>=Sing</tt> (126; 80%).

`NOUN` tokens may have the following values of `Style`:

* `Arch` (1; 1% of non-empty `Style`): <em>pääskysein</em>
* `Coll` (157; 99% of non-empty `Style`): <em>ajas, alakerras, daideilijaa, kauppaa, kelloo, nurtsi, roskiksetki, stevarin, tilan, veres</em>

`Style` seems to be **lexical feature** of `NOUN`. 100% lemmas (130) occur only with one value of `Style`.

### `SCONJ`

115 <tt><a href="fi-pos-SCONJ.html">SCONJ</a></tt> tokens (3% of all `SCONJ` tokens) have a non-empty value of `Style`.

`SCONJ` tokens may have the following values of `Style`:

* `Coll` (115; 100% of non-empty `Style`): <em>et, ku, ett, nii, kun, niinku, vaikk, Mut, Vaik, enneku</em>

`Style` seems to be **lexical feature** of `SCONJ`. 100% lemmas (10) occur only with one value of `Style`.

### `ADJ`

63 <tt><a href="fi-pos-ADJ.html">ADJ</a></tt> tokens (0% of all `ADJ` tokens) have a non-empty value of `Style`.

The most frequent other feature values with which `ADJ` and `Style` co-occurred: <tt><a href="fi-feat-Degree.html">Degree</a></tt><tt>=Pos</tt> (53; 84%), <tt><a href="fi-feat-Number.html">Number</a></tt><tt>=Sing</tt> (46; 73%).

`ADJ` tokens may have the following values of `Style`:

* `Arch` (1; 2% of non-empty `Style`): <em>Wanhan</em>
* `Coll` (62; 98% of non-empty `Style`): <em>sellanen, oikee, ekasta, sellasia, sellasii, sellasta, semmosia, Mimmonen, Punanen, Törkeet</em>

`Style` seems to be **lexical feature** of `ADJ`. 100% lemmas (36) occur only with one value of `Style`.

### `CCONJ`

34 <tt><a href="fi-pos-CCONJ.html">CCONJ</a></tt> tokens (0% of all `CCONJ` tokens) have a non-empty value of `Style`.

`CCONJ` tokens may have the following values of `Style`:

* `Coll` (34; 100% of non-empty `Style`): <em>mut, ku, niinku</em>

### `ADP`

13 <tt><a href="fi-pos-ADP.html">ADP</a></tt> tokens (0% of all `ADP` tokens) have a non-empty value of `Style`.

The most frequent other feature values with which `ADP` and `Style` co-occurred: <tt><a href="fi-feat-AdpType.html">AdpType</a></tt><tt>=Post</tt> (13; 100%).

`ADP` tokens may have the following values of `Style`:

* `Coll` (13; 100% of non-empty `Style`): <em>kans, kaa, kanssas, jälkee, kanssa, luokses, luom, taakse</em>

### `NUM`

13 <tt><a href="fi-pos-NUM.html">NUM</a></tt> tokens (0% of all `NUM` tokens) have a non-empty value of `Style`.

The most frequent other feature values with which `NUM` and `Style` co-occurred: <tt><a href="fi-feat-NumType.html">NumType</a></tt><tt>=Card</tt> (13; 100%), <tt><a href="fi-feat-Number.html">Number</a></tt><tt>=Sing</tt> (13; 100%), <tt><a href="fi-feat-Case.html">Case</a></tt><tt>=Nom</tt> (10; 77%).

`NUM` tokens may have the following values of `Style`:

* `Coll` (13; 100% of non-empty `Style`): <em>yks, kaks, kahteenkyt, kakssattoo, kolmee, viis</em>

### `PROPN`

10 <tt><a href="fi-pos-PROPN.html">PROPN</a></tt> tokens (0% of all `PROPN` tokens) have a non-empty value of `Style`.

The most frequent other feature values with which `PROPN` and `Style` co-occurred: <tt><a href="fi-feat-Number.html">Number</a></tt><tt>=Sing</tt> (10; 100%).

`PROPN` tokens may have the following values of `Style`:

* `Coll` (10; 100% of non-empty `Style`): <em>Ailii, Eve, Hiakal, Improssa, Karriki, Mollaa, Paavokaa, Runkuks, Stiletiks, Stilsuks</em>

`Style` seems to be **lexical feature** of `PROPN`. 100% lemmas (10) occur only with one value of `Style`.

### `INTJ`

7 <tt><a href="fi-pos-INTJ.html">INTJ</a></tt> tokens (3% of all `INTJ` tokens) have a non-empty value of `Style`.

`INTJ` tokens may have the following values of `Style`:

* `Coll` (7; 100% of non-empty `Style`): <em>tota, Kamooon, vittuu</em>

## Relations with Agreement in `Style`

The 10 most frequent relations where parent and child node agree in `Style`:
<tt>PRON --[<tt><a href="fi-dep-mark.html">mark</a></tt>]--> SCONJ</tt> (6; 55%),
<tt>VERB --[<tt><a href="fi-dep-mark.html">mark</a></tt>]--> ADV</tt> (6; 67%),
<tt>ADV --[<tt><a href="fi-dep-ccomp.html">ccomp</a></tt>]--> VERB</tt> (3; 60%),
<tt>PRON --[<tt><a href="fi-dep-obl.html">obl</a></tt>]--> NOUN</tt> (3; 60%),
<tt>ADV --[<tt><a href="fi-dep-fixed.html">fixed</a></tt>]--> SCONJ</tt> (2; 67%),
<tt>ADV --[<tt><a href="fi-dep-orphan.html">orphan</a></tt>]--> ADV</tt> (2; 67%),
<tt>VERB --[<tt><a href="fi-dep-xcomp-ds.html">xcomp:ds</a></tt>]--> NOUN</tt> (2; 100%),
<tt>VERB --[<tt><a href="fi-dep-xcomp-ds.html">xcomp:ds</a></tt>]--> PROPN</tt> (2; 67%),
<tt>ADV --[<tt><a href="fi-dep-case.html">case</a></tt>]--> ADP</tt> (1; 100%),
<tt>ADV --[<tt><a href="fi-dep-conj.html">conj</a></tt>]--> ADV</tt> (1; 100%).

