---
layout: base
title:  'Statistics of Voice in UD_Marathi'
udver: '2'
---

## Treebank Statistics: UD_Marathi: Features: `Voice`

This feature is universal.
It occurs with 1 different values: `Cau`.

2 tokens (0%) have a non-empty value of `Voice`.
2 types (0%) occur at least once with a non-empty value of `Voice`.
2 lemmas (0%) occur at least once with a non-empty value of `Voice`.
The feature is used with 1 part-of-speech tags: <tt><a href="mr-pos-VERB.html">VERB</a></tt> (2; 0% instances).

### `VERB`

2 <tt><a href="mr-pos-VERB.html">VERB</a></tt> tokens (0% of all `VERB` tokens) have a non-empty value of `Voice`.

The most frequent other feature values with which `VERB` and `Voice` co-occurred: <tt><a href="mr-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (2; 100%).

`VERB` tokens may have the following values of `Voice`:

* `Cau` (2; 100% of non-empty `Voice`): बघवत बसविले
* `EMPTY` (701): म्हणाला _ आला गेला आली म्हणाली येत घेऊन बसला दे

