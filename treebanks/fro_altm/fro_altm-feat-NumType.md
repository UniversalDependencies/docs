---
layout: base
title:  'Statistics of NumType in UD_Old_French-ALTM'
udver: '2'
---

## Treebank Statistics: UD_Old_French-ALTM: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

103 tokens (1%) have a non-empty value of `NumType`.
29 types (2%) occur at least once with a non-empty value of `NumType`.
19 lemmas (2%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="fro_altm-pos-NUM.html">NUM</a></tt> (85; 1% instances), <tt><a href="fro_altm-pos-ADJ.html">ADJ</a></tt> (18; 0% instances).

### `NUM`

85 <tt><a href="fro_altm-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (85; 100% of non-empty `NumType`): <em>ij., iij., vj., deus, iiij., x., c., xxj., xx., ix.</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (16) occur only with one value of `NumType`.

### `ADJ`

18 <tt><a href="fro_altm-pos-ADJ.html">ADJ</a></tt> tokens (7% of all `ADJ` tokens) have a non-empty value of `NumType`.

`ADJ` tokens may have the following values of `NumType`:

* `Ord` (18; 100% of non-empty `NumType`): <em>premier, premiere, premieres, première, segont, tiers</em>
* `EMPTY` (251): <em>autre, dit, nouvele, quite, prochain, tel, chief, ainsné, desavenant, voir</em>

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="fro_altm-dep-conj.html">conj</a></tt>]--> NUM</tt> (3; 100%).

