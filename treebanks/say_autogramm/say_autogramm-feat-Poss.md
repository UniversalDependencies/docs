---
layout: base
title:  'Statistics of Poss in UD_Zaar-Autogramm'
udver: '2'
---

## Treebank Statistics: UD_Zaar-Autogramm: Features: `Poss`

This feature is universal.
It occurs with 1 different values: `Yes`.

46 tokens (1%) have a non-empty value of `Poss`.
21 types (2%) occur at least once with a non-empty value of `Poss`.
10 lemmas (1%) occur at least once with a non-empty value of `Poss`.
The feature is used with 1 part-of-speech tags: <tt><a href="say_autogramm-pos-PRON.html">PRON</a></tt> (46; 1% instances).

### `PRON`

46 <tt><a href="say_autogramm-pos-PRON.html">PRON</a></tt> tokens (8% of all `PRON` tokens) have a non-empty value of `Poss`.

The most frequent other feature values with which `PRON` and `Poss` co-occurred: <tt><a href="say_autogramm-feat-PronType.html">PronType</a></tt><tt>=Prs</tt> (46; 100%), <tt><a href="say_autogramm-feat-Number.html">Number</a></tt><tt>=Sing</tt> (28; 61%), <tt><a href="say_autogramm-feat-Person.html">Person</a></tt><tt>=3</tt> (25; 54%).

`PRON` tokens may have the following values of `Poss`:

* `Yes` (46; 100% of non-empty `Poss`): <em>gòs, =tn, gə̀tn, =wàːsə̀ŋ, =wôpm, =wôpíː, =wôs, gòʃíː, =tíː, =wàːsə̀n</em>
* `EMPTY` (515): <em>=tə, =mí, =ʃí, ɣáy, =m, =tə̀, myâːn, =ɣə, ʧì, gíː</em>

`Poss` seems to be **lexical feature** of `PRON`. 100% lemmas (10) occur only with one value of `Poss`.

