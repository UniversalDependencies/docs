

--------------------------------------------------------------------------------

## Treebank Statistics (UD_Italian)

This feature is universal.
It occurs with 2 different values: `Fem`, `Masc`.

111668 tokens (41%) have a non-empty value of `Gender`.
14337 types (54%) occur at least once with a non-empty value of `Gender`.
9554 lemmas (53%) occur at least once with a non-empty value of `Gender`.
The feature is used with 8 part-of-speech tags: [it-pos/NOUN]() (51837; 19% instances), [it-pos/DET]() (37256; 14% instances), [it-pos/ADJ]() (10926; 4% instances), [it-pos/VERB]() (8086; 3% instances), [it-pos/PRON]() (3006; 1% instances), [it-pos/AUX]() (555; 0% instances), [it-pos/ADP]() (1; 0% instances), [it-pos/PROPN]() (1; 0% instances).

### `NOUN`

51837 [it-pos/NOUN]() tokens (97% of all `NOUN` tokens) have a non-empty value of `Gender`.

The most frequent other feature values with which `NOUN` and `Gender` co-occurred: <tt><a href="Number.html">Number</a>=Sing</tt> (34178; 66%).

`NOUN` tokens may have the following values of `Gender`:

* `Fem` (23451; 45% of non-empty `Gender`): <em>città, parte, società, legge, proprietà, attività, servitù, autorità, commissione, opere</em>
* `Masc` (28386; 55% of non-empty `Gender`): <em>anni, fondo, diritto, presidente, proprietario, anno, caso, art., stato, modo</em>
* `EMPTY` (1791): <em>presidente, onorevole, grazie, enfiteuta, leader, fronte, abitanti, partecipanti, componenti, capitale</em>

<table>
  <tr><th>Paradigm <i>proprietario</i></th><th><tt>Masc</tt></th><th><tt>Fem</tt></th></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Sing</tt></td><td><em>proprietario</em></td><td><em>proprietaria</em></td></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Plur</tt></td><td><em>proprietari</em></td><td></td></tr>
</table>

`Gender` seems to be **lexical feature** of `NOUN`. 98% lemmas (6355) occur only with one value of `Gender`.

### `DET`

37256 [it-pos/DET]() tokens (85% of all `DET` tokens) have a non-empty value of `Gender`.

The most frequent other feature values with which `DET` and `Gender` co-occurred: <tt><a href="PronType.html">PronType</a>=Art</tt> (33805; 91%), <tt><a href="Definite.html">Definite</a>=Def</tt> (29462; 79%), <tt><a href="Number.html">Number</a>=Sing</tt> (26484; 71%).

`DET` tokens may have the following values of `Gender`:

* `Fem` (15782; 42% of non-empty `Gender`): <em>la, le, una, sua, un', questa, sue, queste, tutte, quante</em>
* `Masc` (21474; 58% of non-empty `Gender`): <em>il, i, un, gli, lo, suo, questo, tutti, suoi, quanti</em>
* `EMPTY` (6445): <em>l', quale, ogni, che, loro, qualche, tale, qualsiasi, tali, quali</em>

<table>
  <tr><th>Paradigm <i>il</i></th><th><tt>Masc</tt></th><th><tt>Fem</tt></th></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Sing</tt></td><td><em>il, lo, l', l’, lu</em></td><td><em>la, l', le, il</em></td></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Plur</tt></td><td><em>i, gli, il</em></td><td><em>le</em></td></tr>
</table>

### `ADJ`

10926 [it-pos/ADJ]() tokens (63% of all `ADJ` tokens) have a non-empty value of `Gender`.

The most frequent other feature values with which `ADJ` and `Gender` co-occurred: <tt><a href="Number.html">Number</a>=Sing</tt> (7377; 68%).

`ADJ` tokens may have the following values of `Gender`:

* `Fem` (4956; 45% of non-empty `Gender`): <em>prima, stessa, altra, altre, italiana, economica, alta, nuova, giudiziaria, nuove</em>
* `Masc` (5970; 55% of non-empty `Gender`): <em>primo, altri, altro, stesso, nuovo, vero, secondo, pubblico, europeo, alto</em>
* `EMPTY` (6459): <em>grande, presente, comune, ex, maggiore, possibile, sociale, nazionale, internazionale, seguenti</em>

<table>
  <tr><th>Paradigm <i>primo</i></th><th><tt>Masc</tt></th><th><tt>Fem</tt></th></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Sing</tt></td><td><em>primo</em></td><td><em>prima</em></td></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Sing|<a href="NumType.html">NumType</a>=Ord</tt></td><td><em>primo</em></td><td><em>prima</em></td></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Plur</tt></td><td></td><td><em>prime</em></td></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Plur|<a href="NumType.html">NumType</a>=Ord</tt></td><td><em>primi</em></td><td><em>prime</em></td></tr>
</table>

### `VERB`

8086 [it-pos/VERB]() tokens (30% of all `VERB` tokens) have a non-empty value of `Gender`.

The most frequent other feature values with which `VERB` and `Gender` co-occurred: <tt><a href="Person.html">Person</a>=EMPTY</tt> (8086; 100%), <tt><a href="Mood.html">Mood</a>=EMPTY</tt> (8086; 100%), <tt><a href="Tense.html">Tense</a>=Past</tt> (8085; 100%), <tt><a href="VerbForm.html">VerbForm</a>=Part</tt> (8083; 100%), <tt><a href="Number.html">Number</a>=Sing</tt> (5881; 73%).

`VERB` tokens may have the following values of `Gender`:

* `Fem` (2511; 31% of non-empty `Gender`): <em>stata, fatta, stabilite, dovuta, fatte, vista, considerata, costituita, fondata, chiamata</em>
* `Masc` (5575; 69% of non-empty `Gender`): <em>fatto, visto, stato, tenuto, avuto, detto, dato, messo, chiamato, scritto</em>
* `EMPTY` (18429): <em>è, sono, ha, era, essere, trova, far, fare, hanno, fa</em>

<table>
  <tr><th>Paradigm <i>essere</i></th><th><tt>Masc</tt></th><th><tt>Fem</tt></th></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Sing</tt></td><td><em>stato</em></td><td><em>stata</em></td></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Plur</tt></td><td><em>stati</em></td><td><em>state</em></td></tr>
</table>

### `PRON`

3006 [it-pos/PRON]() tokens (28% of all `PRON` tokens) have a non-empty value of `Gender`.

The most frequent other feature values with which `PRON` and `Gender` co-occurred: <tt><a href="Number.html">Number</a>=Sing</tt> (2203; 73%), <tt><a href="Person.html">Person</a>=EMPTY</tt> (1881; 63%).

`PRON` tokens may have the following values of `Gender`:

* `Fem` (733; 24% of non-empty `Gender`): <em>la, le, quella, quelle, una, questa, essa, esse, altra, lei</em>
* `Masc` (2273; 76% of non-empty `Gender`): <em>lo, quello, uno, questo, gli, li, lui, tutto, ciò, quanto</em>
* `EMPTY` (7828): <em>si, che, chi, cui, ci, ne, qual, mi, c', quale</em>

<table>
  <tr><th>Paradigm <i>lo</i></th><th><tt>Masc</tt></th><th><tt>Fem</tt></th></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Sing</tt></td><td><em>lo, gli</em></td><td><em>la</em></td></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Plur</tt></td><td><em>li</em></td><td><em>le</em></td></tr>
  <tr><td><tt></tt></td><td></td><td><em>le</em></td></tr>
</table>

### `AUX`

555 [it-pos/AUX]() tokens (7% of all `AUX` tokens) have a non-empty value of `Gender`.

The most frequent other feature values with which `AUX` and `Gender` co-occurred: <tt><a href="VerbForm.html">VerbForm</a>=Part</tt> (555; 100%), <tt><a href="Tense.html">Tense</a>=Past</tt> (555; 100%), <tt><a href="Person.html">Person</a>=EMPTY</tt> (555; 100%), <tt><a href="Mood.html">Mood</a>=EMPTY</tt> (555; 100%), <tt><a href="Number.html">Number</a>=Sing</tt> (406; 73%).

`AUX` tokens may have the following values of `Gender`:

* `Fem` (166; 30% of non-empty `Gender`): <em>stata, state, potuta, andata</em>
* `Masc` (389; 70% of non-empty `Gender`): <em>stato, stati, dovuto, potuto, voluto, andato, potuti, saputo</em>
* `EMPTY` (6888): <em>è, ha, sono, può, hanno, essere, deve, possono, era, viene</em>

<table>
  <tr><th>Paradigm <i>essere</i></th><th><tt>Masc</tt></th><th><tt>Fem</tt></th></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Sing</tt></td><td><em>stato</em></td><td><em>stata</em></td></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Plur</tt></td><td><em>stati</em></td><td><em>state</em></td></tr>
</table>

### `ADP`

1 [it-pos/ADP]() tokens (0% of all `ADP` tokens) have a non-empty value of `Gender`.

`ADP` tokens may have the following values of `Gender`:

* `Masc` (1; 100% of non-empty `Gender`): <em>du</em>
* `EMPTY` (41430): <em>di, a, in, da, per, con, su, come, ad, d'</em>

### `PROPN`

1 [it-pos/PROPN]() tokens (0% of all `PROPN` tokens) have a non-empty value of `Gender`.

`PROPN` tokens may have the following values of `Gender`:

* `Fem` (1; 100% of non-empty `Gender`): <em>hye</em>
* `EMPTY` (13400): <em>Shakespeare, Balzac, Italia, stati, Europa, San, Uniti, Albania, Marco, Roma</em>

## Relations with Agreement in `Gender`

The 10 most frequent relations where parent and child node agree in `Gender`:
<tt>NOUN --[<a href="../dep/det.html">det</a>]--> DET</tt> (30351; 83%),
<tt>NOUN --[<a href="../dep/amod.html">amod</a>]--> ADJ</tt> (8590; 63%),
<tt>NOUN --[<a href="../dep/conj.html">conj</a>]--> NOUN</tt> (2199; 55%),
<tt>NOUN --[<a href="../dep/acl.html">acl</a>]--> VERB</tt> (1963; 65%),
<tt>VERB --[<a href="../dep/nsubjpass.html">nsubjpass</a>]--> NOUN</tt> (1409; 95%),
<tt>NOUN --[<a href="../dep/det:poss.html">det:poss</a>]--> DET</tt> (1393; 80%),
<tt>ADJ --[<a href="../dep/conj.html">conj</a>]--> ADJ</tt> (329; 52%),
<tt>ADJ --[<a href="../dep/nsubj.html">nsubj</a>]--> NOUN</tt> (310; 55%),
<tt>NOUN --[<a href="../dep/det:predet.html">det:predet</a>]--> DET</tt> (291; 96%),
<tt>PRON --[<a href="../dep/nmod.html">nmod</a>]--> NOUN</tt> (257; 62%).

