---
layout: base
title:  'Statistics of NumType in UD_Scottish_Gaelic-ARCOSG'
udver: '2'
---

## Treebank Statistics: UD_Scottish_Gaelic-ARCOSG: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

1027 tokens (1%) have a non-empty value of `NumType`.
154 types (2%) occur at least once with a non-empty value of `NumType`.
131 lemmas (2%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="gd_arcosg-pos-NUM.html">NUM</a></tt> (1027; 1% instances).

### `NUM`

1027 <tt><a href="gd_arcosg-pos-NUM.html">NUM</a></tt> tokens (98% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="gd_arcosg-feat-NumForm.html">NumForm</a></tt><tt>=Word</tt> (907; 88%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (919; 89% of non-empty `NumType`): <em>aon, dà, deug, trì, fhichead, fichead, ceithir, seachd, dhà, mìle</em>
* `Ord` (108; 11% of non-empty `NumType`): <em>chiad, cheud, dàrna, naodhamh, ochdamh, t-seachdamh, naoidheamh, treas, dara, ceathramh</em>
* `EMPTY` (21): <em>m, 1999, and, aon, eighteen, eighty, fifty, five, forty-thousand, hundred</em>

<table>
  <tr><th>Paradigm <i>ciad</i></th><th><tt>Card</tt></th><th><tt>Ord</tt></th></tr>
  <tr><td><tt></tt></td><td><em>chiad</em></td><td><em>chiad, cheud</em></td></tr>
</table>

`NumType` seems to be **lexical feature** of `NUM`. 98% lemmas (129) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="gd_arcosg-dep-conj.html">conj</a></tt>]--> NUM</tt> (46; 100%),
<tt>NUM --[<tt><a href="gd_arcosg-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (45; 92%),
<tt>NUM --[<tt><a href="gd_arcosg-dep-compound.html">compound</a></tt>]--> NUM</tt> (13; 100%),
<tt>NUM --[<tt><a href="gd_arcosg-dep-nmod.html">nmod</a></tt>]--> NUM</tt> (5; 100%),
<tt>NUM --[<tt><a href="gd_arcosg-dep-flat.html">flat</a></tt>]--> NUM</tt> (3; 100%),
<tt>NUM --[<tt><a href="gd_arcosg-dep-parataxis.html">parataxis</a></tt>]--> NUM</tt> (1; 100%),
<tt>NUM --[<tt><a href="gd_arcosg-dep-reparandum.html">reparandum</a></tt>]--> NUM</tt> (1; 100%).

