---
layout: base
title:  'Statistics of Definite in UD_Turkish-IMST'
udver: '2'
---

## Treebank Statistics: UD_Turkish-IMST: Features: `Definite`

This feature is universal.
It occurs with 1 different values: `Ind`.

1031 tokens (2%) have a non-empty value of `Definite`.
6 types (0%) occur at least once with a non-empty value of `Definite`.
1 lemmas (0%) occur at least once with a non-empty value of `Definite`.
The feature is used with 1 part-of-speech tags: <tt><a href="tr_imst-pos-DET.html">DET</a></tt> (1031; 2% instances).

### `DET`

1031 <tt><a href="tr_imst-pos-DET.html">DET</a></tt> tokens (50% of all `DET` tokens) have a non-empty value of `Definite`.

The most frequent other feature values with which `DET` and `Definite` co-occurred: <tt><a href="tr_imst-feat-PronType.html">PronType</a></tt><tt>=Art</tt> (1031; 100%).

`DET` tokens may have the following values of `Definite`:

* `Ind` (1031; 100% of non-empty `Definite`): <em>bir, birer, ,bir, bi, biri</em>
* `EMPTY` (1012): <em>bu, o, her, hiçbir, tüm, birkaç, şu, bazı, birçok, kimi</em>

## Relations with Agreement in `Definite`

The 10 most frequent relations where parent and child node agree in `Definite`:
<tt>DET --[<tt><a href="tr_imst-dep-det.html">det</a></tt>]--> DET</tt> (2; 67%).

