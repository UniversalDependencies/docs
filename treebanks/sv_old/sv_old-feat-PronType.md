---
layout: base
title:  'Statistics of PronType in UD_Swedish-Old'
udver: '2'
---

## Treebank Statistics: UD_Swedish-Old: Features: `PronType`

This feature is universal.
It occurs with 8 different values: `Art`, `Dem`, `Ind`, `Int`, `Neg`, `Prs`, `Rel`, `Tot`.

75 tokens (15%) have a non-empty value of `PronType`.
35 types (14%) occur at least once with a non-empty value of `PronType`.
17 lemmas (9%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="sv_old-pos-PRON.html">PRON</a></tt> (66; 13% instances), <tt><a href="sv_old-pos-DET.html">DET</a></tt> (8; 2% instances), <tt><a href="sv_old-pos-ADV.html">ADV</a></tt> (1; 0% instances).

### `PRON`

66 <tt><a href="sv_old-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="sv_old-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (58; 88%), <tt><a href="sv_old-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (37; 56%), <tt><a href="sv_old-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (36; 55%).

`PRON` tokens may have the following values of `PronType`:

* `Art` (1; 2% of non-empty `PronType`): <em>en</em>
* `Ind` (6; 9% of non-empty `PronType`): <em>annan, Annat, adhra, adhrom, andra</em>
* `Neg` (1; 2% of non-empty `PronType`): <em>enghin</em>
* `Prs` (44; 67% of non-empty `PronType`): <em>han, thet, thy, honum, hans, hon, sin, sina, hona, mei</em>
* `Rel` (5; 8% of non-empty `PronType`): <em>som</em>
* `Tot` (9; 14% of non-empty `PronType`): <em>alt, alla, allum, allæ, badha, hwan, hwario</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (15) occur only with one value of `PronType`.

### `DET`

8 <tt><a href="sv_old-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="sv_old-feat-Number.html">Number</a></tt><tt>=Sing</tt> (6; 75%), <tt><a href="sv_old-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (5; 63%).

`DET` tokens may have the following values of `PronType`:

* `Art` (5; 63% of non-empty `PronType`): <em>tha, then, thera, thæs</em>
* `Dem` (3; 38% of non-empty `PronType`): <em>thenne, thesso</em>

### `ADV`

1 <tt><a href="sv_old-pos-ADV.html">ADV</a></tt> tokens (2% of all `ADV` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADV` and `PronType` co-occurred: <tt><a href="sv_old-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (1; 100%).

`ADV` tokens may have the following values of `PronType`:

* `Int` (1; 100% of non-empty `PronType`): <em>huru</em>
* `EMPTY` (47): <em>tha, siden, ther, wæl, in, swa, fram, Aldrik, Hær, Sidhen</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="sv_old-dep-conj.html">conj</a></tt>]--> PRON</tt> (1; 100%).

