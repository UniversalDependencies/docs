---
layout: base
title:  'Statistics of Gender in UD_Xavante-XDT'
udver: '2'
---

## Treebank Statistics: UD_Xavante-XDT: Features: `Gender`

This feature is universal.
It occurs with 2 different values: `Fem`, `Masc`.

2 tokens (0%) have a non-empty value of `Gender`.
2 types (0%) occur at least once with a non-empty value of `Gender`.
1 lemmas (0%) occur at least once with a non-empty value of `Gender`.
The feature is used with 1 part-of-speech tags: <tt><a href="xav_xdt-pos-X.html">X</a></tt> (2; 0% instances).

### `X`

2 <tt><a href="xav_xdt-pos-X.html">X</a></tt> tokens (3% of all `X` tokens) have a non-empty value of `Gender`.

The most frequent other feature values with which `X` and `Gender` co-occurred: <tt><a href="xav_xdt-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (2; 100%).

`X` tokens may have the following values of `Gender`:

* `Fem` (1; 50% of non-empty `Gender`): <em>Maze</em>
* `Masc` (1; 50% of non-empty `Gender`): <em>Mare</em>
* `EMPTY` (70): <em>di, wa'wa, ni, norĩ, 're, wamhã, zaʔra, ʔre, 'wa, da</em>

<table>
  <tr><th>Paradigm <i>mare</i></th><th><tt>Masc</tt></th><th><tt>Fem</tt></th></tr>
  <tr><td><tt></tt></td><td><em>Mare</em></td><td><em>Maze</em></td></tr>
</table>

