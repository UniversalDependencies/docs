---
layout: base
title:  'Statistics of NumForm in UD_Latin-UDante'
udver: '2'
---

## Treebank Statistics: UD_Latin-UDante: Features: `NumForm`

This feature is language-specific.
It occurs with 2 different values: `Roman`, `Word`.

150 tokens (0%) have a non-empty value of `NumForm`.
35 types (0%) occur at least once with a non-empty value of `NumForm`.
21 lemmas (0%) occur at least once with a non-empty value of `NumForm`.
The feature is used with 1 part-of-speech tags: <tt><a href="la_udante-pos-NUM.html">NUM</a></tt> (150; 0% instances).

### `NUM`

150 <tt><a href="la_udante-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumForm`.

The most frequent other feature values with which `NUM` and `NumForm` co-occurred: <tt><a href="la_udante-feat-NumType.html">NumType</a></tt><tt>=Card</tt> (150; 100%), <tt><a href="la_udante-feat-Number.html">Number</a></tt><tt>=Plur</tt> (116; 77%).

`NUM` tokens may have the following values of `NumForm`:

* `Roman` (8; 5% of non-empty `NumForm`): <em>xv, I, II, clxxx, lxvij, xiiii, xxiij</em>
* `Word` (142; 95% of non-empty `NumForm`): <em>duo, tria, duorum, duobus, tres, duos, tribus, duas, due, duodecim</em>

`NumForm` seems to be **lexical feature** of `NUM`. 100% lemmas (21) occur only with one value of `NumForm`.

## Relations with Agreement in `NumForm`

The 10 most frequent relations where parent and child node agree in `NumForm`:
<tt>NUM --[<tt><a href="la_udante-dep-conj.html">conj</a></tt>]--> NUM</tt> (3; 100%).

