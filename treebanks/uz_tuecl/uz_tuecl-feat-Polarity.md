---
layout: base
title:  'Statistics of Polarity in UD_Uzbek-TueCL'
udver: '2'
---

## Treebank Statistics: UD_Uzbek-TueCL: Features: `Polarity`

This feature is universal.
It occurs with 1 different values: `Neg`.

33 tokens (4%) have a non-empty value of `Polarity`.
20 types (5%) occur at least once with a non-empty value of `Polarity`.
13 lemmas (5%) occur at least once with a non-empty value of `Polarity`.
The feature is used with 2 part-of-speech tags: <tt><a href="uz_tuecl-pos-VERB.html">VERB</a></tt> (21; 2% instances), <tt><a href="uz_tuecl-pos-AUX.html">AUX</a></tt> (12; 1% instances).

### `VERB`

21 <tt><a href="uz_tuecl-pos-VERB.html">VERB</a></tt> tokens (10% of all `VERB` tokens) have a non-empty value of `Polarity`.

The most frequent other feature values with which `VERB` and `Polarity` co-occurred: <tt><a href="uz_tuecl-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (21; 100%), <tt><a href="uz_tuecl-feat-Number.html">Number</a></tt><tt>=Sing</tt> (14; 67%), <tt><a href="uz_tuecl-feat-Person.html">Person</a></tt><tt>=3</tt> (14; 67%), <tt><a href="uz_tuecl-feat-VerbForm.html">VerbForm</a></tt><tt>=EMPTY</tt> (14; 67%), <tt><a href="uz_tuecl-feat-Mood.html">Mood</a></tt><tt>=Ind</tt> (13; 62%).

`VERB` tokens may have the following values of `Polarity`:

* `Neg` (21; 100% of non-empty `Polarity`): <em>bilmaydi, koʻrmay, olmaydi, qolmadi, turmaklamadi, tushunmaydi, gapirma, kelmagan, ketmasdan, olmadim</em>
* `EMPTY` (180): <em>berdimi, ketdi, oldi, koʻradi, olib, Oʻqiganlarini, oʻqishni, oʻqiyapti, qildi, uxladi</em>

`Polarity` seems to be **lexical feature** of `VERB`. 100% lemmas (11) occur only with one value of `Polarity`.

### `AUX`

12 <tt><a href="uz_tuecl-pos-AUX.html">AUX</a></tt> tokens (26% of all `AUX` tokens) have a non-empty value of `Polarity`.

The most frequent other feature values with which `AUX` and `Polarity` co-occurred: <tt><a href="uz_tuecl-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (10; 83%), <tt><a href="uz_tuecl-feat-Tense.html">Tense</a></tt><tt>=EMPTY</tt> (9; 75%).

`AUX` tokens may have the following values of `Polarity`:

* `Neg` (12; 100% of non-empty `Polarity`): <em>emas, emasdi, yoʻq, emasmi, emasmidi</em>
* `EMPTY` (34): <em>edi, kerak, mumkin, boʻladi, ekanligini, boʻlayotgan, boʻlgandi, boʻlsa, edimi, ekan</em>

