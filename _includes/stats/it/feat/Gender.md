

--------------------------------------------------------------------------------

## Treebank Statistics (UD_Italian)

This feature is universal.
It occurs with 2 different values: `Fem`, `Masc`.

111693 tokens (41%) have a non-empty value of `Gender`.
14345 types (54%) occur at least once with a non-empty value of `Gender`.
9567 lemmas (53%) occur at least once with a non-empty value of `Gender`.
The feature is used with 8 part-of-speech tags: [it-pos/NOUN]() (51850; 19% instances), [it-pos/DET]() (37259; 14% instances), [it-pos/ADJ]() (10885; 4% instances), [it-pos/VERB]() (8081; 3% instances), [it-pos/PRON]() (3055; 1% instances), [it-pos/AUX]() (559; 0% instances), [it-pos/ADP]() (3; 0% instances), [it-pos/PROPN]() (1; 0% instances).

### `NOUN`

51850 [it-pos/NOUN]() tokens (97% of all `NOUN` tokens) have a non-empty value of `Gender`.

The most frequent other feature values with which `NOUN` and `Gender` co-occurred: <tt><a href="Number.html">Number</a>=Sing</tt> (34189; 66%).

`NOUN` tokens may have the following values of `Gender`:

* `Fem` (23453; 45% of non-empty `Gender`): <em>città, parte, società, legge, proprietà, attività, servitù, autorità, commissione, opere</em>
* `Masc` (28397; 55% of non-empty `Gender`): <em>anni, fondo, diritto, presidente, proprietario, anno, caso, art., stato, modo</em>
* `EMPTY` (1801): <em>presidente, onorevole, grazie, enfiteuta, leader, fronte, abitanti, partecipanti, componenti, capitale</em>

<table>
  <tr><th>Paradigm <i>proprietario</i></th><th><tt>Masc</tt></th><th><tt>Fem</tt></th></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Sing</tt></td><td><em>proprietario</em></td><td><em>proprietaria</em></td></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Plur</tt></td><td><em>proprietari</em></td><td></td></tr>
</table>

`Gender` seems to be **lexical feature** of `NOUN`. 98% lemmas (6363) occur only with one value of `Gender`.

### `DET`

37259 [it-pos/DET]() tokens (85% of all `DET` tokens) have a non-empty value of `Gender`.

The most frequent other feature values with which `DET` and `Gender` co-occurred: <tt><a href="PronType.html">PronType</a>=Art</tt> (33754; 91%), <tt><a href="Definite.html">Definite</a>=Def</tt> (29488; 79%), <tt><a href="Number.html">Number</a>=Sing</tt> (26487; 71%).

`DET` tokens may have the following values of `Gender`:

* `Fem` (15783; 42% of non-empty `Gender`): <em>la, le, una, sua, un', questa, sue, queste, tutte, quante</em>
* `Masc` (21476; 58% of non-empty `Gender`): <em>il, i, un, gli, lo, suo, questo, tutti, suoi, quanti</em>
* `EMPTY` (6450): <em>l', quale, ogni, che, loro, qualche, tale, qualsiasi, l’, tali</em>

<table>
  <tr><th>Paradigm <i>il</i></th><th><tt>Masc</tt></th><th><tt>Fem</tt></th></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Sing</tt></td><td><em>il, lo, l', lu, l’</em></td><td><em>la, l', le, il</em></td></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Plur</tt></td><td><em>i, gli, il</em></td><td><em>le</em></td></tr>
</table>

### `ADJ`

10885 [it-pos/ADJ]() tokens (63% of all `ADJ` tokens) have a non-empty value of `Gender`.

The most frequent other feature values with which `ADJ` and `Gender` co-occurred: <tt><a href="Number.html">Number</a>=Sing</tt> (7343; 67%).

`ADJ` tokens may have the following values of `Gender`:

* `Fem` (4951; 45% of non-empty `Gender`): <em>prima, stessa, altra, altre, italiana, economica, alta, nuova, giudiziaria, nuove</em>
* `Masc` (5934; 55% of non-empty `Gender`): <em>primo, altri, altro, stesso, nuovo, vero, secondo, pubblico, europeo, alto</em>
* `EMPTY` (6461): <em>grande, presente, comune, ex, maggiore, possibile, sociale, nazionale, internazionale, seguenti</em>

<table>
  <tr><th>Paradigm <i>altro</i></th><th><tt>Masc</tt></th><th><tt>Fem</tt></th></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Sing</tt></td><td><em>altro</em></td><td><em>altra</em></td></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Plur</tt></td><td><em>altri</em></td><td><em>altre</em></td></tr>
</table>

### `VERB`

8081 [it-pos/VERB]() tokens (30% of all `VERB` tokens) have a non-empty value of `Gender`.

The most frequent other feature values with which `VERB` and `Gender` co-occurred: <tt><a href="Tense.html">Tense</a>=Past</tt> (8081; 100%), <tt><a href="VerbForm.html">VerbForm</a>=Part</tt> (8081; 100%), <tt><a href="Mood.html">Mood</a>=EMPTY</tt> (8081; 100%), <tt><a href="Person.html">Person</a>=EMPTY</tt> (8081; 100%), <tt><a href="Number.html">Number</a>=Sing</tt> (5880; 73%).

`VERB` tokens may have the following values of `Gender`:

* `Fem` (2507; 31% of non-empty `Gender`): <em>stata, fatta, stabilite, dovuta, fatte, vista, considerata, costituita, fondata, chiamata</em>
* `Masc` (5574; 69% of non-empty `Gender`): <em>fatto, visto, stato, tenuto, avuto, detto, dato, messo, chiamato, scritto</em>
* `EMPTY` (18421): <em>è, sono, ha, era, essere, trova, far, fare, hanno, fa</em>

<table>
  <tr><th>Paradigm <i>essere</i></th><th><tt>Masc</tt></th><th><tt>Fem</tt></th></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Sing</tt></td><td><em>stato</em></td><td><em>stata</em></td></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Plur</tt></td><td><em>stati</em></td><td><em>state</em></td></tr>
</table>

### `PRON`

3055 [it-pos/PRON]() tokens (28% of all `PRON` tokens) have a non-empty value of `Gender`.

The most frequent other feature values with which `PRON` and `Gender` co-occurred: <tt><a href="Number.html">Number</a>=Sing</tt> (2243; 73%), <tt><a href="Person.html">Person</a>=EMPTY</tt> (1930; 63%).

`PRON` tokens may have the following values of `Gender`:

* `Fem` (740; 24% of non-empty `Gender`): <em>la, le, quella, quelle, una, questa, essa, esse, altra, lei</em>
* `Masc` (2315; 76% of non-empty `Gender`): <em>lo, quello, uno, questo, gli, li, lui, tutto, ciò, quanto</em>
* `EMPTY` (7808): <em>si, che, chi, cui, ci, ne, qual, mi, c', quale</em>

<table>
  <tr><th>Paradigm <i>lo</i></th><th><tt>Masc</tt></th><th><tt>Fem</tt></th></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Sing</tt></td><td><em>lo, gli</em></td><td><em>la</em></td></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Plur</tt></td><td><em>li</em></td><td><em>le</em></td></tr>
  <tr><td><tt></tt></td><td></td><td><em>le</em></td></tr>
</table>

### `AUX`

559 [it-pos/AUX]() tokens (7% of all `AUX` tokens) have a non-empty value of `Gender`.

The most frequent other feature values with which `AUX` and `Gender` co-occurred: <tt><a href="Tense.html">Tense</a>=Past</tt> (559; 100%), <tt><a href="VerbForm.html">VerbForm</a>=Part</tt> (559; 100%), <tt><a href="Mood.html">Mood</a>=EMPTY</tt> (559; 100%), <tt><a href="Person.html">Person</a>=EMPTY</tt> (559; 100%), <tt><a href="Number.html">Number</a>=Sing</tt> (407; 73%).

`AUX` tokens may have the following values of `Gender`:

* `Fem` (168; 30% of non-empty `Gender`): <em>stata, state, potuta, andata</em>
* `Masc` (391; 70% of non-empty `Gender`): <em>stato, stati, dovuto, potuto, voluto, andato, potuti</em>
* `EMPTY` (6899): <em>è, ha, sono, può, hanno, essere, deve, possono, era, viene</em>

<table>
  <tr><th>Paradigm <i>essere</i></th><th><tt>Masc</tt></th><th><tt>Fem</tt></th></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Sing</tt></td><td><em>stato</em></td><td><em>stata</em></td></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Plur</tt></td><td><em>stati</em></td><td><em>state</em></td></tr>
</table>

### `ADP`

3 [it-pos/ADP]() tokens (0% of all `ADP` tokens) have a non-empty value of `Gender`.

`ADP` tokens may have the following values of `Gender`:

* `Fem` (1; 33% of non-empty `Gender`): <em>della</em>
* `Masc` (2; 67% of non-empty `Gender`): <em>del, du</em>
* `EMPTY` (41468): <em>di, a, in, da, per, con, su, come, ad, d'</em>

<table>
  <tr><th>Paradigm <i>del</i></th><th><tt>Masc</tt></th><th><tt>Fem</tt></th></tr>
  <tr><td><tt></tt></td><td><em>del</em></td><td><em>della</em></td></tr>
</table>

### `PROPN`

1 [it-pos/PROPN]() tokens (0% of all `PROPN` tokens) have a non-empty value of `Gender`.

`PROPN` tokens may have the following values of `Gender`:

* `Fem` (1; 100% of non-empty `Gender`): <em>hye</em>
* `EMPTY` (13344): <em>Shakespeare, Balzac, Italia, stati, Europa, San, Uniti, Albania, Marco, Roma</em>

## Relations with Agreement in `Gender`

The 10 most frequent relations where parent and child node agree in `Gender`:
<tt>NOUN --[<a href="../dep/det.html">det</a>]--> DET</tt> (30357; 83%),
<tt>NOUN --[<a href="../dep/amod.html">amod</a>]--> ADJ</tt> (8593; 63%),
<tt>NOUN --[<a href="../dep/conj.html">conj</a>]--> NOUN</tt> (2197; 55%),
<tt>NOUN --[<a href="../dep/acl.html">acl</a>]--> VERB</tt> (1963; 64%),
<tt>VERB --[<a href="../dep/nsubjpass.html">nsubjpass</a>]--> NOUN</tt> (1425; 95%),
<tt>NOUN --[<a href="../dep/det:poss.html">det:poss</a>]--> DET</tt> (1393; 80%),
<tt>ADJ --[<a href="../dep/conj.html">conj</a>]--> ADJ</tt> (329; 52%),
<tt>ADJ --[<a href="../dep/nsubj.html">nsubj</a>]--> NOUN</tt> (309; 55%),
<tt>NOUN --[<a href="../dep/det:predet.html">det:predet</a>]--> DET</tt> (291; 96%),
<tt>PRON --[<a href="../dep/det.html">det</a>]--> DET</tt> (283; 52%).

