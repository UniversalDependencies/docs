---
layout: base
title:  'Statistics of Aspect in UD_Hausa-NorthernAutogramm'
udver: '2'
---

## Treebank Statistics: UD_Hausa-NorthernAutogramm: Features: `Aspect`

This feature is universal but the values `Aor`, `Iter` are language-specific.
It occurs with 3 different values: `Aor`, `Iter`, `Perf`.

552 tokens (13%) have a non-empty value of `Aspect`.
73 types (9%) occur at least once with a non-empty value of `Aspect`.
2 lemmas (0%) occur at least once with a non-empty value of `Aspect`.
The feature is used with 2 part-of-speech tags: <tt><a href="ha_northernautogramm-pos-AUX.html">AUX</a></tt> (526; 12% instances), <tt><a href="ha_northernautogramm-pos-PART.html">PART</a></tt> (26; 1% instances).

### `AUX`

526 <tt><a href="ha_northernautogramm-pos-AUX.html">AUX</a></tt> tokens (76% of all `AUX` tokens) have a non-empty value of `Aspect`.

The most frequent other feature values with which `AUX` and `Aspect` co-occurred: <tt><a href="ha_northernautogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (417; 79%), <tt><a href="ha_northernautogramm-feat-Person.html">Person</a></tt><tt>=3</tt> (344; 65%).

`AUX` tokens may have the following values of `Aspect`:

* `Aor` (113; 21% of non-empty `Aspect`): <em>shì, à, ìn, tà, kà, sù, kì, kù, mù</em>
* `Perf` (413; 79% of non-empty `Aspect`): <em>yac, ankà, sunkà, yaː, kaː, tac, yaz, naː, taː, yat</em>
* `EMPTY` (164): <em>shinàː, tanàː, anàː, sunàː, akà, inàː, sukà, kanàː, kukà, shikà</em>

### `PART`

26 <tt><a href="ha_northernautogramm-pos-PART.html">PART</a></tt> tokens (14% of all `PART` tokens) have a non-empty value of `Aspect`.

The most frequent other feature values with which `PART` and `Aspect` co-occurred: <tt><a href="ha_northernautogramm-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (26; 100%).

`PART` tokens may have the following values of `Aspect`:

* `Iter` (26; 100% of non-empty `Aspect`): <em>ta</em>
* `EMPTY` (155): <em>gàː, nàː, ba, baːbù, kòː, dai, àkwai, bâː, bàː, naː</em>

