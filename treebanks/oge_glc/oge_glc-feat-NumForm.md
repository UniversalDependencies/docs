---
layout: base
title:  'Statistics of NumForm in UD_Old_Georgian-GLC'
udver: '2'
---

## Treebank Statistics: UD_Old_Georgian-GLC: Features: `NumForm`

This feature is language-specific.
It occurs with 2 different values: `Georgian`, `Word`.

51 tokens (1%) have a non-empty value of `NumForm`.
48 types (2%) occur at least once with a non-empty value of `NumForm`.
37 lemmas (2%) occur at least once with a non-empty value of `NumForm`.
The feature is used with 1 part-of-speech tags: <tt><a href="oge_glc-pos-NUM.html">NUM</a></tt> (51; 1% instances).

### `NUM`

51 <tt><a href="oge_glc-pos-NUM.html">NUM</a></tt> tokens (51% of all `NUM` tokens) have a non-empty value of `NumForm`.

The most frequent other feature values with which `NUM` and `NumForm` co-occurred: <tt><a href="oge_glc-feat-NumType.html">NumType</a></tt><tt>=Card</tt> (32; 63%), <tt><a href="oge_glc-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (30; 59%), <tt><a href="oge_glc-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (29; 57%).

`NUM` tokens may have the following values of `NumForm`:

* `Georgian` (29; 57% of non-empty `NumForm`): <em>ა(1), ბ(2), გ(3), დ(4), ე(5), ვ(6), ზ(7), თ(9), ი(10), ია(11)</em>
* `Word` (22; 43% of non-empty `NumForm`): <em>მეოთხისა, პირველთა, პირველისა, ათორმეტთა, ათრვამეტთა, ერთი, მეათხუთმეტედ, მეათხუთმეტედმდე, მეორე, მეორემან</em>

`NumForm` seems to be **lexical feature** of `NUM`. 100% lemmas (37) occur only with one value of `NumForm`.

## Relations with Agreement in `NumForm`

The 10 most frequent relations where parent and child node agree in `NumForm`:
<tt>NUM --[<tt><a href="oge_glc-dep-conj.html">conj</a></tt>]--> NUM</tt> (1; 100%),
<tt>NUM --[<tt><a href="oge_glc-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (1; 100%).

