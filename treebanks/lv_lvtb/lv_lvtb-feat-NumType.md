---
layout: base
title:  'Statistics of NumType in UD_Latvian-LVTB'
udver: '2'
---

## Treebank Statistics: UD_Latvian-LVTB: Features: `NumType`

This feature is universal.
It occurs with 4 different values: `Card`, `Frac`, `Mult`, `Ord`.

1954 tokens (2%) have a non-empty value of `NumType`.
570 types (2%) occur at least once with a non-empty value of `NumType`.
462 lemmas (3%) occur at least once with a non-empty value of `NumType`.
The feature is used with 3 part-of-speech tags: <tt><a href="lv_lvtb-pos-NUM.html">NUM</a></tt> (1242; 1% instances), <tt><a href="lv_lvtb-pos-ADJ.html">ADJ</a></tt> (701; 1% instances), <tt><a href="lv_lvtb-pos-ADV.html">ADV</a></tt> (11; 0% instances).

### `NUM`

1242 <tt><a href="lv_lvtb-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (1239; 100% of non-empty `NumType`): <em>viens, vienu, trīs, viena, 1, divas, vienā, 3, 2, 50</em>
* `Frac` (3; 0% of non-empty `NumType`): <em>pusotra, pusotru</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (303) occur only with one value of `NumType`.

### `ADJ`

701 <tt><a href="lv_lvtb-pos-ADJ.html">ADJ</a></tt> tokens (12% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="lv_lvtb-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (491; 70%), <tt><a href="lv_lvtb-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (491; 70%), <tt><a href="lv_lvtb-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (491; 70%), <tt><a href="lv_lvtb-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (491; 70%).

`ADJ` tokens may have the following values of `NumType`:

* `Ord` (701; 100% of non-empty `NumType`): <em>1., 2012., 2., 2011., 3., 2007., 9., 2013., 4., pirmo</em>
* `EMPTY` (5078): <em>iespējams, liela, pedagoģisko, lielu, jaunu, nepieciešams, galvenais, jauna, lielā, liels</em>

`NumType` seems to be **lexical feature** of `ADJ`. 100% lemmas (162) occur only with one value of `NumType`.

### `ADV`

11 <tt><a href="lv_lvtb-pos-ADV.html">ADV</a></tt> tokens (0% of all `ADV` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADV` and `NumType` co-occurred: <tt><a href="lv_lvtb-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (11; 100%), <tt><a href="lv_lvtb-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (11; 100%).

`ADV` tokens may have the following values of `NumType`:

* `Mult` (11; 100% of non-empty `NumType`): <em>divreiz, vienreiz</em>
* `EMPTY` (6831): <em>kā, kad, jau, vēl, tad, tā, kur, ļoti, tik, tur</em>

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="lv_lvtb-dep-conj.html">conj</a></tt>]--> NUM</tt> (43; 98%),
<tt>ADJ --[<tt><a href="lv_lvtb-dep-conj.html">conj</a></tt>]--> ADJ</tt> (34; 94%),
<tt>NUM --[<tt><a href="lv_lvtb-dep-flat-name.html">flat:name</a></tt>]--> NUM</tt> (14; 100%),
<tt>NUM --[<tt><a href="lv_lvtb-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (6; 100%),
<tt>NUM --[<tt><a href="lv_lvtb-dep-parataxis.html">parataxis</a></tt>]--> NUM</tt> (6; 100%),
<tt>NUM --[<tt><a href="lv_lvtb-dep-compound.html">compound</a></tt>]--> NUM</tt> (5; 100%),
<tt>NUM --[<tt><a href="lv_lvtb-dep-flat.html">flat</a></tt>]--> NUM</tt> (4; 100%),
<tt>ADJ --[<tt><a href="lv_lvtb-dep-flat.html">flat</a></tt>]--> ADJ</tt> (2; 100%),
<tt>NUM --[<tt><a href="lv_lvtb-dep-acl.html">acl</a></tt>]--> NUM</tt> (2; 100%),
<tt>NUM --[<tt><a href="lv_lvtb-dep-nmod.html">nmod</a></tt>]--> NUM</tt> (2; 100%).

