---
layout: base
title:  'Statistics of Abbr in UD_Dutch-Alpino'
udver: '2'
---

## Treebank Statistics: UD_Dutch-Alpino: Features: `Abbr`

This feature is universal.
It occurs with 1 different values: `Yes`.

236 tokens (0%) have a non-empty value of `Abbr`.
77 types (0%) occur at least once with a non-empty value of `Abbr`.
65 lemmas (0%) occur at least once with a non-empty value of `Abbr`.
The feature is used with 1 part-of-speech tags: <tt><a href="nl_alpino-pos-X.html">X</a></tt> (236; 0% instances).

### `X`

236 <tt><a href="nl_alpino-pos-X.html">X</a></tt> tokens (25% of all `X` tokens) have a non-empty value of `Abbr`.

The most frequent other feature values with which `X` and `Abbr` co-occurred: <tt><a href="nl_alpino-feat-Foreign.html">Foreign</a></tt><tt>=EMPTY</tt> (236; 100%), <tt><a href="nl_alpino-feat-ExtPos.html">ExtPos</a></tt><tt>=EMPTY</tt> (233; 99%).

`X` tokens may have the following values of `Abbr`:

* `Yes` (236; 100% of non-empty `Abbr`): <em>pct., o.a., jl., o.m., pct, mln., a.s., rk, v.j., nl.</em>
* `EMPTY` (720): <em>the, fancy, ad, a, in, of, binnen-, Grand, fiction, to</em>

`Abbr` seems to be **lexical feature** of `X`. 100% lemmas (65) occur only with one value of `Abbr`.

## Relations with Agreement in `Abbr`

The 10 most frequent relations where parent and child node agree in `Abbr`:
<tt>X --[<tt><a href="nl_alpino-dep-conj.html">conj</a></tt>]--> X</tt> (10; 100%),
<tt>X --[<tt><a href="nl_alpino-dep-fixed.html">fixed</a></tt>]--> X</tt> (3; 100%),
<tt>X --[<tt><a href="nl_alpino-dep-parataxis.html">parataxis</a></tt>]--> X</tt> (2; 100%),
<tt>X --[<tt><a href="nl_alpino-dep-flat.html">flat</a></tt>]--> X</tt> (1; 100%).

