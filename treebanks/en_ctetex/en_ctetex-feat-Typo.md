---
layout: base
title:  'Statistics of Typo in UD_English-CTeTex'
udver: '2'
---

## Treebank Statistics: UD_English-CTeTex: Features: `Typo`

This feature is language-specific.
It occurs with 1 different values: `Yes`.

8 tokens (0%) have a non-empty value of `Typo`.
8 types (0%) occur at least once with a non-empty value of `Typo`.
8 lemmas (0%) occur at least once with a non-empty value of `Typo`.
The feature is used with 3 part-of-speech tags: <tt><a href="en_ctetex-pos-NOUN.html">NOUN</a></tt> (4; 0% instances), <tt><a href="en_ctetex-pos-ADJ.html">ADJ</a></tt> (3; 0% instances), <tt><a href="en_ctetex-pos-AUX.html">AUX</a></tt> (1; 0% instances).

### `NOUN`

4 <tt><a href="en_ctetex-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `NOUN` and `Typo` co-occurred: <tt><a href="en_ctetex-feat-Number.html">Number</a></tt><tt>=Sing</tt> (4; 100%).

`NOUN` tokens may have the following values of `Typo`:

* `Yes` (4; 100% of non-empty `Typo`): <em>count, ground, mont, take</em>

### `ADJ`

3 <tt><a href="en_ctetex-pos-ADJ.html">ADJ</a></tt> tokens (0% of all `ADJ` tokens) have a non-empty value of `Typo`.

`ADJ` tokens may have the following values of `Typo`:

* `Yes` (3; 100% of non-empty `Typo`): <em>Stand, air, multi</em>

### `AUX`

1 <tt><a href="en_ctetex-pos-AUX.html">AUX</a></tt> tokens (0% of all `AUX` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `AUX` and `Typo` co-occurred: <tt><a href="en_ctetex-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (1; 100%), <tt><a href="en_ctetex-feat-Tense.html">Tense</a></tt><tt>=EMPTY</tt> (1; 100%).

`AUX` tokens may have the following values of `Typo`:

* `Yes` (1; 100% of non-empty `Typo`): <em>to</em>

