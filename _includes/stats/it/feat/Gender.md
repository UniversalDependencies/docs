

--------------------------------------------------------------------------------

## Treebank Statistics (UD_Italian)

This feature is universal.
It occurs with 2 different values: `Fem`, `Masc`.

121048 tokens (41%) have a non-empty value of `Gender`.
14753 types (54%) occur at least once with a non-empty value of `Gender`.
9877 lemmas (53%) occur at least once with a non-empty value of `Gender`.
The feature is used with 10 part-of-speech tags: [it-pos/NOUN]() (56162; 19% instances), [it-pos/DET]() (40762; 14% instances), [it-pos/ADJ]() (11842; 4% instances), [it-pos/VERB]() (8553; 3% instances), [it-pos/PRON]() (3144; 1% instances), [it-pos/AUX]() (576; 0% instances), [it-pos/ADP]() (4; 0% instances), [it-pos/PROPN]() (3; 0% instances), [it-pos/ADV]() (1; 0% instances), [it-pos/X]() (1; 0% instances).

### `NOUN`

56162 [it-pos/NOUN]() tokens (97% of all `NOUN` tokens) have a non-empty value of `Gender`.

The most frequent other feature values with which `NOUN` and `Gender` co-occurred: <tt><a href="Number.html">Number</a>=Sing</tt> (36671; 65%).

`NOUN` tokens may have the following values of `Gender`:

* `Fem` (25176; 45% of non-empty `Gender`): <em>città, parte, legge, società, persone, proprietà, attività, vita, servitù, autorità</em>
* `Masc` (30986; 55% of non-empty `Gender`): <em>anni, presidente, fondo, diritto, anno, film, proprietario, stato, caso, art.</em>
* `EMPTY` (1895): <em>presidente, onorevole, abitanti, grazie, rappresentanti, fronte, giovani, enfiteuta, leader, partecipanti</em>

<table>
  <tr><th>Paradigm <i>proprietario</i></th><th><tt>Masc</tt></th><th><tt>Fem</tt></th></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Sing</tt></td><td><em>proprietario</em></td><td><em>proprietaria</em></td></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Plur</tt></td><td><em>proprietari</em></td><td></td></tr>
</table>

`Gender` seems to be **lexical feature** of `NOUN`. 98% lemmas (6593) occur only with one value of `Gender`.

### `DET`

40762 [it-pos/DET]() tokens (86% of all `DET` tokens) have a non-empty value of `Gender`.

The most frequent other feature values with which `DET` and `Gender` co-occurred: <tt><a href="PronType.html">PronType</a>=Art</tt> (36883; 90%), <tt><a href="Definite.html">Definite</a>=Def</tt> (32325; 79%), <tt><a href="Number.html">Number</a>=Sing</tt> (28606; 70%).

`DET` tokens may have the following values of `Gender`:

* `Fem` (17072; 42% of non-empty `Gender`): <em>la, le, una, sua, un', questa, sue, queste, tutte, molte</em>
* `Masc` (23690; 58% of non-empty `Gender`): <em>il, i, un, gli, lo, suo, questo, tutti, suoi, alcuni</em>
* `EMPTY` (6786): <em>l', quale, ogni, loro, che, l’, qualche, tale, qualsiasi, quali</em>

<table>
  <tr><th>Paradigm <i>il</i></th><th><tt>Masc</tt></th><th><tt>Fem</tt></th></tr>
  <tr><td><tt></tt></td><td></td><td><em>l’</em></td></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Sing|<a href="PronType.html">PronType</a>=Art</tt></td><td><em>il, lo, l’, l', i1, lu</em></td><td><em>la, l', l’, le, il</em></td></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Plur|<a href="PronType.html">PronType</a>=Art</tt></td><td><em>i, gli, il</em></td><td><em>le, l’</em></td></tr>
</table>

### `ADJ`

11842 [it-pos/ADJ]() tokens (63% of all `ADJ` tokens) have a non-empty value of `Gender`.

The most frequent other feature values with which `ADJ` and `Gender` co-occurred: <tt><a href="Number.html">Number</a>=Sing</tt> (7899; 67%).

`ADJ` tokens may have the following values of `Gender`:

* `Fem` (5361; 45% of non-empty `Gender`): <em>prima, italiana, altra, stessa, altre, europea, nuova, nuove, economica, alta</em>
* `Masc` (6481; 55% of non-empty `Gender`): <em>primo, nuovo, altri, altro, stesso, vero, europeo, secondo, pubblico, italiano</em>
* `EMPTY` (6837): <em>grande, presente, comune, ex, maggiore, possibile, sociale, nazionale, internazionale, importante</em>

<table>
  <tr><th>Paradigm <i>altro</i></th><th><tt>Masc</tt></th><th><tt>Fem</tt></th></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Sing</tt></td><td><em>altro</em></td><td><em>altra</em></td></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Plur</tt></td><td><em>altri</em></td><td><em>altre</em></td></tr>
</table>

### `VERB`

8553 [it-pos/VERB]() tokens (30% of all `VERB` tokens) have a non-empty value of `Gender`.

The most frequent other feature values with which `VERB` and `Gender` co-occurred: <tt><a href="Mood.html">Mood</a>=EMPTY</tt> (8553; 100%), <tt><a href="Tense.html">Tense</a>=Past</tt> (8553; 100%), <tt><a href="Person.html">Person</a>=EMPTY</tt> (8553; 100%), <tt><a href="VerbForm.html">VerbForm</a>=Part</tt> (8552; 100%), <tt><a href="Number.html">Number</a>=Sing</tt> (6277; 73%).

`VERB` tokens may have the following values of `Gender`:

* `Fem` (2577; 30% of non-empty `Gender`): <em>stata, fatta, stabilite, dovuta, fatte, vista, considerata, costituita, fondata, nata</em>
* `Masc` (5976; 70% of non-empty `Gender`): <em>fatto, stato, visto, vinto, avuto, tenuto, detto, nato, dato, ricevuto</em>
* `EMPTY` (20059): <em>è, sono, ha, era, essere, hanno, fare, far, trova, fa</em>

<table>
  <tr><th>Paradigm <i>essere</i></th><th><tt>Masc</tt></th><th><tt>Fem</tt></th></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Sing</tt></td><td><em>stato</em></td><td><em>stata</em></td></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Plur</tt></td><td><em>stati</em></td><td><em>state</em></td></tr>
</table>

### `PRON`

3144 [it-pos/PRON]() tokens (28% of all `PRON` tokens) have a non-empty value of `Gender`.

The most frequent other feature values with which `PRON` and `Gender` co-occurred: <tt><a href="Number.html">Number</a>=Sing</tt> (2291; 73%), <tt><a href="Person.html">Person</a>=EMPTY</tt> (1971; 63%).

`PRON` tokens may have the following values of `Gender`:

* `Fem` (766; 24% of non-empty `Gender`): <em>la, le, quella, quelle, una, questa, essa, altra, esse, lei</em>
* `Masc` (2378; 76% of non-empty `Gender`): <em>lo, quello, uno, li, questo, gli, lui, tutto, ciò, tutti</em>
* `EMPTY` (8113): <em>si, che, chi, cui, ci, ne, qual, mi, c', quale</em>

<table>
  <tr><th>Paradigm <i>lo</i></th><th><tt>Masc</tt></th><th><tt>Fem</tt></th></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Sing|<a href="Person.html">Person</a>=3</tt></td><td><em>lo, gli</em></td><td><em>la</em></td></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Sing</tt></td><td><em>lo</em></td><td></td></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Plur|<a href="Person.html">Person</a>=3</tt></td><td><em>li</em></td><td><em>le</em></td></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Plur</tt></td><td></td><td><em>le</em></td></tr>
  <tr><td><tt><a href="Person.html">Person</a>=3</tt></td><td></td><td><em>le</em></td></tr>
</table>

### `AUX`

576 [it-pos/AUX]() tokens (7% of all `AUX` tokens) have a non-empty value of `Gender`.

The most frequent other feature values with which `AUX` and `Gender` co-occurred: <tt><a href="Person.html">Person</a>=EMPTY</tt> (576; 100%), <tt><a href="Tense.html">Tense</a>=Past</tt> (576; 100%), <tt><a href="VerbForm.html">VerbForm</a>=Part</tt> (576; 100%), <tt><a href="Mood.html">Mood</a>=EMPTY</tt> (576; 100%), <tt><a href="Number.html">Number</a>=Sing</tt> (423; 73%).

`AUX` tokens may have the following values of `Gender`:

* `Fem` (170; 30% of non-empty `Gender`): <em>stata, state, potuta, andata, fatta</em>
* `Masc` (406; 70% of non-empty `Gender`): <em>stato, stati, potuto, dovuto, voluto, andato, fatto, potuti</em>
* `EMPTY` (7506): <em>è, ha, sono, può, hanno, essere, deve, possono, era, viene</em>

<table>
  <tr><th>Paradigm <i>essere</i></th><th><tt>Masc</tt></th><th><tt>Fem</tt></th></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Sing</tt></td><td><em>stato</em></td><td><em>stata</em></td></tr>
  <tr><td><tt><a href="Number.html">Number</a>=Plur</tt></td><td><em>stati</em></td><td><em>state</em></td></tr>
</table>

### `ADP`

4 [it-pos/ADP]() tokens (0% of all `ADP` tokens) have a non-empty value of `Gender`.

`ADP` tokens may have the following values of `Gender`:

* `Fem` (2; 50% of non-empty `Gender`): <em>alla, della</em>
* `Masc` (2; 50% of non-empty `Gender`): <em>del, du</em>
* `EMPTY` (44541): <em>di, a, in, da, per, con, su, come, ad, tra</em>

<table>
  <tr><th>Paradigm <i>del</i></th><th><tt>Masc</tt></th><th><tt>Fem</tt></th></tr>
  <tr><td><tt></tt></td><td><em>del</em></td><td><em>della</em></td></tr>
</table>

### `PROPN`

3 [it-pos/PROPN]() tokens (0% of all `PROPN` tokens) have a non-empty value of `Gender`.

`PROPN` tokens may have the following values of `Gender`:

* `Fem` (3; 100% of non-empty `Gender`): <em>Ceca, Repubblica, hye</em>
* `EMPTY` (14757): <em>Italia, Shakespeare, Balzac, Europa, Roma, Stati, Uniti, San, Albania, Marco</em>

### `X`

1 [it-pos/X]() tokens (1% of all `X` tokens) have a non-empty value of `Gender`.

`X` tokens may have the following values of `Gender`:

* `Masc` (1; 100% of non-empty `Gender`): <em>mixer</em>
* `EMPTY` (183): <em>a, b, c, f, Damage, ad, de, home, la, Come</em>

### `ADV`

1 [it-pos/ADV]() tokens (0% of all `ADV` tokens) have a non-empty value of `Gender`.

The most frequent other feature values with which `ADV` and `Gender` co-occurred: <tt><a href="PronType.html">PronType</a>=EMPTY</tt> (1; 100%).

`ADV` tokens may have the following values of `Gender`:

* `Masc` (1; 100% of non-empty `Gender`): <em>pochissimo</em>
* `EMPTY` (11223): <em>non, più, anche, dove, come, quando, solo, sempre, prima, poi</em>

## Relations with Agreement in `Gender`

The 10 most frequent relations where parent and child node agree in `Gender`:
<tt>NOUN --[<a href="../dep/det.html">det</a>]--> DET</tt> (33279; 84%),
<tt>NOUN --[<a href="../dep/amod.html">amod</a>]--> ADJ</tt> (9387; 63%),
<tt>NOUN --[<a href="../dep/conj.html">conj</a>]--> NOUN</tt> (2390; 55%),
<tt>NOUN --[<a href="../dep/acl.html">acl</a>]--> VERB</tt> (1993; 64%),
<tt>VERB --[<a href="../dep/nsubjpass.html">nsubjpass</a>]--> NOUN</tt> (1461; 81%),
<tt>NOUN --[<a href="../dep/det:poss.html">det:poss</a>]--> DET</tt> (1452; 80%),
<tt>VERB --[<a href="../dep/conj.html">conj</a>]--> VERB</tt> (457; 51%),
<tt>ADJ --[<a href="../dep/conj.html">conj</a>]--> ADJ</tt> (368; 53%),
<tt>NOUN --[<a href="../dep/det:predet.html">det:predet</a>]--> DET</tt> (354; 96%),
<tt>ADJ --[<a href="../dep/nsubj.html">nsubj</a>]--> NOUN</tt> (349; 57%).

