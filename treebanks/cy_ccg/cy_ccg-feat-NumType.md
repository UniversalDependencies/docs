---
layout: base
title:  'Statistics of NumType in UD_Welsh-CCG'
udver: '2'
---

## Treebank Statistics: UD_Welsh-CCG: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

797 tokens (2%) have a non-empty value of `NumType`.
291 types (4%) occur at least once with a non-empty value of `NumType`.
255 lemmas (5%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="cy_ccg-pos-NUM.html">NUM</a></tt> (685; 1% instances), <tt><a href="cy_ccg-pos-ADJ.html">ADJ</a></tt> (112; 0% instances).

### `NUM`

685 <tt><a href="cy_ccg-pos-NUM.html">NUM</a></tt> tokens (99% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="cy_ccg-feat-Mutation.html">Mutation</a></tt><tt>=EMPTY</tt> (616; 90%), <tt><a href="cy_ccg-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (594; 87%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (685; 100% of non-empty `NumType`): <em>un, chwe, 4, dau, ddau, ddwy, tri, tair, pedair, 2019</em>
* `EMPTY` (4): <em>chwech, 150, un</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (229) occur only with one value of `NumType`.

### `ADJ`

112 <tt><a href="cy_ccg-pos-ADJ.html">ADJ</a></tt> tokens (3% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="cy_ccg-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (111; 99%), <tt><a href="cy_ccg-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (103; 92%), <tt><a href="cy_ccg-feat-Mutation.html">Mutation</a></tt><tt>=EMPTY</tt> (89; 79%), <tt><a href="cy_ccg-feat-Degree.html">Degree</a></tt><tt>=Pos</tt> (69; 62%).

`ADJ` tokens may have the following values of `NumType`:

* `Ord` (112; 100% of non-empty `NumType`): <em>cyntaf, ail, gyntaf, trydydd, drydedd, 19, cynta, gynnar, 17, 18</em>
* `EMPTY` (3490): <em>Cymraeg, newydd, Gymraeg, bob, mwy, lleol, eraill, pob, arall, genedlaethol</em>

`NumType` seems to be **lexical feature** of `ADJ`. 100% lemmas (26) occur only with one value of `NumType`.

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="cy_ccg-dep-conj.html">conj</a></tt>]--> NUM</tt> (15; 100%),
<tt>NUM --[<tt><a href="cy_ccg-dep-nmod.html">nmod</a></tt>]--> NUM</tt> (4; 100%),
<tt>NUM --[<tt><a href="cy_ccg-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (4; 100%),
<tt>ADJ --[<tt><a href="cy_ccg-dep-conj.html">conj</a></tt>]--> ADJ</tt> (1; 100%),
<tt>NUM --[<tt><a href="cy_ccg-dep-obl.html">obl</a></tt>]--> NUM</tt> (1; 100%).

