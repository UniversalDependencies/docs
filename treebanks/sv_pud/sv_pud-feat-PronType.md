---
layout: base
title:  'Statistics of PronType in UD_Swedish-PUD'
udver: '2'
---

## Treebank Statistics: UD_Swedish-PUD: Features: `PronType`

This feature is universal.
It occurs with 6 different values: `Art`, `Ind`, `Int`, `Prs`, `Rel`, `Tot`.
Some words have combined values of the feature; 1 combinations have been observed: `Int|Rel`.

1140 tokens (6%) have a non-empty value of `PronType`.
36 types (1%) occur at least once with a non-empty value of `PronType`.
24 lemmas (0%) occur at least once with a non-empty value of `PronType`.
The feature is used with 4 part-of-speech tags: <tt><a href="sv_pud-pos-PRON.html">PRON</a></tt> (619; 3% instances), <tt><a href="sv_pud-pos-DET.html">DET</a></tt> (429; 2% instances), <tt><a href="sv_pud-pos-ADV.html">ADV</a></tt> (46; 0% instances), <tt><a href="sv_pud-pos-SCONJ.html">SCONJ</a></tt> (46; 0% instances).

### `PRON`

619 <tt><a href="sv_pud-pos-PRON.html">PRON</a></tt> tokens (47% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="sv_pud-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (613; 99%), <tt><a href="sv_pud-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (536; 87%), <tt><a href="sv_pud-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (340; 55%), <tt><a href="sv_pud-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (323; 52%).

`PRON` tokens may have the following values of `PronType`:

* `Ind` (2; 0% of non-empty `PronType`): <em>mycket, någon</em>
* `Int,Rel` (299; 48% of non-empty `PronType`): <em>som, vad, vilket, vilka, vem, vilken, vars, vilkas</em>
* `Prs` (316; 51% of non-empty `PronType`): <em>det, de, den, dem, dess</em>
* `Tot` (2; 0% of non-empty `PronType`): <em>båda</em>
* `EMPTY` (701): <em>han, sig, hans, jag, sin, hon, sina, vi, dess, deras</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (14) occur only with one value of `PronType`.

### `DET`

429 <tt><a href="sv_pud-pos-DET.html">DET</a></tt> tokens (42% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="sv_pud-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (424; 99%), <tt><a href="sv_pud-feat-Number.html">Number</a></tt><tt>=Sing</tt> (311; 72%), <tt><a href="sv_pud-feat-Gender.html">Gender</a></tt><tt>=Com</tt> (227; 53%).

`DET` tokens may have the following values of `PronType`:

* `Art` (422; 98% of non-empty `PronType`): <em>den, de, det, the, en</em>
* `Int,Rel` (3; 1% of non-empty `PronType`): <em>vilket, vilken</em>
* `Tot` (4; 1% of non-empty `PronType`): <em>båda</em>
* `EMPTY` (586): <em>en, ett, denna, varje, dessa, alla, detta, några, the, någon</em>

### `ADV`

46 <tt><a href="sv_pud-pos-ADV.html">ADV</a></tt> tokens (5% of all `ADV` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADV` and `PronType` co-occurred: <tt><a href="sv_pud-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (46; 100%).

`ADV` tokens may have the following values of `PronType`:

* `Int,Rel` (46; 100% of non-empty `PronType`): <em>där, hur, varför, när, dit, var, varav, då</em>
* `EMPTY` (966): <em>så, mer, in, ut, också, upp, bara, fram, här, även</em>

### `SCONJ`

46 <tt><a href="sv_pud-pos-SCONJ.html">SCONJ</a></tt> tokens (13% of all `SCONJ` tokens) have a non-empty value of `PronType`.

`SCONJ` tokens may have the following values of `PronType`:

* `Int,Rel` (46; 100% of non-empty `PronType`): <em>när, då</em>
* `EMPTY` (318): <em>att, som, om, än, medan, eftersom, innan, fastän, huruvida, då</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="sv_pud-dep-obl.html">obl</a></tt>]--> PRON</tt> (1; 100%).

