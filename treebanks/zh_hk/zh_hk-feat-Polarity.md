---
layout: base
title:  'Statistics of Polarity in UD_Chinese-HK'
udver: '2'
---

## Treebank Statistics: UD_Chinese-HK: Features: `Polarity`

This feature is universal.
It occurs with 1 different values: `Neg`.

66 tokens (1%) have a non-empty value of `Polarity`.
7 types (0%) occur at least once with a non-empty value of `Polarity`.
6 lemmas (0%) occur at least once with a non-empty value of `Polarity`.
The feature is used with 3 part-of-speech tags: <tt><a href="zh_hk-pos-VERB.html">VERB</a></tt> (41; 0% instances), <tt><a href="zh_hk-pos-AUX.html">AUX</a></tt> (18; 0% instances), <tt><a href="zh_hk-pos-PART.html">PART</a></tt> (7; 0% instances).

### `VERB`

41 <tt><a href="zh_hk-pos-VERB.html">VERB</a></tt> tokens (2% of all `VERB` tokens) have a non-empty value of `Polarity`.

`VERB` tokens may have the following values of `Polarity`:

* `Neg` (41; 100% of non-empty `Polarity`): 沒有、 沒空、 不用、 沒用
* `EMPTY` (1831): 有、 是、 說、 做、 請、 看、 去、 議事、 進行、 選舉

### `AUX`

18 <tt><a href="zh_hk-pos-AUX.html">AUX</a></tt> tokens (4% of all `AUX` tokens) have a non-empty value of `Polarity`.

`AUX` tokens may have the following values of `Polarity`:

* `Neg` (18; 100% of non-empty `Polarity`): 不用、 不要、 沒有
* `EMPTY` (453): 是、 要、 了、 能、 可以、 會、 想、 過、 能夠、 應該

### `PART`

7 <tt><a href="zh_hk-pos-PART.html">PART</a></tt> tokens (1% of all `PART` tokens) have a non-empty value of `Polarity`.

`PART` tokens may have the following values of `Polarity`:

* `Neg` (7; 100% of non-empty `Polarity`): 沒、 無
* `EMPTY` (560): 的、 了、 嗎、 吧、 呢、 啊、 呀、 啦、 得、 地

