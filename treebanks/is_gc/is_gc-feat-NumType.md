---
layout: base
title:  'Statistics of NumType in UD_Icelandic-GC'
udver: '2'
---

## Treebank Statistics: UD_Icelandic-GC: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

746 tokens (1%) have a non-empty value of `NumType`.
294 types (1%) occur at least once with a non-empty value of `NumType`.
292 lemmas (2%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="is_gc-pos-NUM.html">NUM</a></tt> (742; 1% instances), <tt><a href="is_gc-pos-ADV.html">ADV</a></tt> (4; 0% instances).

### `NUM`

742 <tt><a href="is_gc-pos-NUM.html">NUM</a></tt> tokens (39% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="is_gc-feat-Number.html">Number</a></tt><tt>=Plur</tt> (479; 65%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (670; 90% of non-empty `NumType`): <em>2, 0, 1, 3, 5, 10, 15, 30, 12, 20</em>
* `Ord` (72; 10% of non-empty `NumType`): <em>1., 2., 4., 5., 7., 20., 8., 10., 112., 13.</em>
* `EMPTY` (1148): <em>fimm, prósent, tíu, tveggja, tvö, átta, sex, sjö, tveimur, 2017</em>

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (292) occur only with one value of `NumType`.

### `ADV`

4 <tt><a href="is_gc-pos-ADV.html">ADV</a></tt> tokens (0% of all `ADV` tokens) have a non-empty value of `NumType`.

`ADV` tokens may have the following values of `NumType`:

* `Card` (4; 100% of non-empty `NumType`): <em>21, 1,14, 28, 79</em>
* `EMPTY` (10295): <em>ekki, þar, þá, í, á, fram, svo, upp, dag, til</em>

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="is_gc-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (45; 83%),
<tt>NUM --[<tt><a href="is_gc-dep-flat.html">flat</a></tt>]--> NUM</tt> (19; 100%),
<tt>NUM --[<tt><a href="is_gc-dep-advcl.html">advcl</a></tt>]--> NUM</tt> (1; 100%),
<tt>NUM --[<tt><a href="is_gc-dep-amod.html">amod</a></tt>]--> NUM</tt> (1; 100%),
<tt>NUM --[<tt><a href="is_gc-dep-conj.html">conj</a></tt>]--> NUM</tt> (1; 100%).

