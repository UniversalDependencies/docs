---
layout: base
title:  'Statistics of Abbr in UD_German-LIT'
udver: '2'
---

## Treebank Statistics: UD_German-LIT: Features: `Abbr`

This feature is universal.
It occurs with 1 different values: `Yes`.

3 tokens (0%) have a non-empty value of `Abbr`.
3 types (0%) occur at least once with a non-empty value of `Abbr`.
3 lemmas (0%) occur at least once with a non-empty value of `Abbr`.
The feature is used with 2 part-of-speech tags: <tt><a href="de_lit-pos-ADV.html">ADV</a></tt> (2; 0% instances), <tt><a href="de_lit-pos-CCONJ.html">CCONJ</a></tt> (1; 0% instances).

### `ADV`

2 <tt><a href="de_lit-pos-ADV.html">ADV</a></tt> tokens (0% of all `ADV` tokens) have a non-empty value of `Abbr`.

`ADV` tokens may have the following values of `Abbr`:

* `Yes` (2; 100% of non-empty `Abbr`): <em>s, w</em>
* `EMPTY` (3586): <em>so, nur, auch, selbst, wie, noch, doch, aber, immer, wohl</em>

### `CCONJ`

1 <tt><a href="de_lit-pos-CCONJ.html">CCONJ</a></tt> tokens (0% of all `CCONJ` tokens) have a non-empty value of `Abbr`.

`CCONJ` tokens may have the following values of `Abbr`:

* `Yes` (1; 100% of non-empty `Abbr`): <em>u</em>
* `EMPTY` (1940): <em>und, oder, als, wie, aber, sondern, denn, noch, entweder, desto</em>

## Relations with Agreement in `Abbr`

The 10 most frequent relations where parent and child node agree in `Abbr`:
<tt>ADV --[<tt><a href="de_lit-dep-advmod.html">advmod</a></tt>]--> ADV</tt> (1; 100%),
<tt>ADV --[<tt><a href="de_lit-dep-cc.html">cc</a></tt>]--> CCONJ</tt> (1; 100%).

