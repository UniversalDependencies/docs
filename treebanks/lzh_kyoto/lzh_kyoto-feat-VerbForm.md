---
layout: base
title:  'Statistics of VerbForm in UD_Classical_Chinese-Kyoto'
udver: '2'
---

## Treebank Statistics: UD_Classical_Chinese-Kyoto: Features: `VerbForm`

This feature is universal but the values `Conv` are language-specific.
It occurs with 2 different values: `Conv`, `Part`.

17809 tokens (6%) have a non-empty value of `VerbForm`.
1514 types (18%) occur at least once with a non-empty value of `VerbForm`.
1490 lemmas (18%) occur at least once with a non-empty value of `VerbForm`.
The feature is used with 2 part-of-speech tags: <tt><a href="lzh_kyoto-pos-VERB.html">VERB</a></tt> (8979; 3% instances), <tt><a href="lzh_kyoto-pos-ADV.html">ADV</a></tt> (8830; 3% instances).

### `VERB`

8979 <tt><a href="lzh_kyoto-pos-VERB.html">VERB</a></tt> tokens (10% of all `VERB` tokens) have a non-empty value of `VerbForm`.

The most frequent other feature values with which `VERB` and `VerbForm` co-occurred: <tt><a href="lzh_kyoto-feat-Degree.html">Degree</a></tt><tt>=Pos</tt> (5240; 58%).

`VERB` tokens may have the following values of `VerbForm`:

* `Part` (8979; 100% of non-empty `VerbForm`): 大、 太、 皇、 主、 小、 有、 以、 長、 庶、 孝
* `EMPTY` (84501): 曰、 以、 有、 無、 至、 立、 如、 為、 行、 在

`VerbForm` seems to be **lexical feature** of `VERB`. 100% lemmas (1037) occur only with one value of `VerbForm`.

### `ADV`

8830 <tt><a href="lzh_kyoto-pos-ADV.html">ADV</a></tt> tokens (32% of all `ADV` tokens) have a non-empty value of `VerbForm`.

The most frequent other feature values with which `ADV` and `VerbForm` co-occurred: <tt><a href="lzh_kyoto-feat-AdvType.html">AdvType</a></tt><tt>=EMPTY</tt> (8830; 100%), <tt><a href="lzh_kyoto-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (8511; 96%), <tt><a href="lzh_kyoto-feat-Degree.html">Degree</a></tt><tt>=EMPTY</tt> (5807; 66%).

`ADV` tokens may have the following values of `VerbForm`:

* `Conv` (8830; 100% of non-empty `VerbForm`): 以、 大、 始、 然、 無、 當、 親、 凡、 對、 反
* `EMPTY` (18777): 不、 則、 皆、 必、 故、 亦、 非、 乃、 未、 又

`VerbForm` seems to be **lexical feature** of `ADV`. 100% lemmas (1002) occur only with one value of `VerbForm`.

