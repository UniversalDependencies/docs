---
layout: base
title:  'Statistics of Number in UD_Zaar-Autogramm'
udver: '2'
---

## Treebank Statistics: UD_Zaar-Autogramm: Features: `Number`

This feature is universal.
It occurs with 2 different values: `Plur`, `Sing`.

1675 tokens (22%) have a non-empty value of `Number`.
223 types (16%) occur at least once with a non-empty value of `Number`.
86 lemmas (10%) occur at least once with a non-empty value of `Number`.
The feature is used with 7 part-of-speech tags: <tt><a href="say_autogramm-pos-AUX.html">AUX</a></tt> (1026; 13% instances), <tt><a href="say_autogramm-pos-PRON.html">PRON</a></tt> (516; 7% instances), <tt><a href="say_autogramm-pos-NOUN.html">NOUN</a></tt> (51; 1% instances), <tt><a href="say_autogramm-pos-DET.html">DET</a></tt> (49; 1% instances), <tt><a href="say_autogramm-pos-VERB.html">VERB</a></tt> (27; 0% instances), <tt><a href="say_autogramm-pos-ADJ.html">ADJ</a></tt> (5; 0% instances), <tt><a href="say_autogramm-pos-PROPN.html">PROPN</a></tt> (1; 0% instances).

### `AUX`

1026 <tt><a href="say_autogramm-pos-AUX.html">AUX</a></tt> tokens (99% of all `AUX` tokens) have a non-empty value of `Number`.

The most frequent other feature values with which `AUX` and `Number` co-occurred: <tt><a href="say_autogramm-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (783; 76%), <tt><a href="say_autogramm-feat-Tense.html">Tense</a></tt><tt>=EMPTY</tt> (718; 70%).

`AUX` tokens may have the following values of `Number`:

* `Plur` (368; 36% of non-empty `Number`): <em>mə́, mə̀, má, myǎː, tə́, tá, myàː, tə̀, yǎː, tàː</em>
* `Sing` (658; 64% of non-empty `Number`): <em>wò, mə, á, àː, myáː, yáː, kə, ma, ʧáː, tə̀</em>
* `EMPTY` (13): <em>tà, yi, tâ, tàyáː</em>

<table>
  <tr><th>Paradigm <i>á</i></th><th><tt>Sing</tt></th><th><tt>Plur</tt></th></tr>
  <tr><td><tt><tt><a href="say_autogramm-feat-Person.html">Person</a></tt><tt>=1</tt></tt></td><td><em>mə</em></td><td><em>mə́, mə̀</em></td></tr>
  <tr><td><tt><tt><a href="say_autogramm-feat-Person.html">Person</a></tt><tt>=2</tt></tt></td><td><em>kə</em></td><td><em>kə́</em></td></tr>
  <tr><td><tt><tt><a href="say_autogramm-feat-Person.html">Person</a></tt><tt>=3</tt></tt></td><td><em>á</em></td><td><em>tə́</em></td></tr>
</table>

### `PRON`

516 <tt><a href="say_autogramm-pos-PRON.html">PRON</a></tt> tokens (92% of all `PRON` tokens) have a non-empty value of `Number`.

The most frequent other feature values with which `PRON` and `Number` co-occurred: <tt><a href="say_autogramm-feat-PronType.html">PronType</a></tt><tt>=Prs</tt> (511; 99%), <tt><a href="say_autogramm-feat-Person.html">Person</a></tt><tt>=3</tt> (270; 52%).

`PRON` tokens may have the following values of `Number`:

* `Plur` (174; 34% of non-empty `Number`): <em>=mí, =ʃí, ʧì, gòpm, =wôpm, mì, myàːní, =kí, =wàːsə̀n, =wòpm</em>
* `Sing` (342; 66% of non-empty `Number`): <em>=tə, ɣáy, =m, =tə̀, gòs, myâːn, =ɣə, =tíː, =kə, =əm</em>
* `EMPTY` (45): <em>gíː, yóːɗan, níː, nîː, gyòː, gyóːɗan, gín, kóːnúː, =m, dàːsóːɗa</em>

`Number` seems to be **lexical feature** of `PRON`. 100% lemmas (35) occur only with one value of `Number`.

### `NOUN`

51 <tt><a href="say_autogramm-pos-NOUN.html">NOUN</a></tt> tokens (7% of all `NOUN` tokens) have a non-empty value of `Number`.

`NOUN` tokens may have the following values of `Number`:

* `Plur` (51; 100% of non-empty `Number`): <em>mə́n, guɗi, gerʃí, zàrsə̀, gút, kaɗanʃí, mərə́, mərə́m, mərə́n, məːrə́ŋsə</em>
* `EMPTY` (647): <em>laː, də̀n, dzàn, lə̂p, vìː, dzàŋ, ngə́tn, ŋáː, gə̀t, ʒà</em>

`Number` seems to be **lexical feature** of `NOUN`. 100% lemmas (14) occur only with one value of `Number`.

### `DET`

49 <tt><a href="say_autogramm-pos-DET.html">DET</a></tt> tokens (53% of all `DET` tokens) have a non-empty value of `Number`.

The most frequent other feature values with which `DET` and `Number` co-occurred: <tt><a href="say_autogramm-feat-Definite.html">Definite</a></tt><tt>=EMPTY</tt> (49; 100%), <tt><a href="say_autogramm-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (49; 100%).

`DET` tokens may have the following values of `Number`:

* `Plur` (49; 100% of non-empty `Number`): <em>gyaː, gyáː, gyǎː, sú</em>
* `EMPTY` (44): <em>gíː, wón, gón, gyòː, kotá, XX, dúk, gòn, wannan</em>

### `VERB`

27 <tt><a href="say_autogramm-pos-VERB.html">VERB</a></tt> tokens (2% of all `VERB` tokens) have a non-empty value of `Number`.

The most frequent other feature values with which `VERB` and `Number` co-occurred: <tt><a href="say_autogramm-feat-Aspect.html">Aspect</a></tt><tt>=EMPTY</tt> (24; 89%), <tt><a href="say_autogramm-feat-VerbForm.html">VerbForm</a></tt><tt>=Fin</tt> (22; 81%).

`VERB` tokens may have the following values of `Number`:

* `Plur` (27; 100% of non-empty `Number`): <em>kǐːr, kǐːríː, kǔːp, náːt, sǔn, yàːlɣə̀nì, kwáːn, kúːp, màːn, máːn</em>
* `EMPTY` (1193): <em>ɬə́, fi, mán, súː, wul, yel, tu, yi, ɬə, ɬǐː</em>

`Number` seems to be **lexical feature** of `VERB`. 100% lemmas (11) occur only with one value of `Number`.

### `ADJ`

5 <tt><a href="say_autogramm-pos-ADJ.html">ADJ</a></tt> tokens (10% of all `ADJ` tokens) have a non-empty value of `Number`.

The most frequent other feature values with which `ADJ` and `Number` co-occurred: <tt><a href="say_autogramm-feat-Definite.html">Definite</a></tt><tt>=Cons</tt> (4; 80%).

`ADJ` tokens may have the following values of `Number`:

* `Plur` (5; 100% of non-empty `Number`): <em>mərə́, məːri</em>
* `EMPTY` (45): <em>ŋaː, ŋǎː, ʧólák, mari, nʤwâːtn, ʒèlì, ʧolak, banza, bàptàk, mərə</em>

### `PROPN`

1 <tt><a href="say_autogramm-pos-PROPN.html">PROPN</a></tt> tokens (1% of all `PROPN` tokens) have a non-empty value of `Number`.

`PROPN` tokens may have the following values of `Number`:

* `Plur` (1; 100% of non-empty `Number`): <em>Bàtùràːye</em>
* `EMPTY` (195): <em>Kímsə, Bàːbá, Malâːr, Púʤì, Tʃôkn, Zəgì, Féːlêks, Gôː, Kámʃâk, Làːdí</em>

## Relations with Agreement in `Number`

The 10 most frequent relations where parent and child node agree in `Number`:
<tt>PRON --[<tt><a href="say_autogramm-dep-nmod.html">nmod</a></tt>]--> PRON</tt> (9; 82%),
<tt>NOUN --[<tt><a href="say_autogramm-dep-reparandum.html">reparandum</a></tt>]--> NOUN</tt> (4; 100%),
<tt>PRON --[<tt><a href="say_autogramm-dep-reparandum.html">reparandum</a></tt>]--> PRON</tt> (4; 100%),
<tt>PRON --[<tt><a href="say_autogramm-dep-appos.html">appos</a></tt>]--> NOUN</tt> (3; 100%),
<tt>AUX --[<tt><a href="say_autogramm-dep-conj.html">conj</a></tt>]--> AUX</tt> (2; 67%),
<tt>AUX --[<tt><a href="say_autogramm-dep-dislocated.html">dislocated</a></tt>]--> NOUN</tt> (2; 67%),
<tt>ADJ --[<tt><a href="say_autogramm-dep-compound.html">compound</a></tt>]--> NOUN</tt> (1; 100%),
<tt>AUX --[<tt><a href="say_autogramm-dep-discourse.html">discourse</a></tt>]--> PRON</tt> (1; 100%),
<tt>AUX --[<tt><a href="say_autogramm-dep-reparandum.html">reparandum</a></tt>]--> AUX</tt> (1; 100%),
<tt>VERB --[<tt><a href="say_autogramm-dep-acl-relcl.html">acl:relcl</a></tt>]--> VERB</tt> (1; 100%).

