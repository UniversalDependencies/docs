---
layout: base
title:  'Statistics of ExtPos in UD_Yiddish-YiTB'
udver: '2'
---

## Treebank Statistics: UD_Yiddish-YiTB: Features: `ExtPos`

This feature is language-specific.
It occurs with 2 different values: `ADP`, `ADV`.

2 tokens (0%) have a non-empty value of `ExtPos`.
2 types (0%) occur at least once with a non-empty value of `ExtPos`.
2 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 2 part-of-speech tags: <tt><a href="yi_yitb-pos-ADP.html">ADP</a></tt> (1; 0% instances), <tt><a href="yi_yitb-pos-DET.html">DET</a></tt> (1; 0% instances).

### `ADP`

1 <tt><a href="yi_yitb-pos-ADP.html">ADP</a></tt> tokens (0% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADV` (1; 100% of non-empty `ExtPos`): <em>אין</em>

### `DET`

1 <tt><a href="yi_yitb-pos-DET.html">DET</a></tt> tokens (0% of all `DET` tokens) have a non-empty value of `ExtPos`.

`DET` tokens may have the following values of `ExtPos`:

* `ADP` (1; 100% of non-empty `ExtPos`): <em>אַ</em>

