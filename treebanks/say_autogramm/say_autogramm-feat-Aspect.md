---
layout: base
title:  'Statistics of Aspect in UD_Zaar-Autogramm'
udver: '2'
---

## Treebank Statistics: UD_Zaar-Autogramm: Features: `Aspect`

This feature is universal but the values `Iter` are language-specific.
It occurs with 4 different values: `Imp`, `Iter`, `Perf`, `Prog`.

689 tokens (3%) have a non-empty value of `Aspect`.
40 types (1%) occur at least once with a non-empty value of `Aspect`.
4 lemmas (0%) occur at least once with a non-empty value of `Aspect`.
The feature is used with 1 part-of-speech tags: <tt><a href="say_autogramm-pos-AUX.html">AUX</a></tt> (689; 3% instances).

### `AUX`

689 <tt><a href="say_autogramm-pos-AUX.html">AUX</a></tt> tokens (22% of all `AUX` tokens) have a non-empty value of `Aspect`.

The most frequent other feature values with which `AUX` and `Aspect` co-occurred: <tt><a href="say_autogramm-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (689; 100%), <tt><a href="say_autogramm-feat-Tense.html">Tense</a></tt><tt>=EMPTY</tt> (689; 100%), <tt><a href="say_autogramm-feat-Number.html">Number</a></tt><tt>=Sing</tt> (377; 55%).

`AUX` tokens may have the following values of `Aspect`:

* `Imp` (362; 53% of non-empty `Aspect`): <em>yáː, ʧáː, ʧǎː, myǎː, myáː, yǎː, ʧàː, kyáː, myàː, kyǎː</em>
* `Iter` (36; 5% of non-empty `Aspect`): <em>yi, yí, yì, kíyí, yiː, k~, miː, míyí, ʧíyí</em>
* `Perf` (248; 36% of non-empty `Aspect`): <em>àː, káː, máː, tàː, màː, tà~</em>
* `Prog` (43; 6% of non-empty `Aspect`): <em>ʧàː, ʧìɣá, ʧǎː, myáːnaː, myǎː, kiɣá, myaː, yiɣá, ʧáːnaː, kyǎː</em>
* `EMPTY` (2485): <em>á, tə́, wò, tá, tə̀, kə, yáː, mə, nə, mə́</em>

## Relations with Agreement in `Aspect`

The 10 most frequent relations where parent and child node agree in `Aspect`:
<tt>AUX --[<tt><a href="say_autogramm-dep-reparandum.html">reparandum</a></tt>]--> AUX</tt> (1; 100%).

