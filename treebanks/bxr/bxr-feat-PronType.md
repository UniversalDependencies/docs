---
layout: base
title:  'Statistics of PronType in UD_Buryat'
udver: '2'
---

## Treebank Statistics: UD_Buryat: Features: `PronType`

This feature is universal.
It occurs with 4 different values: `Dem`, `Ind`, `Int`, `Prs`.

370 tokens (4%) have a non-empty value of `PronType`.
130 types (3%) occur at least once with a non-empty value of `PronType`.
38 lemmas (2%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="bxr-pos-PRON.html">PRON</a></tt> (231; 2% instances), <tt><a href="bxr-pos-DET.html">DET</a></tt> (122; 1% instances), <tt><a href="bxr-pos-ADV.html">ADV</a></tt> (17; 0% instances).

### `PRON`

231 <tt><a href="bxr-pos-PRON.html">PRON</a></tt> tokens (83% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="bxr-feat-Case.html">Case</a></tt><tt>=Nom</tt> (117; 51%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (87; 38% of non-empty `PronType`): эдэ тэрэ тэрэнэй энэшни Тэдэнэй тэдэнэр энэ бүгэдые бүхэниие тэрэшни
* `Ind` (7; 3% of non-empty `PronType`): Бултанайнгаа бултадаа бултанииемнай бултыемнай бусад бусадһаа олонхимнай
* `Int` (8; 3% of non-empty `PronType`): юуб юу Хамагые юун юуншье
* `Prs` (129; 56% of non-empty `PronType`): би минии манай намда намайе бидэ та ши бидэнэй маанарые
* `EMPTY` (49): өөрынгөө өөрөө нэгэниинь өөрын тэдэнээ хэншье хэнэй юугээрээ юунһээшье Өөһэнөө

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (19) occur only with one value of `PronType`.

### `DET`

122 <tt><a href="bxr-pos-DET.html">DET</a></tt> tokens (98% of all `DET` tokens) have a non-empty value of `PronType`.

`DET` tokens may have the following values of `PronType`:

* `Dem` (78; 64% of non-empty `PronType`): энэ тэрэ эрэ энэл Энээхэн тиихэ
* `Ind` (31; 25% of non-empty `PronType`): булта бусад зарим ямаршье бултадаа нүгөө хамаг хуу хэдэн Бухы
* `Int` (13; 11% of non-empty `PronType`): ямар алишье
* `EMPTY` (2): Тэрэнэй бүхэнэй

`PronType` seems to be **lexical feature** of `DET`. 100% lemmas (19) occur only with one value of `PronType`.

### `ADV`

17 <tt><a href="bxr-pos-ADV.html">ADV</a></tt> tokens (2% of all `ADV` tokens) have a non-empty value of `PronType`.

`ADV` tokens may have the following values of `PronType`:

* `Int` (17; 100% of non-empty `PronType`): хэды юундэ Юундэб хэдышье хэзээб Хаанаһаа хэзээ ямараар
* `EMPTY` (771): ехэ мүнөө гэһэн бэшэ һайхан эндэ эгээл хадаа үшөө бүхы

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="bxr-dep-det.html">det</a></tt>]--> PRON</tt> (3; 100%),
<tt>PRON --[<tt><a href="bxr-dep-det.html">det</a></tt>]--> DET</tt> (1; 100%).

