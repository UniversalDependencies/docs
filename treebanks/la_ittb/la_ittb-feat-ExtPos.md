---
layout: base
title:  'Statistics of ExtPos in UD_Latin-ITTB'
udver: '2'
---

## Treebank Statistics: UD_Latin-ITTB: Features: `ExtPos`

This feature is language-specific.
It occurs with 3 different values: `ADV`, `CCONJ`, `SCONJ`.

105 tokens (0%) have a non-empty value of `ExtPos`.
17 types (0%) occur at least once with a non-empty value of `ExtPos`.
18 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 5 part-of-speech tags: <tt><a href="la_ittb-pos-ADV.html">ADV</a></tt> (86; 0% instances), <tt><a href="la_ittb-pos-SCONJ.html">SCONJ</a></tt> (13; 0% instances), <tt><a href="la_ittb-pos-ADP.html">ADP</a></tt> (2; 0% instances), <tt><a href="la_ittb-pos-CCONJ.html">CCONJ</a></tt> (2; 0% instances), <tt><a href="la_ittb-pos-PRON.html">PRON</a></tt> (2; 0% instances).

### `ADV`

86 <tt><a href="la_ittb-pos-ADV.html">ADV</a></tt> tokens (0% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="la_ittb-feat-AdvType.html">AdvType</a></tt><tt>=EMPTY</tt> (86; 100%), <tt><a href="la_ittb-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (86; 100%), <tt><a href="la_ittb-feat-Compound.html">Compound</a></tt><tt>=EMPTY</tt> (84; 98%).

`ADV` tokens may have the following values of `ExtPos`:

* `ADV` (3; 3% of non-empty `ExtPos`): <em>puta, tunc</em>
* `SCONJ` (83; 97% of non-empty `ExtPos`): <em>ita, simul, sic, statim, utpote, etiam, tamen</em>

### `SCONJ`

13 <tt><a href="la_ittb-pos-SCONJ.html">SCONJ</a></tt> tokens (0% of all `SCONJ` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `SCONJ` and `ExtPos` co-occurred: <tt><a href="la_ittb-feat-Compound.html">Compound</a></tt><tt>=EMPTY</tt> (11; 85%), <tt><a href="la_ittb-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (7; 54%).

`SCONJ` tokens may have the following values of `ExtPos`:

* `ADV` (7; 54% of non-empty `ExtPos`): <em>ut, puta</em>
* `SCONJ` (6; 46% of non-empty `ExtPos`): <em>quod, propterea, si</em>

### `ADP`

2 <tt><a href="la_ittb-pos-ADP.html">ADP</a></tt> tokens (0% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `SCONJ` (2; 100% of non-empty `ExtPos`): <em>in</em>

### `CCONJ`

2 <tt><a href="la_ittb-pos-CCONJ.html">CCONJ</a></tt> tokens (0% of all `CCONJ` tokens) have a non-empty value of `ExtPos`.

`CCONJ` tokens may have the following values of `ExtPos`:

* `ADV` (1; 50% of non-empty `ExtPos`): <em>scilicet</em>
* `SCONJ` (1; 50% of non-empty `ExtPos`): <em>uel</em>

### `PRON`

2 <tt><a href="la_ittb-pos-PRON.html">PRON</a></tt> tokens (0% of all `PRON` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PRON` and `ExtPos` co-occurred: <tt><a href="la_ittb-feat-Case.html">Case</a></tt><tt>=Nom</tt> (2; 100%), <tt><a href="la_ittb-feat-Compound.html">Compound</a></tt><tt>=EMPTY</tt> (2; 100%), <tt><a href="la_ittb-feat-Gender.html">Gender</a></tt><tt>=Neut</tt> (2; 100%), <tt><a href="la_ittb-feat-InflClass.html">InflClass</a></tt><tt>=LatPron</tt> (2; 100%), <tt><a href="la_ittb-feat-Number.html">Number</a></tt><tt>=Sing</tt> (2; 100%), <tt><a href="la_ittb-feat-Reflex.html">Reflex</a></tt><tt>=EMPTY</tt> (2; 100%).

`PRON` tokens may have the following values of `ExtPos`:

* `CCONJ` (1; 50% of non-empty `ExtPos`): <em>id</em>
* `SCONJ` (1; 50% of non-empty `ExtPos`): <em>quod</em>

