---
layout: base
title:  'Statistics of Polarity in UD_Hausa-WesternAutogramm'
udver: '2'
---

## Treebank Statistics: UD_Hausa-WesternAutogramm: Features: `Polarity`

This feature is universal.
It occurs with 1 different values: `Neg`.

267 tokens (2%) have a non-empty value of `Polarity`.
26 types (2%) occur at least once with a non-empty value of `Polarity`.
6 lemmas (1%) occur at least once with a non-empty value of `Polarity`.
The feature is used with 2 part-of-speech tags: <tt><a href="ha_westernautogramm-pos-AUX.html">AUX</a></tt> (176; 1% instances), <tt><a href="ha_westernautogramm-pos-PART.html">PART</a></tt> (91; 1% instances).

### `AUX`

176 <tt><a href="ha_westernautogramm-pos-AUX.html">AUX</a></tt> tokens (7% of all `AUX` tokens) have a non-empty value of `Polarity`.

The most frequent other feature values with which `AUX` and `Polarity` co-occurred: <tt><a href="ha_westernautogramm-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (171; 97%), <tt><a href="ha_westernautogramm-feat-Person.html">Person</a></tt><tt>=3</tt> (146; 83%), <tt><a href="ha_westernautogramm-feat-Aspect.html">Aspect</a></tt><tt>=ProgNeg</tt> (114; 65%).

`AUX` tokens may have the following values of `Polarity`:

* `Neg` (176; 100% of non-empty `Polarity`): <em>bâːshi, bâːta, bài, bàtà, bâːsu, bâː’a, bà’à, bàn, bàsù, bàkà</em>
* `EMPTY` (2300): <em>à, shì, ankà, tà, sù, sunkà, yaː, yac, kà, an</em>

### `PART`

91 <tt><a href="ha_westernautogramm-pos-PART.html">PART</a></tt> tokens (32% of all `PART` tokens) have a non-empty value of `Polarity`.

The most frequent other feature values with which `PART` and `Polarity` co-occurred: <tt><a href="ha_westernautogramm-feat-Aspect.html">Aspect</a></tt><tt>=EMPTY</tt> (91; 100%), <tt><a href="ha_westernautogramm-feat-PartType.html">PartType</a></tt><tt>=Neg</tt> (91; 100%).

`PART` tokens may have the following values of `Polarity`:

* `Neg` (91; 100% of non-empty `Polarity`): <em>ba, bâː, baːbù, kât, bàː, kâsh, kâ', kâk, kâm, kâs</em>
* `EMPTY` (193): <em>kuma, kâu, ta, dai, maː, àkwai, zâː, gàː, hwa, koː</em>

## Relations with Agreement in `Polarity`

The 10 most frequent relations where parent and child node agree in `Polarity`:
<tt>PART --[<tt><a href="ha_westernautogramm-dep-parataxis.html">parataxis</a></tt>]--> PART</tt> (3; 100%),
<tt>PART --[<tt><a href="ha_westernautogramm-dep-advmod.html">advmod</a></tt>]--> PART</tt> (1; 100%),
<tt>PART --[<tt><a href="ha_westernautogramm-dep-aux.html">aux</a></tt>]--> AUX</tt> (1; 100%).

