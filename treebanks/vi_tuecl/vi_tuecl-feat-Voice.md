---
layout: base
title:  'Statistics of Voice in UD_Vietnamese-TueCL'
udver: '2'
---

## Treebank Statistics: UD_Vietnamese-TueCL: Features: `Voice`

This feature is universal.
It occurs with 1 different values: `Pass`.

16 tokens (1%) have a non-empty value of `Voice`.
2 types (0%) occur at least once with a non-empty value of `Voice`.
2 lemmas (0%) occur at least once with a non-empty value of `Voice`.
The feature is used with 1 part-of-speech tags: <tt><a href="vi_tuecl-pos-AUX.html">AUX</a></tt> (16; 1% instances).

### `AUX`

16 <tt><a href="vi_tuecl-pos-AUX.html">AUX</a></tt> tokens (19% of all `AUX` tokens) have a non-empty value of `Voice`.

The most frequent other feature values with which `AUX` and `Voice` co-occurred: <tt><a href="vi_tuecl-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (16; 100%), <tt><a href="vi_tuecl-feat-VerbType.html">VerbType</a></tt><tt>=Aux</tt> (11; 69%).

`AUX` tokens may have the following values of `Voice`:

* `Pass` (16; 100% of non-empty `Voice`): <em>được, bị</em>
* `EMPTY` (67): <em>là, phải, có thể, được, không thể, có vẻ, nghĩa là</em>

