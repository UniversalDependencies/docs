---
layout: base
title:  'Statistics of PronType in UD_Swedish-SweLL'
udver: '2'
---

## Treebank Statistics: UD_Swedish-SweLL: Features: `PronType`

This feature is universal.
It occurs with 9 different values: `Art`, `Dem`, `Ind`, `Int`, `Neg`, `Prs`, `Rcp`, `Rel`, `Tot`.

1519 tokens (18%) have a non-empty value of `PronType`.
96 types (5%) occur at least once with a non-empty value of `PronType`.
36 lemmas (2%) occur at least once with a non-empty value of `PronType`.
The feature is used with 3 part-of-speech tags: <tt><a href="sv_swell-pos-PRON.html">PRON</a></tt> (1212; 14% instances), <tt><a href="sv_swell-pos-DET.html">DET</a></tt> (306; 4% instances), <tt><a href="sv_swell-pos-SCONJ.html">SCONJ</a></tt> (1; 0% instances).

### `PRON`

1212 <tt><a href="sv_swell-pos-PRON.html">PRON</a></tt> tokens (100% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="sv_swell-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (1070; 88%), <tt><a href="sv_swell-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (937; 77%), <tt><a href="sv_swell-feat-Number.html">Number</a></tt><tt>=Sing</tt> (813; 67%), <tt><a href="sv_swell-feat-Gender.html">Gender</a></tt><tt>=Com</tt> (631; 52%).

`PRON` tokens may have the following values of `PronType`:

* `Art` (1; 0% of non-empty `PronType`): <em>ett</em>
* `Dem` (8; 1% of non-empty `PronType`): <em>detta, dessa</em>
* `Ind` (125; 10% of non-empty `PronType`): <em>man, andra, ens, många, någon, någonting, något, Vissa, annat, fler</em>
* `Int` (26; 2% of non-empty `PronType`): <em>vad, vem, vilket</em>
* `Neg` (5; 0% of non-empty `PronType`): <em>ingenting, inga, ingen, inget</em>
* `Prs` (919; 76% of non-empty `PronType`): <em>jag, det, vi, de, sig, mig, du, min, oss, han</em>
* `Rcp` (9; 1% of non-empty `PronType`): <em>varandra</em>
* `Rel` (104; 9% of non-empty `PronType`): <em>som, vilket</em>
* `Tot` (15; 1% of non-empty `PronType`): <em>alla, allt, båda</em>

<table>
  <tr><th>Paradigm <i>en</i></th><th><tt>Prs</tt></th><th><tt>Art</tt></th><th><tt>Ind</tt></th></tr>
  <tr><td><tt><tt><a href="sv_swell-feat-Case.html">Case</a></tt><tt>=Gen</tt>|<tt><a href="sv_swell-feat-Poss.html">Poss</a></tt><tt>=Yes</tt></tt></td><td></td><td></td><td><em>ens</em></td></tr>
  <tr><td><tt><tt><a href="sv_swell-feat-Gender.html">Gender</a></tt><tt>=Com</tt></tt></td><td><em>en</em></td><td></td><td></td></tr>
  <tr><td><tt><tt><a href="sv_swell-feat-Gender.html">Gender</a></tt><tt>=Neut</tt></tt></td><td></td><td><em>ett</em></td><td></td></tr>
</table>

`PronType` seems to be **lexical feature** of `PRON`. 91% lemmas (30) occur only with one value of `PronType`.

### `DET`

306 <tt><a href="sv_swell-pos-DET.html">DET</a></tt> tokens (100% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="sv_swell-feat-Number.html">Number</a></tt><tt>=Sing</tt> (253; 83%), <tt><a href="sv_swell-feat-Definite.html">Definite</a></tt><tt>=Ind</tt> (193; 63%), <tt><a href="sv_swell-feat-Gender.html">Gender</a></tt><tt>=Com</tt> (170; 56%).

`DET` tokens may have the following values of `PronType`:

* `Art` (240; 78% of non-empty `PronType`): <em>en, den, ett, de, det, dem, et</em>
* `Dem` (16; 5% of non-empty `PronType`): <em>detta, denna, dessa</em>
* `Ind` (7; 2% of non-empty `PronType`): <em>några, något</em>
* `Int` (11; 4% of non-empty `PronType`): <em>vilken, vilka</em>
* `Neg` (12; 4% of non-empty `PronType`): <em>ingen, inga, inget, no</em>
* `Tot` (20; 7% of non-empty `PronType`): <em>varje, alla, all, allt, varj</em>

`PronType` seems to be **lexical feature** of `DET`. 100% lemmas (11) occur only with one value of `PronType`.

### `SCONJ`

1 <tt><a href="sv_swell-pos-SCONJ.html">SCONJ</a></tt> tokens (0% of all `SCONJ` tokens) have a non-empty value of `PronType`.

`SCONJ` tokens may have the following values of `PronType`:

* `Rel` (1; 100% of non-empty `PronType`): <em>som</em>
* `EMPTY` (313): <em>att, när, om, eftersom, som, så, då, än, medan, an</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="sv_swell-dep-det.html">det</a></tt>]--> PRON</tt> (1; 100%),
<tt>PRON --[<tt><a href="sv_swell-dep-expl.html">expl</a></tt>]--> PRON</tt> (1; 100%).

