---
layout: base
title:  'Statistics of Aspect in UD_Kaapor-TuDeT'
udver: '2'
---

## Treebank Statistics: UD_Kaapor-TuDeT: Features: `Aspect`

This feature is universal but the values `Frus` are language-specific.
It occurs with 2 different values: `Frus`, `Perf`.

4 tokens (2%) have a non-empty value of `Aspect`.
2 types (1%) occur at least once with a non-empty value of `Aspect`.
2 lemmas (2%) occur at least once with a non-empty value of `Aspect`.
The feature is used with 2 part-of-speech tags: <tt><a href="urb_tudet-pos-AUX.html">AUX</a></tt> (3; 1% instances), <tt><a href="urb_tudet-pos-PART.html">PART</a></tt> (1; 0% instances).

### `AUX`

3 <tt><a href="urb_tudet-pos-AUX.html">AUX</a></tt> tokens (60% of all `AUX` tokens) have a non-empty value of `Aspect`.

The most frequent other feature values with which `AUX` and `Aspect` co-occurred: <tt><a href="urb_tudet-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (3; 100%).

`AUX` tokens may have the following values of `Aspect`:

* `Perf` (3; 100% of non-empty `Aspect`): <em>ʔɪ</em>
* `EMPTY` (2): <em>oho, ʔɪ</em>

### `PART`

1 <tt><a href="urb_tudet-pos-PART.html">PART</a></tt> tokens (4% of all `PART` tokens) have a non-empty value of `Aspect`.

`PART` tokens may have the following values of `Aspect`:

* `Frus` (1; 100% of non-empty `Aspect`): <em>tipe</em>
* `EMPTY` (25): <em>ke, je, tĩ, riki, ta, ketĩ, mi, ã</em>

