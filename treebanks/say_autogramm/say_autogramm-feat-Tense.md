---
layout: base
title:  'Statistics of Tense in UD_Zaar-Autogramm'
udver: '2'
---

## Treebank Statistics: UD_Zaar-Autogramm: Features: `Tense`

This feature is universal but the values `Imm`, `Rec`, `Rem` are language-specific.
It occurs with 4 different values: `Fut`, `Imm`, `Rec`, `Rem`.

318 tokens (4%) have a non-empty value of `Tense`.
41 types (3%) occur at least once with a non-empty value of `Tense`.
13 lemmas (2%) occur at least once with a non-empty value of `Tense`.
The feature is used with 1 part-of-speech tags: <tt><a href="say_autogramm-pos-AUX.html">AUX</a></tt> (318; 4% instances).

### `AUX`

318 <tt><a href="say_autogramm-pos-AUX.html">AUX</a></tt> tokens (31% of all `AUX` tokens) have a non-empty value of `Tense`.

The most frequent other feature values with which `AUX` and `Tense` co-occurred: <tt><a href="say_autogramm-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (317; 100%), <tt><a href="say_autogramm-feat-Aspect.html">Aspect</a></tt><tt>=EMPTY</tt> (275; 86%), <tt><a href="say_autogramm-feat-Number.html">Number</a></tt><tt>=Sing</tt> (210; 66%).

`AUX` tokens may have the following values of `Tense`:

* `Fut` (191; 60% of non-empty `Tense`): <em>wò, má, ma, ka, tá, ká, mayi</em>
* `Imm` (12; 4% of non-empty `Tense`): <em>áyǎː, máːyí, áyí, kíː, mìyǎː, míyàː, tìyǎː, àːyí</em>
* `Rec` (5; 2% of non-empty `Tense`): <em>mənáːyáː, kənáː, mənáyáː, mənáː</em>
* `Rem` (110; 35% of non-empty `Tense`): <em>átâ, mətá, kətá, tà, tə̀tà, mə̀tà, átâyáː, kə̀tàyáː, mətáyáː, tâ</em>
* `EMPTY` (721): <em>mə́, mə̀, mə, á, àː, myáː, tə̀, yáː, kə, ʧáː</em>

`Tense` seems to be **lexical feature** of `AUX`. 100% lemmas (13) occur only with one value of `Tense`.

