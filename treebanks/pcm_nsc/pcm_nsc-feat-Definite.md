---
layout: base
title:  'Statistics of Definite in UD_Naija-NSC'
udver: '2'
---

## Treebank Statistics: UD_Naija-NSC: Features: `Definite`

This feature is universal but the values `Spec` are language-specific.
It occurs with 3 different values: `Def`, `Ind`, `Spec`.

3181 tokens (2%) have a non-empty value of `Definite`.
7 types (0%) occur at least once with a non-empty value of `Definite`.
4 lemmas (0%) occur at least once with a non-empty value of `Definite`.
The feature is used with 2 part-of-speech tags: <tt><a href="pcm_nsc-pos-DET.html">DET</a></tt> (3179; 2% instances), <tt><a href="pcm_nsc-pos-ADP.html">ADP</a></tt> (2; 0% instances).

### `DET`

3179 <tt><a href="pcm_nsc-pos-DET.html">DET</a></tt> tokens (56% of all `DET` tokens) have a non-empty value of `Definite`.

The most frequent other feature values with which `DET` and `Definite` co-occurred: <tt><a href="pcm_nsc-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (3179; 100%), <tt><a href="pcm_nsc-feat-PronType.html">PronType</a></tt><tt>=Art</tt> (3179; 100%).

`DET` tokens may have the following values of `Definite`:

* `Def` (2813; 88% of non-empty `Definite`): <em>di, the</em>
* `Ind` (136; 4% of non-empty `Definite`): <em>a, an</em>
* `Spec` (230; 7% of non-empty `Definite`): <em>one</em>
* `EMPTY` (2483): <em>dis, dat, all, some, dose, any, anoder, which, every, dese</em>

### `ADP`

2 <tt><a href="pcm_nsc-pos-ADP.html">ADP</a></tt> tokens (0% of all `ADP` tokens) have a non-empty value of `Definite`.

`ADP` tokens may have the following values of `Definite`:

* `Ind` (2; 100% of non-empty `Definite`): <em>On</em>
* `EMPTY` (6928): <em>for, to, of, as, like, inside, from, in, on, before</em>

## Relations with Agreement in `Definite`

The 10 most frequent relations where parent and child node agree in `Definite`:
<tt>DET --[<tt><a href="pcm_nsc-dep-reparandum.html">reparandum</a></tt>]--> DET</tt> (28; 65%),
<tt>ADP --[<tt><a href="pcm_nsc-dep-flat.html">flat</a></tt>]--> DET</tt> (2; 100%).

