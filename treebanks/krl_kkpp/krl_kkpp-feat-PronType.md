---
layout: base
title:  'Statistics of PronType in UD_Karelian-KKPP'
udver: '2'
---

## Treebank Statistics: UD_Karelian-KKPP: Features: `PronType`

This feature is universal.
It occurs with 3 different values: `Dem`, `Ind`, `Prs`.

243 tokens (8%) have a non-empty value of `PronType`.
86 types (6%) occur at least once with a non-empty value of `PronType`.
19 lemmas (2%) occur at least once with a non-empty value of `PronType`.
The feature is used with 1 part-of-speech tags: <tt><a href="krl_kkpp-pos-PRON.html">PRON</a></tt> (243; 8% instances).

### `PRON`

243 <tt><a href="krl_kkpp-pos-PRON.html">PRON</a></tt> tokens (84% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="krl_kkpp-feat-Number.html">Number</a></tt><tt>=Sing</tt> (147; 60%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (73; 30% of non-empty `PronType`): <em>še, šiitä, šen, ne, tätä, šitä, šinne, niitä, šiih, niistä</em>
* `Ind` (43; 18% of non-empty `PronType`): <em>kaikki, kaikin, kumpaset, kumpasien, kennih, kumpaista, kumpasešta, monet, muut, muuta</em>
* `Prs` (127; 52% of non-empty `PronType`): <em>hyö, mie, hiän, myö, meijän, miun, šie, meilä, hänen, šiun</em>
* `EMPTY` (46): <em>mitä, mih, iččie, missä, mit, iče, ket, mi, mistä, Molommilla</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (19) occur only with one value of `PronType`.

