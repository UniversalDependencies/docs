---
layout: base
title:  'Statistics of NumType in UD_Esperanto-Prago'
udver: '2'
---

## Treebank Statistics: UD_Esperanto-Prago: Features: `NumType`

This feature is universal.
It occurs with 1 different values: `Card`.

2 tokens (0%) have a non-empty value of `NumType`.
1 types (0%) occur at least once with a non-empty value of `NumType`.
1 lemmas (0%) occur at least once with a non-empty value of `NumType`.
The feature is used with 1 part-of-speech tags: <tt><a href="eo_prago-pos-NUM.html">NUM</a></tt> (2; 0% instances).

### `NUM`

2 <tt><a href="eo_prago-pos-NUM.html">NUM</a></tt> tokens (20% of all `NUM` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `NUM` and `NumType` co-occurred: <tt><a href="eo_prago-feat-NumForm.html">NumForm</a></tt><tt>=Word</tt> (2; 100%).

`NUM` tokens may have the following values of `NumType`:

* `Card` (2; 100% of non-empty `NumType`): <em>unu</em>
* `EMPTY` (8): <em>1, 1887, 2, 3, 4, 5, 6, 7</em>

