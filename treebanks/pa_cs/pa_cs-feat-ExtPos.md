---
layout: base
title:  'Statistics of ExtPos in UD_Punjabi-CS'
udver: '2'
---

## Treebank Statistics: UD_Punjabi-CS: Features: `ExtPos`

This feature is language-specific.
It occurs with 4 different values: `ADJ`, `ADP`, `ADV`, `PRON`.

18 tokens (1%) have a non-empty value of `ExtPos`.
10 types (1%) occur at least once with a non-empty value of `ExtPos`.
9 lemmas (2%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 4 part-of-speech tags: <tt><a href="pa_cs-pos-ADV.html">ADV</a></tt> (13; 1% instances), <tt><a href="pa_cs-pos-PRON.html">PRON</a></tt> (3; 0% instances), <tt><a href="pa_cs-pos-ADJ.html">ADJ</a></tt> (1; 0% instances), <tt><a href="pa_cs-pos-ADP.html">ADP</a></tt> (1; 0% instances).

### `ADV`

13 <tt><a href="pa_cs-pos-ADV.html">ADV</a></tt> tokens (7% of all `ADV` tokens) have a non-empty value of `ExtPos`.

`ADV` tokens may have the following values of `ExtPos`:

* `ADV` (13; 100% of non-empty `ExtPos`): <em>ਇਸ, ਅਜੇ, ਇਸੇ, ਜਦੋਂ, ਜਿਵੇਂ, ਪਰ</em>

### `PRON`

3 <tt><a href="pa_cs-pos-PRON.html">PRON</a></tt> tokens (1% of all `PRON` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PRON` and `ExtPos` co-occurred: <tt><a href="pa_cs-feat-Case.html">Case</a></tt><tt>=EMPTY</tt> (2; 67%), <tt><a href="pa_cs-feat-Number.html">Number</a></tt><tt>=Sing</tt> (2; 67%), <tt><a href="pa_cs-feat-PronType.html">PronType</a></tt><tt>=Prs</tt> (2; 67%).

`PRON` tokens may have the following values of `ExtPos`:

* `PRON` (3; 100% of non-empty `ExtPos`): <em>ਆਪਣੇ, ਉਹ, ਮੈਂ</em>

### `ADJ`

1 <tt><a href="pa_cs-pos-ADJ.html">ADJ</a></tt> tokens (1% of all `ADJ` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADJ` and `ExtPos` co-occurred: <tt><a href="pa_cs-feat-Case.html">Case</a></tt><tt>=Acc</tt> (1; 100%), <tt><a href="pa_cs-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (1; 100%), <tt><a href="pa_cs-feat-Number.html">Number</a></tt><tt>=Sing</tt> (1; 100%).

`ADJ` tokens may have the following values of `ExtPos`:

* `ADJ` (1; 100% of non-empty `ExtPos`): <em>ਵੱਧ</em>

### `ADP`

1 <tt><a href="pa_cs-pos-ADP.html">ADP</a></tt> tokens (0% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (1; 100% of non-empty `ExtPos`): <em>ਇਸ</em>

