---
layout: base
title:  'Statistics of PronType in UD_Icelandic-GC'
udver: '2'
---

## Treebank Statistics: UD_Icelandic-GC: Features: `PronType`

This feature is universal.
It occurs with 1 different values: `Prs`.

4065 tokens (4%) have a non-empty value of `PronType`.
67 types (0%) occur at least once with a non-empty value of `PronType`.
33 lemmas (0%) occur at least once with a non-empty value of `PronType`.
The feature is used with 2 part-of-speech tags: <tt><a href="is_gc-pos-PRON.html">PRON</a></tt> (4047; 4% instances), <tt><a href="is_gc-pos-ADV.html">ADV</a></tt> (18; 0% instances).

### `PRON`

4047 <tt><a href="is_gc-pos-PRON.html">PRON</a></tt> tokens (52% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="is_gc-feat-Number.html">Number</a></tt><tt>=Sing</tt> (2730; 67%), <tt><a href="is_gc-feat-Person.html">Person</a></tt><tt>=3</tt> (2526; 62%), <tt><a href="is_gc-feat-Case.html">Case</a></tt><tt>=Nom</tt> (2410; 60%).

`PRON` tokens may have the following values of `PronType`:

* `Prs` (4047; 100% of non-empty `PronType`): <em>það, hann, ég, við, hún, sér, þeir, því, sig, hans</em>
* `EMPTY` (3755): <em>þetta, því, það, þess, þessu, sínum, allt, þeim, hvað, þessi</em>

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (33) occur only with one value of `PronType`.

### `ADV`

18 <tt><a href="is_gc-pos-ADV.html">ADV</a></tt> tokens (0% of all `ADV` tokens) have a non-empty value of `PronType`.

`ADV` tokens may have the following values of `PronType`:

* `Prs` (18; 100% of non-empty `PronType`): <em>því, það</em>
* `EMPTY` (10281): <em>ekki, þar, þá, í, á, fram, svo, upp, dag, til</em>

