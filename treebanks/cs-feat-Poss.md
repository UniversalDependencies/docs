---
layout: base
title:  'Statistics of Poss in UD_Czech'
udver: '2'
---

## Treebank Statistics: UD_Czech: Features: `Poss`

This feature is universal.
It occurs with 1 different values: `Yes`.

17024 tokens (1%) have a non-empty value of `Poss`.
1973 types (2%) occur at least once with a non-empty value of `Poss`.
1144 lemmas (2%) occur at least once with a non-empty value of `Poss`.
The feature is used with 2 part-of-speech tags: [DET](cs-pos-DET.html) (14317; 1% instances), [ADJ](cs-pos-ADJ.html) (2707; 0% instances).

### `DET`

14317 [DET](cs-pos-DET.html) tokens (26% of all `DET` tokens) have a non-empty value of `Poss`.

The most frequent other feature values with which `DET` and `Poss` co-occurred: <tt><a href="PronType.html">PronType</a>=Prs</tt> (13642; 95%), <tt><a href="Animacy.html">Animacy</a>=EMPTY</tt> (13350; 93%).

`DET` tokens may have the following values of `Poss`:

* `Yes` (14317; 100% of non-empty `Poss`): jeho jejich své její svou svého svých svůj naše svým
* `EMPTY` (40996): to které který která tím kteří tom této tomu tento

`Poss` seems to be **lexical feature** of `DET`. 100% lemmas (12) occur only with one value of `Poss`.

### `ADJ`

2707 [ADJ](cs-pos-ADJ.html) tokens (1% of all `ADJ` tokens) have a non-empty value of `Poss`.

The most frequent other feature values with which `ADJ` and `Poss` co-occurred: <tt><a href="Degree.html">Degree</a>=EMPTY</tt> (2707; 100%), <tt><a href="Polarity.html">Polarity</a>=EMPTY</tt> (2707; 100%), <tt><a href="Number.html">Number</a>=Sing</tt> (2189; 81%), <tt><a href="Animacy.html">Animacy</a>=EMPTY</tt> (1602; 59%).

`ADJ` tokens may have the following values of `Poss`:

* `Yes` (2707; 100% of non-empty `Poss`): Karlovy Karlových Nobelovy Milíčova Masarykově Karlova Karlově prezidentův Benešových Nobelovu
* `EMPTY` (187630): první další české nové druhé poslední státní dalších možné vlastní

`Poss` seems to be **lexical feature** of `ADJ`. 100% lemmas (1132) occur only with one value of `Poss`.

## Relations with Agreement in `Poss`

The 10 most frequent relations where parent and child node agree in `Poss`:
<tt>ADJ --[<a href="../dep/conj.html">conj</a>]--> ADJ</tt> (36; 77%),
<tt>DET --[<a href="../dep/conj.html">conj</a>]--> DET</tt> (26; 100%),
<tt>DET --[<a href="../dep/dep.html">dep</a>]--> DET</tt> (2; 67%).

