---
layout: base
title:  'Statistics of Typo in UD_English-GUM'
udver: '2'
---

## Treebank Statistics: UD_English-GUM: Features: `Typo`

This feature is language-specific.
It occurs with 1 different values: `Yes`.

465 tokens (0%) have a non-empty value of `Typo`.
336 types (2%) occur at least once with a non-empty value of `Typo`.
292 lemmas (2%) occur at least once with a non-empty value of `Typo`.
The feature is used with 15 part-of-speech tags: <tt><a href="en_gum-pos-NOUN.html">NOUN</a></tt> (100; 0% instances), <tt><a href="en_gum-pos-VERB.html">VERB</a></tt> (84; 0% instances), <tt><a href="en_gum-pos-PRON.html">PRON</a></tt> (56; 0% instances), <tt><a href="en_gum-pos-ADV.html">ADV</a></tt> (37; 0% instances), <tt><a href="en_gum-pos-PROPN.html">PROPN</a></tt> (34; 0% instances), <tt><a href="en_gum-pos-ADJ.html">ADJ</a></tt> (29; 0% instances), <tt><a href="en_gum-pos-AUX.html">AUX</a></tt> (26; 0% instances), <tt><a href="en_gum-pos-ADP.html">ADP</a></tt> (24; 0% instances), <tt><a href="en_gum-pos-PUNCT.html">PUNCT</a></tt> (17; 0% instances), <tt><a href="en_gum-pos-DET.html">DET</a></tt> (16; 0% instances), <tt><a href="en_gum-pos-INTJ.html">INTJ</a></tt> (15; 0% instances), <tt><a href="en_gum-pos-SCONJ.html">SCONJ</a></tt> (13; 0% instances), <tt><a href="en_gum-pos-NUM.html">NUM</a></tt> (8; 0% instances), <tt><a href="en_gum-pos-CCONJ.html">CCONJ</a></tt> (3; 0% instances), <tt><a href="en_gum-pos-PART.html">PART</a></tt> (3; 0% instances).

### `NOUN`

100 <tt><a href="en_gum-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `NOUN` and `Typo` co-occurred: <tt><a href="en_gum-feat-Number.html">Number</a></tt><tt>=Sing</tt> (78; 78%).

`NOUN` tokens may have the following values of `Typo`:

* `Yes` (100; 100% of non-empty `Typo`): <em>lotos, etc, kind, nite, per, type, dodge, fisherman, m, order</em>

`Typo` seems to be **lexical feature** of `NOUN`. 100% lemmas (82) occur only with one value of `Typo`.

### `VERB`

84 <tt><a href="en_gum-pos-VERB.html">VERB</a></tt> tokens (0% of all `VERB` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `VERB` and `Typo` co-occurred: <tt><a href="en_gum-feat-Voice.html">Voice</a></tt><tt>=EMPTY</tt> (72; 86%), <tt><a href="en_gum-feat-Mood.html">Mood</a></tt><tt>=EMPTY</tt> (48; 57%), <tt><a href="en_gum-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (48; 57%), <tt><a href="en_gum-feat-Person.html">Person</a></tt><tt>=EMPTY</tt> (48; 57%).

`VERB` tokens may have the following values of `Typo`:

* `Yes` (84; 100% of non-empty `Typo`): <em>got, dwibbling, Pre, questi, se, set, under, understand, United, Untied</em>

`Typo` seems to be **lexical feature** of `VERB`. 100% lemmas (63) occur only with one value of `Typo`.

### `PRON`

56 <tt><a href="en_gum-pos-PRON.html">PRON</a></tt> tokens (0% of all `PRON` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `PRON` and `Typo` co-occurred: <tt><a href="en_gum-feat-PronType.html">PronType</a></tt><tt>=Prs</tt> (51; 91%), <tt><a href="en_gum-feat-Poss.html">Poss</a></tt><tt>=EMPTY</tt> (50; 89%), <tt><a href="en_gum-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (45; 80%), <tt><a href="en_gum-feat-Person.html">Person</a></tt><tt>=3</tt> (36; 64%), <tt><a href="en_gum-feat-Case.html">Case</a></tt><tt>=Acc</tt> (30; 54%).

`PRON` tokens may have the following values of `Typo`:

* `Yes` (56; 100% of non-empty `Typo`): <em>em, it, you, ya, i, it's, we, She, Who, ev</em>

`Typo` seems to be **lexical feature** of `PRON`. 100% lemmas (15) occur only with one value of `Typo`.

### `ADV`

37 <tt><a href="en_gum-pos-ADV.html">ADV</a></tt> tokens (0% of all `ADV` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `ADV` and `Typo` co-occurred: <tt><a href="en_gum-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (36; 97%), <tt><a href="en_gum-feat-Degree.html">Degree</a></tt><tt>=Pos</tt> (25; 68%).

`ADV` tokens may have the following values of `Typo`:

* `Yes` (37; 100% of non-empty `Typo`): <em>aka, all, before, Non, a, abaut, ie, p, really, them</em>

`Typo` seems to be **lexical feature** of `ADV`. 100% lemmas (28) occur only with one value of `Typo`.

### `PROPN`

34 <tt><a href="en_gum-pos-PROPN.html">PROPN</a></tt> tokens (0% of all `PROPN` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `PROPN` and `Typo` co-occurred: <tt><a href="en_gum-feat-Number.html">Number</a></tt><tt>=Sing</tt> (29; 85%).

`PROPN` tokens may have the following values of `Typo`:

* `Yes` (34; 100% of non-empty `Typo`): <em>sea, skittles, #langu, American, Chatnam, Fla., Hutter, JOHN, Misalette, Oija</em>

`Typo` seems to be **lexical feature** of `PROPN`. 100% lemmas (29) occur only with one value of `Typo`.

### `ADJ`

29 <tt><a href="en_gum-pos-ADJ.html">ADJ</a></tt> tokens (0% of all `ADJ` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `ADJ` and `Typo` co-occurred: <tt><a href="en_gum-feat-Degree.html">Degree</a></tt><tt>=Pos</tt> (27; 93%).

`ADJ` tokens may have the following values of `Typo`:

* `Yes` (29; 100% of non-empty `Typo`): <em>residential, 2D, I.=, Indie, Water, beautiful, completed, crowed, digital, first</em>

`Typo` seems to be **lexical feature** of `ADJ`. 100% lemmas (27) occur only with one value of `Typo`.

### `AUX`

26 <tt><a href="en_gum-pos-AUX.html">AUX</a></tt> tokens (0% of all `AUX` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `AUX` and `Typo` co-occurred: <tt><a href="en_gum-feat-VerbForm.html">VerbForm</a></tt><tt>=Fin</tt> (23; 88%), <tt><a href="en_gum-feat-Mood.html">Mood</a></tt><tt>=Ind</tt> (18; 69%), <tt><a href="en_gum-feat-Person.html">Person</a></tt><tt>=3</tt> (17; 65%).

`AUX` tokens may have the following values of `Typo`:

* `Yes` (26; 100% of non-empty `Typo`): <em>are, is, was, be, can, will, been, get, has, have</em>

### `ADP`

24 <tt><a href="en_gum-pos-ADP.html">ADP</a></tt> tokens (0% of all `ADP` tokens) have a non-empty value of `Typo`.

`ADP` tokens may have the following values of `Typo`:

* `Yes` (24; 100% of non-empty `Typo`): <em>on, to, of, with, a, as, fro, from, in, is</em>

`Typo` seems to be **lexical feature** of `ADP`. 100% lemmas (10) occur only with one value of `Typo`.

### `PUNCT`

17 <tt><a href="en_gum-pos-PUNCT.html">PUNCT</a></tt> tokens (0% of all `PUNCT` tokens) have a non-empty value of `Typo`.

`PUNCT` tokens may have the following values of `Typo`:

* `Yes` (17; 100% of non-empty `Typo`): <em>., ", –, -, (, ;, [, |, ’</em>

### `DET`

16 <tt><a href="en_gum-pos-DET.html">DET</a></tt> tokens (0% of all `DET` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `DET` and `Typo` co-occurred: <tt><a href="en_gum-feat-PronType.html">PronType</a></tt><tt>=Art</tt> (11; 69%).

`DET` tokens may have the following values of `Typo`:

* `Yes` (16; 100% of non-empty `Typo`): <em>a, the, an, he, on, some, this, to</em>

### `INTJ`

15 <tt><a href="en_gum-pos-INTJ.html">INTJ</a></tt> tokens (1% of all `INTJ` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `INTJ` and `Typo` co-occurred: <tt><a href="en_gum-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (15; 100%).

`INTJ` tokens may have the following values of `Typo`:

* `Yes` (15; 100% of non-empty `Typo`): <em>y-, Ca-, Ro-, T-, alreet, alroot, f-, i-, n-, plo-</em>

`Typo` seems to be **lexical feature** of `INTJ`. 100% lemmas (14) occur only with one value of `Typo`.

### `SCONJ`

13 <tt><a href="en_gum-pos-SCONJ.html">SCONJ</a></tt> tokens (0% of all `SCONJ` tokens) have a non-empty value of `Typo`.

`SCONJ` tokens may have the following values of `Typo`:

* `Yes` (13; 100% of non-empty `Typo`): <em>cuz, cause, 'til, Altho, despite, that, then, whil, 'cuz, ‘cuz</em>

### `NUM`

8 <tt><a href="en_gum-pos-NUM.html">NUM</a></tt> tokens (0% of all `NUM` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `NUM` and `Typo` co-occurred: <tt><a href="en_gum-feat-NumType.html">NumType</a></tt><tt>=Card</tt> (6; 75%), <tt><a href="en_gum-feat-NumForm.html">NumForm</a></tt><tt>=Word</tt> (5; 63%).

`NUM` tokens may have the following values of `Typo`:

* `Yes` (8; 100% of non-empty `Typo`): <em>1, 19, one, 6:00, fiftyfive, five, to</em>

### `CCONJ`

3 <tt><a href="en_gum-pos-CCONJ.html">CCONJ</a></tt> tokens (0% of all `CCONJ` tokens) have a non-empty value of `Typo`.

`CCONJ` tokens may have the following values of `Typo`:

* `Yes` (3; 100% of non-empty `Typo`): <em>and, n</em>

### `PART`

3 <tt><a href="en_gum-pos-PART.html">PART</a></tt> tokens (0% of all `PART` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `PART` and `Typo` co-occurred: <tt><a href="en_gum-feat-Polarity.html">Polarity</a></tt><tt>=EMPTY</tt> (2; 67%).

`PART` tokens may have the following values of `Typo`:

* `Yes` (3; 100% of non-empty `Typo`): <em>do, the, not</em>

## Relations with Agreement in `Typo`

The 10 most frequent relations where parent and child node agree in `Typo`:
<tt>PROPN --[<tt><a href="en_gum-dep-conj.html">conj</a></tt>]--> PROPN</tt> (2; 67%),
<tt>ADJ --[<tt><a href="en_gum-dep-compound.html">compound</a></tt>]--> PROPN</tt> (1; 100%),
<tt>ADV --[<tt><a href="en_gum-dep-parataxis.html">parataxis</a></tt>]--> ADV</tt> (1; 100%),
<tt>NOUN --[<tt><a href="en_gum-dep-dep.html">dep</a></tt>]--> PRON</tt> (1; 100%),
<tt>PROPN --[<tt><a href="en_gum-dep-mark.html">mark</a></tt>]--> SCONJ</tt> (1; 100%).

