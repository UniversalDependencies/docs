---
layout: base
title:  'Statistics of NumType in UD_Persian-Seraji'
udver: '2'
---

## Treebank Statistics: UD_Persian-Seraji: Features: `NumType`

This feature is universal.
It occurs with 2 different values: `Card`, `Ord`.

3488 tokens (2%) have a non-empty value of `NumType`.
404 types (3%) occur at least once with a non-empty value of `NumType`.
399 lemmas (4%) occur at least once with a non-empty value of `NumType`.
The feature is used with 2 part-of-speech tags: <tt><a href="fa_seraji-pos-NUM.html">NUM</a></tt> (3409; 2% instances), <tt><a href="fa_seraji-pos-ADJ.html">ADJ</a></tt> (79; 0% instances).

### `NUM`

3409 <tt><a href="fa_seraji-pos-NUM.html">NUM</a></tt> tokens (100% of all `NUM` tokens) have a non-empty value of `NumType`.

`NUM` tokens may have the following values of `NumType`:

* `Card` (3409; 100% of non-empty `NumType`): یک، دو، یکی، هزار، سه، میلیون، ۲، چهار، ۵، ۳

`NumType` seems to be **lexical feature** of `NUM`. 100% lemmas (393) occur only with one value of `NumType`.

### `ADJ`

79 <tt><a href="fa_seraji-pos-ADJ.html">ADJ</a></tt> tokens (1% of all `ADJ` tokens) have a non-empty value of `NumType`.

The most frequent other feature values with which `ADJ` and `NumType` co-occurred: <tt><a href="fa_seraji-feat-Degree.html">Degree</a></tt><tt>=Pos</tt> (78; 99%).

`ADJ` tokens may have the following values of `NumType`:

* `Ord` (79; 100% of non-empty `NumType`): دوم، هفتم، اول، سوم، شانزدهم، هشتم، پنجمین، چهاردهم
* `EMPTY` (13595): اسلامی، دیگر، سیاسی، گذشته، فرهنگی، جدید، چند، بیشتر، ملی، پیدا

## Relations with Agreement in `NumType`

The 10 most frequent relations where parent and child node agree in `NumType`:
<tt>NUM --[<tt><a href="fa_seraji-dep-nummod.html">nummod</a></tt>]--> NUM</tt> (225; 100%),
<tt>NUM --[<tt><a href="fa_seraji-dep-conj.html">conj</a></tt>]--> NUM</tt> (169; 100%),
<tt>NUM --[<tt><a href="fa_seraji-dep-compound.html">compound</a></tt>]--> NUM</tt> (5; 100%),
<tt>NUM --[<tt><a href="fa_seraji-dep-fixed.html">fixed</a></tt>]--> NUM</tt> (4; 100%),
<tt>NUM --[<tt><a href="fa_seraji-dep-nmod.html">nmod</a></tt>]--> NUM</tt> (4; 100%).

