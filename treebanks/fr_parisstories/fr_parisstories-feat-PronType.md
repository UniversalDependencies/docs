---
layout: base
title:  'Statistics of PronType in UD_French-ParisStories'
udver: '2'
---

## Treebank Statistics: UD_French-ParisStories: Features: `PronType`

This feature is universal.
It occurs with 7 different values: `Art`, `Dem`, `Ind`, `Int`, `Neg`, `Prs`, `Rel`.

9297 tokens (22%) have a non-empty value of `PronType`.
99 types (3%) occur at least once with a non-empty value of `PronType`.
57 lemmas (2%) occur at least once with a non-empty value of `PronType`.
The feature is used with 4 part-of-speech tags: <tt><a href="fr_parisstories-pos-PRON.html">PRON</a></tt> (5887; 14% instances), <tt><a href="fr_parisstories-pos-DET.html">DET</a></tt> (3364; 8% instances), <tt><a href="fr_parisstories-pos-ADV.html">ADV</a></tt> (36; 0% instances), <tt><a href="fr_parisstories-pos-ADJ.html">ADJ</a></tt> (10; 0% instances).

### `PRON`

5887 <tt><a href="fr_parisstories-pos-PRON.html">PRON</a></tt> tokens (91% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="fr_parisstories-feat-Number.html">Number</a></tt><tt>=Sing</tt> (4805; 82%), <tt><a href="fr_parisstories-feat-Person.html">Person</a></tt><tt>=3</tt> (3658; 62%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (1074; 18% of non-empty `PronType`): <em>c', ça, ce, -ce, celui, ceux, cela, celui-là</em>
* `Ind` (868; 15% of non-empty `PronType`): <em>on, un, tous, tout, une, quelqu'un, quoi, chacun, autre, autrui</em>
* `Int` (18; 0% of non-empty `PronType`): <em>quoi, qu'</em>
* `Neg` (22; 0% of non-empty `PronType`): <em>rien, personne</em>
* `Prs` (3632; 62% of non-empty `PronType`): <em>je, il, j', elle, tu, y, me, moi, ils, nous</em>
* `Rel` (273; 5% of non-empty `PronType`): <em>qui, où, que, qu', dont, quoi, auxquelles, lequel, lesquelles</em>
* `EMPTY` (551): <em>qui, m', lui, y, il, s', que, se, où, nous</em>

<table>
  <tr><th>Paradigm <i>quoi</i></th><th><tt>Int</tt></th><th><tt>Rel</tt></th><th><tt>Ind</tt></th></tr>
  <tr><td><tt>_</tt></td><td><em>quoi</em></td><td><em>quoi</em></td><td></td></tr>
  <tr><td><tt><tt><a href="fr_parisstories-feat-Person.html">Person</a></tt><tt>=3</tt></tt></td><td><em>quoi</em></td><td></td><td><em>quoi</em></td></tr>
</table>

`PronType` seems to be **lexical feature** of `PRON`. 95% lemmas (36) occur only with one value of `PronType`.

### `DET`

3364 <tt><a href="fr_parisstories-pos-DET.html">DET</a></tt> tokens (97% of all `DET` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `DET` and `PronType` co-occurred: <tt><a href="fr_parisstories-feat-Number-psor.html">Number[psor]</a></tt><tt>=EMPTY</tt> (2957; 88%), <tt><a href="fr_parisstories-feat-Person-psor.html">Person[psor]</a></tt><tt>=EMPTY</tt> (2957; 88%), <tt><a href="fr_parisstories-feat-Number.html">Number</a></tt><tt>=Sing</tt> (2654; 79%), <tt><a href="fr_parisstories-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (1897; 56%).

`DET` tokens may have the following values of `PronType`:

* `Art` (2780; 83% of non-empty `PronType`): <em>le, la, un, les, une, l', des, du, d', de</em>
* `Dem` (98; 3% of non-empty `PronType`): <em>cette, ce, ces, cet</em>
* `Ind` (55; 2% of non-empty `PronType`): <em>quelque, chaque, quelques, plusieurs, quelqu', certaines, tout, toute, différents, tous</em>
* `Int` (4; 0% of non-empty `PronType`): <em>quelle</em>
* `Neg` (5; 0% of non-empty `PronType`): <em>aucune, aucun</em>
* `Prs` (422; 13% of non-empty `PronType`): <em>mon, ma, mes, ses, nos, sa, notre, son, leur, leurs</em>
* `EMPTY` (112): <em>le, son, mon, les, des, ta, de, quelqu', quelque, quelques</em>

`PronType` seems to be **lexical feature** of `DET`. 100% lemmas (24) occur only with one value of `PronType`.

### `ADV`

36 <tt><a href="fr_parisstories-pos-ADV.html">ADV</a></tt> tokens (1% of all `ADV` tokens) have a non-empty value of `PronType`.

`ADV` tokens may have the following values of `PronType`:

* `Neg` (26; 72% of non-empty `PronType`): <em>jamais, nul, rien</em>
* `Rel` (10; 28% of non-empty `PronType`): <em>où</em>
* `EMPTY` (3504): <em>pas, donc, parce, enfin, plus, vraiment, là, très, tout, même</em>

### `ADJ`

10 <tt><a href="fr_parisstories-pos-ADJ.html">ADJ</a></tt> tokens (1% of all `ADJ` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `ADJ` and `PronType` co-occurred: <tt><a href="fr_parisstories-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (9; 90%), <tt><a href="fr_parisstories-feat-Number.html">Number</a></tt><tt>=Sing</tt> (9; 90%).

`ADJ` tokens may have the following values of `PronType`:

* `Ind` (10; 100% of non-empty `PronType`): <em>tout, tous, toute</em>
* `EMPTY` (1203): <em>tout, petit, tous, petite, première, même, vrai, toute, autre, bonne</em>

## Relations with Agreement in `PronType`

The 10 most frequent relations where parent and child node agree in `PronType`:
<tt>PRON --[<tt><a href="fr_parisstories-dep-reparandum.html">reparandum</a></tt>]--> PRON</tt> (113; 91%),
<tt>DET --[<tt><a href="fr_parisstories-dep-reparandum.html">reparandum</a></tt>]--> DET</tt> (98; 99%),
<tt>PRON --[<tt><a href="fr_parisstories-dep-fixed.html">fixed</a></tt>]--> PRON</tt> (8; 67%),
<tt>PRON --[<tt><a href="fr_parisstories-dep-dep.html">dep</a></tt>]--> PRON</tt> (5; 83%),
<tt>PRON --[<tt><a href="fr_parisstories-dep-conj.html">conj</a></tt>]--> PRON</tt> (4; 67%),
<tt>DET --[<tt><a href="fr_parisstories-dep-advmod.html">advmod</a></tt>]--> DET</tt> (1; 100%).

