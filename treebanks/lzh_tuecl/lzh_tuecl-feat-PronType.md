---
layout: base
title:  'Statistics of PronType in UD_Classical_Chinese-TueCL'
udver: '2'
---

## Treebank Statistics: UD_Classical_Chinese-TueCL: Features: `PronType`

This feature is universal.
It occurs with 3 different values: `Dem`, `Int`, `Prs`.

43 tokens (7%) have a non-empty value of `PronType`.
11 types (4%) occur at least once with a non-empty value of `PronType`.
11 lemmas (5%) occur at least once with a non-empty value of `PronType`.
The feature is used with 1 part-of-speech tags: <tt><a href="lzh_tuecl-pos-PRON.html">PRON</a></tt> (43; 7% instances).

### `PRON`

43 <tt><a href="lzh_tuecl-pos-PRON.html">PRON</a></tt> tokens (91% of all `PRON` tokens) have a non-empty value of `PronType`.

The most frequent other feature values with which `PRON` and `PronType` co-occurred: <tt><a href="lzh_tuecl-feat-Person.html">Person</a></tt><tt>=3</tt> (22; 51%).

`PRON` tokens may have the following values of `PronType`:

* `Dem` (15; 35% of non-empty `PronType`): 彼、 此、 是、 斯、 焉
* `Int` (2; 5% of non-empty `PronType`): 奚
* `Prs` (26; 60% of non-empty `PronType`): 其、 之、 我、 己、 自
* `EMPTY` (4): 其、 夫、 惡

`PronType` seems to be **lexical feature** of `PRON`. 100% lemmas (11) occur only with one value of `PronType`.

