---
layout: base
title:  'Statistics of PartType in UD_Hausa-NorthernAutogramm'
udver: '2'
---

## Treebank Statistics: UD_Hausa-NorthernAutogramm: Features: `PartType`

This feature is language-specific.
It occurs with 1 different values: `Int`.

8 tokens (0%) have a non-empty value of `PartType`.
4 types (0%) occur at least once with a non-empty value of `PartType`.
4 lemmas (1%) occur at least once with a non-empty value of `PartType`.
The feature is used with 2 part-of-speech tags: <tt><a href="ha_northernautogramm-pos-PART.html">PART</a></tt> (7; 0% instances), <tt><a href="ha_northernautogramm-pos-CCONJ.html">CCONJ</a></tt> (1; 0% instances).

### `PART`

7 <tt><a href="ha_northernautogramm-pos-PART.html">PART</a></tt> tokens (4% of all `PART` tokens) have a non-empty value of `PartType`.

The most frequent other feature values with which `PART` and `PartType` co-occurred: <tt><a href="ha_northernautogramm-feat-Aspect.html">Aspect</a></tt><tt>=EMPTY</tt> (7; 100%), <tt><a href="ha_northernautogramm-feat-Polarity.html">Polarity</a></tt><tt>=Neg</tt> (5; 71%).

`PART` tokens may have the following values of `PartType`:

* `Int` (7; 100% of non-empty `PartType`): <em>ba, hwa, shìn</em>

### `CCONJ`

1 <tt><a href="ha_northernautogramm-pos-CCONJ.html">CCONJ</a></tt> tokens (3% of all `CCONJ` tokens) have a non-empty value of `PartType`.

`CCONJ` tokens may have the following values of `PartType`:

* `Int` (1; 100% of non-empty `PartType`): <em>koː</em>

