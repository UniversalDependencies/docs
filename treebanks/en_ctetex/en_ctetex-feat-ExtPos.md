---
layout: base
title:  'Statistics of ExtPos in UD_English-CTeTex'
udver: '2'
---

## Treebank Statistics: UD_English-CTeTex: Features: `ExtPos`

This feature is language-specific.
It occurs with 1 different values: `SCONJ`.

1 tokens (0%) have a non-empty value of `ExtPos`.
1 types (0%) occur at least once with a non-empty value of `ExtPos`.
1 lemmas (0%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 1 part-of-speech tags: <tt><a href="en_ctetex-pos-ADJ.html">ADJ</a></tt> (1; 0% instances).

### `ADJ`

1 <tt><a href="en_ctetex-pos-ADJ.html">ADJ</a></tt> tokens (0% of all `ADJ` tokens) have a non-empty value of `ExtPos`.

`ADJ` tokens may have the following values of `ExtPos`:

* `SCONJ` (1; 100% of non-empty `ExtPos`): <em>such</em>

