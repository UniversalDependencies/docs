---
layout: base
title:  'Statistics of ExtPos in UD_French-FQB'
udver: '2'
---

## Treebank Statistics: UD_French-FQB: Features: `ExtPos`

This feature is language-specific.
It occurs with 6 different values: `ADP`, `ADV`, `CCONJ`, `DET`, `NOUN`, `PRON`.

46 tokens (0%) have a non-empty value of `ExtPos`.
21 types (0%) occur at least once with a non-empty value of `ExtPos`.
20 lemmas (1%) occur at least once with a non-empty value of `ExtPos`.
The feature is used with 7 part-of-speech tags: <tt><a href="fr_fqb-pos-ADP.html">ADP</a></tt> (17; 0% instances), <tt><a href="fr_fqb-pos-DET.html">DET</a></tt> (16; 0% instances), <tt><a href="fr_fqb-pos-ADV.html">ADV</a></tt> (8; 0% instances), <tt><a href="fr_fqb-pos-PRON.html">PRON</a></tt> (2; 0% instances), <tt><a href="fr_fqb-pos-NOUN.html">NOUN</a></tt> (1; 0% instances), <tt><a href="fr_fqb-pos-NUM.html">NUM</a></tt> (1; 0% instances), <tt><a href="fr_fqb-pos-SYM.html">SYM</a></tt> (1; 0% instances).

### `ADP`

17 <tt><a href="fr_fqb-pos-ADP.html">ADP</a></tt> tokens (1% of all `ADP` tokens) have a non-empty value of `ExtPos`.

`ADP` tokens may have the following values of `ExtPos`:

* `ADP` (4; 24% of non-empty `ExtPos`): <em>en, d', à</em>
* `ADV` (7; 41% of non-empty `ExtPos`): <em>à, d', pour</em>
* `DET` (6; 35% of non-empty `ExtPos`): <em>de</em>

<table>
  <tr><th>Paradigm <i>de</i></th><th><tt>ADP</tt></th><th><tt>ADV</tt></th><th><tt>DET</tt></th></tr>
  <tr><td><tt></tt></td><td><em>d'</em></td><td><em>d'</em></td><td><em>de</em></td></tr>
</table>

### `DET`

16 <tt><a href="fr_fqb-pos-DET.html">DET</a></tt> tokens (0% of all `DET` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `DET` and `ExtPos` co-occurred: <tt><a href="fr_fqb-feat-Gender.html">Gender</a></tt><tt>=Masc</tt> (15; 94%), <tt><a href="fr_fqb-feat-Number.html">Number</a></tt><tt>=Sing</tt> (15; 94%), <tt><a href="fr_fqb-feat-PronType.html">PronType</a></tt><tt>=Art</tt> (15; 94%), <tt><a href="fr_fqb-feat-Definite.html">Definite</a></tt><tt>=Def</tt> (14; 88%).

`DET` tokens may have the following values of `ExtPos`:

* `PRON` (16; 100% of non-empty `ExtPos`): <em>le, quelqu', un</em>

### `ADV`

8 <tt><a href="fr_fqb-pos-ADV.html">ADV</a></tt> tokens (1% of all `ADV` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `ADV` and `ExtPos` co-occurred: <tt><a href="fr_fqb-feat-PronType.html">PronType</a></tt><tt>=EMPTY</tt> (8; 100%).

`ADV` tokens may have the following values of `ExtPos`:

* `ADP` (4; 50% of non-empty `ExtPos`): <em>auprès, autour, lord, près</em>
* `ADV` (1; 13% of non-empty `ExtPos`): <em>dès</em>
* `NOUN` (1; 13% of non-empty `ExtPos`): <em>trop</em>
* `PRON` (2; 25% of non-empty `ExtPos`): <em>moins, plus</em>

### `PRON`

2 <tt><a href="fr_fqb-pos-PRON.html">PRON</a></tt> tokens (0% of all `PRON` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `PRON` and `ExtPos` co-occurred: <tt><a href="fr_fqb-feat-Gender.html">Gender</a></tt><tt>=EMPTY</tt> (2; 100%), <tt><a href="fr_fqb-feat-Number.html">Number</a></tt><tt>=EMPTY</tt> (2; 100%), <tt><a href="fr_fqb-feat-PronType.html">PronType</a></tt><tt>=Prs</tt> (2; 100%).

`PRON` tokens may have the following values of `ExtPos`:

* `ADP` (1; 50% of non-empty `ExtPos`): <em>Il</em>
* `CCONJ` (1; 50% of non-empty `ExtPos`): <em>y</em>

### `NOUN`

1 <tt><a href="fr_fqb-pos-NOUN.html">NOUN</a></tt> tokens (0% of all `NOUN` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `NOUN` and `ExtPos` co-occurred: <tt><a href="fr_fqb-feat-Gender.html">Gender</a></tt><tt>=Fem</tt> (1; 100%), <tt><a href="fr_fqb-feat-Number.html">Number</a></tt><tt>=Sing</tt> (1; 100%).

`NOUN` tokens may have the following values of `ExtPos`:

* `ADP` (1; 100% of non-empty `ExtPos`): <em>suite</em>

### `NUM`

1 <tt><a href="fr_fqb-pos-NUM.html">NUM</a></tt> tokens (1% of all `NUM` tokens) have a non-empty value of `ExtPos`.

The most frequent other feature values with which `NUM` and `ExtPos` co-occurred: <tt><a href="fr_fqb-feat-NumType.html">NumType</a></tt><tt>=Card</tt> (1; 100%).

`NUM` tokens may have the following values of `ExtPos`:

* `PRON` (1; 100% of non-empty `ExtPos`): <em>2000</em>

### `SYM`

1 <tt><a href="fr_fqb-pos-SYM.html">SYM</a></tt> tokens (100% of all `SYM` tokens) have a non-empty value of `ExtPos`.

`SYM` tokens may have the following values of `ExtPos`:

* `CCONJ` (1; 100% of non-empty `ExtPos`): <em>/</em>

