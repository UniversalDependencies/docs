---
layout: base
title:  'Statistics of Typo in UD_Egyptian-UJaen'
udver: '2'
---

## Treebank Statistics: UD_Egyptian-UJaen: Features: `Typo`

This feature is language-specific.
It occurs with 1 different values: `Yes`.

56 tokens (0%) have a non-empty value of `Typo`.
18 types (1%) occur at least once with a non-empty value of `Typo`.
16 lemmas (1%) occur at least once with a non-empty value of `Typo`.
The feature is used with 1 part-of-speech tags: <tt><a href="egy_ujaen-pos-X.html">X</a></tt> (56; 0% instances).

### `X`

56 <tt><a href="egy_ujaen-pos-X.html">X</a></tt> tokens (58% of all `X` tokens) have a non-empty value of `Typo`.

The most frequent other feature values with which `X` and `Typo` co-occurred: <tt><a href="egy_ujaen-feat-Foreign.html">Foreign</a></tt><tt>=EMPTY</tt> (56; 100%).

`X` tokens may have the following values of `Typo`:

* `Yes` (56; 100% of non-empty `Typo`): <em>[...], {nb}, {n}, {k}, {r}, {⸗ꞽ}, {t}, {tꜣ}, {č̣}, {ḥr}</em>

`Typo` seems to be **lexical feature** of `X`. 100% lemmas (16) occur only with one value of `Typo`.

## Relations with Agreement in `Typo`

The 10 most frequent relations where parent and child node agree in `Typo`:
<tt>X --[<tt><a href="egy_ujaen-dep-dep.html">dep</a></tt>]--> X</tt> (2; 100%).

